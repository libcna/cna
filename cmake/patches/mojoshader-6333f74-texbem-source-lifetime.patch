diff --git a/mojoshader_internal.h b/mojoshader_internal.h
--- a/mojoshader_internal.h
+++ b/mojoshader_internal.h
@@ -422,5 +422,7 @@ void buffer_patch(Buffer *buffer, const size_t start,
 #define OPCODE_BREAKC 45
 #define OPCODE_TEXLD 66
+#define OPCODE_TEXBEM 67
+#define OPCODE_TEXBEML 68
 #define OPCODE_TEXLDD 93
 #define OPCODE_SETP 94
 #define OPCODE_TEXLDL 95
diff --git a/profiles/mojoshader_profile.h b/profiles/mojoshader_profile.h
--- a/profiles/mojoshader_profile.h
+++ b/profiles/mojoshader_profile.h
@@ -154,6 +154,7 @@ typedef struct Context
     int previous_coissue;
     int phase_count;
     int bem_count;
+    uint32 texbem_read_registers;
     int coissue;
     int loops;
     int reps;
diff --git a/mojoshader.c b/mojoshader.c
--- a/mojoshader.c
+++ b/mojoshader.c
@@ -1450,6 +1450,17 @@ static int parse_source_token(Context *ctx, SourceArgInfo *info)
     //   instructions: m3x2 - ps, m3x3 - ps, m3x4 - ps, m4x3 - ps, and
     //   m4x4 - ps."
 
+    if (!isfail(ctx) && shader_is_pixel(ctx) &&
+        !shader_version_atleast(ctx, 2, 0) &&
+        (info->regtype == REG_TYPE_TEXTURE) &&
+        (info->regnum >= 0) && (info->regnum < 32) &&
+        ((ctx->texbem_read_registers & (1u << info->regnum)) != 0) &&
+        (ctx->current_opcode != OPCODE_TEXBEM) &&
+        (ctx->current_opcode != OPCODE_TEXBEML))
+    {
+        failf(ctx, "Texture register t%d was consumed by TEXBEM/TEXBEML", info->regnum);
+    } // if
+
     if (!isfail(ctx))
     {
         RegisterList *reg;
@@ -2800,9 +2811,7 @@ static void state_texbem(Context *ctx, const char *opcode)
     } // if
 
-    // !!! FIXME: MSDN:
-    // !!! FIXME: Register data that has been read by a texbem
-    // !!! FIXME:  or texbeml instruction cannot be read later,
-    // !!! FIXME:  except by another texbem or texbeml.
+    if ((ctx->source_args[0].regnum >= 0) && (ctx->source_args[0].regnum < 32))
+        ctx->texbem_read_registers |= 1u << ctx->source_args[0].regnum;
 
     state_texops(ctx, opcode, 2, 1);
 } // state_texbem
