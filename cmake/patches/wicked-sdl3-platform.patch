diff --git a/CMakeLists.txt b/CMakeLists.txt
index a2c88d0..616d174 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -29,6 +29,7 @@ option(WICKED_TESTS "Build WickedEngine tests" ON)
 option(WICKED_IMGUI_EXAMPLE "Build WickedEngine imgui example" ON)
 option(WICKED_ENABLE_IPO "Enable IPO/LTO in non-debug builds" NO)
 option(WICKED_EMBED_SHADERS "Embed shaders into the library" NO)
+option(WICKED_USE_SDL3 "Use SDL3 instead of SDL2 for the Unix window/surface platform layer" OFF)
 option(WICKED_ENABLE_RTTI "Enable RTTI" NO)
 if(UNIX)
     option(WICKED_ENABLE_ASAN "Enable AddressSanitizer in debug builds" OFF)
@@ -114,8 +115,13 @@ if (WIN32)
     add_compile_definitions(WIN32=1)
     add_compile_definitions(_HAS_EXCEPTIONS=0)
 elseif (UNIX)
-    set(PLATFORM "SDL2")
-    add_compile_definitions(SDL2=1)
+    if (WICKED_USE_SDL3)
+        set(PLATFORM "SDL3")
+        add_compile_definitions(SDL3=1)
+    else()
+        set(PLATFORM "SDL2")
+        add_compile_definitions(SDL2=1)
+    endif()
     add_compile_definitions(_GLIBCXX_USE_CXX11_ABI=1)
 endif()
 
diff --git a/WickedEngine/CMakeLists.txt b/WickedEngine/CMakeLists.txt
index 7305dd0..af55d7f 100644
--- a/WickedEngine/CMakeLists.txt
+++ b/WickedEngine/CMakeLists.txt
@@ -39,7 +39,9 @@ if (WIN32)
         )
     endif()
 else ()
-    find_package(SDL2 REQUIRED)
+    if (NOT WICKED_USE_SDL3)
+        find_package(SDL2 REQUIRED)
+    endif()
     find_package(OpenImageDenoise "${MIN_OpenImageDenoise_VERSION}" QUIET)
     find_package(Threads REQUIRED)
     if(NOT ${OpenImageDenoise_FOUND})
@@ -48,7 +50,7 @@ else ()
         message(STATUS "OpenImageDenoise ${OpenImageDenoise_VERSION} Found.")
     endif()
 
-    if(NOT TARGET SDL2::SDL2)
+    if(NOT WICKED_USE_SDL3 AND NOT TARGET SDL2::SDL2)
         # using old SDL2 cmake, lets create a SDL2 target ourselves
         find_library(SDL2_LIBRARY_FILE_LOCATION SDL2 REQUIRED)
 
@@ -63,8 +65,10 @@ else ()
     endif()
 
     # minimum sdl version is 2.0.14 for controller LED support
-    if((${SDL_VERSION_MAJOR} GREATER_EQUAL 2) AND (${SDL2_VERSION_MINOR} GREATER_EQUAL 0) AND (${SDL2_VERSION_PATCH} GREATER_EQUAL 14))
-        add_compile_definitions(SDL2_FEATURE_CONTROLLER_LED=1)
+    if(NOT WICKED_USE_SDL3)
+        if((${SDL_VERSION_MAJOR} GREATER_EQUAL 2) AND (${SDL2_VERSION_MINOR} GREATER_EQUAL 0) AND (${SDL2_VERSION_PATCH} GREATER_EQUAL 14))
+            add_compile_definitions(SDL2_FEATURE_CONTROLLER_LED=1)
+        endif()
     endif()
 endif()
 
@@ -242,11 +246,19 @@ else ()
     # in same cases. Use `robin_hood::unordered_flat_map` instead
     target_compile_definitions(WickedEngine_common PUBLIC WI_UNORDERED_MAP_TYPE=2)
 
-    target_link_libraries(WickedEngine_common PUBLIC
-        Threads::Threads
-        SDL2::SDL2
-        $<$<BOOL:${OpenImageDenoise_FOUND}>:OpenImageDenoise> # links OpenImageDenoise only if it's found
-    )
+    if (WICKED_USE_SDL3)
+        target_link_libraries(WickedEngine_common PUBLIC
+            Threads::Threads
+            SDL3::SDL3
+            $<$<BOOL:${OpenImageDenoise_FOUND}>:OpenImageDenoise> # links OpenImageDenoise only if it's found
+        )
+    else()
+        target_link_libraries(WickedEngine_common PUBLIC
+            Threads::Threads
+            SDL2::SDL2
+            $<$<BOOL:${OpenImageDenoise_FOUND}>:OpenImageDenoise> # links OpenImageDenoise only if it's found
+        )
+    endif()
 
     set_target_properties(FAudio PROPERTIES
         POSITION_INDEPENDENT_CODE ${WICKED_PIC}
@@ -263,6 +275,8 @@ target_link_libraries(WickedEngine_common PUBLIC ${WICKEDENGINE_STATIC_LIBRARIES
 
 if (PLATFORM MATCHES "SDL2")
     target_compile_definitions(WickedEngine_common PUBLIC SDL2=1)
+elseif (PLATFORM MATCHES "SDL3")
+    target_compile_definitions(WickedEngine_common PUBLIC SDL3=1)
 endif()
 
 add_library(dxcompiler SHARED IMPORTED)
diff --git a/WickedEngine/Utility/CMakeLists.txt b/WickedEngine/Utility/CMakeLists.txt
index 6171397..e421236 100644
--- a/WickedEngine/Utility/CMakeLists.txt
+++ b/WickedEngine/Utility/CMakeLists.txt
@@ -3,6 +3,12 @@ cmake_minimum_required(VERSION 3.19)
 include(GNUInstallDirs)
 if (PLATFORM MATCHES "SDL2")
 	add_subdirectory(FAudio)
+elseif (PLATFORM MATCHES "SDL3")
+	set(BUILD_SDL3 ON CACHE BOOL "" FORCE)
+	add_subdirectory(FAudio)
+	# FAudio selects its SDL3 headers on FAUDIO_SDL3_PLATFORM, which this trimmed copy of its
+	# CMakeLists does not define even when BUILD_SDL3 is on.
+	target_compile_definitions(FAudio PUBLIC FAUDIO_SDL3_PLATFORM)
 endif()
 
 file(GLOB_RECURSE HEADER_FILES CONFIGURE_DEPENDS
diff --git a/WickedEngine/wiGraphicsDevice_Vulkan.cpp b/WickedEngine/wiGraphicsDevice_Vulkan.cpp
index f29a67f..a22bd74 100644
--- a/WickedEngine/wiGraphicsDevice_Vulkan.cpp
+++ b/WickedEngine/wiGraphicsDevice_Vulkan.cpp
@@ -2057,6 +2057,19 @@ using namespace vulkan_internal;
 				instanceExtensions.push_back(x);
 			}
 		}
+#elif defined(SDL3)
+		{
+			uint32_t extensionCount = 0;
+			char const* const* extensionNames_sdl = SDL_Vulkan_GetInstanceExtensions(&extensionCount);
+			if (extensionNames_sdl != nullptr)
+			{
+				instanceExtensions.reserve(instanceExtensions.size() + (size_t)extensionCount);
+				for (uint32_t i = 0; i < extensionCount; ++i)
+				{
+					instanceExtensions.push_back(extensionNames_sdl[i]);
+				}
+			}
+		}
 #endif // _WIN32
 
 		if (validationMode != ValidationMode::Disabled)
@@ -3454,6 +3467,12 @@ using namespace vulkan_internal;
 				wilog_messagebox("Error creating a vulkan surface with SDL_Vulkan_CreateSurface!");
 				wi::platform::Exit();
 			}
+#elif defined(SDL3)
+			if (!SDL_Vulkan_CreateSurface(window, instance, nullptr, &internal_state->surface))
+			{
+				wilog_messagebox("Error creating a vulkan surface with SDL_Vulkan_CreateSurface!");
+				wi::platform::Exit();
+			}
 #elif defined(__APPLE__)
 			wilog("Vulkan surface on Apple platform is not yet implemented");
 #else
diff --git a/WickedEngine/wiInput.cpp b/WickedEngine/wiInput.cpp
index 642264a..6d42f94 100644
--- a/WickedEngine/wiInput.cpp
+++ b/WickedEngine/wiInput.cpp
@@ -139,6 +139,14 @@ namespace wi::input
 	static void* cursor_table[arraysize(cursor_table_original)] = {};
 #endif // __APPLE__
 
+#if !defined(_WIN32) && !defined(SDL2) && !defined(__APPLE__)
+	// No cursor renderer in this platform build (e.g. the SDL3 platform layer, where the host
+	// application owns input and cursor state). The tables still exist so the shared code below
+	// compiles and stays inert.
+	static void* cursor_table_original[CURSOR_COUNT] = {};
+	static void* cursor_table[arraysize(cursor_table_original)] = {};
+#endif
+
 	const KeyboardState& GetKeyboardState() { return keyboard; }
 	const MouseState& GetMouseState() { return mouse; }
 
diff --git a/WickedEngine/wiPlatform.h b/WickedEngine/wiPlatform.h
index 4fb99fa..2a61649 100644
--- a/WickedEngine/wiPlatform.h
+++ b/WickedEngine/wiPlatform.h
@@ -41,6 +41,11 @@
 #include "sdl2.h"
 #endif
 
+#ifdef SDL3
+#include <SDL3/SDL.h>
+#include <SDL3/SDL_vulkan.h>
+#endif
+
 #if defined(PLATFORM_LINUX) || defined(PLATFORM_APPLE)
 #include <dlfcn.h>
 #define wiLoadLibrary(name) dlopen(name, RTLD_LAZY)
@@ -57,6 +62,9 @@ namespace wi::platform
 #elif defined(SDL2)
 	using window_type = SDL_Window*;
 	using error_type = int;
+#elif defined(SDL3)
+	using window_type = SDL_Window*;
+	using error_type = int;
 #elif defined(__APPLE__)
 	using window_type = void*;
 	using error_type = int;
@@ -73,6 +81,10 @@ namespace wi::platform
 		SDL_Event quit_event;
 		quit_event.type = SDL_QUIT;
 		SDL_PushEvent(&quit_event);
+#elif defined(SDL3)
+		SDL_Event quit_event = {};
+		quit_event.type = SDL_EVENT_QUIT;
+		SDL_PushEvent(&quit_event);
 #elif defined(__APPLE__)
 		std::exit(0);
 #endif
@@ -106,12 +118,22 @@ namespace wi::platform
 		dest->height = int(rect.bottom - rect.top);
 #endif // PLATFORM_WINDOWS_DESKTOP || PLATFORM_XBOX
 
-#ifdef PLATFORM_LINUX
+#if defined(PLATFORM_LINUX) && defined(SDL2)
 		int window_width, window_height;
 		SDL_GetWindowSize(window, &window_width, &window_height);
 		SDL_Vulkan_GetDrawableSize(window, &dest->width, &dest->height);
 		dest->dpi = ((float)dest->width / (float)window_width) * 96.f;
-#endif // PLATFORM_LINUX
+#endif // PLATFORM_LINUX && SDL2
+
+#if defined(PLATFORM_LINUX) && defined(SDL3)
+		int window_width = 0, window_height = 0;
+		SDL_GetWindowSize(window, &window_width, &window_height);
+		SDL_GetWindowSizeInPixels(window, &dest->width, &dest->height);
+		if (window_width > 0)
+		{
+			dest->dpi = ((float)dest->width / (float)window_width) * 96.f;
+		}
+#endif // PLATFORM_LINUX && SDL3
 		
 #ifdef PLATFORM_APPLE
 		XMUINT2 size = wi::apple::GetWindowSize(window);
@@ -156,8 +178,10 @@ namespace wi::platform
 				SWP_NOOWNERZORDER | SWP_FRAMECHANGED);
 		}
 
-#elif defined(PLATFORM_LINUX)
+#elif defined(PLATFORM_LINUX) && defined(SDL2)
 		SDL_SetWindowFullscreen(window, fullscreen ? SDL_WINDOW_FULLSCREEN_DESKTOP : 0);
+#elif defined(PLATFORM_LINUX) && defined(SDL3)
+		SDL_SetWindowFullscreen(window, fullscreen);
 #elif defined(__APPLE__)
 		wi::apple::SetWindowFullScreen(window, fullscreen);
 #endif // PLATFORM_WINDOWS_DESKTOP
@@ -167,7 +191,7 @@ namespace wi::platform
 	{
 #if defined(PLATFORM_WINDOWS_DESKTOP)
 		return (GetWindowLong(window, GWL_STYLE) & WS_OVERLAPPEDWINDOW) == 0;
-#elif defined(PLATFORM_LINUX)
+#elif defined(PLATFORM_LINUX) && defined(SDL2)
 		auto flags = SDL_GetWindowFlags(window);
 		if (flags & SDL_WINDOW_FULLSCREEN_DESKTOP)
 			return true;
@@ -175,6 +199,8 @@ namespace wi::platform
 			return true;
 		else
 			return false;
+#elif defined(PLATFORM_LINUX) && defined(SDL3)
+		return (SDL_GetWindowFlags(window) & SDL_WINDOW_FULLSCREEN) != 0;
 #elif defined(__APPLE__)
 		return wi::apple::IsWindowFullScreen(window);
 #else
