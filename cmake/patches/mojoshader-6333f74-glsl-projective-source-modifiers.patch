diff --git a/mojoshader.c b/mojoshader.c
--- a/mojoshader.c
+++ b/mojoshader.c
@@ -3647,10 +3647,12 @@ static void process_definitions(Context *ctx)
                     /* The same D3D register number denotes a vertex address
                      * register or a pixel texture register. Shader Model 1
                      * pixel texture registers start with the corresponding
                      * interpolated TEXCOORD, even though that shader model has
-                     * no DCL instruction with which to declare the input. */
+                     * no DCL instruction with which to declare the input. In
+                     * ps_1_4 they are read-only inputs instead of the mutable
+                     * ps_1_1--1_3 register, but still need the attribute. */
                     if (shader_is_pixel(ctx) &&
-                        !shader_version_atleast(ctx, 1, 4))
+                        !shader_version_atleast(ctx, 2, 0))
                     {
                         add_attribute_register(ctx, REG_TYPE_TEXTURE, regnum,
                                                MOJOSHADER_USAGE_TEXCOORD,
diff --git a/profiles/mojoshader_profile_glsl.c b/profiles/mojoshader_profile_glsl.c
--- a/profiles/mojoshader_profile_glsl.c
+++ b/profiles/mojoshader_profile_glsl.c
@@ -372,13 +372,9 @@ const char *make_GLSL_srcarg_string(Context *ctx, const size_t idx,
             break;
 
         case SRCMOD_DZ:
-            fail(ctx, "SRCMOD_DZ unsupported"); return buf; // !!! FIXME
-            postmod_str = "_dz";
             break;
 
         case SRCMOD_DW:
-            fail(ctx, "SRCMOD_DW unsupported"); return buf; // !!! FIXME
-            postmod_str = "_dw";
             break;
 
         case SRCMOD_ABSNEGATE:
@@ -478,6 +474,34 @@ const char *make_GLSL_srcarg_string(Context *ctx, const size_t idx,
         return buf;
     } // if
 
+    if ((arg->src_mod == SRCMOD_DZ) || (arg->src_mod == SRCMOD_DW))
+    {
+        char source[128];
+        snprintf(source, sizeof (source), "%s%s%s%s%s%s%s",
+                 regtype_str, rel_lbracket, rel_offset, rel_regtype_str,
+                 rel_swizzle, rel_rbracket, swiz_str);
+
+        char divisor[128];
+        snprintf(divisor, sizeof (divisor), "%s%s%s%s%s%s.%c",
+                 regtype_str, rel_lbracket, rel_offset, rel_regtype_str,
+                 rel_swizzle, rel_rbracket,
+                 (arg->src_mod == SRCMOD_DZ) ? 'z' : 'w');
+
+        const int vecsize = vecsize_from_writemask(writemask);
+        if (vecsize == 1)
+        {
+            snprintf(buf, buflen, "((%s == 0.0) ? 1.0 : (%s / %s))",
+                     divisor, source, divisor);
+        } // if
+        else
+        {
+            snprintf(buf, buflen,
+                     "((%s == 0.0) ? vec%d(1.0) : (%s / %s))",
+                     divisor, vecsize, source, divisor);
+        } // else
+        return buf;
+    } // if
+
     snprintf(buf, buflen, "%s%s%s%s%s%s%s%s%s",
              premod_str, regtype_str, rel_lbracket, rel_offset,
              rel_regtype_str, rel_swizzle, rel_rbracket, swiz_str,
@@ -1979,7 +2003,7 @@ void emit_GLSL_DEFI(Context *ctx)
 
 void emit_GLSL_TEXCRD(Context *ctx)
 {
-    char source[64];
+    char source[256];
     if (shader_version_atleast(ctx, 1, 4))
     {
         make_GLSL_srcarg_string_masked(ctx, 0, source, sizeof (source));
