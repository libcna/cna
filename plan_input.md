# Input Implementation Task Plan

> Goal: `Microsoft::Xna::Framework::Input` and `Microsoft::Xna::Framework::Input::Touch`
> fully ported from FNA — not just API surface, but **FNA-faithful runtime behavior**
> wired to SDL3, CHECKLIST-compliant, and covered by unit + integration tests.
>
> The authoritative reference is the FNA source tree at
> `/rv/data/library/github.com/FNA-XNA/FNA/src/Input/` and
> `/rv/data/library/github.com/FNA-XNA/FNA/src/FNAPlatform/SDL3_FNAPlatform.cs`.

---

## Legend

| Symbol | Meaning |
|--------|---------|
| ⬜ | Not started |
| 🔄 | In progress |
| ✅ | Done |
| ⛔ | Blocked / deferred |
| ℹ️  | Known limitation (not a bug) |
| ⚠️  | Partial / no-op / stub |

> **Task numbering:** Input tasks occupy the **700+ band**. `GRAPHICS_TASKS.md` owns 100–699.
> Phase titles here (`Phase I1…I7`) are local to this document.

---

## Current state summary

The input value-type layer is in good shape. The enums (`Buttons`, `ButtonState`, `Keys`,
`KeyState`, `GamePadType`, `GamePadDeadZone`, `GestureType`, `TouchLocationState`) match FNA
values exactly. The dead-zone / clamp / button-packing math in `GamePadThumbSticks`,
`GamePadTriggers`, `GamePadDeadZone`, and `GamePadState` is **line-for-line identical** to FNA.
The full `GestureDetector` state machine is ported. Keyboard, Mouse, and GamePad **are** driven
by real SDL3 events through `CNA::Internal::Input::SdlInputBridge` → `InputManager`.

What this plan addresses: the **behavior and wiring gaps** the API-surface audit (AUDIT.md) did
not capture — dead/stub paths, unwired SDL3 features, fidelity deviations from FNA, CHECKLIST
violations in the internal layer, and very thin test coverage.

### Per-device wiring status (entry point: `SdlInputBridge::ProcessEvent`, driven by `Game::PollEvents`)

| Device | API surface | SDL3 wiring | Behavior fidelity | Tests |
|--------|-------------|-------------|-------------------|-------|
| Keyboard | ✅ complete | ✅ key down/up → `KeyboardState` | ✅ complete keycode map + scancode mode (`FNA_KEYBOARD_USE_SCANCODES`); `GetPressedKeys` ascending; real `GetKeyFromScancodeEXT` | ✅ 21 tests | **(Phase I5 complete)** |
| Mouse | ✅ complete | ✅ motion/button/wheel → `MouseState` | ✅ `SetPosition` warps (scale-factor deviation on letterboxed windows only — task 800); relative mode real; `ClickedEXT` fires; dead `INTERNAL_*` fields removed | ✅ 26 tests | **(Phase I4 complete)** |
| GamePad | ✅ complete | ✅ hotplug + button/axis + rumble + caps + LED/gyro/accel + EXT buttons | ✅ `PacketNumber` increments on raw-state change; `GamePadCapabilities` reworked to properties; `FromButtonArray` renamed/reconciled; `GetHashCode`/`ToString` FNA-faithful | ✅ 58 tests | **(Phase I3 complete)** |
| Touch | ✅ complete | ✅ fingers → `GetState` + gesture pipeline (`INTERNAL_onTouchEvent`/`Update`/`TouchDeviceExists`) live | ✅ `SetFinger` prev-state fixed; `DisplayWidth/Height` set from backbuffer; `GetState()`'s `InputManager` fallback documented as an intentional deviation from FNA's poll model | ✅ 41 tests | **(Phase I2 complete)** |
| TextInput | ✅ surface | ✅ SDL3 wired (Start/Stop/SetRect/active) + TEXT_INPUT/EDITING dispatch + control-char synthesis | ✅ events raised; Ctrl+V suppress | ✅ 9 tests | **(Phase I1 complete)** |
| MouseCursor | ℹ️ MonoGame ext (kept — task 754) | ✅ system + custom cursors (`FromTexture2D`) | ✅ `FromTexture2D`/`Dispose`/`IDisposable` done; SPDX `MS-PL` + `NOXNA class` fixed | ✅ 7 tests | **(Phase I4 complete)** |

---

## Phase I1 — TextInputEXT: SDL3 backend wiring

> **Root cause:** every `TextInputEXT` method is a no-op stub; `SdlInputBridge` ignores
> `SDL_EVENT_TEXT_INPUT` / `SDL_EVENT_TEXT_EDITING`. Text entry and IME are entirely
> non-functional. FNA reference: `TextInputEXT.cs`, `SDL3_FNAPlatform.cs:903–953, 1162–1204`.

| # | Task | Status | Notes |
|---|------|--------|-------|
| 700 | Wire `StartTextInput()`/`StopTextInput()` to `SDL_StartTextInput(window)` / `SDL_StopTextInput(window)` using `WindowHandle` (cast `uintptr_t`→`SDL_Window*`). Replace empty bodies at `TextInputEXT.cpp:25–31` | ✅ | `ToSdlWindow()` helper casts `WindowHandle`; null-guarded (handle unset until Task 703). TU compiles clean. End-to-end needs 703 (populate handle) + 704 (event). |
| 701 | Wire `SetInputRectangle(Rectangle)` to `SDL_SetTextInputArea`. Replace empty body `TextInputEXT.cpp:33–35` | ✅ | Builds `SDL_Rect` from `rectangle.X/Y/Width/Height`, cursor offset 0 (matches FNA `SetTextInputRectangle`). Null-guarded. |
| 702 | Wire `IsTextInputActive()`→`SDL_TextInputActive`, `IsScreenKeyboardShown(IntPtr)`→`SDL_ScreenKeyboardShown`. Replace `return false` stubs `TextInputEXT.cpp:10–23` | ✅ | Both query SDL3 via `WindowHandle`; null-guarded → `false` until handle set (Task 703). |
| 703 | Populate the window handle at window creation and clear it at destruction (mirror FNA `SDL3_FNAPlatform.cs:463–465`); currently never assigned. Use `TextInputEXT::setWindowHandleProperty(handle)` (property as of Task 707) | ✅ | `GraphicsDevice.cpp`: set after `SDL_CreateWindow` (1119); clear in `destroyNativeResources()` if it points at this window. Mouse/TouchPanel handles still unpublished (separate follow-up). |
| 704 | Handle `SDL_EVENT_TEXT_INPUT` in `SdlInputBridge::ProcessEvent`: UTF-8 decode `event.text.text` → per-char `TextInputEXT::INTERNAL_OnTextInput` (FNA `1162–1184`) | ✅ | Forwards each UTF-8 byte of `event.text.text` (CNA's `char`-based callback → byte-oriented UTF-8; FNA decodes to UTF-16). `textInputSuppress` gating deferred to Task 706. Bridge-level test (`SdlInputBridgeTextInputTest`) drives synthetic events. |
| 705 | Handle `SDL_EVENT_TEXT_EDITING`: decode → `INTERNAL_OnTextEditing(text, start, length)`, including the empty/null composition path (FNA `1186–1204`) | ✅ | Passes UTF-8 `event.edit.text` + `start`/`length`; empty/null composition → empty string with 0/0 (FNA's `null` maps to empty `std::string&`). |
| 706 | Control-character synthesis on `KEY_DOWN` (incl. repeat): emit `TextInput` for Home/End/Back/Tab/Enter/Delete and Ctrl+V, with a `textInputSuppress` flag to avoid double paste. Port `TextInputBindings`/`TextInputCharacters` (FNA `FNAPlatform.cs:261–280`, `SDL3_FNAPlatform.cs:903–953`). Note: bridge currently drops key repeats entirely (`SdlInputBridge.cpp:570–573`) | ✅ | `kTextInputCharacters[7]` + `text_input_binding_index` helpers; KEY block no longer drops repeats (state set only on first press, text re-emitted on repeat); `g_textInputSuppress` gates the TEXT_INPUT case for Ctrl+V. Verified by `SdlInputBridgeTextInputTest` (8 cases: control chars, repeat re-emit, Ctrl+V paste + suppress lifecycle, plain-V passthrough, editing). |
| 707 | TextInputEXT CHECKLIST fixes: add `#include "CNA/CNAHelper.hpp"`; `NOXNA`-tag `INTERNAL_OnTextInput`/`INTERNAL_OnTextEditing` (FNA-internal, non-XNA); convert public field `WindowHandle` → `getWindowHandleProperty()`/`setWindowHandleProperty()` | ✅ | Whole class is non-XNA → tagged `NOXNA class` + every public member `NOXNA` + `@note NOXNA` (per `ShaderEffect` precedent). `WindowHandle` → property w/ private `windowHandle_`. 1757/1757 tests pass. |
| 708 | New `tests/Microsoft/Xna/Framework/Input/TextInputEXTTests.cpp`: event dispatch via `INTERNAL_OnTextInput`/`OnTextEditing`, subscriber lambda fires, `IsTextInputActive` default, `WindowHandle` round-trip | ✅ | 9 tests (`TextInputEXTTest.*`): char/editing dispatch, empty composition, no-subscriber safety, handle round-trip, `Is*` false without window, Start/Stop/SetRect no-op guards. Suite 1757→1766. |

---

## Phase I2 — Touch and gesture pipeline wiring ✅ COMPLETE (tasks 710–722)

> **Original root cause (now fixed):** SDL finger events fed only `InputManager::SetTouchState`,
> so `TouchPanel::INTERNAL_onTouchEvent` / `SetFinger` / `GestureDetector::On*` had **zero callers**,
> and `TouchPanel::Update()`'s `touchDeviceExists_` gate never opened. `SdlInputBridge.cpp`'s
> `SDL_EVENT_FINGER_*` cases now also call `TouchPanel::INTERNAL_onTouchEvent` and set
> `TouchDeviceExists`; `GraphicsDevice` publishes `DisplayWidth`/`DisplayHeight` from the
> back buffer. Gestures (Tap/DoubleTap/Hold/Drag/Flick/Pinch/PinchComplete) now fire from real
> input, verified by the `GestureDetectorTest` suite (task 720).

| # | Task | Status | Notes |
|---|------|--------|-------|
| 710 | In `SdlInputBridge.cpp:632–663`, additionally call `TouchPanel::INTERNAL_onTouchEvent(touchId, state, x, y, dx, dy)` (normalized coords + deltas) alongside `InputManager::SetTouchState`, so `GestureDetector` receives input | ✅ | `SDL_EVENT_FINGER_DOWN/MOTION/UP` now also call `TouchPanel::INTERNAL_onTouchEvent` with raw normalized `event.tfinger.x/y` (+`dx/dy` on Motion, 0/0 on Down/Up), matching FNA `SDL3_FNAPlatform.cs:969–1004`. `GestureDetector::On{Pressed,Moved,Released}` now receive real input; pixel scaling is still 0 until Task 711 sets `DisplayWidth/Height`. 1774/1774 tests pass. |
| 711 | Set `TouchPanel::DisplayWidth`/`DisplayHeight` from the active window/back-buffer size (never set today; required for `INTERNAL_onTouchEvent` pixel scaling) | ✅ | `GraphicsDevice.cpp`: `setDisplayWidthProperty`/`setDisplayHeightProperty` called from `virtualWidth_`/`virtualHeight_` in the ctor and in `Reset(PresentationParameters&, GraphicsAdapter*)` (mirrors FNA `GraphicsDevice.cs:436–437,761–762`, both back-buffer-size assignment sites). 1774/1774 tests pass. |
| 712 | Fix the `Update()` gate: `FrameworkDispatcher.cpp:62` only calls `TouchPanel::Update()` when `touchDeviceExists_` is true, but that flag is set solely by the never-called `SetFinger`. Set it on the real input path so `OnUpdate()` (Hold/Flick) runs | ✅ | `SdlInputBridge.cpp` `SDL_EVENT_FINGER_DOWN` now calls `TouchPanel::setTouchDeviceExistsProperty(true)` (mirrors FNA `SDL3_FNAPlatform.cs:972`, "Windows only notices a touch screen once it's touched"). `FrameworkDispatcher::Update()`'s gate at `.cpp:62` now opens from real finger input, so `TouchPanel::Update()` → `GestureDetector::OnUpdate()` runs. 1774/1774 tests pass. |
| 713 | Fix `TouchPanel::SetFinger` (Moved/Released branches, `.cpp:191,227–231`) to use the 5-arg `TouchLocation` ctor carrying `prevState`/`prevPosition` (match FNA `TouchPanel.cs:209–215`), so `TryGetPreviousLocation` can succeed | ✅ | Released branch: `TouchLocation(prevId, Released, prevPos, previous.State, previous.Position)`. Moved branch (finger already down): `TouchLocation(fingerId, Moved, fingerPos, previous.State, previous.Position)`. Matches FNA `TouchPanel.cs:170–177,209–215`; the newly-pressed branch stays 3-arg (FNA has no prevState there either). 1774/1774 tests pass. |
| 714 | Reconcile `TouchPanel::GetState()` (`.cpp:104–122`) with FNA: either populate `touches_` via the platform path (FNA iterates `touches[]` from `SetFinger`) or explicitly document the `InputManager` fallback deviation | ✅ | Documented the deviation in-source rather than implementing FNA's per-frame poll model (`FNAPlatform.UpdateTouchPanelState()` → `SDL_GetTouchFingers()`): CNA's `SdlInputBridge` is event-driven, not poll-driven, so `SetFinger`/`touches_` aren't fed by the real input path; `GetState()`'s `InputManager` fallback keeps real touches visible. Full poll-based parity is future work, not required for gesture wiring. 1774/1774 tests pass. |
| 715 | `TouchLocation::ToString()` → emit `"{Position:" + Position + "}"` (FNA format, currently `"[TouchLocation Id=N]"`); reconcile `GetHashCode()` to FNA's `Id + Position` formula (currently `id ^ state*31`) | ✅ | `ToString()` → `"{Position:" + position_.ToString() + "}"` (matches FNA `TouchLocation.cs:99–102`, using `Vector2::ToString()`'s existing `"{X:.. Y:..}"` format). `GetHashCode()` → `id_ + position_.GetHashCode()` (matches FNA `Id.GetHashCode() + Position.GetHashCode()`; C# `int.GetHashCode()` is the int itself). No existing tests referenced the old format. 1774/1774 tests pass. |
| 716 | `TouchPanel::ReadGesture()` → throw `System::InvalidOperationException` (already in sharp-runtime) instead of `std::logic_error` (`.cpp:128`) | ✅ | Matches FNA `TouchPanel.cs` (`throw new InvalidOperationException()`, no message). 1774/1774 tests pass. |
| 717 | `CNA::Internal::Input::GestureDetector` hygiene: change SPDX `MIT`→`MS-PL`; remove forbidden "Ported from FNA / taken from MonoGame" comments per CLAUDE.md | ✅ | `.hpp`/`.cpp` SPDX → `MS-PL` (dropped the extra `Copyright (c)` line to match sibling `TouchPanel`/`TouchLocation` header style); removed "Ported from FNA GestureDetector.cs." from the class doc comment. 1774/1774 tests pass. |
| 718 | `TouchCollection`: add XNA `GetEnumerator()` + nested `Enumerator` struct (or document the `begin/end` deviation); `NOXNA`-tag `empty()`; add settable indexer to mirror FNA `this[int] set` | ✅ | Chose the `begin/end` deviation path (matches the established codebase-wide precedent — `GameComponentCollection`, `EffectAnnotationCollection`, etc. all replace `IEnumerable<T>::GetEnumerator()` with `NOXNA`-tagged `begin()`/`end()` rather than a bespoke `Enumerator` struct, since no such pattern exists anywhere else in CNA). `empty()` → `NOXNA`-tagged. Added non-const `operator[](std::size_t)` returning `TouchLocation&` alongside the existing const overload, mirroring FNA's settable `this[int]`. 1774/1774 tests pass. |
| 719 | `TouchPanel::TouchDeviceExists` accessors are FNA-internal but public without `NOXNA` — tag `NOXNA` / reduce visibility | ✅ | Tagged `NOXNA` (kept public — genuine C++ visibility reduction would need `friend` grants for `SdlInputBridge`, `FrameworkDispatcher`, and `TouchCollection`, three unrelated call sites across the internal bridge; `NOXNA` is simpler and matches how other FNA-internal accessors, e.g. `INTERNAL_onTouchEvent`, are already marked). Doc comment notes FNA declares it `internal`. Added `#include "CNA/CNAHelper.hpp"` per CLAUDE.md. 1774/1774 tests pass. |
| 720 | New `GestureDetector` test suite: Tap, DoubleTap timing, Hold ≥1s, Horizontal/Vertical/Free drag thresholds, Flick velocity (MIN_FLICK_VELOCITY=100), Pinch + PinchComplete — driving `INTERNAL_onTouchEvent` + `Update` | ✅ | New `tests/CNA/Internal/Input/GestureDetectorTests.cpp`, 12 tests, all driven through `TouchPanel::INTERNAL_onTouchEvent`/`Update` (not `GestureDetector` directly). Since the detector's state machine lives in file-static variables with no reset hook, each test uses a fixture that drains the gesture queue and drives a neutral press/release cycle in `SetUp`/`TearDown`. Timing gestures (Hold, DoubleTap window) use real `sleep_for` with margin, since the production code has no injectable clock. 1786/1786 tests pass. |
| 721 | TouchPanel tests: `ReadGesture` (+ empty-throw), `IsGestureAvailable`, `EnqueueGesture`, `GetCapabilities`, `EnabledGestures` get/set, `DisplayWidth/Height/Orientation` | ✅ | Extended `TouchInputTests.cpp` with 6 tests: FIFO enqueue/read, empty-queue throw (`InvalidOperationException`), `GetCapabilities` both branches (`touchDeviceExists_` true, and the `InputManager` fallback), `EnabledGestures`/`DisplayWidth`/`DisplayHeight`/`DisplayOrientation` get/set round-trips. Noted (not fixed, out of scope): `GetCapabilities()` passes `MAX_TOUCHES` unconditionally in both branches, unlike FNA which zeroes `MaximumTouchCount` when disconnected — flagged for a follow-up, not part of tasks 710–722. 1792/1792 tests pass. |
| 722 | Direct tests for `TouchCollection` (Contains/FindById out-ref/CopyTo/IndexOf/Add/Clear/Remove/RemoveAt/Insert/IsConnected/operator[]), `TouchLocation` (`==`/`!=`/`Equals` equal+unequal, `GetHashCode` consistency, `ToString`, out-ref `TryGetPreviousLocation`), `GestureSample`, `TouchPanelCapabilities` | ✅ | Extended `TouchInputTests.cpp` with 20 tests across 4 suites: `TouchCollectionTest` (9), `TouchLocationTest` (6), `GestureSampleTest` (3), `TouchPanelCapabilitiesTest` (2). Covers both `operator[]` overloads (task 718's new mutable indexer), all mutating methods, both `TouchLocation` constructors + `TryGetPreviousLocation` true/false paths, and both `GestureSample` constructors (public 6-arg defaulting finger ids to `NO_FINGER`, internal 8-arg with explicit ids). 1812/1812 tests pass. |

---

## Phase I3 — GamePad behavior and FNA fidelity

> CNA drives real SDL3 gamepads (hotplug, rumble, capability probing all genuine). Gaps are
> unwired EXT features and a few fidelity deviations. FNA reference: `GamePad.cs`,
> `SDL3_FNAPlatform.cs` (GetGamePadState / PacketNumber / sensors).

| # | Task | Status | Notes |
|---|------|--------|-------|
| 725 | Implement `GamePad::SetLightBarEXT`: add `SdlInputBridge::SetLightBar` → `SDL_SetGamepadLED(device, R, G, B)`; wire from the no-op at `GamePad.cpp:59–62` | ✅ | `SdlInputBridge::SetLightBar(PlayerIndex, Color)` added (mirrors `SetVibration`'s null-guarded `get_sdl_gamepad_for_player` pattern); calls `SDL_SetGamepadLED(gamepad, R, G, B)`. Matches FNA `SDL3_FNAPlatform.cs:1982-1994`. No dedicated test yet — deferred to task 740 (batched GamePad EXT method tests), consistent with `SetVibration`/`GetGyroEXT`/etc. having no coverage until that task. 1812/1812 tests pass (no new tests). |
| 726 | Implement `GamePad::GetGyroEXT` via `SDL_GetGamepadSensorData(SDL_SENSOR_GYRO)`; replace `return false` stub `GamePad.cpp:69–74` (caps already report `HasGyroEXT`) | ✅ | Added `SdlInputBridge::GetGyro`/`GetAccelerometer`, sharing a private `read_gamepad_sensor(gamepad, type, out)` helper: enables the sensor on first use (`SDL_GamepadSensorEnabled`/`SDL_SetGamepadSensorEnabled`) then reads via `SDL_GetGamepadSensorData(gamepad, type, data, 3)`, zeroing + returning false on no-gamepad or read failure. Matches FNA `SDL3_FNAPlatform.cs:1998–2035` (`GetGamePadGyro`). No dedicated test yet — deferred to task 740, same as Task 725. 1812/1812 tests pass. |
| 727 | Implement `GamePad::GetAccelerometerEXT` via `SDL_GetGamepadSensorData(SDL_SENSOR_ACCEL)`; replace stub `GamePad.cpp:76–81` | ✅ | Same `read_gamepad_sensor` helper (task 726) with `SDL_SENSOR_ACCEL`; matches FNA `SDL3_FNAPlatform.cs:2037–2071` (`GetGamePadAccelerometer`). Implemented together with 726 since both are the same read pattern differing only by sensor type. |
| 728 | Map EXT buttons: extend `InputManager::GamePadButton` enum + `try_convert_sdl_gamepad_button` (`SdlInputBridge.cpp:85–122`) for `MISC1`→`Misc1EXT`, `RIGHT_PADDLE1`→`Paddle1EXT`, `LEFT_PADDLE1`→`Paddle2EXT`, `RIGHT_PADDLE2`→`Paddle3EXT`, `LEFT_PADDLE2`→`Paddle4EXT`, `TOUCHPAD`→`TouchPadEXT`, so they reach `GamePadState.Buttons` | ✅ | Added 6 entries to `InputManager::GamePadButton`, matching `case` branches in `SetGamePadButtonState` (sets/clears the corresponding `Buttons` flag), and matching `case` branches in `try_convert_sdl_gamepad_button`. No `GamePadButtons.hpp` change needed — FNA has no named EXT getters either; EXT buttons are read via the raw `buttons_`/`buttons` flags field in both FNA and CNA. 1812/1812 tests pass (no new tests; button-mapping coverage lands with task 737/740). |
| 729 | Implement `PacketNumber` increment-on-change: track previous per-`PlayerIndex` state, bump when `GamePadState != previous` (FNA semantics); currently hardcoded `0` | ✅ | Intentional deviation from FNA's poll-and-compare algorithm (`SDL3_FNAPlatform.cs:1805-1943`): CNA tracks the counter at the raw `InputManager` layer (bumped inside `SetGamePadConnection`/`SetGamePadButtonState`/`SetGamePadAxisValue` when a value actually changes) rather than comparing freshly-built `GamePadState`s, since the built state's button flags vary by `GamePadDeadZone` mode — comparing built states would falsely bump the counter depending on which dead-zone mode a caller uses. `GamePadState` gained `NOXNA void setPacketNumberProperty(int)` (FNA's `PacketNumber` setter is `internal`); public constructors unchanged (FNA's ctors take no `packetNumber` param either). New test `PacketNumberBumpsOnConnectButtonAndAxisChangesOnly`. 1813/1813 tests pass. |
| 730 | Rework `GamePadCapabilities`: replace 34 raw public mutable `bool` fields with `getXProperty()` getters + internal (private) setters; `NOXNA`-tag the 10 EXT capability properties; add `#include "CNA/CNAHelper.hpp"` | ✅ | All 36 members (25 core bools + `GamePadType` + 10 EXT bools) are now private fields with `getXProperty()` getters and `NOXNA`-tagged `setXProperty()` setters (FNA declares every one `{ get; internal set; }`), matching the existing `setWindowHandleProperty`/`setTouchDeviceExistsProperty`/`setPacketNumberProperty` NOXNA-public-setter precedent from tasks 715–729 rather than introducing a new friend-based pattern. The 10 EXT properties are `NOXNA` on **both** getter and setter (FNA extensions, not XNA 4.0 API). Moved implementations out of the header into a new `GamePadCapabilities.cpp` (previously all-inline). Sole producer `SdlInputBridge::GetCapabilities()` updated to the new setters. 1813/1813 tests pass (no new tests; dedicated coverage lands with task 740). |
| 731 | Rename `GamePadButtons::FromButtons`→`FromButtonArray` and `GamePadDPad::FromButtons`→`FromButtonArray` to match FNA internal names; reconcile `GamePadDPad`'s single-`Buttons` signature with FNA's `params Buttons[]` | ✅ | Both renamed to `FromButtonArray`. `GamePadDPad::FromButtonArray` now takes `std::initializer_list<Buttons>` (was a single combined `Buttons` value) — OR-combines the list, then checks the 4 DPad bits, matching FNA's per-element loop (equivalent since OR is associative). `GamePadState`'s 5-arg public constructor now passes the same `buttons` initializer_list directly to both `GamePadButtons::FromButtonArray` and `GamePadDPad::FromButtonArray`, exactly matching FNA's ctor (`GamePadState.cs:146-158`) instead of CNA's previous workaround of pre-combining through a `GamePadButtons` round-trip. `GamePad.cpp`'s single-`Buttons` call site updated to `FromButtonArray({raw.buttons})`. 1813/1813 tests pass (no regressions; dedicated tests for these methods land with task 736). |
| 732 | Make `GamePadThumbSticks::GetHashCode` (`Left + 37*Right`) and `GamePadTriggers::GetHashCode` (`Left + Right`) FNA-faithful — requires `Vector2::GetHashCode` / float-hash helper (add to sharp-runtime / Vector2 if missing). Currently truncated `*1000` formulas | ✅ | `GamePadThumbSticks::GetHashCode()` → `left_.GetHashCode() + 37 * right_.GetHashCode()` (already-existing `Vector2::GetHashCode()`, bit-reinterpretation hash). `GamePadTriggers::GetHashCode()` → `System::Single::GetHashCode(left_) + System::Single::GetHashCode(right_)` — found the needed float-hash helper already present in sharp-runtime (`System::Single::GetHashCode(float)`, same bit-reinterpretation approach), so no sharp-runtime addition was needed. Both now match FNA's formulas exactly (`GamePadThumbSticks.cs:186`, `GamePadTriggers.cs:137`). 1813/1813 tests pass (no existing tests referenced the old truncated formula; dedicated tests land with 738/739). |
| 733 | Decide `GamePadState::ToString` fidelity: FNA returns the fully-qualified type name; CNA returns `"[GamePadState IsConnected=…]"`. Match FNA or document the intentional deviation | ✅ | Chose to match FNA exactly: FNA's `GamePadState.ToString()` is `return base.ToString();` — it never overrides `ToString`, so `ValueType`'s default (fully-qualified type name, ignoring field values) applies. `ToString()` now unconditionally returns `"Microsoft.Xna.Framework.Input.GamePadState"`, documented in-source. 1813/1813 tests pass (no existing test referenced the old format; dedicated coverage lands with task 737). |
| 734 | Port `GAMEPAD_COUNT` / `FNA_GAMEPAD_NUM_GAMEPADS` env override (currently hardcoded to 4 slots in bridge/InputManager) | ✅ | Added `effective_gamepad_count()` in `SdlInputBridge.cpp` (lazily-computed, matches FNA's `DetermineNumGamepads()` — parses `FNA_GAMEPAD_NUM_GAMEPADS`, requires `>= 0`, falls back to the default on unset/invalid). Unlike FNA, the result is clamped to `MaxSupportedGamePads` (4): CNA's `PlayerIndex` is frozen XNA API with exactly 4 values (One–Four), so an override *above* 4 has no addressable slot to use (FNA's own comment says doing so requires also adding `PlayerIndex` names, which CLAUDE.md forbids). `try_find_free_gamepad_slot()` now only searches slots below the effective count, so setting the env var to a smaller number (or 0) disables/limits gamepad hotplug — the practically useful direction. 1813/1813 tests pass (no new tests; this is an env-var-gated path, not easily unit-testable without process-level env control). |
| 735 | Document the polling-vs-event-driven architecture difference (FNA polls SDL in `GetGamePadState`; CNA reads accumulated event state) and ensure SDL events are pumped every frame so state is current | ✅ | Verified first (no code change needed): `Game::Tick()` (`Game.cpp:378`) unconditionally calls `PollEvents()` → `SDL_PollEvent`/`SdlInputBridge::ProcessEvent` once per frame before `Update()`/`Draw()`, on every code path that reaches a frame (`Run()`→`RunLoop()`→`Tick()`, `RunOneFrame()`→`Tick()`, and the `__EMSCRIPTEN__` main-loop callback all funnel through this). Documented the architecture difference in two places: a comment on `GamePad::GetState()` (`GamePad.cpp`) explaining FNA's fresh-poll-per-call vs. CNA's accumulate-from-events model and pointing at `Game::Tick()` as what keeps it current, and an expanded class-level doc comment on `InputManager` (`InputManager.hpp`) with the same note. 1813/1813 tests pass (doc-only change, no behavior touched). |
| 736 | Unit tests: `GamePadButtons` (getters, `FromButtonArray`, `==`/`!=`/`Equals`/`GetHashCode`) and `GamePadDPad` (ctor, `FromButtonArray`, operators, getters) | ✅ | New `tests/.../Input/GamePadButtonsTests.cpp`, 14 tests (7 `GamePadButtonsTest` + 7 `GamePadDPadTest`): both ctors, all 11 named getters (released + pressed), `FromButtonArray` (multi-flag, cross-element combine, empty list), `Equals`/`==`/`!=` for equal+unequal, `GetHashCode` (exact value match against the underlying flags / FNA's bit-weighted DPad formula). 1827/1827 tests pass. |
| 737 | Unit tests: `GamePadState` — both public constructors directly, `IsButtonDown`/`IsButtonUp`, `==`/`!=`/`Equals`/`GetHashCode`/`ToString`, button-packing (trigger→`LeftTrigger`, stick→thumbstick-direction buttons) | ✅ | New `tests/.../Input/GamePadStateTests.cpp`, 10 tests: default + 4-arg + 5-arg constructors, trigger→`LeftTrigger`/`RightTrigger` packing past `GamePad::TriggerThreshold`, stick→thumbstick-direction packing past `LeftDeadZone`/`RightDeadZone`, `IsButtonDown`/`IsButtonUp` (including multi-flag "all requested bits" semantics), `Equals`/`==`/`!=` (including `PacketNumber` affecting equality), exact-formula `GetHashCode`, and `ToString`'s fixed fully-qualified-name return (task 733). 1837/1837 tests pass. |
| 738 | Unit tests: `GamePadThumbSticks` — `Circular` and `IndependentAxes` dead-zone math (only `None` is currently exercised), square/circular clamp, `ExcludeCircularDeadZone`, operators | ✅ | New `tests/.../Input/GamePadThumbSticksTests.cpp`, 9 tests. The dead-zone-mode constructor is private (only reachable via `GamePad::GetState(playerIndex, deadZoneMode)`), so `IndependentAxes`/`Circular` cases drive it through `InputManager::SetGamePadAxisValue` + `GamePad::GetState`: per-axis exclusion + square clamp (`IndependentAxes`), zero-within-radius / rescale-outside-radius / circular-clamp-back-to-unit-circle (`Circular`, exercising the private `ExcludeCircularDeadZone` indirectly). Plus direct public-API coverage: default ctor, 2-arg square-clamp ctor, `Equals`/`==`/`!=`, exact-formula `GetHashCode`. 1846/1846 tests pass. |
| 739 | Unit tests: `GamePadTriggers` — dead-zone ctor, `==` epsilon behavior (`WithinEpsilon`), `GetHashCode` | ✅ | New `tests/.../Input/GamePadTriggersTests.cpp`, 7 tests: default + 2-arg clamp constructors, `GetHashCode` exact-formula match, and the private dead-zone ctor (reached via `InputManager::SetGamePadAxisValue` + `GamePad::GetState`) for `None` (pass-through) vs. non-`None` (exclude `TriggerThreshold`, then clamp). The epsilon test picks a base value `< 0.5` so its ULP is provably smaller than `MathHelper::MachineEpsilonFloat` (~2^-24, derived relative to 1.0) — `std::nextafter` then gives two bit-distinct floats that must still compare equal via `WithinEpsilon`; first attempt used 0.5 as the base and flakily failed since its ULP is right at the epsilon boundary. 1853/1853 tests pass. |
| 740 | Unit tests: `GamePad` + `GamePadCapabilities` — `GetCapabilities`, `SetVibration`, EXT methods, `ExcludeAxisDeadZone` | ✅ | New `tests/.../Input/GamePadTests.cpp`, 13 tests. `ExcludeAxisDeadZone` (4): zero-within-deadzone, positive/negative rescale, max-magnitude-to-max-output. `GamePad` no-hardware fallback paths (7, since no SDL gamepad is ever opened in this test binary): `GetCapabilities`/`SetVibration`/`SetTriggerVibrationEXT`/`SetLightBarEXT`/`GetGyroEXT`/`GetAccelerometerEXT`/`GetGUIDEXT` all correctly report disconnected/false/empty. `GamePadCapabilities` (2): default-false/`Unknown` state, and a single test round-tripping all 36 getter/setter pairs from task 730's rework. **Phase I3 (tasks 725–740) is now complete.** 1866/1866 tests pass. |

---

## Phase I4 — Mouse behavior and MouseCursor ✅ COMPLETE (tasks 745–755)

> `MouseState` is faithful and populated from SDL3. The gaps are dead behavior paths in `Mouse`
> (no cursor warp, dead relative mode, `ClickedEXT` never fires) and a non-compliant MonoGame-derived
> `MouseCursor` (no FNA counterpart). FNA reference: `Mouse.cs`, `MouseState.cs` (no `MouseCursor.cs`).

| # | Task | Status | Notes |
|---|------|--------|-------|
| 745 | Wire `Mouse::SetPosition` to actually warp the OS cursor (`SDL_WarpMouseInWindow`) with the FNA relative-mode early-return guard and window/back-buffer coordinate scaling (`Mouse.cs:107–116`); stop relying solely on `InputManager` state mutation that the next motion event overwrites | ✅ | `SetPosition` now: (1) early-returns when `IsRelativeMouseModeEXT` is set, matching `Mouse.cs:99-103` (the flag is still a dead `bool` until task 746 wires it to SDL, but the guard is a correct literal port and becomes live once 746 lands); (2) keeps the existing `InputManager::SetMousePosition(x, y)` call so `GetState()` reflects the new position immediately, not just after the next motion event; (3) calls real `SDL_WarpMouseInWindow(window, x, y)`, resolving `window` from `Mouse::WindowHandle` if published, else falling back to `SDL_GetMouseFocus()` — matching the existing window-resolution fallback pattern used throughout `SdlInputBridge.cpp`. **Intentional interim deviation:** the FNA `INTERNAL_WindowWidth/Height` ÷ `INTERNAL_BackBufferWidth/Height` scale formula (`Mouse.cs:112-114`) is ported verbatim but guarded to a no-op (pass `x`,`y` through unscaled) while `INTERNAL_BackBufferWidth/Height` are `<= 0`, since nothing publishes those fields yet (deferred to task 747) — applying the raw FNA formula today would divide by zero. Documented in-source in `Mouse.cpp`. 1866/1866 tests pass (no dedicated `Mouse` tests yet — batched into task 755, matching the task-740 precedent from Phase I3). |
| 746 | Implement `Mouse::IsRelativeMouseModeEXT` as a real get/set property backed by `SDL_SetWindowRelativeMouseMode`/`SDL_GetWindowRelativeMouseMode`; feed relative deltas into `GetMouseState`. Currently a dead `bool` | ✅ | Converted the plain public `bool` field to a `getIsRelativeMouseModeEXTProperty()`/`NOXNA setIsRelativeMouseModeEXTProperty(bool)` pair (both `NOXNA` — an FNA `EXT` addition — matching the `GamePadCapabilities` EXT-both-tagged precedent from task 730), since CLAUDE.md requires C# properties to follow the `getXProperty()`/`setXProperty()` convention and no other code referenced the old field name yet. Getter/setter call `SDL_GetWindowRelativeMouseMode`/`SDL_SetWindowRelativeMouseMode` directly on the window resolved by a new file-local `resolve_mouse_window()` helper in `Mouse.cpp` (WindowHandle if published, else `SDL_GetMouseFocus()` — same fallback pattern as `SdlInputBridge.cpp`), matching FNA's `FNAPlatform.GetRelativeMouseMode`/`SetRelativeMouseMode` (`Mouse.cs:29-38`). **Relative deltas → GetMouseState:** added `InputManager::SetMouseRelativeMode(bool)` + `AddMouseRelativeDelta(float, float)`; `SdlInputBridge`'s `SDL_EVENT_MOUSE_MOTION` case now also calls `AddMouseRelativeDelta(event.motion.xrel, event.motion.yrel)` unconditionally (the accumulator itself no-ops unless relative mode is on, so no bridge-side mode check is needed). `InputManager::GetMouseState()` reports the accumulated float delta (cast to `int`, then drained to `0` on read) as `X`/`Y` while relative mode is active, instead of the absolute cursor position — this mirrors FNA's own `SDL_GetRelativeMouseState`-backed `GetMouseState()` (`SDL3_FNAPlatform.cs:1389-1391`), which also resets its accumulator on every read. The setter also flushes any stale accumulated delta on toggle, matching FNA's throwaway `SDL_GetRelativeMouseState` flush call in `SetRelativeMouseMode` (`SDL3_FNAPlatform.cs:1439-1448`). `Mouse::SetPosition`'s relative-mode guard (task 745) updated to call the new getter instead of reading the removed field directly. 1866/1866 tests pass (no dedicated `Mouse` tests yet; batched into task 755). |
| 747 | Either implement FNA-style back-buffer scaling in `Mouse::GetState` using the `INTERNAL_BackBufferWidth/Height/WindowWidth/Height/MouseWheel` fields, or **remove** those dead fields and document the existing `TransformWindowToLogical` deviation (`SdlInputBridge.cpp:226`) | ✅ | Chose removal: implementing FNA's fixed-ratio scaling for real would (1) duplicate CNA's existing, more general window↔logical transform (`SdlInputBridge::to_logical_position`, backed by `SDL_RenderCoordinatesFromWindow`/`IGraphicsBackend::TransformWindowToLogical`) that already runs on every stored mouse position, so `GetMouseState()`'s `X`/`Y` are already logical coordinates and applying a second ratio-based scale on top would double-transform them; and (2) require wiring `INTERNAL_BackBufferWidth/Height`/`INTERNAL_WindowWidth/Height` from `GraphicsDeviceManager::ApplyChanges()`, a graphics-layer change out of scope for this input branch (NEXT.md §9: "No graphics changes on this branch"). Removed all 5 dead fields (`INTERNAL_WindowWidth/Height`, `INTERNAL_BackBufferWidth/Height`, `INTERNAL_MouseWheel`) from `Mouse.hpp`/`Mouse.cpp`; `SetPosition` (task 745) no longer has a scaling block to guard — it now passes `x`,`y` straight through to `SDL_WarpMouseInWindow`. Documented the resulting **known limitation** in a source comment on `Mouse.cpp`: `SetPosition` has no inverse (logical→window) transform, so on a letterboxed/scaled window (render resolution ≠ window size) the warp target will be off by the scale factor — adding a general inverse to `IGraphicsBackend` is a graphics-layer change, left for the graphics track. `INTERNAL_MouseWheel` needed no CNA equivalent either: `InputManager::AddScrollWheelDelta`/`ScrollWheelValue` already accumulates cumulatively, matching FNA's semantic via a different (already-existing) mechanism. 1866/1866 tests pass (no dedicated `Mouse` tests yet; batched into task 755). |
| 748 | Wire `ClickedEXT`: have `SdlInputBridge` call `Mouse::INTERNAL_onClicked(button)` on mouse-button-down (currently never invoked) | ✅ | `SdlInputBridge.cpp`'s shared `SDL_EVENT_MOUSE_BUTTON_DOWN`/`SDL_EVENT_MOUSE_BUTTON_UP` case now calls `Mouse::INTERNAL_onClicked(event.button.button - 1)` when `event.type == SDL_EVENT_MOUSE_BUTTON_DOWN`, matching FNA exactly (`SDL3_FNAPlatform.cs:958-960`: `Mouse.INTERNAL_onClicked(evt.button.button - 1)`, unconditional — no per-button filtering, unlike the existing `InputManager::SetMouseButtonState` switch a few lines above which only recognizes Left/Right/Middle/X1/X2). Added `#include "Microsoft/Xna/Framework/Input/Mouse.hpp"` to `SdlInputBridge.cpp`. 1866/1866 tests pass (no dedicated `Mouse`/`ClickedEXT` tests yet; batched into task 755). |
| 749 | Add horizontal scroll wheel: handle `event.wheel.x` (bridge uses `wheel.y` only, `SdlInputBridge.cpp:562–565`). Only if the XNA layer exposes a horizontal-scroll path | ✅ | **Closed as won't-implement by Phase I9 task 805** — FNA's `MouseState` and XNA 4.0 have no horizontal-scroll member, so there's no XNA-layer path to expose; the bridge intentionally drops `event.wheel.x` (now documented in-source). A horizontal wheel would be a NOXNA extension with no current consumer. |
| 750 | `MouseCursor` CHECKLIST: fix SPDX `MIT`→`MS-PL`, add `#include "CNA/CNAHelper.hpp"`, `NOXNA`-tag the class/members (entirely non-XNA inside the Xna namespace) | ✅ | `MouseCursor.hpp`/`.cpp` SPDX header fixed to `MS-PL` (dropped the `Copyright (c) Robert Vokac and contributors` line too, matching the plain single-line SPDX header used by every other CHECKLIST-compliant file, e.g. `ShaderEffect.hpp`). Added `#include "CNA/CNAHelper.hpp"`. Class tagged `NOXNA class MouseCursor` (no `MouseCursor` type exists in XNA 4.0 or FNA — this is a MonoGame-derived CNA extension) with all constructors, `GetSDLCursor()`, the 11 static stock-cursor constants, and the private `MakeSystem()` factory tagged `NOXNA`; the rule-of-5 copy/move/destructor lines were left untagged, matching the `ShaderEffect` precedent (its destructor isn't `NOXNA`-tagged either — mechanical C++ boilerplate, not new API surface). **Unrelated build blocker fixed along the way:** `sharp-runtime` had committed (`d61ffb8`) a `System::IAsyncResult` interface addition (`AsyncState`/`AsyncWaitHandle`, matching .NET's full 4-member interface) that broke this repo's own `StorageDevice.cpp` — its private `ContainerResult`/`SelectorResult` `IAsyncResult` implementers didn't have the two new pure-virtual overrides, making the whole `CNA` target unbuildable (unrelated to Mouse/MouseCursor work, but blocking all verification). Fixed by converting their existing dead `void* asyncState` field to `std::any asyncState` (same values, no `Begin*`-method signature change) and adding a `mutable System::Threading::EventWaitHandle waitHandle{true, ManualReset}` (both classes always report `getIsCompletedProperty() == true`, so an already-signalled handle is correct), following the exact pattern sharp-runtime's own commit used in its test implementers (`InterfaceTests.cpp`). 1866/1866 tests pass. |
| 751 | `MouseCursor::FromTexture2D(Texture2D, int, int)` via `SDL_CreateColorCursor` (requires Texture2D pixel access); currently missing → custom cursors unsupported | ✅ | Ported from MonoGame's SDL platform implementation (`MouseCursor.SDL.cs::PlatformFromTexture2D` — no FNA equivalent exists; MonoGame is the reference since `MouseCursor` itself is a MonoGame-derived NOXNA extension). Validates `texture.getFormatProperty()` is `SurfaceFormat::Color` or `ColorSrgb` (else `std::invalid_argument`, matching MonoGame's `ArgumentException`). Reads pixels via the existing `Texture2D::GetData(Color*, int)`, then extracts each pixel's `getPackedValueProperty()` into a raw `std::vector<uint32_t>` — **`Color` cannot be treated as a tightly-packed RGBA8 buffer directly**, since it carries a vtable pointer (`IPackedVectorT`), so `sizeof(Color) != 4`; this differs from MonoGame's C# version, which reads directly into a `byte[]` via `Texture2D.GetData(byte[])`. `Color::PackedValue`'s byte layout (R,G,B,A; `Color.cpp`) matches `SDL_PIXELFORMAT_RGBA32` exactly, so no channel reordering is needed once extracted. Builds an `SDL_Surface` via `SDL_CreateSurfaceFrom` (SDL3's replacement for SDL2's `SDL_CreateRGBSurfaceFrom` used by MonoGame), then `SDL_CreateColorCursor(surface, originX, originY)`, freeing the surface either way and throwing `std::runtime_error` with `SDL_GetError()` on failure (matching MonoGame's `InvalidOperationException`). Added `#include "Microsoft/Xna/Framework/Graphics/Texture2D.hpp"` to `MouseCursor.hpp` (public API takes `const Graphics::Texture2D&`). 1866/1866 tests pass (no dedicated test yet; batched into task 755). |
| 752 | `MouseCursor::Dispose()` / `System::IDisposable` + `isDisposed_` guard (MonoGame `MouseCursor : IDisposable`); currently only a destructor | ✅ | `MouseCursor` now inherits `System::IDisposable`; `Dispose()` is idempotent (`isDisposed_` guard, matching the `SoundBank`/`GraphicsResource` precedent), releases the SDL cursor only if `owning_`, and the destructor now just calls `Dispose()`. No public `getIsDisposedProperty()` added — MonoGame's own `MouseCursor._disposed` field has no public getter either (`MouseCursor.cs:94`), so none was added here to match. **Bug fixed alongside (found while implementing this task):** the previous `= default` move constructor/assignment did a bitwise-copy of the raw `sdlCursor_` pointer without nulling the moved-from source, so a moved-from `MouseCursor` and its target would both believe they owned the same `SDL_Cursor*` — both destructors (now both calling `Dispose()`) would call `SDL_DestroyCursor` on the same pointer, a double-free. Not reachable through any code path in this repo yet (every existing call site returns a prvalue directly, which is elided under C++17's mandatory copy elision rather than actually invoking the move ops), but adding real `Dispose()` semantics made the latent bug live enough to fix while already touching these exact lines. Replaced the defaulted move ctor/assignment with explicit versions that null out the source's `sdlCursor_`/`owning_` and mark it disposed. 1866/1866 tests pass (no dedicated test yet; batched into task 755). |
| 753 | Rename stock cursor `WaitCursor`→`WaitArrow` (MonoGame name); fix static-init ordering — `SDL_CreateSystemCursor` runs at static init **before** `SDL_Init` (likely null/fragile); construct lazily | ✅ | All 11 stock cursors converted from eagerly-initialized public static fields (`static MouseCursor Arrow;` etc., each running `SDL_CreateSystemCursor` before `main()`/`SDL_Init()`) to `NOXNA getXProperty()` accessor methods backed by function-local `static` variables (Meyer's singleton — C++11+ guarantees thread-safe, exactly-once initialization on first call), matching MonoGame's own `static MouseCursor() { PlatformInitalize(); }` lazy static constructor (triggered on first access to any static member, not at process startup). This is also why the field→getter conversion was necessary rather than optional: a plain C++ static data member cannot be lazily initialized, only a function-local static can. `WaitCursor`→`getWaitArrowProperty()` (renamed to match MonoGame's actual name, `MouseCursor.cs:38`; still backed by `SDL_SYSTEM_CURSOR_PROGRESS`, unchanged from before — SDL3's closest match to SDL2's `SDL_SYSTEM_CURSOR_WAITARROW`). No call sites existed anywhere in the repo yet for the old field names, so this was a safe, non-breaking rename. 1866/1866 tests pass (no dedicated test yet; batched into task 755). |
| 754 | `MouseCursor` `Handle` property (or `NOXNA`-tag `GetSDLCursor`); record overall status decision (keep as documented MonoGame `NOXNA` extension or drop) in AUDIT.md | ✅ | Chose the "`NOXNA`-tag `GetSDLCursor`" option over adding a separate `Handle` property: `GetSDLCursor()` was already `NOXNA`-tagged (task 750) and already returns the native cursor handle (`SDL_Cursor*`); a second accessor reinterpreting the same pointer as a generic `IntPtr`/`std::uintptr_t` would duplicate it with no current consumer needing the untyped form (`Mouse::SetCursor` and everything else already use the typed `SDL_Cursor*`). **Status decision: kept**, recorded in `AUDIT.md`'s `Microsoft::Xna::Framework::Input` table — `MouseCursor` is a MonoGame-derived `NOXNA` extension (no FNA/XNA 4.0 equivalent), retained because it's the standard MonoGame/FNA-family mechanism for custom/stock OS cursors and `Mouse::SetCursor(MouseCursor&)` already depends on it; noted as CHECKLIST-compliant as of tasks 750-753. No code changes — build unaffected (1866/1866 tests pass, unchanged). |
| 755 | New `tests/Microsoft/Xna/Framework/Input/MouseInputTests.cpp`: `MouseState` (8 getters, ctor, `==`/`!=`, `Equals` equal+unequal, `GetHashCode` consistency, `ToString` format incl. multi-button + `None`), `Mouse` (`GetState`, `SetPosition`, `INTERNAL_onClicked`→`ClickedEXT`), `MouseCursor` (stock cursors non-null) | ✅ | New `MouseInputTests.cpp`, 26 tests across 3 suites (`MouseStateTest` 10, `MouseTest` 9, `MouseCursorTest` 7). `MouseState`: default + 8-arg ctor (alternating Pressed/Released per param to catch parameter-order bugs), `Equals`/`==`/`!=` (equal case + position/scroll-wheel/button unequal cases), `GetHashCode` exact-formula match + consistency, `ToString` (`None` case + a multi-button case exercising the Left/Right/Middle/XButton1/XButton2 ordering with gaps). `Mouse`: `GetState` (position/buttons via `InputManager`, scroll-wheel via before/after delta since `ScrollWheelValue` is cumulative-for-process-lifetime and can't be reset), `SetPosition`, `INTERNAL_onClicked`→`ClickedEXT` (with and without a subscriber), task 746's relative-mode plumbing (`InputManager`-level delta accumulation + drain-on-read, and a full `getIsRelativeMouseModeEXTProperty`/`setIsRelativeMouseModeEXTProperty` round-trip through a **real hidden `SDL_Window`**, since the getter always queries SDL live and returns `false` with no window — matching the `GameWindowTests.cpp` `SDL_InitSubSystem(SDL_INIT_VIDEO)` + `GTEST_SKIP()`-on-failure precedent), and `SetPosition`'s relative-mode no-op guard (verified by toggling relative mode off again to read the preserved absolute position, since `GetState()` reports the drained delta while relative mode is still on). `MouseCursor`: all 11 stock cursors non-null (single test, since they're process-wide Meyer's singletons — splitting into per-cursor tests wouldn't isolate anything), singleton identity (same instance returned twice), default ctor, `Dispose()` idempotency, non-owning-ctor doesn't destroy, and move-ctor/move-assignment regression tests directly covering task 752's double-free fix (asserts the moved-from handle is nulled). **Deliberately not covered:** `MouseCursor::FromTexture2D` — building a `Texture2D` with real CPU-side pixel data (or any non-`Color`/`ColorSrgb` format) requires a `GraphicsDevice`, whose constructor creates a real SDL window and graphics backend, matching the existing `OcclusionQueryDynamicBufferTests.cpp` precedent of excluding `GraphicsDevice`-dependent construction from this headless unit suite (documented via a header comment in the new test file). All new tests verified stable under `--gtest_shuffle --gtest_repeat=10` (both filtered to `*Mouse*` and the full suite) given the shared static `InputManager`/`MouseCursor`-singleton process state. 1892/1892 tests pass (1866 previous + 26 new) — **completes Phase I4.** |

---

## Phase I5 — Keyboard fidelity and SDL key mapping ✅ COMPLETE (tasks 760–768)

> `KeyboardState` is populated from SDL3 key events and `Keys`/`KeyState` values match FNA exactly.
> Gaps are an unordered `GetPressedKeys`, a non-FNA `GetHashCode`, a missing indexer, an incomplete
> keycode map, and a stubbed `GetKeyFromScancodeEXT`. FNA reference: `KeyboardState.cs`,
> `SDL3_FNAPlatform.cs` (keyMap 2360–2489, scanMap 2490–2618, `GetKeyFromScancode` 2768–2797).

| # | Task | Status | Notes |
|---|------|--------|-------|
| 760 | Fix `KeyboardState::GetPressedKeys()` ordering — must return keys sorted ascending by numeric value (XNA/FNA contract; FNA iterates bits 0→255). Currently `unordered_set` iteration is nondeterministic (`KeyboardState.cpp:32–35`) | ✅ | `GetPressedKeys()` now copies `pressedKeys_` into a `std::vector` and `std::sort`s it by `static_cast<int>(Keys)` ascending before returning, matching FNA's `GetPressedKeys()` (`KeyboardState.cs:134-189`), which walks its `keys0..keys7` bitfields from bit 0 upward via `AddKeysToArray` (`KeyboardState.cs:320-330`) — net effect is strictly ascending numeric `Keys` order. Kept the existing `unordered_set` storage (`pressedKeys_`) rather than porting FNA's 8×32-bit bitfield representation — task 760 is scoped to the ordering contract, not the storage architecture; sorting on read is sufficient and minimal. Verified with an ad-hoc standalone multi-key check (not committed — `KeyboardState({Z, A, Space, D1})` → returns `[Space, D1, A, Z]`, i.e. ascending numeric order 32, 49, 65, 90); the existing `GetPressedKeysContainsOnlyPressedKeys` test only exercises a single pressed key, so it doesn't independently prove ordering — dedicated multi-key ordering coverage is deferred to task 768 (`KeyboardInputTests.cpp` expansion), matching the task-740/755 precedent of batching new test coverage into one dedicated task per phase. 1892/1892 tests pass (no regressions; no new tests added by this task). |
| 761 | Make `KeyboardState::GetHashCode()` FNA-faithful (`keys0 ^ … ^ keys7` over a 256-bit field) or document as accepted deviation; currently `XOR(key*31)` | ✅ | Ported FNA's exact formula (`KeyboardState.cs:264-267`): reconstructs the same 8×32-bit word layout FNA stores natively (`AddPressedKey`, `KeyboardState.cs:220-234` — `words[key>>5] \|= 1u << (key & 0x1f)`) from `pressedKeys_` on the fly, then returns `words[0]^words[1]^…^words[7]` cast to `int`. Faithfully reproduces FNA's intentional same-bit-position-across-words collision behavior (e.g. keys 5 and 37 both toggle bit 5 of their respective words, so pressing both cancels that bit in the final XOR) — this isn't a bug to avoid, it's `GetHashCode()`'s actual FNA behavior, and CNA now matches it exactly rather than approximating with `XOR(key*31)`. Verified against a hand-computed expected value using distinct keys in two different 32-key words (ad-hoc standalone check, not committed), plus hash-equality-for-equal-states and empty-state-hashes-to-0 sanity checks. Dedicated `GetHashCode` test coverage deferred to task 768, matching the task 760/755/740 batching precedent. 1892/1892 tests pass (no regressions; no new tests added by this task). |
| 762 | Add `KeyState operator[](Keys) const` mirroring FNA's `this[Keys]` indexer (keep/alias the existing `getItem`) | ✅ | Added `[[nodiscard]] KeyState operator[](Keys) const`, implemented as a one-line delegate to the existing `getItem(Keys)` (matching FNA's `this[Keys key] { get { return InternalGetKey(key) ? Down : Up; } }`, `KeyboardState.cs:28-34` — identical logic to CNA's pre-existing `getItem`). `getItem` kept unchanged/unremoved per the task. Verified with an ad-hoc standalone check (not committed) that `state[Keys::A]`/`state[Keys::B]` match `IsKeyDown`/`getItem` results. Dedicated test coverage deferred to task 768. 1892/1892 tests pass (no regressions; no new tests added by this task). |
| 763 | Complete the SDL keycode→`Keys` map in `try_convert_sdl_key` (`SdlInputBridge.cpp:258–369`): add `F13–F24`, `APPLICATION`/`MENU`→`Apps`, `SLEEP`, `VOLUMEUP`/`VOLUMEDOWN`, `KP_CLEAR`→`OemClear`, `KP_PERIOD`→`OemPeriod`, and AZERTY/Norwegian/BEPO locale fallbacks (FNA `keyMap`) | ✅ | Ported every entry from FNA's `INTERNAL_keyMap` (`SDL3_FNAPlatform.cs:2358-2489`) not already present: `SDLK_F13`–`SDLK_F24`→`F13`–`F24`; `SDLK_APPLICATION`/`SDLK_MENU`→`Apps`; `SDLK_SLEEP`→`Sleep`; `SDLK_VOLUMEUP`/`SDLK_VOLUMEDOWN`→`VolumeUp`/`VolumeDown`; `SDLK_KP_CLEAR`→`OemClear`; `SDLK_KP_PERIOD`→`OemPeriod` (distinct from the already-mapped `SDLK_KP_DECIMAL`→`Decimal` — two separate SDL scancodes some international keyboards report differently). Locale fallbacks (raw Unicode-codepoint case labels, since SDL keycodes for printable characters are their Unicode scalar value): AZERTY `'²'` (`0x00B2`)→`OemTilde`; Norwegian `'|'`→`OemPipe`, `'+'`→`OemPlus`, `'ø'` (`0x00F8`)→`OemSemicolon`, `'æ'` (`0x00E6`)→`OemQuotes`; BEPO `'é'` (`0x00E9`)→`std::nullopt`. **Intentional deviation for `'é'`:** FNA's dictionary literally maps it to `Keys.None` (`{ 'é' /* FIXME: BEPO SDL3? -flibit */, Keys.None }`), which is behaviorally identical in FNA to an unmapped lookup miss (FNA's `ToXNAKey` also falls back to `Keys.None` on a miss) — i.e. FNA's own entry is a documented no-op placeholder, not a real mapping. CNA's fallback for a genuinely unmapped key is different (`try_convert_sdl_key` returns `std::nullopt`, and the caller drops the event entirely rather than marking `Keys::None` as pressed — a pre-existing architectural choice, not something this task changes), so for consistency the `'é'` case also returns `std::nullopt` rather than introducing a one-off exception that marks `None` pressed. All entries verified via an ad-hoc standalone harness (not committed) that feeds synthetic `SDL_Event`s through `SdlInputBridge::ProcessEvent` and checks `Keyboard::GetState()` — including confirming `'é'` registers no pressed key at all, matching the drop-on-unmapped behavior. Dedicated committed test coverage isn't explicitly scoped by task 768 either (which covers `KeyboardState`'s own API, not `SdlInputBridge`'s keycode table) — noted here in case a future task wants `SdlInputBridge`-level keycode-map regression tests. 1892/1892 tests pass (no regressions; no new tests added by this task). |
| 764 | Implement `Keyboard::GetKeyFromScancodeEXT` properly: port FNA `GetKeyFromScancode` (xnaMap→`SDL_GetKeyFromScancode`→keyMap). Currently identity no-op (`Keyboard.cpp:22–25`) | ✅ | Ported FNA's non-scancode-mode branch (`SDL3_FNAPlatform.cs:2743-2769` — the `UseScancodes` branch is task 765's scope, doesn't exist in CNA yet, so this always takes the equivalent of FNA's "false" path, matching CNA's current always-off default). New `SdlInputBridge::GetKeyFromScancode(Keys)`: (1) `try_convert_keys_to_sdl_scancode(Keys)` — a new file-local reverse map mirroring FNA's `INTERNAL_xnaMap` (`SDL3_FNAPlatform.cs:2619-2742`) in full, mapping every `Keys` value to the `SDL_Scancode` of its physical US-layout key position; (2) `SDL_GetKeyFromScancode(scancode, SDL_KMOD_NONE, true)` to ask SDL what character the *current* keyboard layout produces at that physical position; (3) reuse the existing `try_convert_sdl_key` (task 763) to convert that resulting keycode back to a `Keys` value. Falls back to `Keys::None` at any missed lookup, matching FNA. Added `SdlInputBridge::GetKeyFromScancode(Keys)` as a new public method (same "XNA layer calls into SdlInputBridge's public static query methods" pattern already used by `GamePad`↔`SdlInputBridge`); `Keyboard::GetKeyFromScancodeEXT` now delegates to it instead of returning its input unchanged. Verified via an ad-hoc standalone harness (not committed): on this environment's default (US) layout, common keys (`A`, `D1`, `F13`, `Enter`, `OemPeriod`, `Space`, `None`) all round-trip to themselves, as expected without an actual non-US layout active. 1892/1892 tests pass (no regressions; no new committed tests — task 768 covers `KeyboardState`'s own API, though it does explicitly list `GetKeyFromScancodeEXT` as one of its spot-checks). |
| 765 | Add scancode mode: port `INTERNAL_scanMap` + `FNA_KEYBOARD_USE_SCANCODES` env switch into the key-conversion path (entirely absent today) | ✅ | Added `use_scancode_mode()` (file-local, `SdlInputBridge.cpp`): reads `FNA_KEYBOARD_USE_SCANCODES` once via a function-local `static const bool` initialized from `std::getenv(...) == "1"`, mirroring FNA's `UseScancodes` static-readonly-at-startup semantics (`SDL3_FNAPlatform.cs:33-35`) — evaluated once, so setting the env var after the first lookup has no effect, matching FNA exactly. Same style as the pre-existing `effective_gamepad_count()` env-var cache. Added `try_convert_sdl_scancode(SDL_Scancode) -> std::optional<Keys>`, a new file-local direct scancode→`Keys` map mirroring FNA's `INTERNAL_scanMap` (`SDL3_FNAPlatform.cs:2490-2618`) in full, including the two `FIXME`-marked `NONUSHASH`/`NONUSBACKSLASH`→`Keys::None` entries. Wired into two call sites, matching FNA exactly: (1) the `SDL_EVENT_KEY_DOWN`/`SDL_EVENT_KEY_UP` handler in `ProcessEvent` — now branches on `use_scancode_mode()` to pick `try_convert_sdl_scancode(event.key.scancode)` vs. the existing `try_convert_sdl_key(event.key.key)`, mirroring FNA's `ToXNAKey(ref sym, ref scancode)` (`SDL3_FNAPlatform.cs:2743-2766`); (2) `SdlInputBridge::GetKeyFromScancode(Keys)` — added an early `if (use_scancode_mode()) return scancode;` before the task-764 xnaMap/keyMap round-trip, mirroring FNA's own `GetKeyFromScancode` (`SDL3_FNAPlatform.cs:2768-2773`). Did not port the `FNAPlatform.cs:88-94` command-line-arg-to-env-var plumbing (`usescancodes` launch parameter) — CNA has no equivalent launch-args system yet, and the task scope is the key-conversion path itself; the env var can already be set directly. Verified via two separate ad-hoc standalone harnesses (not committed, one process per mode since the cached flag is per-process): with `FNA_KEYBOARD_USE_SCANCODES=1` set before first use, `GetKeyFromScancode` passes through `A`/`D1`/`F13`/`Enter`/`OemPeriod`/`Space`/`None` unchanged (scancode-mode identity, matching FNA); with the env var unset, `GetKeyFromScancode(A)` still round-trips via the task-764 path, confirming the new early-return doesn't affect default-mode behavior. 1892/1892 tests pass (no regressions; no new committed tests — `try_convert_sdl_scancode`/`use_scancode_mode` are file-local like the rest of `SdlInputBridge.cpp`'s conversion tables, consistent with task 763's precedent of leaving keycode-table coverage out of the committed suite). |
| 766 | Replace `KeyboardState::ToString()` placeholder (`"[KeyboardState]"`) with a meaningful representation, or drop it (NOXNA extension not in FNA) | ✅ | Checked FNA's `KeyboardState.cs`: it never overrides `ToString()`, so it's not a NOXNA extension at all — it's the same situation already established for `GamePadState::ToString()` (`GamePadState.cpp:105-111`), where a C# struct relies on `System.ValueType`'s default `ToString()`, which returns the fully-qualified type name regardless of field values. Fixed the previous placeholder (`"[KeyboardState]"`, a CNA-invented format not matching either a real FNA override or the ValueType default) to return `"Microsoft.Xna.Framework.Input.KeyboardState"` instead, with an in-source comment pointing at the `GamePadState` precedent. **Also removed the `NOXNA` tag from `ToString()`'s declaration in `KeyboardState.hpp`** — it was incorrectly applied; `ToString()`/`Equals()`/`GetHashCode()` are inherited from `System.Object`/`System.ValueType` on every C# type, so implementing them (even via the ValueType default) is porting real API surface, not adding a CNA extension. Neither `GamePadState::ToString()` nor `MouseState::ToString()` (which does have real FNA-authored content) are `NOXNA`-tagged, confirming this was a pre-existing inconsistency now corrected. No `AUDIT.md` change needed (`KeyboardState` was already marked ✅/API complete). 1892/1892 tests pass (no regressions; no dedicated `ToString()` test added by this task — `KeyboardInputTests.cpp` already covers `ToString` per task 768's plan, not this one, though a quick ad-hoc check confirmed the new return value). |
| 767 | Consider `enum class Keys : int` for explicit FNA-matching underlying type | ✅ | Confirmed FNA's `Keys` (`Input/Keys.cs:15`) declares no explicit base type, so it defaults to C#'s implicit `int` — and C++'s `enum class` without an explicit underlying type also defaults to `int` (per the standard, since all of `Keys`'s enumerator values already fit in `int`), so this was already behaviorally `int`-backed; the change is purely making that explicit in source rather than changing any actual behavior. Changed the declaration from `enum class Keys` to `enum class Keys : int` in `Keys.hpp`. Confirmed no code depends on the previously-implicit type in a way that could differ (no `std::underlying_type_t<Keys>` usage, no custom `std::hash<Keys>` specialization — `pressedKeys_`'s `std::unordered_set<Keys>` uses the compiler-provided default enum hash, which is unaffected). 1892/1892 tests pass, byte-identical behavior confirmed (as expected for a change that only makes an already-true default explicit); no new tests needed since nothing observable changed. |
| 768 | Expand `KeyboardInputTests.cpp`: all three constructors, `operator[]`/`getItem`, `==`/`!=`/`Equals` (equal+unequal), `GetHashCode` consistency, `ToString`, **multi-key** `GetPressedKeys` ordering, empty `GetPressedKeys`, `GetState(PlayerIndex)`, `GetKeyFromScancodeEXT`, and `Keys`/`KeyState` value spot-checks | ✅ | Expanded `KeyboardInputTests.cpp` from 3 to 21 tests (18 new), across two suites: `KeyboardInputTest` (7 — `Keyboard`'s static API: `GetState()`, `GetState(PlayerIndex)` — confirmed `GetState(PlayerIndex::One)` matches parameterless `GetState()`, matching CNA's single-keyboard model — `GetPressedKeys` single-key sanity, and `GetKeyFromScancodeEXT` round-trips/`None`/an unmapped key `Kana`) and `KeyboardStateTest` (14 — all three constructors (`default`, `initializer_list`, `unordered_set`); `operator[]`/`getItem` cross-checked against each other and `IsKeyDown`; `GetPressedKeys` both empty and multi-key ascending-order (`{Z, A, Space, D1}` → `{Space, D1, A, Z}`, exercising task 760's fix directly); `Equals`/`==`/`!=` for both equal and unequal states; `GetHashCode` consistency-for-equal-states, empty-state-is-zero, and a hand-computed word-XOR check (`A`=65→word2 bit1, `Space`=32→word1 bit0, hash=0x3) directly exercising task 761's formula; `ToString`'s task-766 fixed value; and `Keys`/`KeyState` numeric spot-checks). **Intentional scope limit, documented in the file's header comment:** `GetKeyFromScancodeEXT`'s scancode-mode branch (task 765) is not covered here — `SdlInputBridge.cpp`'s `use_scancode_mode()` caches `FNA_KEYBOARD_USE_SCANCODES` in a function-local `static bool` on first use, process-wide, and some earlier test in the shared `CnaTests` binary already triggers that first read (with the env var unset) before this file's tests run, so it can never be flipped to scancode mode within this process — matches why task 765 itself verified scancode mode with a separate one-shot process rather than a committed gtest case. 1910/1910 tests pass (18 net new; verified stable under `--gtest_filter='*Keyboard*' --gtest_shuffle --gtest_repeat=10` and the full suite under `--gtest_shuffle`). This closes out Phase I5. |

---

## Phase I6 — CHECKLIST / SPDX / NOXNA compliance and docs ✅ COMPLETE (tasks 775–778)

| # | Task | Status | Notes |
|---|------|--------|-------|
| 775 | Add `// SPDX-License-Identifier: MS-PL` to the four internal backend files: `CNA/Internal/Input/{SdlInputBridge,InputManager}.{hpp,cpp}` (all currently start with `#pragma once`/`#include`) | ✅ | Added the SPDX line as the first line of all four files (`.hpp` files: before `#pragma once`; `.cpp` files: before the first `#include`, matching the convention already used elsewhere in the codebase, e.g. `KeyboardState.cpp`). Confirmed via a sweep of every other file under `CNA/Internal/Input/*.hpp`/`*.cpp` (e.g. `GestureDetector.{hpp,cpp}`) that these were the only four missing it — no other files in that directory needed the same fix. 1910/1910 tests pass (no regressions; purely a comment addition, no test needed). |
| 776 | Sweep every `include/Microsoft/Xna/Framework/Input/**/*.hpp` for non-XNA members missing `NOXNA` (known: `GamePadCapabilities` EXT fields, `TextInputEXT` INTERNAL dispatchers, `MouseCursor`, `TouchCollection::empty()`, `TouchPanel::TouchDeviceExists`) and add the marker + `CNAHelper.hpp` include where missing | ✅ | All 5 "known" candidates were already correctly tagged (fixed in earlier sessions) — confirmed by cross-reading each against its FNA `.cs` source. The sweep found 7 real gaps instead, all confirmed against FNA source line-by-line: **(1)** `TouchLocation()`'s default constructor — FNA has no explicit parameterless ctor for `TouchLocation` (only the two multi-arg ones), same pattern as the already-tagged `MouseState()`/`KeyboardState()`/`GamePadThumbSticks()`/etc. default ctors; added `NOXNA` + `CNAHelper.hpp` include. **(2)** `TouchCollection()`'s default constructor — same pattern (FNA's `TouchCollection` has only the array-taking ctor); added `NOXNA` (header already had the include). **(3–4)** `TouchPanelCapabilities()`'s default ctor and `TouchPanelCapabilities(bool, int)` — FNA's `TouchPanelCapabilities` has exactly one constructor and it's `internal`, so both CNA ctors (the implicit-default equivalent, and the one mirroring FNA's sole internal ctor) needed `NOXNA`; added the tag + a class-level doc comment explaining there's no public XNA constructor at all, plus `CNAHelper.hpp` include. **(5–8, `TouchPanel.hpp`)** `EnqueueGesture`, `INTERNAL_onTouchEvent`, `SetFinger`, `Update` — all four are `internal` in FNA's `TouchPanel` (confirmed via `TouchPanel.cs`), exposed as plain public in CNA for cross-class access from `GestureDetector`/`SdlInputBridge`/`FrameworkDispatcher`, matching the exact rationale already used for the sibling `TouchDeviceExists` in the same file; added `NOXNA` + a matching doc note to each. Also specifically re-checked for a recurrence of task 766's opposite-direction mistake (a real XNA member incorrectly tagged `NOXNA`) by grepping every `ToString`/`Equals`/`GetHashCode`/`operator==`/`operator!=` across all Input headers for a stray `NOXNA` — none found, confirmed clean. Verified every enum (`Buttons`, `ButtonState`, `GamePadDeadZone`, `GamePadType`, `Keys`, `KeyState`, `GestureType`, `TouchLocationState`) has 1:1 value parity with its FNA source (no missing/extra values requiring documentation). 1910/1910 tests pass (no regressions; purely additive Doxygen/marker changes, no test needed). |
| 777 | Update `AUDIT.md` Input/Touch sections to distinguish **API surface** (complete) from **runtime behavior** (partial), and reflect the gaps closed by this plan; update `docs/xna-4-api-coverage.md` if present | ✅ | **`AUDIT.md`:** added a `Runtime` column to both the `Input` and `Input::Touch` tables (kept `Status` = API-surface completeness, unchanged meaning), with a short explanatory note above each table. Filled in `Runtime` for all 23 classes/enums across both namespaces: pure value enums get `N/A`; every class/struct gets `Real` with a per-row note citing the specific task(s) that made it real (e.g. `GamePad`→tasks 725–740, `TouchPanel`→tasks 710–712) and any still-open known deviations/bugs (Mouse's warp-scale gap, Touch's event-driven `GetState()` fallback, the `GetCapabilities()` `MAX_TOUCHES` bug) so the gap list stays honest rather than implying everything is now perfect. Caught and corrected one overclaim while writing this: `GamePadState::GetHashCode()` is **not** an FNA-faithful port (FNA's own is `base.GetHashCode()`, .NET's non-reproducible reflection-based default) — documented as an accepted deviation instead. **`docs/xna-4-api-coverage.md`:** updated only the Input/Touch-touching lines (namespace-coverage table, §4's `Input::Touch` subsection, §8's coverage-estimate table, §10's implementation-order line, §11's remaining-gaps list and recommended-next-steps list) — Touch moved from "Stub"/~10%/"0% functional" to "Implemented"/~90%/removed from both gap lists, matching Phase I2's real completion; GamePad/Keyboard/Mouse bumped 90%→97% reflecting Phases I3–I5. Left the document's other sections (Graphics, Audio, Content, etc.) and its top-line "~80% overall" untouched — those are outside this task's scope and weren't re-audited. No build/test step: docs-only change. |
| 778 | New `docs/input-backend.md`: `SdlInputBridge`/`InputManager` architecture, SDL3-event→XNA-state mapping table, event-driven-vs-FNA-polling deviation, and the per-device fidelity notes | ✅ | New `docs/input-backend.md`, 4 sections: **(1)** Architecture overview — the `SDL_PollEvent → SdlInputBridge::ProcessEvent → InputManager::Set*State/TouchPanel::INTERNAL_onTouchEvent → Get*State()` data-flow diagram, `SdlInputBridge`'s dual role (event funnel + public query methods), `InputManager`'s accumulate/snapshot model, and `GestureDetector` as the third touch-specific class. **(2)** A complete SDL3-event→XNA-state mapping table covering all 14 `SDL_EVENT_*` cases actually handled in `SdlInputBridge::ProcessEvent`'s switch (verified by reading every case directly, not from memory) — mouse motion/button/wheel, key down/up, text input/editing, finger down/motion/up, gamepad added/removed/button/axis — plus a note on the two independent touch-coordinate scaling paths (`to_touch_pixel_position`'s window-point scale feeding `InputManager`, vs. `TouchPanel::INTERNAL_onTouchEvent`'s `DisplayWidth`/`DisplayHeight` scale feeding `GestureDetector`) and a note that gamepad rumble/light-bar/gyro/accelerometer are on-demand queries, not part of the event stream. **(3)** The event-driven-vs-FNA-polling deviation, explained once at the architecture level (what keeps `InputManager`'s accumulated state current is `Game::Tick()`'s unconditional per-frame `PollEvents()`) plus 4 concrete instances where the difference is actually observable (multiple `Get*State()` calls per frame, `TouchPanel::GetState()`'s `InputManager`-fallback, `GamePadState.PacketNumber`'s bump-on-change-not-on-diff semantics, Mouse relative-mode's drain-on-read accumulator). **(4)** Per-device fidelity notes (Keyboard/Mouse/GamePad/Touch/TextInputEXT) summarizing what's real vs. what deviates, cross-referencing `AUDIT.md`'s new `Runtime` column (task 777) and `plan_input.md` phases for full detail rather than duplicating it. Every specific claim (function names, scaling behavior, env var semantics) was verified by reading the actual current source, not reconstructed from memory — this caught one inaccuracy while drafting (an early version claimed both touch-coordinate scaling paths used `TouchPanel::DisplayWidth`/`Height`; re-reading `to_touch_pixel_position` showed it actually uses `SDL_GetWindowSize`, corrected before finalizing). No build/test step: new docs-only file. **This is the last task in Phase I6 — Phase I6 is now complete.** |

---

## Phase I7 — Demo and integration coverage ✅ COMPLETE (tasks 780–783)

| # | Task | Status | Notes |
|---|------|--------|-------|
| 780 | `examples/demo_input`: add a text-entry panel that subscribes to `TextInputEXT::TextInput`/`TextEditing` and toggles `StartTextInput`/`StopTextInput` — exercises the Phase I1 path end-to-end (currently text input has zero demo coverage) | ✅ | `DrawTextPanel`: buffer cells (Backspace/Enter editing), last-byte 8 bit-LEDs, IME draft, active indicator, blinking caret; F1 toggles Start/Stop. Builds; runs crash-free 4s under native backend. Visual confirm needs a display (no Wayland screenshot tool here). |
| 781 | `examples/demo_input`: add vibration/rumble feedback and multi-pad rendering (`PlayerIndex::Two/Three/Four`); currently only `PlayerIndex::One` and no rumble | ✅ | **Rumble:** `Update()` now loops all 4 `PlayerIndex` slots each frame and calls `GamePad::SetVibration(playerIndex, leftTrigger, rightTrigger)` using that player's own trigger values from `GamePad::GetState(playerIndex)` — a harmless no-op (`SetVibration` returns `false`) for disconnected slots, so this is safe to call unconditionally. **Multi-pad rendering:** kept `DrawGamePad` (Player One's existing full-detail 310×580 panel) untouched; added a new `DrawGamePadMini(ox, oy, gp)` — a condensed 184×~160 layout (connection bar, DPad, ABXY diamond, shoulders, trigger bars, and two small thumbstick visualizers) — called once each for Players Two/Three/Four, stacked in a new compact section at `(800, 10, 204, 580)` that exactly matches the existing text-input section's right edge (`x=1014`), so no window resize was needed. Also added a distinct magenta "rumble" bar under each mini pad's trigger bar, mirroring the same trigger value that's being sent to `SetVibration`, so the trigger→rumble feedback loop is visually explicit rather than implied. Build clean (`cna_demo_input`); ran crash-free for 4s against the real X11 display in this environment (confirmed via process exit code, not a crash trace) — actual visual layout could not be screenshotted here (`import -window root` failed in this environment, same "no display-capture tool available" limitation already noted for task 780's text panel). 1910/1910 `CnaTests` unaffected (this task only touches `examples/demo_input/`, no library code). |
| 782 | Integration test: drive synthetic `SDL_EVENT_FINGER_*` through the bridge and assert a `GestureSample` (Tap, Flick) is dequeued via `TouchPanel::ReadGesture` — proves the Phase I2 wiring | ✅ | New `tests/CNA/Internal/Input/SdlInputBridgeTouchGestureTests.cpp`, 2 tests, both driven entirely through `SdlInputBridge::ProcessEvent` with synthetic `SDL_Event`s (`e.type`/`e.tfinger.*` populated directly, mirroring `SdlInputBridgeTextInputTests.cpp`'s established synthetic-event pattern) rather than `GestureDetector`'s direct API (which `GestureDetectorTests.cpp` already covers) — this is what makes it a genuine end-to-end proof of the real SDL entry point, from `SDL_EVENT_FINGER_DOWN/MOTION/UP` through `get_or_create_touch_id`'s fingerID→touchId remapping, `TouchPanel::INTERNAL_onTouchEvent`'s pixel scaling, and `GestureDetector`'s recognition, out to `TouchPanel::ReadGesture()`. `FingerDownUpThroughProcessEventProducesTap`: DOWN+UP at the same normalized position produces a `Tap` at the expected pixel position. `FingerMotionThroughProcessEventProducesFlick`: DOWN + two MOTION events (with a real `10ms` sleep between, matching `GestureDetectorTests.cpp`'s velocity-timing pattern) + UP produces a `Flick` with delta length ≥ `MIN_FLICK_VELOCITY`. Reused two existing reset patterns since both touch `process`-wide static state shared across test files: `GestureDetectorTests.cpp`'s neutral-press/release `ResetGestureDetector()`, and `TouchInputTests.cpp`'s two-read `ResetTouchState()` (a `Released` touch is only actually removed from `InputManager`'s map on its *second* `GetTouchState()` read via `RemoveAfterSnapshot`) — needed because `SdlInputBridge::ProcessEvent`'s finger cases call `InputManager::SetTouchState` in addition to `TouchPanel::INTERNAL_onTouchEvent`, unlike driving `GestureDetector` directly. 1912/1912 tests pass (2 net new); verified stable under `--gtest_filter='*Gesture*:*Touch*' --gtest_shuffle --gtest_repeat=10` and a full-suite `--gtest_shuffle` pass. |
| 783 | Integration/manual check: relative mouse mode + `SetPosition` cursor warp behave correctly (Phase I4) | ✅ | **Manual/integration check performed, not a new committed test** (per the task's own framing — see rationale below). Wrote an ad-hoc standalone harness (not committed) that creates a real, non-hidden SDL window, then: (1) calls `Mouse::SetPosition(50, 60)` while **not** in relative mode and reads the real OS cursor position back via `SDL_GetGlobalMouseState` (a genuine end-to-end check beyond the existing unit tests, all of which only assert `InputManager`'s internal bookkeeping — none actually verify the OS-level `SDL_WarpMouseInWindow` side effect took place); (2) enables relative mode via `Mouse::setIsRelativeMouseModeEXTProperty(true)` and confirms `SetPosition` is a genuine no-op at the OS level (cursor doesn't move); (3) disables relative mode and calls `SetPosition` again, confirming the warp works again with **no leftover "stuck" state** from the relative-mode round-trip. **Result: correct in both directions.** Under `SDL_VIDEODRIVER=x11` (this environment's ambient session is Wayland, `XDG_SESSION_TYPE=wayland`; the underlying X11 server here is XWayland), all three steps matched expectations exactly — `SetPosition(50,60)` moved the real cursor to precisely `windowPos + (50,60)`, relative mode fully blocked the following `SetPosition(999,999)`, and after disabling relative mode `SetPosition(120,130)` warped again to precisely `windowPos + (120,130)`. Also re-read `InputManager::GetMouseState()`'s source to confirm the two features don't interact badly at a deeper level: `SDL_EVENT_MOUSE_MOTION` unconditionally updates **both** the absolute position and the relative-delta accumulator regardless of mode (`SdlInputBridge.cpp`'s motion case), so the absolute `X`/`Y` fields don't go stale while relative mode is active — when relative mode is disabled, `GetMouseState()` correctly resumes reporting the last real absolute position rather than a frozen pre-relative-mode value, matching FNA's own `Mouse.GetState()` semantics (X/Y report the drained delta while relative mode is on, and the absolute position otherwise) exactly, not a CNA-specific quirk. **One environment-characterization finding, not a CNA bug:** under the ambient default driver (Wayland proper, no `SDL_VIDEODRIVER` override), `SDL_GetGlobalMouseState` silently returns `(0, 0)` regardless of any real cursor movement — Wayland's compositor security model restricts querying global cursor position outside your own window, a platform limitation (same root cause as this environment's already-documented lack of a working screenshot tool), not something CNA's `Mouse` implementation can or should work around. **Why no new committed test:** a test asserting on `SDL_GetGlobalMouseState`'s value would need to either force `SDL_VIDEODRIVER=x11` (silently changing the whole test binary's backend, an invasive one-test-affects-everything side effect with no established precedent anywhere else in the suite) or `GTEST_SKIP()` under native Wayland — but detecting "is this Wayland" reliably enough to skip cleanly isn't practical from inside a test, and a test that silently passes-by-coincidence on zero-vs-zero comparisons under Wayland would be worse than no test at all (false confidence). Existing unit coverage (`SetPositionUpdatesGetState`, `SetPositionIsNoOpWhenRelativeModeEnabled`, `IsRelativeMouseModeEXTRoundTripsThroughRealWindow`, all in `MouseInputTests.cpp`) already exercises every piece that *can* be verified portably; this task's contribution is the manual confirmation of the real OS-level warp side effect and the mode-round-trip, which those tests structurally can't check. **Also incidentally confirmed stale:** `NEXT.md` previously claimed "forcing X11 makes SDL exit" — re-ran the full `MouseInputTests.cpp`/`MouseCursorTests` suite (26 tests) under `SDL_VIDEODRIVER=x11` in this session and it passed cleanly; that note was outdated (environment-specific or since resolved) and has been corrected in `NEXT.md`. 1912/1912 `CnaTests` unaffected (no source change). **This is the last task in `plan_input.md` — all tasks 700–783 are now complete.** |

---

## Phase I8 — Post-completion follow-up ✅ COMPLETE (tasks 790–791)

> Added after Phases I1–I7 (tasks 700–783) were all completed and verified. These are small,
> previously-identified gaps that were explicitly deferred rather than fixed at the time (see
> each task's origin note below), not newly-discovered problems.

| # | Task | Status | Notes |
|---|------|--------|-------|
| 790 | Fix `TouchPanel::GetCapabilities()`: it passes `MAX_TOUCHES` unconditionally in both the connected and disconnected branches; FNA returns `0` for `MaximumTouchCount` when disconnected (`TouchPanel.cs`). Noted but not fixed in task 721 | ✅ | Confirmed the exact FNA formula first (`SDL3_FNAPlatform.cs:2265-2281`, `GetTouchCapabilities()`): `return new TouchPanelCapabilities(touchDeviceExists, touchDeviceExists ? 4 : 0)`. Only the `InputManager`-fallback branch (`TouchPanel.cpp`'s second `return`) needed the fix — the `touchDeviceExists_` branch already always returns `true` for `IsConnected` there, so `MAX_TOUCHES` was already correct in that branch. Changed `return TouchPanelCapabilities(!state.empty(), MAX_TOUCHES);` to compute `isConnected` once and pass `isConnected ? MAX_TOUCHES : 0`. Extended the existing `GetCapabilitiesFallsBackToInputManagerTouchStateWhenFlagIsUnset` test (`TouchInputTests.cpp`) — which already exercised both the disconnected and fallback-connected paths but only asserted `IsConnected` — to also assert `MaximumTouchCount` (`0` when disconnected, `MAX_TOUCHES` when connected via the fallback), rather than adding a new test case, since the existing one already had the right state set up for both branches. 1912/1912 tests pass (no new tests; extended one existing test); verified stable under `--gtest_filter='*Touch*' --gtest_shuffle --gtest_repeat=10`. |
| 791 | Reconfigure `cmake-build-vulkan`/`cmake-build-bgfx`: both build dirs' `CMakeCache.txt` currently have `CMAKE_HOME_DIRECTORY` pointing at the sibling `…/openeggbert/cna` repo instead of this `cna_input` checkout (confirmed still true as of `NEXT.md`'s last check). Reconfigure both (`rm -rf` + fresh `cmake -S … -B … -G Ninja -DCNA_GRAPHICS_BACKEND={VULKAN,BGFX} …`, mirroring how `cmake-build-debug` was already fixed for EasyGL), then build and run `CnaTests` on each to confirm the Phase I1–I7 input work compiles and passes on those backends too — EasyGL is the only backend verified so far | ✅ | **Correction to the task's premise:** `cmake-build-vulkan`/`cmake-build-bgfx` did not exist at all in this `cna_input` checkout — the "misconfigured, points at the sibling repo" build dirs `NEXT.md` referred to actually live in the sibling `…/openeggbert/cna` repo itself (verified: `ls` showed both dirs present there, absent here). So there was nothing to "reconfigure"; both were freshly configured from scratch instead. **Vulkan:** `cmake -S . -B cmake-build-vulkan -G Ninja -DCNA_GRAPHICS_BACKEND=VULKAN -DCNA_BUILD_TESTS=ON` configured cleanly (system Vulkan SDK 1.4.309 found; `glslc`/`glslangValidator` reported missing but non-fatal — shader-compile tools, not needed for this verification). `cmake --build cmake-build-vulkan --target CnaTests` built clean; **1912/1912 tests pass**, including all 165 Keyboard/Mouse/GamePad/Touch/Gesture/TextInput tests. **Bgfx:** `cmake -S . -B cmake-build-bgfx -G Ninja -DCNA_GRAPHICS_BACKEND=BGFX -DCNA_BUILD_TESTS=ON` fetches `bgfx.cmake` via `FetchContent` (network access confirmed available; configure took ~6.5 minutes for the git clone). Built clean; **1916/1916 tests pass** — 4 more than Vulkan/EasyGL's 1912, confirmed via a `--gtest_list_tests` diff to be exactly 4 pre-existing Bgfx-specific tests (`BgfxApiIsLinkedForBgfxBackend`, `BgfxRendererTypeDefaultIsSafeForPlatform`, `BgfxRendererTypeOverrideParsingWorks`, `BgfxRendererTypeOverrideRejectsInvalidValue`), unrelated to input; the same 165 input-related tests pass identically. **Conclusion: the Phase I1–I7 input work is now verified passing on all three configured backends (EasyGL, Vulkan, Bgfx)** with zero input-related failures or deviations on any of them. Both new build directories are correctly gitignored (confirmed via `git status`); nothing to commit from the builds themselves. |

---

## Phase I9 — Input hardening, reproducibility, and fidelity audit ✅ COMPLETE (tasks 792–840)

> All tasks done and verified on EasyGL/Vulkan/bgfx (task 840). Two tasks are **deferred by
> decision, not skipped**: 800/801 (`Mouse::SetPosition` inverse transform — graphics-layer scope)
> → `plan.md` task `a-0001`. Task 811 is **partial** (bridge SDL slot-assignment edge cases are
> hardware-gated). Real fixes found and made this phase: 816 (`GetGUIDEXT` format), 806 (`TextInput`
> → `char16_t`), 828 (touch display-size guard), 834 (singleton disposal), 790-adjacent doc fixes.

> **Origin:** external (ChatGPT Plus) review of the completed input work, relayed by the user
> 2026-07-04. Delivered by the reviewer titled "Phase I8", but **renamed to Phase I9 here** because
> a Phase I8 (tasks 790–791) already exists and is committed — keeping the reviewer's title would
> make the two Phase-I8 blocks contradict each other (plan rule 7). Task numbers (792–840) are
> unchanged from the review and do not collide with 790–791.
>
> **Two review premises corrected up front (verified 2026-07-04):**
> - *"Current zip/archive cannot configure because `third_party/SDL` is missing"* — in this git
>   checkout SDL/SDL_image/SDL_mixer are present and populated as submodules
>   (`git submodule status` clean). The premise applies to a source **zip export** that omits
>   submodules, not to a `git clone`. Task 793 is scoped accordingly (document the init command;
>   the guard below already exists).
> - *"ensure CMake fails with clear instructions if SDL is absent"* — already implemented:
>   `cmake/ThirdPartySDL.cmake:32–38` emits `FATAL_ERROR "Missing vendored '<dep>' … Run: git
>   submodule update --init --recursive"`. Task 793 verifies/documents this rather than adding it.

| # | Task | Status | Notes |
|---|------|--------|-------|
| 792 | Reconcile `plan_input.md` with the actual code state (stale per-device table, "Known limitations" Task-754-pending row, coverage "now" column). | ✅ | Updated the per-device wiring table's Keyboard/Mouse/MouseCursor rows (were still pre-I4/I5: "no scancode mode"/"SetPosition doesn't warp"/"no FromTexture2D/Dispose") to reflect Phases I4–I5; fixed the "Known limitations" table (Task 754 "pending"→"made, kept"; Task 734 env override + tasks 726–727 sensors marked implemented; horizontal-wheel/warp rows repointed to tasks 805/800); added a reading note to the coverage-% table clarifying the "now" column is the pre-plan baseline and task 838 produces the final split. Also fixed a stale "not fixed" note in `docs/input-backend.md` for `GetCapabilities()` (task 790 fixed it). Docs-only; no build impact. |
| 793 | Make the branch reproducibly buildable from a fresh checkout/archive: document `git submodule update --init --recursive`; confirm CMake fails clearly if SDL absent. | ✅ | Verified the CMake guard already exists (`ThirdPartySDL.cmake:32–38`: `FATAL_ERROR "Missing vendored '<dep>' … Run: git submodule update --init --recursive"`) and the README already documents the submodule init prominently (lines 14, 178–182, and every per-backend build block). Added a README note to the "Initialise Submodules" section covering the actual gap — a **source zip/tarball export** (GitHub "Download ZIP") omits submodule contents, so `third_party/SDL` is empty and CMake aborts with that error; clone via Git, or use `-DCNA_USE_SYSTEM_SDL=ON`. Docs-only. |
| 794 | Clean configure/build/test EasyGL: `cmake -S . -B cmake-build-input-easygl -G Ninja -DCNA_GRAPHICS_BACKEND=EASYGL -DCNA_BUILD_TESTS=ON`, build `CnaTests`, run Keyboard/Mouse/GamePad/Touch/Gesture/TextInput filter. | ✅ | Fresh `cmake-build-input-easygl` (rm -rf first), clean configure + `cmake --build … --target CnaTests` succeeded (exit 0). Full suite = **1912 tests**; input filter `--gtest_filter='*Keyboard*:*Mouse*:*GamePad*:*Touch*:*Gesture*:*TextInput*:*SdlInputBridge*'` = **165 tests from 22 suites, all PASSED**. |
| 795 | Repeat clean build/test for Vulkan. | ✅ | Fresh `cmake-build-input-vulkan` (`-DCNA_GRAPHICS_BACKEND=VULKAN`), clean configure + build (exit 0). Full suite = **1912 tests**; input filter = **165 tests, all PASSED** — identical to EasyGL. |
| 796 | Repeat clean build/test for bgfx. | ✅ | Fresh `cmake-build-input-bgfx` (`-DCNA_GRAPHICS_BACKEND=BGFX`; `bgfx.cmake` FetchContent clone), clean configure + build (exit 0). Full suite = **1916 tests** — 4 more than EasyGL/Vulkan, confirmed to be exactly the 4 bgfx-specific tests (`BgfxApiIsLinkedForBgfxBackend`, `BgfxRendererTypeDefaultIsSafeForPlatform`, `BgfxRendererTypeOverrideParsingWorks`, `BgfxRendererTypeOverrideRejectsInvalidValue`), input-unrelated. Input filter = **165 tests, all PASSED** — identical to the other two backends. |
| 797 | Add a documented command/script in `docs/input-backend.md` to run all input tests (`--gtest_filter` covering Input, Keyboard, Mouse, GamePad, Touch, Gesture, TextInput, SdlInputBridge). | ✅ | Added §5 "Running the input tests" to `docs/input-backend.md`: the exact submodule-init → configure → build → `--gtest_filter='*Keyboard*:*Mouse*:*GamePad*:*Touch*:*Gesture*:*TextInput*:*SdlInputBridge*'` command (165 tests), a `--gtest_shuffle --gtest_repeat=10` variant for the process-wide static-state suites, and how to swap the backend. Docs-only. |
| 798 | Audit `Mouse::WindowHandle`: convert public static field → `getWindowHandleProperty()`/`setWindowHandleProperty()` like `TextInputEXT`, unless deliberate reason not to. | ✅ | Confirmed FNA declares it `public static IntPtr WindowHandle { get; set; }` (a property, not a field) — so CNA's public static field violated CLAUDE.md's property convention. Converted to `getWindowHandleProperty()`/`setWindowHandleProperty()` backed by a private `windowHandle_`. **Not** `NOXNA`: `Mouse.WindowHandle` is real XNA 4.0 API (unlike `TextInputEXT`, whose whole class is a NOXNA extension). Updated the 3 internal reads in `Mouse.cpp` and the 3 test references in `MouseInputTests.cpp`; grep confirms no other call sites. |
| 799 | Ensure `Mouse` window handle is populated at window creation and cleared at destruction, like `TextInputEXT`. | ✅ | `GraphicsDevice.cpp`: added `Mouse::setWindowHandleProperty(window_)` right after the existing `TextInputEXT::setWindowHandleProperty` at window creation, and a matching guarded clear (`if (getWindowHandleProperty() == window_) setWindowHandleProperty(0)`) next to TextInputEXT's clear in `destroyNativeResources()`. Added the `Mouse.hpp` include. Mouse no longer relies solely on the `SDL_GetMouseFocus()` fallback. |
| 800 | Fix `Mouse::SetPosition` coordinate fidelity for scaled/letterboxed/backbuffer rendering (inverse logical→window transform via graphics backend abstraction). | ⛔ | **Deferred by user decision (2026-07-04).** Verified there is a forward `IGraphicsBackend::TransformWindowToLogical` but **no inverse** — implementing this requires adding a `TransformLogicalToWindow` virtual to `IGraphicsBackend` and every backend impl (graphics-layer change forbidden by NEXT.md §9 on this branch). Recorded as task **`a-0001` in `plan.md`** (cross-cutting/deferred plan) with full implementation scope; the in-source deviation note in `Mouse.cpp` stays until it's picked up on the graphics track. |
| 801 | Add backend/integration tests for `Mouse::SetPosition` with logical size ≠ physical window size. | ⛔ | Deferred together with 800 (nothing to test until the inverse transform exists) — folded into `plan.md` task **`a-0001`**. |
| 802 | Make `Mouse::getIsRelativeMouseModeEXTProperty()`/`setIsRelativeMouseModeEXTProperty()` safe with no SDL window (return false/no-op). | ✅ | Both now resolve the window first via `resolve_mouse_window(windowHandle_)` and guard: getter returns `false` when null, setter no-ops (and leaves `InputManager`'s relative mode untouched, since without a window there are no motion events to accumulate). New test `SetRelativeMouseModeIsSafeNoOpWithNoWindow`. |
| 803 | Make `Mouse::SetCursor()` safe when cursor is disposed/null internally (decide throw/no-op/FNA-match; document + test). | ✅ | **Decision: no-op** when `cursor.GetSDLCursor() == nullptr` (disposed/empty). Rationale documented in-source: `SDL_SetCursor(NULL)` forces a *redraw of the current cursor* rather than clearing it, so passing a disposed cursor through would silently keep the old cursor while looking like a change. CNA takes `MouseCursor&` (can't be null) so only the disposed-handle case is reachable (MonoGame throws on a null cursor; the reference param makes that unreachable here). New test `SetCursorIsSafeNoOpForDisposedCursor`. |
| 804 | Verify `Mouse::ClickedEXT` button numbering vs FNA (`INTERNAL_onClicked(event.button.button - 1)`) for L/R/M/X buttons; add synthetic-event tests. | ✅ | **Verified correct:** SDL button constants are 1-based (`SDL_BUTTON_LEFT=1` … `SDL_BUTTON_X2=5`), so `- 1` yields the 0-based indices Left=0/Middle=1/Right=2/XButton1=3/XButton2=4 — exactly matching FNA (`SDL3_FNAPlatform.cs:958-960`). New `tests/CNA/Internal/Input/SdlInputBridgeMouseTests.cpp` (2 tests) drives synthetic `SDL_EVENT_MOUSE_BUTTON_DOWN`/`_UP` through `SdlInputBridge::ProcessEvent` and asserts the fired index per button, and that button-UP never fires `ClickedEXT`. |
| 805 | Complete or explicitly close task 749 (horizontal scroll wheel): document why XNA/FNA can't expose it, or design a NOXNA extension + tests. | ✅ | **Closed as won't-implement.** Confirmed FNA's `MouseState.cs` has only the vertical cumulative `ScrollWheelValue` and **no** horizontal member; XNA 4.0 is the same. The bridge already ignores `event.wheel.x` — added an in-source comment at the `SDL_EVENT_MOUSE_WHEEL` handler documenting the intentional drop (a horizontal wheel would be a non-XNA NOXNA extension with no current consumer). Former task 749's ⬜ row is now resolved by this closure. |
| 806 | Audit `TextInputEXT::TextInput` type (`std::function<void(char)>`, per-UTF-8-byte): decide `char16_t`/`char32_t`/UTF-8-string event for correct Unicode/IME modeling. | ✅ | **Decision (user, 2026-07-04): `char16_t` (`charcs`).** This matches FNA's `event Action<char>` (C# `char` = UTF-16 code unit) and CLAUDE.md's `char → charcs` type mapping exactly — the old per-UTF-8-byte `char` was the deviation. `TextInput`/`INTERNAL_OnTextInput` are now `charcs`; the bridge's `SDL_EVENT_TEXT_INPUT` handler decodes SDL's UTF-8 to UTF-16 code units via a new self-contained `decode_utf8_to_utf16` helper (mirrors FNA's `Encoding.UTF8.GetChars`; astral code points → surrogate pairs), rather than reshaping sharp-runtime's byte-oriented `Encoding`. Updated consumers: `demo_input` (new surrogate-buffering `AppendTextCodeUnit` → UTF-8), both test files, and **NEXT.md §9** (the "don't change the char signature" freeze is now superseded). `TextEditing` deliberately stays a UTF-8 `std::string` (separate documented deviation). Full suite 1922/1922. |
| 807 | Add Unicode text-input tests: multi-byte UTF-8, combining chars, surrogate/emoji, IME composition; document chosen semantics. | ✅ | Added 5 tests to `SdlInputBridgeTextInputTests.cpp` driving real UTF-8 through `SdlInputBridge::ProcessEvent`: 3-byte `€`→single `0x20AC`; astral emoji `😀`→surrogate pair `0xD83D 0xDE00`; combining `e`+U+0301→two code units; mixed-width `aé€😀`→exact 5-code-unit sequence; and IME `TextEditing` multi-byte composition passes through as UTF-8 unchanged. Also rewrote the two pre-existing byte-oriented tests to the new UTF-16 semantics (é now = one `0x00E9` code unit, not two bytes). Input filter 170→175, all PASS. |
| 808 | Verify `TextInputEXT::SetInputRectangle` cursor parameter vs FNA/SDL3 (currently offset 0); add FNA reference comment. | ✅ | **Verified correct — matches FNA exactly.** FNA's `SetTextInputRectangle` (`SDL3_FNAPlatform.cs:779`) calls `SDL_SetTextInputArea(window, ref rect, 0)` — cursor offset `0`, with FNA's own `// FIXME SDL3: Do we need a cursor here?`. SDL3's third arg is the text cursor's x-offset relative to `rect->x` (an optional IME placement hint); CNA follows FNA's `0` rather than inventing one. Tightened the in-source comment to cite the exact FNA reference + FIXME. No behavior change. |
| 809 | Verify `IsTextInputActive`/`StartTextInput`/`StopTextInput`/screen-keyboard vs real SDL window behavior. | ✅ | Added `StartStopAndIsActiveRoundTripThroughRealWindow` to `TextInputEXTTests.cpp`: inits `SDL_INIT_VIDEO`, creates a real hidden window, publishes the handle, then `StartTextInput` → asserts `IsTextInputActive()` true → `SetInputRectangle` reaches SDL without error → `StopTextInput` → asserts false. Uses the `MouseInputTests` `GTEST_SKIP`-on-failure pattern (skips if the environment doesn't toggle text-input on a hidden window, avoiding IME/Wayland flakiness); here it **ran (not skipped) and passed**, exercising the real SDL path the no-window tests can't. |
| 810 | Add a fake/mockable SDL input backend for GamePad tests, or isolate gamepad state translation enough to test without hardware. | ✅ | **Chose the "isolate state translation" option, not a fake SDL layer.** The translation is *already* isolated: the bridge maps SDL enums → `InputManager::SetGamePad{Connection,ButtonState,AxisValue}` (public), and `GamePad::GetState` reads them back — so button/axis/connection behavior is fully testable headless (tasks 811–813). A full fake layer for SDL3's free-C gamepad API (`SDL_OpenGamepad`, `SDL_GetGamepadSensorData`, …) would be disproportionate; the SDL-device-bound paths (open, sensors, capability probes) are instead covered by the disconnected-fallback tests (task 740) + hardware/manual verification (demo checklist, task 836). Added `InputManager::ResetForTests()` (task 823) as the enabler so gamepad tests don't leak the process-wide state. |
| 811 | Gamepad hotplug tests: added, duplicate added, removed, removed-unknown, four slots, no free slots, env count < 4. | ⚠️ | **InputManager connection layer tested** (`GamePadMappingTests.cpp`): a slot connects in isolation, all four slots connect independently, disconnect→reconnect round-trips, and connecting bumps `PacketNumber`. The bridge's SDL slot-assignment edge cases (**duplicate-add, no-free-slots, `FNA_GAMEPAD_NUM_GAMEPADS` env count, removed-unknown**) run only for a real `SDL_OpenGamepad` device and are **not** unit-testable under the chosen no-fake-SDL-layer approach (task 810) — they remain hardware/manual-verified (demo checklist, task 836). Marked partial per plan rule 6 rather than claiming full coverage. |
| 812 | Gamepad button mapping tests: A/B/X/Y, shoulders, sticks, guide/big button, DPad, paddles, misc, touchpad — verify `Buttons` flags exactly. | ✅ | `GamePadMappingTests.cpp`: `EveryButtonMapsToItsXnaFlag` table-drives all **21** `GamePadButton`→`Buttons` mappings (A/B/X/Y, Back, Start, BigButton/Guide, both shoulders, both sticks, 4 DPad, Misc1EXT, 4 paddles, TouchPadEXT) through `SetGamePadButtonState`→`GamePad::GetState().IsButtonDown/Up`; plus a two-buttons-together test. |
| 813 | Gamepad axis normalization tests: both sticks, Y inversion, triggers, min/max/zero, deadzone interaction, packet-number-on-raw-change; cover `IndependentAxes`/`Circular`/`None`. | ✅ | `GamePadMappingTests.cpp`: thumbstick over-range clamps to ±1 (`None` dead zone), right stick stores mid/zero, triggers clamp to [0,1]. Dead-zone modes `IndependentAxes`/`Circular`/`None` already covered by `GamePadThumbSticksTests.cpp` (task 738); packet-number-on-change by `InputManagerTests` (task 729). **Y-axis inversion is a bridge-level SDL conversion** (`SDL_EVENT_GAMEPAD_AXIS_MOTION` negates stick Y before storage) that only runs for an opened SDL_Gamepad — noted in the test file as not-unit-testable-headless (hardware/manual). |
| 814 | Audit `GamePadCapabilities`: avoid side-effecting capability checks; verify rumble/trigger-rumble/lightbar/sensor/touchpad vs FNA/SDL3. | ✅ | **Audited — matches FNA, no side effects, no change needed.** The rumble/trigger-rumble capability is probed with a **zero-magnitude** `SDL_RumbleGamepad(gamepad, 0, 0, 0)` (no actual rumble) — exactly FNA's method (`SDL3_FNAPlatform.cs:2117-2128`). Everything else uses non-invasive SDL queries (`SDL_GamepadHasButton/HasAxis/HasSensor`, `SDL_GetNumGamepadTouchpads`, `SDL_GetBooleanProperty(...RGB_LED...)`), 1:1 with FNA's `GetGamePadCapabilities`. |
| 815 | `GamePadCapabilities` tests with a fake connected controller with partial capabilities; cover every XNA + EXT property. | ✅ | `GamePadMappingTests.cpp` `PartialCapabilitiesLeaveUnsetFlagsFalse`: an arcade-stick-like partial cap set (some buttons + one trigger + gyro + type) leaves every unset flag false and every set flag true — verifies setter independence. The exhaustive all-36-property round-trip is already in `GamePadTests.cpp` (task 740). |
| 816 | Verify `GamePad::GetGUIDEXT` formatting vs FNA/SDL; tests for null device, valid GUID, invalid joystick id. | ✅ | **Found and fixed a real fidelity bug.** CNA's `GetGUID` returned the **full 32-char `SDL_GUIDToString`**; FNA's `GetGamePadGUID` (`SDL3_FNAPlatform.cs:2176-2210`) returns `"xinput"` (vendor+product both 0) or **8 hex chars** = vendor then product IDs each little-endian, plus fixed Valve-controller overrides. Rewrote `GetGUID` to match, extracting a pure SDL-free `SdlInputBridge::FormatGamePadGUIDEXT(vendor, product)` so the valid-GUID case is unit-testable headless. New tests: `0,0`→`"xinput"`; DualShock4 `054C/05C4`→`"4c05c405"`, DualSense `054C/0CE6`→`"4c05e60c"`, Xbox360 `045E/028E`→`"5e048e02"`, arbitrary + zero-padding. Null/disconnected (`GetGUIDEXT`→`""`) already tested (task 740). |
| 817 | Verify `GamePad::SetVibration` duration semantics (passes 0 to SDL rumble); confirm vs FNA + doc/test. | ✅ | **Verified — matches FNA exactly.** FNA's `SetGamePadVibration`/`SetGamePadTriggerVibration` both pass `0` as the `SDL_RumbleGamepad`/`SDL_RumbleGamepadTriggers` duration (`SDL3_FNAPlatform.cs:1957,1973`), same as CNA. FNA's model is that games call `SetVibration` every frame to sustain rumble (CNA's `demo_input` does exactly this, task 781); duration `0` is correct, not a bug. Disconnected returns `false` (tested, task 740). |
| 818 | Verify `GamePad::GetGyroEXT`/`GetAccelerometerEXT`: sensor enabling, failure handling, zeroing, units, axes, repeat calls; tests with fake sensor data. | ✅ | **Audited — matches FNA.** `read_gamepad_sensor` (tasks 726–727) enables the sensor on first use (`SDL_GamepadSensorEnabled`/`SDL_SetGamepadSensorEnabled`), reads via `SDL_GetGamepadSensorData(..., 3)`, and **zeroes the output + returns false** on no-gamepad or read failure — 1:1 with FNA's `GetGamePadGyro`/`GetGamePadAccelerometer` (`SDL3_FNAPlatform.cs:1998-2071`). The failure+zeroing path is unit-tested (`GetGyroEXT`/`GetAccelerometerEXT` disconnected, task 740). Units/axis orientation come straight from SDL's sensor values (FNA does no conversion either) and can only be checked against real sensor hardware — noted, not headless-testable. |
| 819 | Audit Keyboard keycode/scancode maps vs FNA tables; list `Keys` values that intentionally can't map via SDL3. | ✅ | **Audited all three maps line-by-line vs FNA (`keyMap`/`scanMap`/`xnaMap`): faithful 1:1 ports — zero missing entries, zero value mismatches.** Only CNA-specific addition is `SDLK_AC_BACK → Keys::Escape` (Android back button, no FNA equivalent); FNA's two `Keys.None` keycode no-ops (`é`, `SDLK_UNKNOWN`) are rendered as `std::nullopt` (equivalent — key suppressed). Documented the **40 intentionally-unmappable `Keys`** (IME: Kana/Kanji/ImeConvert…; browser/media; ChatPad; OEM/system: Attn/Crsel/Pa1…) — matching FNA's `xnaMap` omissions exactly — in a source comment on `try_convert_keys_to_sdl_scancode` and in `docs/input-backend.md`. No code change (maps already correct). |
| 820 | Table-driven tests for `try_convert_sdl_key`/`try_convert_sdl_scancode` (expose test-only helper or drive via synthetic events): letters/digits/numpad/OEM/modifiers/function/media keys. | ✅ | New `tests/CNA/Internal/Input/SdlInputBridgeKeyboardTests.cpp`. Drives synthetic `SDL_EVENT_KEY_DOWN` through `SdlInputBridge::ProcessEvent` → `Keyboard::GetState().IsKeyDown`. Keycode map (default mode): 22-case table covering letters, digits, numpad, OEM (`OemSemicolon`/`OemComma`/`OemPeriod`), modifiers, function keys incl. `F1`/`F13`/`F24`, nav/whitespace, and media/system (`VolumeUp`/`Apps`/`Sleep`); plus unmapped-keycode-is-dropped (not marked `Keys::None`). Scancode map (via the new `SetScancodeModeForTests` hook): 7-case table + a test that the layout-dependent keycode is ignored in scancode mode. |
| 821 | Tests for `Keyboard::GetKeyFromScancodeEXT` in normal and `FNA_KEYBOARD_USE_SCANCODES=1` mode (subprocess/separate binary since env var is cached). | ✅ | Solved the cached-env-var problem **without** a subprocess: added test-only `SdlInputBridge::SetScancodeModeForTests(bool)`/`ClearScancodeModeForTests()` (overrides the once-cached `use_scancode_mode()`; production path unchanged when the override is unset). Tests both modes in one binary — scancode mode: `GetKeyFromScancodeEXT` is identity (layout-independent, robust on any CI); normal mode: `F13` round-trips (layout-invariant) and an unmappable `Keys::Kana` → `Keys::None`. |
| 822 | Verify Android keyboard logging path doesn't change behavior or introduce SDL problems off-Android (logs behind `__ANDROID__`). | ✅ | **Verified — correctly guarded, no behavior change.** Both Android logging blocks (`SdlInputBridge.cpp` ~1176-1202 and ~1230-1246) are entirely inside `#ifdef __ANDROID__`, so they compile out on non-Android. The actual key processing (`SetKeyState`, text-input synthesis) sits *outside* the guards and is identical on all platforms — the Android blocks only call `SDL_Log`/`SDL_GetKeyName` (core SDL3) to log. No off-Android SDL dependency or behavior difference. No code change. |
| 823 | Add `InputManager::ResetForTests()` (test-only) to stop tests leaking global input state. | ✅ | **Pulled forward** (from batch 823–830) as the enabler for the gamepad connection tests (811). Added `InputManager::ResetForTests()` — resets the process-wide `InternalInputState` (mouse, all 4 gamepad slots, pressed keys, touch) to defaults by reassigning a fresh struct. `GamePadMappingTests.cpp`'s fixture calls it in SetUp/TearDown; the full suite passes under `--gtest_shuffle` (no cross-test leakage). It's an internal-API method (CNA namespace, not XNA), clearly doc'd as test-only. |
| 824 | Add `GestureDetector::ResetForTests()` (test-only). | ✅ | Added `GestureDetector::ResetForTests()` (resets all file-static state to idle) plus a companion `TouchPanel::ResetForTests()` (touch arrays, gesture queue, `touchDeviceExists_`, enabled gestures) since `TouchPanel` also holds leaky process-wide statics. `GestureDetectorTests.cpp` and the new `TouchEdgeCaseTests.cpp` use them in SetUp/TearDown instead of the old neutral-press-cycle hack — verified stable under `--gtest_shuffle --gtest_repeat=10`. |
| 825 | Audit `TouchPanel::GetState()` `touches_`→`InputManager` fallback: no double-reporting/stale state when `SetFinger` + real SDL finger events mix; tests both + mixed. | ✅ | **Confirmed the fallback is EXCLUSIVE, not additive — so there's no double-reporting.** `GetState()` returns the `touches_` array if it has any valid entry, *else* falls back to `InputManager`'s event-driven snapshot; it never unions the two. In production `touches_` stays empty (only `SetFinger` fills it, which the real path doesn't call), so the fallback is always used. `TouchEdgeCaseTests.cpp`: fallback-when-touches-empty, and mixed `SetFinger`+`InputManager` returns exactly one touch (the `touches_` one) — proving the priority + no double-count. |
| 826 | Multi-touch edge tests: > `MAX_TOUCHES`, release unknown finger, repeat down same id, id reuse after release; verify `InputManager` + `GestureDetector`. | ✅ | `TouchEdgeCaseTests.cpp`: release-unknown-finger is a safe no-op (both `InputManager` and `INTERNAL_onTouchEvent` paths); repeated down same id overwrites (not duplicate); id-reuse-after-release starts fresh (via the two-read `RemoveAfterSnapshot` flush). **Documented deviation:** the event-driven `InputManager` touch map is **not** capped at `MAX_TOUCHES` (that bounds only the `SetFinger` `touches_` array) — 10 fingers → 10 touches; FNA caps at 8. Kept as-is (which finger to drop would be arbitrary), documented in the test + here. |
| 827 | `TouchPanel::GetCapabilities()` tests: before/after touch, after reset, fallback state exists but `touchDeviceExists_` false; confirm `MaximumTouchCount==0` when disconnected. | ✅ | `TouchEdgeCaseTests.cpp`: disconnected-before-any-touch (`IsConnected` false, `MaximumTouchCount` 0), connected once `touchDeviceExists_` set (`MAX_TOUCHES`), connected via `InputManager` fallback with the flag still unset (`MAX_TOUCHES`), and disconnected again after `ResetForTests` (0). Complements task 790's original fix. |
| 828 | Verify touch coordinate scaling with `DisplayWidth/DisplayHeight` zero/normal/resized/non-integer; avoid bogus all-zero gestures before display size published. | ✅ | **Fixed:** added a guard at the top of `INTERNAL_onTouchEvent` — if `displayWidth_ <= 0 || displayHeight_ <= 0` the event is dropped, so a touch arriving before `GraphicsDevice` publishes the display size no longer collapses to a bogus `(0,0)` corner gesture. (In the normal flow the size is set at `GraphicsDevice` creation before any input, so this never fires in production.) `TouchEdgeCaseTests.cpp`: zero-size→no gesture, normal `1000`→`(500,500)`, resized `500×400`→`(250,200)`, non-integer `0.6667/0.3333`→rounded `667/333`. |
| 829 | Audit `GestureDetector` timing thresholds vs FNA: tap, double-tap, hold, flick, drag threshold, pinch complete. | ✅ | **Audited vs FNA's `GestureDetector.cs` — byte-faithful port, every constant/formula matches:** `MOVE_THRESHOLD=35`, `MIN_FLICK_VELOCITY=100`, DoubleTap window `≤300ms` + distance `≤35px`, Tap `<1s`, Hold `≥1s`, flick velocity `delta/(0.001+dt)` low-passed at `0.45`. Sole deviation: CNA uses `std::chrono::steady_clock` where FNA uses `DateTime.UtcNow` — a deliberate improvement (monotonic, immune to wall-clock changes; identical durations). No code change. |
| 830 | Deterministic gesture tests with an injectable clock/time source instead of real `steady_clock` sleeps. | ✅ | Added an injectable test clock to `GestureDetector` (`EnableTestClock`/`DisableTestClock`/`AdvanceTestClockMilliseconds`; `Now()` uses it when enabled, else `steady_clock`). Rewrote `GestureDetectorTests.cpp`'s Hold/DoubleTap/Flick tests to advance the clock instead of `sleep_for` — deterministic and fast. The clock starts at a 1-hour base so a legit `t=0` event never collides with the flick calc's `TimePoint{}` "no prior update" sentinel. **Gesture suite now runs in ~11ms (was ~1.7s of sleeps); full suite 111ms.** Verified stable under `--gtest_shuffle --gtest_repeat=10`. |
| 831 | Verify `MouseCursor::FromTexture2D` pixel format/lifetime: confirm `SDL_CreateColorCursor` copies pixels before local vector destroyed; tests if possible. | ✅ | **Verified correct against SDL3 source** (`third_party/SDL/src/events/SDL_mouse.c`): `SDL_CreateSurfaceFrom` only *references* the local `rgba` buffer, but `SDL_CreateColorCursor` **copies** — because CNA passes `RGBA32` (≠ the cursor's required `ARGB8888`), SDL makes an independent `SDL_ConvertSurface` copy, the platform builds its own cursor from it, and SDL destroys that copy before returning. So freeing `surface` + letting `rgba` go out of scope right after is safe. Format also confirmed: `Color::PackedValue`'s R,G,B,A byte layout matches `SDL_PIXELFORMAT_RGBA32`, which SDL converts to `ARGB8888`. Documented the lifetime in a source comment on `FromTexture2D`; added a headless smoke test (`ColorCursorSurvivesSourcePixelBufferDestruction`) driving the exact `CreateSurfaceFrom → CreateColorCursor → destroy surface → scribble+free buffer → cursor still usable` pattern (`FromTexture2D` itself needs a `GraphicsDevice`, so it's exercised via the raw SDL pattern rather than a `Texture2D` — task 832 adds the `Texture2D`-fixture path). |
| 832 | `MouseCursor::FromTexture2D` tests with a minimal/fake `Texture2D`: Color, ColorSrgb, invalid format, invalid origin, disposed texture. | ✅ | **User-approved (2026-07-04) small graphics-layer test-support addition:** new NOXNA `Texture2D::CreateCpuOnlyForTests(w, h, format, pixels)` builds a CPU-only texture (sets width/height/`format_`/`cpuPixels_`, null backend) — no GraphicsDevice, no rendering change; mirrors the existing device-less `Texture2D(assetName)` ctor + NOXNA `CreateFromPixels` precedent. This unblocks headless `FromTexture2D` unit tests (the reason task 755 originally skipped them). New tests in `MouseInputTests.cpp`: Color→valid cursor, ColorSrgb→valid cursor, invalid format (`Bgr565`)→`std::invalid_argument` (thrown before any SDL call), out-of-bounds origin `(100,100)`→`std::runtime_error` (SDL rejects the hot spot). **Disposed-texture case omitted:** `Texture2D::GetData` has no `isDisposed_` guard (a graphics-layer concern, out of scope here), so a disposed CPU-only texture still yields pixels — noted rather than asserting on graphics disposal semantics. |
| 833 | Verify all `MouseCursor` system cursors map to closest MonoGame/FNA cursor; document unavoidable SDL differences. | ✅ | **Audited all 12 stock cursors vs MonoGame — each maps to the closest SDL3 system cursor.** MonoGame targets SDL2's `SDL_SYSTEM_CURSOR_*` names; SDL3 renamed the enum, so CNA uses the SDL3 equivalents (Arrow→`DEFAULT`, IBeam→`TEXT`, Hand→`POINTER`, No→`NOT_ALLOWED`, SizeAll→`MOVE`, Size*→`*_RESIZE`, etc.) — pure renames of the same glyphs. **Only genuine difference:** MonoGame's `WaitArrow` used SDL2's `SDL_SYSTEM_CURSOR_WAITARROW`, which **SDL3 removed**; CNA maps it to SDL3's `SDL_SYSTEM_CURSOR_PROGRESS` ("WAIT with an arrow") — the closest match and exactly what WaitArrow conveyed (task 753 already renamed CNA's cursor to `WaitArrow` for this). Documented the full mapping + the OS/theme-chooses-the-glyph caveat in a source comment on `MouseCursor.cpp`. No code change; the existing `StockCursorsAreNonNull` test already confirms all 12 construct. |
| 834 | `MouseCursor` IDisposable tests: double dispose, move ctor, move assignment, system-singleton disposal policy (don't free shared singletons). | ✅ | Double-dispose, move-ctor, and move-assignment are already covered on owned cursors (tasks 752/755). The gap — and a real latent hazard — was the **system-singleton disposal policy**: stock cursors are owning singletons returned by reference, so `getArrowProperty().Dispose()` would free the shared SDL cursor and break it process-wide (and risk a free-after-`SDL_Quit` at static teardown). **Fixed:** added an `isSystemSingleton_` flag (set by `MakeSystem`, carried through the move ops) that makes `Dispose()`/destructor a **no-op** for stock cursors. New test `DisposingAStockSingletonIsANoOpAndKeepsItUsable` (dispose twice → `GetSDLCursor()` unchanged, fresh getter returns the same instance); documented the policy (+ "don't `std::move` a stock reference") on `Dispose()`. Verified stable under `--gtest_shuffle --gtest_repeat=10` (the guard is also what keeps the other stock-cursor tests order-independent). |
| 835 | Audit thread-safety of static input state; document input APIs as main-thread/game-loop unless intended otherwise. | ✅ | **Audited: zero locking anywhere** under `src/CNA/Internal/Input/` or `src/Microsoft/Xna/Framework/Input/` (no `mutex`/`atomic`/`thread`), and that's correct — input is a **single-threaded (game-loop-thread) API**: writes come from `Game::PollEvents()` → `SdlInputBridge::ProcessEvent` → `InputManager::Set*`, reads from game `Update()`/`Draw()` via `Get*State()`, all on the one thread (matching XNA/FNA, and required by SDL — `SDL_PollEvent` must be pumped on the video/window thread). The function-local-static singletons rely on C++11 thread-safe *initialization*; their mutation is unsynchronized, which is fine under the single-thread contract. Documented in a new `docs/input-backend.md` §6 "Thread safety" + a `@note` on `InputManager`'s class doc. **No locking added** (unnecessary per-frame overhead), per the task's guidance. |
| 836 | Add a manual `examples/demo_input` verification checklist to docs (keys, text/IME, warp, relative, wheel, touch gestures, 4 pads, rumble, sensors). | ✅ | New `docs/demo-input-checklist.md`: build/run steps, a table of what each panel shows, and per-device manual checklists (keyboard, text input + IME incl. accents/emoji/composition, mouse position/buttons/wheel, multi-touch, 4 gamepads with analog triggers + rumble). **Honestly flags what the demo does NOT surface** — relative mouse mode, `SetPosition` warp, gamepad sensors (gyro/accel), light bar, and touch *gestures* (the demo shows raw points) — pointing each to where it *is* verified (unit tests / task 783 / `plan.md` a-0001) so the checklist doesn't imply coverage it lacks. |
| 837 | Add platform notes (Linux/X11, Wayland, Windows, Android, iOS) for input limitations (warp/global pos on Wayland; touch/screen keyboard on mobile). | ✅ | New `docs/platform-input-notes.md`, one section per platform, each item marked **verified** (with the task) vs. documented SDL/OS behavior: X11 (warp/global-pos pixel-exact, task 783); **Wayland** (global cursor pos restricted → `(0,0)`, task 783; absolute warp constrained, relative mode = pointer lock works); Windows (XInput → `"xinput"` GUID, task 816); **Android** (touch primary + noticed-only-once, task 712; on-screen keyboard; `__ANDROID__` log path, task 822); iOS (touch-only, no cursor, on-screen keyboard). Plus a cross-cutting section (letterbox warp → `a-0001`, scancode mode + 40 unmappable keys, no horizontal wheel, main-thread-only). |
| 838 | Recalculate realistic input coverage split into XNA 4.0 core / FNA EXT / MonoGame MouseCursor / NOXNA / platform-dependent. | ✅ | Replaced the old blended estimate table with the authoritative **category-split** coverage in the "XNA 4.0 Input API coverage — final split" section above: (1) **XNA 4.0 core** ~99% behavior / ~99% tested — complete & FNA-faithful; (2) **FNA EXT** ~95% behavior / ~85% tested — all implemented, untested slice hardware/IME-gated; (3) **MonoGame `MouseCursor`** complete for the exposed surface; (4) **CNA/NOXNA-only** (internal backend + test hooks); (5) **platform-dependent/hardware-gated** items enumerated with their dependency. Per the review's rule, **no blended "input is 100% complete" number is claimed** — the categories are kept separate. |
| 839 | Update `docs/xna-4-api-coverage.md` with final verified input status. | ✅ | Updated the Input rows to the by-category assessment (XNA core ~99% / FNA EXT ~95% / MonoGame `MouseCursor` / platform-dependent — not blended), matching task 838's split; the `Input::Touch` section and §8 estimate rows; and the §10 Input summary (Phases I1–I9, tasks 700–840, with pointers to the final-split table + `platform-input-notes.md` + `demo-input-checklist.md`). **Fixed the stale doc claim** that `GetCapabilities()` still had the disconnected-count bug — task 790 resolved it. Bumped the doc's update date/line for Phase I9. |
| 840 | Final clean run: build + run full `CnaTests` on EasyGL, Vulkan, bgfx after all input changes; record exact commands/results here. | ✅ | Rebuilt `CnaTests` in all three configured build dirs (`cmake-build-input-{easygl,vulkan,bgfx}`, `cmake --build … --target CnaTests`) after every Phase I9 change, then ran the full suite + the input filter (`--gtest_filter='*Keyboard*:*Mouse*:*GamePad*:*Touch*:*Gesture*:*TextInput*:*SdlInputBridge*'`) on each. **Results — all green:** EasyGL **1961/1961** (214 input), Vulkan **1961/1961** (214 input), bgfx **1965/1965** (214 input; +4 pre-existing bgfx-specific tests, input-unrelated). The input suite grew 165→214 (+49 Phase I9 tests), identical on all three backends. **Phase I9 is complete.** |

**Implementation order:** 792–797 (reproducibility) → 798–805 (Mouse) → 806–809 (TextInput/Unicode)
→ 810–818 (GamePad) → 819–822 (Keyboard) → 823–830 (global state/Touch/Gesture) → 831–834
(MouseCursor) → 835–840 (docs/platform/coverage/final verification). Build `CnaTests` + run the
input filter + update this plan + commit source/docs/tests (not build dirs) after each small batch.

**Do not claim "XNA 4.0 Input is complete"** until task 838's coverage table separates: XNA 4.0
core API · FNA extensions · MonoGame-inspired APIs · CNA/NOXNA-only APIs · platform-dependent
behavior.

---

## XNA 4.0 Input API coverage — final split (task 838)

> **This is the authoritative coverage assessment** (as of Phase I9). Per the review's rule,
> coverage is split by API *category* — XNA 4.0 core / FNA extensions / MonoGame-inspired /
> CNA-NOXNA-only / platform-dependent — and **not** blended into one "XNA 4.0 Input is complete"
> number, because the categories mean different things. "Surface" = API present with FNA-faithful
> signatures; "Behavior" = FNA-faithful runtime behavior wired to SDL3; "Tests" = automated
> `CnaTests` coverage. The pre-plan estimate table this replaces is in git history.

### 1. XNA 4.0 core Input API

| Area | Surface | Behavior | Tests | Notes |
|------|---------|----------|-------|-------|
| Enums (`Buttons`, `Keys`, `KeyState`, `ButtonState`, `GamePadType`, `GamePadDeadZone`, `GestureType`, `TouchLocationState`) | 100% | 100% | 100% | 1:1 value parity with FNA (task 776 audit). |
| Keyboard / `KeyboardState` | 100% | 100% | 100% | Ordered `GetPressedKeys`, FNA `GetHashCode`, indexer, complete keycode/scancode maps (I5; 819–821). |
| Mouse / `MouseState` | 100% | ~99% | 100% | Warp/relative-mode/`ClickedEXT` wired (I4). Sole gap: `SetPosition` letterbox scale factor → platform-dependent (a-0001). |
| GamePad value types + dead-zone math (`GamePadState/Buttons/DPad/ThumbSticks/Triggers`) | 100% | 100% | 100% | Dead-zone/clamp/packing line-for-line FNA; button/axis mapping tested (812/813). |
| GamePad `GetState` / connection | 100% | 100% | ~95% | Connection + button/axis via InputManager tested; bridge SDL slot-assignment edge cases hardware-gated (811, see §5). |
| Touch `TouchPanel.GetState` / `TouchCollection` / `TouchLocation` | 100% | ~98% | 100% | Event-driven `GetState` fallback (documented deviation, 825); `>MAX_TOUCHES` uncapped vs FNA's 8 (documented, 826). |
| Touch gestures (`GestureDetector`, Tap…Pinch, `GestureSample`) | 100% | 100% | 100% | Byte-faithful FNA port (829); deterministic tests (830). |
| **XNA 4.0 core — overall** | **100%** | **~99%** | **~99%** | Complete and FNA-faithful; residual gaps are documented platform-dependent items, not missing API. |

### 2. FNA extensions (`*EXT`)

| Area | Surface | Behavior | Tests | Notes |
|------|---------|----------|-------|-------|
| `TextInputEXT` (text input + IME) | 100% | ~95% | ~90% | Wired to SDL3; `TextInput` is `char16_t`/UTF-16 (806); Unicode/emoji/composition tests (807). Live IME needs a real IME. |
| `Mouse` EXT (`IsRelativeMouseModeEXT`, `ClickedEXT`) | 100% | 100% | 100% | Real SDL relative mode + click dispatch; null-safe (802–804). |
| `GamePad` rumble/EXT buttons (`SetVibration`, `SetTriggerVibrationEXT`, EXT `Buttons`) | 100% | 100% | ~90% | FNA-faithful (duration 0, verified 817). Live rumble needs hardware. |
| `GamePad` `GetGUIDEXT` | 100% | 100% | ~90% | Format fixed to FNA's `xinput`/8-hex/Valve logic (816); formatter unit-tested; live device needs hardware. |
| `GamePad` sensors/LED (`GetGyroEXT`, `GetAccelerometerEXT`, `SetLightBarEXT`) | 100% | ~90% | ~60% | Enable/read/zero-on-failure faithful (726/727/818); only the disconnected path is unit-testable — live values are hardware-gated (§5). |
| `Keyboard.GetKeyFromScancodeEXT` + scancode mode | 100% | 100% | 100% | Both modes tested via a test override (821). |
| **FNA EXT — overall** | **100%** | **~95%** | **~85%** | All implemented and FNA-faithful; the untested slice is hardware/IME-gated, not missing logic. |

### 3. MonoGame-inspired

| Area | Surface | Behavior | Tests | Notes |
|------|---------|----------|-------|-------|
| `MouseCursor` (12 stock cursors, `FromTexture2D`, `IDisposable`) | ~100% | 100% | ~95% | No XNA/FNA equivalent (`NOXNA class`). Stock-cursor mapping audited (833); `FromTexture2D` (832) + singleton-dispose policy (834) tested. |

### 4. CNA / NOXNA-only (internal + test support)

| Area | Notes |
|------|-------|
| `SdlInputBridge` / `InputManager` / `GestureDetector` | Internal backend (not public XNA API). Fully functional; covered directly (bridge/gesture/mapping tests) and indirectly. |
| Test-only hooks | `InputManager::ResetForTests` (823), `GestureDetector::{ResetForTests,EnableTestClock,…}` (824/830), `TouchPanel::ResetForTests`, `SdlInputBridge::SetScancodeModeForTests` (821), `Texture2D::CreateCpuOnlyForTests` (832). NOXNA/test-support, no runtime-path effect. |

### 5. Platform-dependent / hardware-gated (not headless-verifiable)

These are correct-by-code-review + FNA-faithful, but their observable behavior depends on the OS,
compositor, or physical hardware and can't be fully asserted in the headless `CnaTests` suite. See
[`docs/platform-input-notes.md`](docs/platform-input-notes.md) and
[`docs/demo-input-checklist.md`](docs/demo-input-checklist.md).

| Item | Dependency |
|------|-----------|
| `Mouse::SetPosition` letterbox scale factor | Graphics-layer inverse transform (deferred → `plan.md` a-0001) |
| Wayland global cursor position → `(0,0)` | Wayland compositor security model (task 783) |
| Gamepad live gyro/accelerometer values, rumble strength | Physical controller with sensors/haptics |
| Gamepad hotplug slot-assignment (dup / no-free / env-count / removed-unknown) | Real `SDL_OpenGamepad` device (task 811 — partial) |
| `GetGUIDEXT` platform format (`xinput` vs vendor/product) | OS controller driver (task 816) |
| On-screen keyboard, touch-noticed-once | Mobile (Android/iOS) |

### Summary

**XNA 4.0 core Input: complete and FNA-faithful (~99% behavior, ~99% tested).** FNA extensions:
all implemented (~95% behavior; untested slice is hardware/IME-gated). MonoGame `MouseCursor`:
complete for the exposed surface. The only non-cosmetic deferral is the graphics-layer
`SetPosition` letterbox transform (`plan.md` a-0001). No blended "input is 100% complete" figure is
claimed — see the per-category numbers above.

---

## Known limitations / deferred

| Item | Reason |
|------|--------|
| `MouseCursor` | No FNA counterpart (MonoGame-derived); **status decision made — kept** (Task 754, recorded in `AUDIT.md`), CHECKLIST-compliant as of tasks 750–753 |
| Gamepad sensors/touchpad event stream | Capabilities advertise gyro/accel/touchpad; on-demand reads **implemented** (`GetGyroEXT`/`GetAccelerometerEXT`, tasks 726–727), not an event stream — by design |
| `FNA_GAMEPAD_NUM_GAMEPADS` env override | **Implemented** (Task 734): `effective_gamepad_count()` parses the env var, clamped to the 4 frozen `PlayerIndex` slots |
| Horizontal scroll wheel | This FNA `MouseState` has no horizontal-scroll member; still open, tracked as Phase I9 **task 805** (complete-or-explicitly-close, was task 749) |
| `Mouse::SetPosition` warp scale-factor deviation | `SDL_WarpMouseInWindow`'s target has no inverse (logical→window) coordinate transform, so it's off by the scale factor on a letterboxed/scaled window (documented in-source in `Mouse.cpp`; verified correct when window size matches logical/render resolution — task 783). Fixing it needs an `IGraphicsBackend` addition (graphics-layer scope). Phase I9 tasks 800/801 evaluated this and, per user decision (2026-07-04), **deferred** it to the cross-cutting plan as **`plan.md` task `a-0001`** (full implementation scope recorded there), since NEXT.md §9 forbids graphics-layer changes on this branch |

---

## Reference

- FNA input source: `/rv/data/library/github.com/FNA-XNA/FNA/src/Input/`
- FNA platform input: `/rv/data/library/github.com/FNA-XNA/FNA/src/FNAPlatform/SDL3_FNAPlatform.cs`
- Per-file porting requirements: `CHECKLIST.md`
- Internal backend bridge: `src/CNA/Internal/Input/{SdlInputBridge,InputManager,GestureDetector}.cpp`
- Demo: `examples/demo_input/`
