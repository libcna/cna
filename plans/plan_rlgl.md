# RLGL Renderer Plan

> **Retired renderer — historical record.** The `RLGL` renderer identity was retired on 2026-09-17; its implementation was removed from the tree and its C ABI value 51 is permanently reserved. This plan is kept unchanged below as the record of that work; nothing in it describes current CNA support, and paths it names under `modules/renderers/` no longer exist. See `plans/plan_renderer_cleanup.md` and `docs/removed-renderers.md`.

**Status:** Complete — RLGL-022 declares EasyGL-equivalent classic XNA 4.0 renderer parity on the validated Linux SDL3-offscreen/Mesa configuration; explicitly unvalidated platforms and modern CNAEXT facilities remain listed separately
**Target identity:** `RLGL`
**Primary parity reference:** EasyGL
**Initial profile:** OpenGL 3.3 core
**Upstream baseline:** raylib 6.0, commit `dbc56a87da87d973a9c5baa4e7438a9d20121d28`, standalone `src/rlgl.h`

## Status legend

- ✅ **Completed** — implementation or evidence exists and the recorded validation passed.
- 🟨 **In progress** — work exists, but the task's validation gate has not passed.
- ⬜ **Not started** — no implementation should be inferred.
- ⛔ **Blocked / intentionally unsupported** — the exact technical or product reason is recorded.

Only ✅ items count as complete. Compiling code alone is not sufficient evidence for a rendering-capability task.

## Status summary

- Repository architecture, EasyGL responsibilities, platform GL ownership, renderer selection, resource interfaces, effect flow, test infrastructure, and upstream rlgl 6.0 have been audited.
- `RLGL` is a registered compile-time/runtime renderer identity backed by a SHA-256-verified standalone-rlgl dependency target. Its factory now constructs a real CNA-owned OpenGL 3.3 core context and standalone rlgl device.
- The first vertical slice is runtime-validated: selection → CNA-created GL context → rlgl loader/init → `GraphicsDevice` → selective clear → readback → present → resize → shutdown, including two sequential device lifecycles.
- The renderer now implements the common owning-thread context lease. Nested operations share one
  outer policy, complete frames and background content loads are mutually exclusive, CNA's single
  RLGL context moves between native threads, and an unrelated caller GL binding is restored exactly.
- Every implemented device-child resource now joins one renderer lifetime registry. Destruction is
  serialized with context ownership, device shutdown releases children in reverse registration
  order before MojoShader/rlgl/context teardown, and C++ objects that outlive the device retain no
  native handles and issue no late GL calls.
- A recovery subset of that registry now retains and measures exact format-native Texture2D mip
  bytes, RGBA8 Texture3D mip volumes, face/mip TextureCube shadows including partial Color and compressed-block merges, complete
  vertex/index buffer bytes and declarations, target attachment descriptions, fixed pipeline/query
  descriptions, and optional compiled-effect source/value state. Render targets retain no fabricated
  pixels and are classified for the existing `ContentLost` resource contract. Disabling context
  recovery gates future registrations and shadows without weakening deterministic native teardown.
- The renderer now has an explicit lost/recreating/unavailable/available transaction. A loss closes
  `CanBeginDrawEXT`, invalidates old-context identities without GL deletion, and recreation replaces
  the CNA platform context, reloads standalone rlgl, restores renderer-owned samplers, fixed
  pipelines, optional MojoShader core, limits, presentation settings, and every recovery-registered
  child before rebinding active targets and deterministic device state. Texture and buffer contents
  are restored exactly, target contents use the existing `ContentLost` contract, and SpriteBatch,
  occlusion-query, and compiled-effect native state is rebuilt. Child failure rolls every replacement
  identity back and leaves the same retryable unavailable state; repeated complete cycles pass.
- Context creation now requests robust-access/lose-on-reset semantics, records whether the platform
  granted them, loads core/KHR/ARB graphics-reset polling when present, and converts a detected reset
  or failed present into the same once-only lost transaction. Ordinary `GraphicsDevice::Reset()` is
  deliberately presentation-only: it raises Resetting/Reset while status stays Normal, retains the
  live context and resources, reports the actual platform color/depth attributes, and transactionally
  rebuilds renderer-owned backbuffer MSAA for the requested device-clamped count. Creation and bind
  failures roll back transactionally.
- Every classic-XNA Texture2D format is runtime-validated. Uncompressed layouts preserve exact XNA bytes and sampling swizzles; DXT1/3/5 use native S3TC storage when rlgl detects it and otherwise decode to RGBA8 renderer storage while retaining exact compressed block-transfer/readback semantics. Both DXT paths have pixel and transfer evidence.
- Plain `TextureCube` is runtime-validated for every format the current CNA public cube-transfer contract can express: exact `Color` upload/readback plus native-or-decoded DXT1/DXT3/DXT5 block upload and RGBA readback. Six faces, full mip chains, partial regions, cube-unit binding, state restoration, content loading, and Reach/HiDef ceilings have evidence; other format labels are refused instead of silently receiving RGBA8 storage.
- Classic `Texture3D` is runtime-validated for `SurfaceFormat::Color`: width/height-derived mip chains halve all three stored dimensions, full and partial volume transfers/readback preserve slice-major bytes, `AddressW` reaches real source and compiled `sampler3D` draws, the live `GL_MAX_3D_TEXTURE_SIZE` limit is published, and exact defined-level CPU shadows restore after context recreation. Other format labels are refused because CNA's current public Color transfer surface cannot provide their format-native payloads.
- XNA sampler state is runtime-validated through independent GL 3.3 sampler objects on all 16 slots: all nine filter ordinals, U/V/W addressing, mip selection/bias, comparison disable, anisotropy capability/clamping, stale-state reset, slot independence, and sampled pixels have focused evidence.
- XNA blend, depth/stencil, and rasterizer state is runtime-validated through rlgl wrappers plus narrowly-scoped GL 3.3 bridge calls: exhaustive enum/native-state transitions and representative blend, mask, depth, stencil, cull, wireframe, and scissor pixels pass.
- CNA-owned low-level SpriteBatch scheduling is runtime-validated over rlgl shader, VAO, VBO, IBO, uniform, texture, sampler, and indexed-draw wrappers. Built-in texture and SpriteFont rendering covers all sort modes, transforms, float destinations, rotation/origin/flips, blend-state persistence, clipping, viewport-local coordinates, address modes, texture groups, and safe 16-bit capacity flushes. Optional compiled Effects execute through retained rlgl buffers and CNA's embedded stock SpriteEffect, while CNAEXT source `ShaderEffect` uses its own linked desktop-GLSL program and the same projection/texture batch contract.
- Fixed-capacity rlgl vertex and 16/32-bit index resources plus primitive submission are runtime-validated. Every XNA topology works through non-indexed and indexed paths, including exact `vertexStart`, `startIndex`, `baseVertex`, semantic-driven attributes, every classic vertex element's native VAO shape, WVP transforms, and public bound/user draws. Ordinary and stock-effect instanced draws support independently owned per-vertex and per-instance streams with each stream's own buffer, declaration, stride, offset, non-contiguous public slot, and exact `InstanceFrequency` divisor.
- The reusable stock-effect pipeline is runtime-validated for complete classic BasicEffect and AlphaTestEffect behavior. In addition to the unlit/alpha-test slice, BasicEffect now binds Normal0 by semantic and passes EasyGL/XNA pixel oracles for untextured/textured lighting, ambient/emissive, all three directional diffuse/specular lights, material specular and eye position, per-vertex/per-pixel selection, lit vertex color, D3D9 vertex-output saturation, inverse-transpose world normals, large-world precision, alpha, and lit fog.
- DualTextureEffect is runtime-validated with independent TextureCoordinate0/1 semantics and texture/sampler slots, XNA's doubled first-texture combine, null-texture white fallbacks, diffuse/alpha/vertex color, fog, and deterministic transition back to another stock effect.
- EnvironmentMapEffect is runtime-validated over the same rlgl-owned stock pipeline. It binds the base Texture2D and plain, mipmapped, or render-target TextureCube inputs with independent logical sampler slots; applies inverse-transpose world normals, derived eye position, three-light diffuse, emissive/ambient, alpha-scaled environment lerp/specular terms, view-space-Z fog, and XNA's per-vertex D3D9-saturated Fresnel factor; and passes all six cube faces plus every public primitive route.
- SkinnedEffect is runtime-validated for 1/2/4 weighted bone influences, the complete 72-bone palette, Byte4 and Vector4 blend indices, post-skin position/fog and joint/world normal transforms, texture/default-white sampling, materials, three lights/specular, per-vertex/per-pixel selection, and stock-effect transitions. The existing CNAEXT `VertexColorEnabled` option also passes through the same path and is tracked separately below. The palette is packed into the guaranteed OpenGL 3.3 vertex-uniform budget rather than relying on a larger driver-specific limit.
- All eleven formats accepted by FNA's classic `QueryRenderTargetFormat` rule are runtime-validated as exact RenderTarget2D storage: Color, Rgba1010102, Rg32, Rgba64, Single, Vector2, Vector4, HalfSingle, HalfVector2, HalfVector4, and HdrBlendable. They use rlgl-owned framebuffer/texture names, exact format-native CPU transfers and readback, single/multisample Depth16/Depth24/Depth24Stencil8 attachments, target switching, resolve-before-mipmap generation, Preserve/Discard behavior, target-local viewport/scissor coordinates, depth-bias rescaling, upright sampling, and two-through-four-target MRT sets. The shared source-effect oracle additionally proves distinct fragment locations 0–3, independent masks, depth ownership, one-sample shader masks across a real 4× resolve, immediate producer sampling, target-set replacement, and a clean native GL error queue.
- RenderTargetCube is runtime-validated with the same eleven exact renderable color formats, six complete face/mip chains, face-addressable single-sample FBO attachment, six independent MSAA color renderbuffers, exact Depth16/Depth24/Depth24Stencil8 attachments, resolve-before-cube-mipmap generation, Preserve/Discard behavior, top-row-first Color transfer/readback, per-face switching, and mixed cube/2D or multi-face MRT sets. CNA's current cube interface intentionally refuses public format-native transfer for the ten non-Color target formats while their rendered native storage is tested through the renderer-private bridge.
- The optional classic compiled-Effect path reuses CNA's pinned MojoShader parser/OpenGL adapter and shared effect translation. In option-on builds, ordinary, multi-stream, and instanced indexed/non-indexed user and bound-buffer draws plus SpriteBatch execute reflected attributes, uniforms, fragment/vertex 2D, cube, and volume samplers, pass state, render-target orientation, and XNA depth conventions through dedicated rlgl-owned VAOs, and `GraphicsCapability::CompiledEffects` is advertised. SpriteBatch preserves pass-major submission, stock vertex-shader inheritance, slot-zero sprite texture precedence, other-slot device fallback, and state recovery. Vertex-sampler objects, texture resources, and effect state survive native context recreation.
- Classic occlusion queries are runtime-validated through a renderer-private exact `GL_SAMPLES_PASSED` bridge. Query begin/end flushes rlgl's pending batch, preserves CNA/FNA's permissive call sequence without emitting invalid GL calls, reports completion asynchronously, returns zero before a result is available, and releases active or delayed resources safely across device shutdown.
- The representative application-level ladder is runtime-validated from clear/present through
  textures, indexed geometry, SpriteBatch/SpriteFont, every stock-effect family, depth/fog/lighting,
  dynamic buffers, instancing, state transitions, render targets/MRT, cube multipass rendering, and
  five unchanged EasyGL Model workloads including hierarchical meshes and animated skinning.
  `GraphicsCapability::ThreeD` now advertises that completed contract; depth/stencil, target MSAA,
  wireframe, source effects, and option-on compiled effects also report their validated truth.
- The systematic EasyGL campaign runs all 32 central parity fixtures and 11 classic golden sources
  unchanged under RLGL. Both renderers pass every internal oracle, and all 30 applicable whole-frame
  comparisons are byte-identical; the two source-documented diagnostics are classified explicitly.
  Production gaps `RLGL-059`/`060` and stale test expectations `RLGL-061` were closed, after which
  all 141 registered RLGL CTests passed after the completed Texture3D slice. `docs/rlgl-parity-campaign.md`
  records every cross-renderer comparison.
- The Release performance campaign now reports renderer-local draw/state/program/texture/upload/
  framebuffer/flush/uniform/attribute/allocation/conversion/lookup work. Measured fixes skip empty
  rlgl batch drains, cache complete sampler and stock-uniform values, retain VAO attribute state by
  resource-lifetime identity, and replace per-draw attribute vectors with fixed storage. Seven
  alternating llvmpipe runs put the 500-sprite + 50-mesh steady/churn submission p50 at
  0.308/0.304 ms for RLGL versus 0.324/0.327 ms for EasyGL, with zero real batch flushes,
  attribute changes, or attribute-scratch heap allocations after warm-up. The exact reproduction,
  counters, sprite-only control, and software-driver caveats are in `docs/rlgl-renderer.md`.
- `docs/rlgl-renderer.md` records the verified boundary, profile/platform status, build, offline
  dependency, update, licensing, debugging, and performance procedures. The Ubuntu 24.04 CI lane
  checks out exact raylib/FNA3D revisions, builds a generated complete RLGL runtime inventory, and
  makes the full `Rlgl` CTest label an SDL3-offscreen/Mesa software-rendering gate. Hosted execution
  is evidence only after a push/PR; Windows, macOS, on-screen Linux, and SDL2 runtime remain unclaimed.
- RLGL-022 reconciled every classic-XNA matrix row with the renderer/common-platform contract,
  rescanned production TODO/stub/fallback and EasyGL/RLGL hook inventories, and reran the complete
  141-test RLGL label plus adapter, capability, fullscreen, compiled-effect, and repository gates.
  No new classic-XNA gap remains; the two blocked matrix rows are modern CNAEXT work outside parity.
- Classic XNA 4.0 parity is the priority. Existing CNA extensions are tracked separately and do not gate the classic parity declaration.

## Current limitations

1. `RLGL` configures, fully builds, and passes its complete runtime suite with Mesa llvmpipe 25.0.7 through SDL's `offscreen` GL driver (OpenGL 4.5 core granted for the 3.3-core request). This proves classic parity on that Linux configuration, not untested Windows/macOS or on-screen behavior.
2. Device lifecycle, owning-thread context leasing, context-current child teardown, recovery registration and exact CPU resource shadows, ordinary presentation Reset, conditional native-reset detection, native context recreation with complete recovery-registered child restoration, physical/logical presentation tracking, exact selective clears, viewport/scissor coordinates, swap interval, renderer-owned backbuffer MSAA, complete backbuffer pipeline state, all 20 classic Texture2D formats, Color Texture3D, Color/DXT1/DXT3/DXT5 plain TextureCube, GL sampler objects, fixed-capacity vertex/index resources, every primitive topology, public bound/user primitive routes, ordinary and compiled multi-stream/instanced input, occlusion queries, BasicEffect/AlphaTestEffect/DualTextureEffect/EnvironmentMapEffect/SkinnedEffect, built-in SpriteBatch/SpriteFont rendering, CNAEXT source `ShaderEffect`, all eleven classic FNA render-target formats across RenderTarget2D/RenderTargetCube with single/multisample/MRT storage, and the opt-in compiled-Effect matrix are implemented. Source and compiled effects sample real volumes; CNA's current public Texture3D transfer surface exposes `Color` arrays only, so RLGL explicitly refuses non-Color volume formats rather than inventing their bytes. XNA/MojoShader maps the four vertex sampler registers to physical texture units 16–19, matching FNA3D; OpenGL 3.3 guarantees only 16 fragment texture-image units, so RLGL queries the live fragment, vertex, and combined limits and explicitly rejects a vertex sampler that the context cannot expose. The recorded Mesa context exposes all four. Robust native-reset polling is available only when the platform grants robust-access plus lose-context-on-reset semantics and exposes a core/KHR/ARB reset-status function; SDL3 offscreen/Mesa in the recorded run refused that context and used the tested non-robust fallback, where bind/swap failures can still report loss but asynchronous driver reset detection is not promised. A live GL context's default-framebuffer color/depth visual still cannot be renegotiated by an ordinary Reset, so RLGL reports those originally granted values until native recreation; multisampling is no longer part of that limitation because the renderer owns and rebuilds its multisample backbuffer. CNA's current public TextureCube transfer surface cannot express format-native uncompressed payloads other than `Color`; RLGL explicitly refuses those plain-cube labels and public non-Color cube-target transfers instead of substituting a byte layout.
3. Upstream rlgl keeps one process-global `RLGL` state object. The backend therefore enforces one live RLGL device and does not claim concurrent multi-context support.
4. Several GL 3.3 operations required for XNA fidelity have no rlgl 6.0 public wrapper: arbitrary primitive modes and 32-bit index draws, depth comparison, stencil configuration, depth bias, indexed color masks, and some multisample attachment operations. Renderer-private bridge calls through the GL dispatch loaded by rlgl are permitted only for such measured gaps; they do not create CNA public API.
5. OpenGL 3.3 cannot provide every modern CNA extension (for example GL 4.3 compute/SSBO/image-load-store facilities). These are not classic XNA parity blockers.

## 1. Mission

The RLGL renderer is a CNA graphics backend that implements `CNA::Internal::Renderers::Common::IGraphicsRenderer` and its resource interfaces using standalone upstream rlgl. CNA remains the framework and owns XNA behavior, resource objects, effects, the game loop, windowing, input, audio, and platform services.

The verified architecture is:

```text
Microsoft::Xna::Framework graphics API
  -> GraphicsDevice / resources / effects
  -> CNA IGraphicsRenderer contracts
  -> CNA RLGL renderer and renderer-owned resource objects
  -> standalone rlgl low-level resource/state wrappers
  -> OpenGL 3.3 core
  -> CNA IPlatformGlContext
  -> selected CNA platform implementation (SDL2 or SDL3 today)
  -> platform OpenGL implementation
```

It is explicitly not:

```text
CNA -> raylib application framework -> rlgl
```

The backend will not initialize raylib, create a raylib window, use raylib input/audio/game-loop/resource APIs, or route any public XNA API through raylib types. Low-level rlgl APIs are preferred over rlgl's immediate-mode default batch because CNA already owns draw ordering, resources, explicit pipeline state, and effect semantics.

No new CNAEXT graphics API will be introduced to accommodate RLGL.

## 2. Dependency strategy

### Decision

Use CMake `FetchContent` to acquire the SHA-256-verified official `raysan5/raylib` source archive at the exact raylib 6.0 commit `dbc56a87da87d973a9c5baa4e7438a9d20121d28`, but expose and compile only the standalone `src/rlgl.h` implementation and its required `src/external` loader headers. Do not configure, build, or link the raylib library.

This follows CNA's existing pinned-source dependency pattern used by Sokol and PortableGL while keeping the dependency surface substantially smaller than the full framework. An offline source override will be provided through `FETCHCONTENT_SOURCE_DIR_RLGL`, consistent with CMake's standard FetchContent convention.

### Recorded dependency facts

| Item | Decision / evidence |
|---|---|
| Upstream project | `https://github.com/raysan5/raylib` |
| Revision policy | Exact immutable commit archive plus SHA-256 for the selected stable release; never a moving branch or unverified download |
| Initial revision | raylib 6.0, `dbc56a87da87d973a9c5baa4e7438a9d20121d28` |
| License | zlib/libpng license; preserve the upstream notice and record it in CNA third-party documentation |
| Used source | Standalone `src/rlgl.h`; one CNA-owned implementation translation unit defines `RLGL_IMPLEMENTATION` |
| Excluded source | raylib windowing, core loop, input, audio, model, image, and application/resource layers |
| GL loader | rlgl's bundled GLAD implementation for desktop GL 3.3; call `rlLoadExtensions()` with CNA's platform proc-address loader after making the context current |
| Platform libraries | Existing CNA platform/OpenGL context dependencies; no new native window library |
| Update procedure | Change the pinned commit, review `rlgl.h`, loader and license diffs, build the RLGL configuration, run contract/unit/parity/smoke suites, then update this plan and dependency documentation |

Classic compiled `Effect` bytecode is a separate optional dependency seam, not part of rlgl or
raylib. `CNA_RLGL_COMPILED_EFFECTS=ON` reuses CNA's existing `cna_mojoshader` and
`cna_renderer_mojoshader_effect` targets from the pinned FNA3D revision `3240147` (MojoShader
revision `6333f74` plus CNA's declared patch series). Both projects use the zlib license. Only the
MojoShader archive, OpenGL adapter, and CNA renderer-neutral translation are linked; the FNA3D
renderer/library is not. The option stays off by default, matching EasyGL and the other optional
compiled-effect backends, and the dependency remains reproducible through the existing
`FETCHCONTENT_SOURCE_DIR_FNA3D` offline override.

Vendoring a copied header would make upstream provenance and updates less visible. A system raylib package is rejected because it is not reproducible, often exposes only the full framework library, and may be compiled for an incompatible graphics profile. Linking full raylib is rejected because standalone rlgl is sufficient.

## 3. GL context ownership

The repository already has an appropriate GL ownership boundary in `IPlatformGlContext`, `PlatformGlContextOwner`, and `PlatformGlSurfaceState`. RLGL will reuse it.

| Concern | Owner | RLGL responsibility |
|---|---|---|
| Window creation/destruction | CNA platform/window service | Request an OpenGL-capable descriptor; never create a raylib window |
| GL context creation/destruction | CNA `IPlatformGlContext`, held through `PlatformGlContextOwner` | Request GL 3.3 core attributes, bind for rlgl init/use, release through CNA RAII |
| Context switching | CNA GL context service and renderer thread-context lease | Keep rendering calls on the owning thread/context; add explicit lease/rebind coverage before claiming reset or multithread support |
| Proc-address loading | CNA platform GL service | Pass the CNA loader to `rlLoadExtensions()` |
| Buffer swap | CNA GL context service | `Present()` flushes pending work and calls `SwapBuffers()` |
| Drawable/backbuffer size | CNA window surface plus `PlatformGlSurfaceState` | Re-query the drawable extent, update GL viewport and projection-dependent state on resize |
| Resize events | CNA window/event infrastructure | React through existing renderer/presentation entry points; do not install raylib callbacks |
| Vsync | CNA presentation interval / requested swap interval | Call the platform context's `SetSwapInterval()` |
| Fullscreen | CNA window service | Use existing `GraphicsDevice` presentation-mode flow; rlgl has no ownership role |
| Backbuffer MSAA | CNA renderer-owned multisample framebuffer plus a single-sample platform presentation target | Clamp against the live GL limit, preserve samples across target switches, resolve explicitly for readback/present, and rebuild on Reset, resize, or context recreation |

rlgl initialization must occur only after a current context and successful extension load. Shutdown must make the same context current, release all renderer-owned resources, call `rlglClose()`, and then destroy the CNA-owned context.

Because rlgl 6.0 stores its state in a single static global object, the implementation guards against a second live RLGL device. Supporting simultaneous RLGL devices would require upstream isolation work and is not implied by CNA's multi-renderer compile mode.

## 4. Supported rlgl profiles

Upstream rlgl 6.0 has compile-time paths for OpenGL software, OpenGL 1.1, 2.1, 3.3 core, 4.3 core, OpenGL ES 2, and OpenGL ES 3. The public CNA renderer identity remains one `RLGL` identity; profiles are an implementation/build choice rather than separate renderer identities.

| rlgl profile | Assessment | Initial decision |
|---|---|---|
| OpenGL 1.1 | Fixed pipeline; lacks programmable shaders, FBOs, modern buffers, and instancing needed by EasyGL/XNA workloads | Not supported |
| OpenGL 2.1 | Programmable but materially limits VAO, instancing, MRT, integer attributes, and framebuffer behavior | Deferred; not a parity target |
| OpenGL 3.3 core | Best match to CNA's existing desktop GL baseline; supports shaders, VAOs, FBOs, MRT, instancing, and broad texture/state coverage | **Selected initial profile** |
| OpenGL 4.3 core | Adds compute, SSBOs, image operations, and broader modern functionality, mostly CNAEXT rather than classic XNA | Future optional profile after GL 3.3 parity |
| OpenGL ES 2 | Too constrained for the first EasyGL parity campaign | Deferred |
| OpenGL ES 3 | Plausible portability target, but shader versions, readback, texture formats, state, and platform context behavior require a separate measured campaign | Future optional profile |

No public `RLGL33`, `RLGL43`, or `RLGLES` renderer identities will be created. A build-time profile option may be added only when a second profile has real implementation and validation.

## 5. EasyGL capability matrix

This matrix records measured implementation shape, not promises. “Direct GL bridge” means a renderer-private helper in the same translation unit as standalone rlgl, using the GL dispatch initialized by rlgl only where rlgl lacks a required public wrapper.

| Area | Capability | EasyGL | Upstream rlgl 6.0 / GL 3.3 | RLGL status | Classification / next evidence |
|---|---|---|---|---|---|
| Device | Initialization | Implemented | `rlLoadExtensions`, `rlglInit` | ✅ | Two sequential real GraphicsDevice/context/rlgl lifecycles passed under Mesa llvmpipe; single-live guard release proven |
| Device | Thread/context lease | Implemented | CNA platform context handoff; rlgl state remains process-global | ✅ | RLGL-053 validates exact restore/release semantics, nested outer-policy ownership, frame/loader exclusion, native thread handoff, background XNB model creation, and a subsequent ordinary frame |
| Device | Child-resource teardown | Implemented | Unload wrappers require the owning GL context | ✅ | RLGL-054 serializes ordinary disposal and reverse-order device shutdown, covers every implemented child family plus an active query and optional MojoShader effect, and prevents delayed destructors from touching recycled names |
| Device | Resize | Implemented | Viewport/matrix APIs; CNA owns drawable size | ✅ | GraphicsDeviceManager resize, synchronized surface refresh, logical/physical rectangle checks, and post-resize pixel readback passed |
| Device | Fullscreen | Implemented through CNA platform | No window API needed | ✅ | Renderer-independent CNA window ownership applies exclusive/borderless modes; SDL3 mode and cross-platform capability-contract tests pass, while on-screen platform validation remains explicitly unclaimed |
| Device | Vsync | Implemented through platform context | No rlgl ownership | ✅ | Platform swap-interval forwarding and requested-value reporting passed RLGL-007 and the repeated lifecycle matrix |
| Device | Clear | Color/depth/stencil and selective planes | Public helper clears color+depth and omits stencil; exact selective clear needs bridge | ✅ | Distinct color and combined color/depth/stencil clears passed exact pixel readback; exhaustive write-mask restoration transitions passed RLGL-013 |
| Device | Present | Implemented | CNA context swap; no rlgl batch is used | ✅ | Explicit GraphicsDevice.Present passed in both runtime lifecycle cycles |
| Device | Viewport | Implemented | `rlViewport` | ✅ | Top-left CNA to bottom-left GL mapping, depth range, target switching, and pixel parity passed RLGL-013/014 |
| Device | Scissor | Implemented | `rlScissor`; enable belongs to rasterizer state | ✅ | Rectangle mapping, state transitions, clipping pixels, target coordinates, and reset/recovery passed RLGL-013/014/058 |
| Device | Backbuffer formats | Negotiated/reported | GL framebuffer plus CNA context attributes | ✅ | RLGL-058 reads granted RGBA/depth/stencil attributes, maps only exact classic formats, writes the achieved values back to public presentation state, and retains them across ordinary Reset |
| Device | MSAA | Implemented | Renderer-owned multisample framebuffer and explicit platform blit | ✅ | Creation and ordinary Reset apply the live-limit-clamped count without recreating the context; readback/present resolve explicitly, target switches preserve samples, and resize/native recreation rebuild storage (`RLGL-060`). |
| Textures | Texture2D | Implemented | `rlLoadTexture`, update, bind, unload; several exact formats need a bridge | ✅ | All 20 classic formats passed exact transfer and sampled-pixel validation in RLGL-023/026/027/028 |
| Textures | TextureCube | Implemented | `rlLoadTextureCubemap`; subresource transfer/readback need the private GL dispatch | ✅ | Color and DXT1/3/5 cover every payload shape exposed by CNA's current public cube-transfer contract. Exact faces/mips/regions, native/fallback DXT storage, content loading, binding and readback passed RLGL-044/045; the other 16 classic labels are explicitly refused because the public interface has no format-native transfer route for them |
| Textures | Texture3D | Implemented for Color | No public rlgl volume wrapper; GL 3.3 storage/update/readback/binding use a narrow private bridge | ✅ | Full and partial slice-major transfers, width/height-derived mip chains, source and compiled `sampler3D`, `AddressW`, live extent reporting, exact recovery shadows, and non-Color refusal passed RLGL-062 |
| Textures | Mipmaps | Implemented | `rlGenTextureMipmaps`; arbitrary allocation/update lacks public wrappers | ✅ | Texture2D, TextureCube, Texture3D, RenderTarget2D, and RenderTargetCube have allocated non-zero-level transfer/readback evidence; render targets additionally prove generated-mip sampling after resolve |
| Textures | Texture updates | Implemented | `rlUpdateTexture` supports level-zero subrect upload | ✅ | Public full and partial `SetData` paths round-trip exact bytes for all 20 classic formats, including odd-row and DXT block alignment |
| Textures | Readback | Implemented | Desktop `rlReadTexturePixels`; arbitrary levels/subrects and compressed readback unavailable | ✅ | Private desktop bridge provides exact native reads; the DXT fallback retains original block bytes so XNA-format readback stays exact after software decode |
| Textures | Filtering | Implemented | Texture parameters exist; GL 3.3 sampler objects require a private bridge | ✅ | All nine XNA min/mag/mip decompositions plus point/linear sampled pixels passed RLGL-025 |
| Textures | Address modes | Implemented | Texture wrap exists; GL 3.3 sampler objects require a private bridge | ✅ | U/V/W mapping, stale-state reset, slot independence, and Wrap/Clamp/Mirror pixels passed RLGL-025 |
| Textures | Anisotropy | Implemented when supported | rlgl detects extension/ceiling; sampler-object parameter needs a private bridge | ✅ | Capability follows rlgl's live extension probe; requested value clamps to the measured ceiling and non-anisotropic filters reset to one |
| Textures | Classic compressed formats | DXT1/DXT3/DXT5 with software fallback | DXT storage gated by detected S3TC extension | ✅ | rlgl's probe selects native storage; renderer-local DXT decode covers contexts without S3TC. Texture2D preserves exact block readback and TextureCube exposes decoded Color readback, matching their distinct public contracts; both modes have exact block-transfer and sampled/readback pixel evidence |
| Render targets | RenderTarget2D | Implemented for nine exact formats; current Rgba1010102/Rg32 path silently substitutes Color | FBO create/attach/check/unload plus exact GL 3.3 sized formats | ✅ | All eleven formats accepted by FNA's classic adapter rule have exact single/MSAA attachments and truthful live-FBO capability probes; RLGL-042 deliberately does not reproduce EasyGL's two silent substitutions |
| Render targets | RenderTargetCube | Implemented | Cube-face attachment enums exist; exact storage/MSAA allocation and cube mip generation need bridge gaps | ✅ | All eleven classic renderable formats, six faces/mip chains, exact depth/stencil, six independent face MSAA buffers, resolve/mips, usage, switching, Color transfer/readback, and cube/2D or multi-face MRT passed RLGL-046 |
| Render targets | MRT | Implemented | `rlActiveDrawBuffers`, maximum eight | ✅ | HiDef exposes the XNA four-slot ceiling over the live GL limit; transactional rlgl FBOs, ordered attachments, indexed masks/Clear, slot-zero depth, mixed formats, MSAA resolve/mips, usage and transitions passed RLGL-040. The shared four-output source-effect oracle passed RLGL-043. |
| Render targets | Depth/stencil | Implemented | Depth/stencil attachment enums; exact packed allocation needs bridge | ✅ | Depth16/Depth24 rejection/acceptance and Depth24Stencil8 write/read pixels passed RLGL-039 |
| Render targets | MSAA resolve | Implemented | FBO blit wrapper; renderbuffer allocation needs a narrow bridge | ✅ | Device-clamped color/depth/stencil renderbuffers, active/unbound resolve, edge coverage, preservation, and resolve-before-mipmap matrices passed RLGL-041 |
| Render targets | Switching | Implemented | FBO enable/disable | ✅ | Backbuffer/target/target transitions, target-local viewport/scissor, mip finalization, preservation and discard passed RLGL-039 |
| Render targets | Readback | Implemented | FBO/read-pixel paths; format/orientation conversion needed | ✅ | Format-native 1/2/4/8/16-byte reads, signed/HDR values, packed channel layouts, subrectangles, top-left rows, and non-zero float mips passed RLGL-039/042 |
| Buffers | Vertex buffers | Implemented | VBO load/update/unload | ✅ | Fixed-capacity native size/usage and exact public packed bytes passed RLGL-030 |
| Buffers | Dynamic vertex buffers | Implemented | Dynamic upload flag and subrange update | ✅ | None/Discard/NoOverwrite preserve identity/capacity and exact updated prefixes |
| Buffers | 16-bit index buffers | Implemented | EBO wrappers and unsigned-short indexed draw | ✅ | Fixed-capacity allocation and exact values/tails passed RLGL-030; indexed pixels passed RLGL-031 |
| Buffers | 32-bit index buffers | Implemented | Upload works; draw wrapper hardcodes unsigned short | ✅ | Exact values above 65535 passed native storage; the private 32-bit submission gap passed RLGL-031 |
| Buffers | Dynamic index buffers | Implemented | Dynamic upload/update available | ✅ | Both widths passed None/Discard/NoOverwrite resource gates |
| Buffers | Buffer updates | Implemented | Whole/subrange update wrappers | ✅ | Fixed storage never shrinks; Discard uses the documented private orphan bridge |
| Buffers | Primitive topology | Points, lines, triangle list/strip | Low-level draw wrappers hardcode triangles | ✅ | rlgl wrappers handle triangle arrays/16-bit zero-offset indexed triangles; a private GL bridge covers the measured arbitrary-mode gap for all five topologies |
| Buffers | Vertex declarations | Implemented semantic/type mapping | Generic attributes and divisors; CNA must map declarations | ✅ | Position0/Color0 bind by semantic rather than declaration order; all twelve classic formats passed exact native component/type/normalization/stride/offset checks |
| Buffers | User primitives | Implemented | VBO or transient upload; default batch is semantically unsuitable | ✅ | Public `DrawUserPrimitives` passed transformed pixel output through GraphicsDevice's transient resource path |
| Buffers | Indexed user primitives | Implemented | Same plus index-type limitation | ✅ | Public 16-bit and 32-bit routes passed exact index-type and pixel output |
| Buffers | Multi-stream vertex input | Implemented for ordinary, stock-effect, and compiled-effect draws | Generic attributes may select independent VBOs; CNA supplies combined semantic mapping | ✅ | RLGL-032 passed all 21 shared ordinary contracts; RLGL-016 adds stock instancing; RLGL-049 passes reflected compiled attributes across independent per-vertex/per-instance VBOs, declarations, strides, offsets, and public slots |
| Buffers | Instancing | Implemented for classic stock and compiled effects | VAO attribute divisors and instanced draw wrappers; exact non-triangle/index-offset/base-vertex combinations need the rlgl-loaded GL dispatch | ✅ | RLGL-016 passed the stock multi-stream/range/color suites; RLGL-049 adds the shared compiled-effect instancing matrix with exact public divisors, every topology, both index widths, start/base ranges, and ordinary/instanced recovery |
| Pipeline | BlendState | Full XNA mapping | Blend enable plus combined/separate factors/equations | ✅ | All factors/equations, constant factor, sample mask, and premultiplied/opaque transition pixels passed RLGL-013 |
| Pipeline | DepthStencilState | Full XNA depth/stencil mapping | Depth enable/write exposed; compare and stencil ops have no public wrappers | ✅ | All compares/operations, writes, two-sided state, independent reference changes, and depth/stencil pixels passed RLGL-013 |
| Pipeline | RasterizerState | Cull/fill/scissor/depth bias | Cull/scissor/wire exposed; exact cull selection and bias need care/bridge | ✅ | Native A→B transitions plus cull winding, solid/wireframe, and scissor pixels passed RLGL-013 |
| Pipeline | SamplerState | Full XNA mapping | Texture parameters expose core behavior; sampler-object API is not wrapped | ✅ | Independent objects on 16 XNA slots passed complete native-state transitions and focused sampled-pixel validation in RLGL-025 |
| Pipeline | Color write masks | Per-target masks | Global color mask wrapper only | ✅ | Private indexed GL 3.3 masks passed all four native values and a masked-draw/Clear-restoration pixel oracle |
| Pipeline | Culling | Clockwise/counterclockwise/none | Enable/disable and face selection wrappers | ✅ | Both cull selections and none passed native transitions; opposite winding selections produced accepted/rejected pixels |
| Pipeline | Fill modes | Solid/wireframe | Wire/point helpers on desktop | ✅ | rlgl solid/wire wrappers passed native transitions and interior/edge pixel evidence |
| Pipeline | Depth bias | DepthBias/SlopeScaleDepthBias | No public rlgl wrapper | ✅ | Private polygon-offset call scales normalized XNA bias by the granted backbuffer depth precision; native transition passed |
| Pipeline | Stencil operations | Full front/back XNA stencil | No public rlgl wrapper | ✅ | Private GL 3.3 bridge passed all operations, two-sided functions/masks, stencil write/read pixels, and standalone reference mutation |
| Pipeline | Separate blending | Required by XNA state | Separate factor/equation wrappers exist | ✅ | rlgl custom-separate cache path passed independent native RGB/alpha state and premultiplied output |
| Shaders | BasicEffect | Implemented | Shader compile/link/uniform/attribute wrappers are sufficient | ✅ | RLGL-033/034 pass unlit and lit textured/untextured paths, vertex color, material/alpha/emissive, fog, transforms, three lights, specular, per-vertex/per-pixel selection, and pixel-center behavior |
| Shaders | AlphaTestEffect | Implemented | Same | ✅ | RLGL-033 passed all eight comparison functions plus texture/default-white, diffuse, vertex color, fog, transforms, and alpha-boundary pixels |
| Shaders | DualTextureEffect | Implemented | Multiple texture/sampler binding available | ✅ | Independent UV/texture/sampler slots, doubled combine, null fallbacks, material/color/alpha, fog, and stock-transition pixels passed RLGL-035 |
| Shaders | EnvironmentMapEffect | Implemented | Cubemap and shader support available | ✅ | RLGL-036 passed 178 gates for Texture2D/TextureCube inputs, six faces, inverse-transpose normals, eye/world transforms, three lights, emissive/ambient, amount saturation, per-vertex Fresnel, alpha-scaled lerp/specular, fog, sampler isolation, every primitive route, and RenderTargetCube sampling |
| Shaders | SkinnedEffect | Implemented | Uniform arrays available; GL 3.3 guarantees 1024 vertex-uniform components | ✅ | 72 bones packed as 216 vec4 rows keep the complete shader at 957 components; 1/2/4 influences, Byte4/Vector4 indices, lighting/material/fog/normal paths passed RLGL-037 |
| Shaders | Other built-in effect families | The complete classic set is BasicEffect, AlphaTestEffect, DualTextureEffect, EnvironmentMapEffect, and SkinnedEffect, plus internal SpriteEffect | General shader primitives available | ✅ | Every classic family and internal SpriteBatch effect is covered above; additional CNA effects such as PBR are extensions and do not belong to the XNA parity inventory |
| Shaders | Custom compiled Effect | Optional MojoShader path | rlgl can host generated GLSL; the adapter owns program link/uniform upload | ✅ | RLGL-047/048/049/051/062 pass the same shared matrix as DirectX11: runtime/reflection, ordinary/multi-stream/instanced draws, SpriteBatch, attributes/uniforms, fragment and vertex 2D/cube/volume samplers, pass/state transitions, render-target orientation, depth convention, switching, stress, truncation, and context restoration |
| Shaders | Shader constants/uniforms | Implemented for stock, compiled, and source effects | Scalar/vector/matrix/array setters | ✅ | RLGL-050 adds every loose source-uniform setter and array form to the already complete stock and compiled matrices, with exact pixel and context-recovery evidence |
| Shaders | Matrices | XNA conventions normalized by renderer shaders | Column-major rlgl matrix representation; raw uniform upload available | ✅ | Stock and compiled matrix contracts pass; RLGL-050 also uploads source-effect World/View/Projection automatically and preserves arbitrary matrix/matrix-array uniforms across recovery |
| Shaders | Textures/samplers | Implemented for Texture2D/Texture3D/TextureCube | Active texture, cubemap, private volume binding, sampler parameters; vertex slots depend on live GL limits | ✅ | Stock, compiled, and source inputs pass independent slots, dimension checks, mip/filter/U/V/W address behavior, rendered Texture2D row correction, context restoration, and transitions. RLGL-062 adds real source and compiled volume sampling |
| Shaders | Vertex attributes | Semantic declaration mapping | Generic attributes plus integer/divisor variants | ✅ | Stock and compiled declarations pass ordinary and instanced multi-stream input with independent buffers/declarations/strides/offsets/slots, exact public divisors, both index widths, every topology, and ordinary/instanced transitions |
| Shaders | Fog | Implemented in stock effects | General shader support | ✅ | Every fog-capable classic stock family uses the forwarded view-space-Z fog vector with pixel evidence, including post-skin SkinnedEffect and EnvironmentMapEffect |
| Shaders | Lighting | Implemented in stock effects | General shader support | ✅ | BasicEffect, SkinnedEffect, and EnvironmentMapEffect pass three-light/material/specular or environment-specular, vertex/pixel-stage, normal-transform, eye-position, and precision contracts |
| Shaders | Skinning | Implemented | General shader support subject to uniform limits | ✅ | Complete 72-bone palette, 1/2/4 weighted influences, Byte4/Vector4 indices, inverse-transpose joint/world normals, and malformed declarations passed RLGL-037 |
| 2D | SpriteBatch | Implemented through renderer sprite-batch interface | High-level rlgl batch cannot preserve all CNA state/sort semantics; low-level buffers can | ✅ | Built-in path passed RLGL-029, classic compiled Effect composition passed RLGL-051, and RLGL-050 passes source-program projection, texture, custom uniform, background isolation, and stock transition behavior |
| 2D | SpriteFont | Implemented through SpriteBatch | Texture/quad primitives sufficient | ✅ | Glyph atlas, newline, and mirrored glyph-order pixels passed RLGL-029 |
| 2D | Texture drawing | Implemented | Texture/shader/buffer primitives sufficient | ✅ | Source/destination, tint, float coordinates, UV addressing, texture groups, and capacity flush passed RLGL-029 |
| 2D | Rotation/scaling | Implemented | Shader/vertex math sufficient | ✅ | Scaled texture, transform matrix, and rotated source-space origin pixels passed RLGL-029 |
| 2D | Sorting | Implemented XNA sort modes | Must remain CNA-owned, not rlgl batch-owned | ✅ | Deferred, Immediate, Texture, FrontToBack, and BackToFront behavior passed RLGL-029 |
| 2D | Blending | Implemented | Blend APIs sufficient | ✅ | Default premultiplied AlphaBlend and per-draw Immediate state transition passed RLGL-029 |
| 2D | Clipping | Implemented | Scissor APIs sufficient | ✅ | Custom viewport and exact enabled-scissor inside/outside pixels passed RLGL-029 |
| 3D | Meshes | Implemented through Model/Mesh draw paths | General indexed vertex draws available with bridge gaps | ✅ | General indexed topology/resources and stock semantics compose through unchanged one-mesh, two-mesh/effect, and child-hierarchy Model workloads in RLGL-018 |
| 3D | Models | Implemented | Renderer consumes CNA model resources, not raylib models | ✅ | Five unchanged EasyGL workloads pass basic draw, JSON texture content, independent mesh effects, child-bone hierarchy, and animated skinning in RLGL-018 |
| 3D | Lighting | Implemented | Shader capability sufficient | ✅ | Every classic lit stock family passes its measured primitive-level oracle in RLGL-034/036/037 and Model-level composition in RLGL-018 |
| 3D | Skinning | Implemented | Shader/vertex capability sufficient within limits | ✅ | SkinnedEffect's complete primitive/effect contract passes RLGL-037 and the unchanged animated Model playback workload passes RLGL-018 |
| 3D | Render targets | Implemented | FBO support available | ✅ | RenderTarget2D stock/compiled sources, EnvironmentMapEffect cube multipass sampling, source-effect four-output MRT, and the representative multipass ladder all pass RLGL-018 and their focused gates |
| 3D | Custom shaders | Implemented for source GLSL and optional compiled bytecode | Low-level shader APIs available | ✅ | Optional compiled bytecode executes the complete ordinary/multi-stream/instanced shared matrix in RLGL-048/049; RLGL-050 adds source GLSL with declaration-order attributes, indexed/non-indexed draws, uniforms, textures, diagnostics, transitions, and recovery |
| 3D | Instancing | Implemented for classic stock and compiled effects | Instanced draw and divisor APIs available | ✅ | Shared stock range/multi-stream/color pixels plus the parity sample and native snapshots pass RLGL-016; the shared compiled-effect matrix passes RLGL-049. CNAEXT nonzero base-instance remains explicitly unadvertised |
| Other | Occlusion queries | Implemented | No rlgl public query wrappers; GL 3.3 provides exact sample-count query objects | ✅ | Renderer-private `GL_SAMPLES_PASSED` bridge, completion/result behavior, permissive sequencing, state recovery, and disposal/device-lifetime coverage passed RLGL-052 |
| Other | Capabilities reporting | Detailed renderer limits/features | Live extension/limit queries plus explicit renderer opt-ins | ✅ | Every legacy capability is mapped explicitly; format/MSAA/MRT/anisotropy/texture ceilings follow live probes, completed features opt in only after their workload gates, and unlisted capabilities remain false. Focused common capability tests and RLGL workloads pass |
| Other | GraphicsAdapter | Descriptor-driven enumeration and support probes | Generic CNA platform enumeration before a live context; live resource limits stay device-specific | ✅ | The RLGL descriptor participates in the common adapter/display-mode/profile/format contract; 28 applicable adapter/platform-adapter tests and the renderer-descriptor integration gate pass, while the one GLES-only format probe is correctly skipped |
| Other | Resource disposal | Deterministic and context-aware | Unload wrappers available; require current context | ✅ | One device lifetime token owns context-current, idempotent release for Texture2D/cube, both target families, vertex/index buffers, SpriteBatch, queries, and optional compiled effects; delayed destructors issue no GL calls (`RLGL-054`) |
| Other | Device reset/loss | EasyGL has recovery infrastructure | rlgl global state and default-resource recreation require coordinated reinit | ✅ | RLGL-056/057/058 distinguish presentation Reset from genuine loss, poll robust reset status when granted, detect swap/bind failure, replace the context, restore all registered state/resources transactionally, preserve ordered status/events and target `ContentLost`, and pass repeated and failure-injected cycles. Non-robust contexts explicitly cannot promise asynchronous reset detection. |
| Other | Debug validation | Error checks and structured diagnostics | Renderer bridge checks GL boundaries; debug callback support depends on profile/extensions | ✅ | Bridge operations use named GL error boundaries; shader compiler, lifecycle/recovery, resource-refusal, and native MRT error-queue oracles produce observable diagnostics without silent no-ops |
| Other | Threading assumptions | Context lease and renderer synchronization | rlgl global state is not thread-safe | ✅ | RLGL-053 enforces an owning-thread recursive lease, mutually excludes frames and background loading, transfers the native context between threads, restores unrelated bindings, and rejects a second live device |
| CNAEXT | SkinnedEffect.VertexColorEnabled | Implemented by EasyGL and other capable renderers | Color attribute and stock shader multiplication are sufficient | ✅ | Works through the same RLGL-037 primitive path and has dedicated pixel evidence; this does not gate classic XNA parity |
| CNAEXT | Texture arrays and modern extras | EasyGL supports selected extensions | GL 3.3 cannot supply the full modern renderer contract | ⛔ | Kept separate from the completed classic Texture3D path; intentionally deferred |
| CNAEXT | Compute/SSBO/image operations | EasyGL/profile-dependent | rlgl exposes these only for GL 4.3 | ⛔ | Underlying GL-profile limitation; intentionally deferred |

## 6. Shader and effect strategy

RLGL will not introduce a public shader language or effect API. The renderer must consume the same CNA `GpuDrawParams`, stock-effect inputs, `ShaderEffect`, and optional compiled-effect flow used by EasyGL.

The first shader task must inventory EasyGL's stock shader variants and the semantic-to-location, uniform, sampler, matrix, clip/depth, texture-orientation, color, fog, lighting, and skinning conventions. The GL 3.3 implementation will compile renderer-private GLSL 330 variants or reuse compatible shared sources where they actually match. Custom Effect support must reuse CNA's existing representation/translation path rather than accepting RLGL-specific sources.

Regression tests will isolate conventions: matrix layout, half-pixel behavior if applicable, clip-space depth, render-target texture orientation, vertex color normalization, integer attributes, sampler slots, fog endpoints, alpha test boundaries, and bone transforms. Shader variants will not be patched per sample.

## 7. State correctness design

The renderer will use rlgl's low-level resource and draw wrappers, not its default immediate/batch path, for CNA rendering. The default batch's draw records are texture-oriented; program, matrix, VAO, and some state changes force flushes and it unbinds/reset selected GL state after submission. Mixing that machinery with CNA's explicit state model would make state ownership ambiguous.

RLGLRenderer will own a deterministic state cache for program, textures, samplers, blend, depth, stencil, rasterizer, scissor, viewport, framebuffer, vertex/index buffers, and attribute arrays. Cache entries are invalidated after any operation that can mutate or reset state outside the renderer's controlled path. Renderer-private GL bridge operations must be centralized, documented by the missing rlgl wrapper they replace, and followed by the same cache rules.

Every state-family task must test both isolated application and transitions A → B → A. Render-target switches, clears, readbacks, effect passes, and context restoration must preserve or explicitly reapply XNA-visible state.

## 8. Implementation DAG and task ledger

| ID | State | Depends on | Task and completion evidence |
|---|---|---|---|
| RLGL-001 | ✅ | — | Audit renderer selection, common contracts, GraphicsDevice/Adapter, presentation, platform GL ownership, resources, effects, SpriteBatch, lifetime/reset, diagnostics, build/CI, renderer tests, parity infrastructure, and comparable renderer plans. Evidence is recorded in Sections 1–8. |
| RLGL-002 | ✅ | — | Audit upstream rlgl 6.0 profiles, configuration, loader, lifecycle, resource/state/shader/FBO/draw APIs, batch behavior, formats, limits, and restrictions. Evidence is recorded in Sections 2–7 and the matrix. |
| RLGL-003 | ✅ | RLGL-001, RLGL-002 | Decide the dependency, initial GL profile, context ownership, renderer identity, low-level strategy, singleton policy, and private bridge boundary. Decisions are recorded above. |
| RLGL-004 | ✅ | RLGL-001, RLGL-002 | Establish the measured EasyGL/rlgl/RLGL capability matrix and seed the concrete implementation DAG. Matrix reviewed against the public/common renderer contracts. |
| RLGL-005 | ✅ | RLGL-003 | Added `RLGL`/`Rlgl` throughout CMake, the generated registry, dense C++ enum/classifications/tests, append-only C ABI value 51 (ABI 0.27.0), physical-module and GL-combination inventories, and an honest throwing factory pending RLGL-006. `cmake/ThirdPartyRlgl.cmake` fetches the official immutable raylib 6.0 commit archive with SHA-256 `81b06c…e126`, exposes only `src/`, defines GL 3.3, and never configures raylib. Validated 2026-09-12: RLGL configure succeeded with `FETCHCONTENT_SOURCE_DIR_RLGL=/tmp/cna-raylib-6.0-source`; `cmake --build /tmp/cna-rlgl-build3 --target cna_renderer_rlgl -j 4` built the target; C and C++ ABI header probes compiled; identity, runtime-discipline, combination, platform-SDL and whitespace gates passed. |
| RLGL-006 | ✅ | RLGL-005 | Implemented a real factory and minimal renderer using CNA's `PlatformGlContextOwner`: requested/verified GL 3.3 core (initially with optional context-MSAA fallback and achieved attributes; superseded by renderer-owned backbuffer MSAA in `RLGL-060`), one-live-device guard for rlgl's global state, CNA proc loader → `rlLoadExtensions`, transactional `rlglInit`, drawable bookkeeping, physical/logical viewport computation, selective color/depth/stencil clears with write-mask restoration, viewport/scissor mapping, swap interval/present, explicit pending-resource failures, registry lifetime, and context-current `rlglClose`. Standalone rlgl is isolated in one implementation TU; no raylib framework API is linked or called. Validated 2026-09-12: renderer target and full `/tmp/cna-rlgl-build3` configuration built successfully; renderer-target discipline, runtime-renderer discipline, CNAEXT Doxygen, and whitespace gates passed. Runtime GL evidence is deliberately separate in RLGL-007. |
| RLGL-007 | ✅ | RLGL-006 | Added exact RGBA8 backbuffer subrectangle readback through the rlgl-owned GL dispatch, including pack-alignment preservation and GL-to-XNA row normalization. Added `cna_test_rlgl_smoke`, always buildable for an RLGL configuration and registered with CTest when tests are enabled. It runs two sequential `Game`/`GraphicsDevice` lifecycles and checks requested logical size, truthful 3D capability, forwarded swap interval, pre-present color readback, combined color/depth/stencil clear, explicit Present, GraphicsDeviceManager resize + surface refresh, resized presentation rectangle/readback, and clean global rlgl release/reinit. Validated 2026-09-12 with `SDL_VIDEODRIVER=offscreen LIBGL_ALWAYS_SOFTWARE=1 /tmp/cna-rlgl-build3/cna_test_rlgl_smoke`: all 21 per-cycle/terminal checks passed on Mesa llvmpipe 25.0.7, OpenGL 4.5 core. Xvfb+x11 was attempted but this environment's SDL3 has no x11 driver; the registered smoke uses the proven offscreen path and exits 77 when no video device exists. |
| RLGL-008 | ✅ | RLGL-006 | Added `docs/rlgl-renderer.md` with the measured architecture, enablement/offline commands, pinned dependency/update procedure, license link, Linux-only runtime evidence, explicit unvalidated platforms, diagnostics, and current refusal boundary; linked it from the primary documentation indexes and refreshed stale identity/status text. Added `.github/workflows/rlgl-ci.yml`, a Linux compile gate that independently checks out the exact raylib commit, configures without dependency network access, builds `cna_renderer_rlgl` plus `cna_test_rlgl_smoke`, and checks registry consistency; it deliberately does not claim hosted runtime coverage. Validated 2026-09-12: workflow YAML parsed; fresh `/tmp/cna-rlgl-ci-check-008` configure and focused build passed with the documented switches and offline source override (system SDL substituted only because this worktree's vendored SDL submodule is absent); the freshly built smoke passed all 21 checks under SDL3 offscreen/Mesa llvmpipe; identity, runtime-discipline, platform-SDL, and whitespace gates passed. |
| RLGL-009 | ✅ | RLGL-006 | Texture2D and sampler umbrella. RLGL-023/026/027 implement every uncompressed classic format, RLGL-028 completes DXT1/3/5, RLGL-025 completes independent sampler objects, and RLGL-024 records the closed format inventory. Each subtask has exact transfer and sampled-pixel evidence. |
| RLGL-010 | ✅ | RLGL-009 | CNA-owned low-level SpriteBatch is complete: RLGL-029 validates the built-in texture/font path and all scheduling/state semantics, RLGL-051 validates classic compiled-Effect composition, and RLGL-050 validates CNAEXT source-program composition. The dedicated umbrella closure reran the built-in, compiled-effect, and source-effect SpriteBatch executables together; all 3/3 passed on SDL3-offscreen/Mesa-llvmpipe. |
| RLGL-011 | ✅ | RLGL-006 | Buffer/draw umbrella. RLGL-030 completed declaration-aware fixed-capacity resources and dynamic update hints; RLGL-031 completed every topology plus indexed/non-indexed and public bound/user submission; RLGL-032 completed ordinary multi-stream input. Instancing remains explicitly separated into RLGL-016. |
| RLGL-012 | ✅ | RLGL-009, RLGL-011 | Shader/effect umbrella completed by RLGL-033/034/035/036/037 for all five classic stock families and RLGL-038 for optional compiled Effects plus CNAEXT source ShaderEffect. At this milestone the independently missing `Texture3D` resource was a named sampling boundary rather than a hidden shader fallback; RLGL-062 later closed it. The dedicated umbrella closure reran representative Basic/AlphaTest, DualTexture, EnvironmentMap, Skinned, source, compiled, and volume-effect executables; all 9/9 passed on SDL3-offscreen/Mesa-llvmpipe. |
| RLGL-013 | ✅ | RLGL-006 | Implemented every blend factor/function, independent color/alpha state, constant blend factor, four indexed color-write masks, multisample mask, all depth comparisons/writes, all stencil operations, two-sided stencil state, standalone `ReferenceStencil`, cull direction, solid/wire fill, scissor enable, and depth-format-scaled polygon offset. rlgl wrappers own enable/cull/wire and the custom-separate blend cache; the private GL 3.3 bridge covers only unwrapped state. Full assignments are deterministic rather than relying on a second CNA cache, while the minimal stencil record supports the independent reference property. The task moved ahead of buffers after inspection proved it independent and a correctness prerequisite for SpriteBatch. Validated 2026-09-12: `cna_test_rlgl_state` passed exhaustive native state transitions plus premultiplied/opaque blend, color-mask/Clear restoration, depth reject/accept, stencil write/read/reference, cull winding, solid/wireframe, and top-left scissor pixel oracles on SDL3-offscreen/Mesa-llvmpipe. AddressSanitizer passed. RasterizerState.MultiSampleAntiAlias remains a shared renderer-seam omission also present in EasyGL; render-target depth-bias re-scaling will be validated with RLGL-014. |
| RLGL-014 | ✅ | RLGL-009, RLGL-013 | Closed the classic RenderTarget2D umbrella after a fresh audit of FNA's public resource contract, EasyGL's resource/MRT paths, every RLGL target entry point and native FBO transition, and all TODO/stub/fallback diagnostics. RLGL-039/040/041/042 cover exact two-dimensional color/depth/stencil storage, active and idle readback, target-local viewport/scissor/depth bias, upright producer-to-consumer sampling, mip generation, Preserve/Discard/PlatformContents behavior, MSAA resolve, all eleven classic renderable formats, Reach/HiDef limits, and ordered two-through-four-target MRT. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: focused target/state/format/MRT fixtures passed 18/13/15/13 gates; the unchanged EasyGL orientation oracle passed 2 gates and the unchanged OpenGL4 full-target oracle passed 12; the shared first-read matrix passed 34/34 legs (145 checks) and the shared mip/MSAA matrix passed 63/63 legs (845 checks). The audit found no remaining silent RenderTarget2D fallback or classic two-dimensional target stub. Cube resources/faces were subsequently completed by RLGL-015/044–046, distinct shader outputs by RLGL-038/043/050, and destruction/reset hardening by RLGL-017/053–058; none was hidden as completed by this umbrella. |
| RLGL-015 | ✅ | RLGL-009, RLGL-014 | Closed the classic cube-resource umbrella after a fresh audit of `ITextureCubeRenderer`/`IRenderTargetCubeRenderer`, the public TextureCube/RenderTargetCube creation/transfer/binding/disposal paths, EasyGL's corresponding resource/FBO behavior, every RLGL cube bridge/resource/renderer entry point, and all cube TODO/stub/fallback diagnostics. RLGL-044/045 cover every plain-cube transfer shape CNA can currently express (Color and DXT1/3/5); RLGL-046 covers all eleven exact renderable cube-target formats, six faces/mips, depth/stencil, per-face MSAA, usage, switching, readback, and compatible plural bindings. Re-running the focused plus unchanged shared suites produced 17 native + 17 forced-fallback plain-cube gates and 142 cube-target checks, with focused and shared cube-target fixtures also clean under AddressSanitizer. No silent classic cube fallback remains: unexpressible plain formats and public non-Color target transfers fail explicitly. EnvironmentMapEffect consumption subsequently completed in RLGL-036, while generalized resource-loss/reset behavior remains RLGL-017. |
| RLGL-016 | ✅ | RLGL-011, RLGL-012, RLGL-013 | Implemented classic stock-effect hardware instancing without a CPU draw loop. The stock vertex program reserves locations 12–15 for a positional four-`Vector4` instance-world matrix, composes it before the effect World transform, and applies it to positions, normals, lighting, fog, and environment inputs. Every per-vertex and per-instance binding retains its own VBO, declaration, stride, element offset, public slot, and exact `InstanceFrequency` divisor; instance columns may span several streams. The rlgl wrapper handles zero-offset 16-bit triangle lists, while the already-loaded private GL 3.3 dispatch preserves every other topology, 32-bit indices, `startIndex`, and `baseVertex`. Every attribute/divisor is prevalidated and the VAO is reset deterministically; classic `firstInstance == 0` is supported while CNAEXT base-instance stays unadvertised. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: 70/72 tests passed across the complete shared instanced multi-stream, range, vertex-color, diffuse-color, and ordinary multi-stream suites; the only two skips are named EasyGL/D3D-specific probes. This covers every topology, both index widths, offsets/base ranges, frequency arithmetic, multi-stream splits, effect-World composition, dynamic updates, lifetime, and state transitions. The existing parity sample passed 6/6 pixels and the focused primitive fixture passed 28/28 gates including exact native buffers/offsets/divisors. Both executables passed AddressSanitizer (`detect_leaks=0`); complete compiled-effects-on and option-off graphs built with `-j 3`; renderer identity/combination/target/runtime/descriptor, CNAEXT Doxygen, and whitespace gates passed. `GraphicsCapability::Instancing` is now true. Compiled-effect instancing was subsequently completed by RLGL-049; occlusion queries were split into independent RLGL-052. |
| RLGL-017 | ✅ | RLGL-009–RLGL-016, RLGL-052 | Lifecycle umbrella completed by RLGL-053 through RLGL-058: owning-thread/context leases, deterministic child teardown, recovery registration and exact CPU descriptions/shadows, native context recreation, transactional registered-resource restoration, conditional automatic native-loss polling, presentation-only Reset with achieved format/depth/MSAA reporting, complete event/status/content-loss semantics, failure rollback, repeated disposal, and repeated recovery are all validated. The dedicated umbrella closure reran both thread-lease probes, background loading, lifetime, native and forced-DXT recovery shadows, context/resource recreation, and the lifecycle failure matrix; all 9/9 passed on SDL3-offscreen/Mesa-llvmpipe. |
| RLGL-018 | ✅ | RLGL-010–RLGL-017 | Completed the representative workload ladder without duplicating already-focused pixel probes. Existing RLGL smoke, texture/primitive, SpriteBatch/Font, state, Basic/AlphaTest/DualTexture/EnvironmentMap/Skinned effect, buffer, instancing, 2D/cube target, MRT, source/compiled effect, and recovery fixtures cover every requested stage from clear/present through state switching and multipass rendering. The only missing application-level evidence was Model composition, so five unchanged EasyGL sources are now registered under RLGL: basic Model draw, JSON-reader texture content, two meshes with independent effects, child-mesh hierarchy transforms, and animated SkinnedEffect playback. All five passed on SDL3-offscreen/Mesa llvmpipe. `GraphicsCapability::ThreeD` is now true; the activation exposed and corrected truthful depth/stencil, live target-MSAA, wireframe, source-effect, option-on compiled-effect, Reach-MRT, classic Texture2D-format, and cube-target upload expectations. The complete shared graphics binary ran 2,428 tests with 2,254 passing, 174 named skips, and zero failures; the two RLGL-specific skips are the cross-device SpriteBatch cases that cannot construct two simultaneous devices because upstream rlgl 6.0 owns process-global state. All 92 registered RLGL CTests and the complete option-on build passed; option-off smoke, capability, and five-workload gates also passed with builds limited to four jobs. CI compiles the five Model workloads. |
| RLGL-019 | ✅ | RLGL-018 | Executed and recorded the systematic EasyGL comparison in `docs/rlgl-parity-campaign.md`. On SDL3-offscreen/Mesa-llvmpipe, both renderers passed all 32 central renderer-neutral parity fixtures and all 11 classic EasyGL golden sources. Whole-frame RGBA8 comparison was byte-identical for all 30 applicable fixtures at tolerance 2; `compressed_cube` explicitly defines only its within-renderer storage oracle, while `backbuffer_msaa` exposes an EasyGL-only half-pixel reference deviation that RLGL correctly does not copy. Shared graphics binaries had zero failures (EasyGL 2,396 passed/60 named skips; RLGL 2,254/174), and all 134 registered RLGL CTests passed after the campaign found and closed production gaps `RLGL-059`/`060` plus stale local expectations `RLGL-061`. The PBR goldens remain correctly excluded as CNAEXT rather than classic XNA. Builds used at most four jobs. |
| RLGL-020 | ✅ | RLGL-018 | Instrumented the Release benchmark and RLGL bridge with draw, state/program/texture bind, upload, framebuffer, flush, uniform, attribute, allocation, conversion, and semantic-search counters. The first 500-sprite + 50-mesh run exposed 11,774,400 complete sampler applications, 39 stock-uniform uploads per mesh, an empty `rlDrawRenderBatchActive` call and heap-backed attribute construction per draw, and full 16-location VAO resets. RLGL now skips an empty immediate batch, applies a complete sampler only after a partial public setter changes its effective value, caches stock uniform bytes and VAO attributes, uses fixed 16-entry attribute/semantic scratch, and keys cached bindings by a process-unique resource lifetime rather than recyclable GL names. Seven alternating Release runs on SDL3-offscreen/Mesa-llvmpipe (2,400 frames/phase, CPU-pinned, one llvmpipe worker) recorded stable/churn submission p50 medians of 0.308/0.304 ms versus EasyGL's 0.324/0.327 ms; the pre-change RLGL diagnostic was 0.794/0.768 ms. All seven optimized runs produced the same 4,800-frame structural record: 244,800 draws/program binds/flush requests, 24,000 actual state applications, 4,800 texture binds, 9,600 uploads, 4,799 framebuffer transitions, 489,600 uniform uploads, 240,000 fixed scratch builds/conversions, 2,160,000 semantic searches, and zero real batch flushes, attribute changes, or scratch heap allocations. A sprite-only five-run control stayed within 0.002–0.028 ms of EasyGL end-to-end. The focused warmed-cache assertion and 49 affected sampler/lifecycle/stock-effect tests passed; the complete compiled-effects-on graph rebuilt with four jobs and all 134 registered RLGL CTests passed. `docs/rlgl-renderer.md` records commands, results, and tail-latency caveats. |
| RLGL-021 | ✅ | RLGL-019 | Promoted `.github/workflows/rlgl-ci.yml` from a compile-only lane to an Ubuntu 24.04 SDL3-offscreen/Mesa runtime gate. `CNA_BUILD_TESTS=ON` now registers the complete suite; a CMake `cna_test_rlgl_all` meta-target derives every RLGL-directory executable (including the central parity corpus) from the directory target inventory, eliminating the stale handwritten CI list. CI first runs the smoke executable directly so exit 77 cannot masquerade as successful skipped coverage, then executes the exact `Rlgl` CTest label. It installs `libgl1-mesa-dri`, builds with two jobs, retains exact raylib/FNA3D/MojoShader checkouts and compiled effects, and runs all five renderer integration checks. The user documentation now gives the meta-target/full-suite command and explicitly separates the validated Linux SDL3-offscreen environment from unvalidated on-screen Linux, Windows, macOS, and SDL2 runtime plus unsupported Emscripten. It also records the exact GL/profile requirement, offline pins, update review, license notice, diagnostics, capability/parity boundary, performance procedure, and Texture3D/robust-reset/single-device limitations. Validated 2026-09-13: workflow YAML parsed; the refreshed compiled-effects-on configuration generated `cna_test_rlgl_all` with all 32 central parity targets and every focused/golden/workload executable, the meta-target built with four jobs, the direct smoke and all 134 `Rlgl` CTests passed, the five renderer integration gates passed (42 descriptors compiled, four host-SDK families structure-only), and `git diff --check` passed. This unpushed branch cannot truthfully claim a hosted Actions result; that evidence begins with its first push/PR run. |
| RLGL-022 | ✅ | RLGL-019–RLGL-021, RLGL-062 | Completed the final classic-XNA parity audit. EasyGL/RLGL renderer hooks, resource families, state, effects, errors, common adapter/platform ownership, tests, CI, and documentation were reconciled; the production TODO/stub/fallback scan found no hidden classic gap and removed one unused legacy `Unsupported` helper. The six stale matrix rows were closed with existing implementation plus fresh evidence: 41/46 common adapter/capability tests passed with five profile/interface-specific skips, all six SDL3/fullscreen capability tests passed, all 24 RLGL compiled-effect tests passed, the direct smoke and all 141 registered RLGL CTests passed, and renderer identity/target/runtime/combination/descriptor, CNAEXT Doxygen, workflow YAML, and whitespace gates passed. No new classic task was required. Windows, macOS, on-screen Linux, and SDL2 runtime remain honestly unvalidated; concurrent devices and the two modern CNAEXT rows remain explicit limitations rather than parity gaps. Every build used at most four jobs. |
| RLGL-023 | ✅ | RLGL-006 | Implemented the `SurfaceFormat::Color` Texture2D baseline as a genuine rlgl resource: level zero is created by `rlLoadTexture`, released by `rlUnloadTexture`, and bound with rlgl texture-slot wrappers. A documented private GL bridge allocates/updates/reads arbitrary mip levels because rlgl 6.0 hardcodes level zero or full-texture behavior; binding and pixel-store state are restored around bridge operations. The resource tracks defined levels separately from allocated storage and reports the live `GL_MAX_TEXTURE_SIZE`. Validated 2026-09-12: `cna_renderer_rlgl` and `cna_test_rlgl_texture` built; the focused SDL3-offscreen/Mesa-llvmpipe test passed public construction, exact full/partial upload, native whole/subrect readback, top-row-first ordering, rlgl texture-unit binding, declared mip allocation, non-zero mip upload/readback, and invalid-level rejection. An AddressSanitizer rebuild/rerun passed after the test was corrected to reacquire the renderer record that public full-level `SetData` intentionally replaces. The original smoke and repository identity/target/runtime/platform/Doxygen/whitespace gates passed; CI compiles the focused target. |
| RLGL-024 | ✅ | RLGL-023 | Closed the classic Texture2D inventory: `Color`, `Bgr565`, `Bgra5551`, `Bgra4444`, DXT1/3/5, `NormalizedByte2/4`, `Rgba1010102`, `Rg32`, `Rgba64`, `Alpha8`, `Single`, `Vector2/4`, `HalfSingle`, `HalfVector2/4`, and `HdrBlendable`. RLGL-026/027/028 contain the measured format-family evidence. CNAEXT formats 20–26 are tracked outside this classic-XNA gate. |
| RLGL-025 | ✅ | RLGL-023 | Implemented 16 lazily-created per-slot GL 3.3 sampler objects, using the renderer-private rlgl dispatch because rlgl 6.0 has no sampler-object wrapper. Each application is deterministic across all nine filter decompositions, U/V/W wrap, XNA `MaxMipLevel` → minimum LOD, desktop LOD bias, comparison disable, and anisotropy reset/clamp against rlgl's measured extension ceiling; applying filter/address alone resets properties otherwise left stale by an earlier effect. Renderer shutdown deletes the objects before `rlglClose`, one texture may use different samplers on two slots, and `AnisotropicFiltering` now reports the measured implementation. Validated 2026-09-12: `cna_renderer_rlgl`/`cna_test_rlgl_sampler` built; SDL3-offscreen/Mesa-llvmpipe native snapshots passed every ordinal/property, A→B slot independence, shared-texture/different-sampler binding, invalid-slot rejection, and rlgl-batch pixel oracles for PointWrap, PointClamp, PointMirror, point, and linear sampling. An AddressSanitizer rebuild/rerun and the prior smoke/texture regressions passed. The batch is test-only and is not CNA's production scheduler. CI compiles the focused target. |
| RLGL-026 | ✅ | RLGL-023 | Implemented the measured Reach-era uncompressed formats `Bgr565`, `Bgra5551`, `Bgra4444`, `NormalizedByte2`, and `NormalizedByte4`. The first three allocate/update through rlgl; XNA A:R:G:B packed words are reversibly rotated for GL's R:G:B:A 5551/4444 transfers. Because rlgl 6.0 exposes no signed-normalized pixel formats, a private bridge uses GL 3.3 `RG8_SNORM`/`RGBA8_SNORM`. All paths use format-native byte counts and one-byte transfer alignment, including odd two-byte rows; classification advertises only completed layouts and rejects Color-object transfers for signed-normalized storage. Validated 2026-09-12: the focused SDL3-offscreen/Mesa-llvmpipe test passed public construction, full and partial updates, exact native byte readback, packed rotation, odd-row alignment, and supported/unsupported classification for all five formats. The rebuilt AddressSanitizer test, device/sampler regressions, renderer build, and repository identity/target/runtime/platform/Doxygen/workflow/whitespace gates passed. RLGL-027 subsequently added and sampled the two-channel XNA expansion swizzle. |
| RLGL-027 | ✅ | RLGL-026 | Implemented every remaining uncompressed classic format with the exact FNA3D/EasyGL storage model: `RGB10_A2`, `RG16`, `RGBA16`, alpha-only `R8`, `R32F`/`RG32F`/`RGBA32F`, and `R16F`/`RG16F`/`RGBA16F` (also `HdrBlendable`). Exact one-/four-channel float and half-float layouts use rlgl wrappers; two-channel, high-bit UNORM, and alpha-only layouts use the private GL 3.3 dispatch because rlgl has no exact public formats. Per-texture swizzles implement XNA's `(R,1,1,1)`, `(R,G,1,1)`, and `(0,0,0,A)` sampling rules without shader-specific patches. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: public typed full/partial transfers round-tripped exact native bytes for all eleven formats, including signed finite floats, exact half words, odd Alpha8 rows, `HdrBlendable`, and a non-zero `Vector2` mip. rlgl-batch test-only pixels passed Alpha8, Single, Vector2, and NormalizedByte2 channel expansion. An AddressSanitizer rebuild/rerun, device/sampler regressions, renderer build, and repository gates passed. No GL 3.3 storage limitation was found; renderability is separately gated by RLGL-014. |
| RLGL-028 | ✅ | RLGL-026 | Implemented DXT1/DXT3/DXT5 with two truthful storage paths. When rlgl's extension probe maps the requested DXT format, `rlLoadTexture` creates level zero and the private dispatch performs compressed higher-mip allocation, block uploads, and `glGetCompressedTexImage` readback. Without S3TC, the renderer uses the shared CNA DXT decoder and rlgl-owned RGBA8 textures while retaining source blocks per mip solely to preserve XNA compressed `GetData` semantics; GPU pixel sampling proves uploads are not shadow-only. Public transfer validation enforces exact block byte counts and block-aligned rectangles. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe in both native and forced-fallback (`CNA_RLGL_FORCE_DXT_FALLBACK=1`) modes: DXT1/3/5 exact full reads, a changing block partial update with untouched-neighbor preservation, misalignment/short-buffer rejection, nonzero mip transfers, and red/blue sampled pixels all passed. Both modes also passed rebuilt AddressSanitizer runs; device/sampler regressions and renderer identity/target/runtime/platform/Doxygen/workflow/whitespace gates passed. The loader contract keeps DXT blocks intact until this renderer boundary, matching EasyGL's capability-dependent decode strategy; no compression is fabricated. |
| RLGL-029 | ✅ | RLGL-009, RLGL-013 | Implemented the production CNA-owned SpriteBatch scheduler with rlgl's low-level shader/program, VAO, dynamic VBO/IBO, matrix/uniform, texture, and indexed-draw wrappers. CNA's shared layer retains XNA sorting/overloads; RLGL groups by texture, forwards the complete sampler, flushes Immediate draws synchronously, preserves float destinations, rotation/source-space origin/flips/tint, maps custom/default backbuffer viewports, and splits before 16-bit indices overflow. rlgl's global immediate batch is only drained before bindings and is never the production scheduler. Non-null custom Effects throw the stable RLGL-012 diagnostic rather than silently using the stock shader. Validated 2026-09-12: `cna_test_rlgl_spritebatch` passed the custom-effect diagnostic plus 27 public-API pixel checks for texture row/source selection, both flip axes, all five sort modes, multi-texture groups, a 16,384-sprite capacity split, Immediate pipeline mutation, transform, rotation/origin, Wrap, viewport, AlphaBlend, fractional coordinates, scissor, SpriteFont newline, and mirrored glyph order on SDL3-offscreen/Mesa-llvmpipe. AddressSanitizer passed. |

| RLGL-030 | ✅ | RLGL-006 | Implemented declaration-aware, fixed-capacity vertex and 16/32-bit index resources. rlgl owns VBO/EBO allocation, update, and destruction; CNA's private bridge supplies only a reusable upload VAO, storage orphaning absent from rlgl 6.0, and native snapshots. Vertex storage is allocated once its declaration/stride is known; index storage is immediate. None/NoOverwrite update prefixes without shrinking, while Discard orphans the same name and resets recovery bytes. Validated 2026-09-12: `cna_test_rlgl_buffer` passed nine public/native gates covering built-in packed bytes, all twelve classic declaration formats, both index widths (including values above 65535), fixed size/usage, tail preservation, update counters, stable names, empty no-ops, and capacity/stride/width/option rejection on SDL3-offscreen/Mesa-llvmpipe. AddressSanitizer passed. |
| RLGL-031 | ✅ | RLGL-030, RLGL-013 | Implemented non-indexed and indexed point, line-list/strip, and triangle-list/strip submission over rlgl-owned VAO/VBO/EBO/program/uniform/attribute wrappers. rlgl's public draw wrappers are used where exact (triangle arrays and zero-offset 16-bit indexed triangles); its hardcoded topology/index-width/base-offset gaps use the already-loaded private GL 3.3 dispatch. Submission validates capacities, maps Position0/Color0 by semantics rather than field order, applies `world * view * projection`, honors `vertexStart`, `startIndex`, and `baseVertex`, and explicitly refuses lit/advanced/custom or instanced/multi-stream effects until their owning tasks. Validated 2026-09-12: `cna_test_rlgl_primitive` passed 24 gates on SDL3-offscreen/Mesa-llvmpipe: pixels and exact modes/counts for every topology in indexed and non-indexed form; 16/32-bit dispatch; public user and bound-buffer routes; reordered declarations; all twelve native vertex formats; WVP translation; state rebinding after SpriteBatch; malformed attribute rejection; and declaration/effect mismatch refusal. The rebuilt AddressSanitizer target passed the same suite. |
| RLGL-032 | ✅ | RLGL-031, RLGL-012 | Implemented classic ordinary multi-stream vertex input over rlgl's low-level vertex-buffer and generic-attribute wrappers. Every stock semantic is located across the combined CNA declaration and mapped back to its owning stream, whose independent native VBO, stride, declaration offset, and residual `VertexBufferBinding.VertexOffset` are installed before drawing. The existing draw-level `firstVertex`/`baseVertex` then advances all per-vertex attributes uniformly, preserving `startIndex`, both index widths, and every topology without concatenating CPU data. Binding shape, capacity, stream count/slots, declaration/stride agreement, per-instance frequencies, and semantic requirements are validated before native submission; all sixteen attribute locations and divisors are reset on each draw, and test snapshots retain exact per-location buffer/stride/offset evidence. CNA's existing FNA-compatible composition rule continues to drop a fully duplicate stream and reject a partially colliding stream. `GraphicsCapability::MultiStreamVertexInput` is now true while `ThreeD` and `Instancing` remain false. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: the shared `OrdinaryDrawMultiStreamTest.*` suite passed 21/21 without skips, covering non-indexed and 16/32-bit indexed offsets, start/base parameters, secondary range failures, dynamic updates, two/three streams, non-contiguous public slot 15, resource destruction/recreation, and repeated single/multi rebinding. The shared split-stream executable passed six pixel checks; the focused primitive executable passed 27 gates including exact independent native VBO/stride/offset state and post-collision recovery. Twenty-two related renderer/compiled-effect tests remained green. Both focused executables passed rebuilt AddressSanitizer runs (`detect_leaks=0`). Complete compiled-effects-on and option-off graphs built with `-j 3`; renderer identity/combination/target/runtime/descriptor, CNAEXT Doxygen, and whitespace gates passed. Per-instance streams were subsequently completed by RLGL-016 and compiled-effect multi-stream by RLGL-049. |
| RLGL-033 | ✅ | RLGL-031, RLGL-009, RLGL-025 | Generalized the baseline shader into one declaration-driven unlit stock pipeline with optional Color0/TextureCoordinate0, rlgl Texture2D/default-white binding, diffuse/emissive/alpha composition, FNA's four-value alpha-test encoding, view-space-Z fog, and the same 63/64-pixel clip correction EasyGL applies to non-MSAA stock draws. At this milestone lit/dual/cube/skinned/custom shapes still failed before submission; RLGL-034 subsequently lifted the BasicEffect-lighting restriction. Validated 2026-09-12: `cna_test_rlgl_effect` passed 15 gates covering texture row orientation, material and vertex tint, null-texture fallback, textured→untextured reset, all eight `CompareFunction` variants at equality, AlphaTest diffuse/color/fog composition, BasicEffect fog, and explicit declaration/effect-shape rejection. CNA's existing renderer-neutral `xna_pixel_center_contract_test.cpp` passed its measured 1×1 XNA triangle and control triangle; `cna_test_rlgl_primitive` remained 24/24. Effect, pixel-center, and primitive targets passed rebuilt AddressSanitizer runs. |
| RLGL-034 | ✅ | RLGL-033 | Generalized the rlgl-owned stock pipeline with semantic Normal0, full world and inverse-transpose normal transforms, ambient/emissive, three independently gated directional diffuse/specular lights, material specular power, eye position, texture/vertex-color composition, alpha and fog. A stage selector preserves XNA's default Gouraud path versus fresh per-fragment lighting, including D3D9's pre-interpolation color-output saturation. Validated 2026-09-12 against nine unchanged EasyGL/shared fixtures plus `rlgl_basic_effect_lighting_test`: 29 lighting-specific gates cover one/back-facing/disabled lights, all three light slots, emissive, specular/eye movement, per-vertex vs per-pixel output, both 36-byte vertex-color paths, saturation ordering, non-identity camera, 1000x world-space precision, non-uniform normal matrices, lit alpha, and lit fog. All ten lighting executables plus the 15-gate RLGL effect regression passed normal and AddressSanitizer builds under llvmpipe. |
| RLGL-035 | ✅ | RLGL-033 | Extended the rlgl-owned stock program with semantic TextureCoordinate1 at a stable separate attribute, a second sampler on texture unit one, XNA's `Texture * 2 * Texture2` RGB combine, and explicit unit-zero restoration. CNA's existing effect parameters supply both nullable textures, diffuse/alpha, optional Color0, and view-space-Z fog. This milestone initially rejected an absent TextureCoordinate1; `RLGL-059` subsequently aligned it with EasyGL/XNA's disabled-attribute zero default while preserving explicit wrong-format diagnostics. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: the renderer-local fixture passed independent UVs, independent Wrap/Clamp samplers, null texture0/texture1 white fallbacks, vertex color/diffuse/premultiplied alpha, vertex-color disable, fog, and DualTextureEffect→BasicEffect state reset. The unchanged EasyGL independent-UV fixture also passed. Both executables passed rebuilt AddressSanitizer runs. |
| RLGL-036 | ✅ | RLGL-015, RLGL-034 | Implemented EnvironmentMapEffect without changing CNA's public/common shader API: the existing effect bridge already supplied every XNA value, and the renderer-local stock GLSL path now applies inverse-transpose world normals, derived eye vectors, three-light diffuse plus emissive/ambient, base Texture2D sampling, plain/mipmapped/render-target TextureCube reflection, amount, environment specular, alpha-scaled lerp/add terms, alpha, and fog. Fresnel is computed and saturated per vertex before interpolation like XNA's D3D9 COLOR1 output. The unified program maps logical `SamplerStates[1]` onto a conflict-free physical unit because OpenGL forbids active sampler2D/samplerCube uniforms of different types from naming one unit; cube bindings are deterministically cleared and the prior physical sampler is restored after every draw. Declaration-less canonical stride-32 VertexPositionNormalTexture buffers are inferred without weakening declared semantic validation. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: eighteen existing EasyGL/shared/OpenGL4 fixtures passed 178 gates covering the complete formula, amount 0/0.5/1/>1, environment alpha/specular, all six faces, eye/world/normal transforms, three lights, per-vertex Fresnel gradient, fog, all nine sampler filters, U/V/W addressing, mipmaps, independent slots, repeated/interleaved draws, resource lifetimes, static/dynamic/indexed/user routes, and RenderTargetCube sampling. All eighteen passed rebuilt AddressSanitizer runs (`detect_leaks=0`; environment ptrace prevents LeakSanitizer), and BasicEffect/DualTextureEffect/SkinnedEffect/RenderTargetCube regressions remained green. The complete RLGL configuration built with `-j 3`; renderer identity/combination/target/runtime/descriptor, CNAEXT Doxygen, YAML, and whitespace gates passed. |
| RLGL-037 | ✅ | RLGL-034 | Generalized the stock vertex shader with semantic BlendWeight0 plus Byte4/Vector4 BlendIndices0, exact 1/2/4 weighted matrix sums, post-skin position/fog, inverse-transpose joint normals followed by the world normal matrix, and deterministic skinning enable transitions. Canonical declaration-less 52/56/64-byte layouts remain supported without replacing semantic validation for declared buffers. The 72 affine matrices are packed as three vec4 rows each: 864 components, and 957 for the complete vertex shader, below GL 3.3's guaranteed 1024-component minimum; initialization verifies that live limit and uses rlgl's uniform-array setter without a raw GL upload. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: one renderer-local fixture, the shared 10-cell parity fixture, and twelve unchanged EasyGL fixtures produced 66 passing gates across identity/translation/50-50/four-weight skinning, weights-per-vertex exclusion, palette slot 71, both index formats, null/real textures, material/alpha, three lights/specular, vertex color, vertex/per-pixel selection, post-skin fog, non-uniform joint/world normals, malformed-declaration rejection, and SkinnedEffect→BasicEffect reset. All fourteen executables passed rebuilt AddressSanitizer runs. Animated Model integration subsequently passed the unchanged EasyGL playback workload in RLGL-018. |
| RLGL-038 | ✅ | RLGL-033 | Reused CNA's existing effect surfaces without adding RLGL public API. RLGL-047/048/049/051 complete optional classic-XNA bytecode parsing, techniques/passes, reflected state, all draw shapes and SpriteBatch; RLGL-050 completes the separately tracked CNAEXT desktop-GLSL source path, diagnostics, declaration-order attributes, uniforms/arrays, Texture2D/TextureCube/Texture3D bindings, 3D draws, SpriteBatch, transitions, and recovery. The dedicated umbrella closure passed all 24 compiled-effect GTests plus all 4 source/compiled runtime, draw, and volume-effect CTests on SDL3-offscreen/Mesa-llvmpipe. |
| RLGL-039 | ✅ | RLGL-009, RLGL-013 | Implemented single-sample `SurfaceFormat::Color` RenderTarget2D through rlgl's texture/FBO create, attach, completeness, bind, mip-generation and unload wrappers. A narrow private dispatch allocates exact Depth16/Depth24/Depth24Stencil8 renderbuffers and performs level/subrectangle RGBA8 readback because rlgl 6.0 has no exact-depth or framebuffer-read wrapper. Creation/readback/destruction preserve unrelated framebuffer/texture bindings; target switches finalize mip chains, restore the default framebuffer, map viewport/scissor against target height, and rescale XNA normalized depth bias to the active attachment. CPU uploads and readback are normalized to top-row-first; SpriteBatch CPU UVs and a per-slot stock shader uniform correct rasterized bottom-up sources without altering ordinary textures. The shared GraphicsDevice path supplies deterministic DiscardContents clears while preserved/platform content naturally survives. At this milestone non-zero samples and non-Color formats failed explicitly under RLGL-041/042 (both subsequently completed), and the profile ceiling stays one until RLGL-040. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: `cna_test_rlgl_render_target` passed 13 focused gates for capability truth, native identities, every depth format, then-explicit MSAA refusal, asymmetric subrect readback, BasicEffect target sampling, CPU upload, real depth/stencil pixels, depth-bias scaling, non-zero mip readback, and usage transitions; the unchanged EasyGL quadrant fixture passed both orientation gates. Effect/DualTexture/Skinned/SpriteBatch regressions remained green. Both target executables passed rebuilt AddressSanitizer runs, the complete RLGL configuration built successfully, and renderer identity/descriptor/target/runtime-discipline gates passed (42 descriptors compiled; four platform-SDK families were structure-checked). |
| RLGL-040 | ✅ | RLGL-039 | Implemented two-through-four ordered RenderTarget2D slots with one complete candidate framebuffer created by `rlLoadFramebuffer`, target-owned color/depth objects attached through `rlFramebufferAttach`, and `rlActiveDrawBuffers`. A candidate is validated before the old set is finalized, preserving the prior public/native binding on all compatibility failures. Slot zero exclusively owns depth/stencil; every member independently resolves and regenerates mips on replacement/unbind. Destruction explicitly detaches borrowed depth/stencil before `rlUnloadFramebuffer`, whose upstream implementation would otherwise delete them. The live `min(GL_MAX_DRAW_BUFFERS, GL_MAX_COLOR_ATTACHMENTS)` is capped at XNA's four; Reach remains one and HiDef exposes the native ceiling. `GraphicsDevice::SupportsCapability(MultipleRenderTargets)` now combines renderer support with the device's current profile because GraphicsDeviceManager can change that profile after renderer creation. Clear saves, opens, and restores all four indexed color masks. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: `cna_test_rlgl_mrt` passed 13 focused gates covering profile/capability truth, native 2/3/4-slot order, transactional FBO identities, active readback restoration, all indexed masks, four-target Clear/Preserve, reordered stock output zero, slot-zero depth, mixed Color/HdrBlendable, real 4x-MSAA renderbuffers with per-target resolve/mips, DiscardContents, compatibility/count rejection without state loss, and borrowed-depth survival. The shared isolated MSAA/mipmap matrix passed 63/63 legs and 845 checks with its real MRT leg enabled; rebuilt state, focused target, exact-format, and MRT regressions also remained green. The focused fixture passed AddressSanitizer (`detect_leaks=0`; environment ptrace prevents LeakSanitizer), the complete RLGL configuration built with `-j 3`, and identity/combination/runtime/target/descriptor/Doxygen contract gates passed. Distinct custom outputs were subsequently completed by RLGL-043 after the custom-effect path landed. |
| RLGL-041 | ✅ | RLGL-039 | Implemented live-limit sample clamping with actual applied-count reporting, separate multisampled RGBA8 plus exact Depth16/Depth24/Depth24Stencil8 renderbuffers, a sampleable single-sample Color texture, and one resolve FBO per target. rlgl owns framebuffer/texture creation, attachments, completeness, blits and unloads; a narrow bridge allocates renderbuffers because rlgl 6.0 exposes no renderbuffer-storage wrapper. Resolve preserves read/draw FBO bindings, runs for active readback and before mip generation on unbind, and leaves later rendering valid. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: the focused fixture passed 18 gates including device clamping, separate native identities, first/repeated active reads, real mixed edge coverage, multisampled depth/stencil, resolve→mipmap, and Preserve/Discard transitions; the unchanged OpenGL4 full-target fixture passed 12 gates. Shared isolated regressions passed all 34/34 first-read legs (145 checks) and 63/63 MSAA/mip legs (832 checks), with only the separately-owned MRT/cube legs declared as boundaries. All four executables passed rebuilt AddressSanitizer runs, and the complete RLGL configuration built successfully with `-j 3`. |
| RLGL-042 | ✅ | RLGL-039 | Implemented the eleven classic formats FNA's `GraphicsAdapter.QueryRenderTargetFormat` accepts: Color, Rgba1010102, Rg32, Rgba64, Single, Vector2, Vector4, HalfSingle, HalfVector2, HalfVector4, and HdrBlendable. Each resource keeps its exact GL sized format through texture allocation, multisample renderbuffer allocation, resolve, mip generation, format-native 1/2/4/8/16-byte CPU upload/readback, and XNA channel swizzles. Capability answers are restricted to that classic set and require a complete exact 1x1 live FBO; compressed, signed-normalized, legacy packed-16, Alpha8, and CNAEXT formats are refused rather than substituted. Audit found EasyGL maps nine of the eleven exactly but currently lets Rgba1010102/Rg32 fall through to a Color allocation; RLGL follows FNA's authoritative classic rule and does not reproduce that silent substitution, without changing EasyGL. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: `cna_test_rlgl_render_target_formats` passed 15 gates covering the complete capability/refusal set, exact MSAA draw/readback for every format, signed and above-one HDR components, packed layouts, HalfVector4 mip generation, Vector4 CPU transfer/orientation, and Vector2 target sampling. The focused 18-gate target fixture, unchanged 12-gate OpenGL4 full-target fixture, shared 34/34 first-read matrix (145 checks), and shared 63/63 mip/MSAA matrix (832 checks) remained green under AddressSanitizer. The complete RLGL configuration built successfully with `-j 3`. |
| RLGL-043 | ✅ | RLGL-038, RLGL-040 | Registered the shared EasyGL MRT oracle directly as `Rlgl_Mrt_oracle`, selecting its existing GL-source/native-error branch with one fixture-local definition while retaining the truthful RLGL renderer identity. The common fixture now explicitly requests HiDef because XNA Reach permits only one render target; its complete behavior and assertions remain shared rather than copied. No production change or stock-shader output fabrication was needed. Validated 2026-09-13 on SDL3-offscreen/Mesa llvmpipe: RLGL passed 20/20 checks for distinct fragment outputs 0–3, ordered bindings, independent `ColorWriteChannels0..3`, first-target depth ownership, depthless behavior, true 4× MRT MSAA with a one-sample shader mask and resolve, immediate producer-to-consumer sampling, one/MRT and set replacement transitions, Preserve/Discard usage, deterministic incompatibility refusal, and an empty native GL error queue. The original OpenGL33/EasyGL target also passed the same 20/20 checks after the explicit profile correction. All 87 RLGL CTests passed; the complete option-on graph and the option-off oracle target built with at most four jobs; renderer identity/combination/target/runtime/descriptor, workflow-YAML, and whitespace gates passed. |
| RLGL-044 | ✅ | RLGL-009 | Implemented plain `SurfaceFormat::Color` TextureCube with `rlLoadTextureCubemap` owning all six faces and every declared mip allocation and `rlUnloadTexture` owning destruction. rlgl's cube binding handles sampled units; a narrow desktop-GL bridge supplies the missing face/mip/subrectangle upload and readback and clamps the complete mip range. All resource operations restore the caller's active unit plus 2D/cube bindings; CPU transfers remain top-row-first with no render-target inversion. Reach/HiDef limits combine XNA's 512/4096 ceilings with the live GL texture limit, and cube classification advertises only Color until RLGL-045. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: the focused fixture passed 7 gates covering native RGBA8 identities, all face/level widths, exact six-face RGBA including alpha, startIndex isolation, asymmetric partial rows/untouched texels, final 1x1 mip, state restoration, binding, limits, and refusal truth; the unchanged OpenGL4/EasyGL-derived oracle passed all 4 face/partial/orientation/mip gates. Both fixtures plus the 35-gate Texture2D and 13-gate state regressions passed rebuilt AddressSanitizer runs (`detect_leaks=0`; environment ptrace prevents LeakSanitizer). The complete RLGL configuration built with `-j 3`; identity, combination, runtime/target discipline, 46-family descriptor, CNAEXT Doxygen, and whitespace gates passed; CI compiles both cube executables. |
| RLGL-045 | ✅ | RLGL-028, RLGL-044 | Completed every remaining publicly transferable plain-cube format. DXT1/DXT3/DXT5 allocate through `rlLoadTextureCubemap`: live S3TC support selects exact compressed mip storage and subimage upload, while `CNA_RLGL_FORCE_DXT_FALLBACK=1` selects renderer-local block decode into rlgl-owned RGBA8 cube storage. Public block transfers enforce exact sizes and XNA edge-region alignment; Color readback comes from actual GPU cube storage in either mode. The FNA XNB cube reader keeps DXT blocks intact until this boundary, and the repository's 64x64 seven-mip DXT1 cube fixture loads and changes a sentinel at its terminal mip. A measured API audit found CNA's current `ITextureCubeRenderer`/`TextureCube` transfer surface exposes only `Color*` plus compressed-byte upload, so the other 16 classic format labels cannot receive or return their format-native payloads; RLGL refuses them at creation rather than copying EasyGL's silent RGBA8 substitution. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: the focused fixture passed 17 gates in native mode and the same 17 under forced fallback, covering native identities for all three formats, RGB/alpha decoding, independent faces, NPOT edge blocks, sub-four-pixel terminal mips, wrong-payload rejection, content loading, and exact supported/refused classification. The unchanged OpenGL4 cube oracle passed 4/4, and native/fallback cube, oracle, plus the 35-gate Texture2D regression passed rebuilt AddressSanitizer runs (`detect_leaks=0`; environment ptrace prevents LeakSanitizer). The complete RLGL configuration built with `-j 3`; identity, combination, runtime/target discipline, and whitespace gates passed. |
| RLGL-046 | ✅ | RLGL-014, RLGL-044 | Implemented one exact sampleable cube texture with six complete face/mip chains, one face-reattached rlgl FBO, six face-specific MSAA color renderbuffers, one EasyGL/FNA-style shared depth/stencil renderbuffer, an rlgl resolve FBO, cube mip generation, Color transfer/readback, and generalized face-aware target/MRT records. All eleven classic renderable formats retain their exact native identities; unsupported formats and public non-Color transfer shapes are refused rather than substituted. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: the focused HiDef fixture passed 10 storage/depth/stencil/draw/mixed-MRT gates; unchanged shared GetData/usage/MSAA-face/plural-binding contracts passed 55/30/33/14 checks; RenderTarget2D/formats/MRT/TextureCube/state regressions passed 18/15/13/17/13 gates; the complete 142-check cube suite passed under AddressSanitizer with leak detection disabled for the Mesa process. |
| RLGL-047 | ✅ | RLGL-033 | Bootstrapped optional classic XNA Effect Framework bytecode support with `CNA_RLGL_COMPILED_EFFECTS=ON`, reusing CNA's pinned FNA3D `3240147` / MojoShader `6333f74` dependency and shared reflection/state translators without linking FNA3D. One renderer-owned MojoShader OpenGL context uses CNA's proc loader and is destroyed before rlgl/context shutdown; each runtime parses/compiles authentic bytecode, reflects parameters/techniques/passes, bounds raw values, validates same-device 2D/cube textures, translates pass render/sampler assignments, preserves unassigned state, retains assigned native resources, clones current values/technique, and releases cleanly. Capability remains deliberately false until RLGL-048 proves pixels. Validated 2026-09-12: the focused SDL3-offscreen/Mesa-llvmpipe fixture passed 16 gates normally and under AddressSanitizer (`detect_leaks=0`), including an independent clone after source destruction. The complete option-on target graph compiled with `-j 3`; an option-off renderer build also passed, proving the new source is inert by default. CI now compiles the opt-in runtime against independently checked-out immutable pins. |
| RLGL-048 | ✅ | RLGL-047, RLGL-011 | Implemented optional compiled-Effect execution for every ordinary public draw family: indexed/non-indexed user data and bound buffers, typed and raw declarations, 16/32-bit indices, and exact vertex/start/base offsets. MojoShader reflection drives semantic/type attribute capture on a dedicated rlgl-owned VAO; the adapter program is deterministically rebound, uniforms are uploaded, exact 2D/cube targets and existing sampler objects are applied, rendered Texture2D sources use a same-format vertically reversed FBO copy, and the temporary GL depth-range mapping is restored before subsequent stock draws. At that milestone dimension mismatches, Texture3D, multi-stream, instancing, vertex samplers, and SpriteBatch advanced paths failed explicitly under their owning tasks rather than falling back; the later tasks, including RLGL-062 for Texture3D, closed the applicable paths. `GraphicsCapability::CompiledEffects` is true only in option-on builds. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: the focused runtime and public draw executables passed 16 and 11 gates; thirteen shared/renderer GTests passed runtime, ordinary draw, clip/orientation, effect switching, sampled pixels, pass selection, stock isolation, multi-hop render-target sources, cube-face sampling, named dimension refusal and recovery, depth convention/restoration, repeated draws, and truncation. Nine unchanged state/sampler/primitive/SpriteBatch/render-target/cube/stock-effect regressions passed. Both focused executables passed rebuilt AddressSanitizer runs (`detect_leaks=0`); the complete option-on graph and option-off renderer built with `-j 3`; renderer identity/combination/target/runtime/descriptor, CNAEXT Doxygen, workflow YAML, and whitespace gates passed. |
| RLGL-049 | ✅ | RLGL-016, RLGL-032, RLGL-051 | Completed the compiled-effect matrix. MojoShader-reflected attributes now resolve across independently owned streams and bind each stream's VBO/declaration/stride/offset; instance frequencies drive attribute divisors and native array/element instancing for every topology, both index widths, and start/base ranges. Vertex-stage Texture2D/TextureCube sampling uses XNA's four logical registers on MojoShader physical units 16–19, with independent sampler objects, rendered-source correction, dimension validation, explicit unbinding, and context-loss restoration. A one-bit pinned-MojoShader vertex `dcl` reserved-mask bug that rejected cube samplers was fixed by a reproducible fifteenth patch. Live fragment/vertex/combined GL limits gate vertex slots; at that milestone real volume sampling was a named refusal at the missing RLGL `Texture3D` resource, later closed by RLGL-062, and classic GL 3.3 explicitly requires `firstInstance == 0`. Validated 2026-09-13 on SDL3-offscreen/Mesa-llvmpipe: RLGL's then-24-test compiled suite passed 23 tests and skipped only the named volume-resource case, including exactly the 17 shared helpers used by DirectX11. The new multi-stream, instancing, vertex slots 0/3 with 2D/cube textures, recovery, and volume-boundary cases passed AddressSanitizer (`detect_leaks=0`). All 37 EasyGL compiled-effect regressions passed individually against the shared fixture and patched parser; all 85 RLGL CTests passed; complete compiled-effects-on and option-off graphs built with at most four jobs; renderer identity/combination/target/runtime/descriptor, CNAEXT Doxygen, workflow YAML, and whitespace gates passed. |
| RLGL-050 | ✅ | RLGL-048 | Implemented the existing CNAEXT source-based `ShaderEffect` contract without new public API: RLGL owns desktop-GLSL compile/link/program/VAO lifetime; VS/FS/Link-prefixed dynamic logs feed structured stage/line diagnostics; declaration elements map consecutively to locations 0–15 with per-vertex streams preceding instance streams; every scalar/vector/matrix and array setter uploads by name; Texture2D/TextureCube plus the existing Texture3D interface binding use live unit limits; World/View/Projection drive indexed, non-indexed, multi-stream and instanced geometry; SpriteBatch uses the same source program with `projection`; and retained source/uniform state rebuilds after context loss. At that milestone the real Texture3D resource was deliberately not fabricated; RLGL-062 later supplied and validated it. Validated 2026-09-13 on SDL3-offscreen/Mesa-llvmpipe: `cna_test_rlgl_shader_effect` passed 13/13 capability/refusal, diagnostic, custom-declaration, 32-bit indexed/non-indexed, all-uniform/array, Texture2D/TextureCube, state-switch, context-recovery, stock-transition, and SpriteBatch checks. All 86 RLGL CTests passed; the complete compiled-effects-on graph built with four jobs, and the separately configured option-off source target/test also passed with four jobs. Renderer identity/combination/target/runtime/descriptor, CNAEXT Doxygen, workflow YAML, and whitespace gates passed. CI compiles the focused executable. |
| RLGL-051 | ✅ | RLGL-029, RLGL-048 | Implemented classic compiled-Effect SpriteBatch composition with an embedded FNA stock `SpriteEffect` runtime, retained renderer-owned vertex/index buffers, pass-major submission, sprite texture precedence at sampler slot zero, `GraphicsDevice.Textures` fallback for other unassigned pixel slots, complete slot-zero SpriteBatch sampler forwarding, and single render-target row correction in the compiled binder. MojoShader/rlgl program, VAO, buffer, texture, depth-range, and batch state is restored on both success and named sampler-dimension failure. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: all four shared compiled SpriteBatch contracts passed, together with renderer-specific pixel-only stock-vertex inheritance, device slot fallback, post-success stock draw, and post-exception stock draw tests; the complete RLGL compiled suite passed 20/20 tests. The focused compiled draw executable passed 12/12 checks and the unchanged SpriteBatch executable passed 28/28; both passed rebuilt AddressSanitizer runs (`detect_leaks=0`). Complete option-on and option-off graphs built with `-j 3`, and static renderer/workflow/whitespace gates passed. |
| RLGL-052 | ✅ | RLGL-006, RLGL-013 | Implemented classic occlusion-query resources through the GL 3.3 dispatch because rlgl 6.0 exposes no public query-object wrapper. The renderer uses exact `GL_SAMPLES_PASSED`, flushes rlgl before begin/end boundaries, exposes nonblocking availability and zero-before-ready behavior, preserves FNA/CNA's permissive unmatched/duplicate Begin/End sequence without invalid GL calls, and safely ends/deletes active queries during resource or device teardown. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: shared cycle, visible, occluded, and focused lifetime executables passed together with state/primitive regressions; visible one-draw/two-draw counts proved exact additivity, an empty query and a depth-occluded draw returned zero, the precision contract exceeded 4000 samples, and disposal remained idempotent before and after context shutdown. All four query executables passed AddressSanitizer (`detect_leaks=0`). Relevant capability/precision GTests passed 3/3; complete compiled-effects-on and option-off graphs built with `-j 3`; renderer identity/combination/target/runtime/descriptor, CNAEXT Doxygen, and whitespace gates passed. `GraphicsCapability::OcclusionQuery` is now true. |
| RLGL-053 | ✅ | RLGL-006 | Implemented a recursive owning-thread/context lease over CNA's `IPlatformGlContext`: the outer acquisition captures the caller binding and release policy, nested GraphicsDevice operations share it, frame completion may release RLGL's own binding, unrelated bindings are always restored, and a recursive mutex serializes a complete frame or content load while the one context moves between threads. `PlatformGlContextOwner` now owns the reusable restore/release transaction, with fake-platform unit coverage. Validated 2026-09-13 on SDL3-offscreen/Mesa-llvmpipe: exact binding/nesting/handoff, two-worker frame exclusion (240 acquisitions), real background XNB model creation plus two draws, and smoke/sequential lifecycles passed 4/4; all four executables passed AddressSanitizer (`detect_leaks=0`), and the five common context-owner unit tests passed. CI compiles all three lease workloads. |
| RLGL-054 | ✅ | RLGL-053 | Added one renderer lifetime registry sharing the RLGL context-ownership mutex. Texture2D, TextureCube, RenderTarget2D/RenderTargetCube, vertex/index buffers, SpriteBatch pipelines, occlusion queries, and optional compiled effects register only after successful native construction; ordinary destructors unregister and release with the device context current, while renderer shutdown closes the registry and releases children in reverse creation order before renderer-owned MojoShader pipelines, samplers, rlgl, and the platform context. The registry retains no C++ resource ownership, serializes disposal against shutdown, is idempotent, and makes delayed destructors count-only no-ops after all handles have been cleared. Validated 2026-09-13 on SDL3-offscreen/Mesa-llvmpipe: the focused test passed 15/15 gates with compiled effects enabled (nine registered children including the embedded SpriteEffect) and disabled (eight children), covering an active query, exact zeroed old handles, repeated shutdown, a second context recycling the old Texture2D name, delayed destruction without native callbacks, intact fresh Texture2D/cube/2D target/cube target/buffer/query/SpriteBatch behavior, and ordinary in-life disposal. Twelve focused/regression CTests passed after correcting the existing TextureCube XNB fixture's registered working directory. The lifetime, smoke, and compiled-effect draw executables passed AddressSanitizer (`detect_leaks=0`); complete option-on and option-off graphs built with `-j 3`; renderer identity/combination/target/runtime/descriptor, CNAEXT Doxygen, workflow YAML, and whitespace gates passed. CI compiles the lifetime executable. |
| RLGL-055 | ✅ | RLGL-054 | Added a context-serialized recovery subset to the native lifetime registry and initialized its future-registration policy from `GraphicsRendererCreateArgs`, with live forwarding from `GraphicsDevice::SetContextRecoveryEnabled`. Recovery-enabled Texture2D retains exact format-native bytes plus the defined mask for every uploaded mip and shares public level-zero storage instead of duplicating it; TextureCube retains face-major defined masks and full shadows assembled transactionally from partial Color rows or DXT block rectangles. Vertex/index resources retain exact capacity bytes and vertex declarations only when registered. Both target families retain their complete attachment descriptions but deliberately report `contentLostOnReset` with zero content-shadow bytes, preserving the existing `GraphicsResource`/`IContentLosable` contract for the future reset transaction. SpriteBatch/query fixed descriptions and optional compiled-effect bytecode/value state are registry-visible without duplicating unnecessary data. Disabling recovery affects subsequent resources only: those resources remain in deterministic teardown, issue the same native uploads/readbacks (including fallback DXT's operational block copy), and allocate no recovery shadows; re-enabling affects only later creations. Validated 2026-09-13 on SDL3-offscreen/Mesa-llvmpipe: the focused executable passed 14/14 gates in all four compiled-effects-on/off × native/forced-DXT-fallback combinations, measuring 278 bytes of exact texture/buffer shadows before the optional embedded effect and nine defined texture subresources, while classifying exactly the two target families for content loss. Nine lifetime/texture/cube/buffer/target CTests passed. Native/fallback focused runs and the delayed-destruction lifetime test passed AddressSanitizer (`detect_leaks=0`); complete compiled-effects-on and option-off graphs built with at most four jobs; renderer identity/combination/target/runtime/descriptor, CNAEXT Doxygen, workflow YAML, and whitespace gates passed. CI compiles the focused executable. |
| RLGL-056 | ✅ | RLGL-053, RLGL-054 | Implemented the renderer-level lost/recreating/unavailable/available transaction. Loss atomically closes `CanBeginDrawEXT`, invalidates every registered child and renderer-owned native identity without issuing GL calls, abandons only rlgl's CPU allocation graph, and raises Lost once. Restore raises Resetting, replaces the CNA platform context while safely retargeting an active outer context lease, reloads standalone rlgl/limits/swap interval, recreates every realized sampler plus renderer-owned primitive/compiled-draw/MojoShader core, reapplies blend/depth-stencil/rasterizer/viewport/scissor state, and raises Reset only after the complete transaction. Failures before context creation, after context creation, after rlgl initialization, or after state restoration clean partial native state, keep the generation unchanged, expose an exact diagnostic, close drawing, and permit a successful retry. At this milestone registered child pipelines/effects and resource contents deliberately remained invalid; RLGL-057 subsequently restores them. Validated 2026-09-13 on SDL3-offscreen/Mesa-llvmpipe: the focused executable passed 11/11 gates with compiled effects enabled and disabled, including two complete loss/recreate cycles, exact event/status order, all four injected stages, semantic state equality, and post-recreate clear/readback. Nine smoke/lease/lifetime/recovery-shadow/state/sampler/primitive CTests passed; focused context/lifetime executables passed AddressSanitizer (`detect_leaks=0`); complete option-on and option-off graphs built with at most four jobs; renderer identity/combination/target/runtime/descriptor, CNAEXT Doxygen, workflow YAML, and whitespace gates passed. CI compiles the new context-recreate executable. |
| RLGL-057 | ✅ | RLGL-055, RLGL-056 | Added one creation-order child restoration transaction after renderer-core recreation. It reconstructs every recovery-registered Texture2D mip and TextureCube face/mip from exact retained Color or DXT bytes (re-evaluating native versus decoded storage on the replacement context), exact vertex/index buffer capacity and bytes plus declarations, both target attachment families with their achieved MSAA/depth/format descriptions and existing `ContentLost` semantics, SpriteBatch pipelines, fresh occlusion queries, and optional compiled effects from retained bytecode, values, textures, and technique. Active single/MRT 2D/cube targets are rebound only after every replacement name is valid; viewport/scissor and device state then target the rebuilt framebuffer. A zero-based injected child failure reverses every replacement identity, suppresses Reset/content-loss notification, and leaves the device unavailable for retry; any later-stage failure also rolls all child replacements back. Validated 2026-09-13 on SDL3-offscreen/Mesa-llvmpipe: `cna_test_rlgl_resource_recreate` passed 12/12 gates with compiled effects enabled and 10/10 disabled in all four option-on/off × native/forced-DXT-fallback combinations. The test proves total rollback, retry, two complete loss/restore cycles, exact mip/face/buffer/declaration/target state, active mixed MRT and single-target rebinding, `ContentLost`, query reuse, restored SpriteBatch texture pixels, stock indexed draws, and optional retained compiled-effect pixels. Fourteen focused native regressions and three forced-fallback texture regressions passed; resource recreation in both DXT modes plus context recreation and lifetime passed AddressSanitizer (`detect_leaks=0`); complete option-on and option-off graphs built with at most four jobs; renderer identity/combination/target/runtime/descriptor, CNAEXT Doxygen, workflow YAML, and whitespace gates passed. CI compiles the resource-recreate executable. |
| RLGL-058 | ✅ | RLGL-057 | Closed the lifecycle matrix. SDL2/SDL3 GL descriptions request and report exact RGBA/depth/stencil/MSAA plus robust-access/reset-notification attributes. At this milestone RLGL requested robust and optional multisample contexts with deterministic non-robust/non-MSAA fallbacks; `RLGL-060` subsequently moved backbuffer samples into renderer-owned storage while retaining the robust-context fallback. Core/KHR/ARB reset-status polling converts a detected reset or failed present into the once-only lost transaction, and `PresentationParameters` reports the applied backbuffer. Ordinary `GraphicsDevice::Reset()` leaves context generation/resources/content intact and raises Resetting/Reset with Normal status; genuine loss raises Lost/Resetting/Reset with Lost/NotReset/Normal and target `ContentLost`. Creation (four stages), binding (before/after make-current), recreation (four stages), and child restoration failures all have rollback evidence. SpriteBatch and OcclusionQuery dispose renderer state idempotently and reject every operational use after disposal; all other implemented families retain their existing disposal/lifetime coverage. Validated 2026-09-13 on SDL3-offscreen/Mesa-llvmpipe: the lifecycle executable passed 14/14 gates in compiled-effects-on and off graphs, while the original run reported the driver's truthful Color/Depth24Stencil8/0-sample non-robust platform fallback. All 85 RLGL CTests passed after fixing two shared golden registrations to run from the source root. The lifecycle, bind, context, resource, and query-lifetime executables passed AddressSanitizer (`detect_leaks=0`); complete option-on/off graphs built with at most four jobs; OPENGL33/EasyGL and Software common/renderer regression targets built; the SDL2 platform plus RLGL renderer compiled against SDL 2.30.11; identity/combination/target/runtime/descriptor/platform-contract/CNAEXT-Doxygen/workflow-YAML/whitespace gates passed. Windows, macOS, on-screen Linux, and SDL2 runtime behavior remain explicitly unvalidated; robust asynchronous reset polling is conditional on the granted platform/driver contract. CI compiles the lifecycle executable. |
| RLGL-059 | ✅ | RLGL-019 | The parity campaign found that RLGL rejected three valid stock-effect declaration shapes which EasyGL renders: a BasicEffect draw whose explicit declaration has no `Normal0`, the same lit draw with no `Color0`, and a DualTextureEffect draw with no `TextureCoordinate1`. RLGL now derives the effective lighting and vertex-colour gates from explicit semantics instead of stride/effect flags alone, accepts an absent secondary UV as the GL/XNA default attribute, resets every disabled generic attribute to `(0,0,0,1)` before submission, and still rejects a present semantic with the wrong format. Validated on SDL3-offscreen/Mesa llvmpipe with the unchanged `unlit_position_color`, `basic_effect_vertex_color`, and `dual_texture_uv1` central parity fixtures plus the unchanged linear-filter and DualTextureEffect EasyGL golden consumers: 5/5 passed after all five initially exposed the two strict-validation paths. |
| RLGL-060 | ✅ | RLGL-019 | Replaced the platform multisample visual with a renderer-owned multisample color/depth framebuffer, single-sample resolve texture, and explicit resolve-to-platform blit. Creation and ordinary Reset clamp the requested count against the live GL limit; resize and native recreation transactionally rebuild storage; target switches restore the multisample framebuffer; readback selects the resolved default backbuffer; present resolves before swap. Validated 2026-09-13 on SDL3-offscreen/Mesa-llvmpipe: the unchanged shared `backbuffer_msaa` fixture passed all four construction/coverage/interior/target-switch checks with a real applied 4× count and an intermediate edge pixel, its `--reset` mode passed all eight checks across 4×→0→4× transitions, the updated lifecycle matrix passed 14/14 including Reset and native-context restoration of renderer-owned MSAA, and smoke plus nine render-target regressions passed. Focused targets built with at most four jobs. |
| RLGL-061 | ✅ | RLGL-059 | Replaced two stale renderer-local negative assertions that still required BasicEffect to reject a declaration without `Normal0` and DualTextureEffect to reject one without `TextureCoordinate1`. They now prove the same valid behavior as the central parity corpus: BasicEffect selects its unlit red path and DualTextureEffect consumes the disabled attribute's default zero coordinate to produce an exact blue result. Both focused executables passed on SDL3-offscreen/Mesa-llvmpipe after the complete RLGL CTest run exposed only these obsolete expectations. |
| RLGL-062 | ✅ | RLGL-049, RLGL-050, RLGL-057 | Implemented the remaining classic `Texture3D` resource boundary for `SurfaceFormat::Color`. RLGL now queries the live volume extent; allocates every width/height-derived mip while halving all three stored dimensions; performs exact full/partial slice-major upload and desktop readback; binds `GL_TEXTURE_3D`; publishes `GraphicsCapability::Texture3D` and real sampling; retains exact defined-level shadows; and recreates the resource transactionally after context loss. Source `ShaderEffect` and optional MojoShader compiled effects bind volume parameters with dimension validation at fragment and vertex stages; sampler objects carry filter plus U/V/W state unchanged. Non-Color labels are explicitly refused because CNA's public volume transfer API exposes Color arrays rather than their format-native payloads. Validated 2026-09-13 on SDL3-offscreen/Mesa-llvmpipe: six reused EasyGL contracts passed slices, all mips, partial upload/readback, source `sampler3D`, and independent `AddressW`; the focused recovery test passed capability/format/binding/shadow/recreation/disabled-shadow gates; the shared compiled cube/volume contract executed both depth slices without a skip; all 40 applicable shared Texture3D/partial-transfer GTests passed (one unsupported-renderer control skipped by design); all 24 RLGL compiled-effect GTests and all 141 registered RLGL CTests passed. The complete option-on meta-target and a clean option-off renderer/source-volume configuration built with at most four jobs. Both modified shared EasyGL source-volume fixtures also passed directly on OpenGL33. Renderer identity/combination/target/runtime/descriptor, CNAEXT Doxygen, workflow YAML, and whitespace gates passed. |

Tasks discovered while implementing will be appended with stable identifiers; existing identifiers will not be renumbered.

## 9. Validation strategy

Validation proceeds in increasing cost:

1. Renderer identity and descriptor consistency scripts.
2. RLGL configuration and compile-only build.
3. Unit tests that do not require a graphics context.
4. Runtime smoke under a real or virtual GL 3.3 context.
5. Focused resource/state/effect tests with readback.
6. Existing shared parity corpus run under both EasyGL and RLGL.
7. Existing representative samples, preferring repository workloads over parallel applications.
8. Golden-image or pixel comparisons for output-sensitive behavior.
9. Cross-renderer regression builds so RLGL changes do not break established renderers.

Every completed implementation task records the exact commands and result in its task entry or a dated evidence note. Unsupported paths must return truthful capability data or explicit errors; a no-op success is never acceptable.

## 10. CI, documentation, and performance gates

CI should always configure and compile the RLGL backend. Runtime coverage is added only on workers with a proven GL 3.3 context, using the same headless/offscreen mechanism already accepted by CNA where possible. The default and at least one established GL renderer must continue to build after shared changes.

Documentation must explain `-DCNA_GRAPHICS_RENDERER=RLGL`, the exact pinned source and offline override, OpenGL 3.3/core requirements, supported platform/context providers, current parity backed by this matrix, debugging paths, license notices, and the dependency update procedure.

Performance work begins only after the representative correctness gate. Measurements must identify redundant state transitions, binds, uploads, framebuffer changes, flushes, conversions, allocations, or lookup overhead with a reproducible workload. No optimization may weaken XNA semantics.

## 11. Final parity gate

“RLGL ↔ EasyGL classic XNA parity reached” may be stated only when RLGL-022 completes with:

- every classic-XNA matrix row either validated as supported or documented as an unavoidable rlgl/GL-profile limitation with an explicit observable behavior;
- the EasyGL and RLGL contract inventories reconciled;
- representative workloads and shared parity tests passing on supported platforms;
- no hidden stub, fallback, silent no-op, stale-state, resource-lifetime, or shader/effect gap;
- CI/build integration and user/update/debug/license documentation verified;
- CNAEXT gaps separately listed and not confused with the classic-XNA result.

RLGL-022 satisfies this gate on the validated Linux SDL3-offscreen/Mesa configuration. Therefore
“RLGL ↔ EasyGL classic XNA parity reached” is the authoritative status, subject to the explicit
platform, single-device, robustness, public-transfer-surface, and CNAEXT limitations above.
