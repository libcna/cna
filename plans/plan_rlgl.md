# RLGL Renderer Plan

**Status:** Active — device, classic Texture2D/samplers, pipeline state, buffers, primitive submission, BasicEffect/AlphaTestEffect/DualTextureEffect/SkinnedEffect, built-in SpriteBatch/SpriteFont, and Color RenderTarget2D/MSAA slices complete
**Target identity:** `RLGL`  
**Primary parity reference:** EasyGL  
**Initial profile:** OpenGL 3.3 core  
**Upstream baseline:** raylib 6.0, commit `dbc56a87da87d973a9c5baa4e7438a9d20121d28`, standalone `src/rlgl.h`

## Status legend

- ✅ **Completed** — implementation or evidence exists and the recorded validation passed.
- 🟨 **In progress** — work exists, but the task's validation gate has not passed.
- ⬜ **Not started** — no implementation should be inferred.
- ⛔ **Blocked / intentionally unsupported** — the exact technical or product reason is recorded.

Only ✅ items count as complete. Compiling code alone is not sufficient evidence for a rendering-capability task.

## Status summary

- Repository architecture, EasyGL responsibilities, platform GL ownership, renderer selection, resource interfaces, effect flow, test infrastructure, and upstream rlgl 6.0 have been audited.
- `RLGL` is a registered compile-time/runtime renderer identity backed by a SHA-256-verified standalone-rlgl dependency target. Its factory now constructs a real CNA-owned OpenGL 3.3 core context and standalone rlgl device.
- The first vertical slice is runtime-validated: selection → CNA-created GL context → rlgl loader/init → `GraphicsDevice` → selective clear → readback → present → resize → shutdown, including two sequential device lifecycles.
- Every classic-XNA Texture2D format is runtime-validated. Uncompressed layouts preserve exact XNA bytes and sampling swizzles; DXT1/3/5 use native S3TC storage when rlgl detects it and otherwise decode to RGBA8 renderer storage while retaining exact compressed block-transfer/readback semantics. Both DXT paths have pixel and transfer evidence.
- XNA sampler state is runtime-validated through independent GL 3.3 sampler objects on all 16 slots: all nine filter ordinals, U/V/W addressing, mip selection/bias, comparison disable, anisotropy capability/clamping, stale-state reset, slot independence, and sampled pixels have focused evidence.
- XNA blend, depth/stencil, and rasterizer state is runtime-validated through rlgl wrappers plus narrowly-scoped GL 3.3 bridge calls: exhaustive enum/native-state transitions and representative blend, mask, depth, stencil, cull, wireframe, and scissor pixels pass.
- CNA-owned low-level SpriteBatch scheduling is runtime-validated over rlgl shader, VAO, VBO, IBO, uniform, texture, sampler, and indexed-draw wrappers. Built-in texture and SpriteFont rendering covers all sort modes, transforms, float destinations, rotation/origin/flips, blend-state persistence, clipping, viewport-local coordinates, address modes, texture groups, and safe 16-bit capacity flushes. Custom Effect composition remains coupled to RLGL-012.
- Fixed-capacity rlgl vertex and 16/32-bit index resources plus primitive submission are runtime-validated. Every XNA topology works through non-indexed and indexed paths, including exact `vertexStart`, `startIndex`, `baseVertex`, semantic-driven Position0/Color0 binding, every classic vertex element's native VAO shape, WVP transforms, and public bound/user draws. Stock shader families and multi-stream input remain separate gates.
- The reusable stock-effect pipeline is runtime-validated for complete classic BasicEffect and AlphaTestEffect behavior. In addition to the unlit/alpha-test slice, BasicEffect now binds Normal0 by semantic and passes EasyGL/XNA pixel oracles for untextured/textured lighting, ambient/emissive, all three directional diffuse/specular lights, material specular and eye position, per-vertex/per-pixel selection, lit vertex color, D3D9 vertex-output saturation, inverse-transpose world normals, large-world precision, alpha, and lit fog.
- DualTextureEffect is runtime-validated with independent TextureCoordinate0/1 semantics and texture/sampler slots, XNA's doubled first-texture combine, null-texture white fallbacks, diffuse/alpha/vertex color, fog, and deterministic transition back to another stock effect.
- SkinnedEffect is runtime-validated for 1/2/4 weighted bone influences, the complete 72-bone palette, Byte4 and Vector4 blend indices, post-skin position/fog and joint/world normal transforms, texture/default-white sampling, materials, three lights/specular, per-vertex/per-pixel selection, and stock-effect transitions. The existing CNAEXT `VertexColorEnabled` option also passes through the same path and is tracked separately below. The palette is packed into the guaranteed OpenGL 3.3 vertex-uniform budget rather than relying on a larger driver-specific limit.
- `SurfaceFormat::Color` RenderTarget2D is runtime-validated with rlgl-owned framebuffer/texture names, exact single/multisample Depth16/Depth24/Depth24Stencil8 attachments, target switching, active and unbound resolve/readback, resolve-before-mipmap generation, CPU upload, top-left readback, Preserve/Discard behavior, target-local viewport/scissor coordinates, depth-bias rescaling, and upright SpriteBatch/stock-effect sampling. MRT and the remaining renderable classic formats stay separate gates.
- `docs/rlgl-renderer.md` records the verified boundary, profile/platform status, build, offline dependency, update, licensing, and debugging procedures. A Linux compile-focused CI lane builds the renderer and all focused executables from an independently checked-out copy of the exact upstream commit without claiming hosted runtime coverage.
- Classic XNA 4.0 parity is the priority. Existing CNA extensions are tracked separately and do not gate the classic parity declaration.

## Current limitations

1. `RLGL` configures, fully builds, and passes its renderer-local smoke test with Mesa llvmpipe 25.0.7 through SDL's `offscreen` GL driver (OpenGL 4.5 core granted for the 3.3-core request). This proves the current Linux vertical slice, not untested Windows/macOS behavior.
2. Device lifecycle, physical/logical presentation tracking, exact selective clears, viewport/scissor coordinates, swap interval, default-framebuffer restoration, complete backbuffer pipeline state, all 20 classic Texture2D formats, GL sampler objects, fixed-capacity vertex/index resources, every primitive topology, public bound/user primitive routes, BasicEffect/AlphaTestEffect/DualTextureEffect/SkinnedEffect, built-in SpriteBatch/SpriteFont rendering, and single/multisample Color RenderTarget2D are implemented. EnvironmentMap/custom effects, multi-stream input, MRT, non-Color targets, cube resources, reset, and query resources remain unavailable and throw stable diagnostics where a required hook exists.
3. Upstream rlgl keeps one process-global `RLGL` state object. The initial backend will therefore enforce one live RLGL device and will not claim concurrent multi-context support.
4. Several GL 3.3 operations required for XNA fidelity have no rlgl 6.0 public wrapper: arbitrary primitive modes and 32-bit index draws, depth comparison, stencil configuration, depth bias, indexed color masks, and some multisample attachment operations. Renderer-private bridge calls through the GL dispatch loaded by rlgl are permitted only for such measured gaps; they do not create CNA public API.
5. OpenGL 3.3 cannot provide every modern CNA extension (for example GL 4.3 compute/SSBO/image-load-store facilities). These are not classic XNA parity blockers.

## 1. Mission

The RLGL renderer is a CNA graphics backend that implements `CNA::Internal::Renderers::Common::IGraphicsRenderer` and its resource interfaces using standalone upstream rlgl. CNA remains the framework and owns XNA behavior, resource objects, effects, the game loop, windowing, input, audio, and platform services.

The verified architecture is:

```text
Microsoft::Xna::Framework graphics API
  -> GraphicsDevice / resources / effects
  -> CNA IGraphicsRenderer contracts
  -> CNA RLGL renderer and renderer-owned resource objects
  -> standalone rlgl low-level resource/state wrappers
  -> OpenGL 3.3 core
  -> CNA IPlatformGlContext
  -> selected CNA platform implementation (SDL2 or SDL3 today)
  -> platform OpenGL implementation
```

It is explicitly not:

```text
CNA -> raylib application framework -> rlgl
```

The backend will not initialize raylib, create a raylib window, use raylib input/audio/game-loop/resource APIs, or route any public XNA API through raylib types. Low-level rlgl APIs are preferred over rlgl's immediate-mode default batch because CNA already owns draw ordering, resources, explicit pipeline state, and effect semantics.

No new CNAEXT graphics API will be introduced to accommodate RLGL.

## 2. Dependency strategy

### Decision

Use CMake `FetchContent` to acquire the SHA-256-verified official `raysan5/raylib` source archive at the exact raylib 6.0 commit `dbc56a87da87d973a9c5baa4e7438a9d20121d28`, but expose and compile only the standalone `src/rlgl.h` implementation and its required `src/external` loader headers. Do not configure, build, or link the raylib library.

This follows CNA's existing pinned-source dependency pattern used by Sokol and PortableGL while keeping the dependency surface substantially smaller than the full framework. An offline source override will be provided through `FETCHCONTENT_SOURCE_DIR_RLGL`, consistent with CMake's standard FetchContent convention.

### Recorded dependency facts

| Item | Decision / evidence |
|---|---|
| Upstream project | `https://github.com/raysan5/raylib` |
| Revision policy | Exact immutable commit archive plus SHA-256 for the selected stable release; never a moving branch or unverified download |
| Initial revision | raylib 6.0, `dbc56a87da87d973a9c5baa4e7438a9d20121d28` |
| License | zlib/libpng license; preserve the upstream notice and record it in CNA third-party documentation |
| Used source | Standalone `src/rlgl.h`; one CNA-owned implementation translation unit defines `RLGL_IMPLEMENTATION` |
| Excluded source | raylib windowing, core loop, input, audio, model, image, and application/resource layers |
| GL loader | rlgl's bundled GLAD implementation for desktop GL 3.3; call `rlLoadExtensions()` with CNA's platform proc-address loader after making the context current |
| Platform libraries | Existing CNA platform/OpenGL context dependencies; no new native window library |
| Update procedure | Change the pinned commit, review `rlgl.h`, loader and license diffs, build the RLGL configuration, run contract/unit/parity/smoke suites, then update this plan and dependency documentation |

Vendoring a copied header would make upstream provenance and updates less visible. A system raylib package is rejected because it is not reproducible, often exposes only the full framework library, and may be compiled for an incompatible graphics profile. Linking full raylib is rejected because standalone rlgl is sufficient.

## 3. GL context ownership

The repository already has an appropriate GL ownership boundary in `IPlatformGlContext`, `PlatformGlContextOwner`, and `PlatformGlSurfaceState`. RLGL will reuse it.

| Concern | Owner | RLGL responsibility |
|---|---|---|
| Window creation/destruction | CNA platform/window service | Request an OpenGL-capable descriptor; never create a raylib window |
| GL context creation/destruction | CNA `IPlatformGlContext`, held through `PlatformGlContextOwner` | Request GL 3.3 core attributes, bind for rlgl init/use, release through CNA RAII |
| Context switching | CNA GL context service and renderer thread-context lease | Keep rendering calls on the owning thread/context; add explicit lease/rebind coverage before claiming reset or multithread support |
| Proc-address loading | CNA platform GL service | Pass the CNA loader to `rlLoadExtensions()` |
| Buffer swap | CNA GL context service | `Present()` flushes pending work and calls `SwapBuffers()` |
| Drawable/backbuffer size | CNA window surface plus `PlatformGlSurfaceState` | Re-query the drawable extent, update GL viewport and projection-dependent state on resize |
| Resize events | CNA window/event infrastructure | React through existing renderer/presentation entry points; do not install raylib callbacks |
| Vsync | CNA presentation interval / requested swap interval | Call the platform context's `SetSwapInterval()` |
| Fullscreen | CNA window service | Use existing `GraphicsDevice` presentation-mode flow; rlgl has no ownership role |
| Backbuffer MSAA | CNA renderer creation arguments plus platform context attributes | Request compatible sample attributes and report the achieved value; later validate resolve behavior |

rlgl initialization must occur only after a current context and successful extension load. Shutdown must make the same context current, release all renderer-owned resources, call `rlglClose()`, and then destroy the CNA-owned context.

Because rlgl 6.0 stores its state in a single static global object, the initial implementation will guard against a second live RLGL device. Supporting simultaneous RLGL devices would require upstream isolation work and is not implied by CNA's multi-renderer compile mode.

## 4. Supported rlgl profiles

Upstream rlgl 6.0 has compile-time paths for OpenGL software, OpenGL 1.1, 2.1, 3.3 core, 4.3 core, OpenGL ES 2, and OpenGL ES 3. The public CNA renderer identity remains one `RLGL` identity; profiles are an implementation/build choice rather than separate renderer identities.

| rlgl profile | Assessment | Initial decision |
|---|---|---|
| OpenGL 1.1 | Fixed pipeline; lacks programmable shaders, FBOs, modern buffers, and instancing needed by EasyGL/XNA workloads | Not supported |
| OpenGL 2.1 | Programmable but materially limits VAO, instancing, MRT, integer attributes, and framebuffer behavior | Deferred; not a parity target |
| OpenGL 3.3 core | Best match to CNA's existing desktop GL baseline; supports shaders, VAOs, FBOs, MRT, instancing, and broad texture/state coverage | **Selected initial profile** |
| OpenGL 4.3 core | Adds compute, SSBOs, image operations, and broader modern functionality, mostly CNAEXT rather than classic XNA | Future optional profile after GL 3.3 parity |
| OpenGL ES 2 | Too constrained for the first EasyGL parity campaign | Deferred |
| OpenGL ES 3 | Plausible portability target, but shader versions, readback, texture formats, state, and platform context behavior require a separate measured campaign | Future optional profile |

No public `RLGL33`, `RLGL43`, or `RLGLES` renderer identities will be created. A build-time profile option may be added only when a second profile has real implementation and validation.

## 5. EasyGL capability matrix

This matrix records measured implementation shape, not promises. “Direct GL bridge” means a renderer-private helper in the same translation unit as standalone rlgl, using the GL dispatch initialized by rlgl only where rlgl lacks a required public wrapper.

| Area | Capability | EasyGL | Upstream rlgl 6.0 / GL 3.3 | RLGL status | Classification / next evidence |
|---|---|---|---|---|---|
| Device | Initialization | Implemented | `rlLoadExtensions`, `rlglInit` | ✅ | Two sequential real GraphicsDevice/context/rlgl lifecycles passed under Mesa llvmpipe; single-live guard release proven |
| Device | Resize | Implemented | Viewport/matrix APIs; CNA owns drawable size | ✅ | GraphicsDeviceManager resize, synchronized surface refresh, logical/physical rectangle checks, and post-resize pixel readback passed |
| Device | Fullscreen | Implemented through CNA platform | No window API needed | ⬜ | Straightforward integration; platform smoke |
| Device | Vsync | Implemented through platform context | No rlgl ownership | 🟨 | Platform swap-interval forwarding and requested-value reporting implemented; runtime verification is RLGL-007 |
| Device | Clear | Color/depth/stencil and selective planes | Public helper clears color+depth and omits stencil; exact selective clear needs bridge | ✅ | Distinct color and combined color/depth/stencil clears passed exact pixel readback; exhaustive write-mask transitions remain RLGL-013 |
| Device | Present | Implemented | CNA context swap; no rlgl batch is used | ✅ | Explicit GraphicsDevice.Present passed in both runtime lifecycle cycles |
| Device | Viewport | Implemented | `rlViewport` | 🟨 | Top-left CNA to bottom-left GL mapping plus depth range implemented; pixel parity test remains |
| Device | Scissor | Implemented | `rlScissor`; enable belongs to rasterizer state | 🟨 | Rectangle mapping implemented; clipping/state enable tests remain RLGL-013 |
| Device | Backbuffer formats | Negotiated/reported | GL framebuffer plus CNA context attributes | ⬜ | Requires architectural work; achieved-format reporting |
| Device | MSAA | Implemented | rlgl enables context MSAA and exposes framebuffer blit; exact attachment control is incomplete | ⬜ | Requires architectural work; sample/resolve pixels |
| Textures | Texture2D | Implemented | `rlLoadTexture`, update, bind, unload; several exact formats need a bridge | ✅ | All 20 classic formats passed exact transfer and sampled-pixel validation in RLGL-023/026/027/028 |
| Textures | TextureCube | Implemented | `rlLoadTextureCubemap`, cubemap binding and FBO faces | ⬜ | Straightforward implementation; six-face sample |
| Textures | Mipmaps | Implemented | `rlGenTextureMipmaps`; arbitrary allocation/update lacks public wrappers | 🟨 | Declared RGBA8 chains plus exact non-zero RGBA8 and two-channel-float upload/readback passed RLGL-023/027; generation/filtering remain |
| Textures | Texture updates | Implemented | `rlUpdateTexture` supports level-zero subrect upload | ✅ | Public full and partial `SetData` paths round-trip exact bytes for all 20 classic formats, including odd-row and DXT block alignment |
| Textures | Readback | Implemented | Desktop `rlReadTexturePixels`; arbitrary levels/subrects and compressed readback unavailable | ✅ | Private desktop bridge provides exact native reads; the DXT fallback retains original block bytes so XNA-format readback stays exact after software decode |
| Textures | Filtering | Implemented | Texture parameters exist; GL 3.3 sampler objects require a private bridge | ✅ | All nine XNA min/mag/mip decompositions plus point/linear sampled pixels passed RLGL-025 |
| Textures | Address modes | Implemented | Texture wrap exists; GL 3.3 sampler objects require a private bridge | ✅ | U/V/W mapping, stale-state reset, slot independence, and Wrap/Clamp/Mirror pixels passed RLGL-025 |
| Textures | Anisotropy | Implemented when supported | rlgl detects extension/ceiling; sampler-object parameter needs a private bridge | ✅ | Capability follows rlgl's live extension probe; requested value clamps to the measured ceiling and non-anisotropic filters reset to one |
| Textures | Classic compressed formats | DXT1/DXT3/DXT5 with software fallback | DXT storage gated by detected S3TC extension | ✅ | rlgl's probe selects native storage; renderer-local DXT decode covers contexts without S3TC, with exact block transfers and sampled pixels validated in both modes |
| Render targets | RenderTarget2D | Implemented | FBO create/attach/check/unload | 🟨 | Color single/multisample path passed RLGL-039/041; remaining renderable classic formats are RLGL-042 |
| Render targets | MRT | Implemented | `rlActiveDrawBuffers`, maximum eight | ⬜ | Straightforward implementation; multi-attachment shader test |
| Render targets | Depth/stencil | Implemented | Depth/stencil attachment enums; exact packed allocation needs bridge | ✅ | Depth16/Depth24 rejection/acceptance and Depth24Stencil8 write/read pixels passed RLGL-039 |
| Render targets | MSAA resolve | Implemented | FBO blit wrapper; renderbuffer allocation needs a narrow bridge | ✅ | Device-clamped color/depth/stencil renderbuffers, active/unbound resolve, edge coverage, preservation, and resolve-before-mipmap matrices passed RLGL-041 |
| Render targets | Switching | Implemented | FBO enable/disable | ✅ | Backbuffer/target/target transitions, target-local viewport/scissor, mip finalization, preservation and discard passed RLGL-039 |
| Render targets | Readback | Implemented | FBO/read-pixel paths; format/orientation conversion needed | 🟨 | Exact Color level/subrect/top-left reads passed RLGL-039; remaining formats are RLGL-042 |
| Buffers | Vertex buffers | Implemented | VBO load/update/unload | ✅ | Fixed-capacity native size/usage and exact public packed bytes passed RLGL-030 |
| Buffers | Dynamic vertex buffers | Implemented | Dynamic upload flag and subrange update | ✅ | None/Discard/NoOverwrite preserve identity/capacity and exact updated prefixes |
| Buffers | 16-bit index buffers | Implemented | EBO wrappers and unsigned-short indexed draw | ✅ | Fixed-capacity allocation and exact values/tails passed RLGL-030; indexed pixels passed RLGL-031 |
| Buffers | 32-bit index buffers | Implemented | Upload works; draw wrapper hardcodes unsigned short | ✅ | Exact values above 65535 passed native storage; the private 32-bit submission gap passed RLGL-031 |
| Buffers | Dynamic index buffers | Implemented | Dynamic upload/update available | ✅ | Both widths passed None/Discard/NoOverwrite resource gates |
| Buffers | Buffer updates | Implemented | Whole/subrange update wrappers | ✅ | Fixed storage never shrinks; Discard uses the documented private orphan bridge |
| Buffers | Primitive topology | Points, lines, triangle list/strip | Low-level draw wrappers hardcode triangles | ✅ | rlgl wrappers handle triangle arrays/16-bit zero-offset indexed triangles; a private GL bridge covers the measured arbitrary-mode gap for all five topologies |
| Buffers | Vertex declarations | Implemented semantic/type mapping | Generic attributes and divisors; CNA must map declarations | ✅ | Position0/Color0 bind by semantic rather than declaration order; all twelve classic formats passed exact native component/type/normalization/stride/offset checks |
| Buffers | User primitives | Implemented | VBO or transient upload; default batch is semantically unsuitable | ✅ | Public `DrawUserPrimitives` passed transformed pixel output through GraphicsDevice's transient resource path |
| Buffers | Indexed user primitives | Implemented | Same plus index-type limitation | ✅ | Public 16-bit and 32-bit routes passed exact index-type and pixel output |
| Buffers | Instancing | Implemented | VAO attribute divisors and instanced draw wrappers | ⬜ | Requires architectural work; frequency/semantic tests |
| Pipeline | BlendState | Full XNA mapping | Blend enable plus combined/separate factors/equations | ✅ | All factors/equations, constant factor, sample mask, and premultiplied/opaque transition pixels passed RLGL-013 |
| Pipeline | DepthStencilState | Full XNA depth/stencil mapping | Depth enable/write exposed; compare and stencil ops have no public wrappers | ✅ | All compares/operations, writes, two-sided state, independent reference changes, and depth/stencil pixels passed RLGL-013 |
| Pipeline | RasterizerState | Cull/fill/scissor/depth bias | Cull/scissor/wire exposed; exact cull selection and bias need care/bridge | ✅ | Native A→B transitions plus cull winding, solid/wireframe, and scissor pixels passed RLGL-013 |
| Pipeline | SamplerState | Full XNA mapping | Texture parameters expose core behavior; sampler-object API is not wrapped | ✅ | Independent objects on 16 XNA slots passed complete native-state transitions and focused sampled-pixel validation in RLGL-025 |
| Pipeline | Color write masks | Per-target masks | Global color mask wrapper only | ✅ | Private indexed GL 3.3 masks passed all four native values and a masked-draw/Clear-restoration pixel oracle |
| Pipeline | Culling | Clockwise/counterclockwise/none | Enable/disable and face selection wrappers | ✅ | Both cull selections and none passed native transitions; opposite winding selections produced accepted/rejected pixels |
| Pipeline | Fill modes | Solid/wireframe | Wire/point helpers on desktop | ✅ | rlgl solid/wire wrappers passed native transitions and interior/edge pixel evidence |
| Pipeline | Depth bias | DepthBias/SlopeScaleDepthBias | No public rlgl wrapper | ✅ | Private polygon-offset call scales normalized XNA bias by the granted backbuffer depth precision; native transition passed |
| Pipeline | Stencil operations | Full front/back XNA stencil | No public rlgl wrapper | ✅ | Private GL 3.3 bridge passed all operations, two-sided functions/masks, stencil write/read pixels, and standalone reference mutation |
| Pipeline | Separate blending | Required by XNA state | Separate factor/equation wrappers exist | ✅ | rlgl custom-separate cache path passed independent native RGB/alpha state and premultiplied output |
| Shaders | BasicEffect | Implemented | Shader compile/link/uniform/attribute wrappers are sufficient | ✅ | RLGL-033/034 pass unlit and lit textured/untextured paths, vertex color, material/alpha/emissive, fog, transforms, three lights, specular, per-vertex/per-pixel selection, and pixel-center behavior |
| Shaders | AlphaTestEffect | Implemented | Same | ✅ | RLGL-033 passed all eight comparison functions plus texture/default-white, diffuse, vertex color, fog, transforms, and alpha-boundary pixels |
| Shaders | DualTextureEffect | Implemented | Multiple texture/sampler binding available | ✅ | Independent UV/texture/sampler slots, doubled combine, null fallbacks, material/color/alpha, fog, and stock-transition pixels passed RLGL-035 |
| Shaders | EnvironmentMapEffect | Implemented | Cubemap and shader support available | ⬜ | Requires architectural work; reflection/fog/lighting |
| Shaders | SkinnedEffect | Implemented | Uniform arrays available; GL 3.3 guarantees 1024 vertex-uniform components | ✅ | 72 bones packed as 216 vec4 rows keep the complete shader at 957 components; 1/2/4 influences, Byte4/Vector4 indices, lighting/material/fog/normal paths passed RLGL-037 |
| Shaders | Other built-in effect families | Implemented where CNA exposes them | General shader primitives available | ⬜ | Requires architectural work; inventory from EasyGL hooks |
| Shaders | Custom compiled Effect | Optional MojoShader path | rlgl can host generated GLSL, but versions/reflection/samplers need adaptation | ⬜ | Requires architectural work; compile/apply/pass tests |
| Shaders | Shader constants/uniforms | Implemented | Scalar/vector/matrix/array setters | 🟨 | Scalar, vec3/vec4, sampler, matrix, and a 216-element vec4 bone array pass; reflected custom parameters remain RLGL-038 |
| Shaders | Matrices | XNA conventions normalized by renderer shaders | Column-major rlgl matrix representation; raw uniform upload available | 🟨 | WVP/world, inverse-transpose world/joint normals, weighted bone matrices, and full 72-entry palette pass; EnvironmentMap/custom families remain RLGL-012 |
| Shaders | Textures/samplers | Implemented | Active texture, cubemap, sampler parameters | 🟨 | Texture2D units zero/one, per-slot samplers, null/default-white binding, and stale-state transitions pass; cubemap/reflection remain |
| Shaders | Vertex attributes | Semantic declaration mapping | Generic attributes plus integer/divisor variants | 🟨 | Position0/Normal0/Color0/TextureCoordinate0/1 and BlendWeight0/BlendIndices0 (Byte4 or Vector4) pass; multi-stream and custom reflection remain |
| Shaders | Fog | Implemented in stock effects | General shader support | 🟨 | BasicEffect, AlphaTestEffect, DualTextureEffect, and post-skin SkinnedEffect use the forwarded view-space-Z fog vector with pixel evidence; EnvironmentMapEffect remains |
| Shaders | Lighting | Implemented in stock effects | General shader support | 🟨 | BasicEffect and SkinnedEffect pass three-light/material/specular, vertex/pixel-stage, normal-transform, and precision contracts; EnvironmentMapEffect remains |
| Shaders | Skinning | Implemented | General shader support subject to uniform limits | ✅ | Complete 72-bone palette, 1/2/4 weighted influences, Byte4/Vector4 indices, inverse-transpose joint/world normals, and malformed declarations passed RLGL-037 |
| 2D | SpriteBatch | Implemented through renderer sprite-batch interface | High-level rlgl batch cannot preserve all CNA state/sort semantics; low-level buffers can | 🟨 | Built-in path passed RLGL-029; custom Effect composition waits for RLGL-012 |
| 2D | SpriteFont | Implemented through SpriteBatch | Texture/quad primitives sufficient | ✅ | Glyph atlas, newline, and mirrored glyph-order pixels passed RLGL-029 |
| 2D | Texture drawing | Implemented | Texture/shader/buffer primitives sufficient | ✅ | Source/destination, tint, float coordinates, UV addressing, texture groups, and capacity flush passed RLGL-029 |
| 2D | Rotation/scaling | Implemented | Shader/vertex math sufficient | ✅ | Scaled texture, transform matrix, and rotated source-space origin pixels passed RLGL-029 |
| 2D | Sorting | Implemented XNA sort modes | Must remain CNA-owned, not rlgl batch-owned | ✅ | Deferred, Immediate, Texture, FrontToBack, and BackToFront behavior passed RLGL-029 |
| 2D | Blending | Implemented | Blend APIs sufficient | ✅ | Default premultiplied AlphaBlend and per-draw Immediate state transition passed RLGL-029 |
| 2D | Clipping | Implemented | Scissor APIs sufficient | ✅ | Custom viewport and exact enabled-scissor inside/outside pixels passed RLGL-029 |
| 3D | Meshes | Implemented through Model/Mesh draw paths | General indexed vertex draws available with bridge gaps | 🟨 | General indexed topology/resources and BasicEffect texture/normal/lighting semantics work; model corpus and remaining stock families remain RLGL-012/RLGL-018 |
| 3D | Models | Implemented | Renderer consumes CNA model resources, not raylib models | ⬜ | Requires architectural work; existing model samples |
| 3D | Lighting | Implemented | Shader capability sufficient | 🟨 | BasicEffect and SkinnedEffect pass RLGL-034/037; EnvironmentMapEffect remains RLGL-036 |
| 3D | Skinning | Implemented | Shader/vertex capability sufficient within limits | 🟨 | SkinnedEffect's complete primitive/effect contract passes RLGL-037; representative animated Model workload remains RLGL-018 |
| 3D | Render targets | Implemented | FBO support available | ⬜ | Requires architectural work; multipass sample |
| 3D | Custom shaders | Implemented where MojoShader is enabled | Low-level shader APIs available | ⬜ | Requires architectural work; existing compiled-effect samples |
| 3D | Instancing | Implemented | Instanced draw and divisor APIs available | ⬜ | Requires architectural work; existing instancing samples |
| Other | Occlusion queries | Implemented | No rlgl public query wrappers; GL 3.3 provides query objects | ⬜ | Underlying rlgl limitation; renderer-private bridge/test |
| Other | Capabilities reporting | Detailed renderer limits/features | Extension flags are internal and GL limit wrappers are partial | ⬜ | Requires architectural work; bridge queried limits and truthful flags |
| Other | GraphicsAdapter | Descriptor-driven enumeration and support probes | Can use generic CNA adapter reporting before a live context | ⬜ | Straightforward baseline; live limits remain device-specific |
| Other | Resource disposal | Deterministic and context-aware | Unload wrappers available; require current context | ⬜ | Requires architectural work; idempotence/use-after-dispose tests |
| Other | Device reset/loss | EasyGL has recovery infrastructure | rlgl global state and default-resource recreation require coordinated reinit | ⬜ | Requires architectural work; loss/restore/resource-shadow tests |
| Other | Debug validation | Error/debug checks and diagnostics | `rlCheckErrors`, debug callback support depends on profile/extensions | ⬜ | Straightforward baseline, deeper validation later |
| Other | Threading assumptions | Context lease and renderer synchronization | rlgl global state is not thread-safe | ⬜ | Requires architectural work; owning-thread contract and lease tests |
| CNAEXT | SkinnedEffect.VertexColorEnabled | Implemented by EasyGL and other capable renderers | Color attribute and stock shader multiplication are sufficient | ✅ | Works through the same RLGL-037 primitive path and has dedicated pixel evidence; this does not gate classic XNA parity |
| CNAEXT | Texture3D and modern extras | EasyGL supports selected extensions | No public rlgl Texture3D wrapper; GL 3.3 can support some through a bridge | ⛔ | Not part of classic XNA parity; defer until the classic gate |
| CNAEXT | Compute/SSBO/image operations | EasyGL/profile-dependent | rlgl exposes these only for GL 4.3 | ⛔ | Underlying GL-profile limitation; intentionally deferred |

## 6. Shader and effect strategy

RLGL will not introduce a public shader language or effect API. The renderer must consume the same CNA `GpuDrawParams`, stock-effect inputs, `ShaderEffect`, and optional compiled-effect flow used by EasyGL.

The first shader task must inventory EasyGL's stock shader variants and the semantic-to-location, uniform, sampler, matrix, clip/depth, texture-orientation, color, fog, lighting, and skinning conventions. The GL 3.3 implementation will compile renderer-private GLSL 330 variants or reuse compatible shared sources where they actually match. Custom Effect support must reuse CNA's existing representation/translation path rather than accepting RLGL-specific sources.

Regression tests will isolate conventions: matrix layout, half-pixel behavior if applicable, clip-space depth, render-target texture orientation, vertex color normalization, integer attributes, sampler slots, fog endpoints, alpha test boundaries, and bone transforms. Shader variants will not be patched per sample.

## 7. State correctness design

The renderer will use rlgl's low-level resource and draw wrappers, not its default immediate/batch path, for CNA rendering. The default batch's draw records are texture-oriented; program, matrix, VAO, and some state changes force flushes and it unbinds/reset selected GL state after submission. Mixing that machinery with CNA's explicit state model would make state ownership ambiguous.

RLGLRenderer will own a deterministic state cache for program, textures, samplers, blend, depth, stencil, rasterizer, scissor, viewport, framebuffer, vertex/index buffers, and attribute arrays. Cache entries are invalidated after any operation that can mutate or reset state outside the renderer's controlled path. Renderer-private GL bridge operations must be centralized, documented by the missing rlgl wrapper they replace, and followed by the same cache rules.

Every state-family task must test both isolated application and transitions A → B → A. Render-target switches, clears, readbacks, effect passes, and context restoration must preserve or explicitly reapply XNA-visible state.

## 8. Implementation DAG and task ledger

| ID | State | Depends on | Task and completion evidence |
|---|---|---|---|
| RLGL-001 | ✅ | — | Audit renderer selection, common contracts, GraphicsDevice/Adapter, presentation, platform GL ownership, resources, effects, SpriteBatch, lifetime/reset, diagnostics, build/CI, renderer tests, parity infrastructure, and comparable renderer plans. Evidence is recorded in Sections 1–8. |
| RLGL-002 | ✅ | — | Audit upstream rlgl 6.0 profiles, configuration, loader, lifecycle, resource/state/shader/FBO/draw APIs, batch behavior, formats, limits, and restrictions. Evidence is recorded in Sections 2–7 and the matrix. |
| RLGL-003 | ✅ | RLGL-001, RLGL-002 | Decide the dependency, initial GL profile, context ownership, renderer identity, low-level strategy, singleton policy, and private bridge boundary. Decisions are recorded above. |
| RLGL-004 | ✅ | RLGL-001, RLGL-002 | Establish the measured EasyGL/rlgl/RLGL capability matrix and seed the concrete implementation DAG. Matrix reviewed against the public/common renderer contracts. |
| RLGL-005 | ✅ | RLGL-003 | Added `RLGL`/`Rlgl` throughout CMake, the generated registry, dense C++ enum/classifications/tests, append-only C ABI value 51 (ABI 0.27.0), physical-module and GL-combination inventories, and an honest throwing factory pending RLGL-006. `cmake/ThirdPartyRlgl.cmake` fetches the official immutable raylib 6.0 commit archive with SHA-256 `81b06c…e126`, exposes only `src/`, defines GL 3.3, and never configures raylib. Validated 2026-09-12: RLGL configure succeeded with `FETCHCONTENT_SOURCE_DIR_RLGL=/tmp/cna-raylib-6.0-source`; `cmake --build /tmp/cna-rlgl-build3 --target cna_renderer_rlgl -j 4` built the target; C and C++ ABI header probes compiled; identity, runtime-discipline, combination, platform-SDL and whitespace gates passed. |
| RLGL-006 | ✅ | RLGL-005 | Implemented a real factory and minimal renderer using CNA's `PlatformGlContextOwner`: requested/verified GL 3.3 core (with optional context-MSAA fallback and achieved attributes), one-live-device guard for rlgl's global state, CNA proc loader → `rlLoadExtensions`, transactional `rlglInit`, drawable bookkeeping, physical/logical viewport computation, selective color/depth/stencil clears with write-mask restoration, viewport/scissor mapping, swap interval/present, explicit pending-resource failures, registry lifetime, and context-current `rlglClose`. Standalone rlgl is isolated in one implementation TU; no raylib framework API is linked or called. Validated 2026-09-12: renderer target and full `/tmp/cna-rlgl-build3` configuration built successfully; renderer-target discipline, runtime-renderer discipline, CNAEXT Doxygen, and whitespace gates passed. Runtime GL evidence is deliberately separate in RLGL-007. |
| RLGL-007 | ✅ | RLGL-006 | Added exact RGBA8 backbuffer subrectangle readback through the rlgl-owned GL dispatch, including pack-alignment preservation and GL-to-XNA row normalization. Added `cna_test_rlgl_smoke`, always buildable for an RLGL configuration and registered with CTest when tests are enabled. It runs two sequential `Game`/`GraphicsDevice` lifecycles and checks requested logical size, truthful 3D capability, forwarded swap interval, pre-present color readback, combined color/depth/stencil clear, explicit Present, GraphicsDeviceManager resize + surface refresh, resized presentation rectangle/readback, and clean global rlgl release/reinit. Validated 2026-09-12 with `SDL_VIDEODRIVER=offscreen LIBGL_ALWAYS_SOFTWARE=1 /tmp/cna-rlgl-build3/cna_test_rlgl_smoke`: all 21 per-cycle/terminal checks passed on Mesa llvmpipe 25.0.7, OpenGL 4.5 core. Xvfb+x11 was attempted but this environment's SDL3 has no x11 driver; the registered smoke uses the proven offscreen path and exits 77 when no video device exists. |
| RLGL-008 | ✅ | RLGL-006 | Added `docs/rlgl-renderer.md` with the measured architecture, enablement/offline commands, pinned dependency/update procedure, license link, Linux-only runtime evidence, explicit unvalidated platforms, diagnostics, and current refusal boundary; linked it from the primary documentation indexes and refreshed stale identity/status text. Added `.github/workflows/rlgl-ci.yml`, a Linux compile gate that independently checks out the exact raylib commit, configures without dependency network access, builds `cna_renderer_rlgl` plus `cna_test_rlgl_smoke`, and checks registry consistency; it deliberately does not claim hosted runtime coverage. Validated 2026-09-12: workflow YAML parsed; fresh `/tmp/cna-rlgl-ci-check-008` configure and focused build passed with the documented switches and offline source override (system SDL substituted only because this worktree's vendored SDL submodule is absent); the freshly built smoke passed all 21 checks under SDL3 offscreen/Mesa llvmpipe; identity, runtime-discipline, platform-SDL, and whitespace gates passed. |
| RLGL-009 | ✅ | RLGL-006 | Texture2D and sampler umbrella. RLGL-023/026/027 implement every uncompressed classic format, RLGL-028 completes DXT1/3/5, RLGL-025 completes independent sampler objects, and RLGL-024 records the closed format inventory. Each subtask has exact transfer and sampled-pixel evidence. |
| RLGL-010 | 🟨 | RLGL-009 | Implement a CNA-owned low-level SpriteBatch path, then validate texture drawing, transforms, origin/effects, sorting, blending, clipping, and SpriteFont with existing tests/samples. RLGL-029 is the measured built-in-shader/resource baseline; custom Effect composition remains coupled to RLGL-012. |
| RLGL-011 | ✅ | RLGL-006 | Buffer/draw umbrella. RLGL-030 completed declaration-aware fixed-capacity resources and dynamic update hints; RLGL-031 completed every topology plus indexed/non-indexed and public bound/user submission. Multi-stream input and instancing remain explicitly separated into RLGL-032/RLGL-016. |
| RLGL-012 | 🟨 | RLGL-009, RLGL-011 | Shader/effect umbrella. RLGL-033 completes the shared unlit stock pipeline and AlphaTestEffect; RLGL-034/035/037 complete BasicEffect lighting, dual texture, and skinning; RLGL-036/038 retain environment mapping and custom/compiled effects. Only the completed subtasks count as available. |
| RLGL-013 | ✅ | RLGL-006 | Implemented every blend factor/function, independent color/alpha state, constant blend factor, four indexed color-write masks, multisample mask, all depth comparisons/writes, all stencil operations, two-sided stencil state, standalone `ReferenceStencil`, cull direction, solid/wire fill, scissor enable, and depth-format-scaled polygon offset. rlgl wrappers own enable/cull/wire and the custom-separate blend cache; the private GL 3.3 bridge covers only unwrapped state. Full assignments are deterministic rather than relying on a second CNA cache, while the minimal stencil record supports the independent reference property. The task moved ahead of buffers after inspection proved it independent and a correctness prerequisite for SpriteBatch. Validated 2026-09-12: `cna_test_rlgl_state` passed exhaustive native state transitions plus premultiplied/opaque blend, color-mask/Clear restoration, depth reject/accept, stencil write/read/reference, cull winding, solid/wireframe, and top-left scissor pixel oracles on SDL3-offscreen/Mesa-llvmpipe. AddressSanitizer passed. RasterizerState.MultiSampleAntiAlias remains a shared renderer-seam omission also present in EasyGL; render-target depth-bias re-scaling will be validated with RLGL-014. |
| RLGL-014 | 🟨 | RLGL-009, RLGL-013 | Render-target umbrella. RLGL-039 completes the single-sample Color/attachment/switch/readback/mip/usage baseline; RLGL-040/041/042 retain MRT, MSAA resolve, and remaining renderable formats. |
| RLGL-015 | ⬜ | RLGL-009, RLGL-014 | Implement TextureCube, cube render targets, compressed formats, broader readback/mipmap behavior, and 3D/model workload prerequisites. |
| RLGL-016 | ⬜ | RLGL-011, RLGL-012, RLGL-013 | Implement instancing and occlusion queries using rlgl wrappers where present and the documented bridge where absent. Validate divisors, instance frequency, query availability/result/lifetime. |
| RLGL-017 | ⬜ | RLGL-009–RLGL-016 | Implement disposal robustness, owning-thread/context leases, diagnostics, achieved capabilities, context loss/reset, and resource restoration. Validate failure injection and use-after-dispose behavior. |
| RLGL-018 | ⬜ | RLGL-010–RLGL-017 | Run the representative workload ladder: clear/present, textured triangle, indexed geometry, SpriteBatch/Font, multiple textures, render targets, depth 3D, AlphaTest/BasicEffect, lighting, fog, skinning, dynamic buffers, instancing, state switching, and multipass rendering. Add only missing focused probes. |
| RLGL-019 | ⬜ | RLGL-018 | Execute the systematic EasyGL classic-XNA parity campaign using shared/unit/parity tests and golden images; record each observable comparison and append every discovered gap as a concrete task. |
| RLGL-020 | ⬜ | RLGL-018 | Measure backend overhead after correctness: state/program/texture binds, uploads, framebuffer transitions, flushes, per-draw allocations, conversions, and lookups. Optimize only measured regressions and retain semantics. |
| RLGL-021 | ⬜ | RLGL-019 | Complete CI and documentation for supported platforms, runtime GL smoke availability, update procedure, debugging, licensing, parity status, and verified limitations. |
| RLGL-022 | ⬜ | RLGL-019–RLGL-021 | Final audit: rescan EasyGL and RLGL hooks/resources/state/errors/effects/tests/CI/docs, search TODO/stub/fallback paths, rerun representative suites, reconcile this matrix, and create tasks for every new classic-XNA gap. Only this task may declare parity. |
| RLGL-023 | ✅ | RLGL-006 | Implemented the `SurfaceFormat::Color` Texture2D baseline as a genuine rlgl resource: level zero is created by `rlLoadTexture`, released by `rlUnloadTexture`, and bound with rlgl texture-slot wrappers. A documented private GL bridge allocates/updates/reads arbitrary mip levels because rlgl 6.0 hardcodes level zero or full-texture behavior; binding and pixel-store state are restored around bridge operations. The resource tracks defined levels separately from allocated storage and reports the live `GL_MAX_TEXTURE_SIZE`. Validated 2026-09-12: `cna_renderer_rlgl` and `cna_test_rlgl_texture` built; the focused SDL3-offscreen/Mesa-llvmpipe test passed public construction, exact full/partial upload, native whole/subrect readback, top-row-first ordering, rlgl texture-unit binding, declared mip allocation, non-zero mip upload/readback, and invalid-level rejection. An AddressSanitizer rebuild/rerun passed after the test was corrected to reacquire the renderer record that public full-level `SetData` intentionally replaces. The original smoke and repository identity/target/runtime/platform/Doxygen/whitespace gates passed; CI compiles the focused target. |
| RLGL-024 | ✅ | RLGL-023 | Closed the classic Texture2D inventory: `Color`, `Bgr565`, `Bgra5551`, `Bgra4444`, DXT1/3/5, `NormalizedByte2/4`, `Rgba1010102`, `Rg32`, `Rgba64`, `Alpha8`, `Single`, `Vector2/4`, `HalfSingle`, `HalfVector2/4`, and `HdrBlendable`. RLGL-026/027/028 contain the measured format-family evidence. CNAEXT formats 20–26 are tracked outside this classic-XNA gate. |
| RLGL-025 | ✅ | RLGL-023 | Implemented 16 lazily-created per-slot GL 3.3 sampler objects, using the renderer-private rlgl dispatch because rlgl 6.0 has no sampler-object wrapper. Each application is deterministic across all nine filter decompositions, U/V/W wrap, XNA `MaxMipLevel` → minimum LOD, desktop LOD bias, comparison disable, and anisotropy reset/clamp against rlgl's measured extension ceiling; applying filter/address alone resets properties otherwise left stale by an earlier effect. Renderer shutdown deletes the objects before `rlglClose`, one texture may use different samplers on two slots, and `AnisotropicFiltering` now reports the measured implementation. Validated 2026-09-12: `cna_renderer_rlgl`/`cna_test_rlgl_sampler` built; SDL3-offscreen/Mesa-llvmpipe native snapshots passed every ordinal/property, A→B slot independence, shared-texture/different-sampler binding, invalid-slot rejection, and rlgl-batch pixel oracles for PointWrap, PointClamp, PointMirror, point, and linear sampling. An AddressSanitizer rebuild/rerun and the prior smoke/texture regressions passed. The batch is test-only and is not CNA's production scheduler. CI compiles the focused target. |
| RLGL-026 | ✅ | RLGL-023 | Implemented the measured Reach-era uncompressed formats `Bgr565`, `Bgra5551`, `Bgra4444`, `NormalizedByte2`, and `NormalizedByte4`. The first three allocate/update through rlgl; XNA A:R:G:B packed words are reversibly rotated for GL's R:G:B:A 5551/4444 transfers. Because rlgl 6.0 exposes no signed-normalized pixel formats, a private bridge uses GL 3.3 `RG8_SNORM`/`RGBA8_SNORM`. All paths use format-native byte counts and one-byte transfer alignment, including odd two-byte rows; classification advertises only completed layouts and rejects Color-object transfers for signed-normalized storage. Validated 2026-09-12: the focused SDL3-offscreen/Mesa-llvmpipe test passed public construction, full and partial updates, exact native byte readback, packed rotation, odd-row alignment, and supported/unsupported classification for all five formats. The rebuilt AddressSanitizer test, device/sampler regressions, renderer build, and repository identity/target/runtime/platform/Doxygen/workflow/whitespace gates passed. RLGL-027 subsequently added and sampled the two-channel XNA expansion swizzle. |
| RLGL-027 | ✅ | RLGL-026 | Implemented every remaining uncompressed classic format with the exact FNA3D/EasyGL storage model: `RGB10_A2`, `RG16`, `RGBA16`, alpha-only `R8`, `R32F`/`RG32F`/`RGBA32F`, and `R16F`/`RG16F`/`RGBA16F` (also `HdrBlendable`). Exact one-/four-channel float and half-float layouts use rlgl wrappers; two-channel, high-bit UNORM, and alpha-only layouts use the private GL 3.3 dispatch because rlgl has no exact public formats. Per-texture swizzles implement XNA's `(R,1,1,1)`, `(R,G,1,1)`, and `(0,0,0,A)` sampling rules without shader-specific patches. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: public typed full/partial transfers round-tripped exact native bytes for all eleven formats, including signed finite floats, exact half words, odd Alpha8 rows, `HdrBlendable`, and a non-zero `Vector2` mip. rlgl-batch test-only pixels passed Alpha8, Single, Vector2, and NormalizedByte2 channel expansion. An AddressSanitizer rebuild/rerun, device/sampler regressions, renderer build, and repository gates passed. No GL 3.3 storage limitation was found; renderability is separately gated by RLGL-014. |
| RLGL-028 | ✅ | RLGL-026 | Implemented DXT1/DXT3/DXT5 with two truthful storage paths. When rlgl's extension probe maps the requested DXT format, `rlLoadTexture` creates level zero and the private dispatch performs compressed higher-mip allocation, block uploads, and `glGetCompressedTexImage` readback. Without S3TC, the renderer uses the shared CNA DXT decoder and rlgl-owned RGBA8 textures while retaining source blocks per mip solely to preserve XNA compressed `GetData` semantics; GPU pixel sampling proves uploads are not shadow-only. Public transfer validation enforces exact block byte counts and block-aligned rectangles. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe in both native and forced-fallback (`CNA_RLGL_FORCE_DXT_FALLBACK=1`) modes: DXT1/3/5 exact full reads, a changing block partial update with untouched-neighbor preservation, misalignment/short-buffer rejection, nonzero mip transfers, and red/blue sampled pixels all passed. Both modes also passed rebuilt AddressSanitizer runs; device/sampler regressions and renderer identity/target/runtime/platform/Doxygen/workflow/whitespace gates passed. The loader contract keeps DXT blocks intact until this renderer boundary, matching EasyGL's capability-dependent decode strategy; no compression is fabricated. |
| RLGL-029 | ✅ | RLGL-009, RLGL-013 | Implemented the production CNA-owned SpriteBatch scheduler with rlgl's low-level shader/program, VAO, dynamic VBO/IBO, matrix/uniform, texture, and indexed-draw wrappers. CNA's shared layer retains XNA sorting/overloads; RLGL groups by texture, forwards the complete sampler, flushes Immediate draws synchronously, preserves float destinations, rotation/source-space origin/flips/tint, maps custom/default backbuffer viewports, and splits before 16-bit indices overflow. rlgl's global immediate batch is only drained before bindings and is never the production scheduler. Non-null custom Effects throw the stable RLGL-012 diagnostic rather than silently using the stock shader. Validated 2026-09-12: `cna_test_rlgl_spritebatch` passed the custom-effect diagnostic plus 27 public-API pixel checks for texture row/source selection, both flip axes, all five sort modes, multi-texture groups, a 16,384-sprite capacity split, Immediate pipeline mutation, transform, rotation/origin, Wrap, viewport, AlphaBlend, fractional coordinates, scissor, SpriteFont newline, and mirrored glyph order on SDL3-offscreen/Mesa-llvmpipe. AddressSanitizer passed. |

| RLGL-030 | ✅ | RLGL-006 | Implemented declaration-aware, fixed-capacity vertex and 16/32-bit index resources. rlgl owns VBO/EBO allocation, update, and destruction; CNA's private bridge supplies only a reusable upload VAO, storage orphaning absent from rlgl 6.0, and native snapshots. Vertex storage is allocated once its declaration/stride is known; index storage is immediate. None/NoOverwrite update prefixes without shrinking, while Discard orphans the same name and resets recovery bytes. Validated 2026-09-12: `cna_test_rlgl_buffer` passed nine public/native gates covering built-in packed bytes, all twelve classic declaration formats, both index widths (including values above 65535), fixed size/usage, tail preservation, update counters, stable names, empty no-ops, and capacity/stride/width/option rejection on SDL3-offscreen/Mesa-llvmpipe. AddressSanitizer passed. |
| RLGL-031 | ✅ | RLGL-030, RLGL-013 | Implemented non-indexed and indexed point, line-list/strip, and triangle-list/strip submission over rlgl-owned VAO/VBO/EBO/program/uniform/attribute wrappers. rlgl's public draw wrappers are used where exact (triangle arrays and zero-offset 16-bit indexed triangles); its hardcoded topology/index-width/base-offset gaps use the already-loaded private GL 3.3 dispatch. Submission validates capacities, maps Position0/Color0 by semantics rather than field order, applies `world * view * projection`, honors `vertexStart`, `startIndex`, and `baseVertex`, and explicitly refuses lit/advanced/custom or instanced/multi-stream effects until their owning tasks. Validated 2026-09-12: `cna_test_rlgl_primitive` passed 24 gates on SDL3-offscreen/Mesa-llvmpipe: pixels and exact modes/counts for every topology in indexed and non-indexed form; 16/32-bit dispatch; public user and bound-buffer routes; reordered declarations; all twelve native vertex formats; WVP translation; state rebinding after SpriteBatch; malformed attribute rejection; and declaration/effect mismatch refusal. The rebuilt AddressSanitizer target passed the same suite. |
| RLGL-032 | ⬜ | RLGL-031, RLGL-012 | Implement classic multi-stream vertex input: per-stream buffers/strides/offsets, cross-stream semantic mapping, folded vertex/base offsets, and deterministic attribute rebinding. Validate two-stream indexed/non-indexed draws and semantic collisions. Instanced frequencies remain RLGL-016. |
| RLGL-033 | ✅ | RLGL-031, RLGL-009, RLGL-025 | Generalized the baseline shader into one declaration-driven unlit stock pipeline with optional Color0/TextureCoordinate0, rlgl Texture2D/default-white binding, diffuse/emissive/alpha composition, FNA's four-value alpha-test encoding, view-space-Z fog, and the same 63/64-pixel clip correction EasyGL applies to non-MSAA stock draws. At this milestone lit/dual/cube/skinned/custom shapes still failed before submission; RLGL-034 subsequently lifted the BasicEffect-lighting restriction. Validated 2026-09-12: `cna_test_rlgl_effect` passed 15 gates covering texture row orientation, material and vertex tint, null-texture fallback, textured→untextured reset, all eight `CompareFunction` variants at equality, AlphaTest diffuse/color/fog composition, BasicEffect fog, and explicit declaration/effect-shape rejection. CNA's existing renderer-neutral `xna_pixel_center_contract_test.cpp` passed its measured 1×1 XNA triangle and control triangle; `cna_test_rlgl_primitive` remained 24/24. Effect, pixel-center, and primitive targets passed rebuilt AddressSanitizer runs. |
| RLGL-034 | ✅ | RLGL-033 | Generalized the rlgl-owned stock pipeline with semantic Normal0, full world and inverse-transpose normal transforms, ambient/emissive, three independently gated directional diffuse/specular lights, material specular power, eye position, texture/vertex-color composition, alpha and fog. A stage selector preserves XNA's default Gouraud path versus fresh per-fragment lighting, including D3D9's pre-interpolation color-output saturation. Validated 2026-09-12 against nine unchanged EasyGL/shared fixtures plus `rlgl_basic_effect_lighting_test`: 29 lighting-specific gates cover one/back-facing/disabled lights, all three light slots, emissive, specular/eye movement, per-vertex vs per-pixel output, both 36-byte vertex-color paths, saturation ordering, non-identity camera, 1000x world-space precision, non-uniform normal matrices, lit alpha, and lit fog. All ten lighting executables plus the 15-gate RLGL effect regression passed normal and AddressSanitizer builds under llvmpipe. |
| RLGL-035 | ✅ | RLGL-033 | Extended the rlgl-owned stock program with semantic TextureCoordinate1 at a stable separate attribute, a second sampler on texture unit one, XNA's `Texture * 2 * Texture2` RGB combine, and explicit unit-zero restoration. CNA's existing effect parameters supply both nullable textures, diffuse/alpha, optional Color0, and view-space-Z fog; a declaration without Vector2 TextureCoordinate1 fails explicitly rather than aliasing UV0. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: the renderer-local fixture passed ten gates for independent UVs, independent Wrap/Clamp samplers, null texture0/texture1 white fallbacks, vertex color/diffuse/premultiplied alpha, vertex-color disable, fog, missing-semantic rejection, and DualTextureEffect→BasicEffect state reset. The unchanged EasyGL independent-UV fixture also passed. Both executables passed rebuilt AddressSanitizer runs. |
| RLGL-036 | ⬜ | RLGL-015, RLGL-034 | Implement EnvironmentMapEffect over TextureCube with normal/world/eye transforms, three-light diffuse, emissive/ambient, specular/fresnel variants, amount, base texture, and fog. Validate cube-face and lighting output. |
| RLGL-037 | ✅ | RLGL-034 | Generalized the stock vertex shader with semantic BlendWeight0 plus Byte4/Vector4 BlendIndices0, exact 1/2/4 weighted matrix sums, post-skin position/fog, inverse-transpose joint normals followed by the world normal matrix, and deterministic skinning enable transitions. Canonical declaration-less 52/56/64-byte layouts remain supported without replacing semantic validation for declared buffers. The 72 affine matrices are packed as three vec4 rows each: 864 components, and 957 for the complete vertex shader, below GL 3.3's guaranteed 1024-component minimum; initialization verifies that live limit and uses rlgl's uniform-array setter without a raw GL upload. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: one renderer-local fixture, the shared 10-cell parity fixture, and twelve unchanged EasyGL fixtures produced 66 passing gates across identity/translation/50-50/four-weight skinning, weights-per-vertex exclusion, palette slot 71, both index formats, null/real textures, material/alpha, three lights/specular, vertex color, vertex/per-pixel selection, post-skin fog, non-uniform joint/world normals, malformed-declaration rejection, and SkinnedEffect→BasicEffect reset. All fourteen executables passed rebuilt AddressSanitizer runs. Animated Model integration remains a representative-workload gate in RLGL-018 rather than a primitive/effect correctness gap. |
| RLGL-038 | ⬜ | RLGL-033 | Reuse CNA's existing `ShaderEffect` and compiled XNA Effect representations: GLSL 330 adaptation, semantic/reflection mapping, parameter arrays/matrices, sampler/state assignments, techniques/passes, and diagnostics. Do not add an RLGL-specific public shader API. Validate existing portable custom and compiled-effect fixtures. |
| RLGL-039 | ✅ | RLGL-009, RLGL-013 | Implemented single-sample `SurfaceFormat::Color` RenderTarget2D through rlgl's texture/FBO create, attach, completeness, bind, mip-generation and unload wrappers. A narrow private dispatch allocates exact Depth16/Depth24/Depth24Stencil8 renderbuffers and performs level/subrectangle RGBA8 readback because rlgl 6.0 has no exact-depth or framebuffer-read wrapper. Creation/readback/destruction preserve unrelated framebuffer/texture bindings; target switches finalize mip chains, restore the default framebuffer, map viewport/scissor against target height, and rescale XNA normalized depth bias to the active attachment. CPU uploads and readback are normalized to top-row-first; SpriteBatch CPU UVs and a per-slot stock shader uniform correct rasterized bottom-up sources without altering ordinary textures. The shared GraphicsDevice path supplies deterministic DiscardContents clears while preserved/platform content naturally survives. At this milestone non-zero samples failed explicitly under RLGL-041 (subsequently completed); non-Color formats remain RLGL-042, and the profile ceiling stays one until RLGL-040. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: `cna_test_rlgl_render_target` passed 13 focused gates for capability truth, native identities, every depth format, then-explicit MSAA refusal, asymmetric subrect readback, BasicEffect target sampling, CPU upload, real depth/stencil pixels, depth-bias scaling, non-zero mip readback, and usage transitions; the unchanged EasyGL quadrant fixture passed both orientation gates. Effect/DualTexture/Skinned/SpriteBatch regressions remained green. Both target executables passed rebuilt AddressSanitizer runs, the complete RLGL configuration built successfully, and renderer identity/descriptor/target/runtime-discipline gates passed (42 descriptors compiled; four platform-SDK families were structure-checked). |
| RLGL-040 | ⬜ | RLGL-039 | Implement 2–4 simultaneous RenderTarget2D attachments with a reusable rlgl FBO, `rlActiveDrawBuffers`, slot-aligned validation/finalization, slot-zero depth ownership, indexed color masks, and a multi-output stock/custom shader validation path. |
| RLGL-041 | ✅ | RLGL-039 | Implemented live-limit sample clamping with actual applied-count reporting, separate multisampled RGBA8 plus exact Depth16/Depth24/Depth24Stencil8 renderbuffers, a sampleable single-sample Color texture, and one resolve FBO per target. rlgl owns framebuffer/texture creation, attachments, completeness, blits and unloads; a narrow bridge allocates renderbuffers because rlgl 6.0 exposes no renderbuffer-storage wrapper. Resolve preserves read/draw FBO bindings, runs for active readback and before mip generation on unbind, and leaves later rendering valid. Validated 2026-09-12 on SDL3-offscreen/Mesa-llvmpipe: the focused fixture passed 18 gates including device clamping, separate native identities, first/repeated active reads, real mixed edge coverage, multisampled depth/stencil, resolve→mipmap, and Preserve/Discard transitions; the unchanged OpenGL4 full-target fixture passed 12 gates. Shared isolated regressions passed all 34/34 first-read legs (145 checks) and 63/63 MSAA/mip legs (832 checks), with only the separately-owned MRT/cube legs declared as boundaries. All four executables passed rebuilt AddressSanitizer runs, and the complete RLGL configuration built successfully with `-j 3`. |
| RLGL-042 | ⬜ | RLGL-039 | Audit and implement every classic-XNA render-target `SurfaceFormat` that GL 3.3 can attach faithfully, including exact format-native readback and truthful capability probing/refusal. Compressed and signed-normalized formats must not be advertised merely because they are sampleable. |

Tasks discovered while implementing will be appended with stable identifiers; existing identifiers will not be renumbered.

## 9. Validation strategy

Validation proceeds in increasing cost:

1. Renderer identity and descriptor consistency scripts.
2. RLGL configuration and compile-only build.
3. Unit tests that do not require a graphics context.
4. Runtime smoke under a real or virtual GL 3.3 context.
5. Focused resource/state/effect tests with readback.
6. Existing shared parity corpus run under both EasyGL and RLGL.
7. Existing representative samples, preferring repository workloads over parallel applications.
8. Golden-image or pixel comparisons for output-sensitive behavior.
9. Cross-renderer regression builds so RLGL changes do not break established renderers.

Every completed implementation task records the exact commands and result in its task entry or a dated evidence note. Unsupported paths must return truthful capability data or explicit errors; a no-op success is never acceptable.

## 10. CI, documentation, and performance gates

CI should always configure and compile the RLGL backend. Runtime coverage is added only on workers with a proven GL 3.3 context, using the same headless/offscreen mechanism already accepted by CNA where possible. The default and at least one established GL renderer must continue to build after shared changes.

Documentation must explain `-DCNA_GRAPHICS_RENDERER=RLGL`, the exact pinned source and offline override, OpenGL 3.3/core requirements, supported platform/context providers, current parity backed by this matrix, debugging paths, license notices, and the dependency update procedure.

Performance work begins only after the representative correctness gate. Measurements must identify redundant state transitions, binds, uploads, framebuffer changes, flushes, conversions, allocations, or lookup overhead with a reproducible workload. No optimization may weaken XNA semantics.

## 11. Final parity gate

“RLGL ↔ EasyGL classic XNA parity reached” may be stated only when RLGL-022 completes with:

- every classic-XNA matrix row either validated as supported or documented as an unavoidable rlgl/GL-profile limitation with an explicit observable behavior;
- the EasyGL and RLGL contract inventories reconciled;
- representative workloads and shared parity tests passing on supported platforms;
- no hidden stub, fallback, silent no-op, stale-state, resource-lifetime, or shader/effect gap;
- CI/build integration and user/update/debug/license documentation verified;
- CNAEXT gaps separately listed and not confused with the classic-XNA result.

Until then, the authoritative status is the summary, limitations, matrix, and task ledger above.
