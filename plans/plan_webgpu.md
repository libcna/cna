# WebGPU Backend Implementation Plan

## Status summary (2026-08-28)

**144 rows — ✅ 142 · 🟨 1 · ⬜ 1** (counted from the row tables by `tools/count_webgpu_plan_status.sh`,
not by hand). The **2 open rows** are the only WebGPU work not at ✅, and both are blocked on resources
outside this development environment rather than on renderer work: `WEBGPU-1` needs the
Windows/macOS/aarch64 packages built/linked/run on a real CI or those platforms (their hashes are
already pinned here), and `WEBGPU-107` needs a real device-loss scenario to drive and verify a
destroy+recreate path — a large GL-flavoured feature the Vulkan renderer also leaves unimplemented.
Every WebGPU task that could be completed and verified in this environment is done. (Closed 2026-08-28: `WEBGPU-12`/`WEBGPU-59` transient per-draw buffer pool (recycle,
don't churn) + 3-slot SpriteBatch vertex ring + `WebGPU_BufferPoolStress` (create count plateaus,
reuse climbs, zero GPU errors); `WEBGPU-29` shared `Build3DPipelineEXT()` descriptor
builder for all 12 3D families (behaviour-preserving, −318 lines, suite still 104/104); `WEBGPU-28` all WGSL extracted to
`webgpu_shaders.hpp` + `ValidateAllShadersEXT()` whole-set validation + `WebGPU_ShaderValidation`
test + `CNA_WEBGPU_VALIDATE_SHADERS` startup gate. Closed 2026-08-27: `WEBGPU-114` RenderTargetCube per-face MSAA
resolve + `mipMap=true` mip-chain regeneration + `WebGPU_RenderTargetCube` Checks E/F; `WEBGPU-39`
exact per-value `DepthFormat`
mapping (None/Depth16/Depth24/Depth24Stencil8) + `WebGPU_DepthFormat` test; `WEBGPU-70` reachable
vertexStart/baseVertex/startIndex directly tested; `WEBGPU-108` present-mode unit-tested seam;
`WEBGPU-149` FNA fog `FogColor*alpha` fix + strengthened tests; `WEBGPU-150` headless test-skip
launcher; `WEBGPU-151` versioned/hashed download-cache migration. `WEBGPU-1` is back to 🟨 — package
integrity + Linux runtime are done, but the Windows/macOS/aarch64 packages have pinned hashes only, not
build/link/runtime verification.)

### Current limitations (what is genuinely still open)
- **`WEBGPU-1` 🟨** — package integrity (SHA-256) and Linux x86_64 runtime are verified; the
  Windows/macOS/linux-aarch64 packages have pinned hashes but no build/link/runtime verification here.
- **`WEBGPU-107` ⬜** — `DebugSimulateContextLoss` is an inherited no-op (a real device
  destroy+recreate plus re-init of every live GPU resource is a large GL-flavoured feature the Vulkan
  renderer also does not implement).

Everything else in the row tables is ✅. Stock-effect **fog reached full parity** on 2026-08-27
(`WEBGPU-145`–`148`: BasicEffect/AlphaTest/DualTexture/Skinned, matching the pre-existing
EnvironmentMapEffect fog), and `WEBGPU-149` corrected the FNA blend to `FogColor * outputAlpha` with
alpha<1 pixel tests -- so the parity claim holds for translucent draws too, not just opaque. See
Phase 64.1.

---

> WebGPU is an authorized, experimental workstream. Its first goal, a **verified native 2D backend
> on Linux desktop**, was reached 2026-07-12 (`WEBGPU-124`–`WEBGPU-131`, all ✅ — see Phase 56.1).
> Since then (through 2026-08-27) this backend has grown real 3D coverage close to the other
> established backends: `colored3d`/`textured3d`/`colored_textured3d`/`lit_textured3d`/
> `alpha_test3d`/`dual_texture3d` (strides 16/20/24/32), `PbrEffect`/`SkinnedPbrEffect`/
> `SkinnedEffect` (unskinned + skinned + `VertexColorEnabled`, both `PreferPerPixelLighting`
> variants), `EnvironmentMapEffect` (cube-map reflections, flat and Fresnel-weighted), stock-effect
> fog at FNA parity (`WEBGPU-145`–`149`), and real
> instancing (`DrawInstancedPrimitivesEx`, per-instance vertex stream) all work end-to-end through
> real `GpuDrawParams` dispatch. Render state (blend/rasterizer/cull/scissor/viewport/per-slot
> sampler/depth-stencil incl. full stencil ops; **`FillMode::WireFrame` is deliberately refused, not
> implemented — `WEBGPU-115`**), `RenderTarget2D` and `RenderTargetCube` (real render-into,
> eager-flush-on-target-switch), MSAA (backbuffer + `RenderTarget2D` + per-face `RenderTargetCube`,
> device-probed clamping — this machine's adapter supports exactly 4x; `WEBGPU-114`),
> `Texture3D`, and real linear-filtered
> mip generation (`Texture2D`, `TextureCube`, and `RenderTargetCube` render targets — `WEBGPU-114`)
> are all implemented and tested. This is NOT full
> parity with Vulkan yet — see "Remaining work" immediately below for exactly what's still open.
>
> **Status legend:** ✅ implemented *and verified against its stated acceptance criteria*;
> 🟨 code or documentation exists but has not met those criteria; ⬜ not implemented.
>
> **Remaining work (as of 2026-08-27 — but the authoritative live list is the "Status summary" +
> "Current limitations" at the very top of this file; update those whenever a row's status changes):**
>
> **Note on IDs:** this file's `WEBGPU-*` numbering has gaps by design — IDs **62** and **100–105**
> have never existed here (an artefact of the two renumberings recorded under History below). A
> missing ID is not a lost task.
> - **Compressed textures (`WEBGPU-111`/`WEBGPU-144`) — ✅ done 2026-08-27.** WebGPU is the first CNA
>   renderer to upload BC blocks to the GPU natively (DXT1/3/5 + BC7 → `WGPUTextureFormat_BC*`, no CPU
>   decompress), both via the direct `Texture2D`+`SetData` API (Phase 1) and via the XNB/DDS content
>   loaders (Phase 2, gated on the WebGPU-only `LoadsCompressedContentNativelyEXT()`). The old premise
>   below is stale: `ImageData` already carries `surfaceFormat`/`mipLevels` and `Texture2D` already has
>   the compressed block-transfer contract. See the `WEBGPU-144` row and `docs/webgpu-renderer.md`.
> - **MRT — multiple simultaneous render targets (`WEBGPU-85`/`86`/`87`)** — ✅ done 2026-08-27
>   (full WebGPU suite 88/88 on the real GPU). `SetRenderTargets` accepts 2..4 `RenderTarget2D`
>   targets, `ReplayOrderedSegments` builds an N-attachment pass, a custom `ShaderEffect` writing
>   `@location(0..N-1)` fans out to every slot (`WebGPU_MRT` proves slot N holds slot N's content),
>   and a stock draw writes attachment 0 only (the cross-renderer behaviour every other backend has).
>   `SupportsCapability(MultipleRenderTargets)` is now true (the `WEBGPU-134` false arm removed). A
>   Phase-0 gate (2026-08-27) established that MRT needs a multi-`@location` custom WGSL shader, so
>   **`WEBGPU-76` was done first** (see below) as the enabler.
> - **Buffer/device-API gaps — now closed (2026-08-26):** `WEBGPU-44` (`SetDataWithOptions`),
>   `WEBGPU-47` (disposed-buffer guard) and `WEBGPU-99` (their tests) are ✅ -- see `WebGPU_SetDataOptions`
>   and `WebGPU_DisposedGuard`. **`OcclusionQuery` (`WEBGPU-84`) is now implemented** (2026-08-26):
>   `SupportsCapability(OcclusionQuery)` reports true, `CreateOcclusionQuery()` returns a real query
>   backed by a `WGPUQuerySet`, and the sample count is exact — `WebGPU_OcclusionQuery` proves a fully
>   occluded quad reads 0 and a visible one a full target of samples (superseding `WEBGPU-135`'s
>   temporary false arm). **`vertexStart`/`baseVertex` sub-ranging (`WEBGPU-70`)** is 🟨, not ⬜:
>   the ordinary indexed/non-indexed routes honour `startIndex`/`baseVertex` already; the only
>   unapplied case is `vertexStart` on the instanced route, which is **unreachable through any XNA
>   API** (XNA's `DrawInstancedPrimitives` has no `vertexStart` parameter -- see its row), so it stays
>   documented-not-done rather than force-fixed.
> - **`ShaderEffect` custom WGSL (`WEBGPU-76`)** — ✅ done 2026-08-27, on the 3D `DrawPrimitivesEx`
>   route. `WebGPUEffectRenderer` compiles user WGSL (`vs_main`/`fs_main`) with a
>   `DeclareUniformBlockEXT` uniform-block path; `ExecutesShaderEffectSourceEXT()`→true,
>   `GetShaderDialectEXT()`→`Wgsl`. Verified by `WebGPU_ShaderEffect3D` on the real GPU (full suite
>   87/87). Custom **SpriteBatch** effects shipped too (`WEBGPU-142`, `SpriteBatch.Begin(..., effect)`
>   runs the WGSL per sprite; suite 89/89).
> - **Misc small items — mostly closed (2026-08-26):** `WEBGPU-106` (debug-marker no-op, inherited +
>   documented), `WEBGPU-109` (`IsFullScreen`, platform-level), `WEBGPU-116` (vertex-format helper,
>   now single-sourcing all pipeline formats), `WEBGPU-118` (the `docs/webgpu-vs-vulkan-deviations.md`
>   doc), and `WEBGPU-63` (SpriteBatch sort-mode verification) are all ✅. `WEBGPU-28` (WGSL
>   extraction to `webgpu_shaders.hpp` + `ValidateAllShadersEXT()` whole-set validation + the
>   `CNA_WEBGPU_VALIDATE_SHADERS` startup gate) closed 2026-08-28. Still `⬜`: `WEBGPU-107`
>   (`DebugSimulateContextLoss`, an inherited no-op; a real device destroy+recreate is a large,
>   GL-flavoured feature Vulkan also does not implement). (`WEBGPU-115` — the wireframe refusal + its
>   doc + its contract test — is ✅, see its row; it is no longer open.)
> - **Emscripten/browser target (`WEBGPU-119`–`122`)** — the 2D path RUNS IN A REAL BROWSER as of
>   2026-08-26. `WEBGPU-119`/`120`/`121`/`122`/`133` are ✅ -- 2D, 3D BasicEffect and every stock
>   effect shader compile and render in a real browser, and the effect suites' own readback pixel
>   checks pass there too now that the Dawn surface-texture lifetime bug is fixed. The renderer builds for the browser as the *same* `WEBGPU` identity (not a new
>   renderer), sharing all of `WebGPURenderer.cpp` and every WGSL shader. Five `#if defined(__EMSCRIPTEN__)`
>   seams carry the only real differences: a `WGPUEmscriptenSurfaceSourceCanvasHTMLSelector` surface
>   (`"#canvas"`); `WGPUCallbackMode_AllowSpontaneous` + `emscripten_sleep` (Asyncify) instead of the
>   native `wgpuInstanceProcessEvents` pump (and the device-lost callback needs that same mode, which
>   emdawnwebgpu validates but wgpu-native did not); no explicit `wgpuSurfacePresent` (the browser
>   presents the canvas on the `requestAnimationFrame` yield `Game::RunLoop` already does); a
>   depth/MSAA resync to the acquired texture's real size, because the browser sizes the surface from
>   the `<canvas>` backing store, not from `wgpuSurfaceConfigure`; and an `if(EMSCRIPTEN)` branch in
>   `ThirdPartyWebGPU.cmake` that links Emscripten's **emdawnwebgpu** port (NOT the removed
>   `-sUSE_WEBGPU=1`) with no wgpu-native download. One shared guard was relaxed:
>   `PlatformRendererSurfaceState` now accepts the `Web` (canvas) surface, which exposes no native
>   window pointer. `cna_demo_2d` runs 120 SpriteBatch frames in headless Chrome (real AMD Vulkan
>   WebGPU) with audio, no WebGPU validation error, clean teardown, and a mid-run canvas resize —
>   verified by `scripts/run-webgpu-browser-test.sh` (`CNA_WEBGPU_DEMO=cna_house3d_demo` drives the 3D
>   `BasicEffect` path through the same harness -- perspective, depth test and texturing all render
>   in-browser with no shader/validation error; `CNA_WEBGPU_DEMO=cna_webgpu_{pbr3d,envmap3d,skinned3d}_page`
>   likewise confirm the `PbrEffect`, cube-map `EnvironmentMapEffect` and bone-palette `SkinnedEffect`
>   WGSL all compile and render their early checks in-browser; `cna_webgpu_{dualtexture3d,alphatest3d}_page`
>   close the last two pairs the same way). `WEBGPU-121` is now ✅. `WEBGPU-133` -- Dawn rejecting the
>   released-then-referenced backbuffer surface texture under the effect pages' multiple-readback-
>   per-frame pattern -- is also FIXED with a lazy discard: a readback marks the acquired surface
>   texture stale, and `EnsureFrameRendered()` re-acquires a fresh one only when it is about to
>   render again, while a same-frame re-READ still reuses the captured buffer. All five effect pages'
>   readback checks pass in-browser (pbr 5/5, envmap 4/4, skinned 9/9, dualtexture 4/4, alphatest
>   4/4), with the 2D/3D smokes unregressed. (The earlier skinned D2 failure was this same
>   multi-read artifact, not a skinning bug -- per-render probing confirmed the WeightsPerVertex=2
>   quad shifts right correctly.) The one remaining web-specific refusal is the
>   `--webgpu-2d-validation` scene's frame-45 `MinimizeEXT()` (a native-only GameWindow op, not a
>   renderer concern).
> - **Cross-backend pixel-parity test (`WEBGPU-123`)** — ✅ 2026-08-26, verified across four backends
>   via the shared cross-renderer diagnostic harness (`cna_diag_{webgpu,software,easygl,vulkan}`).
>   **VULKAN (native, real AMD RADV GPU, run on `:0` with the owner's go-ahead) vs WEBGPU (browser) is
>   byte-identical (max diff 0)** -- both are AMD Vulkan underneath (Chrome's WebGPU is Dawn-on-Vulkan).
>   WEBGPU and VULKAN each match the SOFTWARE CPU reference at max 1, mean 0.139, 0 outliers. EASYGL
>   (`OPENGL33`, headless Xvfb+llvmpipe) is the only divergence: 57 triangle-EDGE coverage pixels
>   differing from all three (a GL fill-rule difference, not a WebGPU defect). Harness:
>   `scripts/run-webgpu-parity-test.sh`. Only the Bgfx leg is un-run; the conclusion is settled.
> - **`WebGPU_Clear_Readback` (`WEBGPU-141`) — RESOLVED 2026-08-27, now 10/10 (full suite 86/86).**
>   Root cause was bisected to TWO WebGPU-local regressions (not adapter sensitivity, the earlier
>   guess): **Wrap/Mirror** was a genuine renderer regression (`e2e8dee99`, REMED-GFX-072) whose
>   `customViewport` path misfired for the letterboxed backbuffer — fixed by requiring the viewport to
>   be a contained sub-region; **Alpha** was NOT a renderer bug (`f14c764d8`/REMED-GFX-102 made the
>   sprite honour the device BlendState, which is FNA-correct — a straight texture under premultiplied
>   AlphaBlend gives full green, as EasyGL/SOFTWARE/FNA all do) — Check G was corrected to use
>   `NonPremultiplied`. See the WEBGPU-141 row for the full bisect.
> - Everything else in the row table below not listed here is ✅ or an honestly-scoped 🟨 (read
>   that row's own note for the specific remaining gap — e.g. `WEBGPU-52`/`56`/`113`/`114` mip/cube
>   work is done but MSAA-on-cube-faces and a few other narrow edges stay documented-not-done).

> **History:** this content originally lived inline in `plan_graphics.md` as Phases 56–69,
> numbered `501`–`661`, then renumbered to `10001`+ (2026-07-02) to free up the low task-ID
> range for further core-plan growth. Moved to this dedicated file and renumbered again to
> `WEBGPU-1`–`WEBGPU-123` (2026-07-07) to fully separate the parked WebGPU work from the active
> `plan_graphics.md` backlog — no task content changed in either move, only the numbering and
> file location.
>
> **Verified native 2D baseline (2026-07-12, `WEBGPU-124`–`WEBGPU-131`):** a clean Linux x86_64
> CMake configuration with an explicit `CNA_WEBGPU_ROOT` compiles `WebGPURenderer.cpp`
> against the pinned `wgpu-native v29.0.1.1` headers, produces `libcna_backend_graphics_webgpu.a`,
> and copies the native runtime. `cna_demo_2d --smoke 120`/`--webgpu-2d-validation` and a second,
> independent application (`../mobile-eggbert`) both initialize, clear, upload/sample textures,
> draw animated multi-sprite `SpriteBatch` scenes with tint/alpha/rotation/flip/sampler variants,
> resize, and present cleanly on a host desktop session with no WebGPU validation error, device
> loss or loader failure. Automated (non-manual-screenshot) tests and GPU readback remain open —
> see `WEBGPU-88`–`99` below.
>
> **Platform scope (updated 2026-08-26):** verified on Linux x86_64 desktop (X11 and Wayland),
> using an explicit extracted `wgpu-native` package, **and in a real browser** via Emscripten's
> emdawnwebgpu port — `WEBGPU-119`–`122`/`133` are ✅ (2026-08-26): 2D, 3D `BasicEffect` and every
> stock effect compile and render in headless Chrome, effect readback checks included. The browser
> path is no longer a future workstream. Windows and macOS remain **code paths only, not validation
> claims**. Android is still blocked by the absence of an Android package/build route.

## Active execution order — do this one task at a time

**Current open tasks (2026-08-28)** — only these **2** rows are not ✅
(`WEBGPU-1, 107`); do one at a time, each its own commit, never mark ✅ from
source inspection.

1. **`WEBGPU-1`** — build/link/run the Windows/macOS/aarch64 packages (whose hashes are now pinned) on
   an appropriate CI/platform, so package integrity becomes a full non-Linux verification.
2. **`WEBGPU-107`** — real device/context loss recovery + resource re-init, or keep it open with the
   exact lifetime contract (a no-op must not be marked as an implementation).

(Two open rows: `WEBGPU-1, 107`. Matches the "Status summary" and "Current limitations" at the top.)

The dated chronology of completed 2D/3D work below is **archival** — read the "Status summary" and
"Current limitations" at the top of this file for the live state, not this history.

### Archive — completed-work chronology (historical, 2026-07-12 → 2026-08)

1. ~~`WEBGPU-126`~~ – ~~`WEBGPU-131`~~ — 2D baseline established 2026-07-12, all ✅ (Phase 56.1).
2. ~~`WEBGPU-88`~~ – ~~`WEBGPU-91`~~ — automated CTest coverage and reusable GPU readback, all ✅
   2026-07-12 (`WebGPU_Clear_Readback`).
3. ~~`WEBGPU-92`~~ — ✅ 2026-07-12, sampler address-mode (Wrap/Clamp/Mirror) pixel assertions closed
   the last gap. ~~`WEBGPU-99`~~ — buffer disposal/`SetDataOptions`/vertex-format tests, ✅ 2026-08-26
   (`WebGPU_DisposedGuard`, `WebGPU_SetDataOptions`, and the `WebGPUVertexFormatFromVEF` rewire
   verified by the full 3D/sprite suite).
4. 3D backlog (Phases 57–66) is now underway, all landed 2026-07-12:
   - First vertical slice — `WEBGPU-11`/`13`/`14` (UBO + bind group), `WEBGPU-19`
     (`colored3d.wgsl`), `WEBGPU-32` (pipeline, with genuine depth-test verification),
     `WEBGPU-64`/`65`/`67`/`69` (`DrawColoredPrimitives`/`DrawIndexedColoredPrimitives`, real draw
     dispatch) — `WebGPU_Colored3D` CTest, 4/4, git-stash-verified. Found+fixed a real, separate
     gap along the way: `ApplyDepthStencilState()` was entirely unimplemented (`GraphicsDevice.
     DepthStencilState` had zero effect on this backend), now handles the depth portion
     (`WEBGPU-39` partial).
   - `WEBGPU-66` (`DrawPrimitivesEx`/`DrawIndexedPrimitivesEx`, real `GpuDrawParams` dispatch) —
     stride-16 case only, reusing the same `colored3d.wgsl`/pipeline: a `BasicEffect`'s real
     `DiffuseColor`/`VertexColorEnabled` now reach the shader instead of the
     `DrawColoredPrimitives` fallback's hardcoded white/true. `WebGPU_DrawPrimitivesEx` CTest, 3/3,
     git-stash-verified.
   - `WEBGPU-15`/`17`/`20`/`33` (`textured3d.wgsl`, stride 20, second bind group for sampler+
     texture) and the matching `WEBGPU-66` dispatch extension — a real `Texture2D` bound through
     `BasicEffect.Texture` now samples correctly and `DiffuseColor` genuinely multiplies it (not
     just passed through unmultiplied). `WebGPU_Textured3D` CTest, 3/3, git-stash-verified.
   - `WEBGPU-21` (`colored_textured3d.wgsl`, stride 24) — combines the vertex-colour mixing from
     `colored3d.wgsl` with `textured3d.wgsl`'s sampling in one shader, reusing its exact bind
     groups. `WebGPU_ColoredTextured3D` CTest, 3/3, git-stash-verified.
   - `WEBGPU-22`/`33` (`lit_textured3d.wgsl`, stride 32, VertexPositionNormalTexture) — the first
     WebGPU 3D shader with real lighting: FNA's `Lighting.fxh` `ComputeLights()` (3 directional
     lights, ambient, Blinn-Phong specular, emissive), ported from
     `VulkanGraphicsBackend`'s `lit_textured3d.{vert,frag}.glsl`. Needed a *second* UBO
     (`litBindGroupLayout_`, group 0 binding 1 — `LitLightParams`, 272 bytes) since the primary
     128-byte uniform block is already full; the normal matrix (`inverse(world3x3)`, no shader-side
     transpose given this codebase's `dump(M)=M^T` GPU-column-major convention — verified two ways:
     independently against FNA's `EffectHelpers.SetLightingMatrices`/`Lighting.fxh` formula, and by
     confirming it matches `BgfxGraphicsBackend::ComputeNormalMatrix3x3`'s already-shipped output
     byte-for-byte) is computed CPU-side and forwarded pre-baked, since WGSL has no built-in
     `inverse()`. Found+fixed a real shader robustness bug along the way: a disabled/unconfigured
     `DirectionalLight` forwards `Direction=(0,0,0)` (only its Diffuse/Specular get zeroed, matching
     FNA's own `DirectionalLight.cs`), and `normalize()` on a true zero vector is undefined — can
     poison the whole light sum with NaN on real GPU hardware even though that light's own
     contribution is already zero; guarded with a `select()`-based safe-normalize. `WebGPU_LitTextured3D`
     CTest, 4/4, git-stash-verified (reverting the backend changes makes
     `DrawPrimitivesEx` fall through to `DrawColoredPrimitives`, which aborts on the stride-32
     buffer). No fog (same deliberate deferral as the other 3 stride variants).
   - `WEBGPU-23`/`34`/`72` (`alpha_test3d.wgsl`, strides 20/24/32) — `AlphaTestEffect`'s per-pixel
     discard, ported from `VulkanGraphicsBackend`'s `alpha_test3d.{vert,frag}.glsl`/
     `alpha_test_colored3d.vert.glsl`. No lighting, so the primary UBO's ambient/light0 slots are
     repurposed for `{alphaRef, alphaTolerance, passWeight, failWeight}` — same 128-byte shape, so
     `coloredBindGroupLayout_`/`texturedBindGroupLayout_`/`texturedPipelineLayout_` are all reused
     unchanged. One shared shader module for strides 20/32 (position+UV only, the stride-32
     normal is unread), a separate one for stride 24 (adds vertex-colour tint).
     `DrawPrimitivesEx()`/`DrawIndexedPrimitivesEx()` now check alpha test with priority over the
     other stride dispatches, matching Vulkan's own precedence. Found a real WGSL grammar gap:
     unparenthesized `<`/`>` inside `select()` fails to parse (reserved for `vec4<f32>`-style
     generics) — fixed by hoisting comparisons into their own `let`s. `WebGPU_AlphaTest3D` CTest,
     4/4, git-stash-verified.
   - `WEBGPU-24`/`35`/`73` (`dual_texture3d.wgsl`, strides 20/24) — `DualTextureEffect`, ported
     from `VulkanGraphicsBackend`'s `dual_texture3d.{vert,frag}.glsl`/
     `dual_texture_colored3d.vert.glsl` (`tex1.rgb*=2.0; result=tex1*tex2*tint`, both sampled at
     the same UV). The first genuinely new bind-group *shape* since `lit_textured3d`'s second UBO:
     group 1 now needs THREE bindings (one shared sampler + two textures,
     `dualTextureBindGroupLayout_`/`dualTexturePipelineLayout_`, both new) — group 0 still reuses
     `coloredBindGroupLayout_` unchanged (no lighting, no alpha test). `DrawPrimitivesEx()`/
     `DrawIndexedPrimitivesEx()` gained a `needsDualTexture` check, priority below alpha test but
     above env map/skinned/lit-textured, matching Vulkan's own precedence. `WebGPU_DualTexture3D`
     CTest, 4/4, git-stash-verified.
   - plan_cnj.md CNB-58 (Phase 13A) WebGPU counterpart: `pbr3d.wgsl`/`GetOrCreatePipelinePbr3D()`
     — `PbrEffect`'s real glTF 2.0 metallic-roughness BRDF (unskinned, stride 48), ported from
     `EasyGLGraphicsBackend::EnsurePbrProgram()`. `WebGPU_Pbr3D` CTest (5/5), hand-derived
     GGX/Fresnel formula cross-check. See `docs/webgpu-renderer.md`'s "PbrEffect" section.
   - plan_cnj.md Phase 14J: `skinned3d.wgsl` (4 shader-module variants: per-pixel-lit/
     per-vertex-lit × without/with vertex colour, strides 52/56) and `skinned_pbr3d.wgsl` (PBR +
     skinning combo, stride 68) — closes this backend's former "no skinning shader at all" gap,
     ported from `EasyGLGraphicsBackend::EnsureSkinnedProgram()`/`EnsureSkinnedVertexLitProgram()`/
     `EnsurePbrSkinnedProgram()`. `WebGPU_Skinned3D` (9/9) and `WebGPU_SkinnedPbr3D` (5/5) CTests.
     See `docs/webgpu-renderer.md`'s "SkinnedEffect and SkinnedPbrEffect" section.
   - 2026-07-18: `WEBGPU-53`/`54` — real single-target `RenderTarget2D` support (`WebGPURenderTargetBackend`,
     `CreateRenderTarget2D()`/`SetRenderTarget2D()`), this backend's first: before this, both were
     `IGraphicsBackend`'s safe no-op defaults, so a game's off-screen render target either got a
     null backend handle or silently drew to the visible backbuffer. `WebGPU_RenderTarget2D` CTest,
     8/8: Clear-only and real-3D-draw round trips via `GetData()`, a depth+stencil-tested target
     (also closing `WEBGPU-8`/`9`'s previously-unexercised stencil-clear gap), sampling a
     `RenderTarget2D` back through `SpriteBatch`, and the critical "an intervening RT-targeted
     `Clear()` must not leak into the backbuffer's own render pass" architecture proof. Deliberately
     deferred (thrown/honestly-reported, not silently under-delivered): mip-chain regeneration
     (`mipMap=true` throws). MSAA itself is now real and verified end-to-end (`WEBGPU-58`, resolved
     2026-07-18) — a `RenderTarget2D`'s own per-instance `multiSampleCount` constructor parameter is
     still intentionally ignored (it always mirrors the backend's global sample count instead; see
     `WEBGPU-58`'s own row). MRT (`WEBGPU-85`/`86`/`87`) is untouched, a separate follow-up.
   - 2026-07-18: `WEBGPU-25`/`36`/`74` — `env_map3d.wgsl`/`GetOrCreatePipelineEnvMap3D()`, real
     `EnvironmentMapEffect` cube-map reflection dispatch, ported from `VulkanGraphicsBackend`'s
     `env_map3d.{vert,frag}.glsl` (cross-checked against `EasyGLGraphicsBackend::
     EnsureEnvMapped3DProgram()` before porting) — this backend's first cube-map shader and its
     first genuinely new (non-bind-group) *and* new-bind-group-shaped combination (group 1: sampler
     + `texture_2d` + `texture_cube`). Needed a new, minimal `WebGPUTextureCubeBackend`
     (`WEBGPU-56`/`113`) since this backend had no `TextureCube` support at all before this — real
     `SetData()` per face/level, deliberately NOT full parity (no `GetData()`, no
     `RenderTargetCube`, `WEBGPU-114`). `WebGPU_EnvMap3D` CTest, 4/4: hand-derived geometry proves
     the reflection vector samples the exact expected cube face, a differential Fresnel check
     proves that term genuinely gates the blend (not present-but-inert), and
     `EnvironmentMapAmount=0` independently proves the amount also gates it.
   - 2026-07-18: `WEBGPU-27`/`38`/`68` — `instanced3d.wgsl`/`GetOrCreatePipelineInstanced3D()`/
     `DrawInstancedPrimitivesEx()`, real per-instance mat4 world transforms via a genuine second
     `WGPUVertexStepMode_Instance` vertex buffer binding, ported from `VulkanGraphicsBackend`'s
     `instanced3d.{vert,frag}.glsl`. Unlike every prior "genuinely new" shader family, this needed
     **no new bind group layout at all** (vertex buffers are independent of bind groups in WebGPU) —
     reuses `coloredBindGroupLayout_`/`coloredPipelineLayout_` unchanged, with `[0..15]` of that UBO
     reinterpreted as View×Projection (world comes from the per-instance stream, matching
     `FillInstancedPushConst()`'s own choice to ignore the caller's base `World` matrix entirely).
     `params.instanceVb == nullptr` falls back to a real `DrawIndexedPrimitivesEx()` draw rather
     than throwing. `WebGPU_Instanced3D` CTest, 5/5 (tested directly at the `IGraphicsBackend`
     level, matching `d3d9_instanced_test.cpp`'s own established convention for this API): 3
     instances in one draw call each paint their own small quad at their own independently-predicted
     screen location with the shared `DiffuseColor`.
   - Also corrected 2026-07-18 (no code change, stale bookkeeping): `WEBGPU-71` was marked ⬜ but
     `BasicEffect` has genuinely wired to `FillGpuDrawParams` → real UBO upload since `WEBGPU-66` —
     there is no separate `WebGPUEffectBackend` class on this backend, matching
     `VulkanGraphicsBackend`'s own architecture, where the equivalent wiring is direct
     `DrawPrimitivesEx()`/`FillExtPushConst()` dispatch too.
   - 2026-07-18 (follow-up task, texture-completeness cluster): `WEBGPU-51` (`Texture2D::GetData()`),
     `WEBGPU-113` (`TextureCube::GetData()`), and `WEBGPU-57`/`112` (`Texture3D`, this backend's
     first) all closed in one pass — see each row's own detail. All three reuse the exact staged
     MAP_READ-buffer/aligned-row/async-map-and-poll technique `WEBGPU-91`'s `ReadBackbuffer()` first
     established, extended to an arbitrary mip level (`Texture2D`), a specific cube face
     (`TextureCube`), and a full 3D sub-volume (`Texture3D`). A real, previously-latent bug found
     along the way: `WebGPUTextureBackend`/`WebGPUTextureCubeBackend`'s texture descriptors were
     missing `WGPUTextureUsage_CopySrc` (a hard wgpu-native validation error the moment `GetData()`
     tried to copy FROM one) — both fixed. Deliberately deferred, in priority order, per this
     cluster's own task brief: `RenderTargetCube` (`WEBGPU-114`) — a materially larger change (needs
     a per-face-bindable render target integrated with this backend's single-deferred-render-pass
     architecture, closer in scope to the original `WEBGPU-53`/`54` `RenderTarget2D` task than to
     this one) — real mip generation (`WEBGPU-52`, a genuine filtered downsample has no wgpu-native
     built-in the way Vulkan's `vkCmdBlitImage` does, so it needs its own render-pass-based blit
     investigation), and compressed texture upload (`WEBGPU-111`).
   - 2026-07-18 (follow-up task, mips/cube/compressed-textures cluster): `WEBGPU-114`
     (`RenderTargetCube`) and `WEBGPU-52` (real, genuinely-linear-filtered mip generation for a
     plain `Texture2D`/`TextureCube`) both closed — see each row's own full detail. Priority order
     per this cluster's own task brief was RenderTargetCube first (most valuable, most directly
     useful for real games), then mip generation, then compressed textures; both of the first two
     landed solidly tested. `WEBGPU-111` (compressed texture upload) was investigated but
     deliberately NOT implemented — see its own row for the full detail, but in short: no CNA
     backend (not just WebGPU) has ever done real native GPU-side block-compressed texture upload;
     `Texture2D.cpp` always CPU-decompresses DXT source content to RGBA8 first, and the common
     `ImageData` struct has no field for a compressed format at all, so closing this row for real
     (reachable from actual game code, not a synthetic direct-to-backend test) needs a genuine
     cross-backend common-interface change plus a `Texture2D.cpp` change — both out of this task's
     backend-local, `Microsoft::Xna::Framework`-excluded scope. (The real adapter on this
     development machine DOES support `WGPUFeatureName_TextureCompressionBC`, confirmed via a
     standalone diagnostic, so this is not a hardware dead end — just a larger design task than
     this cluster's backend-local scope allows.) Three genuinely new, previously-untested findings
     were made and documented (not silently fixed/hidden) along the way: (1) `QueueSprite()`'s
     clip-space geometry is always computed from the BACKBUFFER's own logical dimensions, never
     from whatever render target is currently bound — see `WEBGPU-114`'s own row for the empirical
     repro and scope note; (2) `WEBGPU-52`'s own real mip generation is a genuine, DELIBERATE
     divergence from FNA and every sibling CNA backend (Vulkan/EasyGL), which only auto-regenerate
     mips for a RENDER TARGET being unbound, never a plain texture — see `WEBGPU-52`'s own row for
     the full investigation and rationale for choosing to implement it anyway (clearly documented,
     not silently claimed as matching everyone else); (3) real compressed-texture upload is a
     cross-backend/XNA-layer gap, not a WebGPU-specific one — see `WEBGPU-111`'s own row.

For every task: use a clean build directory, pass `CNA_WEBGPU_ROOT` and
`CNA_WEBGPU_AUTO_DOWNLOAD=OFF`, record the exact command and result in the task note, and do not
mark it ✅ from source inspection alone.

---

## Phase 56 — WebGPU backend: infrastructure and CMake setup

> The native implementation is pinned to **wgpu-native v29.0.1.1** and uses WGSL. `CNA_WEBGPU_ROOT`
> is the supported reproducible input; automatic download is convenience-only until it has a
> checksum and CI/runtime coverage. Push constants are unavailable in WebGPU, so future 3D work
> must use uniform buffers.

| #   | Task                                                                                                          | Status | Notes                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------- |
| WEBGPU-1 | Add `CNA_GRAPHICS_RENDERER=WEBGPU` CMake option; locate headers + libs; define `CNA_RENDERER_WEBGPU`; verify package integrity | 🟨 | **Status split (re-audited 2026-08-27):** (a) **Package integrity — done** (SHA-256 verification, `WEBGPU-1`+`WEBGPU-151`). (b) **Linux x86_64 — runtime-verified** (`WEBGPU-124`/`128`/`130`, and the whole CTest suite runs on it). (c) **Windows/macOS/linux-aarch64 packages — NOT verified:** their asset SHA-256 hashes are now pinned, but no Windows/macOS/aarch64 build/link/runtime has been done here, so those paths are code-only. Writing a hash is integrity, not a build/link/runtime verification -- this row stays 🟨 until those platforms are at least built in an appropriate CI/platform. --- **SHA-256 verification (done 2026-08-27):** `cmake/WebGPUChecksum.cmake` pins the SHA-256 of every supported wgpu-native v29.0.1.1 asset (linux x86_64/aarch64, macos x86_64/aarch64, windows x86_64/aarch64-msvc, windows x86_64-gnu — all computed from the official GitHub release assets). The auto-download path now (a) refuses to download an asset with no pinned hash, (b) passes `EXPECTED_HASH SHA256=…` to `file(DOWNLOAD)`, and (c) re-verifies with `cna_webgpu_verify_sha256()` **before** `file(ARCHIVE_EXTRACT)`, failing closed — removing the invalid archive **and** the extract stamp — on any mismatch (also covering a pre-placed/tampered archive). `CNA_WEBGPU_ROOT`/offline and `CNA_WEBGPU_AUTO_DOWNLOAD=OFF` are unaffected (that branch bypasses the download entirely). New local test `WebGPU_ChecksumVerification` (`cmake/tests/webgpu_checksum_test.cmake`, pure CMake, no GPU): the pinned table returns the known linux hash and "" for unknown asset/version, a matching archive verifies and is kept, and both a corrupt archive and an empty/unknown hash fail closed with the archive+stamp removed. End-to-end re-verified against the real downloaded archive (`-- CNA WebGPU: verified SHA-256 of …wgpu-linux-x86_64-release.zip`). Linux configure/link/runtime-load were already verified (`WEBGPU-124`/`128`/`130`); non-Linux packages now have pinned hashes but their extracted trees are still not built/run here. |
| WEBGPU-2 | Create `modules/renderers/webgpu/include/CNA/Internal/Renderers/WebGPU/WebGPURenderer.hpp` — class skeleton, all IGraphicsBackend sub-interfaces declared | ✅ | Core backend, Texture2D, vertex/index buffers and SpriteBatch classes exist and are runtime-verified (`WEBGPU-124`–`WEBGPU-130`); Re-audited 2026-08-26: the render-target (`WEBGPU-53`), render-target-cube (`WEBGPU-114`), 3D-draw and stock-effect renderer classes have all since shipped and are CTest-verified on the real RADV GPU (`:0`). The last sub-interface that had not been declared/overridden -- the occlusion-query class -- shipped 2026-08-26 (`WEBGPU-84`: `CreateOcclusionQuery()` now returns a real `WebGPUOcclusionQueryRenderer`), so every `IGraphicsBackend` sub-interface this renderer needs now exists and is CTest-verified. Flipped to ✅. |
| WEBGPU-3 | Create `modules/renderers/webgpu/src/WebGPURenderer.cpp` — initial functional baseline and explicit unsupported 3D paths | ✅ | Compiles against the pinned header (`WEBGPU-124`); the 2D baseline is verified end-to-end (`WEBGPU-125`–`WEBGPU-130`) and full 3D has since landed (Phases 57-66). Re-verified 2026-08-27: the temporary `ThrowUnsupported3DDraw()` helper that guarded the pre-3D paths is gone (removed once every 3D route shipped) -- it was dead code (defined + one comment reference, never called). |
| WEBGPU-4 | SDL3 surface creation | ✅ | X11/Wayland branches runtime-verified on Linux desktop (`WEBGPU-125`, `WEBGPU-127`'s resize/minimize/restore coverage, `WEBGPU-130`'s independent second application). Win32/Metal are code paths only, not validation claims; Android is not a supported build route. |
| WEBGPU-5 | Instance, adapter, device and queue initialization | ✅ | Runtime-verified 2026-07-12 (`WEBGPU-125`); callback/timeout behaviour hardened and verified by `WEBGPU-127`'s `AllowProcessEvents` polling fix. |
| WEBGPU-6 | Surface configuration, resize and backbuffer acquisition | ✅ | Runtime-verified: `WEBGPU-125` (baseline present/clear), `WEBGPU-127` (resize 800×600↔960×540, minimize/restore, zero-size handling). |
| WEBGPU-7 | Command encoding and queue submission per frame | ✅ | Runtime-verified by every `WEBGPU-125`–`130` run (120–180+ frames each, no WebGPU validation errors). |
| WEBGPU-8 | Main render pass with colour/depth/stencil attachments | ✅ | Colour and depth attachments are both runtime-verified (`WEBGPU-125`/`126`/`130` for colour; `WebGPU_Colored3D`'s depth-order checks for depth, 2026-07-12 — see `WEBGPU-32`). Stencil closed 2026-07-18 (`WEBGPU-53`/`54`, `WebGPU_RenderTarget2D` Check C): a `RenderTarget2D` created with `DepthFormat::Depth24Stencil8` gets a real combined depth+stencil `WGPUTexture`/attachment in `RenderPendingDrawsToRenderTarget()`'s render pass (the RT-targeting sibling of `EnsureFrameRendered()`'s swapchain pass, identical attachment-description shape) — verified via a genuine `ClearOptions::Stencil` clear inside a real render pass. Note: this specific run exercised the RenderTarget2D pass, not the swapchain's own main pass directly (same code shape, different pass instance) — a dedicated backbuffer-specific stencil clear/test still hasn't been written, though there is no reason to expect it to behave differently. |
| WEBGPU-9 | Colour/depth/stencil clear state | ✅ | Colour and depth clear are both runtime-verified (`WebGPU_Colored3D`'s `ClearOptions::Target\|DepthBuffer` calls, 2026-07-12). Stencil clear closed 2026-07-18 — see `WEBGPU-8`'s updated note (`WebGPU_RenderTarget2D` Check C, `ClearOptions::Target\|DepthBuffer\|Stencil` against a real `Depth24Stencil8` RenderTarget2D). |
| WEBGPU-10 | Present and recoverable acquisition handling | ✅ | The v29 status API is handled, and both normal present and surface-loss/outdated recovery are runtime-verified (`WEBGPU-127`, `WEBGPU-130`). |

---

## Phase 56.1 — Recovery and verified native 2D vertical slice (active)

| # | Task | Status | Acceptance criteria |
| --- | --- | --- | --- |
| WEBGPU-124 | Align the backend with the pinned `wgpu-native v29.0.1.1` C API, beginning with surface-acquisition status handling. | ✅ | Verified 2026-07-12: fresh offline CMake configure, generated CMake compile of `WebGPURenderer.cpp`, static backend archive and copied `libwgpu_native.so`; no compatibility aliases/workarounds. |
| WEBGPU-125 | Build and run a minimal native window that initializes the backend, clears, presents at least 60 frames, then exits cleanly. | ✅ | Verified 2026-07-12: clean offline build with `-DCNA_WEBGPU_ROOT=$PWD/vendor/wgpu-native -DCNA_WEBGPU_AUTO_DOWNLOAD=OFF`, then `timeout 60s ./cna_demo_2d --smoke 120` on a host desktop session. The run completed 120 frames in 2.10 s with exit code 0 and no uncaptured WebGPU error, device-loss report or loader failure. The earlier X11-close `XInput BadWindow` remains a lifecycle observation for `WEBGPU-127`, not a failure of this smoke gate. |
| WEBGPU-126 | Validate the existing 2D slice: texture upload, source rectangles, tint/alpha, rotation, flip, linear/point sampling, wrap/clamp/mirror, logical presentation and resize. | ✅ | Verified 2026-07-12 with `cna_demo_2d --webgpu-2d-validation --smoke 120`: it uploads `player.png`, submits deterministic source-rectangle/tint/alpha/rotation/both-flip draws, exercises Linear/Point + Clamp/Wrap/Mirror samplers (with UVs extending outside [0,1]), and resizes 800×600→960×540→800×600. The clean native run emitted no WebGPU error. Manual desktop screenshot review confirmed the expected crops, semi-transparent tinted rotation, both flips and sampler variants without artefacts. |
| WEBGPU-127 | Harden lifecycle and failure paths discovered in the first run: request timeout/polling strategy, surface loss/outdated recovery, zero-size windows and destruction order. | ✅ | Verified 2026-07-12: replaced unbounded spontaneous adapter/device callbacks with `AllowProcessEvents` polling and a 10 s timeout (the v29 package's `wgpuInstanceWaitAny` panics as unimplemented); minimized/nulled surfaces now unconfigure and release their depth attachment before restore/reconfigure. `cna_demo_2d --webgpu-2d-validation --smoke 180` exercised resize 800×600→960×540, minimization, restoration to 800×600, regular surface reconfiguration and teardown with exit 0 and no WebGPU error. |
| WEBGPU-128 | Make package discovery and runtime deployment reproducible for the verified Linux target. | ✅ | Verified 2026-07-12 with a clean `/tmp/cna-webgpu-128` configure using `CNA_WEBGPU_ROOT=$PWD/vendor/wgpu-native` and `CNA_WEBGPU_AUTO_DOWNLOAD=OFF`. The final `cna_demo_2d` copies `libwgpu_native.so` beside itself, records `NEEDED libwgpu_native.so` plus `$ORIGIN` first in RUNPATH, and `ldd` resolved the copied sibling runtime before a successful 120-frame run. Auto-download integrity/checksum policy and unvalidated platform packages remain deferred. |
| WEBGPU-129 | Add a backend-specific native smoke-test target and CTest registration that can run only when a display/GPU is available. | ✅ | Verified 2026-07-12: fresh WebGPU configure with `CNA_BUILD_TESTS=ON` registers `WebGPU_Native2D_Smoke`, a CTest wrapper around `cna_demo_2d --smoke 120`. It passed on the host desktop in 2.30 s; with `DISPLAY` and `WAYLAND_DISPLAY` removed it reported SKIPPED with a clear reason. |
| WEBGPU-130 | Integrate `../mobile-eggbert` as the first real desktop 2D application smoke test. | ✅ | Verified 2026-07-12 (commit `dcdb648` in `../mobile-eggbert`, pushed to origin/develop): its CMake targets `../cna_graphics`, keeps `SDL_RENDERER` as the desktop default (matching the repo's own last real committed default, `de40814`) with `WEBGPU` selectable only via explicit `-DCNA_GRAPHICS_RENDERER=WEBGPU`, recognizes `cna_backend_graphics_webgpu` in the GNU linker group, and copies `libwgpu_native.so` beside `WindowsPhoneSpeedyBlupi` with `$ORIGIN` first in RUNPATH. Also fixed an incidental, previously-disabled `worlds/` directory POST_BUILD copy step (referenced an undefined CMake variable) found while verifying this task — a genuinely fresh build directory would not have loaded any level otherwise. On a real desktop session (`DISPLAY=:0`, not the sandbox's Xvfb `:99` — `wgpu-native` needs a real GPU context): a clean WebGPU build ran, reached its main menu automatically ~5s after launch with pixel-correct `SpriteBatch` rendering (title, animated character, player-select panel, Setup gear, Play button), and an `xdotool`-driven click on the Play button correctly triggered the `InitPlay`→`StartMission(1)` sequence — an animated "constructing Blupi" cutscene with a filling progress bar and a cross-fade transition, all rendering correctly frame-by-frame with no WebGPU validation errors. That sequence fades back to the main menu afterward; an identical byte-for-byte-comparable run against a freshly built `EASYGL` binary reproduced the exact same fade-back behavior, confirming it is a pre-existing, backend-independent `Game1`/`InputPad` state-machine behavior in `../mobile-eggbert` itself (its `SetPhase`/fade-transition logic, `Game1.cpp` — see the comment block at the top of that file), not a WebGPU rendering regression. Out of scope to fix here (touches sibling-repo gameplay logic, not a `cna_graphics` graphics-backend concern) — flagged for whoever next works on `../mobile-eggbert`. No Android change was made. |
| WEBGPU-131 | Establish the native 2D test baseline before 3D work. | ✅ | `WEBGPU-124`–`WEBGPU-130` are all now ✅. Manual test evidence and known limitations are recorded in `docs/webgpu-renderer.md` (revised 2026-07-12 alongside this task — see its "Verified native 2D baseline" and "Important limitations" sections). The native 2D SpriteBatch path (texture upload, source rects, tint/alpha/rotation/flips, Linear/Point + Clamp/Wrap/Mirror sampling, logical presentation/resize, lifecycle recovery) is verified on Linux desktop against both the synthetic `cna_demo_2d --webgpu-2d-validation` scene and a real, independently-built game (`../mobile-eggbert`). 3D/effects/render-targets/readback/MRT/browser remain unimplemented — Phase 57 onward, tracked below. |
| WEBGPU-132 | `SpriteBatch` partial alpha blending **FIXED** — real bug, found via `WEBGPU-91`/`92`'s new pixel-asserted readback test, not caught by `WEBGPU-126`'s manual screenshot review (a too-bright translucent overlay reads as "looks about right" by eye against a busy background — exactly why pixel assertions matter more than visual review). | ✅ (historically superseded by `WEBGPU-141`) | **NOTE 2026-08-27: this row's HARDCODED-straight sprite blend was itself a WebGPU-only deviation from FNA, later replaced by the device-driven blend of `REMED-GFX-102`/`WEBGPU-141`(B).** FNA's default `SpriteBatch.Begin()` uses *premultiplied* `AlphaBlend`, so a straight-alpha sprite under it gives full brightness (as EasyGL/SOFTWARE/FNA all do); `WEBGPU-141` corrected this row's Check G to use `NonPremultiplied` instead. The blend factors below are no longer what the renderer uses. Original 2026-07-12 note follows. | Verified 2026-07-12: the sprite pipeline's blend factors (`WGPUBlendFactor_One`/`OneMinusSrcAlpha`, a *premultiplied*-alpha equation) didn't match the sprite WGSL fragment shader's actual output (`textureSample(...) * input.color`, *straight*/non-premultiplied — same as Vulkan's `sprite2d.frag.glsl`), so any tint/texture alpha strictly between 0 and 255 was silently ignored for colour (a 50%-alpha sprite rendered at full brightness; only alpha=0 or alpha=255 ever looked correct, since both happen to be blend-equation-independent edge cases). **Fixed** by matching Vulkan's own, correct pairing: `srcFactor = SrcAlpha` (not `One`) for the colour channel, `alpha.dstFactor = Zero` (not `OneMinusSrcAlpha`, matching Vulkan's `srcAlphaBlendFactor=ONE, dstAlphaBlendFactor=ZERO`) — no shader change. New Check G in `WebGPU_Clear_Readback` (a 50%-alpha sprite over black must land strictly between black and full colour), verified genuinely discriminating via `git stash` (reads back full brightness (255) with the bug reverted, ~188 with the fix — the exact value depends on whether the chosen swapchain format blends in linear or sRGB-encoded space, both legitimate, so the check asserts direction/magnitude via a wide tolerance band, not an exact value). Re-verified `WebGPU_Native2D_Smoke` and the `--webgpu-2d-validation` scene (screenshot-reviewed) still render correctly (mostly-opaque content, so no expected visual difference from this fix in that particular scene). |
| WEBGPU-133 | Browser (Dawn) `Destroyed texture [unlabeled 64x64 BGRA8Unorm] used in a submit` under a **multiple-ReadBackbuffer-per-frame** pattern **-- FIXED**. | ✅ | Found and fixed 2026-08-26. **Root cause:** `ReadBackbuffer()`'s buffer-map wait yields to the browser event loop (`emscripten_sleep`, Asyncify), and the browser presents and *invalidates* the canvas's current surface texture during that yield. The renderer cached `acquiredTexture_` across the yield, so a later same-frame flush (the effect suites do several Clear/draw/ReadBackbuffer cycles per frame) re-submitted the now-destroyed texture -- which wgpu-native tolerates and Dawn rejects. **Fix (lazy discard):** a readback marks the acquired texture stale (`acquiredBackbufferStale_`) instead of releasing it; `EnsureFrameRendered()` discards and re-acquires it only when it is about to RENDER again (`framePending_`), while a same-frame re-READ still reuses the already-captured `readbackBuffer_`. All Emscripten-only; the native path never compiles the flag in and is unchanged. This design handles both browser readback patterns -- draw/read per check AND several reads of one frame. **Verified in headless Chrome:** the five effect pages went from 2/5, 2/4, 4/9, 1/4, 2/4 to `PbrEffect` 5/5, `EnvironmentMapEffect` 4/4, `SkinnedEffect` **9/9**, `DualTextureEffect` 4/4, `AlphaTestEffect` 4/4, all with zero `Destroyed texture` errors; the 2D and 3D browser smokes still pass. **The earlier `SkinnedEffect` D2 failure was NOT a skinning bug** -- reliable per-render probing showed the WeightsPerVertex=2 quad *does* shift right in-browser; the test's second same-frame read (`right`) was returning stale pixels, which the lazy-discard design fixes, taking skinned to 9/9. |
| WEBGPU-134 | WEBGPU reported `MultipleRenderTargets` as supported but `SetRenderTargets` refused count > 1 **-- FIXED**. | ✅ | Found by external audit, fixed 2026-08-26. `WebGPURenderer::SupportsCapability` overrode only `WireFrame`, so `MultipleRenderTargets` inherited `IGraphicsRenderer`'s permissive `true`, while `SetRenderTargets` threw for count > 1 (MRT infra `WEBGPU-85/86/87` still open) -- the exact defect class `WEBGPU-115` ruled on for `WireFrame` (a renderer must not claim a capability it silently refuses). **Fix:** `SupportsCapability(MultipleRenderTargets)` now returns false with a `WEBGPU-115`-style comment; the count > 1 refusal is now a `System::NotSupportedException` (was `std::runtime_error`) whose message says the capability query reports false and to bind a single target instead, matching `RequireSupportedFillModeEXT`'s shape. **Test:** new `WebGpuMrtOcclusionContractTests.cpp` (auto-globbed into `CnaTests`, gated on `CNA_RENDERER_WEBGPU`); `WebGpuWireFrameContractTests.cpp` line 286 updated to `EXPECT_FALSE(MRT)`. **Verified on the real AMD RADV GPU (`:0`), native WEBGPU build `cmake-build-webgpu`:** `WebGpuMrtOcclusionContract.*` 3/3 pass (`MultipleRenderTargetsCapabilityIsFalse`, `SingleTargetBindStillRenders`, `TwoTargetBindThrowsNotSupportedAndDeviceRecovers`) plus `WebGpuWireFrameContract.CapabilityQueryAnswersForWireFrame` -- 4/4 total. Discrimination confirmed: neutralising the MRT arm fails both capability tests. |
| WEBGPU-135 | WEBGPU reported `OcclusionQuery` as supported but `CreateOcclusionQuery()` silently no-ops **-- FIXED**; full `SupportsCapability` audit. | ✅ | Found by external audit, fixed 2026-08-26. `CreateOcclusionQuery()` is not overridden, so it returns the base `nullptr` and `OcclusionQuery::IsComplete` stays false / `PixelCount` 0 with no diagnostic (`WEBGPU-84` -- real occlusion queries -- is open), while `SupportsCapability` inherited `true`. **Fix:** `SupportsCapability(OcclusionQuery)` now returns false (WEBGPU-115 class); NO query implementation added. `OcclusionQueryCapabilityIsFalse` added to `WebGpuMrtOcclusionContractTests.cpp`; `WebGpuWireFrameContractTests.cpp` line 287 -> `EXPECT_FALSE`. Verified on the real RADV GPU (`:0`): 5/5 (`WebGpuMrtOcclusionContract.*` 4/4 + `WebGpuWireFrameContract.CapabilityQueryAnswersForWireFrame`); discrimination confirmed (neutralising the arm fails both). **Full `SupportsCapability` audit (19 members** -- the earlier version of this note said 18, having missed `IndirectDraw`, the last enum member, `GraphicsCapability.hpp:211`, which has no trailing comma). **The seam matters:** `GraphicsDevice::SupportsCapability` (`GraphicsDevice.cpp` ~2355-2377) answers SIX members from dedicated EXT hooks BEFORE it ever delegates to the renderer's own switch, each hook defaulting `false` precisely so a permissive `default: return true` switch cannot answer it, and **WEBGPU overrides none of them** -- so all six report **false at the seam a game actually calls**: `CompiledEffects` (`SupportsCompiledEffects()`), `FloatRenderTargets`/`HalfFloatRenderTargets` (`SupportsSurfaceFormatAsRenderTargetEXT`, whose `Defer` path is `format == Color`, and WebGPU's `ClassifyRenderTargetFormatEXT` defers), `HalfFloatTextureLinearFiltering` (`SupportsHalfFloatTextureLinearFilteringEXT()`), `ComputeShaders` (`SupportsComputeShadersEXT()`), `IndirectDraw` (`SupportsIndirectDrawEXT()`). **Correction:** an earlier version of this note judged `HalfFloatTextureLinearFiltering` and `ComputeShaders` at the *renderer* seam and called them "inherited true" -- they never reach it; both are false at the `GraphicsDevice` seam, verified there. There is no MOD-1502 compute follow-up: compute is already reported false, not a defect. The remaining THIRTEEN members do reach the renderer's own switch and were confirmed truthful, each backed by a CTest or a code path: `ThreeD` (WebGPU_Colored3D/Textured3D/LitTextured3D), `DepthStencilBuffer` + `StencilBuffer` (Depth24PlusStencil8; WebGPU_RenderTarget_DepthStencilUsage), `MultiSampleAntiAliasing` (device-probed; WebGPU_Msaa), `AnisotropicFiltering` (sampler `descriptor.maxAnisotropy`), `Texture3D` (WebGPU_Texture3D), `Instancing` (WebGPU_Instanced3D + cardinality), `AdditiveBlending` (WebGPU_AdditiveBlendContract) -- all `true`; `CustomEffects` `true` in the project's "accepts an effect" sense (stock effects render; custom WGSL source is the separate `ExecutesShaderEffectSourceEXT`/`WEBGPU-76` gap); `MultiStreamVertexInput` `false` (shared REMED-GFX-201 default); and this batch's two fixes `MultipleRenderTargets` `false` (WEBGPU-134) and `OcclusionQuery` `false` (WEBGPU-135), with `WireFrame` already `false` (WEBGPU-115). All nineteen report truthfully. Superseded 2026-08-26 by `WEBGPU-84`: occlusion queries are now implemented, so the temporary false arm this row installed was removed and `SupportsCapability(OcclusionQuery)` truthfully reports true (the contract test was flipped to assert true in the same commit). |
| WEBGPU-136 | The plan's own top banner was stale and contradicted its rows; dead references throughout. | ✅ | Fixed 2026-08-26. `NEXT.md` points readers at this banner as the source of truth, so its drift was load-bearing. Re-dated "Remaining work" 2026-07-18 → 2026-08-26 and re-derived it; corrected the banner's stale claims (`WEBGPU-115` is ✅ not ⬜; `WEBGPU-70` is 🟨 not ⬜, only the instanced route's `vertexStart` is missing; noted the `WEBGPU-134`/`135` capability fixes; `NOXNA` → `CNAEXT`; recorded that IDs 62 and 100–105 never existed). Flipped rows that understated shipped work, each backed by a CTest **run on the real RADV GPU (`:0`) this session, all Passed**: `WEBGPU-93/94/95/96` → ✅, `WEBGPU-98` instancing half done, `WEBGPU-42/43/45/46/48` → ✅, `WEBGPU-70` → 🟨. Fixed dead references throughout: `docs/webgpu-backend.md` → `docs/webgpu-renderer.md` (8×); `WebGPUGraphicsBackend` → `WebGPURenderer` (18×); the `Internal/Backends/WebGPU/` paths → the real `modules/renderers/webgpu/…` locations; `CNA_GRAPHICS_BACKEND` → `CNA_GRAPHICS_RENDERER`; `CNA_BACKEND_WEBGPU` → `CNA_RENDERER_WEBGPU`. Historical evidence text and old verification dates were left untouched. |
| WEBGPU-137 | `docs/webgpu-renderer.md` was internally contradictory. | ✅ | Fixed 2026-08-26. Pruned "Important limitations" to what is genuinely open (MRT, occlusion query, compressed textures, per-`RenderTarget2D` `multiSampleCount`, custom WGSL effects), removing `Texture2D.GetData()` (`WEBGPU-51`), the "full BlendState/RasterizerState cull/viewport/scissor/stencil mapping" line (`WEBGPU-78`–`81`) and "browser/Emscripten WebGPU" (`WEBGPU-119`–`122`) -- all implemented and described in the doc's own later sections -- and cross-referenced the `WEBGPU-134`/`135` capability answers. Corrected "experimental fifth CNA graphics renderer" (there are 49+ identities). Rewrote the teaching offline build recipe to build into the in-repo `cmake-build-webgpu/` with `ccache` and `-j$(nproc)` instead of `/tmp/cna-webgpu-128` at `-j1` (the doc had been teaching the forbidden build-under-`/tmp` practice); the historical "Verified native smoke gate" recipe was left as recorded evidence. Made the Emscripten "three seams" read "five", matching the plan banner. |
| WEBGPU-138 | Stale WEBGPU descriptions outside the plan. | ✅ | Fixed 2026-08-26. `CLAUDE.md` said WEBGPU "has a functional native 2D baseline, not yet the 3D/effect parity of the established GPU renderers" -- rewritten to the real boundary (desktop 3D + every stock effect, instancing, `RenderTarget2D`/`RenderTargetCube`, MSAA, `Texture3D`, and the browser path; still open: MRT, occlusion queries, compressed textures, custom WGSL effects). `docs/web-emscripten-graphics-limitations.md` said the 3D/effect shaders were "not yet exercised in a browser (`WEBGPU-121` 🟨)" in both its intro and its capability table -- corrected, since `WEBGPU-121` is ✅ (2D + 3D `BasicEffect` + every stock effect shader render in-browser). |

---

## Phase 57 — WebGPU backend: uniform buffer system (replaces push constants)

> **Recommended concrete entry point (investigated 2026-07-12, not yet implemented — read this
> before starting Phase 57/58/59/63):** the natural first vertical slice is
> `WebGPURenderer::DrawColoredPrimitives()`/`DrawIndexedColoredPrimitives()` (already
> declared in the header, currently calling `ThrowUnsupported3DDraw`), because
> `GraphicsDevice::DrawUserPrimitives()`/`DrawUserIndexedPrimitives()` already call them directly
> (`GraphicsDevice.cpp:674` and its indexed sibling) for any `VertexPositionColor` (stride-16) draw
> with an effect applied only for its World/View/Projection matrices — no `BasicEffect`
> lighting/texture dispatch (`GpuDrawParams`/`DrawPrimitivesEx`, Phase 63's harder path) required.
> This is exactly `WEBGPU-93`'s own target ("3D coloured-quad pixel test, stride 16").
>
> Findings from reading the Vulkan reference implementation (`VulkanGraphicsBackend::
> DrawColoredPrimitives`, `FillExtPushConst`, `colored3d.vert.glsl`/`.frag.glsl`) that the next
> session should reuse rather than re-derive:
> - The 128-byte/32-float UBO layout `WEBGPU-11` asks for is **not** a new design — it's
>   `FillExtPushConst()`'s existing byte-for-byte layout (`VulkanGraphicsBackend.cpp:3459`):
>   `[0..15]` MVP (column-major mat4), `[16..19]` diffuseColor, `[20..23]` ambientColor+
>   lightingEnabled, `[24..27]` light0Dir+textureEnabled, `[28..31]` light0Diffuse+
>   vertexColorEnabled. Reuse this exact layout so the same UBO/shader-input shape works for
>   `DrawColoredPrimitives` now and `BasicEffect`'s full `DrawPrimitivesEx` dispatch later (Vulkan's
>   own `DrawColoredPrimitives` fills the *same* 32-float layout directly, bypassing
>   `GpuDrawParams`, with `diffuseColor=white` and `vertexColorEnabled=1`, everything else zeroed —
>   copy that approach, not a bespoke smaller struct).
> - WGSL's `mat4x4f` + 4×`vec4f` gives the identical 128-byte/16-byte-aligned layout in a
>   `@group(0) @binding(0) var<uniform>` block — no manual padding needed.
> - **No Vulkan-style NDC Y-flip is needed.** Vulkan's `colored3d.vert.glsl` negates `pos.y`
>   because Vulkan's clip space is Y-down by convention; WebGPU (like D3D/Metal, which is what real
>   XNA's own math already assumes) is Y-up, matching the *already-verified* `QueueSprite()` 2D
>   path's own `1.0 - 2.0*py/H` formula (no separate flip there either). A plain
>   `position = mvp * vec4f(pos, 1.0)` should be directly correct. Depth range `[0,1]` also already
>   matches XNA/WebGPU with no remap, same as Vulkan's own comment says.
> - **Real lifetime hazard to design around:** `GraphicsDevice::DrawUserPrimitives()` creates its
>   vertex buffer as a function-local `unique_ptr<IVertexBufferBackend>` that is destroyed right
>   after `DrawColoredPrimitives()` returns. Since this backend's frame model defers all rendering
>   to `EnsureFrameRendered()` (matching Vulkan/Bgfx's own "whole frame batched into one pass"
>   design — see `NEXT.md`'s architecture notes), the draw **cannot** hold onto the caller's
>   `WebGPUVertexBufferBackend`/its `WGPUBuffer` — it will be gone by render time. Vulkan's own
>   `DrawColoredPrimitives` sidesteps this by `memcpy`-ing the vertex bytes into its own
>   `Pending3DDraw::vbData` *immediately*, at call time, not by holding a reference. `WebGPUVertexBufferBackend`
>   has no CPU-readable accessor today (`Buffer()` only returns the opaque `WGPUBuffer` handle) —
>   add a small CPU shadow copy (`std::vector<std::uint8_t>`, populated inside
>   `SetDataWithOptions()`) so `DrawColoredPrimitives` can copy it out synchronously, mirroring
>   Vulkan's approach, before queuing a `ColoredDrawCommand` for `EnsureFrameRendered()` to actually
>   build a real `WGPUBuffer` + issue the draw from later (parallel to how `spriteCommands_`/
>   `RenderSprites()` already work for 2D).
> - Depth-stencil state: a real pipeline-cache keyed at minimum by `(depthTestEnabled_,
>   depthWriteEnabled_)` is enough for a first slice (WebGPU pipeline state is largely immutable —
>   see `docs/webgpu-renderer.md`'s architecture notes — so this can't be fully dynamic like
>   Vulkan's `VkDynamicState`); `CullMode`/`FillMode::WireFrame` can stay unhandled for the very
>   first slice (tracked separately as `WEBGPU-41`/`WEBGPU-79`/`WEBGPU-115`), documented as a known
>   gap rather than silently ignored. `CullMode` was implemented in `WEBGPU-41`; `FillMode::WireFrame`
>   has no native state to map to at all and is now a truthful refusal (`WEBGPU-115`) -- "documented
>   as a known gap" proved insufficient on its own, because the capability query kept claiming the
>   opposite.

| #   | Task                                                                                                          | Status | Notes                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------- |
| WEBGPU-11 | Design `GpuUniforms` struct (128 bytes = 32 floats) matching Vulkan push constant layout; upload via `wgpuQueueWriteBuffer` | ✅ | Verified 2026-07-12: implemented as the anonymous-namespace `FillColoredUniforms()` helper, byte-for-byte matching `VulkanGraphicsBackend::FillExtPushConst()`'s 32-float layout (`[0..15]` MVP, `[16..19]` diffuseColor, `[20..23]` ambient+lightingEnabled, `[24..27]` light0Dir+textureEnabled, `[28..31]` light0Diffuse+vertexColorEnabled), uploaded via `wgpuQueueWriteBuffer`. Runtime-verified by `WebGPU_Colored3D`. |
| WEBGPU-12 | Create `WGPUBuffer` (uniform, size=128) per frame (or ring buffer of 3); map on CPU side via `wgpuBufferGetMappedRange` | ✅ | **DONE 2026-08-28.** Every per-draw transient buffer (all 33 vertex/uniform/light/factors/skinning/transform/params/instance sites across the 11 3D families) is now ACQUIRED from a bounded pool -- `WebGPURenderer::AcquireTransientBuffer(usage, size)`, keyed by `(usage, power-of-two size class)` -- and RECYCLED back into it after submit (`RecycleTransientBuffer`, replacing the old `pendingBufferReleases_` release), rather than created and destroyed every draw. So a warmed-up scene reuses a bounded working set and stops calling `wgpuDeviceCreateBuffer` entirely. This is safe WITHOUT explicit fencing: every write (`wgpuQueueWriteBuffer`) and read (the recorded draw) goes through the single device queue in FIFO order, and wgpu-native keeps a buffer alive internally until the submission that references it completes, so a recycled buffer reused in a later cycle is only written after the prior cycle's reads. The pool is bounded per size class (cap 128) and released on teardown. Verified by `WebGPU_BufferPoolStress` (a 40-frame fixed scene): the buffer CREATE count plateaus after warm-up while the REUSE count keeps climbing (created=16, reused=608), with zero uncaptured GPU errors and the scene still rendering; the whole existing pixel suite still passes (`104`→`106` tests). Uses `wgpuQueueWriteBuffer` rather than a mapped ring, which is this renderer's upload path everywhere; index buffers keep their exact-size create/release (their odd sizes are released rather than pooled). |
| WEBGPU-13 | `WGPUBindGroupLayout` for slot 0 binding 0 (uniform buffer) — shared across all 3D pipelines | ✅ | Verified 2026-07-12: `coloredBindGroupLayout_`, one `WGPUBufferBindingType_Uniform` entry at binding 0, visible to both vertex and fragment stages. Currently colored3D-pipeline-specific, not yet literally shared with a second 3D pipeline family (none exist yet) — same layout shape is intended to be reused once Phase 58's other WGSL shaders land. |
| WEBGPU-14 | `WGPUBindGroup` creation and per-draw update for MVP matrix | ✅ | Verified 2026-07-12: a fresh `WGPUBindGroup` is created and bound per draw in `RenderColoredDraws()`, pointing at that draw's own uniform buffer. |
| WEBGPU-15 | `WGPUBindGroupLayout` for slot 1 binding 0 (texture sampler) — for textured pipelines | ✅ | Verified 2026-07-12: `texturedBindGroupLayout_` (group 1: binding 0 sampler, binding 1 texture — mirrors the SpriteBatch bind group layout shape exactly), used alongside `coloredBindGroupLayout_` (group 0, the UBO) by the new `textured3d.wgsl` pipeline (`WEBGPU-20`/`33`). |
| WEBGPU-16 | `WGPUSampler` creation mapping `SamplerState` (filter, address mode) → WGPU descriptor | ✅ | Re-audited 2026-08-26. `FillWGPUSamplerDescriptor()`/`GetOrCreateSlotSampler()` map the full XNA `TextureFilter` enum and every address mode (Wrap→Repeat, Mirror→MirrorRepeat, Clamp→ClampToEdge). Proven on the real RADV GPU (`:0`) by the renderer-neutral sampler contract tests -- `WebGPU_PointSamplingContract`, `WebGPU_TextureFilterOrdinalContract`, `WebGPU_StockEffectSamplerContract`, `WebGPU_DualTextureSlotSamplerContract`, all Passed -- each of which drives Linear/Point × Clamp/Wrap/Mirror and asserts the sampled texel changes accordingly (the earlier note's "full per-slot 3D mapping remains open" was stale; see WEBGPU-82). (`WebGPU_Clear_Readback`'s Wrap/Mirror checks, which briefly regressed via the `e2e8dee99` sprite viewport-relative-clip-space change -- `WEBGPU-141` (A) -- are fixed and pass again; that was never a sampler-mapping defect, as the contract tests above sample Wrap/Mirror correctly.) |
| WEBGPU-17 | `WGPUPipelineLayout` combining UBO bind group layout + texture bind group layout | ✅ | Verified 2026-07-12: `texturedPipelineLayout_` combines `coloredBindGroupLayout_` (group 0, UBO) + `texturedBindGroupLayout_` (group 1, sampler+texture) — `WebGPU_Textured3D` proves both groups bind and are read correctly by the same draw (UBO's `DiffuseColor` multiplies the sampled texture colour). `coloredPipelineLayout_` (UBO-only, no texture group) remains separately used by `colored3d.wgsl`. |

---

## Phase 58 — WebGPU backend: WGSL shaders

| #   | Task                                                                                                          | Status | Notes                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------- |
| WEBGPU-18 | Write `sprite2d.wgsl` — 2D sprite vertex + fragment shader (pos + UV + RGBA tint); embed as C++ string literal | ✅ | Embedded WGSL compiles and is runtime-verified: `WEBGPU-126`'s validation scene and `WEBGPU-130`'s independent `../mobile-eggbert` application both render correctly through it with no WebGPU validation errors. |
| WEBGPU-19 | Write `colored3d.wgsl` — 3D vertex shader (float3 pos + ubyte4 color), flat fragment; UBO for MVP | ✅ | Verified 2026-07-12: embedded WGSL in `CreateColoredResources()`, compiles and is runtime-verified by `WebGPU_Colored3D` (4/4 checks, including a genuine depth-test proof — see `WEBGPU-32`/`WEBGPU-64`/`65`). **UPDATE 2026-08-27: fog added (WEBGPU-145)** — colored3d now carries `fogColor`/`fogVector` in its primary UBO and applies FNA's ApplyFog; the original "no fog, deliberately deferred" note below is historical. |
| WEBGPU-20 | Write `textured3d.wgsl` — 3D vertex (float3 pos + float2 UV); texture2D sampler in fragment | ✅ | Verified 2026-07-12: embedded WGSL in `CreateTexturedResources()`, `@group(1)` sampler+texture, reuses `@group(0)` UBO from `colored3d.wgsl`. `WebGPU_Textured3D` CTest (3/3): a solid-colour texture samples correctly, `DiffuseColor` genuinely multiplies the sampled colour (not just passed through), and the indexed draw path works too. **Fog added 2026-08-27 (WEBGPU-145).** (Original: no fog, deferred.) |
| WEBGPU-21 | Write `colored_textured3d.wgsl` — float3 + ubyte4 color + float2 UV; multiply tex×color in fragment | ✅ | Verified 2026-07-12: embedded WGSL in `CreateTexturedResources()`, reuses `textured3d.wgsl`'s exact UBO (group 0) and texture (group 1) bind groups — same `texturedPipelineLayout_`, just a different vertex layout (3 attributes) and shader combining `colored3d.wgsl`'s vertex-colour mixing with `textured3d.wgsl`'s sampling. `WebGPU_ColoredTextured3D` CTest (3/3): vertex colour and texture colour both genuinely multiply (not one silently ignored), and `VertexColorEnabled=false` still correctly falls back to `DiffuseColor`. |
| WEBGPU-22 | Write `lit_textured3d.wgsl` — float3 pos + float3 normal + float2 UV; Blinn-Phong lighting in fragment | ✅ | Verified 2026-07-12: embedded WGSL in `CreateLitTexturedResources()`, ported from `VulkanGraphicsBackend`'s `lit_textured3d.{vert,frag}.glsl` (itself from FNA's `Lighting.fxh` `ComputeLights()`). Second UBO (`litBindGroupLayout_`, group 0 binding 1, `LitLightParams`/272 bytes) carries `DirectionalLight1`/`2`, `EmissiveColor`, `World`, eye position, per-light + material specular, and a CPU-precomputed normal matrix (WGSL has no `inverse()`); group 1 (sampler+texture) reuses `texturedBindGroupLayout_` unchanged. `WebGPU_LitTextured3D` CTest (4/4, git-stash-verified): unlit fallback still works, a light facing the surface fully illuminates it, a light behind it contributes nothing (real N·L gating, not "always bright"), and ambient reaches the shader with no directional light at all. **Fog added 2026-08-27 (WEBGPU-145)**, both per-pixel and per-vertex variants. (Original: no fog, deferred.) |
| WEBGPU-23 | Write `alpha_test3d.wgsl` — per-pixel alpha discard matching XNA AlphaTestEffect semantics | ✅ | Verified 2026-07-12: embedded WGSL in `CreateAlphaTestResources()`, ported from `VulkanGraphicsBackend`'s `alpha_test3d.{vert,frag}.glsl`/`alpha_test_colored3d.vert.glsl`. `AlphaTestEffect` has no lighting, so the primary 128-byte UBO's ambient/light0 slots are repurposed for `{alphaRef, alphaTolerance, passWeight, failWeight}` (`FillAlphaTestUniforms()`) — same size/shape, so `coloredBindGroupLayout_`/`texturedBindGroupLayout_`/`texturedPipelineLayout_` are all reused unchanged, no new bind group layout. Two shader modules: `alphaTestShader_` (strides 20/32, position+UV only — one WGSL module serves both, only the vertex buffer layout differs, since stride 32's normal attribute is simply unread) and `alphaTestColoredShader_` (stride 24, adds vertex-colour tint). Hit and fixed a real WGSL grammar issue while writing this: unparenthesized relational operators (`<`/`>`) as `select()` arguments fail to parse (WGSL's grammar reserves bare `<`/`>` for disambiguating from `vec4<f32>`-style generics) — fixed by hoisting each comparison into its own parenthesized `let`. `WebGPU_AlphaTest3D` CTest (4/4, git-stash-verified): alpha above the reference passes and renders the texture's own colour; alpha below it is genuinely discarded (the background clear colour underneath still shows, not just "always draws"); a stride-24 vertex-colour tint survives a passing test; the indexed-draw counterpart discards too. **Fog added 2026-08-27 (WEBGPU-146).** (Original: no fog, deferred.) |
| WEBGPU-24 | Write `dual_texture3d.wgsl` — two texture samplers, multiply/blend in fragment | ✅ | Verified 2026-07-12: embedded WGSL in `CreateDualTextureResources()`, ported from `VulkanGraphicsBackend`'s `dual_texture3d.{vert,frag}.glsl`/`dual_texture_colored3d.vert.glsl` (`tex1.rgb*=2.0; result=tex1*tex2*tint`, both sampled at the same UV). **Fog added 2026-08-27 (WEBGPU-147).** `DualTextureEffect` has no lighting/alpha test, so group 0 reuses `coloredBindGroupLayout_` unchanged; group 1 is a genuinely NEW shape (`dualTextureBindGroupLayout_`, first WebGPU 3D bind group with 3 entries: one shared sampler + two textures), with its own `dualTexturePipelineLayout_`. One shared shader for stride 20, a separate one for stride 24's vertex-colour tint. `WebGPU_DualTexture3D` CTest (4/4, git-stash-verified): a mid-grey texture0 doubled+clamped through a white texture1 renders white (proves the real `*2.0` boost runs, not a plain multiply); a red texture0 times a green texture1 multiplies to black (proves both textures are genuinely sampled, not just one); stride-24 vertex-colour tint survives dual-texture sampling; the indexed-draw counterpart also multiplies correctly. |
| WEBGPU-25 | Write `env_map3d.wgsl` — cube map sampler + reflection vector from normal | ✅ | Verified 2026-07-18: embedded WGSL in `CreateEnvMapResources()`, ported from `VulkanGraphicsBackend`'s `env_map3d.{vert,frag}.glsl` (cross-checked against `EasyGLGraphicsBackend::EnsureEnvMapped3DProgram()`'s identical GLSL blend formula before porting — both references already agreed field-for-field). Group 0 binding 0 is a new `Transform` UBO (mvp+world, 128 bytes — stands in for Vulkan's push-constant range, WebGPU has none); binding 1 is `EnvMapParams` (240 bytes: eye position, diffuse, emissive+envMapAmount, all 3 directional lights, envMapSpecular+fresnelFactor/fresnelEnabled, fog, plus a CPU-precomputed 3×3 normal matrix — WGSL has no `inverse()`, same reason `CreateLitTexturedResources()`'s own `LitLightParams` precomputes one). Group 1 is a new 3-binding shape (sampler + `texture_2d` + `texture_cube`), mirroring `dualTextureBindGroupLayout_`'s own 3-binding group 1 with the second `texture_2d` swapped for a `texture_cube`. `WebGPU_EnvMap3D` CTest (4/4). |
| WEBGPU-26 | Write `skinned3d.wgsl` — bone palette as uniform array (max 72 mat4); blend 4 weights+indices | ✅ | plan_cnj.md Phase 14J: four shader-module variants (per-pixel-lit/per-vertex-lit × without/with vertex colour, strides 52/56) plus `skinned_pbr3d.wgsl` (stride 68, PBR+skinning combo), ported from `EasyGLGraphicsBackend::EnsureSkinnedProgram()`/`EnsureSkinnedVertexLitProgram()`/`EnsurePbrSkinnedProgram()`. Bone-palette blend matches Task 895 (only the first `WeightsPerVertex` 1/2/4 weight/index pairs summed). `WebGPU_Skinned3D` (9/9) and `WebGPU_SkinnedPbr3D` (5/5) CTests. See `docs/webgpu-renderer.md`'s "SkinnedEffect and SkinnedPbrEffect" section. |
| WEBGPU-27 | Write `instanced3d.wgsl` — per-instance mat4 world transform in second vertex buffer binding | ✅ | Verified 2026-07-18: embedded WGSL in `CreateInstancedResources()`, ported from `VulkanGraphicsBackend`'s `instanced3d.{vert,frag}.glsl` (position-only per-vertex input; a flat, unlit `diffuseColor` fragment output; `[0..15]` of the reused 128-byte Uniforms block is View×Projection, not a full MVP — world comes from the per-instance stream, matching `FillInstancedPushConst()`'s own deliberate choice to ignore the caller's own World matrix entirely rather than combine it). Unlike every bind-group-shaped "new" family (env-map/dual-texture), a second per-instance vertex stream needs **no new `WGPUBindGroupLayout` at all** — WebGPU vertex buffers are set via `wgpuRenderPassEncoderSetVertexBuffer()`, entirely separate from bind groups — so this reuses `coloredBindGroupLayout_`/`coloredPipelineLayout_` unchanged. `WebGPU_Instanced3D` CTest (5/5). |
| WEBGPU-28 | Compile-time validation: embed all WGSL as `constexpr const char*` in `webgpu_shaders.hpp`; validate via `wgpuDeviceCreateShaderModule` at startup | ✅ | **DONE 2026-08-28.** All 20 WGSL sources are extracted byte-for-byte into `webgpu_shaders.hpp` as `inline constexpr char k*[]` (each former inline literal in `WebGPURenderer.cpp` is now a `const char*` alias to the header constant, so the compiled shader bytes are unchanged -- the full pixel suite still passes). A shared `kDirectShaders` registry lists the 18 directly-compiled sources; the two marked templates (`kPbr`/`kSkinnedPbr`, expanded at runtime by `ExpandPbrVertexColourWgslEXT`) are handled by their expanded variants. `WebGPURenderer::ValidateAllShadersEXT()` compiles the WHOLE set (22 modules: 18 + 4 Pbr/SkinnedPbr variants) through the device inside a `WGPUErrorFilter_Validation` error scope and returns the failure count -- so an error in a shader a scene never draws (the lazy mipBlit shader, an unused effect) surfaces as a counted failure rather than only at that effect's first pipeline creation. Reachable at device init via the `CNA_WEBGPU_VALIDATE_SHADERS` env var (off by default) and exercised by the registered `WebGPU_ShaderValidation` test (2/2: zero failures over the whole set + the registry covers all 18 non-template sources). |

---

## Phase 59 — WebGPU backend: render pipeline creation

| #   | Task                                                                                                          | Status | Notes                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------- |
| WEBGPU-29 | `WGPURenderPipelineDescriptor` builder helper: vertex state, primitive state, depth-stencil state, multisample state, fragment state | ✅ | **DONE 2026-08-28.** `WebGPURenderer::Build3DPipelineEXT(const Pipeline3DDescEXT&)` is the one shared `WGPURenderPipelineDescriptor` assembly for all 12 `GetOrCreatePipeline*3D` families: it builds the colour target (`surfaceFormat_` + `CurrentWriteMask()` + MRT via `InitStockColorTargetsEXT`), the `vs_main`/`fs_main` entry points, CCW front face, `ToWGPUCullMode`, the baked blend (`WEBGPU-78`), the renderer-global multisample state (`WEBGPU-58`), the depth-bias scale (`WEBGPU-41`/`79`) and the depth-format + stencil gating (`WEBGPU-39`/`83`), then creates the pipeline. Each caller now passes only its per-family bits (`Pipeline3DDescEXT`: vertex-buffer layout(s), shader module(s), pipeline layout, label) and keeps its own cache key/map. The former block -- character-identical across all 12 (verified field-by-field before extraction) -- is gone: −318 lines net. Behaviour-preserving: the full WebGPU suite is still **104/104** on `:131`, including every 3D/effect/instancing/skinned/pbr pixel test. Earlier steps toward this (`Make3DPipelineKey()`/`FillWGPUBlendState()`/`ToWGPUCullMode()` — `WEBGPU-30`/`41`/`78`/`79`/`82`) already shared the blend/cull/depth-bias logic; this closes the row by factoring the descriptor assembly itself. The SpriteBatch and MipBlit pipelines keep their own builders (they are not 3D families -- SpriteBatch uses a per-snapshot sample count and no depth bias; MipBlit is single-sample colour-only). |
| WEBGPU-30 | Pipeline cache: `std::unordered_map<uint64_t, WGPURenderPipeline>` with MakeKey(topo, depth, blend, cull, stride, wireframe, msaa) | ✅ | Verified 2026-07-17: `Make3DPipelineKey()` (shared anonymous-namespace helper) builds exactly this shape — topology, depthTest/Write/Func, blend enable+real factors (collapsed to fixed bits when disabled, matching `VulkanGraphicsBackend::PackBlendBits()`'s own precedent so different disabled `BlendState`s don't create duplicate pipelines), cullMode, wireframe, depthBias/slopeScaleDepthBias (`std::bit_cast` into the key — wgpu-native has no dynamic depth-bias override, so this is a real, honestly-documented pipeline-permutation cost, not hidden), plus a per-family salt (stride for `AlphaTest3D`/`DualTexture3D`). All 15 pipeline cache maps switched from `unordered_map<int,...>` to `unordered_map<std::uint64_t,...>`. `msaa` is not part of this backend's key — this backend has no MSAA pipeline variant at all yet (unlike Vulkan's own `MakeKey`), a real, narrower scope, not silently pretended otherwise. |
| WEBGPU-31 | `GetOrCreatePipeline2D()` — sprite pipeline (stride=24, Sprite2DVertex layout, no depth) | ✅ | Opaque and premultiplied-alpha variants are runtime-verified: `WEBGPU-126`'s validation scene and `WEBGPU-130`'s independent `../mobile-eggbert` application both render correctly through this pipeline. |
| WEBGPU-32 | `GetOrCreatePipelineColored3D()` — stride=16, VPC layout | ✅ | Verified 2026-07-12: cached by `(topology, depthFunc, depthTest, depthWrite)`, `Float32x3` position + `Unorm8x4` color at stride 16. `WebGPU_Colored3D`'s two depth-order checks (far-then-near and near-then-far both correctly resolve to the near quad, not "last draw wins") prove genuine `WGPUCompareFunction` depth comparison, not just "a pipeline was created." |
| WEBGPU-33 | `GetOrCreatePipelineExt3D()` — stride 20/24/32 dispatch matching Vulkan | ✅ | Strides 20 (`GetOrCreatePipelineTextured3D()`), 24 (`GetOrCreatePipelineColoredTextured3D()`), and 32 (`GetOrCreatePipelineLitTextured3D()`) all done, all cached by `(topology, depthFunc, depthTest, depthWrite)` matching `WEBGPU-32`'s shape, verified 2026-07-12 by `WebGPU_Textured3D`/`WebGPU_ColoredTextured3D`/`WebGPU_LitTextured3D`. |
| WEBGPU-34 | `GetOrCreatePipelineAlphaTest3D()` — alpha discard variant | ✅ | Verified 2026-07-12: cached by `(stride, topology, depthFunc, depthTest, depthWrite)` — stride is part of the key since strides 20/24/32 need distinct `WGPUVertexBufferLayout`s even though 20/32 share the same shader module. See `WEBGPU-23`. |
| WEBGPU-35 | `GetOrCreatePipelineDualTex3D()` — two-texture variant | ✅ | Verified 2026-07-12: cached by `(stride, topology, depthFunc, depthTest, depthWrite)`, same pattern as `WEBGPU-34`. See `WEBGPU-24`. |
| WEBGPU-36 | `GetOrCreatePipelineEnvMap3D()` — cube map variant | ✅ | Verified 2026-07-18: cached by `(topology, depthFunc, depthTest, depthWrite, blend, cullMode, wireframe, depthBias, slopeScaleDepthBias)` via `Make3DPipelineKey()` (no stride salt — this shader is stride-32-only, `VertexPositionNormalTexture`, same layout as `lit_textured3d.wgsl`), same shape as every other `GetOrCreatePipeline*3D()`. See `WEBGPU-25`. |
| WEBGPU-37 | `GetOrCreatePipelineSkinned3D()` — bone palette variant | ✅ | Cached by `(stride, preferVertexLit, topology, depthFunc, depthTest, depthWrite)` — 4 separate cache maps (`skinnedPipelines_`/`skinnedColorPipelines_`/`skinnedVertexLitPipelines_`/`skinnedVertexLitColorPipelines_`), matching `WEBGPU-34`'s own stride-keying precedent. `GetOrCreatePipelineSkinnedPbr3D()` (stride 68) added alongside it. See `WEBGPU-26`. |
| WEBGPU-38 | `GetOrCreatePipelineInstanced3D()` — per-instance binding variant | ✅ | Verified 2026-07-18: two `WGPUVertexBufferLayout`s — binding 0 per-vertex (`WGPUVertexStepMode_Vertex`, only position read regardless of the buffer's real stride, matching Vulkan's own shader), binding 1 per-instance (`WGPUVertexStepMode_Instance`, a mat4 world transform as 4 `Float32x4` columns at locations 4-7). Cached by `(pvStride, instVbStride, topology, depthFunc, depthTest, depthWrite, blend, cullMode, wireframe, depthBias, slopeScaleDepthBias)` — a real, deliberate improvement over `VulkanGraphicsBackend`'s own instanced pipeline, which hardcodes a 64-byte instance-binding stride regardless of the instance vertex buffer's own declared stride (latent, harmless only because every existing caller happens to always use a plain mat4-only instance declaration); this backend's own binding genuinely matches whatever stride the caller's instance buffer declares. See `WEBGPU-27`. |
| WEBGPU-39 | Depth-stencil: `WGPUDepthStencilState` mapping `DepthFormat` + `CompareFunction` + `StencilOperation` | ✅ | **DONE 2026-08-27 — the last (`DepthFormat`) aspect of this row is now closed; the stencil half was already done via `WEBGPU-83`.** `CompareFunction`→`WGPUCompareFunction` (`ToWGPUCompareFunction()`) and `DepthBufferEnable`/`DepthBufferWriteEnable` are runtime-verified via `WebGPURenderer::ApplyDepthStencilState()`; `StencilOperation` bakes into `WGPUStencilFaceState` across every 3D pipeline family with two-sided winding pixel-verified (`WEBGPU-83`, tests `WebGPU_StencilFamily`/`WebGPU_StencilTwoSided` + shared `rendertarget_depthstencil_usage`). **`DepthFormat` is now mapped exactly** by the `MapDepthFormatEXT()` helper — `None`→no attachment, `Depth16`→`Depth16Unorm`, `Depth24`→`Depth24Plus`, `Depth24Stencil8`→`Depth24PlusStencil8` (the same per-value mapping `VulkanRenderer::PickDepthFormat()`/EasyGL/Bgfx already do; the earlier "always `Depth24PlusStencil8`, mirrors Vulkan" note was wrong on both counts and is retracted). The pass's depth attachment, the pipeline `depthStencil` (null for `None`), and the stencil load/store ops (named only on a stencil-carrying format) are all threaded from the target's real format via `replayDepthFormat_`/`replayDepthHasStencil_`, and the format is part of `Make3DPipelineKey`/`SpritePipelineKey` so incompatible passes get their own pipeline. Observably verified by `WebGPU_DepthFormat` (6/6): `None`→no depth test (a farther fragment overwrites), `Depth16`/`Depth24`→depth test works with no stencil op emitted, `Depth24Stencil8`→depth test + a working stamp/gate stencil round trip on the target's own attachment. `WebGPU_SpriteBatch_BlendState` was updated to expect the depth-format-keyed sprite pipeline (a `None` RT and the `Depth24Stencil8` backbuffer now legitimately hold distinct sprite pipelines). |
| WEBGPU-40 | Blend state: `WGPUBlendState` mapping `BlendFunction` + `BlendFactor` (Opaque, AlphaBlend, Additive, NonPremultiplied) | ✅ | Re-audited 2026-08-26. `ToWGPUBlendFactor()` maps all 13 XNA `Blend` enum values, `ToWGPUBlendOperation()` the blend functions, and `FillWGPUBlendState()` builds the full colour+alpha state, keyed into `Make3DPipelineKey()`. Proven on the real RADV GPU (`:0`), all Passed: `WebGPU_SpriteBatch_BlendState`, `WebGPU_AdditiveBlendContract`, `WebGPU_ColorWriteChannels` (the earlier "complete XNA BlendState mapping remains open" was stale). (`WebGPU_Clear_Readback`'s 50%-alpha Check G, which had asserted non-FNA behaviour -- `WEBGPU-141` (B) -- was corrected to use `NonPremultiplied` and passes; it was never a blend-mapping defect, and the dedicated blend tests above confirm the mapping.) |
| WEBGPU-41 | Rasterizer: `WGPUPrimitiveState` mapping `CullMode`, `FillMode` (WireFrame via `topology=LineStrip` fallback or unsupported) | ✅ | Verified 2026-07-17: `CullMode`→`WGPUCullMode` via `ToWGPUCullMode()` (shared anonymous-namespace helper), baked into every one of the 10 3D `GetOrCreatePipeline*()` functions' `pipeline.primitive.cullMode` and folded into each family's pipeline cache key (`Make3DPipelineKey()`, `WEBGPU-30`). `FillMode::WireFrame` is stored (`fillModeWireframe_`) but reaches no pipeline state — wgpu-native has no polygon-mode API at all. Since `WEBGPU-115` (2026-08-04) that gap is a truthful boundary rather than a silent substitution: the capability query reports `false` and a polygon draw that would consume the fill mode throws `System::NotSupportedException` before anything is queued. The "`topology=LineStrip` fallback" this row originally speculated was not implemented, matching Vulkan's own no-fallback precedent. Runtime-verified by `WebGPU_GraphicsState`'s Checks A/B: a hand-derived-winding quad is culled under `CullClockwiseFace` and stays visible under `CullCounterClockwiseFace` (differential pixel-readback proof, not just "a pipeline was created") — the correct `WGPUCullMode_Front`/`Back` pairing was only confirmed by running this test; a first, purely-derived guess from NDC/raster-space reasoning was backwards, matching this project's established "empirically verify, don't just derive" rule for winding subtleties (see `VulkanGraphicsBackend::FillDepthStencilState()`'s own front/back-swap precedent). |

---

## Phase 60 — WebGPU backend: vertex and index buffers

| #   | Task                                                                                                          | Status | Notes                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------- |
| WEBGPU-42 | `WebGPUVertexBufferBackend`: create `WGPUBuffer` (vertex, size=capacity×stride) with `COPY_DST` usage | ✅ | Lazily sized VERTEX\|COPY_DST buffer with capacity validation. Runtime-validated 2026-08-26 on the real RADV GPU (`:0`): the whole pixel-asserted 3D suite (`WebGPU_Colored3D`/`Textured3D`/`ColoredTextured3D`/`LitTextured3D`/`AlphaTest3D`/`DualTexture3D`/`Instanced3D`, all Passed) uploads its geometry through this buffer and draws from it. |
| WEBGPU-43 | `SetData()`: upload via `wgpuQueueWriteBuffer(queue, buffer, 0, data, byteSize)` | ✅ | `wgpuQueueWriteBuffer` upload. Runtime-validated 2026-08-26 (`:0`): every vertex in the pixel-asserted 3D suite reaches the GPU through this path (all 3D-suite CTests Passed). |
| WEBGPU-44 | `SetDataWithOptions()`: `Discard` = reallocate buffer; `NoOverwrite` = `wgpuQueueWriteBuffer` at offset | ✅ | Done 2026-08-26 -- but the honest resolution is NOT the row's original sketch, because that sketch does not fit WebGPU's model. `Discard` exists so an immediate-mode renderer can orphan a buffer the GPU is still reading; `NoOverwrite`-at-offset needs an offset this API has no parameter for. Neither hazard reaches this renderer: **every draw snapshots its own vertex/index bytes into its `ColoredDrawCommand`/`InstancedDrawCommand` at queue time** (`QueueColoredDraw`/`DrawInstancedPrimitivesEx` copy `ShadowData()`) and uploads that snapshot into a fresh per-draw `WGPUBuffer` at replay -- so two draws issued between two `SetData` calls already hold independent copies, a stronger guarantee than orphaning -- and `wgpuQueueWriteBuffer` is a non-stalling queue-ordered copy, so there is no stall for `Discard` to avoid. All three options therefore produce identical, correct results (mirrors `VulkanRenderer`, which also does not branch buffer uploads on the option). Documented at both `SetDataWithOptions` sites in code. New test `WebGPU_SetDataOptions` (`webgpu_setdataoptions_test.cpp`), 4/4 on the real RADV GPU (`:0`): None/Discard/NoOverwrite each upload+draw the right colour, and a `Discard` re-upload issued between two already-queued draws leaves the first draw's snapshot intact (Check D -- the exact immediate-mode hazard, proven safe here). |
| WEBGPU-45 | `WebGPUIndexBufferBackend`: 16-bit and 32-bit index buffers via `WGPUIndexFormat` | ✅ | 16- and 32-bit index buffers. Runtime-validated 2026-08-26 (`:0`): the indexed 3D-suite draws dispatch through these (all Passed). |
| WEBGPU-46 | `SetData16()` / `SetData32()`: `wgpuQueueWriteBuffer` | ✅ | Both index-upload paths. Runtime-validated 2026-08-26 (`:0`) by the indexed 3D-suite CTests (all Passed). |
| WEBGPU-47 | Disposed guard in all SetData methods (throw `ObjectDisposedException`) | ✅ | Done 2026-08-26. The guard lives at the XNA GraphicsResource layer -- `VertexBuffer.cpp`/`IndexBuffer.cpp` check `getIsDisposedProperty()` and throw `System::ObjectDisposedException` before any renderer call (so it is renderer-agnostic, exactly as every other backend relies on; `VulkanRenderer` has no renderer-local guard either). New test `WebGPU_DisposedGuard` (`webgpu_disposed_guard_test.cpp`) proves the WebGPU device path leaves it intact: on the real RADV GPU (`:0`) a live `VertexBuffer`/`IndexBuffer.SetData` succeeds (Checks A/C) while the same call after `Dispose()` throws `ObjectDisposedException` specifically (Checks B/D) -- 4/4 Passed. |
| WEBGPU-48 | `SetVertexBuffer(wgpuRenderPassSetVertexBuffer)` + `SetIndexBuffer(wgpuRenderPassSetIndexBuffer)` in draw dispatch | ✅ | `wgpuRenderPassEncoderSetVertexBuffer`/`SetIndexBuffer` are bound on every draw route. Verified 2026-08-26 (`:0`): the 3D-suite CTests (indexed and non-indexed) all Passed, so both bindings are exercised end to end. |

---

## Phase 61 — WebGPU backend: textures

| #   | Task                                                                                                          | Status | Notes                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------- |
| WEBGPU-49 | `WebGPUTextureBackend`: `WGPUTexture` (2D, RGBA8Unorm, COPY_DST + TEXTURE_BINDING) + `WGPUTextureView` | ✅ | RGBA8Unorm Texture2D + view are runtime-verified: `WEBGPU-126` (`player.png`) and `WEBGPU-130` (`../mobile-eggbert`'s own UI/background textures) both sample correctly with no WebGPU validation errors. |
| WEBGPU-50 | `SetData()`: `wgpuQueueWriteTexture()` with `WGPUImageCopyTexture` + `WGPUTextureDataLayout` | ✅ | Level-upload is runtime-verified by the same two texture uploads above. |
| WEBGPU-51 | `Texture2D::GetData()`: staged MAP_READ copy with aligned rows and asynchronous map/poll completion | ✅ | Implemented 2026-07-18: `WebGPUTextureBackend::GetData()` reuses the exact staged MAP_READ-buffer/aligned-row/async-map-and-poll technique `WEBGPU-91`'s `ReadBackbuffer()`/`WebGPURenderTargetBackend::GetData()` already established — copies the whole requested mip level, then extracts the `x,y,w,h` sub-rectangle from the mapped memory (always `RGBA8Unorm`, so no BGRA swap needed, unlike the swapchain). Required adding `WGPUTextureUsage_CopySrc` to `WebGPUTextureBackend`'s texture descriptor (missing before this, since nothing previously read a plain `Texture2D` back). Note: `Texture2D::GetData()` at the XNA layer is itself CPU-shadow-based (`cpuPixels_`) and only calls through to this method for a `RenderTarget2D`-backed instance (which already uses the separate `WebGPURenderTargetBackend`) — so this was tested directly against `IGraphicsBackend`/`ITextureBackend` (matching `examples/webgpu_instanced3d_test.cpp`'s established convention for a backend capability with no XNA-layer round trip of its own), not through `Texture2D` itself. `examples/webgpu_texture2d_getdata_test.cpp` (`WebGPU_Texture2D_GetData` CTest), 3/3: full-texture round trip (exact byte match against an uploaded gradient), a non-origin sub-rectangle read, and a mip level 1 round trip distinct from level 0. |
| WEBGPU-52 | Mip levels: generate via `wgpuCommandEncoderCopyTextureToTexture` per level or leave as mip=1 (document) | ✅ | 2026-07-18 (mips/cube/compressed-textures cluster): real, genuinely-LINEAR-filtered mip generation, closing this row for both `Texture2D` (`WebGPUTextureBackend`) and `TextureCube` (`WebGPUTextureCubeBackend`). Investigated first, per the task brief: confirmed against this project's pinned wgpu-native v29.0.1.1 `webgpu.h` that `wgpuCommandEncoderCopyTextureToTexture` (this row's own originally-suggested technique) is a raw same-size copy — there is no vkCmdBlitImage-equivalent filtered-downsample primitive in wgpu-native at all, so the row's own alternative technique had to be used instead: a NEW `WebGPURenderer::GenerateMipsForLayer()` (thin wrappers `GenerateMips2D()`/`GenerateMipsCubeFace()`) renders one pass per mip level, each drawing a full-screen triangle (the standard 3-vertex, no-vertex-buffer `@builtin(vertex_index)` trick) that samples the PREVIOUS level through a real `WGPUFilterMode_Linear` `WGPUSampler` into the NEXT level's own single-mip render-attachment view — a genuine filtered downsample, not a nearest-neighbor copy or a raw same-size `CopyTextureToTexture`. **IMPORTANT, DELIBERATE, DOCUMENTED DIVERGENCE from FNA and every sibling CNA backend** (found during investigation, not silently introduced): `VulkanGraphicsBackend`/`EasyGLGraphicsBackend` only ever regenerate mip content for a RENDER TARGET being unbound (`vkCmdBlitImage`/`glGenerateMipmap`-on-unbind, matching real FNA3D `OPENGL_ResolveTarget` semantics) — neither backend, nor real FNA/XNA itself, auto-generates mip content for a PLAIN `Texture2D`/`TextureCube` at all (mip levels beyond 0 are always user/content-pipeline-supplied via explicit per-level `SetData()`, matching how a real XNA content-pipeline-processed `.xnb` already carries every level pre-baked). This WebGPU backend's `GenerateMips2D()`/`GenerateMipsCubeFace()` DOES generate real content automatically instead, triggered whenever content is written to level 0 (`WebGPUTextureBackend::UpdatePixels()` — covers both the constructor's initial upload AND any later `Texture2D::SetData(level=0,...)` call — and `WebGPUTextureCubeBackend::SetData(face, level=0, ...)`, per-face). This makes WebGPU's plain-texture mip behaviour strictly better (never garbage/undefined content at level>0) but genuinely DIFFERENT from FNA and every other CNA backend for the identical public `Texture2D`/`TextureCube(mipMap=true)` constructor call — explicit per-level `SetData(level>0,...)` calls made AFTER a level-0 write are, as before, not retroactively protected (a subsequent level-0 write will regenerate over them). `RenderTarget2D`/`RenderTargetCube` mip regeneration (the FNA-equivalent feature, matching Vulkan/EasyGL's own render-target-only precedent) remains its own separate, already-tracked scope cut (`WEBGPU-53`/`54`/`114`'s own rows), NOT closed by this row. `examples/webgpu_mipgen_test.cpp` (`WebGPU_MipGen` CTest), 9/9: a hard-edged red/blue vertical stripe (deliberately NOT power-of-2-aligned with the 2:1 downsample's texel pairing, so a straddling destination pixel actually exists) proves a genuine ~50/50 LINEAR blend `(128,0,128)` at the boundary — not a hard nearest-neighbor edge — for both `Texture2D` level 1 and one `TextureCube` face's level 1; a chain-downsampled level 2 (sourced from level 1, not level 0) has real, plausible, non-garbage content; and `mipMap=false` construction with non-empty pixel data does not crash (the `mipLevels<=1` no-op guard). Full WebGPU CTest suite (23 tests) still passes with no regressions. |
| WEBGPU-53 | `WebGPURenderTargetBackend`: `WGPUTexture` (RENDER_ATTACHMENT + TEXTURE_BINDING) + depth texture | ✅ | Runtime-verified 2026-07-18 (`WebGPU_RenderTarget2D`, 8/8). Colour texture always uses the swapchain's own chosen format (`surfaceFormat_`) rather than `RGBA8Unorm` so every existing `GetOrCreatePipeline*3D()` (each hardcodes `target.format = surfaceFormat_`) renders into it unchanged, with zero new pipeline-cache dimensions; the depth texture is created in the exact format the requested `DepthFormat` maps to (`WEBGPU-39`, `MapDepthFormatEXT()`: `None`→no depth texture, `Depth16`→`Depth16Unorm`, `Depth24`→`Depth24Plus`, `Depth24Stencil8`→`Depth24PlusStencil8`), matching what `VulkanRenderer::PickDepthFormat()`/EasyGL/Bgfx do; the pipeline's `depthStencil` state and the pass attachment are threaded from that real format (null/absent for `None`). Real per-instance `GetData()` readback (own `WGPUBuffer` MAP_READ + `wgpuCommandEncoderCopyTextureToBuffer`, mirroring `ReadBackbuffer()`'s pattern). Deliberately deferred, with a clear thrown error rather than a silent under-delivery: `mipMap=true` (mip-chain regeneration, `WebGPU_RenderTarget2D` Check G). `multiSampleCount>0` (MSAA) was attempted 2026-07-18 in `WEBGPU-58`: `WebGPURenderTargetBackend::GetMultiSampleCount()` is now overridden and unconditionally mirrors the backend's own global sample count (not its own constructor's requested value) once MSAA is engaged on the backbuffer — `WebGPU_RenderTarget2D` Check F still sees 0 in that test specifically because that test's `GraphicsDeviceManager` never enables `PreferMultiSampling`; see `WEBGPU-58`'s own row for the honest, still-open status of whether the resulting multisampled render actually blends correctly (it does not, yet). Also introduced `IWebGPUSamplable` (mirrors `VulkanGraphicsBackend`'s `IVulkanSamplable`) so a `RenderTarget2D` sampled back as an ordinary texture (`SpriteBatch`/`BasicEffect.Texture`) resolves safely via `dynamic_cast` everywhere a `GpuDrawParams::textureN` is consumed, replacing ~16 previously-unchecked `static_cast<const WebGPUTextureBackend*>` call sites that would have been undefined behaviour the first time any of them actually received a `WebGPURenderTargetBackend*` (verified safe by the full existing `WebGPU_*3D` suite, 15/15, plus `WebGPU_RenderTarget2D` Check D). |
| WEBGPU-54 | `SetRenderTarget(rt)` / `SetRenderTarget(nullptr)`: switch render pass target between RT and swapchain view | ✅ | Runtime-verified 2026-07-18 (`WebGPU_RenderTarget2D`, 8/8, esp. Check E). `SetRenderTarget2D()` eagerly flushes/renders whatever is queued for the OUTGOING target the instant the target actually changes (closing out that render pass immediately and starting fresh accumulation for the new target) rather than tagging every queued draw command with a target and replaying grouped-by-target at final-flush time (`VulkanGraphicsBackend`'s own deferred/replay model) — chosen as the smaller change for a backend with no pre-existing per-draw deferred/replay infrastructure to extend; see `SetRenderTarget2D()`'s own comment for the full design discussion. `WebGPU_RenderTarget2D` Check E is the critical architecture proof: a `RenderTarget2D`-targeted `Clear()` sandwiched between two backbuffer `Clear()`/readback calls does NOT leak into the backbuffer's own render pass. `SetRenderTargets(vector<...>)` (MRT, `WEBGPU-85`/`86`/`87`) is explicitly out of scope; `IGraphicsBackend`'s own default already forwards a single-element call to `SetRenderTarget2D()` unchanged. |
| WEBGPU-55 | `GetBackBufferData()`: readback via MAP_READ buffer + `wgpuCommandEncoderCopyTextureToBuffer` | ✅ | This *is* `WEBGPU-91`'s implementation (`WebGPURenderer::ReadBackbuffer()`/`CaptureReadback()`) — verified 2026-07-12 via `WebGPU_Clear_Readback`, see `WEBGPU-91`'s own row for the full detail. |
| WEBGPU-56 | `WebGPUTextureCubeBackend`: `WGPUTexture` (dimension=2D, arrayLayerCount=6, CUBE_COMPATIBLE) | ✅ | Verified 2026-07-18: a minimal, read-only-after-upload cube-map texture backend exists (`WebGPUTextureCubeBackend`), just enough for `EnvironmentMapEffect.EnvironmentMap` (see `WEBGPU-25`/`74`) — `WGPUTexture` (`WGPUTextureDimension_2D`, `depthOrArrayLayers=6`; wgpu-native/WebGPU has no distinct `CUBE_COMPATIBLE` usage flag the way Vulkan/D3D need one, the *view* alone selects `WGPUTextureViewDimension_Cube`) + `SetData()` per face/level via `wgpuQueueWriteTexture` with `origin.z=face`, mip-level pre-allocation mirroring `EasyGLTextureCubeBackend::CalculateCubeMipLevels()`. 2026-07-18 (same day, follow-up task): real `GetData()` added — see `WEBGPU-113`'s own row for the full detail. 2026-07-18 (follow-up task, mips/cube/compressed-textures cluster): the two remaining gaps both closed — real automatic mip-chain regeneration from level 0 (see `WEBGPU-52`'s own row) and `RenderTargetCube` (a separate sibling class, `WebGPURenderTargetCubeBackend` — see `WEBGPU-114`'s own row). Now ✅. |
| WEBGPU-57 | `WebGPUTexture3DBackend`: `WGPUTexture` (dimension=3D) | ✅ | Implemented 2026-07-18: `WebGPUTexture3DBackend`/`WebGPURenderer::CreateTexture3D()` — this backend's first `Texture3D` (volume texture) support (previously `IGraphicsBackend`'s own `nullptr`-returning default, so `Texture3D::backend_` was always null and every `SetData`/`GetData` call silently no-op'd). `WGPUTextureDimension_3D`, `size.depthOrArrayLayers=depth`; `SetData()`/`GetData()` use a single `wgpuQueueWriteTexture`/`wgpuCommandEncoderCopyTextureToBuffer` call each covering the full 3D sub-volume (origin+extent), not a per-depth-slice loop — wgpu-native's `WGPUTexelCopyBufferLayout::rowsPerImage` already describes a 3D buffer layout directly. Mip level COUNT mirrors `Texture3D.cpp`'s own width/height-only `CalculateMipLevels()` (depth does not participate in the count, matching FNA); wgpu-native still halves the actual per-level depth extent automatically like every other 3D-texture mip rule. `Texture3D`'s XNA-layer `SetData`/`GetData` have no CPU-side shadow at all (unlike `Texture2D`) and always call straight through to `ITexture3DBackend`, so this is a genuine end-to-end XNA-layer test. `examples/webgpu_texture3d_test.cpp` (`WebGPU_Texture3D` CTest), 3/3: a 2x2x4 volume with each of the 4 depth slices given its own distinct solid colour, full-volume + single-voxel (non-origin) readback, and a mip level 1 round trip distinct from level 0. |
| WEBGPU-58 | MSAA: `WGPUTexture` with `sampleCount=4`; resolve in render pass via `resolveTarget` | ✅ | Attempted 2026-07-18, resolved 2026-07-18. **Infrastructure implemented and verified (`WebGPU_Msaa`, now 6/6 PASS):** MSAA is a single BACKEND-GLOBAL `sampleCount_` (not a pipeline-cache-key dimension), mirroring `VulkanGraphicsBackend`'s own precedent — every one of the ~10 `GetOrCreatePipeline*3D()` families plus both SpriteBatch pipelines read the same `sampleCount_` for `WGPUMultisampleState.count`. `ApplyMultiSampleCount()` invalidates and lazily rebuilds every pipeline cache (`ClearAllPipelineCaches()`, see below) and recreates the backbuffer's depth texture and a new multisampled colour texture (`RecreateMsaaColorTexture()`) at the new sample count. `PickSampleCount()`/`Supports4xMsaa()` empirically probe real device capability via `wgpuDevicePushErrorScope`/a scratch 4-sample texture create/`wgpuDevicePopErrorScope` (never assumed) — on this machine/adapter the probe genuinely found exactly 4x supported, nothing higher (wgpu-native itself only defines sample counts 1 and 4, unlike Vulkan's 1/2/4/8/16/32/64 range), and `ApplyMultiSampleCount(8)`'s clamped-return-value contract is verified to return 4, never the raw unsupported request (Check C). `WebGPURenderTargetBackend` unconditionally mirrors the backend's global `sampleCount_` at construction time with its own multisampled colour+depth textures and a real single-sample resolve target, verified to report the SAME real applied value as the backbuffer regardless of its own constructor's requested count (Check D, 1 of 2). Backbuffer's `WGPURenderPassColorAttachment.resolveTarget` is the swapchain's own current texture (wgpu-native auto-resolves at render-pass end, no explicit resolve command) when `sampleCount_ > 1`. `ClearAllPipelineCaches()` fully tears down and recreates every 3D/sprite shader module, bind group layout, pipeline layout AND pipeline cache on every sample-count change (not merely the `WGPURenderPipeline` objects) — a real, independently-defensible fix from the initial 2026-07-18 investigation, kept. **ACTUAL ROOT CAUSE of the previously-reported "Checks B/D-2/E FAIL, genuine multisample rendering does not work end-to-end" symptom (found 2026-07-18, same day, by a follow-up investigation): it was never an MSAA bug at all — it was a test-authoring defect in `examples/webgpu_msaa_test.cpp` itself.** `BasicEffect.Apply()` leaves whatever `RasterizerState` the device already has (the XNA default, `CullCounterClockwiseFace`), and this test's diagonal triangle — unlike every other WebGPU 3D test in this suite (`webgpu_colored3d_test.cpp`, `webgpu_graphicsstate_test.cpp`, etc., all of which explicitly set `RasterizerState::CullNone` before drawing) — never overrode that default. The triangle's winding is a genuine XNA **back face** under `WebGPURenderer::ToWGPUCullMode()`'s mapping, which is itself correct and independently verified by `WebGPU_GraphicsState`'s own non-vacuous, real differential cull-mode checks (Check A/B there: a hand-derived-winding quad is culled under `CullClockwiseFace` and stays visible under `CullCounterClockwiseFace` — both directions genuinely exercised, not vacuous). So the triangle was being legitimately backface-culled on **every** sample count, not just under MSAA — completely unrelated to `sampleCount_`, `resolveTarget`, pipeline caching, or any other MSAA-specific mechanism. Check A's own assertion (`IsBinary()`, which only rejects intermediate/antialiased pixel values) could not detect this: an entirely-background row (nothing drawn at all) is trivially "binary", so Check A was passing **vacuously** the whole time — this is exactly why the original investigation's extensive backend-side elimination (surface/texture format, depth-clear timing, storeOp, viewport/scissor, cull mode *value* though not the missing override, blend state, resource freshness, error-scope state) found nothing: there was nothing wrong to find there. Confirmed via direct experiment: forcing `cullMode=None` for the exact same draw (no other change) immediately produced all 6/6 PASS with genuine, non-vacuous blended pixel values; adding an explicit `device.setRasterizerStateProperty(RasterizerState::CullNone)` call (matching the established pattern) in both `RenderBackbufferRow()` and `RenderRTRow()` in `examples/webgpu_msaa_test.cpp` is the actual, minimal, test-only fix — `modules/renderers/webgpu/src/WebGPURenderer.cpp` is untouched by this fix, confirming the production MSAA implementation was already correct. `CreateRenderTarget2D()`'s per-instance `multiSampleCount` parameter is still intentionally ignored (RT always mirrors the backend's global state) — an unrelated, still-open design note, not a bug. Full suite re-verified: `ctest -L WebGPU`, 16/16 PASS (15 pre-existing + `WebGPU_Msaa`). |

---

## Phase 62 — WebGPU backend: 2D rendering (SpriteBatch)

| #   | Task                                                                                                          | Status | Notes                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------- |
| WEBGPU-59 | `WebGPUSpriteBatchBackend`: dynamic vertex buffer ring (3 frames) for sprite quads | ✅ | **DONE 2026-08-28.** The SpriteBatch dynamic vertex buffer is now a THREE-slot ring (`spriteVertexRing_[3]`): each bind cycle's `UploadSpriteVertices()` rotates to the next slot before writing, so the buffer written this cycle is never the one the previous two submissions are still reading; each slot still grows in place when a cycle needs more than it holds. (Uploads use `wgpuQueueWriteBuffer`, which the queue already serialises, so the ring is defensive/parity with the reference renderers rather than fixing a live stall.) `spriteVertexBuffer_` is a non-owning alias of the active slot so the write/bind sites are unchanged; the ring owns and releases the three buffers. Exercised by `WebGPU_BufferPoolStress` (many frames of sprites rotate the ring) and the full existing SpriteBatch suite. |
| WEBGPU-60 | Upload sprite quads via `wgpuQueueWriteBuffer` per batch | ✅ | Runtime-verified by every `WEBGPU-126`/`130` frame (flattened sprite upload → draw, no validation errors). |
| WEBGPU-61 | Per-batch draw: set pipeline, bind groups (UBO + texture), vertex buffer, draw | ✅ | Runtime-verified the same way — `WEBGPU-130`'s `../mobile-eggbert` run alone issues many real per-frame SpriteBatch draws (title, UI panels, animated character, progress bar, fade overlay) with no WebGPU validation errors. |
| WEBGPU-63 | Verify SpriteBatch sort modes: Immediate, Deferred, Texture, FrontToBack, BackToFront. | ✅ | Done 2026-08-26. New test `WebGPU_SpriteBatch_SortMode` (`webgpu_spritebatch_sortmode_test.cpp`), 6/6 on the real RADV GPU (`:0`): two overlapping solid sprites (red@layerDepth 0.2, blue@0.8) into a `RenderTarget2D`, reading which colour lands on top. Each depth-sorted mode is submitted in the OPPOSITE order to its sorted result, so a pass can only happen if the shared-layer sort actually reaches the WebGPU draw submission -- `BackToFront` -> red on top, `FrontToBack` -> blue on top (both overriding submission order); `Deferred` proven order-based both ways; `Immediate` in submission order; `Texture` (one texture group) order-preserving. As the row intended, the sort itself is shared `SpriteBatch` behaviour and is not reimplemented -- this validates the WebGPU submission path preserves it. |

---

## Phase 63 — WebGPU backend: 3D draw dispatch

| #   | Task                                                                                                          | Status | Notes                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------- |
| WEBGPU-64 | `DrawPrimitives()`: bind colored3d pipeline + UBO + vertex buffer + `wgpuRenderPassEncoderDraw` | ✅ | No literal `DrawPrimitives()` method exists on `IGraphicsBackend` (checked 2026-07-12) — this row's described capability *is* `DrawColoredPrimitives()`, implemented and verified (`WEBGPU-91`'s vertical slice, see `WEBGPU-32`). `WebGPU_Colored3D`'s Check A (`DrawUserPrimitives`, non-indexed) exercises exactly this path. |
| WEBGPU-65 | `DrawIndexedPrimitives()`: bind index buffer + `wgpuRenderPassEncoderDrawIndexed` | ✅ | Same clarification as `WEBGPU-64` — this is `DrawIndexedColoredPrimitives()`. `WebGPU_Colored3D`'s Check B (`DrawUserIndexedPrimitives`, 4 verts + 6 indices) verifies it specifically, not just the non-indexed path. |
| WEBGPU-66 | `DrawPrimitivesEx()`: dispatch by `GpuDrawParams` (stride, textureEnabled, lightingEnabled, dualTexture, skinned, instanced) | ✅ | Verified 2026-07-12 for stride 16 (`colored3d.wgsl`), stride 20 (`textured3d.wgsl`, requires `params.texture0 != nullptr`), stride 24 (`colored_textured3d.wgsl`, same texture requirement), stride 32 (`lit_textured3d.wgsl`, same texture requirement, real Blinn-Phong lighting when `LightingEnabled`, transparent unlit fallback otherwise), `AlphaTestEffect` on strides 20/24/32 (`alpha_test3d.wgsl`/`alpha_test_colored3d`, priority over the other stride dispatches — see `WEBGPU-72`), `DualTextureEffect` on strides 20/24 (`dual_texture3d.wgsl`/`dual_texture_colored3d`, priority below alpha test but above env map/skinned — see `WEBGPU-73`), `PbrEffect` on stride 48 (`pbr3d.wgsl`), and — plan_cnj.md Phase 14J — `SkinnedPbrEffect` on stride 68 and `SkinnedEffect` on strides 52/56 (`skinned_pbr3d.wgsl`/`skinned3d.wgsl`, priority order: pbr&&skinned combo first, then pbr-only, then skinned-only, matching `EasyGLGraphicsBackend::SelectProgram()`'s own precedence): all forward the caller's real `GpuDrawParams` via `FillExtUniforms()`/`FillLitLightUniforms()`/`FillAlphaTestUniforms()`/`FillSkinningParams()` (mirrors Vulkan's push-constant fillers field-for-field), instead of the `DrawColoredPrimitives` fallback's hardcoded white/true/no-texture. `WebGPU_DrawPrimitivesEx`/`WebGPU_Textured3D`/`WebGPU_ColoredTextured3D`/`WebGPU_LitTextured3D`/`WebGPU_AlphaTest3D`/`WebGPU_DualTexture3D`/`WebGPU_Pbr3D`/`WebGPU_Skinned3D`/`WebGPU_SkinnedPbr3D` CTests prove this concretely. Re-audited 2026-08-26: the earlier "Only `EnvironmentMapEffect`/instancing remain open" is now stale -- both shipped and Passed on the real RADV GPU (`:0`): `EnvironmentMapEffect` via `WebGPU_EnvMap3D` (+ `WebGPU_EnvMapEmissive`/`WebGPU_EnvironmentMapEffect_Fog`) and instancing via `WebGPU_Instanced3D` (+ `WebGPU_InstancedOffsetFrequency_Cardinality`/`WebGPU_InstancedVertexColor_Cardinality`). All GpuDrawParams dispatch families are now verified. |
| WEBGPU-67 | `DrawUserPrimitives()`: transient `WGPUBuffer` (COPY_DST + VERTEX, mappedAtCreation=false); upload + draw + release | ✅ | `RenderColoredDraws()` creates a transient `WGPUBuffer` (`Vertex\|CopyDst`) per draw, `wgpuQueueWriteBuffer`s the CPU shadow-copy into it, draws, and releases it after the frame's command buffer is submitted (not `mappedAtCreation` — uses the same queue-write pattern as every other buffer upload in this backend, an equally valid approach). |
| WEBGPU-68 | `DrawInstancedPrimitivesEx()`: second vertex buffer binding (per-instance mat4 world transforms) | ✅ | Verified 2026-07-18: real override (previously `IGraphicsBackend`'s own default, which throws). `params.instanceVb == nullptr` falls back to a real `DrawIndexedPrimitivesEx()` draw, matching `VulkanGraphicsBackend`'s own identical fallback. `WebGPU_Instanced3D` CTest (5/5): 3 instances in ONE draw call each paint their own small quad at their own independently-predicted screen location with the shared `DiffuseColor` (proves the per-instance buffer is genuinely read per-instance, not e.g. always instance 0), a region far from all 3 quads stays untouched, and the `instanceVb==nullptr` fallback renders correctly rather than throwing. See `WEBGPU-27`/`38`. |
| WEBGPU-69 | PrimitiveType mapping: TriangleList→`WGPUPrimitiveTopology_TriangleList`, TriangleStrip→Strip, LineList→LineList, LineStrip→LineStrip, PointList→PointList | ✅ | All 5 `PrimitiveType` values map via the existing `ToTopology()` (unchanged); now genuinely exercised by real 3D draw dispatch (`WEBGPU-64`/`65`), not just present in source. `WebGPU_Colored3D` only exercises `TriangleList` directly, but the pipeline cache is keyed by topology so the other 4 follow the same, now-proven code path. |
| WEBGPU-70 | `vertexStart` / `startIndex` / `baseVertex` support in draw calls | ✅ | **Done 2026-08-27: the reachable routes are now directly tested, and the one unapplied case is unreachable-by-design (option "remove the unreachable requirement").** New test `WebGPU_VertexStart` (`webgpu_vertexstart_test.cpp`), 5/5 on the real RADV GPU (`:131`): two identically-placed triangles (red at vertices 0..2, blue at 3..5) prove `DrawPrimitives(vertexStart=0/3)` selects red/blue (the `vertexStart*stride` byte offset), and `DrawIndexedPrimitives` with `baseVertex=0/3` and `startIndex=0/3` each shift to the correct triangle (`baseVertex` added to every decoded index, `startIndex` the first index element) — a wrong/ignored offset flips the sampled colour. The only route that ignores `vertexStart` is `DrawInstancedPrimitivesEx`, and that is **unreachable through any XNA API** (`DrawInstancedPrimitives` has no `vertexStart` parameter; `GpuDrawParams::vertexStart` is populated only by the ordinary paths), so there is nothing to fix or test there — the requirement is removed for the instanced route rather than force-fixed on untestable code. Historical note follows. `DrawColoredPrimitives`/`DrawIndexedColoredPrimitives` always draw from vertex/index 0 (`wgpuRenderPassEncoderDraw(pass, vertexCount, 1, 0, 0)` / `DrawIndexed(..., 0, 0, 0)`) — matches every real call site today (`GraphicsDevice.cpp`'s two `DrawColoredPrimitives`/`DrawIndexedColoredPrimitives` callers always build a dedicated, zero-based temporary buffer per call), but a genuine sub-range draw would silently ignore a non-zero start/base. Real gap, not yet needed by any current caller. **Historical note (2026-08-26 audit, when this row was 🟨): the gap is UNREACHABLE through any XNA API, so it stayed 🟨 and was not force-fixed (touching working instanced-replay code for an untestable path is the wrong trade):** XNA's only instanced entry point, `GraphicsDevice::DrawInstancedPrimitives(primitiveType, baseVertex, minVertexIndex, numVertices, startIndex, primitiveCount, instanceCount)`, has no `vertexStart` parameter at all -- it controls the geometry stream with `baseVertex` (added to every decoded index) and `startIndex` (first index), both of which this renderer's `DrawInstancedPrimitivesEx` already applies (`command.baseVertex = params.baseVertex + perVertexOffset`, `command.firstIndex = params.startIndex`). `GpuDrawParams::vertexStart` is populated only by the ordinary `DrawPrimitives`/`DrawIndexedPrimitives` paths, never for an instanced draw. So the only way to reach the unapplied-`vertexStart` branch is a hand-built `GpuDrawParams` passed straight to the renderer interface -- no game and no XNA-layer path can, and there is therefore no API-level test that would exercise it. |

---

## Phase 64 — WebGPU backend: Effects

| #   | Task                                                                                                          | Status | Notes                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------- |
| WEBGPU-71 | `WebGPUEffectBackend`: `BasicEffect` wires to `FillGpuDrawParams` → UBO upload | ✅ | Corrected 2026-07-18 (stale bookkeeping, no new code needed): this row's literal `WebGPUEffectBackend` class was never built — matching `VulkanGraphicsBackend`'s own architecture, there is no separate effect-backend class on this backend either. `BasicEffect::FillGpuDrawParams()` → `WebGPURenderer::DrawPrimitivesEx()`/`DrawIndexedPrimitivesEx()` → `FillExtUniforms()`/`FillLitLightUniforms()` → real UBO upload is the actual, already-real wiring, and has been since `WEBGPU-66` (`🟨`, stride 16/20/24/32 real dispatch) — confirmed via the passing `WebGPU_Colored3D`/`WebGPU_Textured3D`/`WebGPU_ColoredTextured3D`/`WebGPU_LitTextured3D`/`WebGPU_BasicEffect_PreferPerPixelLighting` CTests, all of which exercise real `BasicEffect` draws end-to-end with real (non-hardcoded) `DiffuseColor`/lighting/texture parameters. |
| WEBGPU-72 | `AlphaTestEffect`: UBO alpha test params (function, reference) | ✅ | Verified 2026-07-12 — see `WEBGPU-23`. `DrawPrimitivesEx()`/`DrawIndexedPrimitivesEx()` now check `needsAlphaTest` (`params.alphaTest[2]<0 \|\| params.alphaTest[3]<0`) with priority over stride dispatch, matching `VulkanGraphicsBackend`'s own precedence (alpha test wins over lit-textured for stride 32). |
| WEBGPU-73 | `DualTextureEffect`: second texture bind group | ✅ | Verified 2026-07-12 — see `WEBGPU-24`. `DrawPrimitivesEx()`/`DrawIndexedPrimitivesEx()` now check `needsDualTexture` (`params.dualTexture`, priority below alpha test but above env map/skinned/lit-textured, matching `VulkanGraphicsBackend`'s own precedence). |
| WEBGPU-74 | `EnvironmentMapEffect`: cube map bind group + reflection UBO params | ✅ | Verified 2026-07-18: `DrawPrimitivesEx()`/`DrawIndexedPrimitivesEx()` gained a `needsEnvMap` check (`params.envMapping`, stride 32, priority below alpha test/dual texture, above lit-textured — matching `VulkanGraphicsBackend`'s own precedence), routing to `QueueEnvMapDraw()`. Both `EnvironmentMapEffect::Texture` and `::EnvironmentMap` are genuinely optional (unlike every other stride-32+ effect family's `texture0 != nullptr` dispatch gate) — a null texture/cube map falls back to a 1×1 white default at render time (`EnsureEnvMapDefaultTextures()`), matching `VulkanGraphicsBackend`'s own `defaultWhiteView_`/`defaultWhiteCubeView_` fallback. `WebGPU_EnvMap3D` CTest (4/4): hand-derived geometry (`View`=`World`=Identity, quad at z=0.5, `Normal`=(0,0,-1)) makes the reflection vector land exactly on `CubeMapFace::NegativeZ` — proven by painting each of the 6 faces a distinct solid colour and asserting the correct one appears; a differential Fresnel check (same scene, only `FresnelFactor` differs) proves the Fresnel edge-weighting term genuinely gates the blend, not just present-but-inert; `EnvironmentMapAmount=0` proves the amount independently gates the blend; the indexed-draw path is also exercised. See `docs/webgpu-renderer.md` for a fuller writeup if that doc is updated alongside this task. |
| WEBGPU-75 | `SkinnedEffect`: bone palette as large UBO (72 × mat4 = 4608 bytes) in separate bind group | ✅ | Implemented as a `SkinningParams` UBO (a `WeightsPerVertex` vec4f header + `array<mat4x4f, 72>`, 4624 bytes total including the header) in its own bind-group slot (`skinnedBindGroupLayout_` binding 2, `skinnedPbrBindGroupLayout0_` binding 3) — comfortably under the 65536-byte minimum guaranteed limit this row anticipated. See `WEBGPU-26`. |
| WEBGPU-76 | `ShaderEffect` (custom WGSL): `wgpuDeviceCreateShaderModule` from user-provided WGSL source string | ✅ | **Done 2026-08-27, on the 3D `DrawPrimitivesEx`/`DrawIndexedPrimitivesEx` route (the EasyGL/Task-1079 model), verified on the real AMD RADV GPU (`:0`).** New `WebGPUEffectRenderer : IEffectRenderer` (a friend sibling of the renderer, mirroring `WebGPUOcclusionQueryRenderer`): `CompileProgram` builds a vertex + a fragment `WGPUShaderModule` (`vs_main`/`fs_main`) inside a validation error scope, so an invalid source becomes a catchable `GetCompileError()` rather than a device error (the tolerant neutral `ShaderEffectTests` clone survives). WGSL has no loose uniforms, so — unlike EasyGL — `DeclareUniformBlockEXT(size, names, offsets, count)` is implemented as the name→byte-offset bridge and every `SetUniform*` writes into a CPU staging block that a draw uploads to a `@group(0) @binding(0)` uniform buffer; a source-scan for `texture_2d` decides whether the bind-group layout also carries the reserved `@binding(1)` sampler + `@binding(2)` texture. `CreateEffectRenderer` returns it; `ExecutesShaderEffectSourceEXT()` → **true** and `GetShaderDialectEXT()` → **`Wgsl`** are now overridden (WebGPU genuinely runs the source, unlike Vulkan's SPIR-V). Integration: a new `DrawFamily::CustomEffect` + `CustomEffectDrawCommand` (vertex/index bytes, the uniform block **captured by value per draw**, the captured unit-0 texture, the vertex layout derived from the bound `VertexDeclaration` with `location = element index`, and the blend/depth/cull/viewport/scissor snapshots) queued from a branch at the top of both `Draw*PrimitivesEx` when `params.customEffectRenderer` is set, replayed through the one shared `ReplayDrawsInOrder` path (no forked replay); the renderer sets `World`/`View`/`Projection` (column-major, untransposed) on the effect at queue time exactly as EasyGL's `BindCustomEffectMatrices`. The pipeline builder takes a `colorAttachmentCount` (1 here) so MRT (`WEBGPU-85/86/87`) reuses it for N slots. **Test:** new `WebGPU_ShaderEffect3D` (`webgpu_shadereffect_3d_test.cpp`), a Lambert `texture*diffuse*N·L` custom WGSL effect drawing a `VertexPositionNormalTexture` quad — Check A `World=Identity`+light-faces-normal+`uDiffuseColor=red` → red (proves the WGSL runs, lights, samples), Check B `World=RotateY(180°)` → black (proves `World` reaches the WGSL vertex stage, flipping the normal), Check C `uDiffuseColor=blue` → blue (proves the uniform drives the pixel). Discrimination confirmed: forcing the fragment to a constant colour fails all three (0/3). **Full WebGPU suite 87/87 on `:0`** (86 prior + this), so the shared-replay change regressed nothing. This row is the single-colour-target custom-WGSL foundation and the enabler for MRT (`WEBGPU-85/86/87`, done 2026-08-27) and custom SpriteBatch effects (`WEBGPU-142`, done 2026-08-27), both of which reuse this `WebGPUEffectRenderer`. |

---

## Phase 64.1 — WebGPU backend: stock-effect fog parity (FNA EffectHelpers.SetFogVector)

> Fog was previously implemented only for `EnvironmentMapEffect`. Every other FNA stock 3D effect
> (`BasicEffect`, `AlphaTestEffect`, `DualTextureEffect`, `SkinnedEffect`) also exposes
> `FogEnabled`/`FogStart`/`FogEnd`/`FogColor`. The `GpuDrawParams` fog fields (`fogColor`,
> `fogVector`) are already filled by every effect's `FillGpuDrawParams()`; the work here is to carry
> them into each family's WGSL and apply FNA's `ApplyFog`. The design widens the shared primary
> `Uniforms` UBO from 128 to 160 bytes (appends `fogColor` + the FNA view-space `fogVector`); the WGSL
> computes `fogFactor = 1 - clamp(dot(vec4(objectPos,1), fogVector), 0, 1)` in the vertex stage and
> `rgb = mix(fogColor, rgb, fogFactor)` in the fragment stage — the exact `colored3d`/`env_map3d`
> parity form the Vulkan renderer already uses.

| #   | Task                                                                                                          | Status | Notes                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------- |
| WEBGPU-145 | `BasicEffect` fog on the `colored3d`/`textured3d`/`colored_textured3d`/`lit_textured3d` (per-pixel + per-vertex) WGSL families. | ✅ | Done 2026-08-27, verified on the real RADV GPU (`:131`). Widened the shared primary `Uniforms` UBO to 160 bytes (`FillExtUniforms`/`FillColoredUniforms`/`FillAlphaTestUniforms` + all 9 primary-UBO command arrays now 40 floats); `coloredBindGroupLayout_`/`litBindGroupLayout_` binding-0 `minBindingSize`→0 so the families still binding 128 bytes (instanced/pbr/skinned-pbr, which do not read the fog tail) stay valid. Fog blend added to the 5 BasicEffect WGSL fragment shaders (both `lit_textured3d` variants apply it in the unlit branch too, since BasicEffect fog is independent of lighting). New test `WebGPU_BasicEffect_Fog` (`webgpu_basiceffect_fog_test.cpp`), 18/18: `FogStart==FogEnd` collapses every family to `FogColor` (the discriminator that fails if the WGSL blend is dropped — verified by temporarily neutering `colored3d`'s blend: Colored checks then fail), half-fog matches `lerp(FogColor,base,keep)` exactly for colored/textured (via calibration through the fog-off path) and lies between base and `FogColor` for the lit families, `FogStart`/`FogEnd` boundaries, fog-disabled preserves base, and a different `FogColor` genuinely changes the result. Full WebGPU suite 94/94, `EnvironmentMapEffect` fog unregressed. |
| WEBGPU-146 | `AlphaTestEffect` fog on its WGSL families (strides 20/24/32). | ✅ | Done 2026-08-27, verified on the real RADV GPU (`:131`). Both `alpha_test3d` WGSL modules (uncolored stride 20/32, vertex-colour stride 24) now declare the fog tail (a `fogPad` vec4f skips floats [28..31], since the alpha-test `Uniforms` block only uses `diffuseColor`/`alphaTest`/`extra` = floats [16..27]) and apply `mix(fogColor, rgb, fogFactor)` to the pixel that SURVIVES the alpha-test discard (FNA applies fog after the discard). New test `WebGPU_AlphaTestEffect_Fog` (`webgpu_alphatesteffect_fog_test.cpp`), 6/6: for each module, `FogStart==FogEnd`→`FogColor`, half fog matches `lerp(FogColor,base,keep)`, fog-disabled preserves base. `WebGPU_AlphaTest3D` unregressed. |
| WEBGPU-147 | `DualTextureEffect` fog on its WGSL families (strides 20/24). | ✅ | Done 2026-08-27, verified on the real RADV GPU (`:131`). Both `dual_texture3d` WGSL modules use the standard BasicEffect `Uniforms` layout (fog at floats [32..39], no padding), so they gained the fog fields + `mix(fogColor, rgb, fogFactor)` on the composed `sample0*2 x sample1 x tint` result. New test `WebGPU_DualTextureEffect_Fog` (`webgpu_dualtextureeffect_fog_test.cpp`), 6/6: for each module the base is verified as `grey*2 x tex1 = tex1`, then `FogStart==FogEnd`→`FogColor` and half fog matches `lerp(FogColor,base,keep)`. `WebGPU_DualTexture3D` unregressed. |
| WEBGPU-148 | `SkinnedEffect` fog on its 4 WGSL variants (strides 52/56, all `WeightsPerVertex`, both `PreferPerPixelLighting`). | ✅ | Done 2026-08-27, verified on the real RADV GPU (`:131`). All 4 skinned WGSL modules ({per-pixel, per-vertex} x {stride-52, stride-56 vertex-colour}) gained the fog fields in the primary `Uniforms` block and apply `mix(fogColor, rgb, fogFactor)`; the fog factor uses the SKINNED view-space position (`dot(vec4(skinnedPos.xyz,1), fogVector)`), matching FNA SkinnedEffect (which skins the position before `ComputeCommonVSOutput`) and Vulkan's `skinned3d.vert.glsl`. `skinnedBindGroupLayout_` binding-0 `minBindingSize`→0. New test `WebGPU_SkinnedEffect_Fog` (`webgpu_skinnedeffect_fog_test.cpp`), 12/12: for each of the 4 modules, ambient-only base = DiffuseColor, `FogStart==FogEnd`→`FogColor` (drop-the-fog discriminator), half fog matches `lerp(FogColor,base,keep)`. `WebGPU_Skinned3D`/`WebGPU_SkinnedPbr3D` unregressed; full WebGPU suite 97/97. |
| WEBGPU-149 | Correct the FNA fog blend to premultiply `FogColor` by the OUTPUT ALPHA, and strengthen the fog tests. | ✅ | **Done 2026-08-27 (review-fix), verified on the real RADV GPU (`:131`).** FNA `ApplyFog` (Common.fxh) is `color.rgb = lerp(color.rgb, FogColor * color.a, fogFactor)` -- it lerps toward `FogColor * outputAlpha`, not plain `FogColor`. WEBGPU-145..148 (and the ported Vulkan/env-map shaders) omitted the `* color.a`, which is only visible for alpha<1. Fixed all 16 fog blends across every stock-effect WGSL path (BasicEffect colored/textured/colored_textured/lit×2, AlphaTest×2, DualTexture×2, EnvironmentMap, Skinned×4) to `mix(fogColor.xyz * outAlpha, rgb, keep)`; output alpha unchanged. Also set the three fog-family bind-group layouts' binding-0 `minBindingSize` to the exact 160-byte size (all consumers bind 160 bytes; every `*DrawCommand::uniforms` is `std::array<float,40>`) with a `static_assert`, replacing the weaker `minBindingSize=0`; fixed the stale "128-byte" comments. **Tests strengthened** (`FogColor*alpha` discriminator that fails if the alpha-multiply is removed -- verified by temporarily reverting colored3d): `WebGPU_BasicEffect_Fog` **29/29** now also covers stride 24 (`VertexPositionColorTexture`) and an indexed draw; `WebGPU_AlphaTestEffect_Fog` **12/12** adds stride 32 (`VertexPositionNormalTexture`); `WebGPU_DualTextureEffect_Fog` **8/8**; `WebGPU_SkinnedEffect_Fog` **20/20** tests `WeightsPerVertex` 1/2/4 with a Z-shifting bone (`bone1=Translate(0,0,+4)`) so `dot(skinnedPos,FogVector)` is provably distinguished from `dot(originalPos,FogVector)` (WPV=2 moves the skinned z out of the fog range → no fog); the `WebGPU_EnvironmentMapEffect_Fog` `FoggedRgb` was corrected to scale `FogColor` by the output alpha. Full WebGPU suite 100/100. **This is what makes the "full stock-effect fog parity" claim true for alpha<1.** |
| WEBGPU-150 | Common WebGPU test preflight/launcher: a permanently-unavailable display/GPU returns CTest skip 77, a transient X11 error is retried, real failures and expected aborts propagate. | ✅ | **Done 2026-08-27 (review-fix).** `modules/renderers/webgpu/webgpu_test_launcher.sh` (POSIX sh; the WebGPU tests are Linux-only) wraps every display/GPU-backed WebGPU CTest: (1) a **preflight** checks the DISPLAY's X socket (a short retry absorbs a transient race) and exits **77** if unreachable -- this uniformly skips headless runs INCLUDING supervisor/expected-abort tests (which must be skipped, not run, without a display, so "display unavailable" is distinguished from the expected abort by never reaching it); (2) it runs the test and **retries only on a narrow display/adapter-unavailable signature** (`x11 not available`, `no adapter`, `timed out waiting for adapter`, …), skipping (77) after the retries; (3) any other non-zero exit -- a real failure or an expected abort -- is **propagated unchanged**. Wired via an opt-in in the shared `cmake/TestHelpers.cmake` (`CNA_RENDERER_GPU_TEST_LAUNCHER`, set only by the WebGPU examples) that routes single-executable tests whose ENVIRONMENT sets a `DISPLAY` through the launcher; pure-CMake tests (checksum, present-mode) and the native smoke test (its own `SKIP_REGULAR_EXPRESSION`) are unwrapped. `SKIP_RETURN_CODE 77` was already applied directory-wide. **Discriminating self-test** `WebGPU_LauncherSelfTest` (`webgpu_launcher_selftest.sh`, 6/6, uses `CNA_WEBGPU_LAUNCHER_FORCE_DISPLAY` to simulate present/absent with no real X server): absent display→77, pass→0, real failure→propagate 3, expected abort (SIGABRT)→propagate 134 (not skip), persistent x11-unavailable→77, transient→retry succeeds→0. **Verified end to end:** on `:131` the full suite is 101/101 through the launcher; forcing an absent display makes the GPU tests (incl. `WebGPU_PresentLifecycle`, the expected-abort supervisor) report **Skipped** with 0 failures. |
| WEBGPU-151 | Versioned/hashed download-cache stamp + migration, so a pre-checksum cache is re-validated (not blindly trusted). | ✅ | **Done 2026-08-27 (review-fix).** The old gate was `if(NOT EXISTS stamp)`, so a `.cna-extracted` stamp written before the checksum work made the whole verification skip -- an unverified cache. The stamp now carries **version + asset + expected SHA-256** (`cna_webgpu_write_stamp`), and `cna_webgpu_prepare_cache` (in `cmake/WebGPUChecksum.cmake`, now driving `ThirdPartyWebGPU.cmake`) decides the cache state without downloading: a trusted stamp with its archive present is reused; a **legacy/mismatched stamp with an authentic cached archive** (hash matches) is re-extracted and the stamp upgraded; a **corrupt/tampered archive OR a changed version/asset whose archive no longer matches** purges exactly this package's archive+stamp+extracted `lib/`+`include/` (`cna_webgpu_purge_cache` -- targeted, never a wide/recursive delete of `_deps`) and forces a fresh, hash-verified download; a missing archive downloads. `CNA_WEBGPU_ROOT`/`AUTO_DOWNLOAD=OFF` unchanged. **Integration test** `WebGPU_CacheMigration` (`cmake/tests/webgpu_cache_test.cmake`, 12/12, pure CMake, builds a real fixture zip so the extract path runs) exercises: correct new stamp→no download, legacy stamp→re-extract+upgrade, wrong hash→purge (archive+extracted removed)+download, changed version/asset→re-validate, interrupted extraction (archive, no stamp)→extract+stamp, no archive→download. **Verified end to end on the real cache:** a forced legacy 2-line stamp on the existing `cmake-build-webgpu` was re-validated against the pinned hash and upgraded to 3 lines on reconfigure, with no re-download. |

---

## Phase 65 — WebGPU backend: state management

| #   | Task                                                                                                          | Status | Notes                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------- |
| WEBGPU-77 | `SetDepthTestEnabled()` / `SetDepthWriteEnabled()`: bake into pipeline key | ✅ | Already true for every 3D pipeline family since `WEBGPU-32`/`33`/`34`/`35`/`37` (each keyed by `(topology, depthTest, depthWrite, depthFunc)` at minimum); 2026-07-17 confirms and extends this via the shared `Make3DPipelineKey()` helper (`WEBGPU-30`), which now also folds blend/cull/wireframe/depthBias into the exact same key alongside depth. |
| WEBGPU-78 | `SetBlendState()`: map `BlendState` preset → `WGPUBlendState` | ✅ | Verified 2026-07-17: `ApplyBlendState()` (storage-only, mirrors `VulkanGraphicsBackend::ApplyBlendState()`'s Opaque-collapses-to-disabled derivation) + `FillWGPUBlendState()`/`ToWGPUBlendFactor()`/`ToWGPUBlendOperation()` (shared helpers) bake the real per-`BlendState` factors/op into every one of the 10 3D pipeline families' `WGPUColorTargetState::blend` — previously always `nullptr` regardless of `BlendState` (blending was completely disabled for every 3D draw, a real, substantial gap, confirmed missing before this task). Runtime-verified by `WebGPU_GraphicsState` Checks C/D: `BlendState.NonPremultiplied` genuinely blends a 50%-alpha draw to a value strictly between the two colours, while `BlendState.Opaque` on the identical draw produces a pure unblended overwrite — a genuine differential proof of real factor wiring, not just an enabled/disabled bit. |
| WEBGPU-79 | `SetRasterizerState()`: `CullMode` → `WGPUCullMode`; `FillMode::WireFrame` unsupported (log warning) | ✅ | Verified 2026-07-17 — see `WEBGPU-41`'s row (same `ApplyRasterizerState()`/`ToWGPUCullMode()` work). `FillMode::WireFrame` is stored but not logged with a runtime warning: since `WEBGPU-115` (2026-08-04) a log line is not the boundary — the capability query reports `false` and the draw throws. `WebGPU_GraphicsState` Check G now asserts that refusal, that the backbuffer the refused draw was aimed at is untouched, and that Solid renders exactly afterwards. |
| WEBGPU-80 | `SetScissorRectangle()`: `wgpuRenderPassEncoderSetScissorRect` | ✅ | Verified 2026-07-17: `SetScissorRect()` (storage-only) applied once per render pass in `EnsureFrameRendered()` via `wgpuRenderPassEncoderSetScissorRect`, mirroring Vulkan's own `vkCmdSetScissor` call — genuinely dynamic wgpu-native state, not baked into any pipeline. A disabled scissor test still issues an explicit full-target rect (WebGPU has no separate enable toggle — the rect IS the only clip). Runtime-verified by `WebGPU_GraphicsState` Check E: a full-screen quad is clipped exactly at the `ScissorRectangle` boundary (pixel just inside shows the quad, just outside shows the untouched clear colour). |
| WEBGPU-81 | `SetViewport()`: `wgpuRenderPassEncoderSetViewport` | ✅ | Verified 2026-07-17: `SetViewport()` (storage-only) applied once per render pass via `wgpuRenderPassEncoderSetViewport`, defensively clamped to the current physical surface size at the point of GPU application. Real regression found and fixed while landing this: `GraphicsDevice.Viewport`'s own default value (set once via `UpdateViewportFromWindow()`→`IGraphicsBackend::GetViewportSize()`, not refreshed every frame) can be wider than the actual physical swapchain surface in this backend specifically (`GetViewportSize()` returns the CNA-specific *logical/letterboxed* viewport size, not necessarily the physical one) — harmless while `SetViewport()` was a no-op, but once wired for real this stale/oversized value visibly stretched every 3D **and 2D SpriteBatch** draw in the pass (both share the one deferred render pass). Caught via this task's own regression sweep: `git stash` of this whole change on this same branch/environment made `WebGPU_Clear_Readback`/`WebGPU_BasicEffect_PreferPerPixelLighting`/`WebGPU_Skinned3D`/`WebGPU_SkinnedPbr3D` pass 100%, proving the failures were newly introduced, not pre-existing/flaky; the clamp (matching wgpu-native's own hard x+width/y+height-within-target requirement) restores all 4 to passing alongside the new checks — root-caused, not just masked, since clamping is independently correct per the WebGPU spec regardless of why the stored value was oversized. The underlying `GetViewportSize()` logical-vs-physical semantics question is a separate, pre-existing, out-of-scope concern for a future task (not touched here — no `Microsoft::Xna::Framework` changes). Runtime-verified by `WebGPU_GraphicsState` Check F: a half-width `Viewport` confines a full-clip-space quad to its own sub-rectangle. |
| WEBGPU-82 | `SetSamplerState()`: per-slot `WGPUSampler` cache (filter + address mode key) | ✅ | 2026-07-17: extended beyond the SpriteBatch-only 18-entry cache. `ApplySamplerState()` now stores real per-slot `SamplerState` (filter/addressU/addressV/maxAnisotropy, 16 slots) and `GetOrCreateSlotSampler()` (a genuine `unordered_map`-keyed `WGPUSampler` cache, full XNA `TextureFilter` enum incl. anisotropic/mip variants, distinct from `samplerCache_`) is now actually READ by every texture-consuming 3D `Queue*Draw()` (Textured/LitTextured/AlphaTest/DualTexture/Pbr/Skinned/SkinnedPbr) for texture slot 0 — previously every one of these hardcoded Linear/Clamp/Clamp regardless of `GraphicsDevice.SamplerStates[0]` (confirmed: `command.textureFilter`/`addressU`/`addressV` were declared on every `*DrawCommand` struct but never assigned in any `Queue*Draw()` before this task). Re-audited 2026-08-26 -> ✅: the "no dedicated pixel test yet" gap is closed. `WebGPU_StockEffectSamplerContract` (REMED-GFX-169) plus `WebGPU_PointSamplingContract`/`WebGPU_TextureFilterOrdinalContract`/`WebGPU_DualTextureSlotSamplerContract` now prove that a non-default per-slot filter/address mode observably changes a stock 3D effect's sampled output -- all Passed on the real RADV GPU (`:0`). Slot 1 is exercised by the dual-texture slot-sampler contract. |
| WEBGPU-83 | `SetDepthStencilState()`: stencil ops → `WGPUStencilFaceState` | ✅ | **UPDATE 2026-08-27 (Phase 3): two-sided winding is now pixel-validated — WEBGPU-83 is COMPLETE.** New WebGPU-local test `WebGPU_StencilTwoSided` (`webgpu_stencil_twosided_test.cpp`) replicates the cross-renderer parity contract of `easygl_depthstencilstate_stencil_twosided_test.cpp` (Task 318): a back-facing triangle under `TwoSidedStencilMode=true` picks up the `CounterClockwise*` ops (NotEqual→Increment→0x06→green) while the two-sided=false control applies the front ops to every face (Equal→Decrement→0x04→background) — opposite outcomes toggled by that one flag, so a bypassed test or a swapped front/back slot cannot pass both. **2/2 PASS on the RADV GPU**, confirming the front=primary/CW → `stencilFront`, back=CCW → `stencilBack` mapping is correct (the "silent winding quirk" the Phase-1 note feared did NOT bite). Full WebGPU suite 92/92. --- **UPDATE 2026-08-27 (Phase 2): the stencil bake now covers ALL 3D families, not just colored3d.** The same shared machinery (`StencilKeyParams`/`FillWGPUStencilState`/`HashStencilState`/`CaptureStencilStateEXT`) is wired into every `GetOrCreatePipeline*3D` (textured3d, colored-textured3d, lit-textured3d + its per-pixel VertexLit sibling, alpha-test3d, dual-texture3d, env-map3d, instanced3d, pbr3d, skinned3d, skinned-pbr3d): each takes a `const StencilKeyParams& stencil` param, folds `HashStencilState` into its own cache key, bakes `FillWGPUStencilState` into its `depthStencil`, captures the state per-draw in its `*DrawCommand`, and applies `wgpuRenderPassEncoderSetStencilReference` per draw. A new WebGPU-local test `WebGPU_StencilFamily` (`webgpu_stencil_family_test.cpp`) stamps then gates on the **Textured3D** route and asserts the gate is *rejected* outside the stamped region (the discriminator that fails if the family ignored stencil) — 2/2 on the RADV GPU. **Still open (Phase 3):** only the **two-sided** front/back winding remains unvalidated by a dedicated two-sided differential test (both proven routes use non-two-sided stencil); the two-sided mapping is implemented but not pixel-verified. --- **UPDATE 2026-08-27 (Phase 1): stencil ops are now baked into the pipeline on the colored3d route, and the shared cross-renderer acceptance test flipped to assert it.** `GetOrCreatePipelineColored3D` now calls `FillWGPUStencilState` (XNA `CompareFunction`/`StencilOperation` → `WGPUStencilFaceState` via the new `ToWGPUStencilOperation`; front/back = the primary/CW ops, or CW→front + CCW→back when `twoSidedStencilMode`), with the read/write masks folded into the pipeline cache key (WebGPU keeps masks as *pipeline* state, unlike Vulkan's dynamic masks) via a colored3d-local `HashStencilState` XOR (a disabled stencil folds a constant, so it never fragments the cache; `Make3DPipelineKey` stays shared/unchanged). The stencil state is captured per-draw in the `ColoredDrawCommand` (`CaptureStencilStateEXT`), and the reference is applied dynamically per draw (`wgpuRenderPassEncoderSetStencilReference` from `command.stencilRef`) so a stamp and a gate in one pass can differ. **Acceptance:** the shared `rendertarget_depthstencil_usage_test` WEBGPU contract flipped `stencilInRT`/`stencilPreserves` false→true, and its **C2** ("the stencil gate passes exactly where the stamp was and is rejected everywhere else") now passes on the real RADV GPU (`:0`) — 29/29, full suite 90/90. Before this, C2 vacuously asserted "gate passes everywhere". **Still open (narrowed):** the other 3D families (textured3d/lit/skinned/PBR/env-map/dual-texture/alpha-test/instanced) still store the stencil state but do not bake it — the shared `StencilKeyParams`/`FillWGPUStencilState`/`HashStencilState`/`CaptureStencilStateEXT` infra is ready to extend to them the same way; and the **two-sided** front/back winding is implemented but not yet validated by a two-sided differential test (the acceptance test uses non-two-sided stencil, so the primary path is proven, the winding of two-sided is not). Original 2026-07-18 deferral note follows. --- Re-audited 2026-08-26 -- still 🟨, gap unchanged. Deliberately scoped down 2026-07-17 (see task report): `ApplyDepthStencilState()` now stores the FULL real stencil parameter set (`stencilEnable_`/`stencilFunc_`/`stencilPass_`/`stencilFail_`/`stencilDepthFail_`/`stencilReadMask_`/`stencilWriteMask_`/`twoSidedStencilMode_`/`ccwStencil*_`), extending what was previously depth-only storage — but baking these into any pipeline's `WGPUStencilFaceState` is deliberately DEFERRED, not attempted in this task. Rationale: `VulkanGraphicsBackend::FillDepthStencilState()`'s own comment documents a real, empirically-discovered front/back-winding quirk specific to that backend that required swapping which `VkStencilOpState` slot XNA's "front"/CCW settings land in; baking stencil ops into this backend's 10 pipeline families without an equivalent WebGPU-side differential stencil test risks the identical silent-wrongness class — and this task's own cull-mode mapping (`WEBGPU-41`/`79`) already needed exactly one such empirical correction after a first, purely-derived guess turned out backwards, reinforcing the risk is real, not theoretical, for this exact class of winding/orientation subtlety. `SetReferenceStencil()`'s own dynamic-state half (`wgpuRenderPassEncoderSetStencilReference`, applied once per pass) IS implemented and wired (see `WEBGPU-80`/`81`'s row), independent of this remaining gap. A dedicated stencil-ops task (with its own differential pixel test, mirroring Vulkan's `stencil_twosided` precedent) remains open. |
| WEBGPU-84 | `OcclusionQuery`: `WGPUQuerySet` (type=Occlusion) + `wgpuRenderPassEncoderBeginOcclusionQuery` | ✅ | Done 2026-08-26. `CreateOcclusionQuery()` now returns a real `WebGPUOcclusionQueryRenderer` (a `friend` of the renderer, mirroring `VulkanOcclusionQueryRenderer`'s idiom). Each query reserves a slot in a shared 32-slot occlusion `WGPUQuerySet`; `OcclusionQuery.Begin()/End()` set/clear an `activeOcclusionQuery_`, `RecordDrawOrder` tags each queued draw with it, and `ReplayDrawsInOrder` wraps that query's first contiguous run of draws in one `wgpuRenderPassEncoderBeginOcclusionQuery`/`EndOcclusionQuery` pair (only the first run per flush -- a slot may be written once per resolve, the same policy Vulkan uses). Every render pass now carries `occlusionQuerySet_`; after the passes `ReplayOrderedSegments` resolves the set into a `QueryResolve` buffer and copies it to a `MapRead` buffer on the same encoder, and `IsComplete()`/`PixelCount()` map it lazily (also drained at the next flush). **The whole path is gated on `activeOcclusionQuery_`, so a frame with no query records exactly what it did before** -- the full suite stayed at its prior pass count. Capability flipped: `SupportsCapability(OcclusionQuery)` now returns true (the `WEBGPU-135` false arm removed) with `WebGpuMrtOcclusionContractTests` updated in the same commit to assert true. New pixel test `WebGPU_OcclusionQuery` (3/3 on the real RADV GPU, `:0`): the SAME full-screen quad reads back `PixelCount == 0` behind a depth occluder and `4096` (a full 64x64 target) in front, with two independent per-slot query results. Known scope: a query whose draws span more than one render-pass segment records only its first segment (documented; occlusion queries normally wrap draws to one target). |

---

## Phase 66 — WebGPU backend: Multiple Render Targets

| #   | Task                                                                                                          | Status | Notes                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------- |
| WEBGPU-85 | MRT render pass: `WGPURenderPassDescriptor` with array of `WGPURenderPassColorAttachment` (up to 4) | ✅ | **Done 2026-08-27, verified on the real AMD RADV GPU (`:0`), full WebGPU suite 88/88.** `PassDestination` now carries up to 4 colour attachments (slot 0 in its single fields, slots 1..N-1 in `mrt*` arrays; depth stays single/shared) and `ReplayOrderedSegments` builds an N-entry `WGPURenderPassColorAttachment` array from it — one shared replay path, count-parameterised, no MRT/non-MRT fork. `RenderPendingDrawsToRenderTarget` fills the extra attachments from the bound set. **See the Phase-0 note below for why WEBGPU-76 had to land first.** — Phase-0 decision gate, 2026-08-27 — ordering fixed so the next reader does not re-derive it: WEBGPU-76 (custom WGSL `ShaderEffect`) MUST land first; MRT then follows.** Rationale, evidence-backed, not a guess: (1) **Every** stock WGSL fragment shader in `WebGPURenderer.cpp` writes exactly one output — `@fragment fn fs_main(...) -> @location(0) vec4f` (all ~30 of them: lines 2790, 3020, 3196, 3270, 3549, 3675, 3953, 4010, 4229, 4281, 4538, 5004, 5057, 5291, 9052, 9712, 9844, 9986, 10105, …). Nothing this renderer can draw writes `@location(1..3)`. (2) The renderer has **no** custom-WGSL execution path: `ExecutesShaderEffectSourceEXT()` is not overridden (0 occurrences in `WebGPURenderer.cpp`) so it inherits `IGraphicsRenderer`'s `return false` (`IGraphicsRenderer.hpp:2179`), and WebGPU is one of the few renderers that does **not** implement `CreateEffectRenderer` (22 others do). So there is no way to author a fragment shader that writes a second slot. (3) WebGPU pipeline validation requires each non-null colour target to have a matching fragment `@location(i)` output — a stock `@location(0)`-only pipeline cannot even be **created** against a 2-target fragment state, so a stock draw into an MRT set fails at pipeline creation, not merely semantically. (4) This is exactly why the Sokol precedent (the one renderer that shipped MRT here) verified MRT only with a custom `ShaderEffect` writing distinct per-slot outputs and **refuses** a stock-pipeline draw while MRT is bound (`SokolRenderer.cpp` ~5478-5484). (5) Therefore Phase-0 option (b) — "prove real per-slot writes another way" — is unreachable: the only sanctioned mechanism to write slot ≥1 is a multi-`@location` custom WGSL shader (WEBGPU-76). A clears-only MRT (pass descriptor + readback of clear values, no fragment-output routing) is explicitly disallowed by this task. **Conclusion: option (a). Do WEBGPU-76 first (multi-output WGSL is its enabling case for MRT), then build 85/86/87 on top and verify with a custom per-slot ShaderEffect à la Sokol.** Phases 1-5 of the MRT work-order (pass array, `GetOrCreateMRTRenderPipeline`, `SetRenderTargets` 2..4, the capability-trap flip in `WebGpuMrtOcclusionContractTests`/`WebGpuWireFrameContractTests`, and the `WebGPU_MRT` pixel test) are all unchanged and correct — they are simply gated behind WEBGPU-76 existing first. |
| WEBGPU-86 | `GetOrCreateMRTRenderPipeline(colorAttachmentCount)`: pipeline with matching `targetCount` in fragment state | ✅ | **Done 2026-08-27.** Every pipeline that replays into a pass now matches the pass's attachment count, keyed by it (folded into `Make3DPipelineKey` and `SpritePipelineKey` only when > 1, so single-target keys — and the instanced/viewport/scissor cardinality tests — are byte-identical). A **custom `ShaderEffect`** (`IssueCustomEffectDraw`) builds N fragment targets and its WGSL writes `@location(0..N-1)` (this is the real MRT path). A **stock/built-in draw** (3D and SpriteBatch) into an MRT set writes attachment 0 only: `InitStockColorTargetsEXT` widens each stock pipeline to N targets with slots 1..N-1 at `writeMask` 0 (the "the 2D/stock pipeline writes attachment 0" behaviour every other renderer has) — chosen over the Sokol refuse-route because the shared cross-renderer MRT tests (`graphicsdevice_ordered_clear_test` M1, `rendertarget_invalid_mip_level_test` H1, `backbuffer_pass_order_test`) do stock draws into MRT and read attachment 0 back. `replayColorAttachmentCount_` is set at replay from the `PassDestination` and read by all stock builders. |
| WEBGPU-87 | `SetRenderTargets(vector<RenderTarget2D*>)`: configure MRT pass descriptor | ✅ | **Done 2026-08-27.** `SetRenderTargets` accepts 2..4 `RenderTarget2D` targets: it validates every target shares width/height/sample count (refusing a mismatch with a `System::NotSupportedException` that names the disagreeing target), refuses a cube face or a null target in an MRT set, and refuses count > 4 (XNA's ceiling). Slot 0 binds as before; slots 1..N-1 go into `mrtExtraTargets_`. The single-target and cube-face paths are byte-identical, and any single-target / cube-face / backbuffer bind — including `SetRenderTarget(nullptr)` — flushes the outgoing set and fully clears `mrtExtraTargets_`. |
| WEBGPU-142 | Custom-WGSL `ShaderEffect` on the **SpriteBatch** route: `SpriteBatch.Begin(..., effect)` runs the effect's shader per sprite. | ✅ | **Done 2026-08-27, verified on the real AMD RADV GPU (`:0`), full WebGPU suite 89/89.** `WebGPUSpriteBatchRenderer::SetCustomEffect` (previously a hard "not implemented" throw) now resolves the `Effect`'s `WebGPUEffectRenderer` via `GetEffectRendererPtr()` (a compiled Effect-Framework effect, which has no WGSL, is refused with a `System::NotSupportedException`) and stores it in `activeSpriteCustomEffect_`. `QueueSprite` captures that effect and its uniform block **by value** into the `SpriteCommand`; `IssueSprite` routes a sprite with a custom effect to the new `IssueSpriteWithCustomEffect`, which builds a pipeline from the effect's `vs_main`/`fs_main` modules with the fixed `SpriteVertex` layout (`position`/`uv`/`color` at `@location(0/1/2)`; no matrices — sprite vertices are already NDC), the sprite's blend/write-mask on slot 0 (extra MRT slots write nothing), a disabled Depth24PlusStencil8 state matching the stock sprite pipeline, and binds `{uniform block @0, the sprite's own sampler @1, the sprite's texture @2}` using the effect's reserved binding convention. Pipelines are cached on the effect keyed by the sprite pass state (a `"SPRITE01"` salt keeps them disjoint from the effect's 3D-route keys). **Test:** `WebGPU_SpriteBatch_ShaderEffect` (`webgpu_spritebatch_shadereffect_test.cpp`) — a tint effect renders a white texture red (A) then blue (B) under `uTint`, and a no-effect batch renders white (C, proving the effect is per-`Begin`). Discrimination confirmed: dropping `uTint` from the fragment fails A/B (1/3). |
| WEBGPU-143 | Per-slot MRT `ColorWriteChannels`/`1`/`2`/`3` for a custom-effect MRT draw. | ✅ | **Done 2026-08-27, verified on the real AMD RADV GPU (`:0`), full WebGPU suite 89/89.** `ApplyBlendState` previously kept only `colorWriteChannels[0]`; it now stores all four (`colorWriteChannels_[4]`, the framework already delivers them from `BlendState.ColorWriteChannels`/`1`/`2`/`3`). `QueueCustomEffectDraw` captures the four masks into the command, and `IssueCustomEffectDraw` applies slot *i*'s mask to `@location(i)` (XNA `ColorWriteChannels` is bit-identical to `WGPUColorWriteMask`) and folds them into the pipeline key. **Test:** `WebGPU_MRT` gained a 4-target Check C — the same custom effect writes all four slots, but with `ColorWriteChannels`=Red / `1`=Green / `2`=Blue / `3`=All each slot keeps only its enabled channel(s) over a black clear (slot 0→`(200,0,0)`, 1→`(0,40,0)`, 2→`(0,0,120)`, 3→full). Discrimination confirmed: forcing every slot to `writeMask=All` fails slots 0/1/2 (7/10). Stock/single-target draws are unchanged (they use `colorWriteMask_`, still slot 0). Note: XNA has one blend EQUATION for all targets, so only the write mask is per-slot; there is no per-slot independent blend to model. |
| WEBGPU-144 | GPU-native block-compressed textures (DXT1/3/5, BC7): upload BC blocks to a `WGPUTextureFormat_BC*` texture instead of CPU-decompressing to RGBA8 (partially supersedes `WEBGPU-111`). | ✅ (Phase 1 + 2) | **UPDATE 2026-08-27 (Phase 2, XNB-24): the content loaders now reach the native path too, full WebGPU suite 93/93.** `Texture2D::FromStream` (DDS) and the `.xnb` `Texture2DReader` keep DXT content compressed and upload the raw blocks (via the same compressed `SetData` path Phase 1 built) instead of CPU-decoding to Color. Gated on a new renderer-opt-in capability `IGraphicsRenderer::LoadsCompressedContentNativelyEXT()` (default false; **WebGPU-only** — the owner chose the WebGPU-only scope over reusing `IsCompressedTransferFormatEXT`, so Skia and every other renderer keep their deliberate decode-to-Color loaders, no Skia contract/test churn) AND the per-format `IsCompressedTransferFormatEXT`, so a loaded DXT texture keeps its `Dxt*` format exactly when the device can transfer it and decodes to Color otherwise (asserted both ways). The `FromStream(...,w,h,zoom)` resize overload always decodes (it resamples RGBA). **Test:** `WebGPU_CompressedContent` — FromStream DXT1 and `.xnb` DXT5 each keep native `Dxt*` storage + a 4-level chain, and the FromStream texture renders red (3/3). **Bug found + fixed along the way:** baking full mip chains exposed a compressed-mip upload defect — a sub-4x4 tail level (2x2/1x1) must be `wgpuQueueWriteTexture`'d with its **block-aligned** copy extent (`ceil(dim/4)*4`), which wgpu-native v29 validates against, not its logical texel size (Phase 1's single 4x4 level never reached it). New defaults test `RendererCapabilityDefaultsTest.LoadsCompressedContentNativelyDefaultsToFalse` pins the opt-in default. Pre-existing (NOT Phase 2): `Texture2DContentTypeReaderTest.NormalizedByte2RejectsAFourBytePerTexelLevel` fails on a WebGPU device (WebGPU has no `NormalizedByte2`; the test lacks the renderer-skip its `Reads` sibling has) — proven identical at HEAD before this change. --- Phase 1: **Done 2026-08-27, verified on the real AMD RADV GPU (`:0`), full WebGPU suite 90/90.** The WEBGPU-111 premise was partly stale: `ImageData` already carries `surfaceFormat`/`mipLevels` and `Texture2D` already has the compressed block-transfer contract (`IsCompressedTransferFormatEXT`, `SetCompressedDataBytes`/`GetCompressedDataBytes` → `UpdatePixelsLevel`/`GetData`), which only Skia (a CPU rasterizer) implemented. **This is the first renderer to upload BC blocks to the GPU natively.** WebGPU-local: the device requests `WGPUFeatureName_TextureCompressionBC` after `wgpuAdapterHasFeature` (stored in `bcSupported_`); `IsCompressedTransferFormatEXT`/`ClassifySurfaceFormatEXT` report the DXT/BC7 formats supported; `WebGPUTextureRenderer` reads `ImageData.surfaceFormat`, creates a `WGPUTextureFormat_BC{1,2,3,7}*` texture (Dxt1→BC1, Dxt3→BC2, Dxt5→BC3, Dxt5Srgb→BC3Srgb, Bc7→BC7, +Srgb; no RenderAttachment usage), uploads blocks with a block-stride copy layout, and keeps the per-mip blocks as the authoritative store its block `GetData` reads back. **Test:** `WebGPU_CompressedTexture` — a solid-red DXT1 (8-byte block) and a DXT5 (16-byte block) each sample RED (the GPU decoded the block natively, not the black clear) and `GetData` round-trips the exact block bytes (4/4). **Phase 2 DONE (see the Phase-2 update at the top of this row):** the XNB `Texture2DReader` and the DDS/`FromStream` path now reach the native path for WebGPU via `LoadsCompressedContentNativelyEXT()`. BC4/5/6 and non-BC compressed families (ETC/PVRTC/ATC) are not in CNA's `SurfaceFormat` enum. |

---

## Phase 67 — WebGPU backend: automated validation after the native 2D gate

| #   | Task                                                                                                          | Status | Notes                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------- |
| WEBGPU-88 | Establish CMake/CTest registration for native WebGPU tests. | ✅ | Delivered by `WEBGPU-129` (`WebGPU_Native2D_Smoke`) and extended 2026-07-12 by `WEBGPU-91`'s `WebGPU_Clear_Readback` — both registered via the same `CNA_BUILD_TESTS AND CNA_GRAPHICS_RENDERER STREQUAL "WEBGPU"` block, real display/GPU required (no synthetic skip path needed beyond the existing `WebGPUNativeSmokeTest.cmake` one). |
| WEBGPU-89 | No-readback smoke: initialize, clear to a known colour, present 60 frames, release. | ✅ | This is exactly `WEBGPU_Native2D_Smoke` (`cna_demo_2d --smoke 120`), verified 2026-07-12 as part of the `ctest -R "^WebGPU_"` run (2.19s, pass) — restating this row's own acceptance criterion, not new work. |
| WEBGPU-90 | No-readback SpriteBatch scene: RGBA texture, source rectangle, tint, rotation and sampler variants. | ✅ | This is `cna_demo_2d --webgpu-2d-validation`, verified by `WEBGPU-126` and reconfirmed 2026-07-12 (screenshot review after the `WEBGPU-91` `Present()`/`EnsureFrameRendered()` refactor, still renders identically — crops/tint/rotation/flips/sampler variants all correct, no WebGPU error). |
| WEBGPU-91 | Implement reusable GPU readback with correct row alignment, asynchronous map/poll completion and timeout/error propagation. | ✅ | Verified 2026-07-12: `WebGPURenderer::ReadBackbuffer()` implemented via `wgpuCommandEncoderCopyTextureToBuffer` into a 256-byte-row-aligned `MapRead`\|`CopyDst` buffer, `wgpuBufferMapAsync` + the existing `WaitForCompletion` polling helper (`WEBGPU-127`'s pattern), BGRA↔RGBA swizzle matching Vulkan's own `ReadBackbuffer`. Required refactoring `Present()` into a shared `EnsureFrameRendered()` (acquire-if-needed + render-if-`framePending_`) so `GetBackBufferData()` can force an on-demand render of whatever `Clear()`/`SpriteBatch` work is queued so far in the *current* `Draw()` call, matching the Vulkan/Bgfx backends' own on-demand-submit readback semantics (confirmed necessary: `bgfx_graphicsdevice_clear_stencil_test.cpp` reads back the same-frame result of a `Clear()`+draw with no intervening `Present()`). New `WebGPU_Clear_Readback` CTest (`examples/webgpu_clear_readback_test.cpp`), verified genuinely discriminating via `git stash` (reverting the backend changes makes the test abort with the base class's `"ReadBackbuffer: not implemented"` exception instead of passing). Re-verified `WebGPU_Native2D_Smoke` and the `--webgpu-2d-validation` scene (screenshot-reviewed) still pass unchanged after the `Present()` refactor. |
| WEBGPU-92 | Pixel-asserted 2D tests: clear and white 1×1 sprite, then alpha/source-rectangle/sampler cases. | ✅ | `WebGPU_Clear_Readback` (`WEBGPU-91`/`132`) pixel-asserts: solid-colour `Clear()` (twice, proving no stale cache), a colour-tinted `SpriteBatch` quad inside/outside its destination rectangle, `alpha=0` leaving the destination unmodified, `sourceRectangle` cropping (2×1 red\|blue texture, right-texel-only crop samples blue), 50%-alpha landing strictly between black and full colour (`WEBGPU-132`), and — closing the last gap, 2026-07-12 — sampler address-mode (Wrap/Clamp/Mirror), Checks H/I/J: a `sourceRectangle` wider than the texture (4 texels against a 2×1 red\|blue texture) makes UV genuinely exceed 1.0 without any extra plumbing (`SpriteBatch` never clamps UV to the source rectangle itself), and `TextureFilter=Point` makes each sampled colour an exact single-texel lookup, no bilinear blur at the wrap/mirror seam. Verified genuinely discriminating by temporarily neutering `ToAddressMode()` to always return `ClampToEdge` — Wrap and Mirror checks then fail (both silently become Clamp) while the Clamp check itself still passes, exactly the expected failure signature — then reverted. 10/10 pixel checks total, all in the one `WebGPU_Clear_Readback` CTest. |
| WEBGPU-93 | 3D coloured-quad pixel test (stride 16). | ✅ | Shipped as `WebGPU_Colored3D` (`cna_test_webgpu_colored3d`). Verified 2026-08-26 on the real AMD RADV GPU (`:0`): Passed. |
| WEBGPU-94 | 3D textured-quad pixel test (stride 20). | ✅ | Shipped as `WebGPU_Textured3D`. Verified 2026-08-26 on the real RADV GPU (`:0`): Passed. |
| WEBGPU-95 | 3D coloured+textured pixel test (stride 24). | ✅ | Shipped as `WebGPU_ColoredTextured3D`. Verified 2026-08-26 on the real RADV GPU (`:0`): Passed. |
| WEBGPU-96 | Lit-textured, alpha-test and dual-texture effect tests. | ✅ | Shipped as `WebGPU_LitTextured3D` / `WebGPU_AlphaTest3D` / `WebGPU_DualTexture3D`. Verified 2026-08-26 on the real RADV GPU (`:0`): all Passed. |
| WEBGPU-97 | Environment-map and skinned-effect tests. | ✅ | Skinned-effect: `WebGPU_Skinned3D` (9/9) and `WebGPU_SkinnedPbr3D` (5/5), see `WEBGPU-26`. Environment-map: `WebGPU_EnvMap3D` (4/4), closed 2026-07-18, see `WEBGPU-25`/`36`/`74`. |
| WEBGPU-98 | Instancing, render-target, MSAA and occlusion-query tests. | ✅ | Render-target (`WebGPU_RenderTarget2D`, `WEBGPU-53`) and MSAA (`WebGPU_Msaa`, `WEBGPU-58`) tests exist (8/8 and 6/6). **Instancing is done too:** `WebGPU_Instanced3D` plus `WebGPU_InstancedOffsetFrequency_Cardinality` and `WebGPU_InstancedVertexColor_Cardinality` — all three Passed on the real RADV GPU (`:0`) 2026-08-26. The **occlusion-query** half is now closed too (2026-08-26, `WEBGPU-84`): `WebGPU_OcclusionQuery` is a real functional test -- the same quad reads `PixelCount` 0 behind a depth occluder and a full 64x64 target of samples in front, with two independent per-slot results -- and the capability truthfully reports true. All four sub-areas (instancing, render-target, MSAA, occlusion-query) now have real CTests, so this row is ✅. |
| WEBGPU-99 | Buffer disposal, `SetDataOptions` and vertex-format mapping tests. | ✅ | Done 2026-08-26, satisfied by the tests landed with its three sibling tasks, all on the real RADV GPU (`:0`): **buffer disposal** -> `WebGPU_DisposedGuard` (`WEBGPU-47`, 4/4: live upload succeeds, post-`Dispose()` `SetData` throws `ObjectDisposedException` for both `VertexBuffer` and `IndexBuffer`); **`SetDataOptions`** -> `WebGPU_SetDataOptions` (`WEBGPU-44`, 4/4: None/Discard/NoOverwrite each upload+draw correctly, plus the Discard-between-two-queued-draws hazard case); **vertex-format mapping** -> the whole `WebGPU_*3D`/sprite/skinned/instanced suite now routes every attribute format through `WebGPUVertexFormatFromVEF` (`WEBGPU-116`), so a wrong ordinal->format mapping garbles the matching draw and the suite (81/82, only `WEBGPU-141` unrelated) is the discriminating check. |

---

## Phase 68 — WebGPU backend: advanced and parity

| #   | Task                                                                                                          | Status | Notes                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------- |
| WEBGPU-106 | `SetStringMarkerEXT()`: no-op (WebGPU has no debug labels in wgpu-native C API yet) | ✅ | Done 2026-08-26. The WebGPU renderer inherits `IGraphicsRenderer::SetStringMarkerEXT()`'s no-op default (it does NOT override it, unlike `VulkanRenderer`). This is correct, not a stub: `wgpu-native` v29's debug markers attach to a *live* command encoder / render pass (`wgpuRenderPassEncoderInsertDebugMarker` etc.), but this renderer records draws into a deferred command list and has no encoder open at the moment the device-level `SetStringMarkerEXT()` is called, so there is nothing to attach to. Deviation documented in `docs/webgpu-vs-vulkan-deviations.md` (`WEBGPU-118`). No game behaviour depends on it -- it affects only external GPU-capture tooling. |
| WEBGPU-107 | `DebugSimulateContextLoss()`: destroy and recreate device (wgpu-native supports `wgpuDeviceDestroy`) | ⬜ | Re-audited 2026-08-26 (stays ⬜). The WebGPU renderer inherits `IGraphicsRenderer::DebugSimulateContextLoss()`'s no-op default, exactly as `VulkanRenderer` does. The contract is explicitly OpenGL-context-loss-shaped (its own doc: `WEBGL_lose_context` on the web / destroy+recreate the SDL GL context on desktop, "forcing all GPU resources to be re-initialised") -- a GL-renderer debug feature. A faithful WebGPU version would `wgpuDeviceDestroy` and recreate the device, then re-initialise every live GPU resource (every `Texture2D`/vertex/index buffer/pipeline handle owned by an XNA object), resource-recreation machinery neither this renderer nor the Vulkan renderer has. Deferred as a large, GL-flavoured, low-game-impact debug feature; not force-implemented unsupervised. |
| WEBGPU-108 | `PresentationInterval` → vsync: `wgpuSurfaceConfigure.presentMode` (Fifo=VSync, Immediate=no VSync, Mailbox=adaptive) | ✅ | **Done 2026-08-27: the mapping is now a WGPU-free, unit-tested seam.** Extracted the `PresentInterval`→present-mode policy into `SelectPresentModeChoiceEXT()` (`WebGPUPresentMode.hpp`, a pure function taking the swap interval + which modes the surface advertises, returning a `PresentModeChoiceEXT`); the renderer only translates that choice onto a concrete `WGPUPresentMode` (or, for `FirstAvailable`, the surface's first advertised mode). New test `WebGPU_PresentModeMapping` (`webgpu_present_mode_test.cpp`), 9/9, pure CMake-registered unit test (no GPU/display): a zero swap interval prefers `Immediate`→`Mailbox`→`Fifo`; a non-zero interval uses `Fifo`; and **the unsupported-mode fallback is tested** — a v-sync request when the surface does NOT advertise `Fifo` (spec-guaranteed, but modelled defensively) falls back to the first advertised mode, and no advertised modes at all yields `Fifo`. The one thing still not asserted is the GPU-side frame-pacing EFFECT of the chosen mode, which is inherently unobservable through `GetData`/pixel readback (no timing harness); the mapping policy + fallback themselves are now proven. |
| WEBGPU-109 | `IsFullScreen` via `SDL_SetWindowFullscreen` — same as other backends | ✅ | Done 2026-08-26. Fullscreen is a `CNA::Platform` / `GameWindow` concern, not a renderer one: `Sdl3Window::SetFullscreenMode()` calls `SDL_SetWindowFullscreen`/`SDL_SetWindowFullscreenMode`, and the WebGPU renderer shares that exact SDL3 platform path with every other renderer (it only ever receives the surface it is handed). So `IsFullScreen` works for WebGPU identically to all backends with no renderer-local code, which is what "same as other backends" asks for. No WebGPU-specific test is meaningful (it would exercise the platform, not the renderer); documented in `docs/webgpu-vs-vulkan-deviations.md` (`WEBGPU-118`). |
| WEBGPU-110 | `BackBufferWidth/Height` changes: reconfigure swap chain via `wgpuSurfaceConfigure` | ✅ | Runtime-verified by `WEBGPU-127`'s resize sequence (800×600→960×540→800×600) and `WEBGPU-130`'s independent second application, both with clean reconfiguration and no WebGPU validation error. |
| WEBGPU-111 | DXT1/DXT3/DXT5 compressed texture upload: `WGPUTextureFormat_BC1RGBAUnorm` etc. | ✅ (superseded by WEBGPU-144) | **UPDATE 2026-08-27: the native-upload part is DONE as `WEBGPU-144` — see that row.** Two premises in the 2026-07-18 note below are now stale: (a) `ImageData` DOES carry a `surfaceFormat`/`mipLevels` field (added by later Task 924 / SKIA-140+ work), and (b) `Texture2D` DOES have the compressed block-transfer contract (`IsCompressedTransferFormatEXT`/block SetData/GetData), which Skia already used. WebGPU is now the first renderer to upload BC blocks to the GPU natively (via the direct `Texture2D(...,Dxt*)`+`SetData` API). What the 2026-07-18 note correctly identified as still cross-cutting — the XNB/DDS content loaders force-decoding DXT to Color — was closed on 2026-08-27 as WEBGPU-144 Phase 2 (a new `LoadsCompressedContentNativelyEXT()` opt-in, WebGPU-only; see that row). Original 2026-07-18 note follows. --- 2026-07-18 (mips/cube/compressed-textures cluster): investigated, deliberately NOT implemented -- deferred as a genuinely larger, cross-cutting change than backend-local scope allows, not a time-management cut. Two concrete findings: (1) **the real adapter on this development machine DOES support it** -- a standalone diagnostic against this project's pinned wgpu-native v29.0.1.1 (`wgpuAdapterHasFeature(adapter, WGPUFeatureName_TextureCompressionBC)`, the exact enum/function confirmed against the pinned `webgpu.h`) returned `TRUE` (AMD Radeon 780M/RADV Vulkan backend), and requesting a device with that feature in `requiredFeatures` succeeded too (`wgpuDeviceHasFeature()` also `TRUE`) -- so this is NOT a dead end on real hardware, unlike a machine without BC support would be. (2) **No CNA backend -- not just WebGPU -- has ever done real native GPU-side block-compressed texture upload at all.** `Texture2D.cpp`'s own DXT1/3/5 handling (`Texture2D::FromStream()`) always fully CPU-decompresses DXT-compressed source bytes to plain RGBA8 via `DxtUtil::DecompressDxt1()` before ever reaching a backend (see `examples/dxt1_texture_test.cpp`/`cna_test_dxt1_texture`, EasyGL-only) -- Vulkan, D3D9/11/12, Bgfx, SDL_GPU all only ever see RGBA8 `ImageData`, never a real `VK_FORMAT_BC1_...`/`DXGI_FORMAT_BC1_...`-style compressed GPU texture. The common `CNA::Internal::Graphics::ImageData` struct (`include/CNA/Internal/Graphics/ImageData.hpp`) has NO field at all for a surface format or a compressed-bytes flag -- it is hardcoded `std::vector<uint8_t> pixels; // RGBA8`. Closing this row genuinely as originally worded (a real compressed upload reachable from actual game code, not a synthetic direct-to-`IGraphicsBackend` test bypassing the whole XNA layer) needs a real, cross-backend common-interface change to `ImageData` (or a parallel `CreateCompressedTexture()`-style entry point) PLUS a `Texture2D.cpp` change to stop always-decompressing DXT source data -- both explicitly out of this task's backend-local, `Microsoft::Xna::Framework`-excluded scope, and neither verified against any other backend as this task's own constraints require before a common-interface change. A WebGPU-only implementation reachable only via a hand-crafted direct `IGraphicsBackend::CreateTexture()` call with raw BC1 bytes stuffed into the RGBA8-typed `pixels` field (bypassing `ImageData`'s own type contract) was considered and rejected as exactly the kind of "silently-wrong/overreaching" shortcut this project's culture avoids -- it would not be reachable by any real game and would misrepresent `ImageData`'s own documented contract. Left open for a dedicated follow-up task scoped to include the `ImageData`/`Texture2D.cpp` design work, not a backend-only fix. |
| WEBGPU-112 | Texture3D: `WGPUTextureDimension_3D` + layered upload | ✅ | Same `WebGPUTexture3DBackend` as `WEBGPU-57` (a single class covers both rows) — see that row for the full detail. `examples/webgpu_texture3d_test.cpp` (`WebGPU_Texture3D` CTest), 3/3. |
| WEBGPU-113 | TextureCube: `WGPUTexture` arrayLayerCount=6 + `WGPUTextureViewDimension_Cube` | ✅ | Same `WebGPUTextureCubeBackend` as `WEBGPU-56` (a single class covers both rows) — real `WGPUTexture` (`depthOrArrayLayers=6`) + a `WGPUTextureViewDimension_Cube` view, `SetData()` per face/level. Verified indirectly via `WebGPU_EnvMap3D` (real per-face colours correctly sampled). 2026-07-18 (follow-up task): real `GetData()` added — same staged MAP_READ-buffer/aligned-row/async-map technique as `WEBGPU-51`'s `Texture2D::GetData()`, with `origin.z=face` selecting the array layer; required adding `WGPUTextureUsage_CopySrc` to the texture descriptor (missing before this). `TextureCube::GetData()` at the XNA layer has no CPU-side shadow at all (unlike `Texture2D`) and always calls straight through to `ITextureCubeBackend`, so this is a genuine end-to-end XNA-layer test, not a backend-only one. `examples/webgpu_texturecube_getdata_test.cpp` (`WebGPU_TextureCube_GetData` CTest), 8/8: each of the 6 faces round-trips its own distinct colour with no cross-face collision, a non-origin sub-rectangle read, and a mip level 1 round trip distinct from level 0 (this project's first real per-level SetData/GetData exercise on a `TextureCube`, closing the "mip regeneration untested" gap left by `WEBGPU-56`). 2026-07-18 (follow-up task, mips/cube/compressed-textures cluster): `RenderTargetCube` (`WEBGPU-114`, a separate sibling class) and automatic mip-chain regeneration from level 0 (`WEBGPU-52`) both now real too. Now ✅. |
| WEBGPU-114 | RenderTargetCube: `WGPUTexture` cube + per-face `WGPUTextureView` as render attachment | ✅ | **DONE 2026-08-27: the two remaining gaps are now closed — per-face MSAA (Phase 1) and `mipMap=true` mip-chain regeneration (Phase 2).** Per-face MSAA: when the renderer's global `sampleCount_` > 1 each face renders into its own multisampled colour attachment and resolves into that face's single-sample layer (SIX separate single-layer MSAA textures, because WebGPU forbids a multisampled ARRAY texture, unlike Vulkan's 6-layer image; the shared depth attachment is allocated at the same sample count), `GetMultiSampleCount()` reports the applied count mirroring `RenderTarget2D`. mipMap: the colour texture carries a full chain and each face is regenerated from its resolved level 0 after that face's pass (`GenerateMipsForLayer`, the WEBGPU-52 downsample cascade, parameterized by colour format so a `surfaceFormat_`/BGRA cube target works; MSAA and mipMap compose — resolve writes level 0, then the cascade downsamples). `GetData` accepts levels 0..LevelCount-1. Verified by `WebGPU_RenderTargetCube` (18/18: Check E1/E2/E3 mip chain + genuine level-1 downsample, Check F1/F2/F3/F4 4x per-face resolve with no cross-face leak, blended edge pixels, and MSAA+mipMap combined) plus the cross-renderer `rendertargetcube_usage`/`rendertargetcube_getdata_contract` contracts (WEBGPU branch flipped to `mipMapCubeTargets` true / `mipLevel` Exact / `MipTargets::Real`). The previously-listed third gap ("SpriteBatch into an off-screen target") is RESOLVED: `QueueSprite` now derives clip space from the CURRENTLY-BOUND target (REMED-GFX-019), fixed by `WebGPU_SpriteBatch_RenderTarget`, and the letterbox-viewport edge was closed by `WEBGPU-141`(A) — the "computes from the backbuffer's logical dimensions" note below is historical. 2026-07-18 (mips/cube/compressed-textures cluster): real render-into-a-cube-face support -- `WebGPURenderTargetCubeBackend` (`IRenderTargetCubeBackend`), `WebGPURenderer::CreateRenderTargetCube()` (previously `IGraphicsBackend`'s own nullptr-returning default). One shared 6-array-layer `WGPUTexture` (mirrors `WebGPUTextureCubeBackend`'s own layout) + one shared `size`x`size` `Depth24PlusStencil8` texture reused across all 6 faces (only one face is ever bound at a time, mirroring `VulkanRenderTargetCubeBackend`'s identical shared-depth-image choice) + one `WGPUTextureViewDimension_2D` view per face (`faceViews_[face]`, the render-pass colour attachment) + one `WGPUTextureViewDimension_Cube` view spanning all 6 layers (`cubeView_`, for sampling back). Extended this backend's pre-existing `RenderTarget2D` "eager-flush-on-target-switch" design (`WEBGPU-53`/`54`) to a 3rd target kind: `WebGPURenderer::FlushCurrentRenderTarget()` (a new shared entry point factored out of the old `SetRenderTarget2D`-only inline logic) + `currentRenderTargetCubeFace_`/`currentRenderTargetCubeFaceIndex_` let a cube face act as its own distinct "target identity" for that flush/switch logic, so switching directly between two DIFFERENT faces (not just face↔backbuffer) still correctly flushes the outgoing face's own render pass first -- verified explicitly (`WebGPU_RenderTargetCube` Check A). Also added a new small `IWebGPUCubeSamplable` interface (the cube-map sibling of the pre-existing `IWebGPUSamplable`) so `EnvironmentMapEffect.EnvironmentMap` correctly resolves EITHER a `WebGPUTextureCubeBackend` OR a `WebGPURenderTargetCubeBackend` via a safe `dynamic_cast` (`ResolveCubeSamplable()`) -- before this, `GpuDrawParams::envMap` was hardcoded to the concrete `WebGPUTextureCubeBackend` type only, so a `RenderTargetCube` bound as an env map would have silently fallen back to the 1x1 white cube. `examples/webgpu_rendertargetcube_test.cpp` (`WebGPU_RenderTargetCube` CTest), 12/12: 6 faces bound directly face-to-face (no intervening backbuffer switch) each read back their own distinct `Clear()` colour with no cross-face aliasing; a real `BasicEffect`/`VertexPositionColor` 3D draw into one face; an `EnvironmentMapEffect` sampling round trip proving the new `IWebGPUCubeSamplable` wiring; and the critical "an intervening cube-face-targeted `Clear()` must not leak into the backbuffer's own render pass" architecture check (mirrors `WEBGPU-53`/`54`'s own Check E). (Historical: the 2026-07-18 landing of this row deliberately deferred mip regeneration and per-face MSAA as scope cuts, which is why Checks E/F once asserted `mipMap=true` throwing and `MultiSampleCount` reporting 0 — both were closed 2026-08-27 as described above and those checks were rewritten to their real behaviour.) The 2026-07-18 note about `QueueSprite()` computing clip space from the backbuffer's logical dimensions is HISTORICAL — that was fixed by REMED-GFX-019/`WEBGPU-141`(A) (see the top of this row). |
| WEBGPU-115 | `FillMode::WireFrame`: report as unsupported and refuse the draw (no polygon mode in wgpu-native) | ✅ | **DONE 2026-08-04 — post-audit checkpoint blocker RESOLVED.** The row's original scope ("document as unsupported") was no longer sufficient once the path was measured: `SupportsCapability(WireFrame)` returned **`true`** (inherited `IGraphicsBackend` default -- the backend never overrode it), `ApplyRasterizerState` accepted with no throw/warning/log, `command.wireframe` was captured at **10** queue sites (the exit reconciliation's "11" was one high; the eleventh `wireframe` reference is `DrawInstancedPrimitivesEx`'s own capture, already counted), folded into `Make3DPipelineKey`, and a **distinct `WGPURenderPipeline` was created and natively submitted** -- returning a frame **byte-identical to Solid** (`total=18176 interior=1089/1089 AB=298 BC=310 CA=329` for both). Prose in `docs/webgpu-renderer.md` is unreachable from the public API and was contradicted by the query, so nothing accepted the deviation. **Fix, backend-local:** (1) `WebGPURenderer::SupportsCapability` now answers `false` for `WireFrame`, every other capability unchanged; (2) `RequireSupportedFillModeEXT(primitive, route)` throws `System::NotSupportedException` from the top of the five public 3D draw entry points -- `DrawColoredPrimitives`, `DrawIndexedColoredPrimitives`, `DrawPrimitivesEx`, `DrawIndexedPrimitivesEx`, `DrawInstancedPrimitivesEx` -- the narrowest boundary all eleven `Queue*Draw()` families pass through. `ApplyRasterizerState` still STORES the fill mode and still succeeds (a state setter cannot know whether a draw follows -- `REMED-GFX-DECL-GUARD`'s precedent). **Only polygon topologies are refused:** a line or point list has no interior for a fill mode to select, and both fill modes were measured byte-identical there, so refusing them would delete a correct draw (found by `PointListPrimitiveTest.PointListIsNotAffectedByTriangleCulling`). **Measured after:** queued commands +0, pipeline caches +0, native draws 0, submits attributable to the draw 0, target unchanged (`total=0`), uncaptured native errors 0, no device loss; Solid recovery = exactly 1 draw, 1 submit, 0 new pipelines. The `wireframe` bit stays in `Make3DPipelineKey` and is now **unreachable** -- no accepted path computes a key with it set. Tests: `WebGpuWireFrameContract.*` (8 tests: capability, pre-queue cardinality, 12-route matrix, alternation, lifetime, native validation scopes, topology boundary, exception type), `GraphicsDeviceCapabilityTest.WireFrameIsRefusedDeterministicallyOnThisBackend` (replaces `WireFrameSilentlyRendersSolidGeometryOnThisBackend`, which was written to fail the day this landed), `examples/webgpu_graphicsstate_test.cpp` Check G (backbuffer route, 14/14). WebGPU principal suite 5909 ran / 5876 passed / 28 skipped / 5 failed -- exactly the recorded baseline residuals. ASan+UBSan clean. |
| WEBGPU-116 | WebGPU vertex format helper: `WGPUVertexFormat WebGPUVertexFormatFromVEF(VertexElementFormat)` (mirror Task 248) | ✅ | Done 2026-08-26. Added `WebGPUVertexFormatFromVEF()` in `WebGPURenderer.cpp` (anon namespace, beside `ToWGPUCullMode`/`ToWGPUBlendFactor`), a full 12-value switch over `VertexElementFormat` mirroring `VulkanRenderer`'s `ToVkVertexFormat` -- with the one intentional deviation that `Color`→`Unorm8x4` (WebGPU has no B8G8R8A8 vertex format; this renderer stores colour RGBA and the shader reads it straight, so no swizzle-format is needed, unlike Vulkan's `VK_FORMAT_B8G8R8A8_UNORM`). Made it **live and verified, not dead code**: every one of the ~55 hardcoded `attributes[i].format = WGPUVertexFormat_*` literals across all 10+ pipeline builders now routes through it (`Vector2/3/4`, `Color`, `Byte4` are the 5 formats the packed layouts actually use). Verified on the real RADV GPU (`:0`): the full WebGPU CTest set is unchanged at 81/82 (only the pre-existing `WebGPU_Clear_Readback`/`WEBGPU-141` still fails) -- a wrong ordinal→format mapping would garble the matching 3D/sprite/skinned/instanced draw, so the suite is the discriminating test. The other 7 formats (`Single`, `Short2/4`, `NormalizedShort2/4`, `HalfVector2/4`) complete the enum for parity but have no live pipeline consumer yet. |

---

## Phase 69 — WebGPU: documentation and future (Emscripten/WASM)

| #   | Task                                                                                                          | Status | Notes                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------- |
| WEBGPU-117 | `docs/webgpu-renderer.md`: architecture, deviations from Vulkan, WGSL shader map, UBO layout | ✅ | Revised 2026-08-26 (WEBGPU-137): the document now carries only verified claims, an accurate Important-limitations list pruned to what is genuinely open, the browser/Emscripten section, and in-repo `cmake-build-webgpu/` build + `ctest` commands (no `/tmp`, no `-j1`). The earlier "must be revised after `WEBGPU-124`–`WEBGPU-131`" is satisfied. |
| WEBGPU-118 | `docs/webgpu-vs-vulkan-deviations.md`: push constants → UBO, no wireframe, async→sync strategy | ✅ | Done 2026-08-26. Created `docs/webgpu-vs-vulkan-deviations.md`, the single place the WebGPU renderer's deliberate departures from the Vulkan renderer are collected: push constants → per-draw uniform `WGPUBuffer` (`WEBGPU-12`); `FillMode::WireFrame` refused not emulated (`WEBGPU-115`); async adapter/device/map callbacks pumped synchronously via `wgpuInstanceProcessEvents` natively and `emscripten_sleep`/Asyncify in the browser (`WEBGPU-119`–`122`); `VertexElementFormat::Color` → `Unorm8x4` rather than a BGRA vertex format (`WEBGPU-116`); `SetStringMarkerEXT()` no-op (`WEBGPU-106`); MRT/occlusion unimplemented and reported false (`WEBGPU-134`/`135`); windowing/fullscreen handled by the shared platform (`WEBGPU-109`). |
| WEBGPU-119 | Emscripten target: configure CNA for `emcc` build; WebGPU backend routes to browser `navigator.gpu` | ✅ | 2026-08-26. `ThirdPartyWebGPU.cmake` `if(EMSCRIPTEN)` links Emscripten's **emdawnwebgpu** port (`--use-port=emdawnwebgpu`). **Deviation:** the task said `-sUSE_WEBGPU=1`; that built-in was removed in Emscripten 4.0.10 and replaced by the emdawnwebgpu port, whose `webgpu.h` matches wgpu-native v29's unified header. Verified end-to-end: `cna_demo_2d` links and runs in headless Chrome, reaching `navigator.gpu` through the port (see `WEBGPU-122`). |
| WEBGPU-120 | Emscripten: SDL3 Emscripten port + WebGPU surface (canvas selector) | ✅ | 2026-08-26. `CreateSurface()` `#elif defined(__EMSCRIPTEN__)` uses `WGPUEmscriptenSurfaceSourceCanvasHTMLSelector` on `"#canvas"` (SDL3's default canvas). **Deviations:** the task named `emscripten_webgpu_get_device()`; instead the existing `wgpuInstanceRequestAdapter`/`wgpuAdapterRequestDevice` path is reused with an `emscripten_sleep`/Asyncify yield, one code path for native and web. Web-only follow-ons found by the browser run and fixed here: device-lost callback needs a valid `mode` (emdawnwebgpu rejects 0); no `wgpuSurfacePresent` (browser presents on rAF); depth/MSAA resync to the acquired canvas-sized texture on resize; and `PlatformRendererSurfaceState` relaxed to accept the pointerless `Web` surface. |
| WEBGPU-121 | Emscripten: verify all 9 WGSL shader pairs compile in browser via `createShaderModule` | ✅ | 2026-08-26. Every shader family compiles AND renders correctly in headless Chrome, verified by an early pixel check that a failed `createShaderModule` could not produce: the 2D sprite pair; the 3D `BasicEffect` path (`cna_house3d_demo`); and -- via `cna_webgpu_*_page` demos built from the native effect suites (`modules/renderers/webgpu/examples/CMakeLists.txt`) -- `PbrEffect` (metallic-roughness BRDF: "ambient white"/"BRDF energy" PASS), `EnvironmentMapEffect` (cube-map sampling: "correct cube face (cyan)" PASS), `SkinnedEffect` (bone palette: ambient/lit/both PreferPerPixelLighting PASS), `DualTextureEffect` ("texture0 doubled+clamped × texture1 renders white" PASS), and `AlphaTestEffect` (alpha>ref passes / alpha<ref discards PASS). NOTE: those effect pages' *later* readback-based checks fail on `WEBGPU-133` (a Dawn-strict surface-texture issue under their multiple-readback-per-frame pattern), which is NOT a shader-compilation failure and does not affect normal rendering. |
| WEBGPU-122 | Emscripten: run 2D smoke test in headless Chrome | ✅ | 2026-08-26. `scripts/run-webgpu-browser-test.sh` serves the build and drives `cna_demo_2d --smoke 120` through headless Chrome (`--enable-features=Vulkan,WebGPU --use-angle=vulkan --enable-unsafe-webgpu`; SwiftShader has no WebGPU adapter here, the real AMD Vulkan path does). PASS = 120 SpriteBatch frames, audio, no WebGPU validation error, clean teardown; a canvas resize mid-run is covered by the opt-in `CNA_WEBGPU_VALIDATION_SCENE=1` variant. |
| WEBGPU-123 | Cross-backend pixel comparison: same scene rendered on another backend and WebGPU — assert pixel-level parity | ✅ | 2026-08-26. Verified across FOUR backends (`cna_diag_{webgpu,software,easygl,vulkan}`, all the same renderer-agnostic scene): **VULKAN (native, real AMD Radeon 780M / RADV, run on the real `:0` display with the owner's explicit go-ahead) vs WEBGPU (browser) = max diff 0 -- BYTE-IDENTICAL** (both bottom out on the same AMD Vulkan: Chrome's WebGPU is Dawn-on-Vulkan). WEBGPU vs SOFTWARE and VULKAN vs SOFTWARE are each max 1 / mean 0.139 / 0 outliers (rounding vs the CPU rasterizer). The only cross-backend divergence is EASYGL (OPENGL33, headless Xvfb+llvmpipe), which differs from all three others at 57 triangle-EDGE coverage pixels -- a GL fill-rule / pixel-centre convention at the boundary, not a WebGPU defect. Only the Bgfx leg the task also names is un-run (a 5th backend; the parity picture is settled). WEBGPU is now wired into the shared cross-renderer diagnostic harness: `cna_diag_webgpu` builds the renderer-agnostic `cross_renderer_diagnostic_scene` (one unlit vertex-colour triangle -> 64x64 RGBA8) for WEBGPU, native and Emscripten, exactly as `cna_diag_easygl`/`cna_diag_software` do. **Verified parity WEBGPU (browser) vs SOFTWARE (native CPU rasterizer):** `scripts/run-webgpu-parity-test.sh` runs the Emscripten `cna_diag_webgpu` in headless Chrome, extracts its MEMFS dump, and diffs it against `cna_diag_software`'s dump with `cna_diag_compare` -- **max per-channel diff = 1, mean = 0.139** across the whole frame (both dumps have the identical 1682 non-black pixels), well within the tool's default tolerance of 40. **PASS** (0 of 4096 pixels over tolerance). **EASYGL leg added** the same day: `cna_diag_easygl` (native `OPENGL33`, run headless under Xvfb + Mesa `llvmpipe` software GL, no real window) produced a third dump. Full pairwise result -- WEBGPU vs SOFTWARE `max=1 mean=0.139 (0 outliers)`; EASYGL vs SOFTWARE `max=253 mean=1.575 (57)`; WEBGPU vs EASYGL `max=253 mean=1.538 (57)`. **Finding:** WebGPU matches the CPU reference essentially exactly; the outlier is EASYGL, which differs from BOTH others at the SAME 57 pixels -- all verified to be triangle-EDGE coverage pixels (SOFTWARE/WebGPU include them, the GL rasterizer excludes them). That is a GL fill-rule / pixel-centre convention difference, **not a WebGPU defect** -- WebGPU is on the reference-matching side. The VULKAN leg (native `cna_diag_vulkan` on the real GPU) then closed it: byte-identical to the browser WebGPU dump, as noted above. Bgfx is the only named backend not run; the parity conclusion is settled without it. |
| WEBGPU-139 | Re-audit all 16 🟨 rows against current code + registered tests; flip the verified ones, tighten the rest (plan close-out Phase 1a). | ✅ | Done 2026-08-26. Evidence: the full WebGPU CTest set was run on the real AMD Radeon 780M / RADV GPU (`:0`, `SDL_VIDEODRIVER=x11`), **81/82 Passed** (the lone failure is `WebGPU_Clear_Readback`, tracked as `WEBGPU-141`). **Flipped 🟨→✅ (5):** `WEBGPU-16` (sampler filter/address-mode mapping -- `WebGPU_PointSampling`/`TextureFilterOrdinal`/`StockEffectSampler`/`DualTextureSlotSampler` contract tests drive Linear/Point × Clamp/Wrap/Mirror and Passed); `WEBGPU-40` (full 13-value `Blend` mapping -- `WebGPU_SpriteBatch_BlendState`/`AdditiveBlendContract`/`ColorWriteChannels` Passed); `WEBGPU-66` (all GpuDrawParams dispatch families incl. `EnvironmentMapEffect` + instancing -- `WebGPU_EnvMap3D`/`Instanced3D` + effect suite Passed); `WEBGPU-82` (per-slot 3D sampler observably changes output -- `WebGPU_StockEffectSamplerContract` Passed); `WEBGPU-117` (doc revised by `WEBGPU-137`). **Stayed 🟨 (11), notes tightened + re-dated:** `WEBGPU-1`, `WEBGPU-2` (was overstated -- only the occlusion-query class is still open, not render-target/cube/3D/effect), `WEBGPU-12`, `WEBGPU-29`, `WEBGPU-39`, `WEBGPU-59`, `WEBGPU-70`, `WEBGPU-83`, `WEBGPU-98`, `WEBGPU-108` (was thin), `WEBGPU-114`. No row was flipped from source inspection alone -- every ✅ flip is backed by a registered test I personally ran on the real GPU. |
| WEBGPU-140 | Fix the plan's stale standing "Platform scope until expanded by a completed task" paragraph (close-out Phase 1b). | ✅ | Done 2026-08-26. The paragraph still claimed "browser/Emscripten WebGPU is a separate future workstream" -- false since `WEBGPU-119`–`122`/`133` shipped and the browser path was verified in headless Chrome (2026-08-26). Rewritten to: verified on Linux x86_64 desktop (X11/Wayland) AND in a real browser (emdawnwebgpu); Windows/macOS code paths only; Android still blocked (no build route). Swept the rest of the file -- the top banner and the dated "Verified native 2D baseline (2026-07-12)" record are already honest (the latter is dated provenance) and were left unchanged. |
| WEBGPU-141 | `WebGPU_Clear_Readback` fails 3 of 10 checks on the AMD RADV GPU: a 50%-alpha sprite over the backbuffer reads back full-opacity green (`R=0 G=255 B=0`), and `AddressMode=Wrap`/`Mirror` at fractional `u≈1.14`/`1.77` sample the wrong texel (Clamp at the same `u` is correct). | ✅ | **RESOLVED 2026-08-27 -- both parts fixed, `WebGPU_Clear_Readback` is 10/10 and the full WebGPU suite 86/86 on the real RADV GPU.** **Root cause found by bisect on the real RADV GPU (`:0`), 2026-08-27 -- the earlier "adapter sensitivity / NOT a regression / out of scope" note was wrong.** It is TWO independent regressions, both from dated WebGPU-only commits, found by binary-searching clean per-commit builds between the last-green baseline (`f442a4d0e`, WEBGPU-92, 2026-07-12, 10/10) and HEAD. NOTE: the branch pulled ~2732 mainline commits into this window (BLEND2D/svg-dom/openvg/fna3d lanes), not the ~10 first estimated. **(A) Wrap/Mirror (checks H/J) -- GENUINE renderer regression. First-bad `e2e8dee99` "fix(Task REMED-GFX-072): WebGPU SpriteBatch viewport-relative clip space" (2026-07-22):** its parent `eebb222d1` is 10/10, `e2e8dee99` is 8/10 (H/J fail). WebGPU-local (touches only `WebGPUGraphicsBackend`). It added a `customViewport`/viewport-relative NDC path; that path's detection (`viewportW_ != targetWidth`, `WebGPURenderer.cpp` ~5592) MISFIRES for the default full-backbuffer on a logical-vs-physical viewport size mismatch (cf. the CNA viewport logical/physical seam), so the sprite is baked viewport-local + given a per-draw rasterizer viewport, shifting its screen geometry and therefore the interpolated `u` at the probe pixel -- `u` no longer reaches `[0,2)`, so Wrap/Mirror sample the same in-range (blue) texel as Clamp. The test's expectation (Wrap→red) is FNA-correct, so the renderer was wrong. **FIXED 2026-08-27:** the `customViewport` detection now requires the viewport to be a CONTAINED sub-region of the target (`viewportX_+viewportW_ <= targetWidth`, etc.), so the spurious oversized default (107 > 64) no longer takes the viewport-local path; the genuine sub-Viewport case (`WebGPU_SpriteBatch_CustomViewport`, still 13/13) is preserved, and `WebGPU_Clear_Readback` Wrap/Mirror now pass (9/10, full suite 85/86 -- the last failure is the alpha test issue (B)). (`e2e8dee99`'s own message claimed "WebGPU_Clear_Readback fails identically with this change stashed" -- but that was measured on **Xvfb `:99`, which has no WebGPU adapter** so the test always fails there; on the real GPU the parent passes 10/10 and this commit is the regression -- a GPU-less environment masked a real one.) **(B) Alpha (check G) -- NOT a renderer bug; the TEST is wrong. First-bad `f14c764d8` "fix(Task REMED-GFX-102): key WebGPU sprite pipelines by blend state" (2026-07-26):** parent `7038bd9b8` is 8/10 with G passing, `f14c764d8` is 7/10. It changed the sprite blend from HARDCODED straight (`SrcAlpha`/`OneMinusSrcAlpha`, WEBGPU-132's value) to DEVICE-DRIVEN (honouring the `BlendState` `SpriteBatch.Begin()` sets). **That is FNA-correct:** FNA `BlendState.AlphaBlend` is PREMULTIPLIED (`One`/`InverseSourceAlpha`, `FNA/src/Graphics/States/BlendState.cs:172`), `SpriteBatch.Begin()` defaults to it, and FNA's sprite shader outputs straight colour -- so a straight-alpha texture under default AlphaBlend gives FULL brightness in FNA, not partway. EasyGL made the identical fix (Task 956: honour the device BlendState instead of hardcoding -- `EasyGLRenderer.cpp:3579`), so the whole renderer family + FNA agree on full green here. WEBGPU-132's hardcoded-straight was the WebGPU-only DEVIATION and Check G locked it in; `REMED-GFX-102` merely aligned WebGPU with FNA/EasyGL. **So the renderer is now correct; Check G asserts non-FNA behaviour** (it expects `NonPremultiplied` attenuation while using AlphaBlend). Fix: correct the TEST -- Check G should draw with `BlendState::NonPremultiplied` (or a premultiplied texture) so alpha genuinely attenuates against the correct device-driven blend -- NOT the renderer, and this is not tolerance-widening. (WEBGPU-132's premise holds for the sampler checks, which caught the real H/J regression, but its alpha check tested a WebGPU-only deviation from FNA.) **FIXED 2026-08-27:** Check G now draws with `BlendState::NonPremultiplied` and reads back `R=0 G=128 B=0` (genuine attenuation against the correct device-driven blend). **Blast radius: WebGPU-LOCAL for both** -- each first-bad commit touches only `WebGPUGraphicsBackend`, so no `SpriteBatch.cpp`/shared change is involved; EasyGL/SOFTWARE are unaffected (and by the FNA/Task-956 reasoning would themselves give full green for check G). Both parts fixed 2026-08-27: (A) the `customViewport`-containment renderer fix, (B) the Check-G `NonPremultiplied` test fix. |
