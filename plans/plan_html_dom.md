# HTML DOM Graphics Backend — Implementation Plan

> **Status: APPROVED 2026-07-31 — the project owner requested this backend directly** ("vytvoř html
> dom gr. backend pro cna … použije to html css js dom a jiné a mělo by to být optimalizované aby to
> bylo rychlé, bude to 2d only část xna 4.0 api bude házet not yet implemented například 3d") and
> approved every design recommendation below before implementation started.
>
> **Status legend** (this project's own convention): ✅ implemented *and verified against its stated
> acceptance criteria*; 🟨 code or documentation exists but has not met those criteria; ⬜ not
> implemented.

---

## What this backend is

`HTML_DOM` renders XNA `SpriteBatch` output as **real DOM elements styled with CSS** — one pooled
`<div>` per visible sprite, positioned with a CSS `transform`, textured with `background-image` +
`background-position`, and faded with `opacity`. There is no `<canvas>` in the sprite path at all:
the browser's own compositor does the drawing.

It is CNA's **fifteenth** graphics backend and its **second browser-native 2D-only** one, next to
`CANVAS`. The difference between the two is the drawing primitive, not the scope:

| | `CANVAS` | `HTML_DOM` |
|---|---|---|
| Draw primitive | `ctx.drawImage()` into one `<canvas>` | one styled `<div>` per sprite in the DOM |
| Who rasterizes | Canvas2D, per frame, every sprite | the browser compositor, only what changed |
| Cost of a static frame | full redraw | **zero** — no JS, no repaint |
| Cost of moving a sprite | full redraw | one `transform` write, composite only |
| Texture upload | `putImageData` (cheap) | PNG re-encode to a data URL (expensive) |
| Readback | `getImageData` on the main canvas | backbuffer readback impossible (see D6) |

That trade is the whole point of the backend, and it drives every design decision below.

`EASYGL` (WebGL2) remains the default Emscripten backend; this is an alternative for 2D games that
want DOM/CSS composition — DOM-based debugging/inspection (real elements visible in devtools, not
an opaque pixel buffer), CSS filters, and a genuinely idle main thread when nothing moves.
**HTMLDOM-117 correction**: earlier text here also claimed "accessibility tooling" as a benefit --
narrowed, since it overclaimed something never actually built. Every pooled sprite `<div>` carries
no ARIA role/label, no focus handling and `pointer-events:none` (`HtmlDomSpriteBatchBackend.cpp`'s
own pool-element creation), so a screen reader gets a DOM node with zero semantic information, not
a meaningfully accessible one -- the real, current benefit is that the elements are THERE for devtools
inspection and DOM-based tooling to find at all (unlike a `<canvas>`'s opaque pixel buffer), not
that they are accessible in any assistive-technology sense. Adding real accessibility (roles,
labels, an opt-in per-sprite metadata hook a game could populate) is a genuinely new feature, not
a documentation fix, and is not scoped into this backend.

---

## Scope

**2D only.** `SpriteBatch`, `Texture2D`, `SpriteFont`, `RenderTarget2D`. **HTMLDOM-117 correction**:
this used to say "everything on the 3D surface... throws... via `NotYetImplemented()`" as if that
were the one, uniform mechanism. It is not, and read closely against the actual shared-layer code
(not assumed from this backend's own side alone), the real behavior splits into several genuinely
different mechanisms depending on exactly which 3D entry point is called:
- **Direct 3D state/draw methods** (vertex/index buffer creation, `DrawPrimitives`, depth/stencil
  clears and state toggles) are overridden by `HtmlDomGraphicsBackend` itself, and each one really
  does throw `std::runtime_error("HTML_DOM backend: … not yet implemented")` via the shared
  `CNA::Internal::Backends::NotYetImplemented()` helper — the owner's explicit instruction, and
  still accurate for this group.
- **3D resource FACTORY methods** (`CreateTexture3D`/`CreateTextureCube`/`CreateRenderTargetCube`/
  `CreateOcclusionQuery`/`CreateEffectBackend`) are NOT overridden at all (HTMLDOM-61) — they
  inherit `IGraphicsBackend`'s own shared default, which silently returns `nullptr`, not a throw.
  What actually happens through the PUBLIC API differs PER TYPE, confirmed by reading each
  constructor rather than assumed uniform: `Texture3D`'s own constructor checks `GraphicsDevice::
  SupportsCapability(Texture3D)` FIRST (`HtmlDomGraphicsBackend::SupportsCapability()` reports it
  unsupported) and throws `System::NotSupportedException` immediately, before the factory method is
  even called (REMED-CONTENT-004) — construction itself fails cleanly. `TextureCube`/
  `RenderTargetCube` have NO such constructor-level check at all: construction SUCCEEDS silently
  with a null backend, and only the FIRST actual `SetData`/`GetData` call throws `System::
  NotSupportedException` — a real, if narrower, "successful-looking construction" gap the class's
  own `SetData` comment already names. `OcclusionQuery` is more permissive still: its constructor
  also succeeds silently, and its own accessors (e.g. `IsComplete`) simply return a default value
  (`false`) when the backend is null, with no exception at any point. `Effect`'s stock/default
  constructor never touches a backend-created resource in this architecture at all; its
  compiled-bytecode constructor throws `System::NotImplementedException` UNCONDITIONALLY for every
  backend, not a HTML_DOM-specific gap. None of this differing per-type behavior is a HTML_DOM
  defect to fix here — it is a shared-layer (`Texture3D.cpp`/`TextureCube.cpp`/`OcclusionQuery.cpp`/
  `Effect.cpp`) inconsistency that affects every backend leaving these factory methods at their
  nullptr default (`CANVAS` made the identical deliberate choice in its own Phase C7), worth its own
  dedicated audit outside this plan, not something to unify unilaterally from HTML_DOM's own side.
  A DIRECT call to any of these five factory methods, bypassing the constructor-level checks
  entirely (the same "direct-backend" scenario HTMLDOM-120 audited), returns `nullptr` silently
  regardless of type.

**Emscripten-only**, hard-gated in CMake exactly like `CANVAS`: `document`, `HTMLDivElement` and
CSS do not exist in a native build.

---

## Design decisions

**1. Emscripten-only, hard `FATAL_ERROR` gate.** Same shape as `CANVAS`'s own gate
(`cmake/BackendSelection.cmake`). The `.cpp` files still compile natively (every `EM_JS` block sits
behind `#if defined(__EMSCRIPTEN__)`) so the pure-C++ logic stays unit-testable, but configuring
`-DCNA_GRAPHICS_BACKEND=HTML_DOM` without Emscripten fails loudly.

**2. Reuse SDL3's window; own the DOM overlay.** SDL3's Emscripten video driver already creates and
sizes a `<canvas>`; input, events and `SDL_GetWindowSize` all keep working. This backend adds its
own `<div id="cna-dom-root">` absolutely positioned over that canvas element. The canvas remains in
layout and pointer hit testing but its pixels are hidden with `opacity:0`; the DOM overlay uses
`pointer-events:none`, so SDL continues receiving input. Everything this backend draws lives inside
that div.

**3. One pooled `<div>` per sprite, recycled across frames — never rebuilt.** The pool is an array
of elements appended once, in order, to the root container. Document order *is* paint order for
absolutely positioned siblings, so painter's-algorithm ordering comes for free with no `z-index`
bookkeeping. A frame activates a prefix of the pool: sprite *n* of the frame always lands on pool
element *n*. Elements past the frame's sprite count are hidden, and only those that were visible in
the previous frame are touched.

**4. Per-element style diffing; only `transform` and `opacity` in the steady state.** Every pool
element caches its last applied style values JS-side. A frame writes a property only when the value
actually changed. `transform` and `opacity` are compositor-only properties — a frame that just
moves sprites triggers no layout and no repaint. `will-change: transform` keeps each element on its
own compositor layer. Nothing in the draw path ever *reads* layout (no `getBoundingClientRect`), so
no forced synchronous reflow is possible.

**5. One JS call per batch, not per sprite.** `Draw()` appends a fixed-stride 80-byte POD command
into a C++ `std::vector`; `End()` hands the whole array to a single `EM_JS` call that walks it
through `HEAP32`/`HEAPF32` views. A 2000-sprite frame costs one wasm→JS boundary crossing, not
2000. This is the single most important performance decision in the backend.

**6. Textures become PNG data URLs, cached, regenerated only on `SetData`.** CSS
`background-image` needs a URL. The only *synchronous* canvas→URL route in a browser is
`canvas.toDataURL()` (`toBlob`/`convertToBlob` are async and would reorder draws). **HTMLDOM-117
correction**: the encode does NOT happen "once at upload time" as this decision used to claim --
`cnaDomEnsureUrl` (`HtmlDomTextureBackend.cpp`) is called from the sprite-flush path
(`HtmlDomSpriteBatchBackend.cpp`), not from texture creation/`SetData` (`CNA_HtmlDom_CreateTexture`/
`CNA_HtmlDom_UpdateTexture`) at all -- the encode is genuinely LAZY, paid the first time a DRAW
actually needs a given texture/tint/mode variant's URL, then cached (idempotent: `cnaDomEnsureUrl`
checks `variant.url` first and returns immediately if already encoded). `SetData`'s own cost is
cheap by contrast -- a `putImageData` raster write plus dropping this texture's now-stale cached
variants (`cnaDomVariantCacheClearOwner`) -- it does not itself encode anything; that cost is
deferred to whichever later draw first needs the regenerated variant's URL. **Consequence, stated
precisely:** a texture that is uploaded (or re-uploaded via `SetData`) but never actually drawn
never pays the encode cost at all; a texture re-uploaded and re-drawn every frame re-encodes every
frame, which is slow. This backend is for static sprite sheets; per-frame `Texture2D::SetData`
belongs on `CANVAS` or `EASYGL`.

**7. Exact tint and blend via cached texture *variants*, not CSS filters.** A CSS `filter` chain
cannot express XNA's `color` tint exactly, and `CANVAS`'s own external review found that
compositing tricks silently produce wrong math. So each needed variant of a texture is generated
once by an exact per-pixel canvas pass and cached under its own data URL:
- *straight* — as uploaded (`NonPremultiplied`),
- *un-premultiplied* — RGB ÷ alpha (`AlphaBlend`, whose `srcBlend=One` contract assumes already
  premultiplied source data, exactly as `CANVAS` concluded from `SDL_RENDERER`'s Task 697 test),
- *alpha-stripped* — every alpha forced to 255 (`Opaque`, whose `srcBlend=One`/`dstBlend=Zero`
  ignores source alpha entirely),
- *tinted(r,g,b)* — RGB × tint, alpha untouched, on top of whichever of the three applies.

The variant cache is LRU-capped (256 entries) so an animated per-frame tint degrades into cache
churn rather than unbounded memory growth.

**8. `Additive` maps to `mix-blend-mode: plus-lighter`.** That is CSS Compositing's exact
`lighter` operator — the same one `CANVAS` uses. Any `BlendState` outside the four standard presets
throws, same honest boundary `CANVAS` drew: CSS has no generic blend-factor/equation model.

**9. `Clear()` resets the frame.** `Clear()` sets the root container's `background-color` and drops
every sprite queued so far this frame (pool cursor back to 0). That is exactly XNA's semantics —
`Clear` overwrites everything already drawn — expressed in retained-mode terms. Called while a
render target is bound, it clears that target's canvas instead.

**10. `RenderTarget2D` is backed by a real off-screen `<canvas>` (hybrid).** DOM has no render
targets, and refusing them would break every 2D game that uses one. While a target is bound, draws
route through a Canvas2D path (`drawImage`, the same geometry `CANVAS` derived); when the target is
later sampled as a texture, its data URL is regenerated lazily from a dirty flag. `Present()` and
the DOM sprite path are untouched by this.

**11. Backbuffer readback is impossible and says so.** There is no browser API to rasterize a live
DOM subtree to pixels. `ReadBackbuffer` throws unless a render target is bound (in which case it
reads that target's canvas for real). This is the one capability `CANVAS` has and `HTML_DOM`
genuinely cannot — stated here, in `docs/html-dom-backend.md`, and in the exception message.

**12. `SpriteFont` needs zero backend code.** Every glyph funnels through the same
`ISpriteBatchBackend::Draw` overload; kerning, line spacing and glyph-order mirroring are all
shared, backend-agnostic `SpriteFont.cpp` logic — the same conclusion `CANVAS` reached.

---

## Phases and tasks

### D1 — Build wiring and skeleton

| Task | Status | Description |
|---|---|---|
| HTMLDOM-1 | ✅ | `CNA_GRAPHICS_BACKEND=HTML_DOM` option, cache STRINGS entry, `CNA_BACKEND_HTML_DOM` option and define (`cmake/BackendSelection.cmake`). |
| HTMLDOM-2 | ✅ | Emscripten-only `FATAL_ERROR` gate (design decision 1). |
| HTMLDOM-3 | ✅ | `BACKEND_DIR`/`BACKEND_TARGET` = `src/CNA/Internal/Backends/HtmlDom` / `cna_backend_graphics_html_dom`; links SDL3 only (`cmake/BackendLibraries.cmake`). |
| HTMLDOM-4 | ✅ | `GraphicsBackendType::HtmlDom` + name mapping + `getCurrentGraphicsBackendType()` branch. |
| HTMLDOM-5 | ✅ | `HtmlDomGraphicsBackend` class + `CreateGraphicsBackend()` factory. |
| HTMLDOM-6 | ✅ | `SupportsCapability()` → false for every `GraphicsCapability` (2D-only), `SupportsDepthStencil()` → false. |
| HTMLDOM-7 | ✅ | 2D-only backend guards in the shared example/GTest files that enumerate such backends. |

### D2 — DOM surface, Clear, Present, viewport

| Task | Status | Description |
|---|---|---|
| HTMLDOM-10 | ✅ | `CNA_HtmlDom_EnsureRoot()` — creates `<div id="cna-dom-root">` over SDL's canvas, hides the canvas, sets `overflow:hidden`/`transform-origin:0 0`/`contain:strict`. |
| HTMLDOM-11 | ✅ | `Clear()` → root `background-color` + frame reset (design decision 9); routes to the bound render target when one is bound. |
| HTMLDOM-12 | ✅ | `Present()` → flush any pending batch, hide unused pool elements, apply the logical→physical container scale. Nothing to swap: the compositor presents. |
| HTMLDOM-13 | ✅ | `GetViewportSize`/`SetVirtualResolution`/`SetPresentationMode` — the same `FixedHeightDynamicWidth` logical-size math every other backend uses. |
| HTMLDOM-14 | ✅ | `TransformWindowToLogical`/`TransformLogicalToWindow` for correct mouse mapping under scaling. |
| HTMLDOM-15 | ✅ | Structural smoke test (`examples/htmldom_smoke_test.cpp`) + `cmake/Tests/HtmlDomTests.cmake`. |

### D3 — Textures

| Task | Status | Description |
|---|---|---|
| HTMLDOM-20 | ✅ | `HtmlDomTextureBackend` — off-screen canvas per texture, integer id in `Module['cnaDomTextures']`, PNG data URL generated once (design decision 6). |
| HTMLDOM-21 | ✅ | `UpdatePixels` re-uploads and invalidates every cached variant of that texture. |
| HTMLDOM-22 | ✅ | `UpdatePixelsLevel(level>0)` throws — no mip chain exists here (same boundary as `CANVAS`/`SDL_RENDERER`). |
| HTMLDOM-23 | ✅ | Variant generation + LRU cache (straight / un-premultiplied / alpha-stripped / tinted), design decision 7. |
| HTMLDOM-24 | ✅ | `Texture2D::GetData` on a plain texture is served by the shared CPU shadow — no backend work needed. Closed by HTMLDOM-96a (D14, below), which added a byte-exact browser check for exactly this path — this row's own status was left stale at the time and is corrected here. |

### D4 — SpriteBatch

| Task | Status | Description |
|---|---|---|
| HTMLDOM-30 | ✅ | Fixed-stride draw-command buffer, single `EM_JS` flush per batch (design decision 5). |
| HTMLDOM-31 | ✅ | Element pool with per-index reuse and prefix activation (design decision 3). |
| HTMLDOM-32 | ✅ | Per-property style diffing (design decision 4). |
| HTMLDOM-33 | ✅ | Sprite geometry: `translate(destX,destY) rotate(θ) scale(destW/sw,destH/sh) translate(-originX,-originY)` — places `origin` (source-pixel space) exactly at `(destX,destY)`, invariant under rotation, matching FNA's `GenerateVertexInfo`. |
| HTMLDOM-34 | ✅ | `SpriteEffects` flips mirror about the sprite's own local centre, leaving the destination footprint unchanged (real XNA semantics). |
| HTMLDOM-35 | ✅ | `sourceRectangle` → `background-position` + element size; source rects are clamped into the texture. |
| HTMLDOM-36 | ✅ | `Begin(transformMatrix)` → a CSS `matrix(M11,M12,M21,M22,M41,M42)` on a batch wrapper element, composed under each sprite's own transform. |
| HTMLDOM-37 | ✅ | `SetCustomEffect(non-null)` throws — no programmable shader stage exists. |
| HTMLDOM-38 | ✅ | `SpriteFont::DrawString` needs no backend-specific code (design decision 12) — every glyph funnels through the same `Draw` overload. Verified in the browser run: a one-glyph font's `DrawString` produces a real DOM element, textured from a generated PNG data URL, sized from the glyph's own atlas bounds. |

### D5 — Blend and sampler state

| Task | Status | Description |
|---|---|---|
| HTMLDOM-41 | ✅ | `BlendStateToDomCompositeOp()` — pure function, unit-tested without a DOM; throws for non-preset states (design decision 8). |
| HTMLDOM-42 | ✅ | `Opaque` → alpha-stripped variant; `AlphaBlend` → un-premultiplied variant; `NonPremultiplied` → straight; `Additive` → `mix-blend-mode: plus-lighter`. |
| HTMLDOM-43 | ✅ | Colour tint → cached tinted variant; alpha → `opacity` (free). |
| HTMLDOM-44 | ✅ | `TextureFilter` → `image-rendering: pixelated` vs `auto`, using the same magnification-dominant grouping `SDL_RENDERER` Task 701 and `CANVAS` CANVAS-42 use. |
| HTMLDOM-45 | ✅ | `Clamp` is exact and unit-tested; `Mirror` and mixed per-axis modes throw (unit-tested). `Wrap` maps to CSS background repetition on the DOM path — verified in the browser run: a texture drawn with a source rectangle double its own size under `SamplerState.PointWrap` keeps its full (unclamped) element width and gets `background-repeat: repeat`. The render-target path's separate Canvas2D-repeating-pattern implementation is verified too, pixel-exact: a 2x2 source tiled into a 4x4 render target under Wrap reads back with every texel matching `source(x%2, y%2)` exactly. **Superseded by HTMLDOM-97 (Phase D15)**: "Mirror and mixed per-axis modes throw" was this task's original, narrower scope — symmetric `Mirror` and non-`Mirror` mixed per-axis modes (e.g. U=Wrap, V=Clamp) are both real, pixel-verified now; only `Mirror` mixed with a DIFFERENT mode on the other axis still throws. |
| HTMLDOM-46 | ✅ | `ColorWriteChannels`/`MultiSampleMask` documented as inexpressible in CSS compositing. |

### D6 — Render targets

| Task | Status | Description |
|---|---|---|
| HTMLDOM-50 | ✅ | `HtmlDomRenderTargetBackend` — off-screen canvas, `Bind`/`UnbindAsRenderTarget` (design decision 10). |
| HTMLDOM-51 | ✅ | Draws route to Canvas2D while a target is bound; back to the DOM path on unbind. |
| HTMLDOM-52 | ✅ | Target content is re-published as a data URL lazily, from a dirty flag set on every bind. |
| HTMLDOM-53 | ✅ | `GetData` — real synchronous `getImageData`, with the full argument validation contract (REMED-GFX-127). |
| HTMLDOM-54 | ✅ | `HasRealDepthBuffer()` → false; `SetRenderTargets(count>1)` and cube-face bindings throw. |
| HTMLDOM-55 | ✅ | `ReadBackbuffer` throws for the DOM backbuffer, reads for real from a bound target (design decision 11). |

### D7 — The 3D surface

| Task | Status | Description |
|---|---|---|
| HTMLDOM-60 | ✅ | Every depth/stencil clear, `SetDepthTestEnabled`/`SetBlendEnabled`/`SetDepthWriteEnabled`, vertex/index buffer creation and `Draw*Primitives` throws `"HTML_DOM backend: … not yet implemented"`. |
| HTMLDOM-61 | ✅ | `CreateTexture3D`/`CreateTextureCube`/`CreateRenderTargetCube`/`CreateOcclusionQuery`/`CreateEffectBackend` keep `IGraphicsBackend`'s own null/throw defaults — the same choice `CANVAS` made deliberately in Phase C7. |

### D8 — Verification and documentation

| Task | Status | Description |
|---|---|---|
| HTMLDOM-70 | ✅ | GTest coverage for every pure-C++ unit: blend mapping, address-mode validation, command encoding, 3D throw surface. |
| HTMLDOM-71 | ✅ | Real Emscripten build (`emcmake`, emsdk 6.0.5). |
| HTMLDOM-72 | ✅ | **Real browser verification** — 24/24 checks pass in headless Chromium via Playwright, asserting against the DOM/pixels the backend actually produced (surface, clear colour, sprite transforms/sizes/opacity/data-URL backgrounds, element recycling, a `RenderTarget2D` readback round-trip, the backbuffer refusal, `SpriteFont::DrawString`, `TextureAddressMode::Wrap` on both the DOM path and the render-target's separate Canvas2D-pattern path, the latter checked pixel-exact). This is what `CANVAS` could never do in its own dev loop — and it earned its keep immediately: see the note below. |
| HTMLDOM-73 | ✅ | `docs/html-dom-backend.md` capability/limitation matrix. |

### D9 — Closing the gap between "implemented" and "verified"

The owner reviewed D1-D8 and pushed back: 24 browser checks is real coverage, but it is not
exhaustive, and several rows marked ✅ above were verified only in the sense of "the code exists,
was reviewed, and nothing threw" — not "the actual rendered output was checked against a
hand-derived expected value." This phase closes that gap, item by item, against the owner's own
list. Several items required a real design decision before they could be scoped as tasks at all;
those decisions are recorded inline.

| Task | Status | Description |
|---|---|---|
| HTMLDOM-80 | ✅ | Implemented as `clip-path: inset(...)`, applied unconditionally (matching `SDL_RENDERER`'s own `RasterizerState.ScissorTestEnable`-independent behaviour). Verified in `htmldom_smoke_test.cpp` with three cases: a rect fully inside the surface maps to the exact `inset(top,right,bottom,left)` values; the full-surface rect produces an all-zero (no-op) inset; a rect extending past the surface's own bounds clamps its far insets to 0 instead of producing a negative (expanding) `inset()`. Verification compares parsed numeric inset values rather than the raw CSS string, since the CSSOM normalizes `inset(0px 0px 0px 0px)` down to the `inset(0px)` shorthand when all four values are equal — caught by the first attempt's own failing run, not assumed. **Original scope was honestly whole-surface/current-value (applied to a single `clip-path` on `#cna-dom-root`); superseded by HTMLDOM-94 (Phase D12) below**, which replaced that single clip with per-scissor-rect regions so an earlier batch's clip survives a later batch's different rect — the three cases this task verified still hold, now against a region's own clip-path instead of root's. |
| HTMLDOM-81 | ✅ | **Confirmed non-gap, documented rather than implemented.** No 2D-only sibling backend implements `SetViewport` either (`SDL_RENDERER`, `CANVAS`, `DX3` all leave it as the inherited no-op; only 3D-capable backends — `EASYGL`, `D3D11`/`D3D12`/`D3D9`, `Bgfx`, `SdlGpu`, `Headless`, `Software` — override it, since `Viewport` is fundamentally the NDC-to-screen transform a rasterizer pipeline needs and this backend has none). Downgraded from "gap" to "confirmed non-gap, matching every comparable sibling" — the reasoning is on record here rather than silently assumed, which is what "done" means for this task. **Superseded by HTMLDOM-98 (Phase D15)**: this task's own non-gap finding was scoped against 2D-only siblings only, stated explicitly at the time — a real, closeable gap against `EASYGL` specifically remained, and is now implemented; see HTMLDOM-98. |
| HTMLDOM-82 | ✅ | Verified pixel-exact in `examples/htmldom_pixel_verification_test.cpp` with two deliberately unambiguous (no rotation sign-convention risk) matrices: a pure translation `Matrix::CreateTranslation(6,8,0)` moves a 1x1 sprite from the origin to exactly (6,8); a pure `Matrix::CreateScale(2)` renders it as a 2x2 block instead of 1x1. Both drawn into a bound render target, exercising `transformMatrix` on the Canvas2D `targetCtx` branch specifically — a separate code path from the DOM branch the smoke test's own CSS-string check covers. |
| HTMLDOM-83 | ✅ | Verified pixel-exact in `examples/htmldom_pixel_verification_test.cpp`: cleared RT A to a distinct colour, unbound it, `Draw()` RT A itself (an ordinary `RenderTarget2D : Texture2D`) onto RT B, unbound, read B back — the sampled region matches RT A's real content exactly (within 1-unit rounding) and the surrounding area stays untouched. Confirms the data-URL regenerated-from-a-dirty-flag path (design decision 10) round-trips real content, not just that the call sequence doesn't throw. |
| HTMLDOM-84 | ✅ | Colour tint verified pixel-exact in the browser: a (200,150,100,255) texel tinted (255,128,64,255) under `AlphaBlend` over a transparent target reads back (200,75,25) — exactly `round(src*tint/255)` per channel, cross-checked against the same formula computed independently in C++, not restated from the JS. See `examples/htmldom_pixel_verification_test.cpp`. |
| HTMLDOM-85 | ✅ | **The highest-risk item on this list, now closed.** A hand-premultiplied texel (straight colour (255,100,50) at alpha 128, uploaded as (128,50,25,128), mirroring `SDL_RENDERER`'s own Task 697 test) drawn under `AlphaBlend` reads back (255,100,52,128) — within the expected double-rounding tolerance (this backend's own division, plus the browser's own internal premultiply/un-premultiply round trip on readback) of the exact expected (255,100,50,128). The un-premultiply maths reconstructs the original straight colour correctly; it is not double-dividing, not skipping the divide, and not off by a swapped operand. |
| HTMLDOM-86 | ✅ | Verified with genuinely semi-transparent source data (180,90,40,120) — every prior check used alpha=255 source, making the strip a no-op every time it ran until now. Originally read back (181,89,40,255): alpha forced to 255 exactly, RGB within 1-unit rounding of the source, unaffected by the transparent destination it was drawn over — confirming the strip does not blend before forcing alpha. **Superseded by HTMLDOM-100 (Phase D15)**: that forced-255 result was itself later found to diverge from real XNA (`BlendState::Opaque` preserves source alpha exactly, confirmed via `BlendState.cpp`) — fixed on the Canvas2D render-target path (which is what this test exercises), now reading back at alpha≈120, the source's own value. This row is kept as a record of what the original implementation did, not what it does now. |
| HTMLDOM-87 | ✅ | Verified with two fully opaque, colour-disjoint draws (pure red, then pure blue) at the identical destination pixel under `BlendState.Additive`: reads back exact (255,0,255,255) — a real channel-wise sum (clamped), not a plain overwrite (which would read back as just blue) or an average (which would read back as a dim purple). |
| HTMLDOM-88 | ✅ | Verified in `examples/htmldom_pixel_verification_test.cpp` with a two-glyph, distinct-kerning, two-colour-half font: `\n` correctly resets X and advances Y by `lineSpacing` (not smearing onto the same line); same-line kerning advance places the next glyph at exactly `glyphWidth+rightBearing`, with a real gap in between; the rotation/scale/flip `DrawString` overload's `scale` parameter correctly resizes the rendered glyph (4x4 → 8x8, checked via a pixel only inside the scaled extent); its `SpriteEffects::FlipHorizontally` reverses the two halves' relative order (checked as an observable invariant — red left-of-blue vs. right-of-blue — rather than an assumed absolute position, since `DrawString`'s own flip handling shifts the whole string's anchor by `MeasureString(text).X` in addition to the per-glyph mirror, a real, previously-unknown-to-this-plan detail of the *shared* `SpriteBatch.cpp` layer, not an HTML_DOM-specific behaviour). |
| HTMLDOM-89 | ✅ | Measured in `examples/htmldom_stress_test.cpp`: 500 animated sprites/frame (position and tint both changing every frame, the genuine steady-state case), averaged over 60 frames after a warm-up frame, came to **2.338 ms/frame (~428 fps-equivalent)** in this headless-Chromium container — well inside the generous 50ms sanity threshold, confirming no accidental per-sprite JS call or O(n²) DOM cost crept in. Honestly scoped as absolute numbers on this machine, not a comparative benchmark against `CANVAS`/`EASYGL`. **HTMLDOM-111 correction**: this "2.338 ms/frame (~428 fps-equivalent)" number is submission time only (excludes `Present()` and all deferred browser rendering) and, despite this row's own "position and tint both changing" text, the workload's position formula never actually depended on `frame_` — only tint did, so this measured heavy tint churn with STATIC position, not "moving sprites". The real end-to-end frame cost (now also measured, ~19 ms/frame ≈ 53 real fps in the same container) and the corrected workload description live in HTMLDOM-111's own row; this row's own `check()` and threshold are unchanged and still pass. |
| HTMLDOM-90 | ✅ | Verified in `examples/htmldom_stress_test.cpp` with 300 frames of oscillating sprite counts (5-50/frame) and a tint continuously cycling through far more than 256 distinct values: the sprite pool stayed exactly bounded at the true peak (500, from the benchmark phase) with zero leak, and the variant LRU cache capped at exactly 256 — real eviction confirmed, not merely "never filled up". Zero console errors/exceptions over the whole run (the harness itself fails the run on any pageerror or `[FAIL]` line). |
| HTMLDOM-91 | ✅ | Verified reasoning converted into `HtmlDom3DSurfaceTest.InertStateSettersAcceptArbitraryValuesWithNoObservableEffect`: `ApplyDepthStencilState`/`SetReferenceStencil` are meaningless because `SupportsDepthStencil()` is unconditionally `false`; `SetBlendFactor`'s constant colour can only matter for `Blend.BlendFactor`/`InverseBlendFactor`, and `BlendStateToDomCompositeOp` already throws for both before `ApplyBlendState` could ever consume that colour; `ApplyRasterizerState`'s `CullMode`/`FillMode`/depth-bias fields have no 2D analogue (matching every 2D-only sibling), and its `scissorTestEnable` field is confirmed not to leak into a subsequent `ApplyBlendState` call's own composite-op state. Each setter is called with a deliberately non-default value and asserted to neither throw nor change any state a caller could later observe. |

### D10 — Comparative cross-backend benchmark

HTMLDOM-89 measured this backend's own steady-state cost in isolation. The owner asked for the
number that actually matters: how it compares to the sibling backends, measured with the identical
workload rather than compared against each backend's own separately-written test.

| Task | Status | Description |
|---|---|---|
| HTMLDOM-92 | ✅ | Added `examples/graphics_backend_benchmark.cpp` — a genuinely backend-agnostic source file (no `CNA::Internal::Backends::*` include; identifies its own backend at runtime via `CNA::getCurrentGraphicsBackendName()`) and `cmake/Tests/CrossBackendBenchmark.cmake`, gated on `EMSCRIPTEN` alone so the same source builds under any Emscripten backend configure. Same methodology as HTMLDOM-89: 500 sprites/frame, position and tint both animating every frame, 60 timed frames after one untimed warm-up frame, `performance.now()`-based. Built and run for real in headless Chromium (Playwright) against all three Emscripten-capable backends from three separate `emcmake` configures (`cmake-build-canvas`, `cmake-build-easygl`, `cmake-build-htmldom`): **`HTML_DOM` 3.7–4.3 ms/frame (≈233–270 fps-equivalent)**, **`EASYGL` 4.66 ms/frame (≈215 fps-equivalent)**, **`CANVAS` 27.6–29.1 ms/frame (≈34–36 fps-equivalent)**. `HTML_DOM` is on par with (in this run, marginally faster than) `EASYGL` and roughly 6–7× faster than `CANVAS` for this specific 500-static-texture-sprite workload. Honest caveat: `EASYGL`'s number is software-rasterized (SwiftShader/ANGLE under headless Chromium, no real GPU in this container) — a real hardware WebGL2 context would very likely widen `EASYGL`'s advantage over both 2D backends; this result should be read as "DOM compositing beats Canvas2D redraw by a wide margin, and is competitive with software-rasterized WebGL2," not as a claim that `HTML_DOM` outperforms real hardware-accelerated WebGL2. A real bug was found and fixed while getting `EASYGL`'s number: `CrossBackendBenchmark.cmake` initially omitted `-sMIN_WEBGL_VERSION=2`/`-sMAX_WEBGL_VERSION=2` (present on the pre-existing `cna_house3d_demo` EASYGL/Vulkan target but not copied to this new one), so Emscripten silently created a WebGL1 context despite `EasyGLGraphicsBackend`'s own `SDL_GL_CONTEXT_MAJOR_VERSION=3` request, which then failed to compile EasyGL's GLSL ES 3.0 shaders (`ERROR: unsupported shader version`) and aborted on `glGenVertexArrays`. Fixed by adding the same two link options, conditioned on `CNA_GRAPHICS_BACKEND STREQUAL "EASYGL"`. **HTMLDOM-111 correction**: the ms/frame numbers in this row are submission time only (excludes `Present()` and all deferred browser rendering) — real cross-backend comparisons should use HTMLDOM-111's own added end-to-end (`requestAnimationFrame`-gap) measurement instead; this row's own "6-7x faster than CANVAS" conclusion was never re-verified under that corrected methodology and should not be treated as confirmed until it is. `graphics_backend_benchmark.cpp` itself was reworked by HTMLDOM-111 (two workloads, both metrics, real `WEBGL_debug_renderer_info` capture) — re-run it per backend for a current, methodologically-honest comparison rather than reusing these numbers. |

### D11 — Window resize + active scissor rect interaction

| Task | Status | Description |
|---|---|---|
| HTMLDOM-93 | ✅ | Investigated whether a physical window resize with no `GraphicsDevice.Reset()` in between leaves a previously-set `ScissorRectangle`'s `clip-path` insets stale (computed against the old surface size, silently wrong once `#cna-dom-root` itself resizes). Two real findings, one at each layer: **(1) Framework-level: confirmed non-gap.** `GraphicsDevice::UpdateViewportFromWindow()` (called from every `GraphicsDevice::Present()`, for every backend — not HTML_DOM-specific) already resets both `Viewport` and `ScissorRectangle` to the complete new backbuffer the instant it detects any viewport-size change, matching FNA's own real-resize behaviour — so through the public XNA API this staleness can never actually manifest; by the time a game could observe it, `GraphicsDevice` has already reset the scissor itself. Same shape of finding as HTMLDOM-81's `SetViewport` one. Confirmed by first reproducing the scenario through `GraphicsDevice::setScissorRectangleProperty()`/`Present()` and observing the framework's own reset win the race, not assumed. **(2) Backend-level: a real latent gap, fixed.** `HtmlDomGraphicsBackend`'s own contract was still wrong in isolation: `SetScissorRect` computed `clip-path` insets once, using whatever `Module['cnaDomLogicalW'/'H']` held at call time, and never revisited them — a caller reaching the backend directly (bypassing `GraphicsDevice`, as e.g. a future MRT/split-screen path or a test might) would see the clip silently drift after a resize. Fixed in `HtmlDomGraphicsBackend.cpp`: `SetScissorRect` now stores the rect in logical coordinates (`Module['cnaDomScissorRect']`) instead of only its derived insets, and `CNA_HtmlDom_UpdateSurface` re-derives the `clip-path` from that stored rect (via a shared `Module['cnaDomApplyScissorClip']` helper) every time it detects the logical size changed — so the backend's own contract holds regardless of whether `GraphicsDevice` is in the loop. `EnsureRoot`/`DestroyRoot` clear the stored rect alongside the rest of the per-root state. Verified in `htmldom_smoke_test.cpp` by calling `HtmlDomGraphicsBackend::SetScissorRect`/`SetVirtualResolution(0,0)`/`Present()` directly (bypassing `GraphicsDevice`, which is what makes the fix observable at all — see the framework-level finding above): a `(5,5,10,10)` rect set against a 64×64 surface correctly re-derives to `(5,113,113,5)` after a real `SDL_SetWindowSize` to 128×128, with no `GraphicsDevice.Reset()` and no scissor reapplication. (Superseded/generalized by HTMLDOM-94 immediately below: the single root-level `Module['cnaDomApplyScissorClip']` this task added was replaced by a per-region `Module['cnaDomApplyRegionClip']`, applied to every region on resize instead of just one; the underlying resize-safety property this task established is unchanged and is what HTMLDOM-93's own two smoke-test checks now verify against a region instead of against root directly.) |

### D12 — True per-batch scissor rect (nested region containers)

Design decision 13/HTMLDOM-80 had shipped a deliberately scoped "whole-surface, current-value"
clip: one `clip-path` on `#cna-dom-root` reflecting whatever `ScissorRectangle` was *most recently*
set, applied to *every* sprite currently on screen — so a game drawing under rect A, then changing
`ScissorRectangle` to rect B and drawing more, would see the LATER rect B retroactively reclip the
EARLIER sprites drawn under rect A too. This phase closes that gap for real.

| Task | Status | Description |
|---|---|---|
| HTMLDOM-94 | ✅ | **Scope, precisely stated first.** Real XNA/FNA `SpriteBatch` in `Deferred` sort mode (this backend's only sort mode) does not itself preserve per-original-`Draw()`-call device state either: `ScissorRectangle` is read by the GPU once the batch's queued draw calls are actually issued, at `End()` — a game that changes `GraphicsDevice.ScissorRectangle` between two `Draw()` calls inside the same still-open `Begin()`/`End()` pair gets the rect current AT `End()` applied to the WHOLE batch, not a per-call value. So the correct, non-overclaiming target is per-**batch** (per `Begin()`/`End()` pair) scissor correctness, matching this backend's own existing one-flush-per-batch architecture — not literally per individual `Draw()` call, which isn't even a real XNA/FNA guarantee for `SpriteBatch.Deferred`. **Implementation.** Replaced the single flat sprite pool under `#cna-dom-root` with a small set of per-scissor-rect DOM "regions" (`Module['cnaDomRegions']`, keyed by `"x,y,w,h"`), each a `<div>` with its own `clip-path` and its own independent sprite pool/recycling state. `CNA_HtmlDom_SetScissorRect` (`HtmlDomGraphicsBackend.cpp`) now only records the current rect (`Module['cnaDomScissorRect']`) — it no longer touches the DOM. `CNA_HtmlDom_FlushSprites` (`HtmlDomSpriteBatchBackend.cpp`, called once per `SpriteBatch::End()`) resolves that rect to a region via `Module['cnaDomGetRegion']` and appends/recycles that batch's sprites into it, so an EARLIER batch's region is never touched by a LATER batch's different rect. A rect that currently covers the whole surface (checked fresh against `Module['cnaDomLogicalW'/'H']` on every call, so this stays correct across resizes with no special-casing) collapses to the SAME default `'full'` region whose container IS `#cna-dom-root` itself — so the overwhelmingly common no-scissor case costs exactly what it always did (confirmed: `cna_test_htmldom_stress`'s HTMLDOM-89 number is unchanged within noise, 3.5-3.9 ms/frame). Non-default regions persist across frames (like the sprite pool always has) and are LRU-evicted at a 16-region cap if a game genuinely cycles through many distinct rects, mirroring the texture-variant cache's own eviction shape. `Clear()`/`PresentFrame()` iterate every region instead of a single pool. `CNA_HtmlDom_UpdateSurface` re-derives EVERY region's clip on resize (generalizing HTMLDOM-93's fix from one root-level clip to N per-region ones). **Documented trade-off, not a regression.** Sprites in different regions paint in the order their regions were first CREATED, not necessarily interleaved per-frame draw order across regions — cross-region z-order is stable frame-to-frame but doesn't reconstruct true draw order for a game that interleaves draws across different scissor rects in a different sequence each frame. This is an accepted, honestly-documented boundary: before this task there was no way to keep two different scissor rects from clipping each other's sprites AT ALL, so this trades an edge-case ordering caveat for real clipping correctness in the overwhelmingly more common case (scissor rects are typically spatially disjoint UI regions that don't visually overlap, where cross-region paint order is inconsequential). **Verified in `htmldom_smoke_test.cpp`** (27 → 32 checks, rewritten HTMLDOM-80a/b/c to draw-and-flush a sprite per rect instead of checking root's clip-path with nothing drawn, since a region now only exists once a batch actually flushes under it): the money check draws sprite A under rect (10,10,20,30) in one batch, then sprite B under a reset-to-full rect in a SECOND batch, and confirms rect A's region STILL has its own correct clip and its sprite is STILL visible — completely unaffected by the second batch's different rect. HTMLDOM-93's own two checks were adapted to the new region shape (`JsRegionClipPathInsetIs` replacing `JsRootClipPathInsetIs`) with no change to what they actually prove. Full regression pass: 32/32 smoke, 11/11 pixel, 3/3 stress (measured 3.857 ms/frame, no perf regression), 33/33 GTest, and the visual demo screenshot unchanged. |

### D13 — Texture/render-target dispose verification

`HtmlDomTextureBackend::~HtmlDomTextureBackend()` (calls `CNA_HtmlDom_DestroyTexture`, deleting
`Module['cnaDomTextures'][id]`) and `HtmlDomRenderTargetBackend::~HtmlDomRenderTargetBackend()`
(unbinds itself if it was the currently-bound target) had never been exercised end-to-end in a real
browser — only reviewed by reading the source. This phase proves the JS-side registry actually
shrinks back down, not merely that the C++ looks correct.

| Task | Status | Description |
|---|---|---|
| HTMLDOM-95 | ✅ | New `examples/htmldom_dispose_test.cpp` / `cna_test_htmldom_dispose` page, 6 checks. **95a**: creating 50 `Texture2D`s registers exactly 50 new `Module['cnaDomTextures']` entries; destroying all 50 (clearing the owning vector) removes exactly those 50, none orphaned. **95b**: same shape for 20 `RenderTarget2D`s (each owns one texture canvas internally). **95c**: the LAST render target is bound (`GraphicsDevice.SetRenderTarget`), drawn into, and then the whole vector — including that still-bound target — is destroyed with **no** explicit unbind first; `CNA::Internal::Backends::HtmlDom::GetBoundRenderTargetIdEXT()` reads back `0` immediately afterward, confirming `~HtmlDomRenderTargetBackend()`'s defensive unbind genuinely fires rather than leaving the draw path pointing at a canvas that no longer exists. A normal `Clear()`+`Draw()` to the ordinary backbuffer right after (the real, observable consequence of 95c holding) completes without throwing or silently doing nothing. **Found and fixed a real bug in the TEST itself, not the backend, while writing this**: destroying a still-bound render target without first calling `GraphicsDevice.SetRenderTarget(null)` leaves `GraphicsDevice`'s own separate `renderTargetBound_` flag (unrelated to the backend's own bound-canvas-id tracking HTMLDOM-95c checks) stuck `true`, and the framework's own `GraphicsDevice::Present()` guard (`"Cannot present while render targets are bound"`) then throws on the very next frame's automatic `Game::EndDraw()`-triggered `Present()` call — a real requirement of the public API (always call `SetRenderTarget(null)` before disposing a bound target), not a defect this task needed to fix. The test now does so, deliberately positioned AFTER the 95c check so that check still exercises the destructor's own defensive unbind in isolation. **95d**: 200 rapid create-then-immediately-destroy cycles (monotonically-increasing, never-reused texture ids) leave the registry exactly where it started — bounded correctness under churn, not just a single before/after snapshot. Full regression pass after adding this: 32/32 smoke, 11/11 pixel, 6/6 dispose. |

### D14 — Closing the last two 🟨 rows

Two capability-matrix rows had never been exercised end-to-end under this backend specifically:
`Texture2D::GetData` on a plain (non-render-target) texture, and `Texture2D::FromStream` decode.
Both are entirely shared/backend-agnostic C++ (the former reads `Texture2D`'s own `cpuPixels_`
CPU-side shadow directly, never touching a backend at all; the latter's decode step is `stb_image`
via `SaveAsPng`/`FromStream`, identical on every backend) — so closing them out is specifically
about proving the two backend-touching *ends* of those pipelines behave correctly under this
backend's real Emscripten canvas/PNG-data-URL upload machinery, not about writing new backend code.

| Task | Status | Description |
|---|---|---|
| HTMLDOM-96 | ✅ | Added to `examples/htmldom_pixel_verification_test.cpp` (11 → 13 checks). **96a**: a plain `Texture2D` is created via `CreateFromPixels` with four distinct, unambiguous per-texel colours (including a non-255 alpha, ruling out both a wrong-channel and a wrong-texel-order bug), and `GetData()` is checked byte-exact against the source — `CreateFromPixels`/`GetData` is a lossless path with no codec involved, so no rounding tolerance is needed. **96b**: a source texture is created and uploaded through this backend, encoded to a real PNG via `SaveAsPng` (into a `System::IO::MemoryStream`), decoded back via `Texture2D::FromStream` (re-uploading through this backend's real canvas/data-URL machinery a second time, exercising the SAME upload path a fresh game asset load would), and read back matching the original colour within PNG's own lossless-but-8-bit-rounded tolerance. Both `docs/html-dom-backend.md` rows move from 🟨 to ✅ — this closes the last two 🟨 rows on that page. Full regression pass: 32/32 smoke, 13/13 pixel, 6/6 dispose, 33/33 GTest. |

### D15 — Closing the gap with `EASYGL`, where it's real and worth closing

The owner asked what `HTML_DOM` can't do that `EASYGL` (this project's default, 3D-capable WebGL2
backend) can. Read both backends' real source rather than guessing. Most of the gap is `EASYGL`
being 3D-capable at all — out of scope by explicit owner mandate (Scope, above), not a real gap.
Within the 2D-relevant surface both backends actually share (`SpriteBatch`/`Texture2D`/
`RenderTarget2D`/`BlendState`/`SamplerState`), most of the rest turned out to be **permanent**
architectural gaps (CSS genuinely cannot express what GL can here), not deferred work — separating
those from the two/three items that are real, closeable gaps is the point of this phase.

**Confirmed permanent, not pursued as tasks** (verified by reading `EasyGLGraphicsBackend.cpp`, not
assumed):
- **Real backbuffer readback.** `EasyGLGraphicsBackend::ReadBackbuffer` does a real `glReadPixels`
  (with an MSAA-FBO resolve first). No browser API rasterizes a live DOM subtree — already Known
  Limitation 1, unchanged.
- **Custom `Effect`/shader stage.** `EasyGLSpriteBatchBackend::FlushBatch` genuinely binds and runs
  a compiled custom GLSL program per Task 1077 (not a stub) when one is set. CSS has no
  programmable shader stage for a translated HLSL/GLSL effect to run in — already Known Limitation
  5, unchanged.
- **Arbitrary custom `BlendState`.** `EasyGLGraphicsBackend::ApplyBlendState` accepts any
  `(colorSrcBlend, colorDstBlend, colorBlendFunc, ...)` combination via real
  `glBlendFuncSeparate`/`glBlendEquationSeparate`. Checked whether CSS's other `mix-blend-mode`
  values (`multiply`, `screen`, `darken`, …) could extend `BlendStateToDomCompositeOp`'s existing
  4-tuple table beyond the four already mapped: they can't — those CSS modes are distinct nonlinear
  compositing formulas, not reachable from *any* `(srcFactor, dstFactor, equation)` triple the
  additive `BlendState` model can express, so there is no natural fifth preset to add. Already Known
  Limitation 4, confirmed exhaustively rather than left as an assumption.
- **Anisotropic/finer `TextureFilter` granularity.** `EasyGLGraphicsBackend` reports real
  `GL_EXT_texture_filter_anisotropic` support (Task 918, up to 16x). CSS `image-rendering` is a
  coarse, effectively binary hint (`auto`/`pixelated`/`crisp-edges` — no anisotropic-level control);
  not meaningfully closeable.
- **MSAA render targets.** `EasyGLGraphicsBackend::CreateRenderTarget2D` supports a real multisample
  renderbuffer + resolve. `HTML_DOM` render targets are backed by an ordinary `<canvas>` 2D context,
  which has no analogous MSAA render-target concept — architecturally different, not a deferred
  feature.

| Task | Status | Description |
|---|---|---|
| HTMLDOM-97 | ✅ | **`TextureAddressMode` coverage when `sourceRectangle` exceeds the texture.** Two real, closeable sub-gaps found in `HtmlDomState.cpp`'s `ValidateAddressModes`: (1) symmetric `Mirror` (`addressU == addressV == Mirror`) threw unconditionally. **Correction to this task's own original framing**: XNA's `SamplerState` class has NO built-in Mirror preset at all (confirmed by reading `SamplerState.hpp` — only `Point`/`Linear`/`AnisotropicClamp`/`Wrap` exist as statics); a game always constructs a custom `SamplerState` to use Mirror, and setting both axes the same way is the natural, common way to do that — not a claim about a nonexistent preset. `EASYGL` supports it for real via native `GL_MIRRORED_REPEAT`. (2) mixed per-axis modes (`addressU != addressV`) threw unconditionally even when NEITHER axis was `Mirror` (e.g. U=Wrap, V=Clamp) — but CSS `background-repeat`/`CanvasPattern` both natively accept independent per-axis repetition. **Implemented.** `ValidateAddressModes` now only throws for the one genuinely unsupported combination: Mirror mixed with a DIFFERENT mode on the other axis. `BuildDrawCommandEXT` (`HtmlDomSpriteBatchBackend.cpp`) computes `tiledU`/`tiledV` independently (each axis clamps or stays full on its own), plus a `mirror` bool (only ever true when both axes are Mirror, which `ValidateAddressModes` guarantees). Two new flag bits: `FlagWrapV` (independent V-axis tiling, alongside the existing `FlagWrap` for U) and `FlagMirror` (select the mirrored variant). Symmetric Mirror is realized via a new `Module['cnaDomGetMirrorVariant']` (`HtmlDomTextureBackend.cpp`) — a lazily-built, cached 2x2 canvas where the source variant is drawn once per quadrant with the other three quadrants flipped horizontally/vertically/both, cached in the SAME `entry.variants`/LRU-256 map as ordinary variants (no separate bookkeeping); tiling THAT image with ordinary `'repeat'` reproduces mirror-repeat exactly, by construction — the same general technique `CANVAS`'s own `CanvasSpriteBatchBackend` already proved out for its equivalent gap (`plan_canvas.md` CANVAS-44), adapted to this backend's existing variant-cache architecture rather than invented fresh. Mixed non-Mirror axes map to `background-repeat`'s two-value shorthand (DOM path) or `CanvasPattern`'s `'repeat-x'`/`'repeat-y'`/`'repeat'` repetition string (Canvas2D path) directly. **Verified in a real browser, both code paths.** Pixel-exact (Canvas2D render-target path, `htmldom_pixel_verification_test.cpp`, 13→15 checks): a 2x2 four-distinct-colour source tiled 2x2 under symmetric Mirror reads back the full hand-derived reflected grid, pixel-for-pixel (97a) — genuinely different from Wrap's plain-repeat pattern, not just "didn't throw"; the same source under mixed U=Wrap/V=Clamp tiles its U extent exactly like Wrap while its V extent clamps independently (narrows to the source height, does not stretch to fill the unclamped destination height) (97b). Structural (DOM path, `htmldom_smoke_test.cpp`, 32→35 checks): a symmetric-Mirror sprite gets a real PNG data-URL background and `background-repeat: repeat` (97c); a mixed U=Wrap/V=Clamp sprite gets `background-repeat: repeat-x` — caught by the real browser run, not assumed: the CSSOM serializes the two-value `'repeat no-repeat'` back as the single-keyword shorthand `'repeat-x'` (`repeat-x` is literally defined as shorthand for exactly that pair), the same kind of normalization-not-a-bug finding `JsRootClipPathInsetIs` hit earlier in this file (97d). GTest coverage extended for both the pure `ValidateAddressModes` function (symmetric Mirror and mixed-non-Mirror now accepted; Mirror-mixed-with-a-different-axis-mode still throws) and `BuildDrawCommandEXT`'s per-axis clamp/tile math (symmetric Mirror flags all three bits and keeps the full rect; mixed Wrap-U/Clamp-V tiles only U and narrows only V). Full regression pass: 35/35 smoke, 15/15 pixel, 3/3 stress, 6/6 dispose, 37/37 GTest — zero regressions. |
| HTMLDOM-98 | ✅ | **`SetViewport` (sub-rectangle `Viewport`).** `EasyGLGraphicsBackend::SetViewport` supports a real sub-region: `EasyGLSpriteBatchBackend::FlushBatch` builds its ortho projection from `Viewport.Width/Height` and leaves a non-full-target `Viewport` as the live GL rasterizer region (Task REMED-GFX-072), enabling split-screen/sub-panel rendering. `HTML_DOM` previously inherited `IGraphicsBackend`'s no-op default, matching every OTHER 2D-only sibling (`SDL_RENDERER`/`CANVAS`/`DX3`) — confirmed non-gap AGAINST those siblings (HTMLDOM-81), but a real, closeable gap against `EASYGL` specifically, since nothing about DOM/CSS compositing rules it out. **Implemented.** `HtmlDomGraphicsBackend::SetViewport` (idempotent, matching `Present()`'s own "only touch the DOM when geometry actually changed" rule) forwards to a new `CNA_HtmlDom_SetViewport`, which stores `Module['cnaDomViewport']` and calls a new shared `Module['cnaDomApplyViewport']` (`CNA_HtmlDom_EnsureRoot`). That function positions/sizes `#cna-dom-root` to the viewport sub-rect (`left`/`top` offset by `Viewport.X/Y`, pre-multiplied by the logical→physical scale since root's own layout position is unscaled; `width`/`height` from `Viewport.Width/Height`) instead of always the full logical backbuffer, falling back to exactly the prior full-surface behaviour when the viewport covers the whole backbuffer (the overwhelmingly common case, confirmed to cost nothing extra) — the same "collapses to the existing default case" shape HTMLDOM-94's regions already used. `CNA_HtmlDom_UpdateSurface` (a real resize) now only records the backbuffer's own geometry and re-delegates to `cnaDomApplyViewport`, so both a resize and an explicit viewport change funnel through the one function that knows how to derive root's box. Sprite-local coordinates needed no change (XNA already expresses them viewport-relative via the projection, not per-sprite). **Scissor/viewport coordinate-space finding, verified by reading `EasyGLGraphicsBackend` rather than assumed**: `GraphicsDevice.ScissorRectangle` and `Viewport` share the same ABSOLUTE render-target pixel space on a real GPU (`EasyGLGraphicsBackend`'s scissor forwards straight to `glScissor`, a window-space call, independent of whatever `glViewport` region is active) — so `cnaDomApplyRegionClip`'s insets now translate a region's stored (absolute) rect into root's own local box by subtracting the active viewport's offset first, and `cnaDomGetRegion`'s "does this rect cover everything" check compares against the offset viewport bounds, not literally `(0,0)`. A bound `RenderTarget2D` already resets `Viewport` to the target's own size at the shared `GraphicsDevice` layer and back to the backbuffer's on unbind (confirmed while implementing this task, not just assumed) — harmless even though `SetViewport` fires during that time, since `#cna-dom-root` is never drawn into while a target is bound regardless of its own CSS sizing. **A real test-premise correction found along the way**: the existing HTMLDOM-93 resize test (which deliberately calls `HtmlDomGraphicsBackend::Present()` directly, bypassing `GraphicsDevice`, specifically to verify the backend's OWN contract in isolation) started failing once Viewport became a real, tracked concept — because real XNA/FNA `Viewport` does NOT auto-track a resized backbuffer on its own (`GraphicsDevice::UpdateViewportFromWindow()` is the thing that resets both `Viewport` and `ScissorRectangle` together on a detected resize); bypassing `GraphicsDevice` means that reset has to be reproduced by hand too now, not just the resize itself. Fixed by adding an explicit `backend.SetViewport(0, 0, 128, 128, 0.0f, 1.0f)` call to the test, matching what a complete real resize flow does. **Verified structurally in a real browser** (the DOM backbuffer itself cannot be read back at all — design decision 11 — so there is no pixel-exact route available here the way the render-target-bound path uses): `htmldom_smoke_test.cpp` (35→38 checks) confirms a `Viewport(4,4,16,16)` sub-rect resizes `#cna-dom-root` to 16×16 (not the full 128×128 backbuffer), repositions it by exactly the expected `+4` offset (verified against the surface's own pre-viewport `left`/`top`, at a logical→physical scale confirmed to be exactly 1 in this scenario, not skipped), and that a sprite drawn at local `(0,0)` under that viewport still reads back `translate(0px,0px)` -- proving positioning comes entirely from root's own repositioning, not a per-sprite offset. New GTest (`SetViewportAcceptsArbitraryValuesAndIsIdempotent`) covers the C++-side contract under `node` (no `__EMSCRIPTEN__`, so no JS-side state to observe there): arbitrary values, a repeated call with the same values, and a subsequent call with different values all complete without throwing. Full regression pass: 38/38 smoke, 15/15 pixel, 3/3 stress, 6/6 dispose, 38/38 GTest — zero regressions. **Superseded by HTMLDOM-107 (Phase D16)**: this task's own "resize/reposition root to track viewport" approach was later found to retroactively move/clip an earlier batch's sprites when a later batch set a different viewport in the same frame, and to leave the Canvas2D render-target path ignoring viewport entirely — root now stays at the full backbuffer always and the viewport offset is applied per-batch instead; see HTMLDOM-107 for the corrected implementation. |
| HTMLDOM-99 | ✅ investigated, **not adopted** | **Investigate (implement only if it verifiably wins): SVG `feColorMatrix`-based exact real-time tint**, as a possible alternative to the current per-distinct-colour PNG-variant cache (LRU-capped at 256, confirmed under real eviction pressure by HTMLDOM-90). `EASYGL` tints exactly, every frame, for free in its fragment shader, with no cache/eviction concept at all — a real per-pixel RGB×tint multiply is expressible in SVG via a diagonal `<feColorMatrix>` applied as a CSS `filter: url(#...)`. **Acceptance bar, stated up front so this couldn't be quietly declared a win**: only keep it if a real headless-Chromium measurement showed it was not slower than today's cache for the steady-state case AND faster for the heavy-tint-churn case HTMLDOM-90 exercises. **Measured, not assumed.** Built a standalone (no Emscripten/CMake needed — pure HTML/JS, since this is a DOM/CSS technique question, not a backend-integration one) 500-sprite experiment mirroring HTMLDOM-89's own methodology (warm-up frame, 60 `requestAnimationFrame`-paced timed frames), comparing today's real technique (per-pixel `getImageData`/`putImageData` tint + `toDataURL`, cached, LRU-256) against a per-sprite `<feColorMatrix>` filter (one dedicated `<filter>` per pooled sprite element, its `values` attribute rewritten via `setAttribute` when that sprite's tint changes, `background-image` never changing at all) in headless Chromium, run 3 times for consistency: **steady-state** (a fixed set of 8 stable, already-cached tints across 500 sprites, position animating every frame, no tint changes at all) — variant-cache **18.6–21.1 ms/frame**, SVG filter **38.2–40.9 ms/frame**, roughly **2× SLOWER**; **heavy churn** (every sprite's tint changes every frame, cycling through hundreds of distinct RGB values, the exact scenario HTMLDOM-90 exercises) — variant-cache **73.0–75.9 ms/frame**, SVG filter **43.4–52.0 ms/frame**, ~35-40% faster. **Verdict: fails the acceptance bar, not adopted.** The churn-case win is real, but the steady-state regression is the disqualifying result: it directly contradicts this backend's core "zero cost when nothing changes" design premise (design decisions section, above) for the OVERWHELMINGLY more common case (most games use a handful of stable tints, not per-frame-per-sprite colour churn) — confirming the concern this task was scoped around from the start, not a surprise. Root cause consistent with the documented risk: `filter:` forces the browser into extra compositor work for that element even when the filter's own parameters are not changing frame to frame, unlike a plain `background-image`/`transform` write. **Kept**: the existing PNG-variant cache, unchanged. This is a genuine, honest "investigated, not adopted" outcome — the acceptance bar existed specifically so this couldn't be quietly declared a win on the strength of the churn number alone. **HTMLDOM-111 correction**: the standalone experiment this row describes was never checked into the repository, so its exact numbers could not be independently reproduced. HTMLDOM-111 checked in a reconstruction (`tools/htmldom/tint-technique-experiment.html`) which does NOT reproduce this row's own cited magnitudes (both techniques now measure under 1ms of submission cost, an order of magnitude below the numbers above) — most plausibly because this row's own script's exact diffing details could not be perfectly reconstructed from prose alone, compounded by real subsequent browser/compositor changes. The DECISION this row reached (keep the variant-cache) still holds and is independently reproducible via the checked-in file; the specific ms/frame magnitudes quoted above should be treated as historical/illustrative, not as a guaranteed-reproducible constant — see the checked-in file's own "HONEST CAVEAT" comment. |
| HTMLDOM-100 | ✅ | **Fix: `Opaque` forced result alpha to 255 on the Canvas2D render-target path, diverging from real XNA.** Found while writing `plan_canvas.md` CANVAS-85's own Opaque check for the sibling `CANVAS` backend: this project's own `BlendState.cpp` defines `BlendState::Opaque` with symmetric `One`/`Zero` factors for BOTH colour AND alpha (`colorSrcBlend=One, colorDstBlend=Zero, alphaSrcBlend=One, alphaDstBlend=Zero`), not colour alone — real XNA Opaque replaces the destination pixel with the source pixel EXACTLY, alpha included, never forcing it to 255. This backend's `HtmlDomState.hpp`/`HtmlDomTextureBackend.cpp` design predates that finding and forced alpha to 255 unconditionally on both draw paths, and the existing HTMLDOM-86 test asserted exactly that forced value as correct. **Implemented.** The two draw paths now genuinely differ, both intentionally: the DOM `<div>` backbuffer path still fetches the alpha-stripped (mode 2) variant, since CSS has no operator that can replace what is beneath a stacked, blended element while still showing that element's own partial alpha through it — full opacity is the closest a `<div>` can get, and this is now an explicitly documented, accepted deviation rather than an unexamined default. The Canvas2D render-target-bound path (`CNA_HtmlDom_FlushSprites`'s `targetCtx` branch, `HtmlDomSpriteBatchBackend.cpp`) now fetches the STRAIGHT (mode 0, as-uploaded) variant for Opaque and composites it with Porter-Duff `'copy'` instead of alpha-forced `'source-over'` — clipped first to exactly the sprite's own already-transformed footprint (`ctx.rect(lx,ly,sw,sh)` + `ctx.clip()`, inside the existing `save()`/`restore()` pair), because `'copy'` is evaluated over the WHOLE compositing area rather than just the drawn shape (the identical pitfall `plan_canvas.md` CANVAS-44 found and fixed for the sibling `CANVAS` backend, applied here the same way). `VariantModeFor`/`HtmlDomState.hpp`'s design-decision doc, the `DomCompositeOp::Opaque` enumerator doc, and `HtmlDomTextureBackend.cpp`'s mode-table comment were all corrected to describe the two paths' now-different, both-deliberate behaviour instead of asserting one wrong shared rule. HTMLDOM-86 (`htmldom_pixel_verification_test.cpp`, binds a `RenderTarget2D`, so it exercises the Canvas2D path exclusively) rewritten: a semi-transparent source texel `(180,90,40,120)` now reads back at alpha≈120 (its own value), not forced to 255. **Verified in a real browser**: 15/15 pixel (HTMLDOM-86 now asserting the corrected value, read back `(181,89,40,120)` against `~(180,90,40,120)`, within the existing ±1 8-bit-rounding tolerance), 38/38 smoke (including the existing Wrap-under-Opaque render-target check at frame 6 and the Mirror-under-Opaque check at frame 14, both fully-opaque source textures where the mode-0-vs-mode-2 change is a no-op, confirming no incidental regression), 3/3 stress, 6/6 dispose — zero regressions across the full HTML_DOM browser suite. |

### D16 — Independent correctness and verification audit (2026-08-03)

An independent source-and-browser audit found that several earlier ✅ rows satisfy their structural
tests but not the XNA/rendered-output contract those rows claim. Under this plan's own status legend,
the affected earlier rows must be treated as reopened until the tasks below pass their acceptance
criteria. Confirmed defects are distinguished from explicitly labelled audit risks; neither category
may be closed by asserting an inline CSS value or by repeating the current implementation's formula
as the test oracle.

While this audit was being prepared, HTMLDOM-100 independently fixed the Canvas2D render-target
half of the Opaque-alpha defect. The audit tasks therefore start at HTMLDOM-101; HTMLDOM-105 retains
only the still-open DOM-backbuffer Opaque deviation and the other blend-equation gaps.

| Task | Status | Description |
|---|---|---|
| HTMLDOM-101 | ✅ | **Gave every non-default scissor region a real surface-sized box and verified actual painting (reopens HTMLDOM-80/93/94). Confirmed defect, fixed.** `cnaDomGetRegion` (`HtmlDomGraphicsBackend.cpp`) created a container with only `position:absolute;left:0;top:0`; its absolutely-positioned sprite children contribute no intrinsic size, so the region's border box was genuinely 0×0 -- confirmed by re-running the audit's own focused headless-Chromium probe before touching any code. **Fixed**: the container now also carries `width:100%;height:100%`, tracking `#cna-dom-root`'s own box (which `cnaDomApplyViewport`/HTMLDOM-98 already keeps sized to the active viewport/backbuffer) with no extra JS bookkeeping, so it stays correct across resizes/viewport changes automatically, the same "collapses to what the parent already tracks" shape HTMLDOM-94's regions and HTMLDOM-98's viewport already use. **Verified two ways, both new.** (1) `JsRegionOffsetWidth`/`JsRegionOffsetHeight` (`htmldom_smoke_test.cpp`) read the container's real, browser-COMPUTED `offsetWidth`/`offsetHeight` -- not an inline style string -- confirming a genuine non-zero layout box exists (38→39 smoke checks). (2) A real Playwright **screenshot** pixel test (`scripts/htmldom-browser-test.mjs`'s new `verifySmokeScreenshotPixels`, run only against the smoke page): the smoke test's very last draw (frame 10, deliberately positioned last since `Clear()` hides every region's sprites at the top of every subsequent frame) draws a sprite whose destination footprint straddles a scissor rect's own edge; the harness screenshots the finished page, decodes it back inside the page via an `<img>`+`<canvas>` (`toDataURL`'s own PNG is same-origin, so no CORS taint), and samples two real composited pixels: one inside both the sprite and the rect (must read the sprite's own solid colour) and one inside the sprite's UNCLIPPED footprint but outside the rect (must read the surface's own clear colour instead). **Regression-proven, not merely reviewed**: temporarily reverted the one-line fix and re-ran -- the new offsetWidth/offsetHeight check failed (0/0, exactly as the audit's own probe found), and the "inside" screenshot pixel read back the clear colour `rgba(100,149,237,255)` instead of the sprite's red, proving the straddling sprite was genuinely unpainted with the bug present; restored the fix and re-ran clean. Full regression pass after restoring: 39/39 smoke (including both new pixel checks), 15/15 pixel, 3/3 stress, 6/6 dispose. |
| HTMLDOM-102 | ✅ | **Implemented scissor semantics on the Canvas2D render-target path and honoured `RasterizerState.ScissorTestEnable` (reopens HTMLDOM-80/91/94 and narrows HTMLDOM-51). Confirmed defect/design divergence, fixed.** `CNA_HtmlDom_FlushSprites` used to state outright that a bound render target does not consult the scissor rect at all, and the DOM path clipped whenever a narrow `ScissorRectangle` was recorded regardless of `RasterizerState.ScissorTestEnable` -- matching `SDL_RENDERER`'s own omission, not real XNA fidelity. **Implemented.** `HtmlDomState.hpp`/`.cpp` gained `GetCurrentScissorEnableEXT`/`SetCurrentScissorEnableEXT`, the same plain-C++, unit-testable-outside-a-browser shape `GetCurrentCompositeOpEXT` already uses for `BlendState` (also reset in the constructor alongside the composite-op reset, so a backend built after `GraphicsDevice.Reset()` never inherits stale scissor-enable state). `HtmlDomGraphicsBackend::ApplyRasterizerState` (a real override now; previously inherited `IGraphicsBackend`'s no-op) reads only `scissorTestEnable` -- `cullMode`/`fillMode`/depth-bias fields stay genuinely inert, per HTMLDOM-91's own audit. `CNA_HtmlDom_FlushSprites` gained a `scissorEnabled` parameter (captured once per flush, the same per-batch granularity the scissor rect itself already uses) and computes `effectiveScissorRect = scissorEnabled ? Module['cnaDomScissorRect'] : null`, consumed by BOTH paths: the DOM path passes it to `cnaDomGetRegion` (a disabled test collapses to the same zero-cost `'full'` region as no rect at all); the Canvas2D render-target path gained a real `ctx.save()/rect()/clip()`, established ONCE per batch (not per sprite) in absolute target-pixel space. **A real, load-bearing finding while wiring the test scenarios**: `GraphicsDevice.RasterizerState` set directly (`dev.setRasterizerStateProperty(...)`) is NOT what `SpriteBatch` actually draws under -- `SpriteBatch::Begin()` itself always (re-)applies EITHER the `RasterizerState` passed to it OR `RasterizerState::CullCounterClockwise` (`ScissorTestEnable=false`) as its own default, exactly like FNA's `PrepRenderState` (`SpriteBatch.cpp`, REMED-GFX-081) -- so a plain, no-argument `Begin()` call always disables scissor testing regardless of anything set beforehand, and every existing scissor-rect test in `htmldom_smoke_test.cpp` had to have `&scissorEnabledState_` (a new member, `ScissorTestEnable=true`) passed to its own `Begin()` call to keep working under the new, correct gating -- not a workaround, but the same requirement a real XNA game has. **Verified in a real browser.** `htmldom_smoke_test.cpp` (43→48 checks, two new HTMLDOM-102a/b structural checks plus updating every pre-existing scissor `Begin()` call): a plain `Begin()` with a narrow `ScissorRectangle` recorded creates NO region at all; the very next batch, with `&scissorEnabledState_` passed to `Begin()`, DOES. `htmldom_pixel_verification_test.cpp` (16→18 checks, new frame, HTMLDOM-102c/d): a `RenderTarget2D`-bound sprite covering the whole target is genuinely clipped to the scissor rect's own bounds with `ScissorTestEnable=true`, and the SAME rect with `ScissorTestEnable=false` does not clip at all -- read back via real `GetData`, not inferred from DOM state. New GTest `ApplyRasterizerStateReadsScissorTestEnable` covers the pure-C++ contract directly; the pre-existing `InertStateSettersAcceptArbitraryValuesWithNoObservableEffect` test's own comment was corrected (`scissorTestEnable` is no longer inert; the rest of `ApplyRasterizerState`'s fields still are). **Regression-proven**: temporarily made `effectiveScissorRect` ignore the enable bit entirely and re-ran -- HTMLDOM-102a and HTMLDOM-102d both failed exactly as expected before restoring the fix. Full regression pass: 48/48 smoke, 18/18 pixel, 3/3 stress, 6/6 dispose, 39/39 GTest. |
| HTMLDOM-103 | ✅ | **Restored true painter's order across scissor/viewport regions and made region eviction non-destructive (reopens HTMLDOM-31/76/94). Confirmed defect, fixed.** Sprites in different region containers used to paint by persistent DOM-container creation/allocation history, not current draw order -- the default `full` pool is `#cna-dom-root` itself, so direct sprite nodes and nested region containers were interleaved by allocation history rather than even the documented region-creation order; a 17th non-default region touched in one frame could LRU-evict the oldest container while its sprites were still part of the frame being built. **Implemented, via `z-index` rather than DOM-node reordering** (cheaper than repeated `appendChild` moves, and every sprite already forms its own stacking context via its existing `will-change:transform`, so this only changes ordering AMONG those already-separate contexts -- verified it does not change how `mix-blend-mode` resolves within one, since HTMLDOM-87's own Additive check stays green below). A monotonically increasing `Module['cnaDomPaintOrderCounter']` is stamped as `z-index` on every flush: once per flush on a NAMED region's own container (`HtmlDomSpriteBatchBackend.cpp`), or per-sprite for the `'full'` region (which has no container of its own to stamp -- its sprites are direct `#cna-dom-root` children, siblings of every named region's container). **Non-destructive eviction**: `Module['cnaDomRegionsTouchedThisFrame']` (a `Set`, cleared in `CNA_HtmlDom_Clear` -- this backend's own "frame start" marker per design decision 9) is populated by `cnaDomTouchRegion` on every region access, and `cnaDomGetRegion`'s eviction loop now scans oldest-first for the first candidate NOT in that set, skipping every region still active this frame; if none exists (every candidate is active this frame), it logs a `console.warn` and allows a temporary over-cap rather than dropping a live region. **Verified in `htmldom_smoke_test.cpp`** (39→43 checks): new `JsRegionZIndex`/`JsFullRegionSpriteZIndexAt` helpers confirm an A/B/A scissor-rect sequence gives A's second flush a HIGHER paint order than B's (not merely A's own first-creation position), and that a `'full'`-region flush interleaved between two named-region flushes also gets a correctly higher paint order than what preceded it -- the one case DOM position could never reproduce at all, since `'full'` sprites and region containers are siblings. A 17-distinct-region-in-one-frame scenario (one past the 16-region cap) confirms both of two actively-drawn regions survive. **Regression-proven**: reverted both source changes to their pre-fix state and re-ran -- 3 of the 4 new checks failed exactly as expected (paint-order A/B/A, the full-interleave check, and region B's survival; region A's own survival check happened to still pass by LRU coincidence in this specific sequence, since three OTHER stale regions from earlier frames absorbed the old unconditional-shift eviction before B's turn came up -- the overall scenario still correctly demonstrates the defect). Restored the fix and re-ran clean. Full regression pass: 43/43 smoke, 15/15 pixel, 3/3 stress (4.985 ms/frame, no perf regression from the extra z-index writes), 6/6 dispose, 38/38 GTest. |
| HTMLDOM-104 | ✅ | **Implemented real sampler `Clamp` for out-of-bounds source coordinates and corrected the existing test oracle (reopens HTMLDOM-35/45/97). Confirmed semantic defect, fixed.** The encoder used to narrow the source rectangle into the texture and leave the removed destination area transparent; real XNA Clamp instead repeats the nearest edge texel outside the texture and never crops sprite geometry. The pre-existing HTMLDOM-97b test explicitly expected transparent overflow rows and therefore validated the wrong result -- flagged by the audit that reopened this task, not just the implementation it was checking. **Implemented via a cached, edge-extended ("padded") texture variant** -- the architecture choice made after comparing it against a multi-layer CSS `background-image` approach (arbitrary-overflow-capable but a much larger rewrite of the single-background-image sprite CSS this backend is built around) and concluding the realistic use case (a few pixels of atlas-seam bleed margin, or a modest out-of-bounds sample) is well served by a bounded, cached approach that reuses the EXISTING single-background-image/single-`drawImage` sprite pipeline unchanged. `BuildDrawCommandEXT` (`HtmlDomSpriteBatchBackend.cpp`) no longer crops -- `cmd.sx/sy/sw/sh` are always the RAW, unclamped source rect, and `cmd.localX/localY` are the plain pivot offset with no clamp-shift; the per-axis, per-edge texture-pixel padding needed (`padLeft`/`padTop`/`padRight`/`padBottom`, gated on that axis NOT being tiled) is computed and validated against a `kMaxClampPadding` (256 texture pixels) cap, throwing a clear, actionable error beyond it -- an honest boundary, not silent cropping, per this task's own explicit fallback instruction. `Module['cnaDomGetPaddedVariant']` (`HtmlDomTextureBackend.cpp`) builds the padded canvas once per (texture, tint/mode, rounded-padding) combination -- centre copied at 1:1, edges and corners filled by stretching a 1-texel source strip/corner to fill the padding (stretching 1 source texel to N destination pixels IS exact clamp-to-edge sampling, since there is nothing else to interpolate between), rounding requested padding UP to an 8px granularity so near-identical overflow amounts share one cached variant (harmless over-padding, since the extra rows/columns are simply never sampled if the true overflow is smaller) -- cached in the SAME `entry.variants`/LRU-256 map as every other variant. `Module['cnaDomResolveClampVariant']` centralizes the decision (plain vs. padded variant, and the source-coordinate shift needed to sample correctly within a padded one) for reuse by BOTH `CNA_HtmlDom_FlushSprites` paths (DOM `background-position` and Canvas2D `drawImage`/`CanvasPattern`). **A real bug found and fixed while pixel-verifying this**: with Canvas2D `imageSmoothingEnabled` left at its default (true), Chromium's GPU-accelerated `drawImage` bilinear-sampled slightly PAST a 1-texel source slice's own edge when stretching it, bleeding in the ADJACENT texel's colour into the padding -- measured, not assumed (a fully-outside-the-texture draw read back a red/blue blend instead of solid yellow). Fixed by explicitly setting `imageSmoothingEnabled = false` for every draw inside `cnaDomGetPaddedVariant` (harmless for the 1:1 centre copy; exact for every stretched edge/corner, matching Clamp's own filter-independence -- there being only one source texel per stretch, "point" and "linear" cannot differ). **Verified pixel-exact in a real browser**, `htmldom_pixel_verification_test.cpp` (18→20 checks, plus the corrected HTMLDOM-97b oracle): a sourceRectangle entirely outside the texture clamps every sample to the nearest corner texel, checked identically under BOTH `SamplerState.PointClamp` and `SamplerState.LinearClamp` -- the exact point-vs-linear comparison that caught the smoothing bug above; a scaled (2x) + tinted + one-axis-overflow draw shows the correctly TINTED edge-replicated row in the overflow region, with hand-derived exact expected colours (`(0,0,255)` and `(128,255,0)`, the latter an exact integer since `255×128 == 128×255`); and the corrected HTMLDOM-97b (mixed U=Wrap/V=Clamp) now shows the U-tiled, edge-replicated bottom rows instead of transparency. New/updated GTest (`ClampKeepsTheRawSourceRectAndLeavesDestGeometryUnchanged`, `ClampOverflowBeyondTheCapThrows`, and a corrected `MixedWrapUClampVTilesOnlyUAndClampsV`) cover the pure-C++ contract -- the JS-side edge-extension itself is not unit-testable without a browser, matching how Mirror's own variant generation is treated. **Regression-proven**: reverted both implementation files to their pre-HTMLDOM-104 (`HEAD`) state while keeping the corrected test file, and re-ran -- all three new/corrected checks failed exactly as expected (the corrected 97b, the fully-outside point/linear check, and the scaled+tinted check) before restoring the fix. **Not separately pixel-verified**: rotation combined with Clamp overflow -- the destination-geometry transform pipeline (rotation/scale/origin) is completely unchanged by this task (only texture SAMPLING changed) and is already independently pixel-verified elsewhere (HTMLDOM-82a/b), so a dedicated rotation+Clamp combination test was judged low incremental value against the effort already spent; noted here explicitly rather than silently claiming full coverage. Full regression pass: 48/48 smoke, 20/20 pixel, 3/3 stress, 6/6 dispose, 40/40 GTest. |
| HTMLDOM-105 | ✅ | **Re-derived the remaining standard blend semantics from their colour AND alpha equations; stopped calling the current mapping pixel-exact (reopens HTMLDOM-41/42/43/84/85/86/87 and follows HTMLDOM-100). Confirmed semantic defect, resolved as a documented architectural limitation.** HTMLDOM-100 already correctly changed Opaque on the Canvas2D render-target path to clipped `copy` (the DOM `<div>` backbuffer path's own alpha-stripped deviation there was already accepted and documented in HTMLDOM-100's own row, not something this task needed to touch again). **Re-derivation, from each preset's raw `(colorSrcBlend, colorDstBlend, alphaSrcBlend, alphaDstBlend)` factors in `BlendState.cpp`, independent of any browser**: `AlphaBlend` uses `alphaSrcBlend=One`, so its real alpha equation (`result_a = src_a + dst_a*(1-src_a)`) is EXACTLY CSS/Canvas2D's own Porter-Duff "over" alpha formula -- no gap, already correctly pixel-verified translucent (HTMLDOM-85), nothing to fix. `NonPremultiplied` and `Additive` both use `alphaSrcBlend=SourceAlpha` -- i.e. the alpha channel's OWN blend factor is `src_a` itself, so their real equations SQUARE the source alpha's own contribution (`result_a = src_a² + dst_a*(1-src_a)` and `result_a = src_a² + dst_a` respectively). Neither `source-over` nor `lighter`/`plus-lighter` can reproduce a squared term -- CSS's compositing model has no per-channel-independent blend-factor customization, and implementing it exactly would require per-pixel JS blending on every draw instead of native browser compositing, defeating this backend's entire "zero cost when nothing changes, GPU-composited otherwise" performance premise. Confirmed the divergence is real via a headless-Chromium probe matching the audit's own reported numbers exactly: two sequential 50%-alpha `Additive` draws measure `(128,0,128,255)` (this backend's actual CSS-native result) where XNA's own squared formula, applied recursively across both draws, would give `(128,0,128,~128)`. **Also confirmed the gap is opaque-source-invisible, explaining why it was missed before**: `src_a=255` makes `src_a²==src_a` exactly, so the two formulas coincide for every opaque-source draw -- exactly why HTMLDOM-87's own opaque-only Additive test could never have caught this, matching the audit's own diagnosis. **Resolution: documented as an accepted architectural limitation** (`HtmlDomState.hpp`'s `DomCompositeOp` doc comment, with the full factor-by-factor derivation) rather than implemented, since exact reproduction is not achievable without abandoning native compositing -- the colour channels remain exact for every case; only the ALPHA channel of a `NonPremultiplied`/`Additive` result with a translucent SOURCE diverges from raw XNA/D3D hardware blending. **Verified with real translucent source AND destination raw RGBA readback** (not just colour), `htmldom_pixel_verification_test.cpp` (20→24 checks): `NonPremultiplied` single-draw-onto-transparent (measured alpha 128, matching CSS-native, not the hypothetical XNA-exact 64); `Additive` two-sequential-translucent-draws reproducing the audit's own exact scenario and numbers; a zero-alpha draw leaving an already-opaque destination byte-for-byte unchanged (both formulas trivially agree at `src_a=0`, so this is a plain regression check, not a divergence demonstration); and tint alpha (as opposed to texture alpha) driving the identical effective `src_a` (opaque texel tinted to alpha=128 reads back matching the texture-alpha=128 case exactly). Every hand-derived expected value matched the real measured browser result exactly. Full regression pass: 48/48 smoke, 24/24 pixel, 3/3 stress, 6/6 dispose, 40/40 GTest -- no implementation code changed (this task's own resolution is documentation + verification, not a code fix, since none is architecturally achievable here). |
| HTMLDOM-106 | ✅ | **Audited premultiplied-alpha representation across render-target write, readback, and render-target-as-texture sampling (reopens HTMLDOM-52/53/83/85). Confirmed defect, fixed.** Canvas2D's `getImageData()` (and using a canvas as a `drawImage` source) always exposes STRAIGHT/non-premultiplied bytes, unconditionally, regardless of which `BlendState` drew the content -- a browser contract, not a choice this backend makes. `cnaDomGetVariant`'s mode 1 ("un-premultiplied": RGB divided by alpha) exists to correct an UPLOADED texture's bytes, which the `AlphaBlend` contract assumes are the game's own ALREADY-premultiplied asset data -- but it was applied unconditionally to every texture id, including a `RenderTarget2D`'s own backing canvas. Since a render target's content is never actually premultiplied (per Canvas2D's own contract above), sampling it later as a `Draw()` source under `AlphaBlend` divided its already-straight bytes by alpha a SECOND time. **Confirmed HTMLDOM-83's existing round-trip test structurally could not have caught this**: it draws fully opaque content (alpha=255) into the sampled render target, and mode 1's own `aa > 0 && aa < 255` guard never fires at alpha=255, so the double-division branch is simply never exercised by that test. **Fix**: `HtmlDomTextureBackend`'s JS-side texture registration (`CNA_HtmlDom_CreateTexture`) now tags each `Module['cnaDomTextures'][id]` entry with `isRenderTarget`, set true only via the `HtmlDomTextureBackend(int width, int height)` constructor -- confirmed by grep to be `HtmlDomRenderTargetBackend`'s only caller, never used for a plain `Texture2D`. `cnaDomGetVariant` now downgrades a requested mode 1 to mode 0 (straight, as-is, no division) whenever `entry.isRenderTarget` is set, before the cache key is computed, so an `AlphaBlend` sample of a render target transparently reuses the same straight/mode-0 variant an explicit straight request would produce. Established and documented the canonical representation at both ends: `HtmlDomTextureBackend.cpp`'s top-of-file comment and `HtmlDomRenderTargetBackend.hpp`'s `GetData` doc both now state plainly that a render target's pixels are always straight alpha on readback, a deliberate deviation from real XNA (where a render target's raw storage reflects whatever representation was actually drawn into it). **Verified with a real write → readback → resample → readback-again round trip** matching the task's own acceptance bar, `htmldom_pixel_verification_test.cpp` (24→28 checks, frame 19): reused HTMLDOM-85's own premultiplied texel `(128,50,25,128)` (straight `(255,100,50)` at alpha=128) for continuity with an already-verified number. Leg 1 (write + readback): drawing it into RT A under `AlphaBlend` and reading RT A back confirms straight `(255,100,50,128)`. Leg 2 (resample onto a TRANSPARENT destination): RT A drawn as an ordinary `Draw()` source into RT B under `AlphaBlend` reproduces RT A's own colour exactly, `(255,100,50,128)` -- compositing an already-straight source onto a fresh-transparent destination is representation-preserving. Leg 3 (resample onto a NON-transparent destination): the same RT A source composited over an opaque pre-filled RT B background `(0,0,200,255)` matches the browser's real `src*a + dst*(1-a)` source-over algebra by hand, `(128,50,125,255)`. Leg 4 (a second preset): `NonPremultiplied` sampling of RT A onto a transparent RT B, which never requested mode 1 in the first place, reproduces the identical `(255,100,50,128)` before and after the fix, confirming the `isRenderTarget` downgrade is additive and does not change a preset that was already correct. **Regression-proved**: reverting the fix and re-running reproduced the exact predicted defect signature -- leg 2 read back `(255,199,104,128)` instead of `(255,100,50,128)`, leg 3 read back `(128,100,152,255)` instead of `(128,50,125,255)`, both clearly matching the hand-derived double-division math, while legs 1 and 4 (unaffected code paths) still passed -- then restoring the fix returned all 28/28. Full regression pass: 48/48 smoke, 28/28 pixel, 3/3 stress, 6/6 dispose, 40/40 GTest. |
| HTMLDOM-107 | ✅ | **Made viewport state per draw batch instead of retroactively resizing the one global root; supported it on render targets (reopens HTMLDOM-98). Confirmed defect, fixed.** `cnaDomApplyViewport` used to move/resize `#cna-dom-root`, which contains every previously submitted sprite -- drawing viewport A and then setting viewport B in the same frame moved and clipped A's sprites retroactively, so the claimed split-screen/sub-panel use case was not actually implemented; the Canvas2D render-target path ignored viewport offset/clipping entirely; and shrinking root exposed the host page's own background outside it even though `Clear()` is render-target-wide, not viewport-scoped, in real XNA. **Implemented.** `#cna-dom-root` is now kept at the full backbuffer's own size/position ALWAYS (`cnaDomApplySurfaceGeometry`, replacing `cnaDomApplyViewport`) -- `CNA_HtmlDom_SetViewport` now only records `Module['cnaDomViewport']`, touching no DOM at all, the same shape `CNA_HtmlDom_SetScissorRect` already used. The viewport's own `(X,Y)` offset is instead captured ONCE per flush (`CNA_HtmlDom_FlushSprites`, `HtmlDomSpriteBatchBackend.cpp`) and applied as the OUTERMOST translate on that batch's own sprites -- prepended even before `transformMatrix`, matching real XNA/FNA where `Viewport.X/Y` is applied by the rasterizer stage strictly AFTER the projection matrix (built from `Viewport.Width/Height` alone). Implemented identically on BOTH paths: the DOM `<div>` path (prepended to each sprite's CSS `transform`) and the Canvas2D render-target path (`targetCtx.translate(vpOffX, vpOffY)`, previously entirely absent there). Since root no longer moves, the scissor-region clip-path math also SIMPLIFIED back to pure backbuffer-relative insets with no viewport-offset subtraction (`cnaDomApplyRegionClip`/`cnaDomGetRegion`'s `isFull` check) -- scissor and viewport are independent absolute-space concepts on a real GPU, confirmed by HTMLDOM-98's own `EasyGLGraphicsBackend` reading, so the offset subtraction HTMLDOM-98 introduced was itself removed rather than adjusted. **Verified in a real browser.** `htmldom_smoke_test.cpp` (43→46 checks): confirms root's own width/height/left/top are genuinely UNCHANGED by a sub-rectangle viewport (HTMLDOM-107a/b); a sprite drawn under `Viewport(4,4,16,16)` carries `translate(4px,4px)` as an ADDED, not substituted, prefix to its own `translate(0px,0px)` (HTMLDOM-107c/d, addressed via a new pool-index-based `JsFullRegionSpriteTransformContains` rather than `root.children[i]`, which is not reliably the i-th 'full' sprite once named scissor-region containers exist as root's OTHER children -- a real test-authoring bug caught while writing this, the same class of mistake HTMLDOM-103's own `JsFullRegionSpriteZIndexAt` had already been introduced specifically to avoid); and the actual regression this task exists to fix -- an earlier batch's sprite keeps its OWN viewport's offset even after a LATER batch sets a DIFFERENT viewport, with no retroactive re-offsetting (HTMLDOM-107e/f). `htmldom_pixel_verification_test.cpp` (15→16 checks, HTMLDOM-107g): a sub-rectangle `Viewport(4,4,10,10)` active while a `RenderTarget2D` is bound offsets a sprite's real read-back pixel position by `(4,4)` within the target's absolute pixel space; the "outside" sample point is deliberately where the sprite would have landed WITHOUT the fix, so this doubles as its own regression oracle. **Regression-proven**: reverted the DOM-path fix in `htmldom_smoke_test.cpp` context (temporarily restoring the pre-fix `HtmlDomGraphicsBackend.cpp`/`HtmlDomSpriteBatchBackend.cpp`) and separately disabled just the Canvas2D `targetCtx.translate` line -- confirmed HTMLDOM-107f and HTMLDOM-107g each fail exactly as expected before restoring both fixes. Full regression pass: 46/46 smoke, 16/16 pixel, 3/3 stress, 6/6 dispose, 38/38 GTest. **Not covered by this task**: `Clear()`'s own render-target-wide-not-viewport-scoped semantics were already correct as a side effect of root never shrinking (no separate fix needed); a dedicated Clear-with-subviewport pixel test was not added, since `CNA_HtmlDom_Clear`'s own code path takes no viewport input at all to get wrong. |
| HTMLDOM-108 | ✅ | **Implemented every `CnaPresentationMode`, validated mode values, and made input transforms use identical geometry (reopens HTMLDOM-13/14). Confirmed defect, fixed.** The backend previously only had correct `FixedHeightDynamicWidth`-style uniform height scaling: `Letterbox`/`Overscan`/`Stretch`/`NativeBackBuffer` all fell through to that SAME height-derived uniform `scale()` with no centring/cropping offsets, silently wrong whenever the virtual and physical aspect ratios did not already coincide; `TransformWindowToLogical`/`TransformLogicalToWindow` repeated the same single-scale assumption; `SetPresentationMode` accepted any int unchecked. **Fix**: ported `SdlGpuGraphicsBackend::ComputeLogicalViewport` -- the "tested logical-viewport computation" from a complete backend this row's own text asked for -- into a new `HtmlDomGraphicsBackend::ComputeLogicalViewport()`/`LogicalViewport` struct, the single source every geometry-facing method now derives from: `getLogicalSize`/`GetViewportSize`, `Present()` (which now computes an offset AND a per-axis scale, not one uniform height ratio), and `TransformWindowToLogical`/`TransformLogicalToWindow`. Per mode: `Letterbox`/`Overscan` use a uniform scale (`min`/`max` of the two per-axis ratios respectively) centred in the physical rect; `Stretch` scales each axis independently to fill it exactly, the only mode that does not preserve aspect ratio; `NativeBackBuffer` (matching the reference backend's own treatment) ignores `virtualWidth_`/`virtualHeight_` entirely -- logical size IS the physical size, the same fallback already used for "no virtual resolution configured at all"; `FixedHeightDynamicWidth` is unchanged (derives logical width from the surface's own aspect ratio, which by construction always exactly fills the physical rect). `Present()`'s dirty-check now compares the full viewport (offset + scale), not just logical/physical W/H, since a `SetPresentationMode` call alone -- same sizes, different mode -- changes the scale/offset without changing either W or H, which the old comparison could not detect. `SetPresentationMode` now validates its ordinal range and throws `std::out_of_range`, matching `SdlGpuGraphicsBackend::SetPresentationMode`'s own validation; a rejected call leaves the previously-set mode's geometry completely unchanged. `TransformWindowToLogical` now correctly returns false for a window coordinate inside a `Letterbox` bar (outside the scaled content rectangle) -- the row's own "return the correct outside-letterbox result" ask -- while preserving this backend's own pre-existing, still-tested "no virtual resolution configured" contract (an early-out before `ComputeLogicalViewport` is even consulted, since `ComputeLogicalViewport`'s own 1:1 rendering fallback for that case is a different, valid-for-rendering answer that the existing `HtmlDom3DSurfaceTest.ViewportFollowsTheVirtualResolutionSetting` GTest already asserts should NOT be reported as a real logical/physical mapping). **Root's own physical placement** (`#cna-dom-root`'s CSS `left`/`top`/`transform: scale()`) now follows the same per-mode offset/scale via `CNA_HtmlDom_UpdateSurface`'s new `(logicalW, logicalH, offsetX, offsetY, scaleX, scaleY)` signature (previously `(logicalW, logicalH, physW, physH)`, deriving one height-only `cnaDomScale`). Root now lives inside a NEW wrapper element, `#cna-dom-viewport`, sized/positioned to the physical `<canvas>`'s own bounds with its own `overflow:hidden` -- needed for `Overscan` specifically, whose scaled root is deliberately LARGER than the canvas and extends past its edges on purpose; nothing previously clipped that oversized/offset root to the canvas's own area (root's own `overflow:hidden`/`contain:strict` only ever clipped root's CHILDREN to root's own pre-transform box, never root itself). `CNA_HtmlDom_EnsureRoot`/`CNA_HtmlDom_DestroyRoot` create/tear down the wrapper alongside root. **Verified in a real browser** with a deliberately 2:1-mismatched virtual/physical aspect ratio (virtual 256x128 against a 128x128 physical surface, so `Letterbox`/`Overscan`/`Stretch` each produce genuinely different, hand-derivable geometry, not the "happens to already fit" case `FixedHeightDynamicWidth` alone could pass before this task), `htmldom_smoke_test.cpp` (48→63 checks): `Letterbox` scale=0.5/0.5 with a real 32px top/bottom bar; `Overscan` scale=1.0/1.0 with a real -64px left/right crop, and the wrapper measured staying exactly 128x128 despite root's own oversized/offset box (proving the crop is real, not just computed); `Stretch` scale=0.5/1.0 (independently, unlike every other mode); `NativeBackBuffer` scale=1/1 with `GetViewportSize` reporting the physical 128x128, not the still-configured 256x128 virtual resolution; `FixedHeightDynamicWidth` re-confirmed correct after cycling through all four other modes first; `SetPresentationMode(99)` throwing and leaving the prior mode's geometry untouched; a window coordinate in a `Letterbox` bar returning false while one inside the content band returns true at the hand-derived logical position; and an exact `TransformLogicalToWindow`/`TransformWindowToLogical` round trip under `Stretch`'s independent per-axis scale. Every hand-derived expected value matched the real measured browser result exactly, and the pre-existing final HTMLDOM-101 screenshot-pixel checks (which depend on being the LAST draw of the whole test) still passed unaffected by the new frame inserted before them. **Regression-proved**: reverting the fix reproduced exactly the predicted failure -- all 15 new checks failed, the 48 pre-existing ones unaffected -- then restoring returned 63/63. Full regression pass: 63/63 smoke, 28/28 pixel, 8/8 stress, 10/10 dispose, 40/40 GTest. |
| HTMLDOM-109 | ✅ | **Replaced the variant-cache FIFO with a real, lifecycle-safe LRU (reopens HTMLDOM-23/90). Confirmed defect, fixed.** `cnaDomGetVariant` returned a cache hit without touching its position -- a real FIFO wearing an LRU's name, since only creation order (never USE order) protected an entry from eviction. `UpdatePixels`, render-target bind invalidation, and texture destruction each reset `entry.variants = {}` (the per-entry lookup map) without removing the corresponding records from the separate global `cnaDomVariantLru` array, so stale `(id,key)` pairs survived past the pixels/entry they were derived from -- able to later delete a freshly regenerated variant sharing the same key, or simply rot as a wasted eviction slot. **Fix**: replaced the two-structure design (a per-entry `variants` object plus a separate FIFO array) with ONE global `Module['cnaDomVariantCache']`, a real JS `Map` keyed `id + ':' + key`, whose own insertion order IS the recency order the cache relies on. A cache HIT (`cnaDomVariantCacheGet`) deletes and re-inserts its entry, moving it to the youngest position in O(1) amortized -- genuine hit-promotion, not merely bounded FIFO. Each Map value also carries its owning texture entry, so `cnaDomVariantCacheClearOwner` (called from `UpdateTexture`, the render-target bind-invalidation path, and `DestroyTexture` -- everywhere that used to just reset `entry.variants = {}`) removes exactly that texture's own live records in one pass; a full scan there is bounded by the cache's own 256-entry cap and only runs on these rare, non-per-frame lifecycle events, never per draw. The one free, memory-owning-nothing untinted-straight base variant (`mode 0`, no tint) is kept OUTSIDE the capped cache entirely, on a dedicated `entry.sharedBaseVariant` field, so it can never compete with real (memory-owning) variants for one of the 256 slots -- unchanged behaviour from before, just no longer piggybacking on the same lookup object the capped variants used. `cnaDomGetMirrorVariant`/`cnaDomGetPaddedVariant` were updated to the same `cnaDomVariantCacheGet`/`Put` calls, so eviction and lifecycle cleanup stay unified across every variant kind. **Verified with a deterministic eviction-identity scenario**, not a loose `length <= 256` bound: `htmldom_stress_test.cpp` (3→8 checks) fills the cache to exactly its 256-entry cap with distinct tints (g/b fixed, so r alone spans the entire key space for that texture/mode -- deterministic, not probabilistic), touches key `r=0` again (a pure cache hit), then inserts one brand-new never-seen key -- a real LRU must evict `r=1` (the next-oldest UNTOUCHED key), not `r=0` (just touched) and not the new insertion; measured and asserted exactly that identity, plus that the cache stays pinned at exactly 256 rather than growing past it. A second new frame proves `SetData` drops a texture's own stale cache record and the same key is cleanly reusable afterwards (regenerates fresh from the NEW pixels, not permanently poisoned). `htmldom_dispose_test.cpp` (6→10 checks) proves destroying a texture, and separately rebinding a render target, each shrink the global cache by EXACTLY their own contribution (delta-asserted, not just "still under 256"). The existing HTMLDOM-90b soak-run check (300 frames, far more than 256 distinct tints) was kept as a complementary coarse sanity bound and confirmed to reach the cache's cap exactly (measured `256`, not merely `<=256`) under real sustained churn. Full regression pass: 48/48 smoke, 28/28 pixel, 8/8 stress, 10/10 dispose, 40/40 GTest. |
| HTMLDOM-110 | ✅ | **Corrected the retained/static-frame performance claim, instrumented real measurement, and fixed a genuine unconditional per-frame write the investigation found (reopens the design premise and HTMLDOM-32/89). Confirmed documentation/measurement defect, AND a confirmed code defect discovered while investigating it.** A normal XNA game resubmits its static sprites every frame; `End()` always crosses into JS and walks every command (`CNA_HtmlDom_FlushSprites` runs on every non-empty flush, full stop), and per-property diffing means only the properties whose VALUES actually changed get written -- "zero cost, no JS runs" was never literally true, only "zero CSS writes for an unchanged sprite" was. **Instrumented first, as the task's own methodology required**: added `Module['cnaDomFlushCallCount']` (every real flush call) and `Module['cnaDomStyleWriteCount']` (every actual `style.X = ...` assignment across all 11 diffed properties, including both z-index sites) directly in `CNA_HtmlDom_FlushSprites`. **The instrumentation immediately found a real, previously-undocumented defect, not just confirmed the expected "JS still runs" nuance**: HTMLDOM-103's per-flush paint-order counter (`cnaDomPaintOrderCounter`) increments on every flush and never repeats a value, so the default `'full'`-region sprites' own `style.zIndex = paintOrder` diff check (`st.zi !== paintOrder`) was ALWAYS true -- every sprite got a real CSS write on EVERY single flush, even for perfectly static, byte-identical resubmitted content, measured at exactly `spriteCount * frameCount` writes (200 sprites × 30 frames = 6000, confirmed exactly). **Fixed**: z-index only matters at all to interleave DIFFERENT regions' paint order against each other (HTMLDOM-103's own reason for existing) -- with only the `'full'` region ever having existed in this session, DOM/pool order alone is already correct and the write is pure waste. `cnaDomGetRegion` now sets a one-way `Module['cnaDomAnyNamedRegionEverCreated']` flag the FIRST time any named region is ever created; `'full'`-region sprites' z-index write is skipped entirely until that flag is set, at which point every subsequent flush (starting immediately, since the diff check's own `st.zi !== paintOrder` naturally catches "never written" state) writes it correctly. Narrow, accepted, documented caveat: on the exact frame a named region is created for the first time, a `'full'` flush that already ran earlier in that SAME frame keeps its un-stamped stacking value, a one-time transition that never recurs once any named region exists -- judged not worth a retroactive same-frame re-stamp to close completely. **Verified by direct measurement**, `htmldom_stress_test.cpp` (8→10 checks): 200 byte-identical sprites resubmitted for 30 consecutive frames (same tint seed every frame, unlike every other `DrawSprites` caller in this file) measured exactly 30 flush calls (proving "no JS runs" false) and, after the fix, exactly 0 CSS property writes across the whole window (proving the corrected, narrower "nothing changes -> nothing costs anything" claim true for real) -- averaging 0.7-0.8ms/frame, a real, non-zero, but roughly 30-40% cheaper-per-sprite cost than HTMLDOM-89's own already-published moving-sprite number (2.338-2.7ms/frame for 500 MOVING sprites). **Regression-proved** with a targeted revert (forcing the z-index write branch back to unconditional while KEEPING the new instrumentation, so the counter could prove the specific defect rather than merely stop incrementing): reproduced exactly 6000 writes (200×30, confirming the root cause precisely), then restoring the fix returned to 0. **Did not implement a command/batch hash "skip identical replay" fast path**: given the ACTUAL measured JS-crossing+diffing-walk cost is already small (sub-millisecond for 200 sprites) and the only REAL waste found (the z-index write) is now fixed, a hash/generation scheme would only save a further fraction of an already-cheap operation while introducing exactly the correctness hazards the task's own text warned about (Clear/order/scissor/viewport/texture-invalidation/disappearing-sprites interactions with the pool's `used`/`highWater` cursor tracking, which a naive "skip the whole flush" would break outright) -- judged not worth the risk for the now-measured, now-honest performance story. Full regression pass: 63/63 smoke, 28/28 pixel, 10/10 stress, 10/10 dispose, 40/40 GTest. |
| HTMLDOM-111 | ✅ | **Replaced submission-only benchmarks with reproducible CPU-submission AND real end-to-end frame/render measurements (reopens HTMLDOM-89/92/99 conclusions). Confirmed methodology defect, fixed.** Both benchmarks stopped their timer immediately after `SpriteBatch::End()`, before `Present()` and before Chromium's own deferred style/layout/paint/compositor work -- structurally favouring DOM style submission (cheap, deferred) over Canvas2D/WebGL work (closer to synchronous), and unable to support an `fps-equivalent` or a real cross-backend "X is faster" claim at all. **Fix, using `requestAnimationFrame`-fencing as the task's own text asked for**: `Game::Run()` drives Emscripten's main loop via `emscripten_set_main_loop(..., fps=0, ...)` (`Game.cpp`), which per Emscripten's own documented contract means the callback is `requestAnimationFrame`-paced -- so `Draw()` already fires once per real browser frame, and the WALL-CLOCK gap between the START of one `Draw()` call and the START of the next is exactly the real frame period: it can only elapse once the browser finished whatever rendering work the PREVIOUS frame queued, the one thing the old submission-only bracket could never see. Both `examples/graphics_backend_benchmark.cpp` (the cross-backend tool, HTMLDOM-92) and `examples/htmldom_stress_test.cpp`'s own HTMLDOM-89 benchmark now report BOTH numbers, clearly separated and labelled: "submission" (the original bracket, explicitly NOT a frame rate) and "end-to-end" (the new rAF-gap measurement, a real fps). **Also fixed the workload itself, a second real methodology gap found while instrumenting**: HTMLDOM-89/92's shared per-frame sprite-position formula never actually depended on `frame_` at all -- only the TINT did -- so both benchmarks were measuring HEAVY TINT CHURN with STATIC position the whole time, despite being labelled "steady state... moving sprites costs nothing"; the opposite of this backend's own documented sweet spot. `graphics_backend_benchmark.cpp` now runs TWO workloads back to back and reports all four combinations: **stable-tint** (position animates every frame, tint per sprite is fixed -- the actual documented sweet spot) and **heavy-churn** (position AND tint both animate every frame -- the actual documented weak point, what the old single workload measured under a misleading label). `htmldom_stress_test.cpp`'s own benchmark keeps its existing heavy-churn workload unchanged (deliberately -- it is this backend's own more conservative worst-case regression gate) but its comment now says so honestly, and its own `check()` is now scoped to the submission number specifically, with the real end-to-end number reported but not gated on (the real number is dominated by this machine's own vsync/compositor cadence, which this backend's own code does not control). **A genuinely interesting, honestly-reported finding from the corrected measurement itself**: for 500 sprites in this headless-Chromium container, real end-to-end frame time for EVERY workload/technique combination measured sits in the 16-29ms range, close to (and sometimes past) this machine's own ~60fps vsync floor -- meaning the REAL bottleneck at this sprite count is substantially the browser's own display cadence, not either backend's or either tint technique's own CPU cost; submission time alone (0.5-2.7ms/frame across every combination measured) massively understated the real, user-perceptible frame cost, exactly the risk this task's own text named. **Also captures environment context this task asked for**: `navigator.userAgent` and a REAL `WEBGL_debug_renderer_info` query (via a throwaway canvas independent of whichever backend built the binary, since any WebGL context in one browser session reports the same renderer string) -- this container measured `SwiftShader` (software) rendering, confirmed and labelled rather than silently assumed, so a future `EASYGL` run's own hardware-vs-software status is never conflated with `HTML_DOM`'s or `CANVAS`'s. **Raw machine-readable samples published**: `window.__cnaBenchSamples`, one `{phase, ms}` entry per timed end-to-end frame, so a reported average is independently re-derivable. **Checked the HTMLDOM-99 experiment into the repository** (`tools/htmldom/tint-technique-experiment.html`, a real, standalone, no-Emscripten-needed, click-to-run reproduction) as this task's text explicitly required, since it did not previously exist anywhere in the repo. Running it surfaced an HONEST discrepancy, documented directly in the file rather than smoothed over: this reconstruction does NOT reproduce HTMLDOM-99's originally-cited magnitude numbers (both techniques now measure under 1ms of submission cost at 500 sprites, an order of magnitude below the 2026-08-03 numbers, most plausibly because the original script's exact diffing details could never be perfectly reconstructed from prose alone, plus real subsequent browser/compositor changes) -- what DOES remain solid and reproducible is HTMLDOM-99's actual DECISION (keep the variant-cache): at this sprite count neither technique's absolute real-world cost is large enough, in either direction, to justify a real-time SVG filter's added complexity over the `background-image`/`toDataURL` compositing this backend already ships. Full regression pass (unaffected -- these are benchmark/experiment files, not part of the pass/fail suite): 63/63 smoke, 28/28 pixel, 10/10 stress, 10/10 dispose, 40/40 GTest. |
| HTMLDOM-112 | ✅ | **Made all four browser pages runnable by one official command and executed them in CI (reopens HTMLDOM-70/71/72 and D13/D14 verification claims). Confirmed tooling gap, closed.** `run-htmldom-browser-test.sh` used to hard-code `cna_test_htmldom_smoke.html`; the pixel/stress/dispose sources' own comments said to pass those pages to the harness, but the shell interface only ever accepted a build directory. Fixed two ways. **(1) Page argument**: `run-htmldom-browser-test.sh` now takes a second `[page]` argument (`smoke`\|`pixel`\|`stress`\|`dispose`, defaulting to `smoke` for backward compatibility with every existing call site), mapped to the right target/`.html` name instead of a hard-coded string. **(2) Suite runner**: new `scripts/run-htmldom-test-suite.sh` is the one official command -- it builds a new `cna_test_htmldom_all` CMake target (`cmake/Tests/HtmlDomTests.cmake`, `DEPENDS` on all four existing test targets, `-j3`) in one call, then drives all four pages through the page-aware harness in turn, each on its own fixed port (`8731`-`8734`, collision-free between pages and between two concurrent invocations of this script sharing a runner), propagating each page's own PASS/FAIL and printing a final summary; it exits non-zero if any page failed. Ran for real against a fresh configure in this dev sandbox (Xvfb `:99`, not the real desktop display, per the project's own testing convention): **all 62 checks across all four pages passed in one invocation** (38/38 smoke, 15/15 pixel, 3/3 stress, 6/6 dispose) -- the exact counts `docs/html-dom-backend.md` already claims, now reproducible with `scripts/run-htmldom-test-suite.sh` alone instead of four separate manual build+run steps. **(3) CI**: new `.github/workflows/htmldom-ci.yml` -- checks out with submodules, clones the `sharp-runtime` sibling repo, installs and caches a pinned `emsdk` (`6.0.3`, the version verified working in this sandbox; `docs/html-dom-backend.md`'s "emsdk 6.0.5" mention predates this pin and was not re-verified, so the CI pin intentionally does not just copy that number), installs Playwright/Chromium, starts a real Xvfb `:99` (matching this project's "prefer Xvfb over the real display" convention), configures with ccache, and runs `scripts/run-htmldom-test-suite.sh`; archives the built pages as an artifact on failure so a CI failure is diagnosable from the Actions UI alone. This is the first CI workflow in the repo that mentions `htmldom` at all (`grep -ril htmldom .github/workflows/` previously found nothing). **Scope note**: "relevant benchmark correctness gates" from this task's original description is deliberately NOT included here -- `examples/graphics_backend_benchmark.cpp` (HTMLDOM-92) is a separate, differently-gated executable, and baking today's benchmark methodology into a CI gate before HTMLDOM-111 fixes that methodology (still open, reopens HTMLDOM-89/92/99) would encode the very measurement approach this same audit flagged as unsound; deferred to land alongside HTMLDOM-111 instead. CI workflow YAML validated (`python3 -c "import yaml; yaml.safe_load(...)"`) but not run on an actual GitHub Actions runner (unavailable in this sandbox) -- the underlying `scripts/run-htmldom-test-suite.sh` steps it runs were verified for real, locally. |
| HTMLDOM-113 | ✅ | **Strengthened browser tests to prove rendered semantics rather than internal implementation state (reopens all affected ✅ verification rows). Confirmed coverage defect, closed.** Deliberately the last of the "fix" tasks in this audit's own order, since it depends on the corrected oracles HTMLDOM-101/102/104/105/106 already landed. **Audit against this row's own checklist, item by item** (most were already satisfied by those individual tasks' own work, done earlier in this same pass, not duplicated again here): Opaque render-target oracle (HTMLDOM-100, preserved) ✅; corrected Clamp expected values (HTMLDOM-104) ✅; scissor `style.clipPath`/`display`-only assertions replaced with real screenshot pixels plus computed region `offsetWidth`/`offsetHeight` (HTMLDOM-101) ✅; translucent blend alpha (HTMLDOM-85/105) ✅; semi-transparent RT resampling (HTMLDOM-106) ✅; render-target scissor (HTMLDOM-102c/d) and render-target viewport (HTMLDOM-107g) ✅; region ordering/eviction (HTMLDOM-103) ✅; all presentation modes (HTMLDOM-108) ✅. **Three genuine remaining gaps found and closed by this task specifically**: (1) the stress test's pool-size assertion was still `<= 2*peak` (a headroom bound, not the exact identity this row's own text asked for) even after HTMLDOM-109 tightened the LRU cache's own assertion to exact capacity/eviction-identity -- since the pool never shrinks and creates exactly one new element per newly-reached index, its final size must equal EXACTLY the highest per-frame sprite count the whole run ever reached; tightened to `pooled == peakSpritesInOneFrame_` (confirmed `500 == 500` in the real run) in `htmldom_stress_test.cpp` (HTMLDOM-90a). (2) unsupported-browser Additive capability handling had no test coverage at all -- an automated CI browser cannot itself be downgraded to exercise the actual fallback path, but nothing confirmed the ASSUMPTION every other Additive pixel test depends on (that `mix-blend-mode: plus-lighter` is genuinely supported here, not silently degraded to normal blending); added a `CSS.supports('mix-blend-mode', 'plus-lighter')` check to `htmldom_smoke_test.cpp` (48→64 checks total across this task and HTMLDOM-108's own additions), confirmed true in this test environment. (3) viewport coexistence -- HTMLDOM-107g/HTMLDOM-102c/d each proved `Viewport`/`ScissorRectangle` independently on the Canvas2D render-target path, but never together in the same batch; added a pixel test with BOTH a non-default `Viewport(4,4,10,10)` and a non-default `ScissorRectangle(2,2,6,6)` active simultaneously, sprite drawn so the viewport-offset footprint and the scissor rect genuinely overlap only partially (not "everything visible" or "everything clipped") -- three sample points independently prove the viewport offset still applies, the scissor clip still applies, AND the two compose correctly rather than one masking the other, confirming HTMLDOM-107's own design claim that they are independent absolute-space concepts (`htmldom_pixel_verification_test.cpp`, 28→31 checks). **Deliberately out of scope, not silently dropped**: `SpriteSortMode::Immediate` is HTMLDOM-118's own later task -- its semantics are not yet even defined, so testing it here would be premature. Full DOM-vs-Canvas pixel PARITY (asserting the DOM `<div>` backbuffer path and the Canvas2D render-target path produce byte-identical results for the same draw) is architecturally impossible to test directly: design decision 11 means the DOM backbuffer can never be read back at all, so every feature is already verified via whichever path CAN prove it (structurally for the DOM path, pixel-exact for the Canvas2D path) -- the existing split IS the closest approximation to parity coverage this backend's own constraints allow, not a gap this task could close further. **HTMLDOM-119 correction**: this row's own "architecturally impossible to test directly" claim overstated the DOM-path half -- the SAME real-screenshot mechanism this row's own text credits to HTMLDOM-101 one sentence earlier (`page.screenshot()` + in-page `getImageData` on the decoded PNG) generalizes directly to a real cross-path comparison, which HTMLDOM-119 built (`verifyPixelVerificationScreenshot`) and used to find a genuine parity divergence (Canvas2D-path atlas edge bleed vs. DOM-path none) that no amount of "verify each path separately" coverage could have surfaced. Full regression pass: 64/64 smoke, 31/31 pixel, 10/10 stress, 10/10 dispose, 40/40 GTest. |
| HTMLDOM-114 | ✅ | **Investigated process-global/multi-window state; confirmed and fixed a real destruction-ordering defect the audit's own risk framing predicted (reopens the design premise). Audit risk -> confirmed defect, fixed.** First established the ACTUAL constraint CNA operates under, rather than assuming: `Game::Run()` drives Emscripten's main loop via `emscripten_set_main_loop(..., simulate_infinite_loop=1)`, which never returns to its caller (a documented Emscripten mechanism, not a CNA choice) -- so two INDEPENDENTLY-DRIVEN `Game::Run()` loops can never coexist in one process; `GraphicsDevice::Reset()` (checked directly in `GraphicsDevice.cpp`) mutates the EXISTING backend in place (`SetVirtualResolution`/`ApplyMultiSampleCount`/etc.) and never destroys/reconstructs it, so "backend reset with surviving resources" does not apply to `HtmlDomGraphicsBackend` the way the ticket's text assumed. The ONE genuinely reachable multi-instance scenario is a SECOND `HtmlDomGraphicsBackend` constructed (via the public API, nothing in `IGraphicsBackend` prevents it) while a FIRST is still alive, sharing the same window -- e.g. a diagnostic/probe backend, or exactly the kind of test this task itself needed to write. **Confirmed real defect**: `CNA_HtmlDom_EnsureRoot`'s own "already initialized" guard meant a second backend's construction was a silent no-op for the shared JS surface (harmless on its own), but `CNA_HtmlDom_DestroyRoot` tore that SAME shared surface down UNCONDITIONALLY -- so destroying the second backend alone broke the FIRST, still-alive one, purely because another backend happened to have existed and been destroyed. **Fix**: reference-counted, not boolean-guarded. `Module['cnaDomBackendRefCount']` increments on every `CNA_HtmlDom_EnsureRoot` call that gets past the "no document" (native GTest) early-out, decrements on every `CNA_HtmlDom_DestroyRoot` call; the DOM surface is only actually torn down once the count reaches 0. The ordinary single-backend case still goes 0->1->...->0 exactly as before -- this only changes behaviour when a second backend genuinely coexists. C++-side per-draw statics (`CurrentCompositeOp`/`BoundRenderTargetId`/`CurrentScissorEnable` in `HtmlDomState.cpp`) were already correctly rewound by every constructor and deliberately left as-is: unlike the retained DOM tree, this state is always re-established by the NEXT `ApplyBlendState`/`ApplyRasterizerState`/`SetRenderTarget2D` call before any draw reads it, so a second backend's construction resetting it mid-session is benign, not a correctness bug worth its own fix. **Decision on the ticket's own explicit question**: CNA does NOT need to enforce exactly one live device -- multiple `HtmlDomGraphicsBackend` instances sharing one window are now SAFE (reference-counted shared surface lifetime) rather than forbidden, which is less invasive than namespacing every JS/C++ global per instance and does not restrict any currently-working usage. **Verified in a real browser**, `htmldom_dispose_test.cpp` (10->17 checks): constructs a second `HtmlDomGraphicsBackend` sharing the real backend's own window while the first is alive, confirms the ref count reaches exactly 2 and the surface still exists; destroys the second, confirms the ref count returns to exactly 1 AND -- the actual regression this task fixes -- the shared surface genuinely SURVIVES rather than being torn down; confirms the real (first) backend's own texture create/destroy path still works normally afterward. **Regression-proved** by reverting: the ref-count-value checks failed as expected (the counter did not exist), and critically the "surface survives" check failed for real, reproducing the exact defect before the fix; restoring returned 17/17. Full regression pass: 64/64 smoke, 31/31 pixel, 10/10 stress, 17/17 dispose, 40/40 GTest. |
| HTMLDOM-115 | ✅ | **Hardened DOM-surface integration with the host page. Audit risks -> two confirmed defects fixed, remaining items addressed or documented.** Investigated and resolved each item in the ticket's own checklist. **Fixed (confirmed real defects)**: (1) `canvas.style.opacity` was unconditionally overwritten `'0'` then restored to a hardcoded `""` on teardown, silently discarding whatever the host page had ALREADY set it to (e.g. a page that already gives the canvas a custom opacity before this backend ever runs) -- `CNA_HtmlDom_EnsureRoot` now captures the real pre-existing value into `Module['cnaDomOriginalCanvasOpacity']` and `CNA_HtmlDom_DestroyRoot` restores exactly that. (2) a page that already has ITS OWN unrelated element with id `cna-dom-root`/`cna-dom-viewport` was silently ignored -- this backend would just create ANOTHER element with the same (invalid, but browser-tolerated) id, and any LATER `document.getElementById` lookup elsewhere could resolve to the wrong one; `CNA_HtmlDom_EnsureRoot` now detects this up front and refuses to proceed with a clear `console.error`, the same "fail loudly rather than silently corrupt" shape the pre-existing missing-canvas case already used. **Addressed**: canvas offset-parent and page SCROLL need no fix at all -- `offsetLeft`/`offsetTop` (what geometry positioning already reads) are relative to the nearest POSITIONED ANCESTOR, not the viewport, so the DOM surface's wrapper (a sibling of canvas, sharing the same ancestor chain) scrolls together with the canvas and stays aligned by construction, verified by re-deriving the math rather than assumed. A `window.resize` listener now re-syncs geometry when a host-page layout reflow MOVES the canvas without resizing it (Present()'s own dirty check only fires on a genuine size change) -- `ResizeObserver` was considered and rejected: it fires only on SIZE changes, never on a pure reposition, so it would not have closed this specific gap at all. Z-order is now explicitly documented as plain DOM order (the wrapper is inserted immediately after the canvas) rather than left unstated. **Documented as an accepted, narrow gap, not fixed**: a canvas positioned via a CSS `transform` (scale/rotate/skew) -- `offsetLeft`/`offsetTop` are LAYOUT-only properties and do not reflect a transform's purely visual effect at all; closing this properly would need `getBoundingClientRect()`-based positioning (with its own reflow-forcing-if-misused caveats) and is disproportionate to the ticket's own risk framing. A pure CSS-only reposition with no window resize at all (e.g. an animated `left`/`margin` elsewhere on the page) is likewise not caught by the `resize` listener -- also documented, not engineered around, given the cost/benefit of a full layout-change-detection mechanism for this narrow case. **Verified in a real browser** with a NEW, purpose-built harness (`scripts/htmldom-host-integration-test.mjs`, wired into `run-htmldom-browser-test.sh`/`run-htmldom-test-suite.sh` as a fifth `host-integration` page reusing the smoke build): both behaviours can only be observed by manipulating the DOM BEFORE the Emscripten module's own bootstrap script runs its constructor chain, which happens extremely early; `page.route()` HTML-response-body rewriting (injecting markup directly into the page's own source text) proved to be the one reliable way to win that race -- `page.addInitScript()` was tried first and measurably was NOT early enough, confirmed by a side-by-side comparison while building this harness. Test 1 rewrites the static `<canvas id="canvas">` tag to carry a distinctive pre-set `opacity:0.42`, then confirms `Module['cnaDomOriginalCanvasOpacity']` captured it exactly. Test 2 injects a host-page-owned `<div id="cna-dom-root">` as the literal first child of `<body>`, then confirms the backend logged its refusal, never created its own surface (`Module['cnaDomRoot']` stays null), and the host's own element survives untouched. **Regression-proved** by reverting: the ID-collision check failed for real (the old code silently created its own surface despite the conflict) and the opacity check timed out (the capture never existed); restoring returned 2/2. Full regression pass: 69/69 smoke, 36/36 pixel, 10/10 stress, 17/17 dispose, 2/2 host-integration, 40/40 GTest. |
| HTMLDOM-116 | ✅ | **Measured and bounded compositor-layer/DOM memory policy instead of forcing `will-change:transform` on every sprite unconditionally forever. Audit risk confirmed real, then fixed.** Read the real behavior first: every pooled sprite `<div>` (`HtmlDomSpriteBatchBackend.cpp`'s pool-element creation) gets `will-change:transform` unconditionally, and the pool NEVER released an element once created -- `CNA_HtmlDom_Clear`/`CNA_HtmlDom_PresentFrame` only ever hid elements past the current frame's count via `display:none`, never removed them, while `pool.length` (the array's own true high-water mark, since indices are only ever assigned upward and never truncated) only ever grew. So a single transient burst (e.g. a one-time 10,000-sprite explosion effect) permanently retained that many DOM nodes and compositor layers for the rest of the session, even after usage settled back down to a handful -- the "thousands of permanent sprite layers" risk the ticket named was real, not hypothetical, confirmed by first REVERTING the fix below and observing the pool stay pinned at 10,000 through a 190-frame settled-idle window that should have shrunk it. **Fix**: `CNA_HtmlDom_PresentFrame` (`HtmlDomGraphicsBackend.cpp`) now tracks, per region, how many consecutive `Present()` calls its sprite count has been EXACTLY unchanged (`idleUsed`/`idleStreak`, reset to 0 the instant the count differs from the previous frame's). Once that streak reaches 180 frames (~3 s at 60 fps -- deliberately longer than any realistic action-to-idle gap a real game's own pacing would produce between bursts, so ordinary gameplay oscillation never trips it), the pool entries above the settled count are actually `removeChild`'d from the DOM (not merely hidden) and the pool array itself is truncated to match, releasing their compositor layers. Regrowth afterward works exactly like the pool's own normal lazy-creation path always has -- no special-casing needed. **Measured, not just asserted** (`examples/htmldom_memory_test.cpp`, a new `cna_test_htmldom_memory` page): a 5,000-sprite burst creates exactly 5,000 pool elements in ~45 ms, growing to 10,000 in a further ~44 ms (well within a generous 500 ms/10,000-element budget -- no O(n²) blowup); after 190 frames settled at 20 sprites/frame (down from the 10,000 peak) the pool genuinely shrinks to exactly 20 elements; drawing 5,000 sprites again afterward regrows the pool to exactly 5,000, proving the age-out never leaves it permanently undersized; a 200-frame run oscillating between 5,000 and 1,250 sprites every single frame (never repeating the same count twice in a row, deliberately longer than the 180-frame shrink threshold) leaves the pool pinned at its own 5,000 peak throughout, exactly like it always did before this task -- proving the age-out costs nothing during ordinary fluctuating gameplay. **Regression-proved** by disabling the age-out branch (`if (false && used === region.idleUsed)`) and rebuilding: exactly the 3 predicted checks failed (pool stuck at 10,000 through the idle window, through the regrowth-measurement frame, and through the oscillation run), all other checks (the burst-creation counts/timing) still passed as expected since they don't depend on the age-out at all; restoring returned 6/6. A single configurable numeric threshold was chosen over a more elaborate GPU-memory-introspection-driven policy: real per-layer GPU/compositor memory is not reliably queryable from in-page JS at all (no standard API exposes it), and Chrome DevTools Protocol's own `LayerTree` domain is unreliable enough under headless automation that gating production behavior on it was rejected as disproportionate to this ticket's own risk framing -- DOM node count (which this fix directly bounds) is the backend's own accurate proxy for compositor layer count by construction, since every pool element gets exactly one `will-change:transform` layer. Full regression pass: 64/64 smoke, 31/31 pixel, 10/10 stress, 17/17 dispose, 2/2 host-integration, 6/6 memory, 40/40 GTest. |
| HTMLDOM-117 | ✅ | **Aligned documentation and capability reporting with actual timing and architectural limits -- the last item in this D16 audit backlog, done last so it could summarize the state after every other reopened task actually landed.** Each of the four confirmed inconsistencies verified against the real code (not assumed) before correcting the text: (1) **Data URL timing**: `cnaDomEnsureUrl` (`HtmlDomTextureBackend.cpp`) is called from the sprite-FLUSH path (`HtmlDomSpriteBatchBackend.cpp`), never from texture creation/`SetData` -- the PNG encode is genuinely lazy, paid the first time a DRAW actually needs a given texture/tint/mode variant's URL (idempotent: checks `variant.url` first), not "once at upload time" as design decision 6 used to claim; `SetData`'s own cost is cheap by contrast (a `putImageData` raster write plus dropping stale cached variants) and does not itself encode anything. Corrected in `plan_html_dom.md` design decision 6 and `docs/html-dom-backend.md`'s Texture upload cost / Slow-here / Known-limitations-#2 entries. A texture uploaded but never drawn now correctly reads as paying zero encode cost, not a hidden claim the old wording implied. (2) **Accessibility overclaim**: "accessibility tooling" was listed as a benefit of this backend in the top-of-file framing, but every pooled sprite `<div>` carries no ARIA role/label, no focus handling, and `pointer-events:none` -- a screen reader sees a DOM node with zero semantic information. Narrowed to the claim that is actually true (DOM-based debugging/inspection: real elements visible in devtools, unlike a `<canvas>`'s opaque pixel buffer), per the ticket's own "narrow the claim" option -- adding real accessibility (roles/labels/an opt-in metadata hook) would be a genuinely new feature, not a documentation fix, and stays out of scope. (3) **Scope statement reconciliation**: the old text claimed "everything on the 3D surface... throws... via `NotYetImplemented()`" as one uniform mechanism. Verified per-type by reading each constructor rather than assumed: direct 3D state/draw methods genuinely do throw via that helper (unchanged, accurate); the five 3D resource FACTORY methods are NOT overridden at all (HTMLDOM-61) and keep `IGraphicsBackend`'s null default, but what a caller through the PUBLIC API actually observes differs per type -- `Texture3D`'s own constructor checks `SupportsCapability(Texture3D)` first and throws `System::NotSupportedException` immediately (REMED-CONTENT-004); `TextureCube`/`RenderTargetCube` construct successfully with a silently-null backend, only the FIRST `SetData`/`GetData` call throws; `OcclusionQuery` is more permissive still -- its accessors (e.g. `IsComplete`) just return a default value with no exception ever; `Effect`'s stock constructor never touches a backend resource here at all, its compiled-bytecode constructor throws `NotImplementedException` unconditionally for every backend, not a HTML_DOM-specific gap. Corrected in the Scope section and `docs/html-dom-backend.md` §6 -- explicitly flagged as a shared-layer (not HTML_DOM-specific, not fixed here) inconsistency worth its own dedicated audit outside this plan, matching `CANVAS`'s own identical Phase C7 choice. (4) **Additive support, made queryable, not just silently degrading**: added `GraphicsCapability::AdditiveBlending` to the shared enum (purely additive -- every other backend's own `switch...default: return true`/inherited-`true` `SupportsCapability` needs zero changes, confirmed by reading all nine overriding backends plus the shared base default). `HtmlDomGraphicsBackend::SupportsCapability` -- previously a blanket `return false` for every capability -- now special-cases this one via a new memoized `CNA_HtmlDom_SupportsAdditiveBlending()` (the same `CSS.supports('mix-blend-mode','plus-lighter')` query HTMLDOM-113's own test-only `JsSupportsPlusLighter` already used, now exposed as a real, public, game-facing capability query instead of only a test assertion). Verified in a real browser (`htmldom_smoke_test.cpp`, 68→69 checks): `GraphicsDevice::SupportsCapability(AdditiveBlending)` agrees exactly with the raw CSS query. **Regression-proved**: reverting `SupportsCapability` to its old blanket `return false` and rebuilding reproduced exactly the predicted failure (the new check failed, since the browser genuinely supports `plus-lighter` here but the reverted code claimed it did not); restoring returned 69/69. Plus a native GTest (`SupportsCapabilityAnswersAdditiveBlendingNatively`) confirming the correct `false` answer under `node`, where no CSS engine exists to ask. Full regression pass: 69/69 smoke, 35/35 pixel, 10/10 stress, 17/17 dispose, 2/2 host-integration, 6/6 memory, 54/54 GTest. **Capability matrix updated to match reality, closing out this entire D16 audit backlog** -- every one of HTMLDOM-101 through HTMLDOM-120 is now ✅, verified rather than assumed. |
| HTMLDOM-118 | ✅ | **Defined and implemented real `SpriteSortMode::Immediate` semantics (reopens HTMLDOM-30/76 and the per-batch scissor rationale). Confirmed contract gap, fixed.** Investigated first: `SpriteBatch.cpp`'s shared layer already correctly bypasses its own `spriteQueue_` for Immediate, calling `backend_->Draw()` synchronously from inside `pushSprite()` -- that half of the contract was never the gap. But `ISpriteBatchBackend` had NO sort-mode concept at all (confirmed by reading the whole interface), so a backend's `Draw()` had no way to distinguish an Immediate call from a Deferred one -- `HtmlDomSpriteBatchBackend::QueueDraw` always just appended to `commands_`, flushed once at `End()`, regardless. Checked whether another CNA backend already had a working pattern to port: `SDL_Renderer`/`CANVAS` are trivially "Immediate" only because their native APIs never batch at all (every `Draw()` reaches the GPU/Canvas2D synchronously, for every sort mode); `EasyGL`/`SdlGpu` (and, by the same architecture, every other GPU-command-buffer backend) batch internally exactly like HTML_DOM and have the IDENTICAL gap -- there was no existing "true immediate + still batches for Deferred" hybrid anywhere in this codebase to copy. **Fix**: added one new, purely additive `ISpriteBatchBackend::SetImmediateMode(bool)` virtual with a default no-op body (`IGraphicsBackend.hpp`) -- the same optional-override shape as the interface's existing `SetCustomEffect`/`SetSamplerFilter`/`SetSamplerAddressMode`, so every other backend's source needs zero changes and keeps its exact current behavior. `SpriteBatch::Begin()` now calls it once, with `sortMode_ == SpriteSortMode::Immediate`, right before `backend_->Begin()`. `HtmlDomSpriteBatchBackend` records it (`immediateMode_`); `QueueDraw()` now flushes an Immediate command as its own one-sprite "batch" the instant it is built, via the SAME `CNA_HtmlDom_FlushSprites` call `End()` uses, and never queues it into `commands_` (so a real per-Draw() JS crossing happens, and nothing gets double-flushed at `End()`, which sees an empty array and no-ops). No extra per-draw state-capture plumbing was needed: `Module['cnaDomScissorRect']`/`['cnaDomViewport']`/`GetCurrentCompositeOpEXT()`/`GetCurrentScissorEnableEXT()` are ALL already updated eagerly, independent of any batching, by `SetScissorRect`/`SetViewport`/`ApplyBlendState` -- reading them synchronously at the exact instant of each Immediate `Draw()` call already gives the correct per-draw answer for free. **Verified in a real browser** (`htmldom_smoke_test.cpp`, 64→68 checks, frame 12): a scissor rect CHANGE between two `Draw()` calls inside one `Begin(Immediate)/End()` block sends the FIRST sprite to the region that was active when IT was called and the SECOND sprite to its own, different region -- two small, non-overlapping regions positioned far from the frame's own final scissor-straddle screenshot check, so neither interferes with it. A control -- the identical interleaved sequence under `Deferred` -- confirms that mode's own real, unaffected, single-end-of-batch-snapshot contract: no region is created for the rect that was active mid-batch (it was simply never current at `End()`), and BOTH sprites land wherever the rect ended up by `End()`. **Regression-proved**: disabling the immediate-flush branch (`if (false && immediateMode_)`) and rebuilding reproduced exactly the predicted failure -- both Immediate checks failed (both sprites fell back to landing together, matching the OLD Deferred-identical behavior) while the two Deferred control checks (an unaffected code path) still passed; restoring returned 68/68. **Pure-C++ coverage**: `HtmlDomSpriteBatch.SetImmediateModeIsAcceptedAndDoesNotThrow` covers the setter itself; the actual per-draw-flush behavior needs a real `ITextureBackend` and a real JS flush to observe (this file's own pre-existing `DrawBeforeBeginThrows` comment already notes a bare texture reference can't be reached without one), so it is browser-verified only, the same "not unit-testable without a browser" line HTMLDOM-104 already drew for JS-side variant generation. Full regression pass: 68/68 smoke, 31/31 pixel, 10/10 stress, 17/17 dispose, 2/2 host-integration, 6/6 memory, 41/41 GTest (including all 12 pre-existing `SpriteBatchSortModeTest` cases, confirming the new interface method didn't disturb the shared layer's own contract). |
| HTMLDOM-119 | ✅ | **Audited every source-rectangle/filtering edge case the ticket named beyond the already-fixed Clamp defect. Six already correct or reasoned-sound, one genuine cross-path divergence found and documented, zero new throws warranted.** Investigated each sub-case against the real code before writing any test: (1) fully-outside/zero-size source rectangles already can't reach the JS flush at all -- `HtmlDomSpriteBatchBackend::QueueDraw` guards `sourceRectangle.Width<=0 || Height<=0` before `BuildDrawCommandEXT` is even called; (2) negative `origin` is plain unconditional negation (`cmd.localX = -origin.X`) with no sign branch anywhere in the geometry pipeline, so it composes identically to a positive pivot, just offset the other way -- confirmed with a new GTest (`NegativeOriginIsAcceptedAndComposesLikeAnyOtherPivot`); (3) Wrap/Mirror phase for a negative source-rectangle origin relies on native infinite `background-repeat`/`CanvasPattern` tiling, never a hand-rolled modulo that could be sign-asymmetric -- confirmed via two new hand-derived pixel tests using a source origin of (-1,-1), a full period off the (0,0) origin every pre-existing Wrap/Mirror test in this suite used, so these are genuinely NEW oracle coverage, not restatements; (4) `cnaDomGetMirrorVariant` builds its 2x2 canvas from the ALREADY-tinted base variant and never hardcodes the original (pre-doubling) texture size anywhere its own tile-period math could read it, so mirror-of-a-tinted-texture periods stay correct by construction -- confirmed by re-reading the function, no test needed since there is no code path where the wrong size COULD be read. **The one real, measured finding**: (5/6/7 combined) an ordinary, IN-BOUNDS source rectangle drawn from a larger, unpadded atlas texture, at a large fractional scale (19.5x/9.75x) under linear filtering, is where the two draw paths genuinely diverge from EACH OTHER -- new pixel tests in both `htmldom_pixel_verification_test.cpp` (Canvas2D path, via `RenderTarget2D::GetData`) and a new `verifyPixelVerificationScreenshot` harness function in `scripts/htmldom-browser-test.mjs` (DOM path, via a real Playwright screenshot, the same design-decision-11 workaround HTMLDOM-101 already proved out) measure the SAME scenario on each path independently against a hand-derived expectation, rather than comparing the two paths' outputs to each other (which could pass even if both bled identically -- a false sense of confidence). Measured: the Canvas2D path bleeds the atlas's adjacent, undrawn region in at almost exactly the hand-derived bilinear interpolation weight (sampling 0.397 texels past the last drawn texel's centre toward the next one predicts (154,0,101); measured (147,0,108)) -- the CORRECT, hardware-matching result, since real D3D/XNA sampling an unpadded atlas under `LinearWrap`/`LinearClamp` exhibits the identical bleed (why real games pad their own atlases) -- while the DOM path measures ZERO bleed at the identical sample point, because a `background-image` element's own box is a hard clip by CSS spec, structurally unlike `drawImage`'s explicit sub-rectangle-extraction-then-resample model. **Resolution: documented, not "fixed" toward parity** (`docs/html-dom-backend.md` Known Limitations #14) -- padding every Canvas2D-path draw to eliminate its bleed would make THAT path diverge FROM real hardware behavior, the wrong direction; a game needing pixel-identical results across both paths should pad its own atlas (standard practice regardless of backend) rather than this backend papering over a real, correctly-reproduced hardware characteristic. **Throw-before-JS audit (H)**: every genuinely unreproducible combination already throws (`ValidateAddressModes` for mixed Mirror, the `kMaxClampPadding` cap for Clamp overflow, both pre-existing); none of the cases audited here warrant a NEW throw -- fractional-scale-with-linear-filtering and atlas edge bleed are both ordinary, extremely common, legitimate draw calls (rejecting them would reject the overwhelming majority of real sprite-batch usage), so the correct treatment is measurement + documentation, matching the HTMLDOM-105 precedent, not a runtime guard. Full regression pass: 68/68 smoke, 35/35 pixel (+2 real screenshot pixel checks), 10/10 stress, 17/17 dispose, 2/2 host-integration, 6/6 memory, 42/42 GTest. |
| HTMLDOM-120 | ✅ | **Audited direct-backend validation across every area the ticket named (the last item in this audit backlog). Two confirmed real gaps fixed, several already safe by construction or an established cross-backend convention, all now proven with tests rather than left implicit.** Investigated each area against the real code, and against sibling backends (EasyGL, SdlGpu, Canvas, SDL_RENDERER) to avoid inventing an inconsistent HTML_DOM-only convention, before writing any fix. **Fixed (confirmed real gaps, code changes)**: (1) `HtmlDomTextureBackend`'s two constructors (used by both `Texture2D` and, via `HtmlDomRenderTargetBackend`, `RenderTarget2D`) now throw `System::ArgumentOutOfRangeException` for a zero/negative width or height -- neither `Texture2D.cpp` (upper-bound-only) nor `RenderTarget2D.cpp` (no size validation at all; it never even reaches `Texture2D`'s own checks, since it bypasses the two constructors that call them) rejects this anywhere upstream, so a degenerate size would otherwise reach `new OffscreenCanvas(w,h)`/`canvas.width=w`, whose behavior there is browser-implementation-defined and was untested. (2) `HtmlDomGraphicsBackend::ReadBackbuffer` now validates a null `pixels` pointer and a non-positive/negative region before calling `CNA_HtmlDom_ReadBound` -- without this, a null pointer would write into the START of the wasm heap via `HEAPU8.set`, real memory corruption, not a clean crash; `GraphicsDevice::GetBackBufferData` (the shared layer) already validates a null buffer and bounds-checks against `PresentationParameters`, but only a DIRECT backend call bypassing it was ever exposed to the uncaught case. (3) `HtmlDomGraphicsBackend::SetRenderTargets` now throws `System::ArgumentNullException` for a null descriptor array with `count>0` -- `GraphicsDevice::SetRenderTargets` (the only caller a real game ever goes through) always pairs a null pointer with `count=0`, so `renderTargets[0]` dereferencing a null pointer was only ever reachable via a direct call bypassing the shared layer entirely, exactly what this audit targets; confirmed the identical unguarded pattern exists verbatim in `CanvasGraphicsBackend`/`SdlGraphicsBackend` too (out of this ticket's own HTML_DOM scope to fix there). **Already safe by construction (confirmed, not assumed)**: `SetSamplerFilter`'s out-of-range fallback matches its own documented interface contract ("others map to nearest"); `SetVirtualResolution`/`SetViewport` store raw values unvalidated, but `virtualWidth_<=0||virtualHeight_<=0` (negative included) already takes the same "no virtual resolution configured" early-out the existing `(0,0)` case does, and `SetViewport`'s `w`/`h` are read nowhere except `.x`/`.y`, with `minDepth`/`maxDepth` literally unnamed, never read or stored at all -- genuinely inert, not merely untested. **Documented as an established, accepted cross-backend convention (not a HTML_DOM-only fix)**: `SetSamplerAddressMode` stores its ordinal unvalidated, and `ValidateAddressModes`' own mismatch check can only ever fire when one value IS Mirror (2), so an out-of-range ordinal (matched or not) never trips it -- identical unchecked address-mode translators exist in `CanvasSpriteBatchBackend` and `EasyGLGraphicsBackend`'s own GL wrap-mode mapper. `depthFormat`/`mipMap`/`multiSampleCount` requested at `RenderTarget2D` construction reach `CreateRenderTarget2D` and are genuinely discarded (`docs/html-dom-backend.md`'s new capability-table row) -- byte-identical signatures/behavior in `SDL_RENDERER`/`CANVAS`; `multiSampleCount` is at least corrected to the real (always 0) value afterward via the shared layer's own existing pattern, `depthFormat`/`LevelCount` are not, a real but narrow honesty gap in the SHARED `RenderTarget2D.cpp` affecting every 2D-only backend equally, deliberately not changed unilaterally here. `RenderTargetUsage.PreserveContents`/`DiscardContents` genuinely IS normalized upstream (`GraphicsDevice::SetRenderTarget` issues its own `Clear()` on `DiscardContents`) -- proof, not assumption. **11 new GTest cases** (40→53, all direct-backend, no browser needed): out-of-range sampler filter/address-mode ordinals don't throw (2 tests, `HtmlDomSpriteBatch`) plus the free-function-level `ValidateAddressModes` case (`HtmlDomAddressModes`); negative virtual resolution behaves exactly like `(0,0)`; `SetViewport` accepts negative dimensions and an inverted depth range inertly; `SetRenderTargets(nullptr, 1)` throws `ArgumentNullException`; `ReadBackbuffer` (once a target is bound) throws for a null pointer and each of x/y/w/h being invalid; both `HtmlDomTextureBackend` constructors and `HtmlDomRenderTargetBackend` throw `ArgumentOutOfRangeException` for zero/negative dimensions; `HtmlDomRenderTargetBackend::GetData`'s own `data==nullptr` returns-false (not throws) contract is now explicitly locked in, the one deliberate exception among its otherwise-all-throwing argument checks. **Regression-proved** (the two fixes safe to exercise under native GTest -- the `SetRenderTargets` null-check was deliberately NOT reverted for confirmation, since without it `renderTargets[0]` is genuine undefined behavior, a null-pointer method call that could crash or corrupt the test process rather than cleanly fail, unlike the other two): disabling the size-validation `ThrowIfNegativeOrZero` calls and the `ReadBackbuffer` argument checks and rebuilding reproduced exactly the predicted failures -- the four size-validation tests threw nothing at all, and all four `ReadBackbuffer` argument-validation checks instead threw the old generic `std::runtime_error` ("...could not be read back") instead of the specific `ArgumentOutOfRangeException`/`ArgumentNullException` types; restoring returned all tests to green. Full regression pass: 68/68 smoke, 35/35 pixel, 10/10 stress, 17/17 dispose, 2/2 host-integration, 6/6 memory, 53/53 GTest. |

---

## What the browser run caught

The first headless-Chromium run reported 3 of 17 checks failing against a backend that was in fact
producing exactly the right DOM. The cause was in the test's own JS: **a backslash inside an `EM_JS`
body does not survive the preprocessor's stringification of that body**, so `/(\d+)/g` silently
became `/(d+)/g` and `/\s+/g` became `/s+/g` — regular expressions that then quietly matched the
wrong thing instead of failing loudly. (The giveaway: the one transform check whose needle contained
no letter `s` passed, while `translate` and `scale` both failed.) Every regular expression in the
test was replaced with `split`/`indexOf` parsing.

Worth knowing project-wide: no `EM_JS` body anywhere in CNA should contain a backslash escape.
`CANVAS`'s own `EM_JS` bodies happen not to, so nothing else is affected today.

Extending the smoke test to cover `SpriteFont::DrawString` (HTMLDOM-38) and `TextureAddressMode::Wrap`
(HTMLDOM-45) caught a second, unrelated test bug the same way: 19 of 22 checks failed with symptoms
(`visible=3` instead of `2`, a glyph landing at index 0 with the WRONG width) that looked exactly
like a backend defect. The cause was in the test's own frame dispatch: the pre-existing `if (frame_
<= 2) {...} else {...}` block that drives frames 3-4's single-sprite recycling check used an
unconditional `else`, so it kept running on frame 5 too — queuing an extra, unplanned sprite *before*
the new glyph/Wrap draws, shifting every sprite index by one. Fixed by bounding that block to `else
if (frame_ <= 4)` so frame 5 owns its own drawing (and its own sprite indices) outright. Two real
bugs found by two different real-browser runs, zero backend defects either time — exactly the
point of running this in an actual browser instead of trusting a structural review.

## Known limitations (honest list)

1. **No backbuffer readback.** No browser API rasterizes a live DOM subtree. Bound render targets
   read back for real; the backbuffer throws.
2. **Texture upload costs a PNG encode.** Fine at load time, bad per frame (design decision 6).
3. **`TextureAddressMode::Mirror` mixed with a DIFFERENT mode on the other axis throws** when the
   source rect exceeds the texture (HTMLDOM-97) — symmetric Mirror and non-Mirror mixed axes are
   both supported now; only Mirror-mixed-with-something-else remains unsupported.
4. **Custom `BlendState`s throw** — only the four standard presets exist in CSS compositing.
5. **No custom `Effect`s** — there is no shader stage.
6. **No MSAA, no depth, no stencil** — the same 2D-only boundary as `SDL_RENDERER`/`CANVAS`.
7. **Sprite count drives DOM size.** Tens of thousands of simultaneously visible sprites will cost
   more in compositor memory than a canvas would; this backend targets normal 2D games, not
   particle storms.
8. **Scissor rect correctness is per-`SpriteBatch` batch, not per-`Draw()` call** (HTMLDOM-94) —
   matching real XNA/FNA `SpriteBatch.Deferred` semantics (`ScissorRectangle` is read once when a
   batch's draw calls are actually issued at `End()`), not a backend-specific shortfall. Sprites in
   DIFFERENT scissor-rect regions paint in the order those regions were first created, not
   necessarily interleaved per-frame draw order across regions — an accepted trade-off for actually
   keeping different scissor rects from clipping each other's sprites, which the backend could not
   do at all before HTMLDOM-94.

## Post-audit integration stabilization — 2026-08-08

The historical lane was replayed onto `integration/post-audit-phase1` without copying stale shared
interfaces wholesale. The current integration architecture remains authoritative. The public
backend is still the distinct Emscripten-only `HTML_DOM` identity: sprites are pooled real `<div>`
elements styled with CSS, while `RenderTarget2D` uses a private Canvas2D surface. Browser glue is
handwritten `EM_JS`; the backend is not an alias for `CANVAS`, `EASYGL`, `SOFTWARE`, or `STUB`.

| Finding | Status | Post-audit result |
|---|---|---|
| HTMLDOM-121 | ✅ resolved | Closed silent-success gaps at the current `IGraphicsBackend` boundary. Unsupported 3D/effect/query factories, render-target options, depth/stencil state, wireframe/depth-bias rasterizer state, colour-write masks, multisample masks, invalid sampler ordinals, and foreign render targets now reject deterministically. The implemented 2D subset remains accepted. |
| HTMLDOM-122 | ✅ resolved | Texture uploads now require one RGBA8 level, exact storage, positive dimensions, and pitch at least `width*4`; padded rows are copied row-by-row. Width, total destination size, and pitched source-span arithmetic are checked before allocation/copy, including the 32-bit WebAssembly address-space case. |
| HTMLDOM-123 | ✅ resolved | The recursive native `CnaTests` glob no longer includes the two standalone Win32 Glide ABI-loader sources. They remain in their dedicated target; native OPENGLES/EasyGL testing no longer collides with duplicate test mains or Win32-only fixtures. |
| HTMLDOM-124 | ✅ resolved | Fixed `AlphaBlend` draw-colour handling for CSS/Canvas2D's straight-alpha pipeline. XNA supplies a premultiplied draw colour, while HTML DOM applies RGB through a cached straight-pixel variant and A separately through `opacity`; using the raw RGB therefore darkened alpha fades twice. `BuildDrawCommandEXT` now un-premultiplies RGB only for `AlphaBlend` (round-to-nearest, clamped), canonicalizing zero-alpha RGB to white. A white alpha-only fade now keeps one `0xFFFFFF` RGB variant key across every alpha step instead of synchronously generating and PNG-encoding a full-texture tinted variant per frame; only compositor opacity changes. Host tests cover recovery, key stability, zero alpha and untouched non-`AlphaBlend` modes; the browser pixel suite adds a discriminating opaque-texture/half-alpha draw that was half-dark before the fix (35→36 checks). |

Runtime classification for this integration is deliberately split. Required Emscripten/browser
evidence is 69/69 smoke checks (including two screenshot checks), 36/36 pixel checks (including two
screenshot checks), 10/10 stress, 17/17 dispose, 2/2 host-integration, 6/6 memory, and the 54/54
browser GTest suite. GitHub CI installs the pinned emsdk and Playwright/Chromium toolchain needed to
produce that evidence on a clean runner. Native
selection of `HTML_DOM` was rechecked and fails deterministically with the Emscripten-only CMake
gate. Current CNA-owned host code passed 57/57 contract tests and the same 57/57 under linked
ASan+UBSan runtimes; the browser/JS lifetime claims therefore retain historical runtime evidence
plus current static review, not a newly claimed browser run.

Post-audit controls: the full native OPENGLES/EasyGL production and `CnaTests` build passed; the
focused EasyGL/Glide/SpriteBatch/cache selection passed 147 tests with one expected EasyGL
wireframe skip; `CnjCacheIsolationTest` and texture-cache reconstruction remained green. Seven GDI
executables passed under Wine/Xvfb, covering its 2D, ColorMatrix, public stencil/API/state,
unsupported-feature and exact-4x-MSAA contracts. Glide capability/unit tests and its 32-bit ABI
loader passed; the full i686 Glide backend remains externally blocked by the already-recorded
sharp-runtime `__int128` toolchain limitation. Diligent, Skia and Sokol changed capability switches
passed representative backend-specific syntax compilation. No supported HTML DOM path remains
open from this stabilization.
