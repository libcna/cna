# Lane card — `gltf` · **SECOND INTEGRATION LANE** · **DIRECT MERGE**

| Field | Value |
|---|---|
| Logical lane | `gltf` |
| Refs | `refs/heads/feature/gltf` and `refs/remotes/origin/feature/gltf` — **identical**, and **unmodified by the integration** |
| Head | `86ada7a7bdc7c8e76fff536be4f6c1f5bff3df43` |
| Archive tag | **`archive/preintegration/gltf-20260804`** → `86ada7a7` · annotated · GPG-signed · verifies good · local only · **unchanged** |
| Merge base with checkpoint | `32639a135b718916f93ac3f64fa83bc11099ffac` — **audit-stacked**, not develop-forked |
| Own commits / files | **1 / 1** · diff `+451, −0` — purely additive |
| Ahead / behind the integration head | 1 ahead / 231 behind |
| Subsystem | **documentation only** — a dated engineering analysis of glTF import defects |
| Shared interfaces | **none** — no `GraphicsDevice.cpp`, no `IGraphicsBackend.hpp`, no `GraphicsCapability.hpp` |
| Dependencies | **none**, internal or external |
| Conflict class | **LOW** — confirmed: the added path does not exist at the integration head |
| Development status | DEVELOPMENT COMPLETE |
| History class | **HISTORY CLEAN** |
| Integration readiness | **DIRECT MERGED — see §Integration record** |

## Dependency chain

None. The merge base `32639a13` is an ancestor of `integration/post-audit-phase1`; nothing gates this
lane and it gates nothing.

---

# Phase 1 — original commit audit

## Commit inventory — the complete lane

`git log 32639a13..feature/gltf` returns **exactly one commit**. Expected, and verified rather than
assumed.

| Field | Value |
|---|---|
| SHA | `86ada7a7bdc7c8e76fff536be4f6c1f5bff3df43` |
| Subject | `docs: analyze glTF rendering issues` |
| Body | **empty** |
| Trailers | **empty set** (`%(trailers)` → `[]`) |
| Author | `Robert Vokac <robertvokac@robertvokac.com>`, 2026-07-28 20:28:28 +0200 |
| Committer | `Robert Vokac <robertvokac@robertvokac.com>`, 2026-07-28 20:28:28 +0200 |
| GPG status | **`U`** — good signature, key `FB9CE8E20AADA55F`, uncertified key (this repository's normal pass state) |
| Files changed | `A  gltfissues.md` — **one file**, `+451 / −0` |
| Purpose | Record the root-cause analysis of a `ChronographWatch.glb` mis-render, attributing each defect to a specific layer |

## Content-class exclusion — verified, not assumed

The commit's single added path is `gltfissues.md` at the repository root. It therefore contains:

| Prohibited class | Present? |
|---|---|
| Production source | **no** |
| Public headers | **no** |
| Tests | **no** |
| CMake / build registration | **no** |
| Generated assets | **no** |
| Dependency updates | **no** |
| Submodule revision | **no** |
| Unrelated documentation | **no** — the file is entirely about glTF import |
| AI attribution | **no** — see below |

The repository root already carries 43 tracked `.md` documents at the integration head
(`AUDIT.md`, `plan_*.md`, `known_bugs.md`, `cnj.md`, `xnb.md`, …). A root-level analysis document is
the established convention here, so **no relocation is warranted**.

## Attribution scan

Both the commit metadata **and** the document body were swept for every banned token in
`INTEGRATION_HISTORY_POLICY.md` §2:

```
git log --format='%H%nSUBJ:%s%nBODY:%b%nAUTH:%an <%ae>%nCOMM:%cn <%ce>%nTRLR:%(trailers)' \
  32639a13..feature/gltf | grep -inE 'CC OK|Claude|Anthropic|Co-authored-by|generated by|authored by|\bbot\b|\bagent\b|\bAI\b'
→ ZERO HITS

git show feature/gltf:gltfissues.md \
  | grep -inE 'CC OK|Claude|Anthropic|Co-authored-by|generated by|authored by|\bbot\b|\bagent\b|\bAI\b'
→ ZERO HITS
```

The document body was scanned as well as the message because this lane's entire payload **is** prose.
A metadata-only sweep would have proven nothing about it.

---

# Phase 1b — is the document still correct against the checkpoint?

This is the substantive question for a documentation lane, and it was answered by measurement rather
than by reading.

## The document pins its own baseline

`gltfissues.md` states its own provenance in its body:

- line 3 — `Analysis date: 2026-07-28`
- lines 51–54 — *"The viewer was built against `../cnaaudit`, commit
  `32639a135b718916f93ac3f64fa83bc11099ffac`."*

Every source citation in the document is therefore, **by the document's own declaration**, relative
to `32639a13`. That is exactly this lane's merge base.

## Blob-identity measurement

The document cites nine in-repo source files. Comparing each one's blob between the document's
declared baseline `32639a13` and the integration head `61bd1a1b`:

| File | Baseline vs integration head |
|---|---|
| `include/CNA/Internal/GltfImport/GltfImportCore.hpp` | **IDENTICAL** |
| `src/CNA/Internal/GltfImport/GltfImportCore.cpp` | **IDENTICAL** |
| `tools/gltf_to_cnj/gltf_to_cnj.cpp` | **IDENTICAL** |
| `src/Microsoft/Xna/Framework/Content/ContentManager.cpp` | **IDENTICAL** |
| `include/Microsoft/Xna/Framework/Graphics/PbrEffect.hpp` | **IDENTICAL** |
| `src/Microsoft/Xna/Framework/Graphics/PbrEffect.cpp` | **IDENTICAL** |
| `src/Microsoft/Xna/Framework/Graphics/DirectionalLight.cpp` | **IDENTICAL** |
| `src/Microsoft/Xna/Framework/Graphics/SamplerStateCollection.cpp` | **IDENTICAL** |
| `src/CNA/Internal/Backends/EasyGL/EasyGLGraphicsBackend.cpp` | **CHANGED** — `4624867273fdd716` → `b7cdeaa01f77dcfa` (+1320 / −244, the remediation campaign) |

**Eight of nine are byte-identical.** Every claim resting on them — issues 1, 2, 4 and the whole of
§5 — is still literally true at the integration head, line numbers included. Each cited line was
re-read at `integration/post-audit-phase1` and matches what the document says is there.

## The one drifted citation

`EasyGLGraphicsBackend.cpp:4121` is the document's only stale line reference.

| | |
|---|---|
| At `32639a13` (the document's declared baseline) | line `4121` is `"    vec3 Lo=vec3(0.0);\n"` — the start of the PBR fragment shader's light accumulation. **The citation was accurate when written.** |
| At `61bd1a1b` | that same shader line is now at **`4948`**; line `4121` is unrelated `prog_lit_textured_` uniform setup |

**The substance is unchanged.** At the integration head the PBR shader still computes

```
Lo = PbrLight(light0) + PbrLight(light1) + PbrLight(light2)
ambient = uAmbientColor * albedo * occlusion
FragColor = vec4(ambient + Lo + emissive, alpha)
```

byte-for-byte as quoted, the only difference being that the two texture fetches are now wrapped in
the render-target-flip helper `cnaSampleUV(...)`. `PbrEffect.hpp`, `PbrEffect.cpp` and
`DirectionalLight.cpp` are all identical, so zero ambient plus three disabled directional lights
still composes to black. **The conclusion the citation supports survives intact.**

## Decision on the drift: retained as historical, not rewritten

The line number is **deliberately left as `4121`**. Rewriting it to `4948` would make a document that
declares itself a 2026-07-28 analysis of commit `32639a13` cite a line that did not exist on that
date, destroying the self-consistency that makes it verifiable at all. The drift is recorded here
instead, which is where a reader integrating the lane will look.

## No stale counts, no stale identifiers

| Checked for | Result |
|---|---|
| Fixed branch counts | **none** — the two `branch`/`backends` hits are `"these two branches"` (the `cnaaudit`/`cnagltf` checkouts) and `"CNA PbrEffect and its backends"` (no number) |
| Backend counts | **none** |
| Ticket / task identifiers | **none** — the document references no `REMED-*`, `GFX-*`, `CNB-*`, `Task N` or any other identifier, so none can dangle |
| Old API names | **none** — every cited symbol (`MeshGroup`, `CollectMeshGroups`, `MeshOut`, `ExtractClips`, `pbrUv2Mismatch`, `nodeToNewIndex`, `usePbr`, `EnableDefaultLighting`, `setDiffuseColorProperty`) still resolves at the integration head |
| Cited in-repo paths | **9 / 9 exist** at the integration head |

## Plan content is plan content — stated, not blurred

**The document is part analysis and part plan, and the plan part is not implemented.**

| Section | Nature | State at the integration head |
|---|---|---|
| §Executive Summary, §1–§4, §5, §Assessment | **Analysis of observed behaviour** | Still accurate — the code it describes is byte-identical apart from the one drifted line |
| **§Recommended Repair Order** (P0 items 1–6, P1 items 7–11, P2 items 12–13) | **Proposal** | **NOT IMPLEMENTED.** No node-transform preservation, no `baseColorFactor` propagation, no map-independent PBR selection, no `KHR_materials_transmission`, no sampler preservation, no rigid-node animation import, no sRGB/linear split |
| **§Missing Regression Tests** (12 proposed cases) | **Proposal** | **NOT IMPLEMENTED.** None of the twelve exists |
| §Authoritative glTF References | External specification links | Unchanged |

Integrating this lane adds a **description of open defects and a proposed repair order**. It adds no
capability, closes no ticket, and must not be read as evidence that glTF import was fixed.

## Relationship to checkpoint documentation — complementary, not contradictory

The integration head's `NEXT.md` carries a 2026-07-18 handoff recording that `plans/plan_cnj.md` Phase 14
closed a set of glTF import gaps. That set and this document's set are **disjoint or explicitly
complementary**, so no contradiction is imported:

| Phase 14 closed | `gltfissues.md` says |
|---|---|
| multi-UV-set **diagnostics** | agrees — `pbrUv2Mismatch` *"can emit a warning, but it does not correct the data"` (§5). A diagnostic, not a fix |
| `KHR_texture_transform` | agrees, and bounds it — only the base-colour transform is baked into the shared UVs; an independent transform on another map is not preserved (§5) |
| `KHR_lights_punctual` | not a contradiction — the document's point is that *the analysed model contains no lights*, not that CNA lacks support |
| morph targets, CUBICSPLINE, Draco, tangent generation, `KHR_materials_emissive_strength` | not discussed — outside this analysis |
| — | node transforms, `baseColorFactor`, PBR selection condition, `KHR_materials_transmission`, samplers, rigid-node animation, sRGB/linear: **none of these are Phase 14 items**, and all remain open |

---

# Phase 2 — direct merge versus adaptation

## Decision: **DIRECT MERGE**

Every `INTEGRATION_ORDER.md` direct-merge condition was evaluated against evidence:

| # | Condition | Verdict |
|---|---|---|
| 1 | The lane contains exactly the intended documentation commit | ✅ one commit, one file, `+451 / −0` |
| 2 | Its base is an ancestor of the integration branch | ✅ `git merge-base --is-ancestor 32639a13 integration/post-audit-phase1` → yes |
| 3 | Human-authored, or otherwise permitted by the history policy | ✅ authored **and** committed by `Robert Vokac <robertvokac@robertvokac.com>` — policy A1, preserve |
| 4 | GPG-signed | ✅ `U`, key `FB9CE8E20AADA55F`, verifies good |
| 5 | Message is technically clean | ✅ `docs: analyze glTF rendering issues` — conventional-commit form (policy F4), empty body, empty trailer set. No process narrative to strip |
| 6 | No prohibited attribution | ✅ zero hits in metadata **and** in the document body |
| 7 | Patch applies without conflict | ✅ `git merge-tree --write-tree` succeeded; `gltfissues.md` does not exist at the integration head, so there is nothing to conflict with |
| 8 | Introduces no stale or false present-tense claim | ✅ eight of nine cited files byte-identical, so the present-tense claims are **still true**; the single drifted line number is retained as historical with its reason recorded above |
| 9 | Merging introduces no unrelated commits | ✅ the lane is one commit; the merged tree differs from the integration head by exactly `gltfissues.md` |

**All nine hold, so the original commit is preserved exactly and not recreated.** This is policy P1/P2
at its cheapest: the history is already what the policy asks adaptation to produce.

## Why adaptation was rejected

Adaptation was considered against each of its stated triggers and rejected on all of them:

| Trigger | Applies? |
|---|---|
| Wording stale against the checkpoint | **No** — one line number drifted; the claim it supports is unchanged, and rewriting it would falsify the document's own dated provenance |
| Prohibited attribution in the original commit | **No** — zero hits |
| Direct merge would introduce unrelated history | **No** — one commit, and its base is already an ancestor |
| A conflict requires semantic adaptation | **No** — no conflict exists |
| Document location or structure changed | **No** — root-level `.md` is the repository's established convention, and 43 such files already sit there |

**`depthcrt` used adaptation; that is not a reason for this lane to.** `depthcrt` was
`AUTHOR/TRAILER CLEANUP REQUIRED` on 6 of 6 commits with 0 of 6 signed. This lane is `HISTORY CLEAN`
and signed. The two lanes are in different history classes and correctly get different treatment —
which is precisely what Batch 0 was designed to exercise (`INTEGRATION_ORDER.md` §3: *"The batch
deliberately spans all three history classes"*).

Note that `INTEGRATION_HISTORY_POLICY.md` §4 F5 — *"no merge commits into the integration branch
during adaptation"* — is scoped to **adaptation**, whose purpose is to keep `range-diff` meaningful
against replayed commits. This lane is not adapted, so nothing is replayed and there is no
`range-diff` to protect; the integration merge itself is the same signed `--no-ff` shape used for
`depthcrt`.

---

# Phase 3 — losslessness and provenance

## Provenance table

| Field | Value |
|---|---|
| Original ref | `refs/heads/feature/gltf` (= `origin/feature/gltf`) |
| Original head | `86ada7a7bdc7c8e76fff536be4f6c1f5bff3df43` |
| Archive tag | `archive/preintegration/gltf-20260804` → `86ada7a7`, signed, verifies good |
| Merge base | `32639a135b718916f93ac3f64fa83bc11099ffac` |
| Integration base | `61bd1a1b6c81e299251443e738699908af158e1f` (the `depthcrt` integration merge) |
| Path | **direct merge** — no `adapt/gltf` branch, no adaptation worktree, no recreated commit |
| Original commit after integration | `86ada7a7` — **unchanged object, reachable from the integration branch** |
| Integration merge commit | **`722a2f5adb07a0e75616c72ebc528ca628b19198`** — signed (`U`), non-fast-forward, parents `61bd1a1b` + `86ada7a7` |

## Losslessness proof — direct-merge form

For a direct merge, `range-diff` is not the instrument: no commit was replayed, so there is no
adapted range to compare. The stronger property is available instead — **object identity**.

| Check | Result |
|---|---|
| Original commit object reachable unchanged from the integration branch | verified — `git merge-base --is-ancestor 86ada7a7 integration/post-audit-phase1` |
| Commit SHA identical to the archive tag's target | verified — `86ada7a7` on both |
| Tree diff unchanged | verified — the merge's second-parent diff is `A gltfissues.md`, `+451 / −0`, byte-identical blob |
| Blob identity | `git rev-parse 86ada7a7:gltfissues.md` = `git rev-parse integration/post-audit-phase1:gltfissues.md` |
| No original line silently dropped | 451 of 451 lines present; the integrated file and the original blob are the same object |
| Merge commit adds no content of its own | verified — the merged tree equals the integration head's tree plus exactly `gltfissues.md` |

Because the commit object is the *same object*, not a copy, losslessness is not merely measured — it
is structural. Nothing can have been dropped.

**Omitted content: none.** **Intentional wording changes: none.** No line of `gltfissues.md` was
altered; the staleness observations live on this card, not in the file.

---

# Phase 4 — documentation validation

| Check | Result |
|---|---|
| Markdown table structure | **3 tables, all well-formed** — each is a consistent 3-column table with a correct `\|---\|---\|---\|` separator row (lines 134–138, 180–187, 353–360) |
| Fenced code blocks | **balanced** — 6 fence markers, 3 blocks, all closed |
| Encoding | UTF-8, no BOM, **no CRLF**, **zero** trailing-whitespace lines |
| Internal path references | **9 / 9 exist** at the integration head |
| Line references | **13 of 14 resolve exactly**; the 14th (`EasyGLGraphicsBackend.cpp:4121`) is the documented historical drift |
| Symbol references | **9 / 9 resolve** at the integration head |
| Ticket / task identifiers | none referenced — nothing to dangle |
| Stale fixed branch counts | none present |
| Implemented vs planned behaviour | **distinguished explicitly** — see §Plan content above |
| Backend terminology | current — the document names `EASYGL` via `CNA_GRAPHICS_BACKEND` and refers to `easy-gl` / `meta-gl` as the internal libraries they are, consistent with the `feature/gl` boundary (EasyGL is internal, not a public CNA backend) |
| `git diff --check` | **clean** |

**No build was run and no tests were run.** The lane changes one Markdown file at the repository root
and touches nothing the build system reads. A no-op build would have proven nothing and is recorded
as deliberately skipped, not overlooked.

---

# Integration record

**Performed 2026-08-04. Status: MERGED into `integration/post-audit-phase1`.**

## Identity

| Field | Value |
|---|---|
| Path taken | **DIRECT MERGE** — the original commit is preserved, not recreated |
| Adaptation branch | **none created** — no `adapt/gltf`, no adaptation worktree |
| Integration base | `61bd1a1b6c81e299251443e738699908af158e1f` |
| Merge commit | **`722a2f5adb07a0e75616c72ebc528ca628b19198`** |
| Merge shape | `--no-ff`, GPG-signed, two parents `61bd1a1b` + `86ada7a7` |
| Merge signature | **`U`**, key `FB9CE8E20AADA55F` |
| Merge author / committer | `Robert Vokac <robertvokac@robertvokac.com>` — both |
| Commits added to the integration branch | **2** — the preserved original `86ada7a7` and the merge `722a2f5a` |

## Original-to-integrated commit mapping

| # | Original | Purpose | Integrated as | Disposition | Reason |
|---|---|---|---|---|---|
| 1 | `86ada7a7` | `docs: analyze glTF rendering issues` — adds `gltfissues.md` | **`86ada7a7`** (the same object) | **PRESERVED** | Every direct-merge condition held; recreating a clean, signed, human-authored commit would have destroyed provenance for no gain |

**No original commit is unaccounted for.** 1 PRESERVED, 0 TRANSFERRED, 0 SUPERSEDED, 0 OMITTED,
0 REIMPLEMENTED, 0 SPLIT, 0 COMBINED.

## Post-merge verification

| Check | Result |
|---|---|
| Merge shape | ✅ `--no-ff`, signed `U`, parents `61bd1a1b` + `86ada7a7` |
| Checkpoint tag still an ancestor | ✅ `git merge-base --is-ancestor cna-post-audit-remediation-phase1 HEAD` |
| `depthcrt` merge still present | ✅ `61bd1a1b` an ancestor, and all five adapted commits `88244b3a`, `3299c211`, `b9b63809`, `0998acc4`, `3cca0b19` reachable |
| Exactly one additional logical lane | ✅ **2** merges since the checkpoint, and no third |
| Original commit reachable **unchanged** | ✅ `86ada7a7` is an ancestor of the integration head — the same object, not a copy |
| Blob identity | ✅ `86ada7a7:gltfissues.md` = `HEAD:gltfissues.md` = `c16bc32d8602706825ec54552953b512b5ce4c35` |
| Merge introduced no content of its own | ✅ `git diff 61bd1a1b HEAD` = `A gltfissues.md`, `+451 / −0`, and nothing else |
| Original branch unchanged | ✅ `refs/heads/feature/gltf` still `86ada7a7` |
| Archive tag unchanged | ✅ `archive/preintegration/gltf-20260804` still `86ada7a7`, still verifies |
| Signatures over `d79214e7..HEAD` | ✅ **8 / 8 `U`** — 5 adapted `depthcrt` + `depthcrt` merge + preserved `86ada7a7` + this merge. No `N`, no `E` |
| Attribution sweep over the integrated range | ✅ **zero hits**, both merge commits included |
| No production / test / build change | ✅ nothing under `src/`, `include/`, `tests/`, `cmake/`, `tools/`, `examples/`, `third_party/`; no `CMakeLists.txt`; no `.gitmodules` |
| `audit/` untouched | ✅ across the entire integrated range |
| `git diff --check` | ✅ clean |
| Integration worktree | ✅ clean |
| Pushed | ✅ **this session pushed nothing** — the merge `722a2f5a` is local. Note that `origin` already carried `feature/audit`, `integration/post-audit-phase1` @ `61bd1a1b` and `adapt/depthcrt` before this session began (owner push, 2026-08-04 16:54:40); all 21 archive tags remain local only. See `INTEGRATION_BRANCH_INVENTORY.md` §10 |

## A measurement trap this lane creates for later sessions

`feature/gltf`'s head **is** an ancestor of the integration branch, because it was direct merged.
`feature/depthcrt`'s head is **not**, because it was adapted — its commits were replayed as new
objects `88244b3a`…`3cca0b19`.

A naive `git merge-base --is-ancestor <lane-head> integration/post-audit-phase1` sweep therefore
reports **1 integrated, 20 pending**, which is wrong. The campaign now mixes both paths, so lane
completion must be read from the lane cards and `INTEGRATION_ORDER.md`, not inferred from ancestry.

**The correct count after this lane: 21 logical lanes, 2 integrated, 19 pending.**

## Conditional blockers re-checked for this lane

- **`REMED-CONTENT-007` / `-008`** — re-checked per `INTEGRATION_ORDER.md` §6. This lane adds one
  Markdown file and changes no code at all, so it touches `ContentManager`, `ContentReader`, the XNB
  type readers and external resource resolution **not at all**. **Not blockers here.** They remain
  open HIGH / P1 and are carried forward unchanged. Note the distinction: `gltfissues.md` *discusses*
  the glTF import path, but discussing it introduces no path-resolution code.
- **`REMED-GFX-DECL-GUARD`, `REMED-GFX-209` / `WEBGPU-115`** — apply only to lanes adding a
  rasterizing backend. This lane adds none. Out of scope.

## Completion criteria

| # | Criterion | State |
|---|---|---|
| 1 | Current refs fetched before any claim | ✅ `git fetch --all --prune --tags`, exit 0 |
| 2 | Exact original head and archive tag verified | ✅ `86ada7a7` on branch, remote and tag; tag signature verifies good |
| 3 | Every original commit inspected | ✅ all **1** of them — SHA, author, committer, signature, subject, body, trailers, files, purpose |
| 4 | History cleanliness proven | ✅ `HISTORY CLEAN` — human-authored and committed, signed, empty trailer set, zero attribution hits in metadata **and** body |
| 5 | Direct merge used because it was safe | ✅ all nine conditions evaluated against evidence; adaptation rejected against all five of its triggers |
| 6 | No glTF plan content lost | ✅ structural — the integrated commit **is** the original object; blob hashes match |
| 7 | Stale statements corrected or explicitly retained as historical | ✅ one drifted line reference (`EasyGLGraphicsBackend.cpp:4121` → `:4948`), **retained as historical** with the reason recorded |
| 8 | No production / test / build file changed | ✅ verified by path-class sweep |
| 9 | Original branch not rewritten | ✅ `refs/heads/feature/gltf` unchanged |
| 10 | Merge commit signed | ✅ `722a2f5a`, `U`, key `FB9CE8E20AADA55F` |
| 11 | Checkpoint and `depthcrt` remain ancestors | ✅ both |
| 12 | Exactly the glTF lane added | ✅ one file, `+451 / −0` |
| 13 | `audit/` untouched | ✅ |
| 14 | `git diff --check` passes | ✅ |
| 15 | No push occurred | ✅ |

**Lane status: DONE.** `integration/post-audit-phase1` now contains its second integrated feature
lane and its first direct merge. **This session pushed nothing.**
