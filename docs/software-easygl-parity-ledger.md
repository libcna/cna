# Software <-> EasyGL XNA/Core parity ledger

This ledger is the durable evidence map for the parity campaign in
`plans/plan_software.md`. It was started on 2026-09-07 at CNA commit
`cde325ecd4d771d50b7d8ec3ef99f1657ad217ac` on branch `software`.

The behavioral authority order is measured Microsoft XNA 4.0/IL, FNA, EasyGL, current Software,
then prose. EasyGL is the CNA coverage reference, not permission to reproduce a demonstrated
EasyGL bug. A similarly named implementation method is not evidence. A test that succeeds by
recording an unsupported boundary is gap evidence, not parity evidence.

## Classification vocabulary

- `IN-SCOPE-XNA`: public XNA 4.0 behavior.
- `IN-SCOPE-CORE`: renderer-agnostic CNA plumbing required to make the XNA surface real.
- `DEFERRED-CNAEXT`: modern/CNAEXT behavior excluded from this campaign.
- `NOT-APPLICABLE-SOFTWARE`: physical GPU/window behavior with no meaningful CPU-renderer analog.
- `ALREADY-PARITY`: implementation and behavioral evidence already support the parity claim.
- `REAL-GAP`: in-scope behavior is absent, incomplete, silently degraded, or only boundary-tested.

The scope and verdict columns are intentionally separate: for example, a feature can be
`IN-SCOPE-XNA` and `REAL-GAP`, or `IN-SCOPE-XNA` and `ALREADY-PARITY`.

## Repository evidence census

- EasyGL owns 246 renderer example/test translation units. These include classic XNA behavior,
  renderer-native GL details, and modern CNAEXT shader/PBR examples; filenames are therefore an
  audit corpus, not an automatic scope list.
- Software owned 16 local example/test translation units and registered 60 `Software` CTests at the
  starting commit. The completed campaign registers 160 Software-labelled contracts, most from
  renderer-neutral graphics sources also compiled for EasyGL; all 160 pass display-free with
  two-test concurrency.
- Later remediation tests already cover depth state, viewport/scissor, culling, wireframe, depth
  bias, exact indexed addressing, render-target readback/ordering/lifetime, blend equations/write
  masks, sampler component isolation, mip filters, texture/cube transfers and descriptor capacity.
- The RenderTargetCube fixtures that originally accepted Software's unsupported boundary now run
  the same positive face, mip, depth/stencil, MSAA, transfer and sampling contracts on Software and
  EasyGL after `SOFTWARE-119`.

## Renderer contract and public XNA/Core ledger

| Feature family | EasyGL evidence | Software evidence | Scope | Verdict | Task | Verification / next proof |
|---|---|---|---|---|---|---|
| Renderer creation, no-display CPU operation, clear/readback | `easygl_goldenimage_smoke_test.cpp`, pixel fixtures | `software_smoke_test.cpp` verifies the descriptor requests neither video nor a window and the device owns no window under authenticated Xvfb; backbuffer readback/first-read/reject shared tests prove CPU operation | IN-SCOPE-CORE | ALREADY-PARITY for meaningful CPU behavior | SOFTWARE-100 / SOFTWARE-154 ✅ | Physical GL context and a successful display connection are not required of Software. Common GraphicsAdapter enumeration may independently pin platform video when one is available; this is not renderer ownership. |
| Physical window, swap interval, present interval, context loss | EasyGL real-window/vsync/context tests; public present-interval/fullscreen-field fixtures pass unchanged | Software intentionally owns no native window/context and `Present()` is immediate CPU completion; the same public field fixtures pass 5/5 and 7/7 | NOT-APPLICABLE-SOFTWARE for native operations; ALREADY-PARITY for renderer-independent state | SOFTWARE-127 ✅ | Never fabricate a GPU context or swap timing. Renderer-neutral context-lease ownership is covered separately by SOFTWARE-124. EasyGL exclusive-fullscreen execution is unavailable under SDL offscreen, while its public field test is green on Software. |
| Capability/factory consistency | EasyGL explicitly probes device-dependent MRT/query/Texture3D/instancing/aniso | `Software_CapabilityContract` proves twelve positive promises, the actual custom-effect false boundary and real query/Texture3D factories; shared expectations require MRT, CPU query, anisotropy, volume storage and CPU instancing true; no null factory or fixed fallback is advertised as support | IN-SCOPE-CORE | ALREADY-PARITY for truthful reporting; feature implementations remain separately evidenced | SOFTWARE-104 ✅ / SOFTWARE-117 ✅ / SOFTWARE-118 ✅ / SOFTWARE-120 ✅ / SOFTWARE-122 ✅ / SOFTWARE-129 ✅ / SOFTWARE-133 ✅ | 16/16 public checks plus the green full `CnaGraphicsTests`; future tasks opt in only with their own behavioral proof. |
| TriangleList indexed/non-indexed | broad EasyGL draw/user/buffer corpus; nine shared buffer/draw fixtures pass 122/122; signed-base and native-range regressions pass on desktop GL | the same fixtures plus `Software_IndexedAddressing`; ordinary/instanced signed-base and safe undefined-range transitions pass | IN-SCOPE-XNA | ALREADY-PARITY after removing shared false sign and managed-range restrictions | SOFTWARE-108 ✅ / SOFTWARE-109 ✅ / SOFTWARE-171 ✅ / SOFTWARE-322 ✅ | Retain exact valid start/count/signed-base/16/32-bit and mutation coverage. XNA/FNA forward buffered start/base/range hints to the native API; Software prevents undefined inputs from becoming host-memory reads without promising their pixels. |
| TriangleStrip indexed/non-indexed | shared `triangle_strip_winding_test.cpp` executes EasyGL paths | shared public fixture requires Software's complete six-path matrix: 299/299 checks, 0 boundaries, 36/36 matrix cases; shared unit topology/range suites add both widths and deferred lifetime | IN-SCOPE-XNA | ALREADY-PARITY | SOFTWARE-105 ✅ / SOFTWARE-132 ✅ | Odd/even winding, all cull modes, user/buffer, indexed/non-indexed, Uint16/Uint32 and nonzero offsets/base are exact. |
| LineList/LineStrip | EasyGL primitive tests and shared range suites | Software effect-aware routes pass indexed 16/32-bit exact-colour coverage plus non-indexed exact ranges/topology switches; line location uses an exact-RGBA 5×5 probe because valid line coverage conventions may choose an adjacent pixel | IN-SCOPE-XNA | ALREADY-PARITY at current evidence level | SOFTWARE-132 ✅ | Retain clipping/state coverage and add user-indexed line offsets if a future mismatch appears. |
| PointListEXT | renderer-neutral exact pixel/address/range/state corpus | Software runs the same existing CPU route through 14 positive tests covering persistent/user, indexed/non-indexed, exact nonzero vertex starts/counts, 16/32-bit, dynamic buffers, depth, scissor, viewport, blend, render targets and topology switches | DEFERRED-CNAEXT | Existing support retained and now accurately tested; no expansion required | SOFTWARE-133 ✅ / SOFTWARE-324 ✅ | Regression coverage only; PointListEXT is not counted toward XNA campaign completion. |
| Homogeneous frustum clipping | GL hardware clipping across normal test corpus | bounded six-plane clip in every CPU primitive route; `Software_Clipping` passes 11/11 public probes including near/far with depth disabled, multi-plane polygon, line/point rejection and clipped wireframe | IN-SCOPE-XNA | ALREADY-PARITY at clip-volume contract level | SOFTWARE-106 ✅ | Retain in shared differential scenes and under top-left/MSAA changes. |
| Fill convention/shared edges and constant varyings | EasyGL runs the shared additive split-quad fixture through hardware fill/interpolation rules | Software uses the winding-normalized D3D top-left predicate for single and 4x coverage; affine-difference barycentric interpolation keeps constant attributes bit-exact and no solid-fill diagonal exceptions remain | IN-SCOPE-XNA | ALREADY-PARITY | SOFTWARE-107 ✅ / SOFTWARE-136 ✅ | Shared `TopLeftFill`: 6/6 on each renderer, including exact flat-varying output, order/winding and MSAA resolve. |
| XNA/D3D9 integer pixel centers | EasyGL's measured `63/128` single-sample correction and `REMED-GFX-235` MSAA exclusion; shared XNA one-pixel fixture passes 2/2 | Software applies the same correction only to single-sample destinations and passes 2/2 | IN-SCOPE-XNA | ALREADY-PARITY | SOFTWARE-131 ✅ / SOFTWARE-134 ✅ | Four strict MSAA/RT supervisor images prove that multisample coverage is not displaced; retain both guards together. |
| Perspective interpolation, clip/depth range, viewport and scissor | EasyGL stock shaders apply the XNA/D3D-to-OpenGL clip-Z conversion; viewport/scissor/texture tests include SOFTWARE-310's corrected zero-area behavior and SOFTWARE-336's absolute near-depth/near-clip proof | perspective correction in raster core; Software custom viewport/scissor tests plus full six-plane homogeneous clipping | IN-SCOPE-XNA | ALREADY-PARITY after the confirmed EasyGL stock-depth repair | SOFTWARE-103/310/336 ✅ | The SOFTWARE-336 discriminator passed on Software and failed both clauses on pre-fix EasyGL: `z=0` mapped to 0.5 and `z=-0.5` survived. Both now agree with XNA's `0<=z<=w`; shared raster, texture, viewport, scissor and clipping probes retain the wider cases. |
| CullMode, wireframe, depth bias | Desktop EasyGL now uses native polygon mode and passes the shared winding, fill transition and normalized-bias wireframe oracles. GLES/WebGL line expansion still misses culling, depth bias, clip-generated edges and several draw paths | Software culling/wireframe/front-face tests, recalibrated 17-check depth-bias scene, and both shared normalized-bias oracles pass | IN-SCOPE-XNA | PARITY on desktop after repair; REAL-GAP on EasyGL ES/WebGL | SOFTWARE-176/177 ✅; SOFTWARE-178 ⬜ | The adversarial bias tests use ±0.02. Million-scale constants no longer mask native-unit conversion bugs, and a zero-bias wireframe control prevents a vacuous pass. |
| MSAA coverage/depth/stencil/resolve and rasterizer toggle | EasyGL MSAA tests plus shared per-sample and four supervisor contracts; desktop GL passes the 29/29 adversarial triangle/line/wireframe contract and GLES passes its 20 representable checks | Software owns four independent color/depth/stencil samples and evaluates coverage/depth at the standard D3D 4x positions. `SOFTWARE-160` repaired dropped triangle-state propagation; `SOFTWARE-315` repaired whole-pixel line/wireframe coverage; `SOFTWARE-316` repaired their shared pixel-center depth; `SOFTWARE-319` replaced a nonconforming regular grid | IN-SCOPE-XNA | ALREADY-PARITY for the supported 4x mode after four confirmed repairs | SOFTWARE-110 ✅ / SOFTWARE-134 ✅ / SOFTWARE-160 ✅ / SOFTWARE-315 ✅ / SOFTWARE-316 ✅ / SOFTWARE-319 ✅ | The boundary oracle distinguishes partial triangle/line/wireframe coverage, proves that covered line/wireframe samples can independently straddle a depth threshold, and distinguishes the canonical standard pattern from the former quarter grid. False variants have no partial coverage; true/false/true restores output on Software and desktop EasyGL. Software's depth/first-read/mip/invalid-level supervisors pass 31/31, 34/34, 63/63 and 16/16 legs. FNA3D and EasyGL cannot represent the rasterizer toggle on GL ES. |
| VertexDeclaration-driven input | EasyGL binds every defined XNA storage format through its native conversion table after semantic lookup; SOFTWARE-320 makes stock and compiled-effect paths consume FNA3D's binding-order collision remap, and SOFTWARE-321 keeps inactive stock inputs disabled | shared `Software_VertexDeclaration` / `EasyGL_VertexDeclarationFormats`: 20/20 public pixel cases on Software and both EasyGL profiles covering all 12 formats, reordered equal-stride layouts, offsets, both user-index widths, split streams, partial/full cross-stream usage collisions and two short-but-inactive stream cases | IN-SCOPE-XNA | ALREADY-PARITY for declared stock-effect input and the shared binding rule | SOFTWARE-108 ✅ / SOFTWARE-129 ✅ / SOFTWARE-130 ✅ / SOFTWARE-133 ✅ / SOFTWARE-320 ✅ / SOFTWARE-321 ✅ / SOFTWARE-322 ✅ | A duplicate semantic takes the first free usage index and unique neighbours remain active. Inactive streams form no native/CPU fetch; active short streams are now natively forwarded without a managed exception, with Software defaulting missing records safely. Arbitrary-effect consumption still depends on SOFTWARE-162..165. |
| Multiple vertex streams/offsets | EasyGL stock and compiled-effect attribute lookup consumes the renderer-neutral effective usage indices; stock binding disables inactive inputs and compiled multi-stream/instancing controls pass 2/2 | declaration-driven Software resolves semantics across streams and bounds-checks each actual CPU record; binding-offset, mutation, inactive/active-short and 16/32-bit indexed probes pass | IN-SCOPE-XNA | ALREADY-PARITY | SOFTWARE-108 ✅ / SOFTWARE-109 ✅ / SOFTWARE-129 ✅ / SOFTWARE-320 ✅ / SOFTWARE-321 ✅ / SOFTWARE-322 ✅ | Retain separate per-vertex/per-instance offset, lifetime, collision, inactive-short and safely forwarded active-short probes. Pixels from out-of-range native inputs remain undefined. |
| Instancing | EasyGL stock instancing consumes a four-column matrix at locations 12..15 and advances every per-instance binding by its divisor | Software rasterizes every instance on the CPU through the same stock-effect pipeline, concatenates matrix columns across declared streams, applies each stream's offset/frequency independently and keeps base vertex geometry-only | IN-SCOPE-XNA | ALREADY-PARITY at the classic stock-effect contract level | SOFTWARE-129 ✅ / SOFTWARE-323 ✅ | SOFTWARE-323 removes a stale compile guard: all 17 applicable complete-range/lifecycle cases pass, with only the exact EasyGL and D3D implementation pins skipped. Wider shared coverage includes split geometry/matrix streams, 16/32-bit indices, offsets, arbitrary frequencies, dynamic mutation, queued lifetime, color/effect state and ordinary↔instanced transitions. |
| Buffer `SetDataOptions`, dynamic updates, readback, disposal and validation | Nine public buffer/draw fixtures, ownership tests and native-range forwarding probes; plural validation follows Microsoft's ordered-prefix failure state | the same sources compile unchanged for Software; exact addressing and safe valid/undefined/valid transitions add CPU-specific proof | IN-SCOPE-XNA | ALREADY-PARITY after recovered ownership, plural-binding and native-draw-range repairs | SOFTWARE-109 ✅ / SOFTWARE-124 / SOFTWARE-223 ✅ / SOFTWARE-224 ✅ / SOFTWARE-322 ✅ | Preserve foreign-resource and plural-binding discriminators. Required counts remain managed; native buffered offsets/capacity are forwarded, while user-array range checks remain separate. |
| `DrawUserPrimitives` / `DrawUserIndexedPrimitives` validation | EasyGL receives the same renderer-neutral validation before any GL staging or submission | Software formerly let typed and legacy raw overloads apply negative offsets directly to pointers and accepted zero indexed vertices; safe middle-pointer probes failed before repair. Shared validation now covers null vertex/index pointers, negative offsets, non-positive primitive/indexed-vertex counts and all representability arithmetic before memory access | IN-SCOPE-XNA / IN-SCOPE-CORE | ALREADY-PARITY for every condition observable from CNA's established pointer signatures | SOFTWARE-197 ✅ | Focused 48/48 on Software and desktop EasyGL. Unlike XNA's managed arrays, a bare C++ pointer carries no allocation length, so the managed upper-length check is an explicit `CHECKLIST.md` language-mapping boundary rather than invented metadata. |
| BlendState factors/functions/BlendFactor | Seven preset/separate-state programs plus the shared 35-check analytic matrix pass on Mesa EasyGL under authenticated Xvfb | The same seven public sources compile unchanged for Software; the shared target-readback matrix passes 35/35 | IN-SCOPE-XNA | ALREADY-PARITY with complete classic equation evidence | SOFTWARE-152 ✅ | Every source factor, every GL/FNA-valid destination factor, all five independent RGB/alpha functions and the live constant are exercised with distinct source/destination/factor colours and a two-byte normalized-store tolerance. |
| ColorWriteChannels/MultiSampleMask | EasyGL color-write tests, shared four-mask sample contract and stock-effect MRT contract. The expanded desktop-GL contract proves both BlendState and the independent GraphicsDevice mask | Software color-write test, exact bit-per-sample implementation and four retained MRT masks. `SOFTWARE-166` repaired the formerly cache-only device property; SOFTWARE-357 executes four distinct compiled outputs through those masks | IN-SCOPE-XNA | ALREADY-PARITY for classic output paths after confirmed common-property and compiled-routing repairs | SOFTWARE-110 ✅ / SOFTWARE-120 ✅ / SOFTWARE-166 ✅ / SOFTWARE-357 ✅ | Each BlendState sample-mask bit resolves to one quarter on both renderers. The 18/18 shared contract also proves device mask 0/1/all, whole-state synchronization and independent override/restoration. A synthetic compiled ps_2_0 now makes all higher per-target write masks observable on four 4x MSAA attachments. |
| DepthStencilState depth compare/write | EasyGL compare/write golden tests | Software depth-contract/depthstate suite covers all compare functions | IN-SCOPE-XNA | ALREADY-PARITY | -- | Regression under alpha, MSAA and clipping tasks. |
| Stencil masks/ops/reference/two-sided | Five renderer-neutral EasyGL stencil fixtures pass 13/13 when compiled unchanged for Software; the shared exhaustive matrix passes 46/46 on both EasyGL and Software after SOFTWARE-372 corrected its reversed winding labels | Software snapshots both face tuples plus shared masks/reference and applies fail/depth-fail/pass in every colored/shaded triangle, strip, line, point, wireframe and SpriteBatch fragment path; positive-area CCW triangles select the CCW tuple while non-triangles use ordinary state; EasyGL makes the same topology transition explicitly; alpha discard precedes it and 4x storage/operations are per sample | IN-SCOPE-XNA | PARITY after confirmed shared absolute-face/topology repair | SOFTWARE-121/372 ✅ / SOFTWARE-110 ✅ | Preserve the 59 state checks plus the shared independent-stencil-sample probe. |
| Sampler point/linear, min/mag/mip, U/V/W wrap/clamp/mirror, slot independence, max-mip and LOD bias | EasyGL filter/address/mip/sampler effect tests; the adversarial explicit-mip contract passes all 97 desktop-GL checks and the 91 states representable on GLES. Its enabled compiled-effect suite proves volume sampling and applies W through `GL_TEXTURE_WRAP_R` | REMED-GFX-150/175/182 contracts; after XNA pixel-center and NPOT reconciliation the complete shared point/linear address matrix passes 162/162. `SOFTWARE-158` first failed 8/97 explicit-mip checks, then proved max-mip lower bounds, positive/negative bias, clamp+bias, transitions and SpriteBatch forwarding. SOFTWARE-356 executes the opt-in compiled Effect's 2D/cube/volume samplers and proves live slot replacement/null, U wrap/mirror, linear filtering, max mip, bias, cube direction and W wrap/clamp/mirror; SOFTWARE-357 proves rendered target and SpriteBatch slot-zero routing | IN-SCOPE-XNA | ALREADY-PARITY for stock 2D/cube sampling and the implemented compiled sampler/target/SpriteBatch slices; public compiled-Effect conformance remains incomplete. LOD bias is unavailable on GL ES exactly as in FNA3D | SOFTWARE-131 / SOFTWARE-144 / SOFTWARE-158 ✅ / SOFTWARE-159 ✅ / SOFTWARE-308 ✅ / SOFTWARE-356/357 ✅ | The sampler callback consumes the same per-draw Software sampler slots as stock rendering, so properties affect actual texel selection. `AddressW` is no longer a no-op in the opt-in executor, but the public capability remains false until the encompassing compiled-effect work completes. |
| Anisotropic sampling | EasyGL driver path plus shared directional contract: 15/15 | Software computes the Jacobian principal axes, chooses LOD from the minor rate and averages up to 16 major-axis taps; maximum is independent per slot and forwarded through SpriteBatch | IN-SCOPE-XNA | ALREADY-PARITY | SOFTWARE-117 ✅ | Shared `AnisotropicFilterContract` passes 15/15 on both renderers: exact 1x/4x/16x mip levels, U/V orientation, clamp bounds, SpriteBatch transitions, DualTexture slots and Clamp/Wrap. |
| Texture2D mips/partial transfer/NPOT/XNB | EasyGL's unchanged 3x5 sampled-row scene; shared 162-check point/linear-address, 70-check filter-ordinal and 87-check mip contracts; both public texture collections reject foreign-device resources; XNB preserves all 20 classic formats | Software passes the same sampled-row scene and shared sampler corpus; exact public 3x5 full/non-zero-offset/partial-row plus 1x2/1x1 mip transfers pass; XNB retains all 17 uncompressed formats and DXT blocks | IN-SCOPE-XNA | ALREADY-PARITY for the complete classic plain-2D content/storage path | SOFTWARE-126 / SOFTWARE-140 ✅ / SOFTWARE-144 ✅ / SOFTWARE-172 ✅ / SOFTWARE-223 ✅ / SOFTWARE-275 ✅ / SOFTWARE-276 ✅ / SOFTWARE-308 ✅ | Five sampled bands are exact on both renderers. Shared contracts cover 3x3 Clamp/Wrap/Mirror/mixed-axis addressing plus 3x3/3x2/5x3/6x6 NPOT point, linear, minification, per-resource dimensions and mip selection. Non-positive dimensions fail before native allocation; foreign pixel/vertex textures throw transactionally. The XNB matrix uses distinguishing bytes for every format rather than format-only assertions. Generic transfers accept application-defined value types, allow any element width that divides a texel, and require Microsoft's exact region byte total. |
| TextureCube faces/mips/partial transfer/sampling/XNB | EasyGL cube face/mip/content tests plus shared DXT, normalized-integer and HDR public checks; exact byte readback covers uncompressed texels and retained DXT blocks | Software cube mip/face storage and cube sampler contracts; every XNA-permitted declared layout retains exact face/mip bytes beside its format-correct float sampler representation | IN-SCOPE-XNA | ALREADY-PARITY for every classic XNA-permitted plain-cube format, generic transfer and XNB representation | SOFTWARE-126 / SOFTWARE-145 ✅ / SOFTWARE-149 ✅ / SOFTWARE-150 ✅ / SOFTWARE-275 ✅ / SOFTWARE-277 ✅ | Shared typed full/start/rectangle/mip transfer and `EnvironmentMapEffect` sampling checks cover all seven normalized-integer and seven float/half layouts; values above one prove no RGBA8 intermediate. A custom POD, scalar-float and Color cross-width tests prove arbitrary raw element semantics and exact total sizes. The XNB matrix covers all 15 legal uncompressed formats on every face, rejects `NormalizedByte2/4`, and retains exact DXT1/3/5 blocks through byte elements only. |
| Texture3D storage/transfer/sampling | EasyGL allocates the exact packed/normalized/float/half GPU representation for all 15 XNA HiDef volume formats and retains exact per-mip bytes for lossless full/window/box readback. Its runtime XNB reader now retains that serialized format and every exact level byte. With compiled effects enabled, the classic `SharedCubeAndVolumeSamplerContract` samples a Color Texture3D through XNA Effect bytecode | Software owns every mip in slice-major declared-format CPU memory, passes the same Color contracts plus all-format and arbitrary-POD transfers, and preserves the same authored XNB representation. SOFTWARE-356 adds canonical shader-visible float4 planes for all levels/formats and point/trilinear compiled volume sampling with mip and U/V/W state; SOFTWARE-357 proves target transitions in adjacent compiled 2D/cube sampling | IN-SCOPE-XNA | ALREADY-PARITY for storage, generic transfer and XNB loading after confirmed repairs; the opt-in compiled triangle executor now has real sampling, while public compiled-Effect conformance remains incomplete | SOFTWARE-118 ✅ / SOFTWARE-159 ✅ / SOFTWARE-172 ✅ / SOFTWARE-173 ✅ / SOFTWARE-274 ✅ / SOFTWARE-278 ✅ / SOFTWARE-356/357 ✅ | Shared tests cover nonzero source/destination windows, partial 3D boxes, final mips, whole reconstruction, an application-defined POD and XNA's total-byte element-count rule on both renderers. The SOFTWARE-356 exact probe distinguishes both depth slices, all W address modes and trilinear filtering. No stock effect consumes Texture3D, so the public shared Effect contract remains gated until the compiled capability is honest. |
| Texture2D packed-16 formats | EasyGL packed-16 transfer path and exact four-primary sampled-draw oracle | Software retains exact `Bgr565`/`Bgra5551`/`Bgra4444` bytes and independently decodes every supplied level into RGBA8 CPU sampling storage | IN-SCOPE-XNA | ALREADY-PARITY | SOFTWARE-126 / SOFTWARE-140 ✅ | Shared public full/partial/mip transfers are exact for all three formats; the unchanged sampled-draw oracle passes 3/3 with zero mismatches on both Software and Mesa EasyGL. |
| DXT Texture2D formats | EasyGL two-block sampled-draw oracle plus the shared full/partial/mip/NPOT transfer contract; the campaign repaired EasyGL's missing compressed readback and context-restoration shadow | Software retains exact DXT1/3/5 blocks per mip, decodes independent RGBA8 sampling planes, preserves compressed DDS/XNB mip chains and passes the unchanged sampled-draw scene | IN-SCOPE-XNA | ALREADY-PARITY | SOFTWARE-126 / SOFTWARE-141 ✅ | Transfer contract passes for all three formats including NPOT tails; sampled draw is exact 3/3 on both renderers and Software compressed-content loading passes 3/3. |
| Remaining classic uncompressed Texture2D formats | EasyGL now uses real SNORM/packed/float/half storage, widens ordinary one/two-channel textures for stock-shader expansion, and gates 16-bit normalized storage on desktop GL or `EXT_texture_norm16`; the shared public contract passes | Software retains exact declared-format bytes plus an independent float RGBA sampling plane for all 13 formats, preserving negative SNORM and HDR values until the consuming shader applies its own range rules | IN-SCOPE-XNA | ALREADY-PARITY wherever the active EasyGL context can represent the format exactly; incapable GL profiles reject rather than narrow | SOFTWARE-126 / SOFTWARE-142 ✅ / SOFTWARE-155 ✅ | Full/partial/mip typed transfers and all 13 point-sampled channel layouts pass. `BasicEffect` proves `Single(2) * 0.25`; exact transfer proves negative SNORM retention, while the stock `COLOR0` path correctly saturates a negative diffuse value before sampling. `Alpha8` follows FNA `(0,0,0,A)`; one/two-channel colour formats follow measured XNA expansion. Cube and target formats are independently complete under SOFTWARE-143. |
| RenderTarget2D sampling/readback/mips/usage/lifecycle | EasyGL RT/readback/mip/usage suite plus shared float, normalized, device-ownership and binding-identity contracts; the campaign repaired inherited no-op target uploads, exact `Rg32` GLES readback and missing target channel swizzles | Software implements the complete classic renderable matrix: Color, three packed/normalized and seven float/half layouts, with format-domain mips, typed full/partial/mip transfer, target sampling, ordered-pass/readback/lifetime and sample-correct 4x tests | IN-SCOPE-XNA | ALREADY-PARITY for every classic XNA-renderable target layout | SOFTWARE-110 ✅ / SOFTWARE-143 ✅ / SOFTWARE-146 ✅ / SOFTWARE-151 ✅ / SOFTWARE-221 ✅ / SOFTWARE-222 ✅ | Shared target cases distinguish normalized quantization, missing-channel expansion, unclamped float clear/raster/additive blend, 2D sampling, depth, applied 4x resolve, mips and exact typed transfer on both renderers. Foreign-device targets reject transactionally, identical binding sets are true no-ops, and real target/face changes reset viewport/scissor. Non-renderable layouts reject before allocation. |
| Plain TextureCube formats | EasyGL retains exact Color, DXT1/3/5, seven normalized-integer and seven float/half layouts, uploads each through exact native storage or format-correct channel widening, and rejects layouts the active context cannot represent | Software retains exact declared bytes independently from decoded float sampling planes for the same full matrix; its cube classifier rejects only measured-XNA-invalid `NormalizedByte2/4` | IN-SCOPE-XNA | ALREADY-PARITY for the complete XNA-permitted matrix | SOFTWARE-143 ✅ / SOFTWARE-145 ✅ / SOFTWARE-148 ✅ / SOFTWARE-149 ✅ / SOFTWARE-150 ✅ | Exact full/start/partial/mip transfers, face isolation and distinguishing EnvironmentMap samples pass across every promoted layout. `Single`/`Vector*` use explicit IEEE binary32 serialization; half layouts use their packed words. Full suites pass 2,324/2,378 Software and 2,336/2,394 EasyGL. |
| RenderTargetCube | EasyGL cube corpus plus shared float/normalized contracts cover Color and all ten non-Color renderable layouts; exact declared-format cube transfers replaced its former RGBA8-only path | Software owns six isolated declared-format face surfaces, one shared depth/stencil attachment, face-local 4x resolve, exact typed partial transfer and format-quantized CPU box mips; `EnvironmentMapEffect` samples both HDR and normalized rendered faces without RGBA8 substitution | IN-SCOPE-XNA | ALREADY-PARITY for every classic XNA-renderable target layout | SOFTWARE-119 ✅ / SOFTWARE-143 ✅ / SOFTWARE-146 ✅ / SOFTWARE-151 ✅ | Shared tests prove every face/mip transfer, normalized clear/mip/sample and HDR sampling. The new six-face 4x test exposed EasyGL overwriting `lastFace_` without resolving the previous subresource; every face transition now finalizes first and the resolved texture is detached before mip generation. Mesa GLES and Software both preserve the requested four samples; ordinary Color EasyGL cube MSAA remains 33/33. Full suites pass 2,324/2,378 Software and 2,336/2,394 EasyGL. |
| Multiple render targets | `easygl_mrt_test.cpp`, the shared 22-check stock-effect/cube contract and `GraphicsProfileResourceCeilingTest.Mrt*` | Both renderers bind four ordered 2D/cube-face color planes, clear/finalize each independently, give depth/stencil ownership to slot zero and pass the corrected shared contract | IN-SCOPE-XNA | ALREADY-PARITY for classic 2D/cube-face MRT | SOFTWARE-120 ✅ / SOFTWARE-135 ✅ / SOFTWARE-220 ✅ | Common public validation covers count, dimension, applied-sample, resource-identity and pixel-size rules. Mixed 2D/cube sets use distinct resources; face-local 4x resolve, cube/2D mips, cube depth ownership and bound-cube destruction with peer finalization are differential-proven. Recovered Microsoft XNA overturned the former subresource-based duplicate rule: two faces of the same cube may not coexist in one set, while two different formats remain compatible when their pixel sizes match. |
| BasicEffect unlit combinations | EasyGL combinations/golden/texture/vertex-color tests plus the shared 8/8 unlit stock-output contract | The unchanged EasyGL five-way combination and lit clamp scenes pass on Software; the shared contract proves disabled/bound/null/real texture cases, vertex-colour gating, emissive/diffuse/alpha multiplication and constant/varying D3D9 COLOR saturation | IN-SCOPE-XNA | PARITY after shared XNA repairs | SOFTWARE-103 ✅ / SOFTWARE-113 ✅ / SOFTWARE-153 ✅ / SOFTWARE-303 ✅ | Software and EasyGL formerly multiplied an unsaturated material value in the fragment stage; both now clamp the FNA Common.fxh output per vertex. A separately measured Microsoft XNA probe establishes opaque black, including alpha one, when texturing is enabled with a null Texture. Preserve the three-byte analytic material tolerance and six-byte midpoint tolerance only for raster position/normalization rounding. |
| BasicEffect lighting | EasyGL default/one/multilight/ambient/emissive/specular/per-pixel/world-scale tests; renderer-neutral contract passes 15/15 | Software implements three FNA directional lights, ambient/diffuse/emissive/specular, default lighting, per-vertex saturation/interpolation, per-pixel evaluation and inverse-transpose normals; shared contract passes 15/15 | IN-SCOPE-XNA | ALREADY-PARITY at current evidence level | SOFTWARE-113 ✅ | Preserve the two distinct lighting paths and tight two-byte oracle tolerance. |
| AlphaTestEffect | EasyGL modes/compare sweep/fog/vertex-color/diffuse/golden tests; FNA `AlphaTestEffect.fx`; shared alpha and fog contracts pass 30/30 and 33/33; common-output saturation probe passes exactly | Software evaluates the encoded comparison after all classic alpha sources and before depth/stencil/colour/fog; its material/vertex result is clamped per vertex like FNA's D3D9 COLOR output | IN-SCOPE-XNA | PARITY after a shared XNA repair | SOFTWARE-111 ✅ / SOFTWARE-112 ✅ / SOFTWARE-153 ✅ / SOFTWARE-303 ✅ | Preserve pre-interpolation material saturation, compare/source/discard and final fog ordering. The null-texture case requires exact opaque black and proves alpha one still participates in the comparison. |
| DualTextureEffect | EasyGL doubling/independent-UV/alpha/fog/golden tests plus the corrected Microsoft-XNA null-sampler oracle; shared slot contract passes 122/122 and fog contract 33/33; common-output saturation probe passes exactly | Software carries declaration-driven UV0/UV1 independently through clipping/perspective interpolation, derives a separate mip footprint per channel and applies each slot's sampler; either null sampler contributes opaque black, while the clamped vertex material, doubling/modulation, alpha and final fog ordering are retained | IN-SCOPE-XNA | PARITY after a shared XNA repair | SOFTWARE-112 ✅ / SOFTWARE-116 ✅ / SOFTWARE-153 ✅ / SOFTWARE-302 ✅ | Preserve pre-interpolation material saturation, byte-exact coordinate/sampler coverage, and the slot × indexed/non-indexed opaque-black null matrix. All classic stock samplers share the measured black-null rule; only CNAEXT PBR uses an absent-map white identity. |
| EnvironmentMapEffect | EasyGL amount/fresnel/eye/world/multilight/specular/fog corpus passes 23/23; shared environment contract passes 16/16; shared fog contract passes 33/33 | Software performs FNA vertex lighting/reflection/Fresnel with inverse-transpose normals and pre-interpolation COLOR saturation; the same shared contracts pass 16/16 and 33/33, plus the public API/clamp filter at 47/47 | IN-SCOPE-XNA | PARITY after a shared XNA repair | SOFTWARE-112 ✅ / SOFTWARE-114 ✅ / SOFTWARE-303 ✅ | Preserve per-vertex stage placement, alpha-scaled environment terms and final fog ordering. Independent stale-binding controls require both a null base Texture and a null EnvironmentMap cube to sample opaque black. |
| SkinnedEffect | EasyGL bones/weights/lighting/specular/fog/world-normal corpus; renderer-neutral material and full contracts pass 9/9 and 19/19 | Software performs 1/2/4-bone position/direct-normal skinning, inverse-transpose World normals, three-light ambient/diffuse/emissive/specular lighting, vertex/pixel dispatch, default lighting, texture/alpha and post-skin fog; the same shared contracts pass 9/9 and 19/19 | IN-SCOPE-XNA | PARITY after a shared XNA repair at the low-level evidence level; model orchestration remains separate | SOFTWARE-112 ✅ / SOFTWARE-115 ✅ / SOFTWARE-125 / SOFTWARE-303 ✅ | Preserve FNA's direct weighted-bone normal transform (not inverse-transpose), both lighting stages, the two-byte oracle tolerance and the measured opaque-black null texture; prove `Model.Draw()` separately. |
| SpriteEffect | FNA's internal stock shader and EasyGL's equivalent embedded/default sprite program implement MatrixTransform followed by texture × vertex color | Software transforms the same textured/tinted CPU quads through its fixed shaded-triangle path | IN-SCOPE-XNA | ALREADY-PARITY for the internal XNA SpriteBatch effect contract | SOFTWARE-123 ✅ | The shared transform, tint, texture, viewport and exact sub-pixel corpus proves observable output. CNA's historical public SpriteEffect exposure is not part of the XNA public surface and arbitrary custom shaders remain separately deferred. |
| Classic compiled XNA `Effect` bytecode | With `CNA_EASYGL_COMPILED_EFFECTS=ON`, EasyGL translates Direct3D 9 Effect Framework bytes through MojoShader and advertises `CompiledEffects=true`. Its enabled suite covers reflection, parameters, techniques/passes, state, exact draw pixels, ordinary/indexed/multi-stream/instanced input, SpriteBatch custom effects, targets, cube/volume samplers, loops, scope-valid relative input/vertex-constant and dependent temporary texture coordinates in all sampler dimensions, nested/conditional subroutines, derivatives, SM3 destination/`TEXKILL` predication and packed semantics, ps_1_4 projective coordinates including swizzle ordering and mip selection, destination-selected sampling, SM3 pixel/vertex sampler-result swizzles, signed/zero `LOG`/`LOGP`, fixed-XYZ `NRM`, Shader Model 1.1 fixed inputs/`EXPP`, vertex-only `SGN` scratch semantics, vertex-only scalar `EXPP`/`LOGP` validation, vertex-only `LIT`/`SLT`/`SGE` and scalar `EXP` validation, complete pixel Shader Model 1.x, vertex Shader Model 1.1 and exact Shader Model 2.0 opcode/dynamic-flow/source-modifier validation, fixed pixel-profile slot-budget validation and legacy destination-mask/co-issue opcode and pair-structure validation plus read-only input, restricted-destination, write-only output-source, address-register, sampler-source, typed-control and special-control, relative-source and miscellaneous-input enforcement, valid phase transitions, duplicate-phase rejection, register depth and temporary initialization including `TEXKILL`, legacy stateful texture-matrix arithmetic/sampling/reflection/depth, component-remap, dependent RGB/dot sampling and bump-environment operations, `VPOS`/`VFACE` and context recreation; all 276 tests pass on isolated Mesa/Xvfb. SOFTWARE-365 repaired non-unit `LOOP`, SOFTWARE-367 repaired subroutine output and SOFTWARE-369/371/373/374/375/376/377/378/379/380/381/382/383/384/385/386/398/399/400/401/402/403/404/405/406/407/408/409/410/411/412/413/414/415/416/417/418/419/420/421/422/423/424/425/426/427/428/429/430/431/432 restored XNA pixel-centre/facing/predication/semantic/legacy behavior and parser validation rather than preserving EasyGL as an infallible oracle | `SOFTWARE-162/163` provide the opt-in parser/runtime, D3D9 token IR and vertex execution. SOFTWARE-355 executes texture-free pixels through triangles and the output merger; SOFTWARE-356 executes TEX/TEXLD/TEXLDD/TEXLDL against live 2D/cube/volume slots, with SOFTWARE-360 correcting TEXLDD to D3D9 opcode 93; SOFTWARE-357 routes COLOR0-3 through MRT and runs target/cube sources plus multi-pass SpriteBatch custom effects with original SpriteEffect stage inheritance; SOFTWARE-358 extends the same interpreter/output merger to clipped `LineList`, `LineStrip` and wireframe triangle edges; SOFTWARE-361/362 close the applicable omitted modern and legacy straight-line arithmetic; SOFTWARE-363 adds structured conditions and predicate comparisons; SOFTWARE-373 adds SM3 identity/replicate/negated destination predication in both stages, SOFTWARE-374 proves the special non-writing `TEXKILL` case, SOFTWARE-375 executes zero-safe ps_1_4 `_dz`/`_dw` through two-operand `TEXCRD`, SOFTWARE-376 executes ps_1_4 `TEXLD` with the destination-selected sampler stage, SOFTWARE-377 crosses a valid phase marker while retaining temporary RGB, SOFTWARE-386 rejects a duplicate marker in the shared parser, SOFTWARE-378 executes paired `TEXM3X3PAD`/`TEXM3X3`, SOFTWARE-379 executes `TEXREG2AR`/`TEXREG2GB` with component-correct implicit LOD, SOFTWARE-380 executes sampled `TEXM3X2PAD`/`TEXM3X2TEX` with transformed implicit LOD, SOFTWARE-381 executes `TEXM3X3TEX` with CTAB-selected volume/cube dimensions, SOFTWARE-382 executes both reflected `TEXM3X3SPEC` variants with reflected implicit LOD, SOFTWARE-383 executes ps_1_3 matrix and ps_1_4 register pixel-depth output, SOFTWARE-384 executes dependent RGB/dot texture operations with transformed implicit LOD, SOFTWARE-385 executes `TEXBEM`/`TEXBEML`/`BEM` using persistent pass-stage state, SOFTWARE-387 adds variable-token relative-source execution and SOFTWARE-429 corrects its invalid pixel-constant premise while enforcing legal pixel-input/vertex forms and inherited loop scope, SOFTWARE-388/389 derive implicit 2D/cube/volume LOD from fully executed temporary-coordinate expressions in aligned helper quads; SOFTWARE-390/391 apply projective division after swizzling and derive projected LOD, SOFTWARE-392/393 apply Shader Model 3 pixel/vertex sampler-result swizzles, SOFTWARE-394 restores D3D9 logarithm semantics, SOFTWARE-395 restores fixed-XYZ normalization, SOFTWARE-396/397 restore legacy `EXPP` plus the complete fixed vertex-input table, SOFTWARE-398 rejects first-instruction temporary self-reads, SOFTWARE-399 validates complete `TEXKILL` temporary masks, SOFTWARE-400 restores vertex-only `SGN` scratch rules, SOFTWARE-401 restores vertex-only scalar `EXPP`/`LOGP` validation, SOFTWARE-402 restores vertex-only `LIT`/`SLT`/`SGE` plus scalar `EXP` validation, SOFTWARE-403 enforces the complete pixel Shader Model 1.x opcode allowlist, SOFTWARE-404 enforces the complete vertex Shader Model 1.1 allowlist, SOFTWARE-405 enforces the exact pixel Shader Model 2.0 allowlist, SOFTWARE-406 enforces the exact vertex Shader Model 2.0 dynamic-flow/predication boundary, SOFTWARE-407 rejects the pixel-3.0-only `LOOP`, SOFTWARE-408 enforces matrix operand/aliasing rules, while SOFTWARE-409/410 gate Shader Model 3 absolute source modifiers and their float-constant consistency rule, and SOFTWARE-411 enforces sampler-register ranges and SOFTWARE-412 enforces profile-specific temporary-register ranges and SOFTWARE-413/414 enforce every pixel/vertex input and output-bank range and SOFTWARE-415 enforces the fixed constant/control banks, SOFTWARE-416 enforces fixed vertex-profile instruction-slot limits, SOFTWARE-417 enforces the exact `ps_2_0` arithmetic/texture budgets, SOFTWARE-418 enforces the pixel Shader Model 1.x category, phase and co-issue budgets, and SOFTWARE-419 enforces its destination-mask restrictions, and SOFTWARE-420 rejects ineligible co-issue opcodes, SOFTWARE-421 enforces pair structure, and SOFTWARE-422 rejects read-only input destinations, and SOFTWARE-423 enforces restricted destinations, SOFTWARE-424 rejects write-only output sources, and SOFTWARE-425 restores address-register access/conversion, SOFTWARE-426 rejects direct sampler sources, SOFTWARE-427 restricts typed-control sources, SOFTWARE-428 restricts predicate, label and loop-counter sources, SOFTWARE-429 enforces relative-source register/scope rules plus correct vertex-subroutine epilogue placement, SOFTWARE-430 restricts `vPos` declarations and `vFace` sources, SOFTWARE-431 preserves disjoint packed semantics while rejecting invalid declarations, and SOFTWARE-432 makes Software combine separately stored semantics by declaration mask; SOFTWARE-364 adds bounded nested loops, integer loop-address state and all structured break forms; SOFTWARE-366 adds validated call frames, forward labels and inherited loop-address state in both CPU stages; SOFTWARE-368 runs `DSX`/`DSY` across aligned triangle-fragment quads, SOFTWARE-370 extends real helper quads to lines/wireframe, and SOFTWARE-371 supplies D3D9 `VPOS`/`VFACE`. Software still reports `SupportsCompiledEffects=false` because remaining profile, lifecycle and content conformance are open | IN-SCOPE-XNA | REAL-GAP is materially narrower, but the former full-parity claim remains false until public conformance is complete | SOFTWARE-161..163 ✅ / SOFTWARE-355..432 (applicable rows) ✅ / SOFTWARE-164/165 ⬜ | No stock-effect substitution or CNAEXT `ShaderEffect` fallback is used. Unimplemented Shader Model 2.x/3 profile cases, unproven ps_1_4 placement rules and full lifecycle/content closure remain required before enabling the capability; CNAEXT `PointListEXT` is not part of this classic slice. |
| `GraphicsDevice.VertexTextures` / `VertexSamplerStates` | EasyGL consumes the four HiDef collections for compiled vertex samplers at native units 16..19, independently from its sixteen pixel units; dimension checks, all seven sampler properties and rendered-target row correction apply | Software consumes the same per-draw collections in its `vs_3_0 TEXLDL` interpreter and samples 2D/cube/volume resources through the CPU sampler core | IN-SCOPE-XNA | PARITY for the currently implemented classic compiled vertex-sampling surface | SOFTWARE-167/168/225 ✅; corroborates FX-109 | Collection tests prove zero Reach/four HiDef slots, seven legal float/half formats, validation order and lifecycle. Shared output tests additionally prove slot isolation, post-Apply override authority, point-only format rejection from the correct stage, MaxMipLevel/LOD-bias selection, AddressW and null/disposal unbind. |
| Arbitrary ShaderEffect and custom shader examples | numerous EasyGL shader/bloom/distort/shadow examples | Software intentionally accepts source without executing it and truthfully reports `ExecutesShaderEffectSourceEXT=false` | DEFERRED-CNAEXT | Excluded; existing fallback must not regress | -- | No parity implementation in this campaign. |
| PbrEffect/SkinnedPbrEffect/glTF modern shading | EasyGL PBR/glTF examples | Software retains limited existing fallback fields | DEFERRED-CNAEXT | Excluded | -- | Regression only; no new modern renderer work. |
| SpriteBatch geometry, sorting, transform and state | Sixteen EasyGL public scenes plus exact shared sub-pixel, large-batch, immediate-target, layer-depth and lifecycle contracts | Twenty renderer-independent scenes compile unchanged for Software; the focused public corpus includes exact UInt16-overflow, Draw-before-target-switch, depth-tested-layer and homogeneous-transform discriminators plus shared lifecycle units | IN-SCOPE-XNA | PARITY | SOFTWARE-123 ✅ / SOFTWARE-137..139 ✅ / SOFTWARE-251/252/337..340 ✅ | Covers every overload family, flips, rotation/origin, both scale overloads, signed/zero source rectangles, every declared sort mode plus Microsoft's invalid-mode flush error, complete `(x,y,layerDepth,1)` transform including Z/W, homogeneous clipping and viewport depth mapping, targets, viewports, scissor, state forwarding/leakage, deterministic disposal, unbounded public queues over fixed 2,048-sprite native chunks, and literal per-Draw Immediate submission. |
| SpriteFont layout | Six EasyGL glyph/spacing/newline/default-character/flip/rotation/scale scenes plus the exact DrawString sub-pixel oracle | The same six public scenes pass unchanged on Software, backed by shared overload/layout units; the runtime content test renders a DXT3 XNB atlas and its Color CNB transcode through the same public SpriteBatch path | IN-SCOPE-XNA | ALREADY-PARITY at current evidence level | SOFTWARE-123 ✅ / SOFTWARE-137 ✅ / SOFTWARE-138 ✅ / SOFTWARE-147 ✅ | Fractional placement and scale reach CPU quads without truncation. Atlas equivalence is judged by exact sampled output on both Software and Mesa EasyGL, not an invalid typed raw transfer across different storage formats. |
| OcclusionQuery | EasyGL cycle/visible/occluded scenes plus shared precise-count and recovered-XNA lifecycle tests pass under Mesa/Xvfb | Software compiles the same scenes unchanged; its 44/44 exact contract proves synchronous lifecycle, scissor/alpha/depth/stencil/color-mask ordering, exact 1×/4× sample masks and deferred SpriteBatch. SOFTWARE-199 adds Reach refusal, unavailable-result and Begin/End/reuse ordering; SOFTWARE-309 proves active destruction leaves a later query usable | IN-SCOPE-XNA | PARITY after shared lifecycle repair; Software provides a precise deterministic count | SOFTWARE-122/199/309 ✅ | Preserve counting of the shared post-test sample mask, not submitted primitives or color writes. The public state machine comes from recovered Microsoft XNA, not FNA's omitted validation. |
| GraphicsDevice/resource/platform lifecycle, events and validation | Nine renderer-neutral public programs pass 126/126; redirected `HandleRelease` stress passes 10/10 after the campaign fixed disposal during an active EasyGL frame lease; the shared video-ref fixture passes five consecutive Mesa/Xvfb runs | The same nine sources compile unchanged for Software and pass 126/126; the current complete Software label passes 160/160; the same ref fixture proves the windowless device owns zero video references | IN-SCOPE-XNA / IN-SCOPE-CORE | ALREADY-PARITY | SOFTWARE-124 ✅ / SOFTWARE-157 ✅ | Covers range validation, disposed use, double-dispose, bound resources, renderer-handle release, move ownership, resource events, device-first destruction and leak accounting. GraphicsDeviceManager releases an active renderer lease before device teardown and removes every callback token on disposal. Ambient `GraphicsAdapter` video pins now transfer to a replacement platform before the predecessor is released; deterministic platform units prove transfer order, idempotence and reset cleanup without confusing the cache pin with a device leak. |
| GraphicsDevice presentation/reset/public resize state | Six public EasyGL fixtures plus shared MSAA and render-target restoration contracts; Mesa EasyGL passes the shared 5/5 MSAA contract with its truthful applied count of zero | Six unchanged fixtures pass 43/43 on Software; shared contracts add 11/11 and the CPU-specific depth/stencil/resize contract passes 8/8 | IN-SCOPE-XNA / IN-SCOPE-CORE | ALREADY-PARITY for meaningful renderer-independent behavior | SOFTWARE-127 ✅ | Software resizes the CPU backbuffer, resets viewport/scissor only on target-size changes, clamps requested 8x to real 4x storage and applies `None`/`Depth24`/`Depth24Stencil8` planes at reset. Native window/swap/context effects remain separately N/A. |
| `Model.Draw()` rigid/skinned orchestration | Nine EasyGL public fixtures cover rigid/skinned drawing, hierarchy, per-mesh effects, JSON content import, texture materials and both index widths | the same nine sources compile unchanged for Software and pass 9/9; shared Model units pass 122/122 | IN-SCOPE-XNA | ALREADY-PARITY at current evidence level | SOFTWARE-125 ✅ | Preserve hand-built and `Content.Load<Model>()` paths, child-bone placement, 32-bit indices and sampled-animation deformation. Final Software and EasyGL graphics-suite coverage completed under authenticated TCP Xvfb. |

## Final adversarial parity challenge

The challenge started from clean commit `92ed418b3dfcd4a2e03ea8c0d092a0b4caca9e9f` on branch
`software`. The first display-free Software baseline passed 155/156 CTests; the sole non-pass was
the window-dependent `Software_PresentLifecycle` supervisor, whose expected-abort child could not
open X11 and returned the skip code. The prior authenticated-Xvfb baseline was 156/156. At the
start, the plan had 94 explicit task rows: 91 complete, `SOFTWARE-100` blocked by its documented
repository-wide criteria, and the historical optional `SOFTWARE-85/86` pending.

The `SOFTWARE-168` vertex-sampler closure was verified by the displayless Software runtime, two
shared EasyGL output contracts and the complete 60/60 EasyGL compiled-effect family. The broad
post-change graphics suites pass 2,625/2,688 on Software with 63 classified skips and
2,635/2,688 on desktop EasyGL with 53 renderer/platform skips under isolated Mesa/Xvfb.

### Prompted hypotheses

| Hypothesis | Finding | Behavioral proof | Verdict/task |
|---|---|---|---|
| `SamplerState.MaxMipLevel` | Common GraphicsDevice state carried the value, but Software inherited the no-op and SpriteBatch dropped it for every renderer | The new self-identifying mip contract failed eight of 97 Software checks before repair; lower-bound clamp, oversized clamp, combined bias and SpriteBatch transitions now pass | Confirmed and repaired, SOFTWARE-158 ✅ |
| `SamplerState.MipMapLevelOfDetailBias` | Software neither stored nor applied it; SpriteBatch dropped it. Desktop EasyGL already mapped it, while GL ES has no FNA3D-equivalent state | The same pre-fix failures distinguish positive and negative bias and post-clamp ordering. Software and desktop EasyGL now pass 97/97; GLES passes all 91 representable checks | Confirmed and repaired with SOFTWARE-158 ✅ |
| `SamplerState.AddressW` | Common code already propagated W and opt-in EasyGL consumed it for classic compiled Texture3D sampling. Software inherited the no-op until SOFTWARE-356 added a stored W component and compiled volume sampling | The focused Software runtime now renders distinguishable voxels under Wrap, Clamp and Mirror and also proves trilinear W filtering; EasyGL's enabled shared cube/volume contract remains the public reference. No fabricated stock-effect test was used because none declares a volume sampler | Confirmed and repaired in the opt-in executor, SOFTWARE-159/356 ✅; public compiled-effect closure remains SOFTWARE-164/165 ⬜ |
| `RasterizerState.MultiSampleAntiAlias` | GraphicsDevice had dropped the property and both renderers ignored it | The new boundary scene failed two of 12 Software checks before repair. With a 4x target, true has eight partially covered edge pixels; false has zero and replicates one pixel-center decision to all samples; true/false/true restores exactly | Confirmed and repaired, SOFTWARE-160 ✅ |
| Compiled Effects on classic line and wireframe primitives | Software explicitly rejected every compiled `LineList`, `LineStrip` and `FillMode::WireFrame` draw while EasyGL submitted them through its ordinary translated shader path | The focused Software test failed at the explicit guard before repair. It now byte-compares compiled and stock coverage for non-indexed/indexed lines and wireframe, then independently proves sampled varying interpolation, wireframe depth bias and true/false 4x MSAA line coverage | Confirmed and repaired, SOFTWARE-358 ✅ |
| Software assigned `TEXLDD` the D3D9 `SETP` opcode | The SOFTWARE-356 unit constructed opcode 94 and called it TEXLDD, exactly repeating the implementation error; no fixture used authentic gradient-sampling bytecode | CNA's pinned MojoShader instruction table identifies `TEXLDD=93`, `SETP=94`. Switching only the test to 93 failed with `unsupported opcode 93`; after repair its two gradients reach the sampler. The original predicate-shaped 94 probe first proved explicit rejection without sampling; SOFTWARE-363 now executes it and preserves the zero-sample proof, while SOFTWARE-373/374 separately close instruction-token destination and `TEXKILL` predication | Confirmed and repaired, SOFTWARE-360/363/373/374 ✅ |
| Fixture-driven opcode coverage omitted valid straight-line shader arithmetic | None of the committed stock/authentic Effects happened to contain `SINCOS`, pixel matrices or `DP2ADD`, but EasyGL's MojoShader table accepts all of them in the applicable D3D9 stages | The focused test first failed on vertex opcode 37. After that stage alone was repaired it reported independent pixel rejections for opcodes 20-24, 90 and 37. Exact matrix/DP2ADD results and near-exact SM2/SM3 sine/cosine probes now pass, as does the full 2,688-test Software graphics suite | Confirmed compiled-effect instruction gaps and repaired, SOFTWARE-361 ✅; control flow/predicates/derivatives subsequently closed, with legacy profile cases remaining under SOFTWARE-164/165 |
| EasyGL-supported Shader Model 1 arithmetic remained absent | MojoShader's active GLSL profile implements `EXPP`/`LOGP` as its full-precision EXP/LOG emitters and pixel `CND` as `src0 > 0.5`; Software rejected opcodes 78-80 | Staged red runs first prove vertex 78 rejection, then all three pixel rejections independently. Exact Shader Model 1 operands now produce `2^3=8`, `log2(8)=3`, and the component-wise expected result on equal/below/above-threshold inputs; the full Software suite remains green | Confirmed and repaired, SOFTWARE-362 ✅; `BEM` is not falsely claimed, while the independently demonstrated SM3 destination-predication gap is now SOFTWARE-373 ✅ |
| Structured compiled-shader conditions and predicate comparisons were absent | EasyGL's active MojoShader GLSL emitter supports `SETP`, `IF`, `IFC`, `ELSE` and `ENDIF`; Software rejected opcode 94 and had no program-counter conditional state. This is distinct from instruction-token predication | A Shader Model 3 vertex program failed on 94 before repair; with that stage alone enabled, the pixel program failed independently on local `DEFB`. Exact outputs now prove predicate masks/swizzles, all six comparison controls, nested inactive blocks, true/false/no-else branches and partial pixel writes; the full Software suite remains green | Confirmed and repaired, SOFTWARE-363 ✅; loops/subroutines/derivatives subsequently closed by SOFTWARE-364/366/368/370 and destination predication by SOFTWARE-373 |
| Compiled-shader loops and structured breaks were absent | [Microsoft's D3D9 `LOOP` contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/loop---vs) defines the bounded iteration/initial-`aL`/signed-step triple and immediate enclosing `aL`; EasyGL's active MojoShader GLSL emitter supports `LOOP`/`ENDLOOP`, `REP`/`ENDREP`, `BREAK`, `BREAKC` and `BREAKP`. Software had no program counter or loop stack. Its pixel stage also rejected local `DEFI` | The staged focused run first failed on vertex opcode 27. After only vertex repair it independently failed on pixel opcode 48. Exact outputs now prove three-iteration non-unit `aL` stepping, nested `aL` restoration, natural REP completion, every break form, conditional-stack unwinding, an inactive loop whose invalid operand is not evaluated and continued execution afterward. A direct hostile count of 256 throws an iteration-limit error instead of hanging; the full Software suite remains green | Confirmed and repaired, SOFTWARE-364 ✅; subroutines/derivatives subsequently closed by SOFTWARE-366/368/370 |
| Compiled-shader subroutines were absent in both Software stages | [Microsoft's D3D9 `CALL` contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/call---vs) requires forward targets, return-address behavior, inherited enclosing `aL` and at most four nested Shader Model 3 calls. EasyGL's MojoShader path emits `CALL`, `CALLNZ`, `LABEL` and `RET`; both Software stages rejected their first call opcode | The staged red proof failed first in the vertex machine, then independently in the pixel machine. Both now prevalidate labels and retain bounded return/depth frames. Exact results combine unconditional, Boolean/predicate-selected, nested and structured-IF calls plus two non-unit inherited-`aL` invocations. Six malformed families reject in each stage, and the same parsed ps_3_0 fixture used by EasyGL produces exact quarter RGB. The focused runtime, 160/160 Software CTests and full 2,625/2,688 graphics run pass | Confirmed and repaired, SOFTWARE-366 ✅; derivatives subsequently closed by SOFTWARE-368/370, while profile/lifecycle/content closure remains SOFTWARE-164/165 |
| Classic compiled `Effect` parser and vertex phases | The SOFTWARE-161 audit proved that the gap was a real standalone subsystem, so the capability could not honestly be enabled by reflection alone or by executing only one shader stage | The opt-in SOFTWARE-162 contract parses all six stock binaries, a compiler-owned conformance binary and an authentic XNA 4 binary and proves reflection/state/preshader/token behavior. SOFTWARE-163 executes every vertex program in those fixtures and adds exact instruction/register and draw-address probes for ordinary, indexed, multi-stream and instanced input; declared outputs pass through clipping and perspective setup. Each draw then deliberately identifies SOFTWARE-164 rather than silently rendering with the stock path | Parser/IR and vertex phases completed, SOFTWARE-162/163 ✅; pixel/conformance gap remains SOFTWARE-164/165 ⬜ |

### Newly discovered gaps

| Finding | Why the prior tests missed it | Behavioral proof | Verdict/task |
|---|---|---|---|
| EasyGL/MojoShader used the loop-address limit as the D3D9 `LOOP` iteration bound | Existing compiled fixtures contained no non-unit, negative or zero loop-address step, so the enabled EasyGL path appeared conformant. The exact pinned GLSL emitter used `aL < i.x + i.y`, coupling termination to the address instead of counting `i.x` iterations | [Microsoft's D3D9 contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/loop---vs) assigns count/initial/step to `i#.x/y/z`. A parsed ps_3_0 Effect with count 3 and step +2 rendered red 128 before repair instead of 191. The dependency patch emits an independent count variable and the shared behavioral test now proves three iterations for +2, -2 and zero steps. The targeted test and complete enabled EasyGL compiled family pass 41/41 on isolated Mesa/Xvfb | Confirmed EasyGL classic-XNA conformance gap and repaired, SOFTWARE-365 ✅; Software's independent CPU execution was already correct under SOFTWARE-364 |
| EasyGL/MojoShader emitted malformed GLSL for valid subroutines | No committed compiled fixture used a partial destination write immediately before `CALL`/`LABEL` or nested a subroutine call to a later label. MojoShader reused the stale destination writemask while formatting label-register operands and emitted no prototypes before its ordered function definitions | The direct parse output first contained `ps_l1.x()` and `void ps_l1.x()`. Raw names removed those syntax errors, after which Mesa independently rejected the legal nested call because `ps_l1` was not declared yet. The patch emits raw identifiers and helper-section prototypes. A parsed ps_3_0 Effect now renders exact quarter-intensity RGB through unconditional, Boolean-conditional and nested calls; all 42 enabled EasyGL compiled tests pass in isolated Xvfb | Confirmed EasyGL classic-XNA conformance gap and repaired, SOFTWARE-367 ✅; matching Software execution is SOFTWARE-366 |
| Software rejected D3D9 pixel derivatives | Feature and stock-binary inventories contained no `DSX`/`DSY`, and the scalar per-fragment interpreter had no adjacent invocation from which to derive them | Microsoft's D3D9 documentation requires the rate of change from the same register in adjacent lock-step pixels. A direct nonlinear-register probe proves horizontal/vertical lane pairs plus a later derivative of a value depending on an earlier derivative. A parsed ps_3_0 triangle fixture squares interpolated TEXCOORD and produces distinct 16/80 quad rates on a 4x4 target. SOFTWARE-370 adds line helper invocations by projecting all four aligned-lane centres onto the unclamped segment: horizontal and vertical fixtures produce exact 9/37 rates only in their along-line component, and a wireframe edge produces the same exact result | Confirmed and repaired on triangle, line and wireframe fragments, SOFTWARE-368/370 ✅ |
| EasyGL compiled Effects omitted the XNA/D3D9 pixel-centre correction used by its stock effects | Constant and linear shaders could not distinguish a half-pixel phase shift; the first squared-varying derivative fixture did | Measured XNA 4.0 repository evidence establishes integer 3D pixel centres and the existing CNA correction. The shared 4x4 fixture produces 16/80 with that placement but pre-fix EasyGL produced 32/96 because MojoShader emitted only Y/depth fixups. A managed opt-in uniform now applies the viewport-scaled correction; the exact test and all 43 compiled tests pass | Confirmed and repaired, SOFTWARE-369 ✅ |
| Compiled `ps_3_0` raster inputs disagreed with XNA on both renderers | No Effect fixture declared miscellaneous register 17. Software dropped `vPos`/`vFace` declarations and rejected their first read; EasyGL inherited MojoShader's explicit half-pixel TODO and exposed OpenGL's opposite front-face convention | [Microsoft's ps_3_0 register table](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-registers-ps-3-0) and [semantics reference](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx-graphics-hlsl-semantics) establish integer D3D9 coordinates and positive clockwise/front triangles. The 4x4 red proof threw on Software; EasyGL quantized `.5/1.5/2.5/3.5` to 32/96/159/223 and inverted blue for both windings. The shared fixture now checks all 32 rendered pixels exactly; Software's focused runtime and all 44 EasyGL compiled tests pass | Confirmed Software and EasyGL classic-XNA gaps and repaired, SOFTWARE-371 ✅; `VFACE` remains intentionally unspecified on standalone line/point primitives |
| Both renderers assigned `CounterClockwiseStencil*` to clockwise geometry; EasyGL then exposed that tuple to lines | The old two-sided test called its positive-area CCW quad “front” and its negative-area CW quad “back”; the later cull oracle documented those names were reversed but assumed the stencil test was unaffected. It therefore proved only that two anonymous windings differed. Correcting the triangle mapping alone left GL lines consuming `GL_FRONT`/CCW state | [Microsoft's D3D9 state definition](https://learn.microsoft.com/en-us/windows/win32/direct3d9/d3drenderstatetype) explicitly assigns ordinary stencil state to clockwise triangles and `CCW_STENCIL*` to counter-clockwise triangles; its [two-sided-stencil contract](https://learn.microsoft.com/en-us/windows/win32/direct3d9/two-sided-stencil) says lines ignore the second tuple. Correcting only the fixture failed its two-sided column on both renderers. Software now selects positive area; EasyGL installs CCW on `GL_FRONT`, switches both faces to ordinary state for lines/points and restores the split on a later triangle. The differential and `ReferenceStencil` tests pass, and a 46/46 shared matrix separately proves CCW function/pass/fail/depth-fail, ordinary CW, the line exception and the no-state-reassignment transition | Confirmed shared classic-XNA conformance gap and repaired, SOFTWARE-372 ✅ |
| Both renderers rejected valid Shader Model 3 instruction-token destination predication | Predicate comparison tests exercised `SETP` only as input to structured flow. No fixture set the instruction predication bit, so Software's explicit throw and MojoShader's explicit `predicated destinations unsupported` failure were never reached. MojoShader additionally treated pixel `p0` as scalar despite Microsoft's four-component register contract | [Microsoft documents](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/setp-comp---ps) component-wise arithmetic/texture destination masking by identity or replicated `p0`, with optional `!`. A parsed SM3 Effect first failed creation on both paths. Software now intersects the predicate and destination masks; the managed GLSL path snapshots a vector predicate and one evaluated result before typed conditional component writes. The one-draw fixture makes vertex and pixel stages independently contribute to exact `(223,159,159,32)`. Software's focused runtime and all 45 then-enabled EasyGL tests passed | Confirmed shared classic-XNA conformance gap and repaired, SOFTWARE-373 ✅; SOFTWARE-374 subsequently proves the non-destination `TEXKILL` case |
| EasyGL ignored the predicate on `TEXKILL` | The first predication repair necessarily routed destination-writing instructions through the shared assignment formatter. `TEXKILL` has a destination-encoded operand but writes nothing, so its independent emitter never consulted `ctx->predicate_arg`; Software's existing component mask was likewise untested | Microsoft's [`setp_comp` contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/setp-comp---ps) admits texture instructions and [`TEXKILL`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/texkill---ps) tests x/y/z for negative values. A two-state parsed Effect returns lime when false `p0.x` suppresses a negative-x kill and preserves magenta clear when true `p0.x` executes it. Pre-fix EasyGL failed the lime branch by always discarding. The emitter now snapshots identity/replicate/negated `p0` and gates each negative component; Software's focused runtime and all 46 EasyGL tests pass | Confirmed EasyGL classic-XNA conformance gap and repaired, SOFTWARE-374 ✅ |
| Both compiled paths rejected Shader Model 1.4 `_dz`/`_dw` projection | No fixture used the ps_1_4 two-operand `TEXCRD`; both public paths therefore reached MojoShader's explicit `SRCMOD_DZ unsupported` failure only after this hostile profile probe. Software also accepted only the pre-1.4 instruction shape and divided by zero to infinity. Removing the translator refusal exposed a second independent EasyGL bug: its GLSL named `ps_t0` without declaring the read-only ps_1_4 texture input | [Microsoft's ps_1_4 modifier contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-registers-modifiers-ps-1-4) specifies x/y division by z or w and x/y=1 when the divisor is zero. The shared parsed Effect carries a four-component TEXCOORD and proves `(.25,.5,.5,.25) -> (.5,1)` plus both zero fallbacks in exact rendered channels. The managed MojoShader patch emits the zero-safe projection and treats ps_1_4 texture registers as TEXCOORD attributes; Software decodes the two-operand form and applies the same rule. Its focused runtime and all 47 then-enabled EasyGL compiled tests pass | Confirmed shared classic-XNA conformance gap and repaired, SOFTWARE-375 ✅; ps_1_4 `TEXLD` was subsequently closed by SOFTWARE-376 |
| Both compiled paths rejected Shader Model 1.4 `TEXLD` | No prior fixture separated the destination-selected texture stage from the source coordinate register. The first real `texld r1, t0_dw` Effect failed both paths in the common pinned translator with the exact diagnostic `TEXLD == Shader Model 1.4 unimplemented`, while Software's otherwise unreachable instruction path applied projective modifiers after source selection | [Microsoft's ps_1_4 `texld` contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/texld---ps-1-4) assigns the texture stage from destination `r#`, accepts independent `t#`/`r#` coordinates and permits the ps_1_4 projective modifiers. The managed translator patch validates that register shape, declares the destination-numbered sampler through the Effect map and emits the matching 2D/cube/volume lookup. Software projects raw x/y before swizzle. One shared parsed Effect proves stage 1 independently consumes TEXCOORD0, `.2/.5 -> .4` selects green from an exact four-texel row, and zero-w produces x/y=1 and the clamped white final texel. The focused Software runtime and all 48 EasyGL compiled tests pass | Confirmed shared classic-XNA compiled-Effect gap and repaired, SOFTWARE-376 ✅; multi-phase ps_1_4 and other legacy stateful texture forms remain under SOFTWARE-164/165 |
| Software rejected the valid Shader Model 1.4 `PHASE` marker | The decoder retained the marker as an instruction, but the CPU executor had no control/no-op case and routed its one-token shape into the ordinary arithmetic guard. A real two-phase parsed Effect failed before drawing with `Software pixel shader: malformed instruction`. EasyGL's active MojoShader GLSL emitter already defines the same marker as a no-op | [Microsoft's ps_1_4 `phase` contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/phase---ps) says it separates phase 1 from phase 2, temporary RGB persists, and alpha must be initialized again before valid use. The CPU now executes the decoded marker as a no-op. A shared program copies TEXCOORD0 into `r1.xy` before the marker, copies that retained RGB into legacy output `r0` afterward and initializes z/w independently, producing exact `(64,128,64,255)`. The focused Software runtime and all 49 EasyGL compiled tests pass | Confirmed Software classic-XNA compiled-Effect gap and repaired, SOFTWARE-377 ✅; invalid marker count/placement still belongs to full SOFTWARE-164/165 validation |
| Legacy `TEXM3X3` failed in Software and required a nonexistent sampler in EasyGL | No test exercised the state retained by two `TEXM3X3PAD` instructions. Software stopped on opcode 73. The pinned MojoShader validator then exposed an independent shared-reference bug by rejecting sampler-free `TEXM3X3` with `needs a 3D or Cubemap sampler`, despite its own GLSL emitter doing no texture lookup | [Microsoft's `texm3x3` contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/texm3x3---ps) defines the three row dots and explicitly says textures at all three stages are ignored. Software now retains the paired dot results and emits `(dot0,dot1,dot2,1)`; the managed MojoShader patch omits dimension validation only for this no-lookup variant. A real ps_1_2 Effect transforms `(1,0,0)` by three fixed rows to exact `(0.25,0.5,0.25,1)`, rendered as `(64,128,64,255)`. The focused Software runtime and all 50 EasyGL compiled tests pass | Confirmed shared classic-XNA compiled-Effect gap and repaired, SOFTWARE-378 ✅; sampled matrix variants remain explicit SOFTWARE-164/165 work |
| Legacy `TEXREG2AR`/`TEXREG2GB` were rejected by both compiled paths | The decoder knew opcode lengths, but the CPU executor had no case and the active MojoShader GLSL profile explicitly returned `unimplemented`. No prior sampler fixture selected coordinates from non-xy source components or made those components control mip derivatives | Microsoft's [`TEXREG2AR`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/texreg2ar---ps) and [`TEXREG2GB`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/texreg2gb---ps) contracts select destination stage and map AR/GB to u/v. A shared ps_1_2 Effect first fails creation in both renderers, then after repair selects distinct red/green base quadrants. Its ignored source component varies 32 times across four pixels while selected coordinates remain constant; choosing raw xy for Software's implicit footprint would incorrectly select the blue smaller mip. The focused Software runtime, all 160 Software CTests and all 51 EasyGL compiled tests pass | Confirmed shared classic-XNA compiled-Effect gap and repaired, SOFTWARE-379 ✅ |
| Software rejected sampled legacy `TEXM3X2PAD`/`TEXM3X2TEX` | EasyGL's pinned translator already emitted the two row dots and stage-selected 2D lookup, but Software stopped on opcode 71. A point-only proof would still have hidden CPU implicit LOD derived from the raw source register | Microsoft's [`texm3x2tex`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/texm3x2tex---ps) contract retains the pad dot, computes the second dot and samples the final destination stage. Software now retains and validates the sequence and carries its two row registers into triangle footprint evaluation. A shared real ps_1_2 Effect first selects green at fixed `(.25,.75)`; varying only source z from 0 to 8 then forces transformed derivatives to a blue smaller mip while raw source xy remains constant and would select red. The focused Software runtime, all 160 Software CTests and all 52 EasyGL compiled tests pass | Confirmed Software classic-XNA compiled-Effect gap and repaired, SOFTWARE-380 ✅; remaining sampled 3x3/depth/specular operations stay SOFTWARE-164/165 work |
| `TEXM3X3TEX` was absent in Software, and EasyGL misclassified a legacy volume sampler as cube | The Software decoder retained opcode 74 but the executor rejected it. MojoShader's legacy three-dimensional default let EasyGL compile the same shader, yet its absent sampler map lost the CTAB `sampler3D` declaration and the draw rejected the bound `Texture3D` as dimensionally incompatible | [Microsoft's instruction contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/texm3x3tex---ps) explicitly permits a volume or cube texture at the final stage. Software now executes the three row dots and lookup, derives sampler type from CTAB and evaluates transformed cube/volume footprints. EasyGL detects a Shader Model 1 sampler disagreement after the first bounded parse and recompiles that shader with CTAB-derived Mojo sampler mappings. The shared fixture proves fixed green volume coordinates, a blue transformed-LOD volume case whose raw source remains LOD zero, and a yellow +Y cube lookup. Software passes 160/160 and EasyGL compiled Effects pass 53/53 | Confirmed Software and shared-reference classic-XNA gaps and repaired, SOFTWARE-381 ✅; SOFTWARE-382 closes reflection and SOFTWARE-383 closes legacy depth |
| Legacy matrix reflection failed in Software and generated invalid/wrong GLSL in EasyGL | Software rejected `TEXM3X3SPEC` at opcode 76 (and had no opcode 77 path). MojoShader emitted a trailing comma for the constant-eye form, causing a GLSL syntax error; its shared helper then used component-wise multiply/divide, algebraically unlike the required reflection equation | Microsoft's [`TEXM3X3SPEC`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/texm3x3spec---ps) and [`TEXM3X3VSPEC`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/texm3x3vspec---ps) contracts define `2*(dot(N,E)/dot(N,N))*N-E`, with E from a constant register or the three matrix-row w components. The CPU now executes both and evaluates the reflected cube/volume footprint. The pinned translator patch removes the comma and uses scalar dot products. A shared fixture maps constant/varying forms to exact -X/-Z faces and varies only row w across a 256 cube so correct implicit LOD selects the black smaller mip while an unreflected normal footprint remains at the yellow base face. Software passes 160/160 and EasyGL compiled Effects pass 54/54 | Confirmed Software and EasyGL classic-XNA compiled-Effect gaps and repaired, SOFTWARE-382 ✅; SOFTWARE-383 closes adjacent legacy depth, while bump/dependent forms remain SOFTWARE-164/165 |
| Legacy matrix/register pixel depth failed in both compiled paths | Real ps_1_3 `TEXM3X2PAD`/`TEXM3X2DEPTH` and ps_1_4 `TEXDEPTH r5` fixtures were accepted by the Effect container but both Software and EasyGL stopped at MojoShader's explicit `TEXM3X2DEPTH unimplemented in glsl profile`; Software also had no executor cases for opcodes 84/87 | Microsoft's [`texm3x2depth`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/texm3x2depth---ps) and [`texdepth`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/texdepth---ps) contracts define numerator/denominator order, zero divisor → one, raster-depth replacement and one shader depth for all MSAA samples. The managed translator patch emits `gl_FragDepth` and validates ps_1_3 sequencing plus ps_1_4 `r5`. Software executes both into its existing depth output, which runs before stencil/depth/color merging and replicates to four samples. The shared test clears depth .5, submits raster depth .75 and distinguishes the required sub-.5 ratios (lime) from zero-divisor depth one (red clear remains). Focused Software and EasyGL cases pass | Confirmed Software and EasyGL classic-XNA compiled-Effect gap and repaired, SOFTWARE-383 ✅ |
| Dependent RGB/dot texture instructions failed in both compiled paths | Real ps_1_2 `TEXREG2RGB`, `TEXDP3TEX` and `TEXDP3` programs reached their decoded instruction streams, but Software had no handlers for opcodes 82/83/85 and EasyGL's pinned MojoShader explicitly marked all three GLSL emitters unimplemented. The focused pre-fix EasyGL proof stopped at `TEXREG2RGB unimplemented in glsl profile` | Microsoft defines RGB as the dependent 2D/cube/volume coordinate, `TEXDP3TEX` as a 1D `(dot,0,0)` lookup and `TEXDP3` as a dot replicated to all four channels; ps_1_2/1_3 also permit `_bx2` on texture-address sources. Software now executes all three and reconstructs the transformed source/matrix row per triangle for implicit mip selection. The managed MojoShader patch emits dimension-correct GLSL and validates profile, register ordering, source modifier and 2D-only dot sampling. One shared fixture proves a green `_bx2` volume lookup, blue mips selected from RGB and dot derivatives, and exact half-gray RGBA replication. Focused Software and EasyGL cases pass | Confirmed Software and EasyGL classic-XNA compiled-Effect gap and repaired, SOFTWARE-384 ✅; remaining bump-environment instructions stay in SOFTWARE-164/165 |
| Legacy bump-environment Effects were rejected or misexecuted by both compiled paths | The public Effect preflight and pinned MojoShader parser rejected authentic indexed pass-state identifiers 112-115/117/118 before either renderer could draw. After permitting those values, Software still had no opcode 67/68/89 execution; EasyGL never uploaded the six stage values, had no GLSL `BEM`, discarded `_bx2` on `TEXBEM/L` and read luminance offset from the scale component | Microsoft's Shader Model 1 contracts define the four matrix terms, `TEXBEML` blue-channel luminance factor and ps_1_4 `BEM` arithmetic; the Direct3D Effect table supplies the state identifiers and stage index. Shared pass translation now preserves partial persistent stage assignments. Software reconstructs transformed sample coordinates for implicit LOD and executes all three opcodes. The managed MojoShader patch validates/emits them, preserves the signed modifier and stage-zero state carrier; EasyGL uploads all six values before draw. One real parsed-Effect discriminator independently probes M00/M01/M10/M11, scale/offset and exact BEM RGBA arithmetic. Focused Software and isolated-Mesa EasyGL tests pass | Confirmed shared classic-XNA compiled-Effect gap and repaired, SOFTWARE-385 ✅; aggregate capability remains false while SOFTWARE-164/165 stay open |
| Both compiled paths accepted a duplicate Shader Model 1.4 `PHASE` marker | The pinned MojoShader parser retained the marker only as a profile event and carried an explicit FIXME about the missing count validation. An otherwise-valid ps_1_4 fixture with two consecutive markers was accepted by Software and EasyGL before repair, independently failing Software's explicit rejection check and EasyGL's `EXPECT_ANY_THROW` | [Microsoft's `phase` contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/phase---ps) describes the one separator between the shader's two phases. The managed parser patch retains a count and rejects a second marker before emission. The same fixture now throws in both paths; the valid two-phase output and legacy bump tests remain green, Software's focused runtime passes, and the complete EasyGL compiled family passes 58/58 on isolated Mesa/Xvfb | Confirmed shared classic-XNA compiled-Effect validation gap and repaired, SOFTWARE-386 ✅; other unproven profile-placement rules remain SOFTWARE-164/165 work |
| Relative SM3 source-address rules and inherited loop scope were wrong in both compiled paths | SOFTWARE-387 used pixel `c0[aL]` as its positive oracle because the pinned parser and EasyGL accepted it. Microsoft's published `ps_3_0` register contract rejects pixel relative constants in every scope. Both renderers also rejected legal pixel `v0[aL]`, accepted relative sources outside a loop and lacked the rule that a subroutine called from a loop inherits `aL`; EasyGL had no GLSL array for addressable inputs. A valid vertex-constant subroutine further exposed EasyGL emitting its vertex epilogue after a subroutine `RET`, outside any GLSL function | A Wine builtin D3DX differential independently accepts pixel `v0[aL]`, vertex `v0[aL]` and vertex `c0[aL]` inside `LOOP` or a called subroutine and rejects those forms outside inherited loop scope, rejects pixel `v0[a0.x]`, and rejects pixel `c0[aL]` everywhere. The common parser now enforces that matrix, tracks loop inheritance through labels and marks relative source registers used. GLSL gets fixed addressable input arrays, while its vertex coordinate/depth epilogue is emitted only when closing `main`. The corrected shared texture draw uses legal `v0[aL]` and selects the blue minified mip; four invalid and four valid creation probes cover the scope/register matrix | Confirmed shared classic-XNA parser/translation gap and corrected historical SOFTWARE-387 premise; repaired, SOFTWARE-429 ✅ |
| Software derived SM3 temporary-coordinate 2D mip LOD from an unrelated input register | A valid ps_3_0 fixture computes `r0 = v0 * 8` then samples with `texld r1, r0, s0`. The actual coordinate spans two repeats over four pixels and EasyGL selects a blue minified mip; Software used temporary register number zero as though it identified TEXCOORD0, reconstructed only the input's one-quarter repeat and selected the red base mip | Software now routes implicit TEX loads sourced from temporary registers through aligned 2x2 pixel-machine execution. It records the final runtime coordinate after arbitrary preceding arithmetic, swizzle, modifier and projection, differences it across helper lanes, and re-executes the 2D sample with those explicit gradients. A bounded iterative pass also supports sample-dependent temporary chains without embedding a shader-expression analyzer in the renderer. The shared parsed fixture now returns the same blue mip on Software and isolated EasyGL | Confirmed Software classic-XNA compiled-Effect gap and repaired, SOFTWARE-388 ✅; cube/volume closure followed in SOFTWARE-389 |
| Software ignored executed temporary-coordinate gradients for cube-map LOD | The companion ps_3_0 fixture multiplies only x/y of a +Z direction by eight. EasyGL selects a blue minified cube mip, while Software received the runtime gradients added by SOFTWARE-388 but discarded them in its cube branch and selected the red base mip from the raw input direction. A volume fixture already passed through the explicit-gradient branch and guarded the third sampler dimension | Software now projects the current direction plus its horizontal and vertical helper-lane neighbors onto the selected face and computes the common face-local texel footprint from those three points. The shared 8x8 cube and 8x8x8 volume contract distinguishes the raw sub-texel rate from the executed multi-texel rate; focused Software and isolated EasyGL cases select blue for both dimensions | Confirmed Software classic-XNA compiled-Effect gap and repaired, SOFTWARE-389 ✅; temporary-coordinate LOD is now covered for 2D/cube/volume samplers |
| Software applied ps_1_4 projective source modifiers before swizzling | SOFTWARE-375 proved identity-swizzle `_dz`/`_dw`, but a `.zxyw_dz` and `.wxyz_dw` fixture produced `(.5,.5,.25,1)` on Software while EasyGL/MojoShader produced `(1,.5,1,1)`. The old implementation divided raw x/y and only then selected components, so choosing raw z/w exposed the reversed order | MojoShader's GLSL emission explicitly builds the swizzled source expression and divides that vector by the original register's unswizzled z/w component, with a vector of ones for zero. Both Software source readers now retain the raw divisor, swizzle, and finally divide every selected component. The old identity and zero-divisor cases remain green alongside the new differential fixture | Confirmed Software classic-XNA compiled-Effect gap and repaired, SOFTWARE-390 ✅; profile combination coverage, not opcode presence, found it |
| Software selected ps_1_4 projected texture mips from the pre-division footprint | `texld r1, t0_dw` sampled the right individual coordinate, but Software's mip heuristic re-read raw TEXCOORD0 at the triangle vertices. A fixture keeps raw u/v fixed while w falls from 1 to .125: isolated EasyGL selects a blue minified mip from the expanding projected coordinate, while pre-fix Software selected the red base mip from its constant raw footprint | Projective implicit TEX instructions now request aligned 2x2 execution even when their source is a direct texture/input register. The same iterative machinery used for dependent temporaries records post-swizzle, post-division coordinates and supplies their lane differences to the sampler. Existing fixed-w and zero-divisor results remain exact | Confirmed Software classic-XNA compiled-Effect gap and repaired, SOFTWARE-391 ✅; closes a test-combination omission in the earlier SOFTWARE-376 proof |
| Software ignored Shader Model 3 sampler source swizzles | A valid parsed ps_3_0 fixture samples `(32,64,128,255)` through `s0.bgra`. Isolated EasyGL/MojoShader returns `(128,64,32,255)`, while pre-fix Software returned the original channel order. Direct CPU probes independently failed for implicit, explicit-level and explicit-gradient sampling | [Microsoft's ps_3_0 `texld` contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/texld---ps-2-0) permits swizzles on both texture operands. FNA's pinned MojoShader applies the sampler operand swizzle after lookup and before the destination write. Software now performs the same ordering for `TEX`, `TEXLDL` and `TEXLDD`; its focused runtime and all 160 CTests pass, full graphics remains 2,625/2,688 with 63 classified skips, and the complete isolated EasyGL compiled family passes 65/65 | Confirmed Software classic-XNA compiled-Effect gap and repaired, SOFTWARE-392 ✅; aggregate capability remains false while SOFTWARE-164/165 stay open |
| Software ignored the same sampler-result swizzle in vertex `TEXLDL` | SOFTWARE-392 covered all three pixel texture opcodes but the separately implemented vertex lookup still wrote `SampleEXT()` directly. A direct `vs_3_0` program preserved its volume `s3`, coordinate and explicit LOD but returned `(0.125,0.25,0.5,1)` for `s3.bgra` instead of `(0.5,0.25,0.125,1)`. A parsed Effect then encoded a full quad only in the post-swizzle vector; pre-fix Software collapsed every clip x to zero and drew the background | [Microsoft's vertex `TEXLDL` contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/texldl---vs) applies the sampler source swizzle to the fetched value before destination masking, which FNA's pinned MojoShader already emits. Software now performs that ordering in the vertex machine. The real parsed fixture renders red on isolated EasyGL and Software, while the direct probe independently pins register, dimension, LOD and exact vector ordering. Focused Software, 160/160 CTests, 2,625/2,688 graphics with 63 classified skips and all 66 isolated EasyGL compiled tests pass | Confirmed Software classic-XNA compiled-Effect gap and repaired, SOFTWARE-393 ✅; aggregate capability remains false while SOFTWARE-164/165 stay open |
| Software and EasyGL used host/GLSL logarithms instead of D3D9 `LOG`/`LOGP` semantics | Existing arithmetic coverage used only positive inputs, and an older direct Software round-trip supplied an invalid non-replicated LOG source. Both CPU stages evaluated `std::log2` component-wise, while MojoShader emitted raw `log2`; therefore negative inputs produced NaN and zero produced negative infinity | Microsoft's [`log` pixel contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/log---ps), [`log` vertex contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/log---vs) and [`logp` contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/logp---vs) require a replicated `log2(abs(src))` result and finite `-FLT_MAX` for zero. Direct Software programs now prove scalar replication, negative-input magnitude and exact finite zero behavior; a real ps_2_0 Effect makes `LOG(-2)` and `LOG(0) * 0` produce an exact yellow draw on both renderers. The CPU interpreters use the absolute selected scalar and finite sentinel; the managed MojoShader patch emits the same branch for both LOG and LOGP. The focused Software runtime, all 160 Software CTests, the full 2,625/2,688 Software graphics suite with 63 classified skips, and all 67 isolated EasyGL compiled tests pass | Confirmed shared classic-XNA compiled-Effect conformance gap and repaired, SOFTWARE-394 ✅; aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Software and EasyGL normalized the destination-selected component set instead of source XYZ | The prior arithmetic probe used a full destination and expected `sqrt(x²+y²+z²+w²)`, directly preserving the bug. Software summed only components named by the destination mask; MojoShader generated `normalize()` over a source expression narrowed by that same mask. Thus a full write incorrectly included W and `nrm r0.xy, c0` became a 2D operation | Microsoft's [`nrm` pixel contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/nrm---ps) and [`nrm` vertex contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/nrm---vs) explicitly compute one reciprocal length from XYZ, multiply all four source components by it, and use finite `FLT_MAX` when XYZ is zero. Direct Software probes pin full-write W exclusion, partial-write XYZ inclusion and zero-XYZ W scaling. A real ps_2_0 Effect combines an XY mask over `(3,4,12,26)` with a W-only zero-XYZ case: both paths emitted black before repair and exact white afterwards. The CPU stages now use one fixed-XYZ factor; the managed MojoShader expression preserves full source swizzle/modifiers and narrows the result only after normalization. Focused Software, 160/160 CTests, 2,625/2,688 Software graphics with 63 classified skips and all 68 isolated EasyGL compiled tests pass | Confirmed shared classic-XNA compiled-Effect conformance gap and repaired, SOFTWARE-395 ✅; aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Shader Model 1.1 vertex input/`EXPP` behavior was incomplete in Software and wrong in both compiled paths | A valid `vs_1_1` has no `dcl` instruction. EasyGL/MojoShader derives the fixed D3D9 semantic map for used v0..v16 registers, while Software populated inputs only from decoded declarations and therefore left parsed v0 at `(0,0,0,1)`. An independent direct arithmetic probe then showed both Software and MojoShader treated legacy `EXPP` exactly like the Shader Model 2+ replicated operation | MojoShader's own parser table supplies the declaration-free v-register map, now imported by Software. [Microsoft's `EXPP` contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/expp---vs) separately specifies `(2^floor(v), v-floor(v), 2^v, 1)` for vs_1_1 and replicated `2^v` only from vs_2_0 onward. A plain parsed v0/POSITION0 transform proves the binding repair independently. Exact CPU components and a real Effect whose quad survives only when all four `EXPP(3.25)` components satisfy distinct thresholds prove the arithmetic split; both paths failed before repair. Focused Software, 160/160 CTests, 2,625/2,688 Software graphics with 63 classified skips and all 70 isolated EasyGL compiled tests pass | Confirmed Software input-binding gap plus shared classic-XNA arithmetic gap and repaired, SOFTWARE-396 ✅; aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Software's first Shader Model 1.1 inverse input map was incomplete and offset | SOFTWARE-396's declaration-free fixture consumed only v0/POSITION0, so it did not catch that the new inverse mapped TEXCOORD0 to v5 and omitted COLOR0/1. MojoShader's canonical Shader Model 1 table instead assigns v5/v6 to COLOR0/1 and v7..v14 to TEXCOORD0..7 | A second parsed `vs_1_1` consumes both v5 and the boundary v14 and multiplies a full-quad position by their values. The pre-fix Software parser rejected COLOR0 because it had no register mapping; the corrected table draws white and an isolated EasyGL execution independently validates the fixture. The focused Software runtime and all 71 isolated EasyGL compiled tests pass | Confirmed Software classic-XNA compiled-Effect gap and repaired, SOFTWARE-397 ✅; this follow-up also demonstrates why a single POSITION0 test was insufficient |
| Software and EasyGL accepted a destination temporary read before its first completed write | MojoShader's parser marks a destination register written before it parses that instruction's sources. Consequently an initial `mov r0, r0` evaded its existing uninitialized-temporary check, and both CNA compiled paths accepted the invalid ps_2_0 program | Microsoft's pixel-shader register contract explicitly requires shader creation to fail when a temporary register is read before it is written. The managed parser now records whether the current destination existed before the instruction and rejects a same-register source when it did not. The synthetic Effect failed both renderers before repair; the direct Software check, all 160 Software CTests and all 72 isolated EasyGL compiled tests now pass | Confirmed shared classic-XNA validation gap and repaired, SOFTWARE-398 ✅; aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| `TEXKILL` accepted undefined temporary components and counted itself as a write | MojoShader represented all prior temporary writes as one Boolean and destination-token parsing marked every `TEXKILL` operand written before its semantic validation. A ps_2_0 fixture wrote only `r0.x` and killed on all of r0; both renderers accepted it despite undefined y/z/w | Microsoft's [temporary-register contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-registers-temporary) requires every used component to be defined, while the translator's adjacent FIXME records the `TEXKILL`-specific all-components rule. The parser now accumulates write masks, requires all four prior bits and restores the pre-instruction mask because kill is read-only. A split xy + zw positive control prevents over-rejection. The direct Software runtime, all 160 Software CTests and all 74 isolated EasyGL compiled tests pass | Confirmed shared classic-XNA validation gap and repaired, SOFTWARE-399 ✅; general per-opcode component-use validation remains SOFTWARE-164/165 work |
| `SGN` profile and scratch validation were inverted | MojoShader exposed opcode 34 to both stages and parsed its two scratch operands as value reads. Both renderers therefore rejected a valid vertex shader using distinct uninitialized r1/r2, while accepting constants as scratch operands and accepting `SGN` in a pixel shader; an aliased scratch pair failed only accidentally through the wrong uninitialized-read check | Microsoft's [`sgn` contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/sgn---vs) makes the opcode vertex-only and names src1/src2 as two different temporary scratch registers whose incoming values do not participate. A dedicated argument parser suppresses read-before-write validation only for those operands, while semantic validation enforces their type/distinctness and the opcode table enforces the stage. All four discriminators, the direct Software runtime, 160/160 Software CTests and all 78 isolated EasyGL compiled tests pass | Confirmed shared classic-XNA parser gap and repaired, SOFTWARE-400 ✅; aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| `EXPP`/`LOGP` stage and scalar-source validation was incomplete | MojoShader exposed both partial-precision opcodes to pixel shaders and checked only `LOGP` for a replicate source swizzle. Both renderers consequently accepted validly encoded ps_2_0 `EXPP`/`LOGP` instructions and a vs_2_0 `EXPP c0.xyzw` instruction | Microsoft's [`EXPP`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/expp---vs) and [`LOGP`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/logp---vs) contracts list only vertex profiles and require an explicit `.x`, `.y`, `.z` or `.w` replicate source. Three independent parser fixtures failed on both renderers before the managed patch and now pass; the direct Software runtime, 160/160 Software CTests and all 81 isolated EasyGL compiled tests are green | Confirmed shared classic-XNA parser gap and repaired, SOFTWARE-401 ✅; aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Vertex-only `LIT`/`SLT`/`SGE` and scalar `EXP` validation was absent | MojoShader exposed all four arithmetic opcodes to either shader stage and did not validate `EXP`'s source swizzle. Both renderers accepted pixel `LIT`/`SLT`/`SGE` and pixel/vertex `EXP c0.xyzw`; two earlier behavioral fixtures also unknowingly relied on invalid pixel `SLT`/`SGE`, so their passing results preserved the shared defect | Microsoft's vertex contracts define [`LIT`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/lit---vs), [`SLT`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/slt---vs) and [`SGE`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/sge---vs) while the pixel instruction tables omit them, and the [`EXP`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/exp---ps) contract requires an explicit replicate source. Five independent negative fixtures failed on both paths before the managed patch. The NRM and signed-LOG fixtures now use legal `ADD`/`CMP` gates; the direct Software runtime, 160/160 Software CTests and all 86 isolated EasyGL compiled tests pass | Confirmed shared classic-XNA parser gap and repaired, SOFTWARE-402 ✅; aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Pixel Shader Model 1.x accepted later-profile opcodes | MojoShader applied individual argument checks but had no complete version-level opcode gate. EasyGL accepted all thirteen well-formed ps_1_4 `RCP`/`RSQ`/`MIN`/`MAX`/`EXP`/`LOG`/`FRC`/`POW`/`CRS`/`ABS`/`NRM`/`DSX`/`DSY` fixtures; Software accepted seven and rejected the rest only in its later decoder | Microsoft's [`ps_1_1`–`ps_1_4` table](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-instructions-ps-1-x) is an explicit allowlist with minor-version columns. The shared parser now encodes that whole table, including legacy texture and `DP4`/`CMP`/`BEM` version boundaries. All thirteen negative probes, the direct Software runtime, 160/160 Software CTests and the complete 99-test isolated EasyGL compiled family pass; its existing ps_1_2/1_3/1_4 draw fixtures are positive regression proof | Confirmed broad shared classic-XNA profile-validation gap and repaired, SOFTWARE-403 ✅; aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Vertex Shader Model 1.1 accepted later-profile opcodes | MojoShader had no profile-level vertex opcode gate. EasyGL accepted all nine well-formed `vs_1_1` `ABS`/`CRS`/`NRM`/`POW`/`SINCOS`/`SGN`/`MOVA`/`DEFB`/`DEFI` fixtures; Software accepted one and rejected the others only after the shared parser in its private decoder | Microsoft's [`vs_1_1` table](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-instructions-vs-1-1) defines the complete legal set. The shared parser now encodes it explicitly, retaining `DCL`, `DEF`, legacy `EXP`/`LOG`, `EXPP`/`LOGP` and excluding even otherwise-common `LRP`. All nine negative probes, the direct Software runtime, 160/160 Software CTests and the complete 108-test isolated EasyGL compiled family pass; existing declaration-free vs_1_1 fixtures provide positive regression proof | Confirmed broad shared classic-XNA profile-validation gap and repaired, SOFTWARE-404 ✅; aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Pixel Shader Model 2.0 accepted Shader Model 2.x/3 opcodes | MojoShader had no exact-profile pixel opcode gate after the new Shader Model 1.x check. Both renderers accepted all seven well-formed `ps_2_0` probes for `DEFB`, `DEFI`, `REP`, `IF`, `DSX`, `DSY` and `SETP`, even though typed constants, flow control, derivatives and predication first appear in later pixel profiles | Microsoft's [`ps_2_0` table](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-instructions-ps-2-0) defines the complete legal set. The shared parser now encodes it explicitly without constraining `ps_2_x`, software profiles or `ps_3_0`. All seven negative probes, the direct Software runtime, 160/160 Software CTests and the complete 115-test isolated EasyGL compiled family pass; existing ps_2_0 draw fixtures prove retained arithmetic, declaration and sampling instructions | Confirmed broad shared classic-XNA profile-validation gap and repaired, SOFTWARE-405 ✅; aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Exact Shader Model 2.0 accepted Shader Model 2.x dynamic flow and predication | MojoShader had no complete exact-`vs_2_0` opcode gate and accepted predicate operands or instruction predication regardless of minor profile. Software accepted all eight probes; EasyGL accepted seven and rejected predicate `CALLNZ` only later and accidentally in GLSL translation | Microsoft's [`vs_2_0` table](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-instructions-vs-2-0) permits only static Boolean flow, while [`vs_2_x`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-instructions-vs-2-x) adds `IFC`, break forms, predicate `IF`/`CALLNZ`, `SETP` and instruction predication. The shared parser now applies the exact 2.0 allowlist plus operand/token profile checks. All eight probes, the direct Software runtime, 160/160 Software CTests and the complete 123-test isolated EasyGL compiled family pass | Confirmed broad shared classic-XNA profile-validation gap and repaired, SOFTWARE-406 ✅; the aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Pixel Shader Model 2.x accepted the Shader Model 3-only `LOOP` block | MojoShader's loop state validator was shared between stages and versions, so both Software and EasyGL created an otherwise-well-formed `ps_2_x` Effect containing `LOOP`/`ENDLOOP` | Microsoft's [`ps_2_x` table](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-instructions-ps-2-x) permits `REP` but not `LOOP`; [`ps_3_0`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-instructions-ps-3-0) adds `LOOP`. The shared validator now rejects it only in pre-3.0 pixel programs. The direct Software runtime, 160/160 Software CTests and the complete 124-test isolated EasyGL compiled family pass; `TEXLDL`, the other generic 3.0-only candidate, was already correctly gated | Confirmed shared classic-XNA profile-validation gap and repaired, SOFTWARE-407 ✅; the aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Matrix opcodes accepted forbidden `src1` modifiers and `dst == src0` | MojoShader's five matrix validators checked only the destination mask. Both renderers accepted `ps_2_0 M4X3` with a negated matrix base, a swizzled matrix base, or one initialized temporary serving as both destination and vector input | Microsoft's [`m4x3` contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/m4x3---ps) forbids swizzle/negate on matrix `src1` and forbids aliasing `dst` with vector `src0`. The common row-replication helper now validates all three rules for all five matrix shapes and both shader stages. Three negative fixtures, the direct Software runtime, 160/160 Software CTests and the complete 127-test isolated EasyGL compiled family pass; existing exact matrix draws remain positive controls | Confirmed shared classic-XNA operand-validation gap and repaired, SOFTWARE-408 ✅; the aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Shader Model 1/2 accepted Shader Model 3 absolute source modifiers | MojoShader decoded `abs` and `-abs` without checking the shader version. Software and EasyGL consequently accepted all four otherwise-valid pixel/vertex Shader Model 2.0 probes | Microsoft's [vertex](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-registers-modifiers-source) and [pixel](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-registers-modifiers-source) tables permit negate in all profiles but restrict both absolute forms to Shader Model 3. The shared parser now rejects those two token forms below 3.0 while retaining valid negate. Four negative fixtures, the direct Software runtime, 160/160 Software CTests and the complete 131-test isolated EasyGL compiled family pass | Confirmed shared classic-XNA source-modifier profile gap and repaired, SOFTWARE-409 ✅; the aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Shader Model 3 accepted mixed absolute and ordinary float-constant reads | MojoShader retained an explicit FIXME instead of enforcing the profile-wide consistency rule. Software and EasyGL accepted all four pixel/vertex and order permutations | Microsoft's same [vertex](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-registers-modifiers-source) and [pixel](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-registers-modifiers-source) tables require either every SM3 float-constant read to use `abs`/`-abs`, or none. The parser records the first mode and rejects a later mismatch across all normalized float-constant banks. Four negative fixtures plus legal all-absolute pixel/vertex controls pass in the direct Software runtime, all 160 Software CTests and the complete 136-test isolated EasyGL compiled family | Confirmed shared classic-XNA Shader Model 3 source-validation gap and repaired, SOFTWARE-410 ✅; the aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Pixel and vertex sampler DCLs accepted the first out-of-range register | The parser had the official four-slot vertex limitation only as a FIXME and no pixel range check. Both renderers accepted `vs_3_0 s4` and `ps_2_0 s16` at Effect creation | Microsoft's [`vs_3_0`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-registers-vs-3-0) and [`ps_2_x`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-registers-ps-2-0) tables define four and sixteen sampler registers respectively. The shared DCL validator now rejects the first invalid register before a renderer can index its binding collections. The corrected pixel fixture really declares and samples `s16`; the direct Software runtime, 160/160 Software CTests and complete 138-test isolated EasyGL compiled family pass | Confirmed shared classic-XNA register-range gap and repaired, SOFTWARE-411 ✅; the aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Fixed shader profiles accepted out-of-range temporary registers | Neither destination nor source parsing validated temporary indices. Both Software and EasyGL accepted all nine tested first-invalid registers spanning `ps_1_1`, `ps_1_4`, exact `ps_2_0`, `ps_2_x`, `ps_3_0`, `vs_1_1`, exact `vs_2_0`, `vs_2_x` and `vs_3_0` | Microsoft's profile register tables define fixed limits of 2/6/12/32 pixel temporaries and 12/32 vertex temporaries; 2.x allows a device-dependent count up to 32, so the cap-free common parser uses 32 as its acceptance ceiling. Shared source and destination validation now rejects each first-out-of-range token. Nine paired maximum-register controls prove the last permitted token remains accepted for every profile. The direct Software runtime, 160/160 Software CTests and complete 148-test isolated EasyGL compiled family pass | Confirmed shared classic-XNA register-range gap and repaired, SOFTWARE-412 ✅; the aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Pixel and vertex input banks accepted their first out-of-range register | Generic token parsing recorded any 11-bit input index without consulting its shader profile. Both renderers accepted all eight first-invalid probes across legacy pixel color/texture banks, `ps_2_0` color/texture-coordinate inputs, `ps_3_0` interpolators and `vs_2_0`/`vs_3_0` vertex inputs | Microsoft's profile tables define `ps_1_x` v0..v1 and t0..t3/t5, `ps_2_x` v0..v1 and t0..t7, `ps_3_0` v0..v9, and vertex v0..v15. Shared destination and source validation now enforces those exact stage/profile limits. Eight maximum-register controls prevent off-by-one rejection. The direct Software runtime, 160/160 Software CTests and complete 157-test isolated EasyGL compiled family pass | Confirmed shared classic-XNA register-range gap and repaired, SOFTWARE-413 ✅; the aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Shader output banks accepted their first out-of-range register | Generic destination parsing accepted all six Software probes and five of six EasyGL probes. EasyGL happened to reject only `oDepth1` later in GLSL translation; `oC4`, `oD2`, `oT8`, the fourth fixed raster kind and `vs_3_0 o12` reached both compiled paths | Microsoft's register tables expose four pixel colors, one depth, two fixed vertex colors, eight fixed texture coordinates, three raster kinds and twelve generic Shader Model 3 vertex outputs. One common destination validator now rejects the first invalid index for every bank; paired controls retain each final legal register. The direct Software runtime, 160/160 Software CTests and complete 164-test isolated EasyGL compiled family pass | Confirmed Software/shared classic-XNA register-range gap and repaired, SOFTWARE-414 ✅; the aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Fixed constant and singleton control banks accepted out-of-range registers | Both compiled paths accepted ten first-invalid pixel/vertex probes across float, integer, Boolean, predicate and address registers. `vs_2_0 aL1` was worse under Software: it reached an emitter assertion loop instead of a validation error, so the unsafe pre-fix EasyGL probe was intentionally not repeated | Microsoft's profile tables define pixel float counts 8/32/224, sixteen `i#` and `b#` constants, plus one `p0`, `a0` and `aL`. Shared source/destination validation now rejects all eleven first-invalid tokens before emission; eleven paired controls retain the maximum valid registers. The direct Software runtime, 160/160 Software CTests and complete 176-test isolated EasyGL compiled family pass. Vertex floats remain capability-dependent; `l0..l15` already had a dedicated validator | Confirmed shared classic-XNA validation and assertion-safety gap, repaired as SOFTWARE-415 ✅; aggregate Software capability remains false while SOFTWARE-164/165 stay open |
| Fixed vertex profiles accepted programs beyond their instruction-slot limits | MojoShader already assigned and accumulated per-opcode slot costs but never compared the total with the shader profile. Both Software and EasyGL accepted `vs_1_1` with 129 slots and `vs_2_0`/`vs_2_x` with 257 | Microsoft's [vertex-profile table](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-differences) fixes the limits at 128/256/256. Shared validation now rejects each first excess slot before translation, while paired 128/256/256 programs remain accepted. The direct Software runtime, 160/160 Software CTests and complete 180-test isolated EasyGL compiled family pass. The cap-free parser leaves `vs_3_0` alone because its limit is device-dependent above the guaranteed 512 minimum | Confirmed shared classic-XNA profile-validation gap and repaired, SOFTWARE-416 ✅; later capability-dependent profiles remain in SOFTWARE-164/165 and aggregate Software capability remains false |
| Exact pixel Shader Model 2.0 accepted programs beyond either independent instruction budget | The common parser exposed a single aggregate instruction count and never tracked the profile's arithmetic and texture categories. Software and EasyGL therefore both accepted otherwise-valid programs with 65 `MOV` slots and 33 `TEXLD` slots | Microsoft's [`ps_2_0` profile](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-2-0) fixes 64 arithmetic and 32 texture slots. Common validation now counts the categories independently and rejects the first excess slot; paired 64/32 controls remain accepted. The direct Software runtime, 160/160 Software CTests and complete 183-test isolated EasyGL compiled family pass | Confirmed shared classic-XNA profile-validation gap and repaired, SOFTWARE-417 ✅; capability-dependent later-profile budgets remain in SOFTWARE-164/165 and aggregate Software capability remains false |
| Pixel Shader Model 1.x had no arithmetic, texture or per-phase slot validation | Both compiled paths accepted all eight maximum-plus-one probes: 1.1 arithmetic/texture, 1.2 double-slot arithmetic, 1.4 arithmetic/texture/second-phase arithmetic, and 1.1 co-issued-pair/`TEXBEML` arithmetic | Microsoft's [profile limits](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-differences) and [instruction table](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-instructions-ps-1-x) define four/six texture and eight arithmetic slots, 1.4 per-phase reset, zero-slot NOP, two-slot DP4/CMP, dual-category TEXBEML, two-slot BEM and co-issued RGB/alpha pairing. The shared parser now accounts for each rule. Maximum controls include 32 surplus NOPs, TEXBEML plus seven MOVs, eight co-issued pairs and two full 1.4 phases. The direct Software runtime, 160/160 Software CTests and complete 192-test isolated EasyGL compiled family pass | Confirmed shared classic-XNA profile-validation gap and repaired, SOFTWARE-418 ✅; capability-dependent later ceilings and remaining placement/operand rules stay in SOFTWARE-164/165 |
| Pixel Shader Model 1.x destination masks were not validated against the legacy pipeline | Software accepted all three invalid bytecode probes: arbitrary `.rb` on a 1.1 `MOV`, alpha-only 1.1 `DP3` and partial 1.1 `TEXCOORD`. EasyGL accepted the first two and rejected only the texture case later during GLSL emission | Microsoft's [write-mask table](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-registers-modifiers-write-mask) permits only `.rgba`/`.rgb`/`.a` before 1.4, only `.rgba`/`.rgb` for legacy `DP3`, and no texture-address masks before 1.4's special `TEXCRD`/`TEXLD`. Common early validation now enforces those rules. Positive controls retain every ordinary 1.1 mask plus arbitrary 1.4 arithmetic and `TEXCRD` masks. The direct Software runtime, 160/160 Software CTests and complete 196-test isolated EasyGL compiled family pass | Confirmed Software/shared classic-XNA validation gap and repaired, SOFTWARE-419 ✅; remaining co-issue and placement rules stay in SOFTWARE-164/165 |
| Pixel Shader Model 1.x accepted ineligible co-issue opcodes | Both Software and EasyGL accepted a co-issued texture-address instruction and `DP4`; the 1.4 `DP4` also received an incorrect zero slot because the parser assumed every one-slot co-issued opcode used only one pipeline | Microsoft's [Shader Model 1.x overview](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-1-x) permits pairing arithmetic rather than texture-address instructions, and the [`DP4` contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dp4---ps) explicitly says it cannot be co-issued because it occupies both pipelines. Common validation rejects both before accounting/emission. RGB→alpha and alpha→RGB controls remain accepted; Wine's Windows-differential `d3d8/visual.c` corpus independently records the reverse order. The direct Software runtime, 160/160 Software CTests and complete 199-test isolated EasyGL family pass | Confirmed shared classic-XNA validation/accounting gap and repaired, SOFTWARE-420 ✅; structural pairing rules remain in SOFTWARE-164/165 |
| Pixel Shader Model 1.x did not validate co-issue pair structure | Both renderers accepted all six executed structural violations after the opcode gate: orphan, same-pipeline pair, full-mask predecessor, texture predecessor, `DP4` predecessor and phase-crossing pair. The parser also had no state to prevent a third co-issued instruction | Microsoft's [write-mask contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-registers-modifiers-write-mask) defines one RGB and one alpha pipeline operation per pair. Common parse state now requires exactly one eligible arithmetic predecessor, rejects a third member, remembers pre-1.4 masks and clears adjacency at `PHASE`. RGB→alpha and the Windows-observed alpha→RGB order remain accepted. Shader Model 1.4 is intentionally not reduced to mask bits because Microsoft documents `DP3` as vector-pipeline even when writing alpha. The direct Software runtime, 160/160 Software CTests and complete 206-test isolated EasyGL family pass | Confirmed shared classic-XNA validation/accounting gap and repaired, SOFTWARE-421 ✅; unmeasured 1.4 instruction-specific pairing remains in SOFTWARE-164/165 |
| Shader input banks were range-checked but not consistently read-only | Six ordinary `MOV` programs targeted ps_1_1 `v0`, ps_2_0 `v0`/`t0`, ps_3_0 `v0` and vs_2_0/vs_3_0 `v0`. Software accepted the ps_3_0 case and both vertex cases; EasyGL happened to reject all six later in GLSL translation | Microsoft's [ps_1_x table](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-registers-ps-1-x), [ps_2_0 table](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-registers-ps-2-0) and [vertex input contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-registers-input) mark these inputs read-only. The common parser now rejects input destinations in ordinary instructions while retaining `DCL` and legacy ps_1_x read/write texture destinations. The direct Software runtime, 160/160 Software CTests and complete 212-test isolated EasyGL family pass | Confirmed Software classic-XNA validation gap and repaired in common plumbing, SOFTWARE-422 ✅; other register permissions remain under SOFTWARE-164/165 |
| Constant, sampler, miscellaneous, loop and predicate banks lacked instruction-specific destination validation | Thirteen ordinary `MOV` programs targeted restricted pixel/vertex registers. Software accepted ten: float/integer/Boolean constants, loop state and predicates; EasyGL rejected all thirteen only during later GLSL translation | Microsoft's register contracts reserve constant writes for `DEF`/`DEFI`/`DEFB`, predicate writes for `SETP`, sampler use for declarations/sampling and miscellaneous/loop registers for their defining semantics. Common parsing now rejects every ordinary restricted destination before backend translation while preserving `DCL` and legal special instructions. The direct Software runtime, 160/160 Software CTests and complete 225-test isolated EasyGL family pass. Address-register rules remain separate because `MOV` versus `MOVA` is profile-sensitive | Confirmed Software classic-XNA validation gap and repaired in common plumbing, SOFTWARE-423 ✅; write-only output sources and address/source-operand rules remain under SOFTWARE-164/165 |
| Write-only output registers could be read as ordinary instruction sources | Software accepted all six probes spanning pixel `oC#`/`oDepth`, fixed vertex `oPos`/`oD#`/`oT#` and generic Shader Model 3 vertex `o#`. EasyGL accepted five and happened to reject only `oDepth` during GLSL translation | Microsoft's [pixel output-color](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-registers-output-color), [fixed vertex-output](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-registers-output), [`vs_2_0`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-registers-vs-2-0) and [`vs_3_0`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-registers-vs-3-0) contracts mark these banks write-only. Shared source-token validation now rejects them before either renderer emits code. The direct Software runtime, 160/160 Software CTests and complete 231-test isolated EasyGL family pass | Confirmed shared classic-XNA validation gap and repaired in common plumbing, SOFTWARE-424 ✅; address/source-operand rules remain under SOFTWARE-164/165 |
| Vertex address-register access and conversion were not validated or executed exactly | Software accepted all six illegal probes: direct `a0` reads in vertex Shader Models 1.1/2.0/3.0, `ADD a0` in 1.1 and `MOV a0` in 2.0/3.0. EasyGL accepted the 2.0/3.0 direct reads, rejected the remaining invalid cases only during GLSL emission, and failed to translate a legal `vs_1_1 MOV a0` because it assigned a float expression to an integer register | Microsoft's [address-register contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-registers-address), [`MOV`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/mov---vs) and [`MOVA`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/mova---vs) documentation require relative-address-only reads, `MOV` writes in 1.1, `MOVA` writes in 2.0+, and round-to-nearest conversion. Common validation now enforces that matrix, while both execution paths round signed values before conversion. Three legal creation controls, six rejection probes and a direct Software `+1.6/-1.6` relative-read oracle pass with all 160 Software CTests and the complete 238-test isolated EasyGL family | Confirmed Software/shared classic-XNA validation and execution gap plus EasyGL translation gap; repaired, SOFTWARE-425 ✅; remaining instruction-specific source/placement rules stay under SOFTWARE-164/165 |
| Sampler pseudo-registers could reach backend translation as ordinary sources | Three forged programs used `MOV r#, s0` in `ps_2_0`, `ps_3_0` and `vs_3_0`. Software publicly accepted both pixel programs and rejected only the vertex case during its later IR conversion; EasyGL rejected all three only while translating invalid GLSL types | Microsoft's [pixel sampler](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-registers-sampler) and [vertex sampler](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-registers-sampler) contracts explicitly call `s#` a pseudo-register that cannot be read directly. Common parse state now admits it only as source 1 of `TEXLD`/`TEXLDL` or source 3 of `TEXLDD`. The direct Software runtime, all 160 Software CTests and the complete 241-test isolated EasyGL family pass; existing sampled-effect cases retain every legal sampler position and dimension | Confirmed Software classic-XNA validation gap and repaired in common plumbing, SOFTWARE-426 ✅; typed flow-control and remaining source/placement rules stay under SOFTWARE-164/165 |
| Integer and Boolean constant banks were accepted as ordinary arithmetic sources | Four Shader Model 3 programs used `MOV r#, i0` or `MOV r#, b0.x` in both stages. Software accepted all four; EasyGL accepted both integer cases and rejected Boolean cases only during GLSL type checking | Microsoft's [pixel integer](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-registers-constant-integer), [vertex integer](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-registers-constant-integer), [pixel Boolean](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-registers-constant-boolean) and [vertex Boolean](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-registers-constant-boolean) contracts reserve these banks for flow control. Shared validation now accepts `i#` only in `LOOP`/`REP` and `b#` only in `IF`/`CALLNZ`, while executed control-flow tests retain every legal position. The direct Software runtime, all 160 Software CTests and the complete 245-test isolated EasyGL family pass | Confirmed shared classic-XNA validation gap and repaired, SOFTWARE-427 ✅; predicate, label, loop-counter and miscellaneous source restrictions remain under SOFTWARE-164/165 |
| Predicate, label and loop-counter registers were accepted as ordinary arithmetic sources | Six Shader Model 3 programs used `MOV r#, p0/l0/aL` across both stages. Software accepted all six; EasyGL rejected all six only during GLSL translation | Wine's builtin `D3DXAssembleShader` independently rejected the identical six programs with `X5679`/`X5681`. Microsoft's `SETP`/flow, `CALL`/`LABEL` and loop-counter contracts confine these special banks to instruction predication or their named control/relative-address positions. Common validation now enforces the exact ordinary source ordinals and distinguishes the extra predication token from ordinary sources. Existing executable predication, `IF`/`CALLNZ`/`BREAKP`, label/subroutine, loop and relative-address cases remain positive controls. The direct Software runtime, all 160 Software CTests and the complete 251-test isolated EasyGL family pass | Confirmed Software classic-XNA validation gap and repaired in common plumbing, SOFTWARE-428 ✅; SOFTWARE-429 closes relative-`aL` scope and SOFTWARE-430 closes `vPos`/`vFace` placement; remaining operand rules stay under SOFTWARE-164/165 |
| `ps_3_0` miscellaneous-input masks and sources were under-validated | Microsoft's [`ps_3_0` register contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-registers-ps-3-0) permits exactly `.x`, `.y` or `.xy` when declaring `vPos` and confines scalar `vFace` to comparison conditions; compiler-produced D3D9 programs additionally use it as CMP's condition source | Before repair, both common parsers accepted `.z`, `.xyz` and full `vPos` declarations. Software also accepted `MOV oC0, vFace`; EasyGL rejected that fourth case only accidentally during GLSL type checking. Common validation now enforces the three legal position masks and admits `vFace` only as CMP source zero or either SETP/IFC/BREAKC comparison operand. Four invalid and four valid creation probes pass in both paths, while the existing rendered raster-input scene remains the output oracle | Confirmed Software/shared classic-XNA parser gap and repaired, SOFTWARE-430 ✅; the audit also corrected earlier D3DX evidence attribution after loader tracing proved the local DLL is Wine builtin, not Microsoft-native |
| Shader Model 3 semantic declarations were overwritten instead of packed by component | Microsoft's [`ps_3_0` semantic contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dcl-usage---ps) and [`vs_3_0` output contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dcl-usage-outut-register---vs) permit distinct semantics to share one physical register only through unique, nonoverlapping masks; duplicate semantics are invalid, vertex inputs have no masked form, and position/point-size outputs require full masks | Both paths accepted seven invalid overlap, duplicate and partial-mask programs. EasyGL additionally stored only the last declaration for a physical register, so a legal reversed-order COLOR0.zw/TEXCOORD0.xy pair rendered black rather than exact `(64,128,191,255)`; Software's full raw semantic vectors happened to render the legal control but did not validate it. The common parser now retains each declaration and rejects every invalid combination. GLSL uses one local physical register and masked semantic copies at the vertex epilogue/pixel prologue. The direct Software runtime and all 275 isolated EasyGL tests pass | Confirmed shared classic-XNA parser and EasyGL execution gap; repaired, SOFTWARE-431 ✅; centroid-qualified interpolation remains separately unaudited under SOFTWARE-164/165 |
| Software's masked SM3 pixel-input assembly overwrote unrelated components | SOFTWARE-431's first legal program packed TEXCOORD0.xy and COLOR0.zw into the same VS physical register, so both Software semantic values carried the same complete vector and could not reveal a whole-register assignment in the pixel prologue | A second valid program stores the semantics in separate VS `o1`/`o2` registers with conflicting values in every unowned component, then maps them into PS `v0.xy`/`v0.zw`. EasyGL passed exact `(64,128,191,255)` while Software failed before repair. The Software IR now retains DCL masks and copies only owned components. Both valid controls, seven invalid cases, the direct runtime, all 160 Software CTests and all 276 isolated EasyGL tests pass | Confirmed Software classic-XNA execution gap and repaired, SOFTWARE-432 ✅; aggregate public capability remains false under SOFTWARE-164/165 |
| Explicit and automatic `COLOR` centroid interpolation was absent | Microsoft's [`ps_2_0+` modifier contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-ps-instructions-modifiers-ps-2-0) requires an explicitly centroid-qualified input to move interpolation inside the primitive when a multisample pixel's center is uncovered, and says pixel-shader `COLOR` inputs receive that behavior automatically | A 4x-MSAA triangle edge makes ordinary center interpolation select red while a covered-sample interpolation selects green. Ordinary TEXCOORD controls passed before repair in Shader Models 2 and 3; Software and EasyGL both selected red for implicit COLOR and both rejected the explicit qualifier. Software now retains the qualifier and replaces only centroid varyings with covered-sample interpolation. MojoShader emits paired centroid varyings and specializes the vertex shader at link time only for semantics the pixel shader requests. Six SM2/SM3 behavioral controls, the direct Software runtime, all 160 Software CTests and all 282 isolated EasyGL tests pass | Confirmed shared classic-XNA execution/translation gap and repaired, SOFTWARE-433 ✅; aggregate public capability remains false under SOFTWARE-164/165 |
| EasyGL's `DST`/`CRS` GLSL emission ignored partial destination masks | The translator constructed the complete `vec4` distance result or `vec3` cross product, then assigned it directly to a destination such as `r0.xz`; no existing Effect combined either composite-result instruction with a partial mask | Microsoft's [vertex masking contract](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx9-graphics-reference-asm-vs-registers-modifiers-masking) allows ordered component combinations, while [`CRS`](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/crs---vs) explicitly permits every nonempty XYZ subset. Valid `DST r0.xz` and `CRS r0.xz` fixtures both execute correctly in Software but pre-fix EasyGL fails GLSL compilation with `vec4 -> vec2` and `vec3 -> vec2`. The managed emitter now swizzles each constructed result by the destination mask before assignment. Both shared full-quad output probes, the direct Software runtime, all 160 Software CTests and all 284 isolated EasyGL compiled-Effect tests pass | Confirmed EasyGL classic-XNA translation gap and repaired, SOFTWARE-434 ✅; aggregate Software capability remains false under SOFTWARE-164/165 |
| `GraphicsDevice.MultiSampleMask` was cache-only | Existing output tests assigned the identically named property on `BlendState`; the public device-level property had only getter/setter storage and no renderer call | Six new shared assertions initially passed only two: the getter reflected direct assignment and whole-state output worked through the separate BlendState path. Mask 0/1 output, whole-state getter synchronization and independent restoration failed. Software and desktop EasyGL now pass all 18 contract checks | Confirmed and repaired, SOFTWARE-166 ✅ |
| `GraphicsDevice.VertexTextures` and `VertexSamplerStates` reached no renderer | Existing tests checked defaults and disposal unbinding, while compiled-effect tests used effect-owned pixel/cube/volume samplers. No test executed a public vertex-stage sampler | SOFTWARE-168 carries the owning device collections in the renderer-neutral draw contract. EasyGL maps them to MojoShader vertex units 16..19; Software executes `vs_3_0 TEXLDL` against its 2D/cube/volume samplers. Exact output proves public override, stage isolation, mip bias/clamp, W addressing, validation and lifecycle. A managed MojoShader patch also fixes the vertex sampler DCL reserved mask that rejected valid cube/volume declarations | Confirmed renderer-wide classic-XNA gap and repaired, SOFTWARE-167/168 ✅ |
| Software treated non-Add One/Zero blending as enabled | The blend matrix tested every factor and function, but function cases deliberately used One/One so blending was necessarily enabled; it never combined the opaque factors with non-Add functions | FNA3D's GL and D3D11 drivers derive BlendEnable from the four factors only, as EasyGL does. The added five-function/two-channel matrix failed four Software checks (`ReverseSubtract` and `Min`, RGB and alpha) before repair; both renderers now pass 45/45 | Confirmed and repaired, SOFTWARE-169 ✅ |
| Software applied factors before MIN/MAX blending | Existing function cases used One/One, so multiplying first was indistinguishable from the native rule | D3D9/11 and OpenGL ignore factors for MIN/MAX, which FNA3D exposes directly. Zero/Zero factors must still produce raw component min/max; four new RGB/alpha checks failed on Software before repair. Software and desktop EasyGL now pass 49/49 | Confirmed and repaired, SOFTWARE-170 ✅ |
| CNA rejected every negative indexed `baseVertex` | Positive-base coverage could only prove that the addend was not dropped. The prior public-contract test explicitly encoded CNA's non-XNA rejection as expected behavior, so green tests preserved the defect | Recovered Microsoft XNA 4.0 passes the signed value directly to D3D9 and FNA forwards it to FNA3D. New ordinary and instanced pixel tests use positive stored indices compensated by negative bases; the old guard necessarily threw before either draw. With 64-bit effective-range validation and Software's decoded-address guard, both tests pass on Software and desktop EasyGL while underflow cases still throw | Confirmed shared XNA conformance bug and repaired for the audited renderers, SOFTWARE-171 ✅ |
| Texture and render-target constructors delegated invalid sizes to renderers | Existing happy-path tests started at one texel, so renderer-specific exceptions or invalid native zero-sized allocations stayed invisible | Recovered XNA checks Texture2D width/height, TextureCube size and Texture3D width/height/depth before allocation. Five focused tests cover zero/negative ordinary and render-target construction and pass on Software and OPENGLES3 EasyGL after the shared guard | Confirmed shared XNA conformance bug and repaired, SOFTWARE-172 ✅ |
| `Texture3D` implemented only one of 15 XNA HiDef volume formats | The storage contracts used `SurfaceFormat::Color`; the constructor's generic format parameter looked complete while `Texture::ValidateFormat` rejected every other format and the class exposed only `Color*` transfers | Recovered XNA/FNA's `ValidVolumeFormats` list contains Color plus 14 packed/integer/float/half formats. The pre-fix constructor oracle failed nineteen assertions. The resource-specific gate, typed transfer surface and exact Software/EasyGL storage now pass four shared all-format behavior tests, including a `Vector4` volume transferred as four `float` elements per voxel under XNA's total-byte rule | Confirmed renderer-wide classic-XNA gap and repaired, SOFTWARE-173 ✅ |
| EasyGL's base-vertex-less GL profiles corrupted compensated negative indexed draws | `SOFTWARE-171` initially ran Software and desktop EasyGL only. The GLES fallback had positive-base tests, so shifting every enabled per-vertex pointer looked sufficient | On OPENGLES3, indices 3..5/base -3 produced garbage instead of the green decoy discriminator, and the instanced -12 case missed all six expected triangles while lighting 14,608 pixels outside the range. The repaired path rebases only the selected CPU-shadowed index slice. Ordinary Uint16, Uint32 plus nonzero `startIndex`, and instanced probes pass on OPENGLES3 and Software | Confirmed EasyGL GLES/WebGL classic draw gap and repaired, SOFTWARE-174 ✅ |
| EasyGL cached a context-owned raw `glEnable` dispatch forever | Ordinary one-device tests never reconstruct a renderer in the same process immediately after a 32-bit indexed draw; debugger delay also masks the failure | Repeating an existing public Uint32 draw reliably made the next desktop device call address zero. A normal-speed signal trace resolved the saved return address to `EnableVertexProgramPointSize()` at renderer construction. The new one-test oracle performs the draw, destroys that device, constructs a replacement and clears it; it passes after resolving the entry point from each current context, and the provoking draw repeats three times | Confirmed EasyGL lifecycle gap and repaired, SOFTWARE-175 ✅ |
| Both renderers interpreted `RasterizerState.DepthBias` in native rather than XNA units | The original Software oracle used `3000000.0f`; that number made `value * 2^-24` visible and therefore encoded the implementation error instead of testing a realistic public value. EasyGL had no format-switching bias oracle | FNA3D GL/D3D11 scale the public float by `(2^depthBits)-1`. A new shared test applies ±0.02 while the backbuffer is active, switches to Depth16/Depth24/Depth24Stencil8 targets, and requires the second layer to change its depth winner. It fails under both old formulas and passes on Software, Mesa OpenGL33 and Mesa OpenGLES3; Software's older 17-check suite also passes after replacing its compensating million-scale constant | Confirmed shared XNA conformance bug and repaired, SOFTWARE-176 ✅ |
| EasyGL's `GL_LINES` wireframe bypassed depth bias and several draw routes | Feature-oriented tests used an ordinary stock zero-bias triangle. They did not cross FillMode with bias, SpriteBatch, compiled Effects, multiple streams or instancing | The new wireframe oracle first left 72 green coplanar pixels visible under +0.02 on desktop EasyGL while Software produced zero. Native desktop polygon mode plus polygon-offset-line now passes that oracle and all five existing wireframe capability/output/transition checks. Source-path audit proves ES/WebGL still expands only four single-stream stock branches and cannot express native culling, polygon offset or clip-generated boundary edges | Desktop repaired, SOFTWARE-177 ✅; ES/WebGL real gap, SOFTWARE-178 ⬜ |
| Software and EasyGL inherited unlimited XNA `GraphicsProfile` resource ceilings | Shared constructors appeared to enforce Reach/HiDef by querying renderer hooks, but only D3D9 overrode them; default-device tests therefore normalized illegal Reach volume textures and MRT | Recovered/documented profile data and CNA's already-established D3D9 table require Reach 2048/512/no-volume/one-MRT and HiDef 4096/4096/256/four-MRT. The new public two-profile contract rejects all six over-limit resource cases, accepts HiDef volume/MRT, and the common-default unit fixes the bypass for every renderer. Software passes full 2,390/2,390; Mesa OpenGL33 passes full 2,406/2,406 and OpenGLES3 passes the focused 11/11 | Confirmed shared XNA/Core conformance bug and repaired, SOFTWARE-179 ✅ |
| Software and EasyGL claimed requested packed backbuffer formats they never allocated | The adapter query correctly fell back to Color, but direct construction/reset normalized through an inherited identity method; ordinary tests requested default Color only | The new public contract requests `Bgr565` at construction and `Bgra4444` at reset. Before repair, `PresentationParameters` retained the false two-byte format and `GetBackBufferData(Color*)` rejected the renderer's actual four-byte pixels. Both transitions now report Color and return every exact cleared pixel on Software, Mesa OpenGL33 and Mesa OpenGLES3 | Confirmed inherited-default/core state bug and repaired, SOFTWARE-180 ✅ |
| EasyGL's selected backbuffer depth/stencil format did not match fragment behavior | The old presentation test asserted only that the requested enum round-tripped. Single-sample EasyGL always used the window's hidden Depth24Stencil8 planes, while its 4× FBO always allocated Depth24 without stencil—the two paths were wrong in opposite directions | A new direct-device contract failed 0/3 before repair: `None` rejected a farther fragment, Depth24 exposed stencil, and 4× Depth24Stencil8 exposed none. EasyGL now gates the fixed window planes by the selected semantic format and allocates the exact selected multisample attachment. The contract passes 3/3 on Software, OpenGL33 and OpenGLES3; the shared runtime Reset scene passes 8/8 on all three and the affected desktop gtest set passes 57/57 | Confirmed EasyGL classic state/attachment gap and repaired, SOFTWARE-181 ✅ |
| EasyGL advertised backbuffer MSAA but ignored the ordinary manager preference | The direct-construction path allocated a faux multisample FBO, while the eager Game device existed before `GraphicsDeviceManager` preferences and EasyGL inherited the reset-time no-op. The old shared test encoded zero as the expected result instead of demanding the advertised capability | Strengthening the shared test to require four real samples failed 3/5: both public/native counts stayed zero and sample mask 1 produced full red. EasyGL now reallocates in place and reports its driver-clamped count. The contract passes 7/7 on Software, OpenGL33 and OpenGLES3, proving quarter-red output, release to full red, and re-enable back to quarter red; the updated EasyGL transition matrix passes 10/10 | Confirmed inherited-default/Core propagation gap and repaired, SOFTWARE-182 ✅ |
| `Texture2D` Color transfer overloads disagreed on invalid inputs | Older unit tests explicitly blessed the convenience setter's silent return, while happy-path rectangle coverage never used non-positive extents. Cube, volume and byte-transfer overloads already rejected the same calls | FNA delegates its convenience setter into the validating overload. A new real-device regression failed 0/4 before repair: null input, zero count, paired-negative Set extents, and zero/negative Get extents all returned normally. The shared focused Texture2D selection now passes 46/46 on Software and Mesa OpenGL33; rejected Get leaves its destination unchanged | Confirmed shared public-XNA validation gap and repaired, SOFTWARE-183 ✅ |
| Color cube transfers reached undeclared mip storage; cube/volume windows could overflow | Typed/compressed cube overloads had the correct upper mip guard, making the missing Color check easy to overlook. Existing start-index tests covered negatives and ordinary offsets, not representability of the endpoint | New Color Set/Get probes failed 0/2 before repair: `level == LevelCount` reached Software storage and surfaced `NotSupportedException` rather than public range rejection; sufficiently larger shifts were undefined. Shared 64-bit endpoint tests now reject all four cube/volume Set/Get windows before dereference. The complete focused families pass 106/106 on Software and Mesa OpenGL33 | Confirmed shared public-XNA safety/conformance gap and repaired, SOFTWARE-184 ✅ |
| CNA lacked a common pre-draw rasterizer-state application boundary | FNA explicitly reapplies RasterizerState in `ApplyState()` before every draw; CNA applied it only at assignment and several draw routes had no common state boundary | SOFTWARE-185 centralized the flush before all 22 native draw submissions. Its original K4 oracle also treated mutation through the bound device getter as legal, but recovered Microsoft XNA later proved every bound state property immutable. SOFTWARE-232 replaces that premise with rejected-mutation checks and explicit new-state assignments while retaining the useful common flush | Plumbing repaired, SOFTWARE-185 ✅; mutable interpretation superseded by SOFTWARE-232 ✅ |
| EasyGL compiled draws ignored post-`Apply` public texture and sampler overrides | Pass-state and sampler tests verified what the effect initially published, but none changed the public slots between `EffectPass.Apply()` and `Draw*`. The draw path rebound the effect runtime's private sampler snapshot | A three-way pixel discriminator assigns a blue 1x1/PointClamp effect sampler, then public red-green 2x1/PointWrap at U=1.25. Before repair the result was blue; a texture-only repair would be green; the corrected path is red. Current device collections now flow through every ordinary compiled draw and override the snapshot. The full enabled family passes 38/38 on Mesa OpenGL33 | Confirmed EasyGL classic-Effect state-authority gap and repaired, SOFTWARE-186 ✅ |
| SpriteBatch did not publish its resolved sampler to public device slot 0 | Renderer-output tests could pass because CNA sent the state directly to the private SpriteBatch renderer. No test inspected `GraphicsDevice.SamplerStates[0]`, and the compiled sprite route had no device sampler source | Explicit seven-property and null-to-LinearClamp transitions both failed before repair and pass on Software. A compiled sprite with red/green/blue mips magnifies its source while requesting `MaxMipLevel=2`: disabling the new forwarding produces red, while the repaired path produces blue. The complete enabled compiled family passes 39/39 | Confirmed shared public-XNA state/publication gap and repaired, SOFTWARE-187 ✅ |
| Non-Immediate SpriteBatch modes applied device state during `Begin()` instead of their flush | Prior tests encoded CNA's eager null-blend transition and never inspected the other three state families before `End()`. Output was correct, hiding the public timing difference | FNA calls `PrepRenderState()` in Immediate Begin but from `FlushBatch()` for every other mode, before even its empty return. A new Deferred empty-batch discriminator failed all four pre-End checks: blend, sampler, depth and rasterizer had already changed. SpriteBatch now retains resolved value copies; old state survives Begin and all four requested values become public at End. Focused units pass 29/29 | Confirmed shared public-XNA state-timing gap and repaired, SOFTWARE-188 ✅ |
| A renderer-private End exception left SpriteBatch permanently inside Begin | Success-path Begin/End misuse tests never injected an exception from `ISpriteBatchRenderer::End`. CNA reset its guard only after that private call returned | A throw-once renderer makes the first End fail, then accepts the next session. The recovery seam remains necessary because a failed CNA renderer End cannot safely retry its native pair. SOFTWARE-351 later disproved the original FNA-led generalization: common deferred state/flush failures must retain the public pair and device accounting | Confirmed CNA renderer-seam lifecycle gap and repaired, SOFTWARE-189 ✅; common-failure claim superseded by SOFTWARE-351 ✅ |
| Software implemented half-float linear filtering but denied the capability | Format and sampler tests exercised the pixels independently from capability reporting, so the inherited conservative default looked like an intentional restriction | A HiDef `HalfVector4` red-blue midpoint draw produced approximately `(128,0,128,255)` before repair, then failed solely on `SupportsCapability(HalfFloatTextureLinearFiltering) == false`. Software now overrides the dedicated probe and the combined output/capability test passes on Software and desktop EasyGL | Confirmed false-negative capability report and repaired, SOFTWARE-190 ✅ |
| Compressed texture region validation overflowed before rejecting malformed coordinates | Happy-path DXT tests exercised block alignment and NPOT tails, while ordinary exception tests could still appear green after undefined signed addition | Maximum X/Y public regions made UBSan stop at `Texture2D.cpp:721` on `2147483647 + 4`. The 2D Set/Get and cube Set routes now reject bounds before deriving right/bottom edge contact. The exact two-test sanitizer reproduction and eight focused DXT regressions pass; the latter also pass on desktop EasyGL | Confirmed shared classic byte-transfer safety gap and repaired, SOFTWARE-191 ✅ |
| `GraphicsDevice.GetBackBufferData` had unsafe malformed-range handling | Pixel/readback tests covered exact output, dimensions and ordinary offsets, but no negative or representational endpoint boundary | A negative one-pixel destination offset was accepted past its own contract check, while maximum X/Y regions made UBSan stop at `GraphicsDevice.cpp:3816` on `2147483647 + 1`. Destination windows now validate in 64 bits and rectangles use subtraction bounds before readback. The three output/format/validation tests pass on ordinary and UBSan Software and desktop EasyGL | Confirmed shared classic-XNA readback safety gap and repaired, SOFTWARE-192 ✅ |
| `SetVertexBuffers` accepted disposed resources | Singular binding and draw-time resource tests did not inspect every entry of the plural binding set, especially a bad secondary stream | Before repair a disposed second buffer raised no exception. SOFTWARE-193 added the required `ObjectDisposedException`; recovered Microsoft flow later proved the old “transactional preservation” interpretation false. SOFTWARE-224 now retains only the valid prefix processed before the disposed slot, while the original same-first-binding case remains green | Confirmed shared classic-XNA lifecycle gap and repaired, SOFTWARE-193 ✅; failure state corrected by SOFTWARE-224 ✅ |
| A disposed `GraphicsDevice` remained publicly usable after native teardown | Existing lifetime tests proved owned resources were released, but did not call renderer-facing methods after disposal or reenter disposal from the event | All thirteen original post-dispose discriminator operations silently returned or changed cached state before repair. A centralized guard now rejects Clear, Present, Reset, state mutations, resource bindings, draw and readback with `ObjectDisposedException`. FNA sets `IsDisposed` before raising `Disposing` and releasing resources; CNA now follows that order, so a callback sees the final state, recursive `Dispose` is harmless, and a currently bound render target is released during owner teardown. Both lifecycle tests and the affected validation/readback slice pass 9/9 on Software and desktop EasyGL | Confirmed shared classic-XNA lifecycle gap and repaired, SOFTWARE-194 ✅ |
| A buffer disposed after binding crashed the next draw | Setter validation covered only resources already disposed at assignment time. Neither the draw nor unchecked `GetRenderer()` reference dereference reconsidered lifetime after the binding was cached | A public vertex-buffer discriminator exited by SIGSEGV 139 immediately after its renderer object was released; the indexed route had the identical unchecked path. All bound vertex streams and the index binding are now checked before range inspection or submission. Both regressions pass on Software and desktop EasyGL, and the 113-test Software draw/range/stream selection retains 111 passes plus two renderer-selector skips | Confirmed shared classic-XNA lifecycle/safety gap and repaired, SOFTWARE-195 ✅ |
| CNA state assignments neither retained properties nor froze the source as Microsoft XNA does | Pixel/state tests compared copied values and trusted FNA's mutable records; the old test deliberately required source mutation to remain legal and invisible | Recovered Microsoft state classes retain the assigned object, reject disposed state on `Apply`, set `isBound`, and make every later property setter throw. SOFTWARE-232 shares the property payload across assignment and freezes all 41 public setters at direct device/collection/SpriteBatch application; SOFTWARE-234 makes later binding observe disposal through any payload alias; SOFTWARE-349 shares observable base-resource metadata, lifetime/events and parent device | Observable conformance repaired, SOFTWARE-232/234/349 ✅; literal wrapper identity remains SOFTWARE-198 ⬜ |
| `OcclusionQuery` encoded FNA's missing validation instead of Microsoft XNA's state machine | The old audit stopped at FNA and explicitly required repeated Begin/End to be harmless; output/count tests only exercised valid cycles | Recovered XNA 4.0 code rejects Reach, unavailable PixelCount, End-before-Begin, nested Begin, repeated End and reuse before IsComplete observation. The new discriminator failed all five lifecycle assertions before repair. Software, desktop EasyGL and GLES3 now pass the shared 3/3 profile/lifecycle/count selection; Software's exact fragment contract passes 44/44, all three EasyGL query programs pass, and the fresh native C-ABI Software smoke covers the same transitions | Confirmed shared classic-XNA profile/lifecycle gap and repaired, SOFTWARE-199 ✅ |
| `VertexBufferBinding` accepted offsets outside the bound buffer | Existing coverage proved null, negative offset/frequency and ordinary property round trips, but never challenged the buffer-relative upper boundary | Recovered Microsoft XNA 4.0 source requires `0 <= VertexOffset < VertexCount`; FNA omits the constructor checks. A safe pre-fix discriminator proved that offsets 16 and 17 were both accepted for a 16-vertex buffer, while 15 remained the last legal value. The shared constructor now enforces the Microsoft bound and all 16 focused checks pass under Software, desktop EasyGL and OpenGLES3 | Confirmed shared classic-XNA validation gap and repaired, SOFTWARE-200 ✅ |
| Default `Viewport` was not XNA's zero-valued struct and `ToString` forced six decimals | Existing tests incorrectly expected `MaxDepth=1` despite naming the case “ZeroFields”; the string test only looked for four integer substrings | Recovered Microsoft XNA 4.0 source has no parameterless constructor, so `default(Viewport).MaxDepth` is zero, while only parameterized construction installs 0..1. Its exact format operation emits ordinary float text rather than `std::to_string` padding. Both discriminators failed before repair; all 24 focused Viewport tests now pass | Confirmed shared classic-XNA public-value gap and repaired, SOFTWARE-201 ✅ |
| `VertexElement.ToString()` followed FNA's double-brace divergence | Four substring assertions never constrained the braces or the extra space after `UsageIndex:` | Recovered Microsoft XNA 4.0 passes escaped braces through `string.Format`, yielding `{Offset:0 Format:Color Usage:TextureCoordinate UsageIndex:1}`. FNA concatenates literal `{{...}}`, and CNA copied it. The exact discriminator failed before repair; the complete VertexElement suite now passes with Microsoft's result | Confirmed shared classic-XNA public-value gap and repaired, SOFTWARE-202 ✅ |
| `DisplayMode` omitted two public XNA members and display strings lost `Single` precision | Existing DisplayMode tests ended at Format/equality and Viewport text used only terminating decimals; constructor/equality helpers also appeared as unmarked XNA API | Recovered Microsoft XNA 4.0 exposes `TitleSafeArea` as the full mode and formats `{Width:... Height:... Format:... AspectRatio:...}` using `Single`'s seven significant digits. `DisplayMode(1920,1080,Color)` now yields aspect `1.777778`; a Viewport with one-third/two-thirds depths yields `0.3333333`/`0.6666667`. Non-XNA public helpers are marked `CNAEXT`; all 44 focused DisplayMode/Viewport checks pass | Confirmed shared classic-XNA public-surface gap and repaired, SOFTWARE-203 ✅ |
| Static and dynamic vertex/index buffers accepted zero capacity | The dedicated empty-data suites did not miss the behavior: they explicitly called it valid and asserted zero logical/native capacity across all four buffer kinds | Every recovered Microsoft XNA 4.0 constructor rejects `count <= 0`. Shared creation now validates before renderer allocation; the native C API documents and enforces the same positive requirement. The rewritten 29-test capacity/transfer slice passed on Software and desktop EasyGL, as did the two pure-C ABI smoke tests. Its separate FNA-derived empty-transfer conclusion was later disproved and repaired by SOFTWARE-290 | Confirmed shared classic-XNA construction gap and repaired, SOFTWARE-204 ✅; transfer clause superseded by SOFTWARE-290 ✅ |
| `VertexDeclaration` accepted structurally impossible layouts | Existing declaration tests exercised legal formats, padding and unusual element order, but omitted every recovered validator rejection; the raw-buffer suite explicitly required a 13-byte stride to work | Microsoft XNA validates four-byte stride/offset alignment, element bounds, usage ordinals, duplicate semantic/index pairs and overlapping byte ranges in both declaration constructors. CNA skipped the validator entirely. The shared constructors now apply the recovered rules, while a legal padded layout replaces the invalid raw fixture. Ten new rejection checks cover all four overloads; the 90-test declaration/layout/buffer selection and both full graphics suites pass on Software and desktop EasyGL | Confirmed shared classic-XNA validation gap and repaired, SOFTWARE-205 ✅ |
| Device/profile-dependent declaration validation was absent | Constructor-only tests could not prove XNA's later bind rules. Before repair, CNA had no declaration `Bind` equivalent: default Reach devices accepted HiDef-only half formats, unknown formats, invalid usage indices, strides above 255 and more than 16 elements | Microsoft XNA validates those constraints when the declaration first binds. CNA now applies the same two-profile rules before static/dynamic VertexBuffer allocation and every explicit-declaration DrawUser staging path, rejects disposed declarations, and preserves only the separately marked empty CNAEXT legacy path. The new matrix plus the corrected validation-order case pass 12/12 on Software and desktop EasyGL; the exhaustive Software format executable passes all 16 probes under HiDef | Confirmed shared classic-XNA profile gap and repaired, SOFTWARE-206 ✅ |
| `VertexBuffer.VertexDeclaration` did not preserve the XNA resource semantics | Output and layout tests compared only copied element/stride values. An isolated Microsoft XNA 4.0 probe proved `ReferenceEquals`, device binding, later Name/Tag mutation, disposal and event sender on the original object; FNA has the same reference assignment and permits ownership to rebind across devices. Microsoft decompilation further shows declaration bindings are released separately at device teardown without disposing the managed declaration | The pre-fix shared test failed the device, metadata, disposal and event observations. SOFTWARE-348 adds an opt-in `GraphicsResource` identity for declaration aliases, preserves it safely as wrappers move or leave scope, and makes the buffer property mutable like the managed object. Five semantic/lifetime/rebind tests pass within a 98-test declaration/buffer slice on both renderers, including non-disposing device teardown and reusable built-in declarations across completed C++ device scopes; full suites pass 2,615/2,678 on Software and 2,609/2,678 on EasyGL GLES. CNA still returns its safe buffer-owned C++ wrapper and wrapper-local event tokens rather than the caller's literal identity because legal stack objects and temporaries cannot be borrowed | Observable shared classic-XNA resource semantics repaired, SOFTWARE-348 ✅; literal `ReferenceEquals`/event-collection identity remains SOFTWARE-207 ⬜ |
| Buffer construction ignored XNA profile byte and index-width limits | Positive-capacity and 16/32-bit behavior tests exercised renderer allocation and transfers, but they neither approached XNA's byte ceilings nor declared HiDef for 32-bit indices | Recovered Microsoft XNA 4.0 profile data sets both buffer maxima to 67,108,863 bytes and permits 32-bit indices only under HiDef. Shared construction now performs overflow-safe byte checks and rejects Reach 32-bit static/dynamic buffers. Three new tests prove every exact boundary and the profile distinction; the 22 affected legacy failures were all corrected to explicitly request the HiDef feature they meant to test. Full suites pass 2,398/2,453 on Software and 2,410/2,469 on desktop EasyGL, with only the previously classified skips | Confirmed shared classic-XNA profile gap and repaired, SOFTWARE-208 ✅ |
| Draw calls ignored XNA primitive/instance ceilings and Reach accepted 32-bit user indices | Existing range tests jumped directly to `INT_MAX` and expected topology overflow, never checking either profile boundary; 32-bit buffer fixtures did not cover the separate user-array gate | Recovered Microsoft XNA 4.0 applies 65,535/1,048,575 Reach/HiDef maxima to all five classic draw families, including `instanceCount`, and checks the user `int[]` index overload against the same HiDef-only capability as an IndexBuffer. A shared validation layer now covers all persistent and typed/raw/declaration user paths. Three tests distinguish the exact legal boundary from its first illegal successor using deliberately tiny downstream ranges, cover both instanced count axes and prove Reach rejects wide user indices. Full suites pass 2,401/2,456 on Software and 2,413/2,472 on desktop EasyGL | Confirmed shared classic-XNA profile gap and repaired, SOFTWARE-209 ✅ |
| Unknown `IndexElementSize` values threw instead of following XNA's enum overload | Normal buffer tests used only the two declared values, so CNA's extra `ArgumentOutOfRangeException` appeared harmless | Recovered Microsoft XNA 4.0 chooses two bytes only for exactly `SixteenBits`; every other enum value takes the four-byte path and its getter reports canonical `ThirtyTwoBits`. Static and dynamic CNA buffers now make the same choice before profile and size validation. The shared regression proves successful HiDef construction/getters and Reach's authoritative 32-bit-profile rejection for value 99 | Confirmed shared classic-XNA validation gap and repaired, SOFTWARE-210 ✅ |
| `Texture2D` ignored XNA's 2048:1 profile aspect-ratio ceiling | Resource-ceiling tests checked only the 2048/4096 per-axis maxima, so a legal per-axis size guaranteed a pass regardless of the other dimension | Recovered Microsoft XNA 4.0 computes the ceiling of larger/smaller and rejects values above `MaxTextureAspectRatio`, set to 2048 in both profiles. Shared validation now performs the equivalent overflow-safe comparison before renderer allocation. The public boundary test accepts 4096×2 in both orientations and rejects both 4096×1 orientations on Software and EasyGL | Confirmed shared classic-XNA profile gap and repaired, SOFTWARE-211 ✅ |

| Blend-state profile restrictions were never enforced | Pixel equations were tested mostly under HiDef or with profile-legal states, while property round trips never bound adversarial combinations | Recovered Microsoft XNA 4.0 validates states on bind: Reach rejects separate alpha factors/functions and destination `SourceAlphaSaturation`; Reach and HiDef reject Min/Max with factors other than One/One. Shared validation now runs before renderer mutation, recognizes adjusted color/alpha factor equivalence, and passes four public two-profile tests plus both full renderer suites | Confirmed shared classic-XNA profile gap and repaired, SOFTWARE-212 ✅ |
| `GraphicsDevice.GetBackBufferData` was incorrectly available in Reach | Existing readback tests either used the default Reach profile without challenging support or focused only on output and malformed ranges | Recovered Microsoft XNA 4.0 advertises no backbuffer readback capability in Reach and throws `NotSupportedException` before null/range validation. CNA now applies that shared profile gate before every overload funnels into backend readback. The two-profile test proves rejection precedence and exact HiDef pixel output; renderer-output fixtures use HiDef explicitly. Full Software and desktop EasyGL suites pass | Confirmed shared classic-XNA profile gap and repaired, SOFTWARE-213 ✅ |
| Texture creation ignored NPOT profile rules and classic DXT alignment | Mip-count tests treated every NPOT texture as legal under the default Reach profile, and transfer tests explicitly constructed 7×5 DXT 2D resources and 7×7 DXT cubes even though no XNA profile permits those dimensions | Recovered Microsoft XNA 4.0 allows only non-mipped, uncompressed NPOT 2D textures in Reach; requires power-of-two Reach cubes; and rejects classic DXT dimensions not divisible by four in either profile. Common Texture2D/TextureCube validation now runs before renderer allocation. The two-profile matrix distinguishes legal conditional NPOT, legal HiDef NPOT mips, Reach rejections and HiDef DXT-alignment `ArgumentException`; valid transfer/mip tests remain green on both renderers | Confirmed shared classic-XNA profile/validation gap and repaired, SOFTWARE-214 ✅ |
| Render targets bypassed their base texture shape/profile limits | Ordinary Texture2D/TextureCube construction and render targets take separate CNA constructors, but the earlier profile tests exercised only the ordinary resource path | Recovered Microsoft XNA 4.0 explicitly calls the corresponding texture validator after render-target format selection. CNA now rejects Reach mipped NPOT 2D targets, Reach NPOT cubes, dimensions over the 2D/cube profile maxima and a HiDef 4096×1 target over the 2048:1 aspect ceiling before renderer allocation. The shared eight-boundary test passes on Software and desktop EasyGL | Confirmed shared classic-XNA profile gap and repaired, SOFTWARE-215 ✅ |
| Preferred render-target formats bypassed XNA adapter/profile substitution | The existing exact-format capability tests deliberately required unsupported construction to throw, and HDR tests used the default Reach profile while constructing HiDef-only float targets | Recovered Microsoft XNA 4.0 queries an applied format first and constructs with that result. The already measured CNA profile table says unsupported preferences substitute Color. Adapter queries now reject HiDef-only formats under Reach; 2D/cube constructors allocate and report the selected format; the exact-support query returns false when the active profile would substitute. Shared tests distinguish Reach `Single` fallback, HiDef exact `Single`, and DXT-to-Color fallback on Software/EasyGL | Confirmed shared classic-XNA query/construction gap and repaired, SOFTWARE-216 ✅ |
| Float/half filtering and render-target blending bypassed XNA draw-time restrictions | Format transfer and pixel tests treated a renderer's physical ability to filter or blend as the XNA contract; one test explicitly required linearly filtered `HalfVector4` and another blended into forbidden `Vector4`. No test paired every format with the state flags Microsoft validates at draw submission | Recovered Microsoft XNA 4.0 identifies seven point-filter-only formats (`Single`, `Vector2`, `Vector4`, `HalfSingle`, `HalfVector2`, `HalfVector4`, `HdrBlendable`) and six non-blendable formats (the same set excluding `HdrBlendable`). Its blend tracker treats any non-opaque factor/function or any of four non-All write masks as requiring blend support. Shared pre-draw validation now covers public compiled-effect slots, stock-effect textures/cubes, all active render targets, and SpriteBatch's bypass path at its immediate/deferred flush time. Six regressions exercise all formats, every mixed filter, blend and mask rejections, both SpriteBatch timings, and prove `HdrBlendable` remains blendable. Full suites pass 2,419/2,474 on Software and 2,431/2,490 on desktop EasyGL | Confirmed shared classic-XNA draw-state gap and repaired, SOFTWARE-217 ✅ |
| Deferred SpriteBatch common failures and renderer End failures were conflated | Earlier exception-safety tests used FNA's early `beginCalled` clear and treated every failure before renderer End as recoverable, leaving public and renderer state out of sync | SOFTWARE-351's Microsoft IL/runtime evidence proves a common deferred state/flush failure retains the public pair, queued work, renderer pair and GraphicsDevice count for a repeated End. Cleanup is now limited to an exception from the CNA-private renderer End seam; that branch remains reusable and its throw-once test passes | Original two-layer bug repaired but lifecycle interpretation corrected, SOFTWARE-218/351 ✅ |
| Reach conditional NPOT textures were allowed to Wrap/Mirror | SOFTWARE-214 restored resource-construction rules but did not follow the resulting legal NPOT texture into Microsoft XNA's separate draw-time `MustClamp` state tracker; happy-path NPOT tests used in-range coordinates or HiDef | Recovered XNA `Texture2D.MustClamp` marks any NPOT texture under a profile without unconditional NPOT support, and `VerifyCanDraw` intersects that flag with non-Clamp U/V sampler state. Shared validation now uses the real 2D dimensions and sampler slot across stock, compiled-effect and SpriteBatch paths. The profile discriminator accepts Reach PointClamp, rejects Wrap-U and Mirror-V independently plus SpriteBatch PointWrap, and accepts HiDef PointWrap. Full suites pass 2,421/2,476 on Software and 2,433/2,492 on desktop EasyGL | Confirmed shared classic-XNA sampler/profile gap and repaired, SOFTWARE-219 ✅ |
| MRT duplicate and compatibility validation contradicted Microsoft XNA | The shared 22-check contract explicitly required two different faces of one cube to bind simultaneously; common validation compared selected subresources, dimensions and applied samples but never pixel size | Recovered Microsoft XNA checks target object identity independently of cube face and throws when one resource appears twice. Its `RenderTargetHelper.IsSameSize` compares width, height, multisample count and pixel size—not exact format. Shared validation now rejects one cube's PositiveX/NegativeX pair and Color/Rgba64, accepts Color/Single, and leaves prior state unchanged. The corrected MRT scene uses two cube resources and declares HiDef. Full suites pass 2,424/2,479 on Software and 2,436/2,495 on desktop EasyGL; both 22-check MRT executables pass | Confirmed shared classic-XNA MRT conformance gap and repaired, SOFTWARE-220 ✅ |
| Render-target binding ignored resource device ownership | Existing disposed/bound-target tests used only resources created by the receiving device; the common binding loop checked null, disposal and concrete target type but never `GraphicsResource.GraphicsDevice` | Microsoft XNA throws `InvalidOperationException` when a target belongs to another device before resolving old targets or mutating state. The pre-fix Software discriminator accepted both foreign 2D and cube targets and replaced a valid local 2D binding. The shared guard now runs before descriptor creation. Both tests pass on Software/EasyGL, including transactional preservation; full suites pass 2,426/2,481 and 2,438/2,497 respectively | Confirmed shared classic-XNA resource-lifecycle gap and repaired, SOFTWARE-221 ✅ |
| Rebinding an identical render-target set was not a no-op | Existing transition coverage proved viewport/scissor reset when moving between a target and backbuffer but never repeated the current set; its prose incorrectly said the reset was unconditional | Recovered Microsoft XNA returns immediately when count, target resources and cube faces all match, including empty-to-empty. Before repair, repeated 2D/cube-face and empty sets erased custom viewport/scissor state. The common early comparison now preserves state and prevents redundant resolve/discard/rebind; a different cube face still resets and updates the public binding. Both unit tests and the corrected transition pixel executable pass on Software/EasyGL; full suites pass 2,428/2,483 and 2,440/2,499 | Confirmed shared classic-XNA binding-state gap and repaired, SOFTWARE-222 ✅ |
| Vertex/index/texture binding ignored resource device ownership | Disposal and render-target ownership tests did not cross devices for ordinary draw resources; all four common setters accepted any live object | Recovered Microsoft XNA rejects foreign vertex/index buffers and textures with `InvalidOperationException`. Before repair, singular and secondary vertex buffers, an index buffer, and both pixel/vertex texture collections accepted the foreign resource and replaced local state. Common validation now runs before mutation; both regressions pass on Software/EasyGL and full suites pass 2,430/2,485 and 2,442/2,501 | Confirmed shared classic-XNA resource-ownership gap and repaired, SOFTWARE-223 ✅ |
| `SetVertexBuffers` encoded FNA's null-slot and transactional semantics instead of Microsoft's | Existing unit/example coverage explicitly accepted 16 null bindings and expected `ArgumentOutOfRangeException` for 17; the disposed-secondary test used the same first binding and could not distinguish a transaction from ordered application | Recovered Microsoft XNA throws `ArgumentException` at a null slot, `NotSupportedException` above 16, keeps only the prefix applied before a later failure, and ignores `vertexOffset` when singular null unbinds. All three pre-fix discriminators failed. The shared setter now reproduces that state machine; both full suites remain green and the corrected EasyGL validation executable passes 5/5 | Confirmed shared classic-XNA validation/state gap and repaired, SOFTWARE-224 ✅ |
| Vertex texture/sampler collections ignored profile, format and device lifetime | Existing default/disposal tests treated every device as sixteen vertex slots and bound Color resources, so they asserted states XNA cannot represent; owned texture collections never checked their parent after disposal | Recovered XNA constructs zero Reach/four HiDef vertex slots, permits seven float/half vertex texture formats, checks device/value/format before index range, and rejects collection texture access after device disposal. Nine pre-fix assertions failed. Shared owned collections now enforce those rules while leaving the no-parent CNA helper at sixteen slots; 19 focused checks and both full suites pass. SOFTWARE-168 subsequently proves real draw-time sampling | Confirmed shared classic-XNA collection and execution gaps repaired, SOFTWARE-225/168 ✅ |
| Viewport/scissor setters had no XNA validation and their getters survived device disposal | Existing coverage proved output mapping, state reset and setter rejection after disposal, but never supplied invalid geometry/depth or read those properties after disposal | Recovered Microsoft XNA requires a viewport with positive dimensions inside the active surface, MinDepth/MaxDepth in ordered 0..1 range, and a nonnegative scissor rectangle inside the same bounds; both getters check device lifetime. Before repair CNA forwarded and cached invalid values. Shared validation is overflow-safe, precedes renderer mutation, uses the first bound target rather than backbuffer bounds, permits XNA's zero-area boundary scissor, and the getters now throw after disposal. Four focused tests pass on both renderers; full suites pass 2,436/2,491 on Software and 2,448/2,507 on EasyGL | Confirmed shared classic-XNA public-state/lifecycle gap and repaired, SOFTWARE-226 ✅ |
| Native-query `DisplayMode` and `GraphicsDeviceStatus` survived device disposal | Lifecycle coverage checked rendering, state setters and viewport/scissor, but not the two remaining properties whose Microsoft implementations query the native device | Recovered Microsoft XNA begins both getters with `CheckDisposed`, unlike its cached adapter/profile/presentation/state properties. CNA now throws `ObjectDisposedException` before its renderer/cache logic. The expanded lifecycle contract and both full suites pass with unchanged counts | Confirmed shared classic-XNA property-lifecycle gap and repaired, SOFTWARE-227 ✅ |
| `GraphicsDevice.Reset` retained active render-target bindings | Reset coverage exercised backbuffer resize/depth/presentation, but never reset while a target was bound | Recovered Microsoft XNA explicitly unbinds all targets before native reset and its saved-state restore does not include them. CNA kept the target vector/flag across renderer reconfiguration, so public state still named the old target and `Present()` remained rejected. Reset now uses the ordinary empty binding transition after `DeviceResetting`, finalizing the old target and restoring backbuffer viewport/scissor before reconfiguration. The new lifecycle test proves empty target state, full-backbuffer viewport and successful Present on both renderers; full suites pass 2,437/2,492 and 2,449/2,508 | Confirmed shared classic-XNA reset/lifecycle gap and repaired, SOFTWARE-228 ✅ |
| Five render targets threw FNA's array-style argument exception instead of XNA's profile failure | The existing cap test explicitly documented and required `std::invalid_argument` from CNA's fixed-size guard | Recovered Microsoft XNA compares against `ProfileCapabilities.MaxRenderTargets` and throws `NotSupportedException`. The common hard ceiling now reports that type, and the corrected test requests HiDef to discriminate its four-target maximum from Reach's lower ceiling. Focused and full suites pass on both renderers | Confirmed shared classic-XNA validation gap and repaired, SOFTWARE-229 ✅ |
| A null member of `SetRenderTargets` leaked `std::invalid_argument` | The default-binding test explicitly required CNA's native exception type | Recovered Microsoft XNA rejects a null target reference with `ArgumentException(NullNotAllowed)`. The shared pre-renderer binding loop and corrected test now use `System::ArgumentException`; full suites remain green on both renderers | Confirmed shared classic-XNA exception gap and repaired, SOFTWARE-230 ✅ |
| Texture/sampler collection indexers leaked `std::out_of_range` | Standalone and device-owned boundary tests asserted only CNA's native exception, including Reach's zero vertex slots and HiDef's four-slot upper edge | Both recovered Microsoft collection indexers throw `ArgumentOutOfRangeException("index")`. CNA now uses the SharpRuntime type from pixel and vertex texture/sampler getters/setters; eight focused cases and both full suites pass on Software/EasyGL | Confirmed shared classic-XNA exception gap and repaired, SOFTWARE-231 ✅ |
| Graphics state objects stayed mutable after binding | The adversarial audit initially stopped at FNA, whose state records remain mutable, and K3/K4 explicitly mutated RasterizerState after device assignment. No test covered the Microsoft `isBound` state machine or disposed state application | Recovered Microsoft `BlendState`, `DepthStencilState`, `RasterizerState` and `SamplerState` call `ThrowIfBound` from every property setter; presets start bound and `Apply` rejects disposal. Shared payload assignment now freezes both the source and cached wrapper. Forty-one assertions cover every property, disposed direct/collection binds are rejected, both full graphics suites pass, and the corrected deferred-scissor executable passes 50/50 on Software and EasyGL | Confirmed shared classic-XNA state/lifecycle gap and repaired, SOFTWARE-232 ✅; observable wrapper resource semantics repaired by SOFTWARE-349 ✅ |
| Deferred SpriteBatch used a stale sampler snapshot | The renderer-private sampler tuple was copied during `Begin`, while the public state payload retained by SOFTWARE-232 correctly remained mutable until a deferred `End`. Existing tests checked only Begin/End publication timing or states that were never changed between those calls | A pre-fix recording renderer retained the old values for Filter, AddressU/V, MaxAnisotropy, MaxMipLevel and LOD bias. A separate Point-sampled red/green texture changed Clamp to Wrap after queuing; both Software and EasyGL produced green instead of the required red. Refreshing the complete tuple from the retained payload at `applyRenderState()` makes both discriminators and the affected 40-test slice pass on both renderers | Confirmed shared classic-XNA state-timing/output gap and repaired, SOFTWARE-233 ✅ |
| State disposal disappeared across assigned wrappers and deferred SpriteBatch retention | SOFTWARE-232 proved only that a disposed wrapper presented directly to a bind was rejected. Assignment still reset the destination wrapper's base `isDisposed_`, and no test disposed a source after device/collection assignment or between deferred `Begin` and `End` | Pre-fix Software accepted a disposed BlendState at Immediate Begin and both an already-disposed and subsequently disposed SamplerState at deferred End. Disposal after direct assignment was also invisible to a later alias bind. All four shared payloads now carry lifetime state and every application checks it; SOFTWARE-349 additionally shares public base `IsDisposed` and delivers disposal to every wrapper-local subscriber. Seven original payload tests plus the new identity matrix pass on both renderers | Confirmed shared classic-XNA lifetime gap and repaired, SOFTWARE-234/349 ✅; literal event-token identity remains SOFTWARE-198 ⬜ |
| State parent-device semantics were overstated as a foreign-device rejection | The first follow-up discriminator expected four custom states to fail on a second GraphicsDevice and failed 0/2, apparently confirming a gap. The prior plan summary said Microsoft's `Apply` rejected a foreign parent, but had not quoted the complete branch | Complete decompiled XNA `Apply` bodies show `_parent != device` assigns the new parent, sets `isBound`, and rebuilds cached native fields; no rejection occurs. Shared tests apply all four families to a second device. SOFTWARE-349 makes that rebind observable through every public alias; SOFTWARE-350 pins that assigning the identity back to a property/slot already caching it is a reference no-op, whereas assigning a different sampler slot invokes `Apply` and rebinds the parent | Hypothesis disproved and parent/reference identity repaired, SOFTWARE-235/349/350 ✅ |
| Live logical backbuffer extent could exceed the stale presentation description during a window change | SOFTWARE-226 tested fixed backbuffers and render targets, but no real-window control combined its new validation with `FixedHeightDynamicWidth` during `GraphicsDeviceManager.CreateDevice` | Under Mesa/Xvfb, a debugger captured `UpdateViewportFromWindow` accepting and pushing a 960x480 logical viewport while the not-yet-reset description remained 800x480, then rejecting its own matching scissor. Backbuffer validation now uses the renderer's live logical extent with a safe description fallback; bound render targets still override it. The previously aborting anisotropy control reaches its assertions and the dedicated EasyGL resize contract passes 8/8 | Confirmed shared Core/presentation-state regression and repaired, SOFTWARE-236 ✅ |
| Negative `SamplerState.MaxAnisotropy` used an intuitive signed clamp rather than Microsoft's unsigned conversion | Existing anisotropy coverage exercised 1, 4, 16, zero and a large positive request, but never a negative `Int32`; property round-trip tests could not reveal the applied native/CPU value | Recovered Microsoft `SamplerState.Apply` converts the signed request to `UInt32` before comparing with the device maximum, making `-1` select the cap. New ordinary BasicEffect and deferred SpriteBatch image checks failed on both renderers before repair. Software and both EasyGL sampler paths now preserve the unsigned conversion; the shared executable passes 17/17 on both and full suites pass 2,453/2,508 Software plus 2,464/2,524 EasyGL | Confirmed shared classic-XNA sampler edge-case gap and repaired, SOFTWARE-237 ✅ |
| Negative `SamplerState.MaxMipLevel` selected mip 0 instead of the last stored level | Earlier mip coverage proved positive, oversized-positive, bias-order and SpriteBatch values, but did not exercise a negative `Int32` or reconcile the recovered DWORD write | Microsoft passes `cachedMaxMipLevel` as `uint` to D3D9. The new one-to-one BasicEffect and deferred SpriteBatch checks both failed on Software/EasyGL before repair. Conversion now occurs at Software's common sampling boundary and before EasyGL's minimum-LOD write, so the resource clamps the wrapped request to its last mip. The corrected HiDef fixture passes 99/99 Software and 93/93 GLES EasyGL; full suites remain 2,453/2,508 and 2,464/2,524 | Confirmed shared classic-XNA sampler edge-case gap and repaired, SOFTWARE-238 ✅ |
| Default stencil masks exposed FNA's `Int32.MaxValue` rather than Microsoft's `-1` | Stencil output consumed only the low eight bits, for which both values are identical; existing public unit tests explicitly pinned FNA's value | Recovered Microsoft `DepthStencilState.SetDefaults` assigns `-1` to both fields and every preset begins from that method. A fresh state plus Default/DepthRead/None discriminator failed 0/3 before the shared payload correction and passes 3/3 on both configurations. Full suites pass 2,454/2,509 Software and 2,465/2,525 EasyGL | Confirmed shared classic-XNA public-value gap and repaired, SOFTWARE-239 ✅ |
| `VertexPositionColor` defaulted its color to opaque white | The renderer consumes packed vertex streams, while the only default-constructor test explicitly required CNA's invented white value; ordinary scenes construct vertices with explicit colors | Microsoft XNA and FNA expose no parameterless constructor on this value type, so CLR default construction zeroes both fields and produces packed-zero transparent black. CNA's explicit default constructor is now defaulted, and the corrected public discriminator requires `Color::Transparent` while retaining zero position. The focused 15-test family passes in both renderer configurations | Confirmed shared classic-XNA public-value gap and repaired, SOFTWARE-240 ✅ |
| Five classic vertex value types returned a constant zero hash | Existing tests checked only that equal objects produced equal hashes, so the FNA TODO looked conformant; no test required field sensitivity or Microsoft's all-zero sentinel | Recovered Microsoft `Helpers.SmartGetHashCode` XORs every complete 32-bit struct word and returns `Int32.MaxValue` when that result is zero. The exact six-test discriminator failed 0/6 before repair on both configurations. CNA now hashes the logical XNA fields rather than its layout-incompatible C++ objects, including exact float words and packed Color; it passes 6/6. Full suites pass 2,460/2,515 Software and 2,471/2,531 EasyGL | Confirmed shared classic-XNA public-value gap and repaired, SOFTWARE-241 ✅ |
| `VertexPositionColor` did not implement `IVertexType` | Existing draw overloads special-case the concrete built-in type and use its static declaration, hiding the missing public interface; layout-test prose incorrectly generalized from the other built-ins | Both recovered Microsoft XNA and FNA declare the interface and return the static declaration from its instance property. A public type-trait discriminator failed before repair. CNA now restores the base relationship and override; the 16-test value/interface family passes on both configurations, while the existing object-to-packed-stream layout/draw controls prove the C++ vtable does not leak into the 16-byte GPU record | Confirmed shared classic-XNA public-interface gap and repaired, SOFTWARE-242 ✅ |
| `SpriteBatch` ordering failures leaked a native exception type | Every guard test expected `std::runtime_error`, so a green suite positively enforced CNA's divergence rather than the XNA contract | Recovered Microsoft XNA and FNA use `InvalidOperationException` for repeated `Begin`, premature `End`, and all classic `Draw`/`DrawString` calls outside a batch. A three-branch exact-type discriminator failed 0/3 before repair. The shared front end now throws `System::InvalidOperationException` from every classic guard, all overload-specific tests require it, and the complete 35-test family passes on both configurations. Full suites pass 2,462/2,517 Software and 2,473/2,533 Mesa EasyGL | Confirmed shared classic-XNA lifecycle/error gap and repaired, SOFTWARE-243 ✅ |
| `GetBackBufferData` bypassed XNA validation and read active targets | Existing pixel tests used exact-sized valid arrays after returning to the backbuffer; the only malformed-range test explicitly expected native exceptions and never tried an oversized count or a bound target | Recovered Microsoft XNA checks destination range first, uses `ArgumentException` for invalid rectangles/total size and prohibits readback while any target is active. Before repair, seven exact assertions all failed on both configurations, oversized data was accepted, and active-target reads returned normally and overwrote the sentinel from the target. The common front end now rejects every representable invalid range before renderer work and preserves the active binding/destination. Full suites pass 2,463/2,518 Software and 2,474/2,534 Mesa EasyGL | Confirmed shared classic-XNA transfer/lifecycle gap and repaired, SOFTWARE-244 ✅ |
| `RenderTargetBinding` omitted XNA's implicit 2D conversion | Explicit construction in every existing MRT test hid the missing operator; the generic `Texture*`/array-slice constructor made the common call sites appear more capable while exposing a non-XNA API without `CNAEXT` | Recovered Microsoft XNA exposes typed 2D/cube constructors and an implicit `RenderTarget2D` conversion. The pre-fix public convertibility discriminator failed. CNA now supplies those typed pointer mappings, routes its singular cube bind through them, and explicitly marks the retained generic constructors/array-slice getter as extensions. Both targets rebuild and the shared binding/validation slice passes 25/25 | Confirmed shared classic-XNA API-surface gap and repaired, SOFTWARE-245 ✅ |
| Texture data transfers ignored resource-in-use state | Happy-path upload/readback tests unbound resources first, so no test attempted mutation from a pixel or vertex sampler or any transfer from an active render target | Recovered Microsoft XNA rejects mutation of sampled 2D/cube/volume textures, permits their readback, and rejects both reads and writes for active 2D/cube targets. Six new discriminators failed 0/6 on each renderer before the common guard was added. They now prove full/partial, cube, volume and active-target boundaries plus unbind recovery; full suites pass 2,470/2,525 Software and 2,481/2,541 Mesa EasyGL | Confirmed shared classic-XNA transfer/lifecycle gap and repaired, SOFTWARE-246 ✅ |
| Vertex/index buffer uploads ignored resource-in-use state | Existing upload/readback tests either wrote before binding or treated mutation of a currently bound static buffer/dynamic `None` upload as legitimate, so passing tests preserved the divergence | Recovered Microsoft XNA rejects ordinary nonempty writes to every currently bound vertex buffer and the current index buffer, permits bound readback, and exempts dynamic `Discard`/`NoOverwrite`. Four new discriminators failed 0/4 on both renderers before the common guards. They now prove secondary-slot coverage, data preservation and unbind recovery; ten nonconforming fixtures were corrected. Full suites pass 2,474/2,529 Software and 2,485/2,545 Mesa EasyGL | Confirmed shared classic-XNA transfer/lifecycle gap and repaired, SOFTWARE-247 ✅ |
| `IndexBuffer` omitted generic/classic window transfers and misinterpreted readback `startIndex` | Earlier tests used only unsigned 16/32-bit pointers; the sole slice executable expected `GetData(data, 1, 2)` to read buffer elements 1..2 into destination zero, exactly reversing XNA's destination-array meaning | Recovered Microsoft exposes all three generic set/get forms and two dynamic options-bearing forms. Four IndexBuffer calls in a direct compile probe failed before repair. New shared tests prove signed types, byte-sized elements, unaligned buffer offsets, destination sentinels, inherited dynamic uploads, `NoOverwrite` windows and bound rejection. The corrected public executable passes 20/20 on both renderers; full suites pass 2,478/2,533 Software and 2,489/2,549 Mesa EasyGL | Confirmed shared classic-XNA API/transfer gap and repaired, SOFTWARE-248 ✅ |
| `VertexBuffer` omitted generic slices/windows and inverted its readback index/stride semantics | Built-in overloads and raw extension helpers made ordinary uploads look complete. Existing slice assertions positively required `GetData(data, 1, 2)` to read buffer vertices 1..2 into destination zero, and no test used a compact custom element in a padded buffer slot | Recovered Microsoft exposes all three generic set/get forms plus both dynamic option forms. Four static calls failed a direct compile discriminator. New shared tests prove a source slice and untouched destination prefix, 8-byte application elements placed in 12-byte buffer slots, a bound dynamic `NoOverwrite` window, and compact `VertexPositionColor` packing. The public EasyGL executable remains 20/20 after correcting its four classic assertions; full suites pass 2,482/2,537 Software and 2,493/2,553 Mesa EasyGL | Confirmed shared classic-XNA API/transfer gap and repaired, SOFTWARE-249 ✅ |
| Buffer `CopyData` coupled transfer type to index width and leaked the wrong resource-size exception | Width-matched happy paths made the draw interpretation and generic transfer element size appear to be the same concept. Every oversized fixture explicitly expected `ArgumentOutOfRangeException`, so green tests preserved a second divergence | Recovered Microsoft copies `sizeof(T)` bytes regardless of `IndexElementSize` and reports `InvalidOperationException(ResourceDataMustBeCorrectSize)` when a vertex/index transfer exceeds native storage. Four new tests failed 0/4 before repair, covering both cross-width directions, dynamic options, set/get exception type and failure preservation. The full 41-test buffer family and 20/20 public executable pass on both renderers; full suites pass 2,486/2,541 Software and 2,497/2,557 Mesa EasyGL | Confirmed shared classic-XNA transfer/error gap and repaired, SOFTWARE-250 ✅ |
| EasyGL `SpriteBatch` let its 16-bit indices wrap in large same-texture batches | Small overload/sorting/state scenes flushed far below 65,536 vertices, and texture changes caused incidental flushes. No test crossed the first `uint16_t` vertex-base overflow while keeping the source constant | Microsoft XNA and FNA both submit at most 2,048 sprites per native batch while retaining larger public sorting queues. A new public test makes sprites 1..16,384 offscreen and sprite 16,385 the only visible output; pre-fix Software passed while EasyGL returned an untouched target. EasyGL now chunks at 2,048 and preallocates the compiled-effect sprite buffers for the same fixed maximum. The test passes on Software, OpenGL ES EasyGL and compiled-effect desktop EasyGL; full suites pass 2,486/2,541 Software and 2,497/2,557 Mesa EasyGL | Confirmed classic-XNA EasyGL/conformance gap and repaired, SOFTWARE-251 ✅ |
| EasyGL turned `SpriteSortMode::Immediate` back into renderer-private deferred work | Shared recording tests stopped at the `ISpriteBatchRenderer::Draw` call and therefore proved only front-end timing. Output scenes read after `End` without changing targets, where EasyGL's late flush looked identical | FNA prepares state at Immediate `Begin`, draws from every public `Draw`, and does no rendering at `End`; the shared hook already publishes that mode. A new exact test switches from target A to B after `Draw` but before `End`: pre-fix EasyGL moved the red pixel into B while Software left it in A. EasyGL now consumes the hook and flushes before each Immediate Draw returns. Software, OpenGL ES and compiled-effect desktop EasyGL all pass | Confirmed classic-XNA EasyGL timing gap and repaired, SOFTWARE-252 ✅ |
| Classic draw validation used native exceptions and the wrong precedence | Existing tests checked isolated invalid inputs only after applying effects/binding buffers, or explicitly expected `std::runtime_error`. The EasyGL missing-buffer executables supplied invalid `numVertices`, installed no shader, and caught the base native type, so they could pass without ever reaching the state they claimed to test | Recovered Microsoft XNA validates buffered counts/profile limits before `VerifyCanDraw`, then reports shader before index/vertex data with `InvalidOperationException`; user arrays validate null/range first, except the public UInt32 overload rejects Reach first. Two new tests failed 0/2 before repair with ten wrong family assertions. All buffered and user-array routes now share the recovered order/type. A third precedence test covers exact parameter/null/profile edges; the focused 38-test slice and corrected public 3/3 + 2/2 executables pass on both renderers. Full suites pass 2,489/2,544 with 55 classified skips on Software and 2,500/2,560 with 60 on EasyGL | Confirmed shared classic-XNA validation/error gap and repaired, SOFTWARE-253 ✅ |
| Model collection string indexers leaked native errors and accepted empty names | Existing tests explicitly expected `std::out_of_range` for missing bones/meshes and never asked either `TryGetValue` overload about an empty name, so the wrong public contract was preserved as success | Recovered Microsoft source throws `ArgumentNullException("boneName"/"meshName")` before writing the out value when the name is null or empty, while the name indexer throws `System.Collections.Generic.KeyNotFoundException` for an ordinary miss. Six focused tests failed before repair across empty/populated bone and mesh collections. The repaired 26-test family passes on both renderers; full suites pass 2,491/2,546 Software and 2,502/2,562 Mesa EasyGL | Confirmed shared classic-XNA collection/error gap and repaired, SOFTWARE-254 ✅ |
| Effect collection integer indexers could not express Microsoft's null result | FNA indexes its backing lists directly, and CNA copied that behavior into reference-returning operators; existing tests required `std::out_of_range`, so the green suite actively preserved both the wrong behavior and an incompatible public return type | Recovered Microsoft source explicitly range-checks all four annotation/parameter/pass/technique integer indexers and returns null for negative and upper-invalid indices. The indexers now return stable mutable/const pointers or null, with both directions tested in the 59-test shared collection slice. The dependency-enabled desktop EasyGL reflection contract proves invalid indices on a real parsed Effect graph and the current-technique executable passes 7/7. Full suites pass 2,491/2,546 Software and 2,502/2,562 Mesa EasyGL | Confirmed shared classic-XNA Effect reflection gap and repaired, SOFTWARE-255 ✅ |
| `GetParameterBySemantic` incorrectly required exact case | FNA uses exact `string.Equals`, and CNA mirrored it; a dedicated existing test required lowercase `projection` to miss uppercase `PROJECTION`, so green tests actively pinned the lower-authority divergence | Recovered Microsoft source uses `string.Compare(..., StringComparison.OrdinalIgnoreCase)`. The replacement mutable/const and mixed-case duplicate discriminators failed 0/2 before repair. Both overloads now use SharpRuntime's ordinal-ignore-case comparison, the 21-test collection family passes on both renderers, and a dependency-enabled EasyGL reflection test resolves lowercase `scalar` against parsed `SCALAR` | Confirmed shared classic-XNA Effect semantic-lookup gap and repaired, SOFTWARE-256 ✅ |
| `Effect.CurrentTechnique` accepted null, foreign and post-disposal assignments | FNA performs no managed validation and CNA copied that lower-authority behavior; two existing tests explicitly required null and an ownerless technique to become current, while no test exercised the setter after disposal | Recovered Microsoft source calls `CheckDisposed` before comparison, throws `ArgumentNullException("value")` for null, and compares the technique's parent with the receiving Effect before throwing `InvalidOperationException`. Three replacement tests failed 0/3 before repair, including same-value assignment after disposal. The shared 35-test Effect slice passes on Software/OPENGLES3 with one classified compiled-effect skip, desktop compiled EasyGL passes 35/35, its authentic current-technique executable passes 7/7, and full suites remain 2,491/2,546 Software plus 2,502/2,562 EasyGL | Confirmed shared classic-XNA Effect lifecycle/ownership gap and repaired, SOFTWARE-257 ✅ |
| `EffectPass.Apply` reported membership failure before disposal | Existing disposal coverage applied only the current pass, while the non-current test used only a live Effect; each isolated check therefore passed despite the reversed combined-state precedence | Recovered Microsoft source checks the Effect lifetime before comparing the pass's technique with `CurrentTechnique`. A new disposed-plus-non-current discriminator failed before repair with `InvalidOperationException` instead of `ObjectDisposedException`. `EffectPass` now performs the recovered disposal guard first; the 36-test Effect/apply slice passes on Software and OPENGLES3 with one classified compiled-effect skip. Full suites pass 2,492/2,547 Software and 2,503/2,563 EasyGL | Confirmed shared classic-XNA Effect lifecycle/error gap and repaired, SOFTWARE-258 ✅ |
| Disposed Effects remained cloneable | Existing coverage explicitly described post-disposal cloning as a CNA safety improvement, while all six stock overrides constructed from the source device and never reached the base clone constructor | Recovered Microsoft `Effect(Effect cloneSource)` checks disposal before cloning; `Effect.Clone`, all stock effect clones and EffectMaterial construction transitively use that contract. Two replacement cases failed 0/2 before repair. The shared guard now covers base/custom clone constructors and virtual cloning, with explicit assertions for all eight classic public Effect families. The focused slice passes 27 tests with six compiled-effect skips on both renderers, the desktop compiled clone/material slice passes 9/9, and full suites pass 2,493/2,548 Software plus 2,504/2,564 Mesa EasyGL | Confirmed shared classic-XNA Effect lifecycle gap and repaired, SOFTWARE-259 ✅ |
| `EffectMaterial.Clone` preserved the derived runtime type | A prior remediation explicitly accepted CNA's override as an improvement and the only test required `dynamic_cast<EffectMaterial*>` to succeed, despite recording that FNA would return base Effect | Recovered Microsoft XNA independently agrees with FNA: EffectMaterial declares no override and inherited `Effect.Clone` constructs a base Effect. Replacement noncompiled and authentic compiled-effect tests failed 0/1 on Software plus 0/2 on desktop EasyGL before repair. Removing the override restores the public runtime type while the compiled discriminator proves independent parameters, techniques and current selection survive through the base clone. The repaired slice passes 3 plus one capability skip on Software and 4/4 on dependency-enabled EasyGL; full Software passes 2,493/2,549 with 56 classified skips and generic Mesa EasyGL passes 2,504/2,565 with 61 | Confirmed shared classic-XNA Effect runtime-type gap and repaired, SOFTWARE-260 ✅; supersedes REMED-NA-007 |
| All nine EffectParameter array getters accepted non-positive counts | Happy-path coverage used only positive counts; the sole negative-count test explicitly required empty results for Int32 and Single, and no test combined a bad count with a type mismatch | Recovered Microsoft source starts every Boolean/Int32/Single/Matrix/MatrixTranspose/Quaternion/Vector2/Vector3/Vector4 array getter with the same `count <= 0` `ArgumentOutOfRangeException` guard. The replacement test failed 19 assertions before repair, including error precedence over object/numeric validation. A shared first-step guard now covers all nine; the complete 55-test family passes on both Software and EasyGL, and full suites pass 2,493/2,549 Software plus 2,504/2,565 Mesa EasyGL | Confirmed shared classic-XNA Effect reflection/validation gap and repaired, SOFTWARE-261 ✅ |
| EnvironmentMapEffect and SkinnedEffect validation exposed generic C++ exception types | Existing tests deliberately accepted `std::runtime_error`, `std::out_of_range` and `std::invalid_argument`; a scan of all six classic stock-effect sources found these were the final six generic throw sites | Recovered Microsoft IL and FNA agree on the precise contract: required-lighting setters use `NotSupportedException`; invalid skin weights and bone counts use named `ArgumentOutOfRangeException`; an empty C++ bone vector maps the managed null-or-empty check to named `ArgumentNullException`; and oversized palettes use plain `ArgumentException`. Nine exact-type/parameter-name checks failed 0/9 before repair and now pass 9/9; the full two-effect property slice passes 102/102 on both Software and Mesa EasyGL, while full Software remains 2,493/2,549 with 56 classified skips | Confirmed shared classic-XNA stock-effect error-semantics gap and repaired, SOFTWARE-262 ✅ |
| `Texture3D` accepted non-exact transfer sizes and exposed native argument exceptions | Existing tests required only that `elementCount` cover the box, explicitly allowing surplus source/destination elements, and expected `std::invalid_argument`/`std::out_of_range`; no test checked that rejected transfers preserved both sides | Recovered Microsoft `CopyData<T>` checks managed copy indices, validates the box, and compares the exact format-byte total against `sizeof(T) * elementCount`; either smaller or larger totals throw plain `ArgumentException`. Its null/count/box paths use named `System::Argument*Exception` types. Seventeen tests failed 0/17 before repair. Both renderers now reject mismatches before storage/readback, with exact concrete types and parameter names; the 51-case Texture3D/binding slice passes 50 plus the one inverse-capability skip on each renderer, and full suites pass 2,493/2,549 Software plus 2,504/2,565 Mesa EasyGL | Confirmed shared classic-XNA transfer/error gap and repaired, SOFTWARE-263 ✅ |
| Reflected compiled Effect parameters accepted texture access through incompatible declared types | Existing parameter coverage exercised successful round trips and object-versus-numeric rejection but never crossed a texture accessor with a scalar or a dimension-specific texture with another dimension getter | Recovered Microsoft IL checks the declared parameter type before native access: each dimension getter accepts generic `Texture` or its exact dimension, while the base texture setter accepts only the five texture types. A desktop EasyGL test using authentic `BasicEffect.fxb` plus CNA's compiler-produced generic-texture fixture failed six assertions before repair. The shared check now throws `InvalidCastException` on every invalid pairing; the generic texture remains legal through all three getters, and the 55 standalone tests prove the C API compatibility carve-out remains intact. The public compiled slice passes 82/82 on desktop EasyGL; full suites pass 2,493/2,550 Software plus 2,504/2,566 generic Mesa EasyGL | Confirmed shared classic-XNA compiled Effect validation gap and repaired, SOFTWARE-264 ✅; Software still skips execution because the separately documented compiled-Effect subsystem is absent |
| Shared compiled-Effect conformance ran a HiDef-only state on default Reach devices | Feature-specific EasyGL tests passed and the broader renderer executable was not rerun after profile ceilings became honest; its shared contract stopped at separate-alpha validation before exercising backend behavior | The render-state section deliberately publishes color `ReverseSubtract` plus alpha `Add`; XNA Reach forbids that combination. The full EasyGL family reproduced the dormant failure at 13/14. All four backend entry points now create HiDef devices, and the shared helper asserts that prerequisite. The failed case passes and the full dependency-enabled EasyGL compiled runtime family is 14/14 | Confirmed test-evidence integrity gap and repaired, SOFTWARE-265 ✅; no product behavior change |
| Compiled Effect texture assignment ignored resource lifetime and active render-target use | Existing tests covered reflected texture types but supplied only live, resolved textures; no case combined a bad resource with an incompatible reflected type to establish validation order | Recovered Microsoft IL checks disposal and `isActiveRenderTarget` before its reflected-type switch. Desktop EasyGL accepted a disposed texture and a bound RenderTarget2D through the authentic compiled Texture parameter, while the scalar parameter reached `InvalidCastException` first; all four assertions failed before repair. The shared compiled path now reports `ObjectDisposedException` or `InvalidOperationException` before type validation, accepts the render target after unbinding, and leaves standalone C API parameters unchanged. The focused EasyGL slice passes 57/57 and its complete public Effect/EffectParameter family passes 83/83; Software passes the 55 renderer-neutral cases and skips exactly the two compiled-only discriminators through `CompiledEffects=false` | Confirmed shared classic-XNA compiled Effect lifecycle/validation-order gap and repaired, SOFTWARE-266 ✅; Software still lacks the separately documented compiled execution subsystem |
| Compiled typed EffectParameter setters ignored reflected class, dimensions and array shape | The shared contract exercised only matching typed assignments; the public suite had no cross-shape cases, and the synthetic fixture incorrectly encoded ordinary structure members as one-element arrays | Recovered Microsoft IL gives each Matrix/Vector/Quaternion overload an explicit shape matrix and throws `InvalidCastException` before native mutation. A real fxc-produced conformance Effect failed all ten negative discriminator assertions before repair. Compiled setters now enforce non-array Matrix, exact-width non-array Vector, Vector-array and bounded Matrix-array targets. The hand-built fixture now uses the same zero-element encoding for ordinary structure members as the committed fxc binary and recorded FNA reflection, with an explicit 0/0/2 member-elements contract. Desktop EasyGL passes 84/84 public Effect/EffectParameter and 14/14 backend compiled-Effect tests; Software retains three exact compiled capability skips and passes all 55 standalone cases | Confirmed shared classic-XNA compiled Effect validation gap and repaired, SOFTWARE-267 ✅; standalone CNA/C API parameters deliberately retain their existing leniency |
| Compiled scalar EffectParameter setters changed only the first component and accepted array parents | Existing scalar tests used standalone parameters, and compiled tests assigned scalar values only to scalar parameters; neither exposed reflected vector/matrix broadcast or the scalar-versus-array boundary | Recovered Microsoft bool/int/float setter IL rejects any parameter whose element collection is nonempty, converts the source to the reflected scalar type, and asks the Effect Framework to write the value across a Scalar, Vector or Matrix target. The compiler-produced conformance Effect failed seven assertions before repair: three full Vector4 values, one full 4x4 Matrix and three required `InvalidCastException` results. The shared compiled path now validates class/type/shape and writes every component using the reflected register layout. Desktop EasyGL passes 85/85 public Effect/EffectParameter and 14/14 backend compiled-Effect tests; Software passes 80/85 with five exact compiled-capability skips | Confirmed shared classic-XNA compiled Effect value/validation gap and repaired, SOFTWARE-268 ✅; Software still cannot execute the separately documented compiled-Effect subsystem |
| Compiled numeric-array setters accepted Structure parameters | The shared numeric guard intentionally allowed Structure so callers could reach its members, but no test distinguished member access from assigning an array to the structure root itself | Recovered Microsoft bool/int/float array setter IL checks the root class first and admits only Scalar, Vector and Matrix. All three overloads silently wrote the compiler-produced `Lighting` structure before repair and now throw `InvalidCastException`; valid numeric arrays and standalone parameters remain unchanged. Desktop EasyGL passes 86/86 public Effect/EffectParameter and 14/14 backend tests; Software passes 80/86 with six exact compiled-capability skips | Confirmed shared classic-XNA compiled Effect class-validation gap and repaired, SOFTWARE-269 ✅ |
| Compiled typed getters reinterpreted scalar bits and ignored reflected aggregate shapes | Existing coverage round-tripped matching setter/getter types. It also explicitly required a reflected `float4` to succeed through `GetValueVector2/3`, contradicting Microsoft, and never tested scalar broadcast or int-to-float reads | Recovered Microsoft IL uses destination-typed Effect Framework getters, broadcasts Scalar values into every requested vector/matrix component, requires exact non-array Vector widths, rejects incompatible aggregate classes, and reserves Matrix-array getters for Matrix arrays. A compiler-produced conformance Effect plus authentic BasicEffect failed 16 assertions before repair. The shared compiled layer now performs reflected storage conversion, scalar broadcast and shape validation, and the lower-authority conformance assertion was corrected. Desktop EasyGL passes 87/87 public Effect/EffectParameter and 14/14 backend tests; Software passes 80/87 with seven exact compiled-capability skips | Confirmed shared classic-XNA compiled Effect getter gap and repaired, SOFTWARE-270 ✅ |
| Compiled array getters returned fewer than `count` values and repacked cross-width vectors incorrectly | Existing tests requested no more values than the parameter held and matched each requested vector width to the reflected width, hiding both truncation and logical-stream packing | Recovered Microsoft IL allocates exactly `count` results, reads only the reflected byte capacity and leaves the remainder at managed zero defaults. Vector/Quaternion arrays copy one flat scalar stream into the requested result structs; Matrix and transpose arrays likewise retain zero matrices beyond the reflected array. The new compiled discriminator failed on its first length assertion before repair and now spans all nine array getter families, scalar type conversion, `float4`→Vector2/3 packing and stock Bones matrix-array padding. Desktop EasyGL passes 88/88 public Effect/EffectParameter and 14/14 backend tests; Software passes 80/88 with eight exact compiled-capability skips | Confirmed shared classic-XNA compiled Effect array-getter gap and repaired, SOFTWARE-271 ✅ |
| Compiled numeric-array setters copied source-type bits instead of converting to the reflected type | Matching-type tests hid the bug because the source and destination representations agreed; no compiled test assigned bool/int arrays to Single or a float array to Int32 | Recovered Microsoft IL selects destination-typed native array entry points from the reflected parameter type. The new compiler-produced/stock discriminator failed all four checks before repair: Single scalar/vector storage contained int/bool bit patterns, and stock BasicEffect's Int32 `ShaderIndex` contained the float bit pattern. Each compiled bool/int/float array cell is now converted to reflected Bool, Int32 or Single storage before the dirty flag is published. Desktop EasyGL passes 89/89 public Effect/EffectParameter and 14/14 backend tests; Software passes 80/89 with nine exact compiled-capability skips | Confirmed shared classic-XNA compiled Effect storage-conversion gap and repaired, SOFTWARE-272 ✅ |
| `Texture3D` mip-chain length ignored a depth larger than width/height | Existing mip tests used cubes or width-dominant volumes, and one shared assertion explicitly encoded FNA's two-dimensional calculation, so public and native counts agreed while both were wrong for depth-dominant volumes | Recovered Microsoft XNA creates mipmapped D3D9 volume textures with `Levels=0`, which allocates the complete chain through the largest of width, height and depth. `1x1x8` and `1x2x7` failed as 1/2 levels instead of 4/3 before repair. The shared API plus Software, EasyGL and every other real CNA volume allocator now use the three-dimensional rule; a `1x1x8` upload/readback at level 3 proves native storage exists. The focused shared slice passes 46/46 on Software, EasyGL GLES and desktop EasyGL; full Software and EasyGL GLES graphics suites remain green | Confirmed shared classic-XNA Texture3D gap and repaired, SOFTWARE-273 ✅ |
| `Texture3DReader` discarded authored formats and rejected depth-only mips | Its only positive fixture was Color and its negative test explicitly treated legal `Bgr565` as unsupported. The canonical validator still assumed four bytes per voxel, admitted forbidden DXT volumes and inherited the stale width/height-only mip count; the reader then converted accepted input to RGBA8 and reported `SurfaceFormat::Color` | FNA's reader constructs the serialized format and forwards every exact `byte[]` to `Texture3D.SetData<byte>`. The new shared reader contract exercises all 15 legal HiDef formats with distinguishing raw bytes, rejects DXT1/3/5, and reads all four levels of `Alpha8 1x1x8`. Software, EasyGL GLES and desktop EasyGL pass the focused seven-test family | Confirmed renderer-wide classic-XNA content gap and repaired, SOFTWARE-274 ✅ |
| `Texture2DReader`/`TextureCubeReader` rejected or RGBA8-converted legal authored formats | The positive content corpus covered Color, normalized 2D and one real DXT1 cube. Its 2D negative test explicitly classified legal `Bgr565` as unsupported, while cube conversion made non-Color formats appear loadable only after losing their declared representation. Three normalized-format tests also still skipped Software after its storage gained support | FNA constructs the serialized format and forwards exact `byte[]` payloads except for its explicit backend-dependent DXT fallback. New matrices retain distinguishing bytes for all 17 uncompressed 2D formats, all 15 legal uncompressed cube formats on six faces and exact DXT1/3/5 blocks; the cube matrix separately rejects XNA-invalid `NormalizedByte2/4`. Public byte transfers prove caller windows and DXT storage on Software, EasyGL GLES and desktop EasyGL. The focused selections pass 3/3 and 7/7, while the broader families pass 118/118 graphics and 38/38 content on all three renderers with the stale skips removed | Confirmed renderer-wide classic-XNA content/public-transfer gap and repaired, SOFTWARE-275 ✅ |
| `Texture2D.SetData<T>`/`GetData<T>` was neither generic nor byte-width compatible | Existing format tests exercised only CNA's fixed type/format pairs and explicitly allowed surplus `elementCount`; no compile discriminator used an application value type or composed one texel from several narrower elements | Recovered Microsoft `GetAndValidateSizes<T>` permits every value type whose size divides the format width, while `ValidateTotalSize` requires exact total bytes. A custom four-byte POD and `float[4]` over one Vector4 texel failed to compile/run before repair. Shared tests now cover custom source/destination windows, cross-width scalar floats, too-wide types and both total-size directions; the relevant Software selection passes 113/113 | Confirmed shared classic-XNA public-transfer gap and repaired for Texture2D, SOFTWARE-276 ✅; dimension-specific follow-ups are SOFTWARE-277/278 ✅ |
| `TextureCube.SetData<T>`/`GetData<T>` restricted types, accepted surplus totals and semantically converted raw data | The public templates admitted only CNA packed and float/vector types. Color and byte overloads accepted extra elements; Color on a non-Color layout used RGBA conversion, and DXT returned decompressed Color pixels despite Microsoft's generic transfer contract | Recovered Microsoft `TextureCube.CopyData<T>` calls `GetAndValidateSizes<T>` and exact `ValidateTotalSize`; its DXT format-size oracle is one byte, so compressed transfers reject wider element types. New shared tests exercise an application POD through a face/mip/rectangle and caller windows, `float[4]` plus `Color[4]` over one Vector4 texel, both total-size directions and the DXT byte-only rule. Full Software and both EasyGL graphics suites pass; TextureCube is 69/69 on each EasyGL profile | Confirmed shared classic-XNA public-transfer gap and repaired, SOFTWARE-277 ✅ |
| `Texture3D.SetData<T>`/`GetData<T>` rejected application value types at compile time | Exact byte totals and all fifteen declared formats were already tested, but every fixture used CNA's fixed packed/float/byte concepts, so the generic C# API shape remained narrower in C++ | Recovered Microsoft exposes `T : struct` and its volume-specific `GetAndValidateSizes<T>` accepts any element width no larger than and evenly dividing the format width. Three new shared discriminators compile a custom POD, round-trip a whole volume and a mip box through nonzero caller windows, reconstruct the surrounding level and reject an eight-byte element on Color. The 52-test Software volume slice and all three focused EasyGL checks pass | Confirmed shared classic-XNA public-transfer gap and repaired, SOFTWARE-278 ✅ |
| `Texture2D` transfer validation leaked native C++ exceptions and could derive an invalid byte pointer first | Earlier tests asserted broad `std::invalid_argument`/`std::out_of_range` bases, so they stayed green while every public overload disagreed with XNA's exception identity and parameter name. The uncompressed byte path also evaluated `data + startIndex` before its downstream null/index guards | Recovered Microsoft `CopyData<T>` and `Helpers.ValidateCopyParameters` establish `ArgumentNullException("data")`, `ArgumentOutOfRangeException("dataIndex"/"elementCount")` and `ArgumentException("rect")`. Thirteen exact-type checks failed 13/13 before repair, spanning Color, packed, arbitrary POD, byte, both directions, invalid regions and null precedence. The focused Software selection passes 48/48 and the full suite passes 2,514 with 64 classified skips; both EasyGL profiles pass the shared proof | Confirmed shared classic-XNA public-transfer/error-semantics gap and repaired, SOFTWARE-279 ✅ |
| `Texture2D.CopyData<T>` validation precedence and invalid mip failures disagreed with Microsoft XNA | The earlier mip test explicitly required `dataIndex` to precede an invalid level, while packed/generic routes checked the rectangle before element-width compatibility. Sampled-resource rejection also occurred only in the downstream raw copy, after public validation. Invalid levels were reported as native `std::out_of_range` | Recovered Microsoft IL fixes the order as disposed, null/empty data, active render target, sampled texture for writes, D3D9 `GetLevelDesc`, copy window, element width, rectangle and exact total. D3D9's invalid-level `D3DERR_INVALIDCALL` reaches XNA's framework fallback as `InvalidOperationException`. Mixed-invalid discriminators prove resource use wins over bad level/window, bad level wins over window, window wins over element/rect, and element width wins over rect. Fifteen focused checks pass on Software, EasyGL GLES and desktop EasyGL; the 106-check Software render-target integration test passes; full graphics pass 2,517/2,581, 2,528/2,597 and 2,529/2,597 respectively | Confirmed shared classic-XNA lifecycle/error-order gap and repaired, SOFTWARE-280 ✅ |
| Standalone Software renderer tests silently depended on HiDef behavior while constructing the XNA-default Reach profile | The earlier campaign reported green shared graphics binaries but did not rerun the complete renderer CTest label after `SOFTWARE-213` made `GetBackBufferData` correctly HiDef-only. The adversarial run passed 70/158 and failed 88; most aborted at their observation readback, while others reached 32-bit-index, MRT or NPOT-mip profile gates | All 88 failures were mapped to source. Eighty-one positive fixtures now explicitly request HiDef and seven dual-profile contracts enable their intended profile; intentional Reach-negative coverage remains Reach. Newly reachable stale contracts were reconciled against XNA/FNA evidence, and the sole product defect became `SOFTWARE-282`. The complete Software label passes 158/158; full graphics pass 2,520/2,584 on Software, 2,531/2,600 on EasyGL GLES and 2,532/2,600 on desktop EasyGL | Confirmed standalone regression-corpus gap and repaired without weakening profile gates, SOFTWARE-281 ✅ |
| `TextureCube.CopyData<T>` validation identity and precedence diverged across overloads and from Microsoft XNA | The unit corpus asserted native `std::invalid_argument`/`std::out_of_range`, checked failures mostly in isolation and left the standalone cube-target invalid-mip path unreachable until the profile audit. Invalid faces were validated first even though Microsoft does not consume the face until the later native surface call | Recovered Microsoft IL establishes disposed → null → active/sampled resource → mip → copy window → element width → rectangle → exact total → native face use. The shared Core prelude now gives Color, byte, packed, float/vector, arbitrary value types and DXT bytes the same order and System exception identities in both directions. Ten route/direction null+mip discriminators plus mixed-invalid, disposed, sampled, active-target, invalid-rectangle/total/face checks pass 83/83 on Software, EasyGL GLES and desktop EasyGL; Software's full graphics suite passes 2,520/2,584 with 64 classified skips and the cube-target contract passes 55/55 | Confirmed shared classic-XNA public-transfer/lifecycle gap and repaired, SOFTWARE-282 ✅ |
| `Texture3D.CopyData<T>` retained native exception identities and route-dependent validation precedence | Existing tests mostly isolated one bad argument at a time and explicitly expected `std::out_of_range` for invalid levels. Template routes checked null before disposal and did not reject a sampled write until their downstream byte-copy helper; Color checked box validity before element width | Recovered Microsoft IL establishes disposed → null/empty data → sampled resource on writes → native mip description → copy window → element width → box → exact total. A shared validator now covers Color, byte, packed, float/vector and arbitrary POD Set/Get routes. Ten route/direction mip discriminators plus mixed-invalid resource/window/width/box and disposed+null tests pass 52/52 on Software, EasyGL GLES and desktop EasyGL; full graphics pass 2,523/2,587, 2,534/2,603 and 2,535/2,603 respectively | Confirmed shared classic-XNA public-transfer/lifecycle gap and repaired, SOFTWARE-283 ✅ |
| Standalone EasyGL tests silently depended on HiDef and on unavailable depth storage | The earlier parity campaign exercised shared binaries but did not run the complete 336-executable EasyGL renderer corpus. The adversarial serial baseline passed 143 and failed 193: most failures stopped at Reach profile gates, while depth fixtures could report success without a depth buffer and several transfer tests encoded contracts superseded by recovered Microsoft evidence | All failures were mapped to source. Positive fixtures now request HiDef, depth tests allocate `Depth24`, active public render targets are unbound before readback, temporary bound buffers are released, and stale broad/native exception, exact-transfer, MRT/format and cube-orientation expectations are reconciled. The configured display passes 333/336; all three remaining binaries pass unchanged on a fresh Xvfb context. Two are permitted GL anisotropic footprint differences; the negative-MaxMip portability split is separately repaired by SOFTWARE-285 | Confirmed regression-corpus/profile gap and repaired, SOFTWARE-284 ✅; no Software feature gap was hidden by these 193 failures |
| EasyGL converted a negative `MaxMipLevel` request to an out-of-range GL float | Software's integer LOD clamp passed, and the EasyGL behavior happened to pass on one Mesa/Xvfb context, hiding that `uint32_t(-1)` became approximately 4.29 billion before `GL_TEXTURE_MIN_LOD` | The shared mip contract proves XNA's unsigned conversion must select the resource's last stored level through both stock 3D and SpriteBatch. EasyGL now caps the unsigned request at GL's standard default upper LOD of 1000, safely beyond every legal XNA mip index. All 93 checks pass on both formerly disagreeing Mesa displays | Confirmed EasyGL classic-XNA portability gap and repaired, SOFTWARE-285 ✅ |
| Compiled-Effect fuzz and benchmark tools no longer compiled after XNA collection indexers became pointer-valued | Renderer/unit builds did not include both manually invoked tools, so `SOFTWARE-255` correctly changed the public collections while leaving stale member syntax in these consumers | A repository-wide old-indexer search found only the fuzzer and benchmark. Both now consume pointers explicitly; the fuzzer null-guards all reflected elements. The desktop compiled-effect build replays 26 valid and historical crash-corpus binaries, the benchmark executes construction/clone/apply/draw paths, and the complete EasyGL build succeeds | Confirmed verification-infrastructure regression and repaired, SOFTWARE-286 ✅; no renderer parity delta |
| Destructor-only texture/buffer cleanup left dangling `GraphicsDevice` bindings | Explicit `Texture::Dispose` cleared sampler collections, while explicit buffer disposal intentionally retained FNA's managed-object binding. The shared fixture then manually unbound buffers and never let a bound texture die naturally. In C++, the later `GraphicsResource` base destructor cannot dispatch to those derived cleanup hooks, so public collections/getters retained dead wrapper addresses | FNA strongly roots bound managed resources; its stale disposed buffer reference therefore remains a live object. CNA now keeps that explicit-dispose behavior only until the C++ wrapper dies, then clears the index or complete vertex-stream set. Texture2D/3D/Cube destructors invoke their derived cleanup so pixel/vertex sampler slots cannot dangle. A weak device-lifetime token makes destructor cleanup safe when device/resource order is reversed. The shared source passes 24/24 on Software and EasyGL, with handle, move-smoke and device-first families also green; full graphics pass 2,523/2,587 and 2,534/2,603 respectively | Confirmed renderer-neutral XNA/Core lifetime adaptation gap and repaired, SOFTWARE-287 ✅ |
| C++ resource moves orphaned the live destination from its device and bindings | The previous smoke test checked only shared/native handle ownership while source and destination occupied one scope. `GraphicsResource`'s default address identity remained in `GraphicsDevice::resources_`; raw vertex/index/sampler/target bindings likewise continued to name the moved-from wrapper. In a scope-crossing proof the source destructor therefore unregistered and unbound the still-live destination, failing 9/12 new lifecycle assertions (24/33 total). Moving across devices exposed the dual error: the overwritten destination stayed bound on its former owner | Tracking entries now transfer or coalesce during move construction/assignment, moved-from bases become inert, and type-specific hooks replace every applicable raw binding while retaining vertex offsets and instance frequencies. The 60-check shared contract covers buffers, 2D/3D/cube textures, pixel/vertex samplers, an active render target, both move forms and destruction order; both Software and EasyGL GLES pass. Two shared unit tests prove cross-device detachment/tracking and reject overwriting a bound render target before mutation. Four neighboring lifecycle families pass 5/5 on each renderer | Confirmed renderer-neutral C++ adaptation gap and repaired, SOFTWARE-288 ✅; no renderer parity delta |
| The exact Software skip ledger lagged nine new compiled-Effect conformance tests | The ledger still reported the old 55-skip corpus and only six in-scope skips, while the current full Software graphics run passes 2,525 of 2,589 tests and skips 64. Each reported name and its source guard was re-audited rather than inferred from the total | The same 49 tests remain exact platform/build-shape restrictions, foreign-renderer selectors, negative capability controls or CNAEXT paths. Six `EffectMaterialTest` and nine `EffectTest` cases are gated only by `GraphicsCapability::CompiledEffects == false`; all 15 are evidence of the already documented SOFTWARE-162..165 classic compiled-Effect gap. The exact section below names every member | Confirmed documentation/audit drift and repaired, SOFTWARE-289 ✅; no additional renderer gap was concealed |
| Classic vertex/index copies accepted null or zero-element ranges and used the wrong failure order | SOFTWARE-204 followed FNA's no-op behavior and rewrote 29 tests around it. The feature-oriented buffer audit covered capacity, normal transfers and bound writes, but never compared mixed-invalid calls with Microsoft's implementation. CNA also named the caller index `startIndex`, checked it before bound-resource state and checked WriteOnly before null | Recovered Microsoft `VertexBuffer`/`IndexBuffer.CopyData<T>` checks disposed, null/empty data, bound non-streaming write or WriteOnly readback, then `Helpers.ValidateCopyParameters`, whose public names are `dataIndex` and `elementCount` and which requires a positive count. Two new discriminators failed 0/2 before repair across 18 exact/mixed exception checks. Typed and generic classic routes now share the recovered order; rejected calls preserve prior data, while raw CNAEXT vertex transfers retain their documented empty no-op. The focused 53-test selection passes on Software, EasyGL GLES and desktop compiled-effect EasyGL; full Software graphics passes 2,527/2,591 with 64 classified skips | Confirmed shared classic-XNA public-copy/error-order gap and repaired, SOFTWARE-290 ✅ |
| Classic vertex/index byte-window copies rejected a negative buffer offset too early and with the wrong exception | Existing window tests proved successful partial transfers and oversized capacity failures, but no mixed-invalid test combined a bad caller index with a negative `offsetInBytes`, and no test pinned the negative-offset failure family. CNA explicitly threw `ArgumentOutOfRangeException("offsetInBytes")` before IndexBuffer's `dataIndex` validation | Recovered Microsoft `CopyData<T>` validates the managed copy window first, then includes signed `offsetInBytes` in an unsigned native buffer-span check. A strongly negative offset therefore becomes an invalid resource range and reports `InvalidOperationException`. The new Set/Get × vertex/index discriminator failed six of eight checks before repair. Shared classic generic paths now validate `dataIndex`/`elementCount` first and let negative or oversized offsets use the same resource-size failure; raw CNAEXT windows retain their own contract. The complete 48-test buffer slice passes on Software, EasyGL GLES and desktop compiled-effect EasyGL; full Software graphics passes 2,528/2,592 with 64 classified skips | Confirmed shared classic-XNA public-copy/error-order gap and repaired, SOFTWARE-291 ✅ |
| `SetDataOptions` combinations and generic dynamic vertex copies diverged from Microsoft XNA | Tests used only the three named enum values and application vertex types whose size equaled their declaration stride. CNA provided no flag operators, checked streaming values by exact equality, routed the generic overload through raw CNAEXT empty/stride rules, and passed `sizeof(T)` to renderers as both vertex count and draw stride | Recovered Microsoft marks the enum `[Flags]`, converts it by bit precedence (`Discard` before `NoOverwrite`, unknown bits ignored), and treats generic data as a contiguous byte span bounded by the buffer's fixed byte capacity. New shared tests prove 3/5/6 option combinations on bound vertex/index buffers, null/count/resource failure order, a transfer type narrower than the declaration, fixed renderer capacity/stride, source selection and preserved suffix on a short `NoOverwrite`. The two original discriminators failed 0/2 before repair; the complete 28-test buffer slice now passes on Software, EasyGL GLES and desktop compiled-effect EasyGL, and full Software graphics passes 2,531/2,595 with 64 classified skips | Confirmed shared classic-XNA flags/copy/storage gap and repaired, SOFTWARE-292 ✅ |
| Native-width index-buffer prefix uploads shrank the logical renderer buffer and destroyed its suffix | Generic transfer widths and explicit byte windows used the fixed-capacity shadow, but the optimized 16→16/32→32 path sent only `elementCount` to the renderer, then zero-expanded readback. An older test explicitly pinned the resulting one-entry renderer count after constructing four entries | Recovered Microsoft locks only the requested span inside constructor-fixed `_size`; neither `IndexCount` nor untouched bytes change. A new shared static/dynamic test failed 0/1 before repair: both suffix checks returned zeros and both renderer counts were 2 instead of 4. All paths now compose fixed storage; complete dynamic replacements retain their hint, while partial/windowed fallbacks preserve exact output without a false `NoOverwrite` promise. The focused 10-test slice passes on Software and both EasyGL profiles; full Software graphics passes 2,532/2,596 with 64 classified skips | Confirmed shared classic-XNA fixed-buffer copy gap and repaired, SOFTWARE-293 ✅ |
| Fresh classic vertex/index buffers had no readable or drawable storage before first `SetData` | Static and dynamic constructors exposed their fixed public counts but left `cpuShadow_` empty and renderer counts at zero. Four valid `GetData` calls threw `InvalidOperationException`, and a one-triangle indexed draw failed CNA's client-side range check before renderer submission | FNA calls `FNA3D_GenVertexBuffer`/`FNA3D_GenIndexBuffer` with the complete constructor byte size. Recovered Microsoft `CopyData<T>` validates against that fixed size and has no “has uploaded” condition. The new shared static/dynamic discriminator failed 0/1 before repair with nine independent failures. Declaration-backed vertex constructors and every index constructor now publish a deterministic full-size zero allocation; the focused 39-test buffer slice passes on Software, EasyGL GLES and desktop compiled-effect EasyGL, and full Software graphics passes 2,533/2,597 | Confirmed shared classic-XNA constructor/storage gap and repaired, SOFTWARE-294 ✅ |
| Classic `Model` public failures leaked native C++ exception families | Existing undersized-array tests expected only `std::out_of_range`; no test passed a base Effect that lacks `IEffectMatrices` through `Model.Draw`. The three copy methods and draw therefore disagreed with the XNA-visible exception contract while remaining green | FNA declares `ArgumentOutOfRangeException("destinationBoneTransforms"/"sourceBoneTransforms")` for the three size guards and `InvalidOperationException` for an incompatible Effect. Four exact tests failed 0/4 before repair. The complete 24-test Software `ModelTest` suite passes and the same focused 4/4 proof passes on EasyGL GLES | Confirmed shared classic-XNA error-semantics gap and repaired, SOFTWARE-295 ✅ |
| Unresolved SpriteFont glyphs leaked `std::invalid_argument` | The two existing `MeasureString` tests explicitly accepted the native C++ type, while DrawString coverage stopped at its pre-Begin guard and never exercised a real atlas with an unknown glyph | FNA's string and StringBuilder measure/draw paths explicitly throw `ArgumentException` naming `text` when neither the requested glyph nor a default is available. Three exact type-and-parameter discriminators failed before repair. The complete Software font/batch family passes 80/80; the focused 3/3 proof passes on both EasyGL profiles | Confirmed shared classic-XNA public error-semantics gap and repaired, SOFTWARE-296 ✅ |
| `Texture2D.FromStream` ignored the caller's current stream position and FNA's rewind-at-end rule | Existing format and resize tests always created a fresh stream at position zero. CNA used its complete Length as the read count regardless of Position, so a valid embedded image over-read by the prefix length and a reusable seekable stream at EOF read no bytes | FNA explicitly rewinds only when `CanSeek && Position == Length`; otherwise its decoder callbacks consume from the current Position. A prefixed image on the ordinary overload and an at-end PNG on the resize overload both failed before repair. The shared reader now applies those exact position rules; SOFTWARE-307 later migrated the first discriminator from BMP to supported PNG after measuring XNA's format boundary | Confirmed shared classic-XNA stream-semantics gap and repaired, SOFTWARE-297 ✅ |
| `Texture2D.SaveAsPng/SaveAsJpeg` could not save a resolved render target | Existing save tests used only ordinary textures whose uploaded pixels remain in `cpuPixels_`. Render targets intentionally have no authoritative upload shadow, so both methods threw before consulting renderer storage | FNA always performs a native level-zero texture readback before either encoder. Two clear-to-target/save/decode/full-readback tests failed before repair for PNG and JPEG. Color saves now use the same authoritative readback path as public `GetData`; the full Software save family passes 16/16 and both focused cases pass on both EasyGL profiles | Confirmed shared classic-XNA render-target readback gap and repaired, SOFTWARE-298 ✅ |
| PNG/JPEG save treated every classic texture layout as RGBA8 | Existing tests covered only ordinary Color textures. One/two-byte formats were read beyond their buffers, all packed/signed/float/half layouts encoded their raw bits as channels, DXT textures had no usable shadow, and CNA preserved hidden RGB at alpha zero | A real Microsoft XNA 4.0 probe successfully saved Color, all 16 other classic uncompressed formats and DXT1/3/5 through both APIs. Decoded PNG pixels pinned packed, signed, missing-channel, float/half, DXT and exact-zero-alpha behavior. Five shared discriminators failed 0/5 before repair; the complete Software save family now passes 21/21 and the focused proof passes 5/5 on both EasyGL profiles | Confirmed shared classic-XNA conversion and memory-safety gap and repaired, SOFTWARE-299 ✅. FNA3D's RGBA8-assuming writer contradicts the measured Microsoft result and was not treated as higher authority |
| PNG/JPEG target-size changes used a bilinear filter | Existing tests asserted only encoded dimensions, so they could not distinguish any filter or source-coordinate mapping | Microsoft XNA 4.0 oracle output pins floor-mapped nearest texels for `2→3`, `2→4`, and `4→2`; JPEG separately pins the same hard boundary before compression. Two public discriminators failed 0/2 before repair. The save-only nearest helper has a direct two-axis proof, while the general bilinear scaler remains independently tested and unchanged | Confirmed shared classic-XNA resize gap and repaired, SOFTWARE-300 ✅; focused 3/3 proof passes on Software and both EasyGL profiles |
| PNG/JPEG save validation exposed native C++ exceptions and wrong parameter identity | Existing null-stream tests explicitly expected `std::invalid_argument`; no test distinguished zero from negative target dimensions or pinned disposed-resource precedence on the public save methods | Microsoft XNA 4.0 oracle probes report `ArgumentNullException("stream")`, unparameterized `ArgumentException` for zero width/height, named `ArgumentException("targetWidth"/"targetHeight")` for negative dimensions, and `ObjectDisposedException("Texture2D")` for a disposed valid request. Four new shared test cases failed 0/4 before repair while both disposed proofs already passed. The complete Software save family passes 27/27 and the six focused cases pass on both EasyGL profiles under virtual X | Confirmed shared classic-XNA public validation gap and repaired, SOFTWARE-301 ✅; full Software graphics passes 2,551/2,615 with 64 classified skips |
| `DualTextureEffect` null samplers used a CNA-invented opaque-white convention | The two EasyGL-only tests explicitly asserted white and Software had no shared null matrix: `Texture=null` silently bypassed the dual formula, while `Texture2=null` threw on both indexed and non-indexed paths | A real Microsoft XNA 4.0 HiDef probe with both required texture-coordinate semantics returned exact opaque black for either null slot. The new shared four-way matrix draws valid textures first to expose stale bindings, then covers both null slots across indexed and non-indexed submission. It failed 0/4 on Software before repair; the pre-fix EasyGL executables returned `(160,80,240,255)` under an isolated virtual X display. The repaired matrix passes 4/4 on Software, EasyGL GLES and desktop compiled-effect EasyGL; both corrected standalone probes pass byte-exactly, and full Software graphics passes 2,555/2,619 with 64 classified skips | Confirmed shared classic-XNA stock-effect gap and repaired, SOFTWARE-302 ✅; SOFTWARE-303 later generalized the measured rule |
| Remaining classic stock effects used the same unmeasured opaque-white/null-bypass convention | SOFTWARE-111/115/153 and Task 379 asserted white for BasicEffect, AlphaTestEffect and SkinnedEffect; Software bypassed a missing base sample and rejected a null environment cube, while EasyGL used a 2D white texture even for the cube sampler | Five isolated Microsoft XNA 4.0 HiDef probes returned exact opaque black for BasicEffect with texturing enabled, AlphaTestEffect, EnvironmentMapEffect's base Texture and EnvironmentMap independently, and SkinnedEffect. Before repair the four corrected Software contracts passed 7/8, 29/30, aborted, and 18/19; EasyGL GLES passed 7/8, 29/30, 14/16 and 18/19. Both renderers now preserve the stock formula with black 2D/cube null samples while CNAEXT PBR retains its separate white identity. The complete families pass 8/8, 30/30, 16/16 and 19/19 on Software and both EasyGL profiles; the corrected standalone stale-binding proof passes 3/3. Full graphics passes 2,555/2,619 on Software and 2,566/2,635 on EasyGL GLES | Confirmed shared classic-XNA stock-effect gap and repaired, SOFTWARE-303 ✅ |
| `Texture2D.FromStream` resize dimensions and decode failures exposed native exceptions | Existing resize tests exercised only positive dimensions and valid PNG data. CNA decoded first, so invalid dimensions did unnecessary stream work before leaking `std::invalid_argument`; empty or corrupt non-DDS data leaked `std::runtime_error` in both overloads | Once XNA's separately recovered seekability precondition is satisfied, isolated Microsoft XNA 4.0 probes establish width-before-height-before-decode ordering: every zero/negative dimension is a named `ArgumentOutOfRangeException`, with `width` winning when both are invalid. Empty and nonempty corrupt streams produce exact `InvalidOperationException` in both overloads. Two shared discriminators with ten exact checks failed 0/2 before repair. The complete format/resize family passes 10/10 on Software and both EasyGL profiles; full graphics passes 2,557/2,621 Software and 2,568/2,637 EasyGL GLES with unchanged classified skips. DDS-specific extension diagnostics remain precise | Confirmed shared classic-XNA public-validation gap and repaired, SOFTWARE-304 ✅ |
| `Texture2D.FromStream` omitted XNA's seekable-stream precondition | Every existing test used `MemoryStream`, and FNA lacks Microsoft's explicit entry guard. CNA therefore queried `Length` even when `CanSeek` was false, leaking whatever exception the custom stream chose to throw | Microsoft XNA 4.0 rejects a non-seekable readable stream with exact `ArgumentException("stream")` in both overloads before any `Length`, `Position`, `Read` or `Seek` call. The same result wins over invalid requested dimensions. One shared discriminator failed 0/1 before repair and now pins all three paths plus zero property/data access. The focused 3/3 proof passes on both EasyGL profiles; full suites pass 2,558/2,622 Software and 2,569/2,638 EasyGL GLES with unchanged skips | Confirmed shared classic-XNA validation/precedence gap and repaired, SOFTWARE-305 ✅ |
| Classic `Texture2D.FromStream` silently accepted DDS | CNA documentation called DDS auto-detection an intentional permanent deviation, so no test compared the same valid payload with Microsoft XNA; FNA's ordinary image decoder also deliberately excludes DDS and exposes a separately named extension | An isolated Microsoft XNA 4.0 oracle rejects a valid 4x4 DXT1 DDS with exact `InvalidOperationException` in both classic overloads. The new shared two-overload discriminator failed 0/1 because CNA successfully returned a texture. Ordinary decoding now follows the XNA format boundary, while explicit `CNAEXT DDSFromStreamEXT` entry points retain native compressed loading and CNA's pre-existing resize route. Both new tests pass on Software and both EasyGL profiles; full graphics passes 2,560/2,624 Software and 2,571/2,640 EasyGL GLES with unchanged classified skips | Confirmed shared classic-XNA API-boundary gap and repaired, SOFTWARE-306 ✅ |
| Classic `Texture2D.FromStream` accepted every format understood by stb | The prior test called a successful BMP decode a compatibility proof and no accepted/rejected container matrix existed. CNA therefore also accepted valid TGA, QOI, PSD, HDR and PPM through both public overloads | An isolated Microsoft XNA 4.0 oracle accepts PNG/JPEG/GIF but returns exact `InvalidOperationException` for BMP3 and all five other containers. FNA enables TGA/QOI too, but the measured Microsoft contract has higher authority. The shared matrix first proves the broad internal decoder can decode all six inputs, then requires both public overloads to reject them; it recorded twelve missing exceptions before repair. A classic-only signature gate now admits PNG/JPEG/GIF while preserving the internal decoder. The complete 14-test family passes on Software and both EasyGL profiles; full suites pass 2,561/2,625 Software and 2,572/2,641 EasyGL GLES with unchanged skips | Confirmed shared classic-XNA container-boundary gap and repaired, SOFTWARE-307 ✅ |
| Negative and NPOT bilinear Wrap/Mirror addressing lacked exact output proof | The prior 154-check point-sampling corpus proved negative Wrap/Mirror and mixed axes only under point filtering. Linear coverage could therefore stay green even if either neighbour of a bilinear footprint used Clamp or mishandled a negative tile boundary | A new analytic whole-image oracle maps a 3x3 NPOT source spanning negative and positive periods through LinearClamp, LinearWrap, LinearMirror and mixed Mirror-U/Wrap-V. Each leg checks all 576 pixels and reports its maximum channel error, with only a two-byte allowance for interpolation rounding. All four legs report zero mismatches on Software, EasyGL GLES and desktop compiled-effect EasyGL, bringing the shared contract to 162/162 | Disproved as an implementation gap; conformance proof closed SOFTWARE-308 ✅ |
| Active `OcclusionQuery` destruction was followed by a non-discriminating health check | The existing 50-cycle stress proved no throw and no tracked-resource leak, but then called `IsComplete` on a fresh query and unconditionally executed `check(true)`. It could not detect a native query target left active or a new query that never completed | The corrected test begins/ends a fresh query, forces native command progress through backbuffer readback, requires `IsComplete == true` and then requires `PixelCount` to be readable. Software, EasyGL GLES and desktop compiled-effect EasyGL all pass after the same 50 active destructions | Disproved as an implementation gap; lifecycle proof closed SOFTWARE-309 ✅ |
| EasyGL ignored a legal zero-area `ScissorRectangle` | The shared deferred-scissor fixture explicitly expected EasyGL to retain the previous rectangle for zero width/height. That renderer-specific declaration turned a known classic-XNA output mismatch green | Recovered Microsoft XNA permits nonnegative extents and submits the empty D3D rectangle; FNA sends zero to `glScissor`, whose empty box rejects all fragments. Flipping EasyGL to the XNA result failed 49/50 on GLES and desktop GL: seven of nine probes were yellow instead of black. The `w <= 0 || h <= 0` early return is now negative-only, and both EasyGL profiles pass all 50 checks alongside Software | Confirmed EasyGL classic-XNA raster-state gap and repaired, SOFTWARE-310 ✅ |
| EasyGL let an enabled scissor clip `GraphicsDevice.Clear` | The ordered-Clear fixture declared the resulting 4x4 partial clear as an acceptable EasyGL divergence, even though Software already cleared all 8x8 pixels. It therefore kept a renderer-specific classic-XNA mismatch green | Recovered Microsoft XNA reads and temporarily disables `D3DRS_SCISSORTESTENABLE`, expands the viewport to the full target, issues `IDirect3DDevice9::Clear`, then restores both states. FNA3D independently disables `GL_SCISSOR_TEST` around `glClear`. Correcting the expected result failed 48/49 on both EasyGL profiles with 48/64 pixels uncleared. EasyGL now neutralizes/restores scissor testing in every color/depth/stencil clear combination; the fixture separately proves color, depth, stencil and post-clear state, passing 45/45 on Software and 51/51 on both EasyGL profiles | Confirmed EasyGL classic-XNA clear/state gap and repaired, SOFTWARE-311 ✅ |
| Ordered-clear coverage falsely declared Software cube targets and stencil unavailable | The renderer already advertised and separately tested both capabilities, but this shared fixture hard-coded all three support flags false. It skipped cube-face clear isolation/readback and every stencil aspect/state case while still reporting a fully passing Software result | Enabling `cubeTargets`, `cubeReadback` and `stencilBuffer3D` required no renderer change and passed all formerly hidden cases immediately. Software now runs 51/51, including both cube-face cases, standalone/combined stencil clears, values 0/1/255, stencil state across a clear boundary and stencil clear under scissor | Disproved as an implementation gap; stale capability skips removed, SOFTWARE-312 ✅ |
| Pass/lifecycle fixtures retained pre-implementation Software cube and MRT boundaries | `PresentLifecycle`, `BackbufferPassOrder` and `RenderTargetPassBoundary` still classified Software cube destinations/readback as unavailable after SOFTWARE-119. The pass-boundary fixture also stayed at Reach, converting its expected MRT and backbuffer-readback profile errors into three accepted renderer skips on both Software and EasyGL | Cube declarations now reflect the actual renderer. The positive pass-boundary fixture requests HiDef, making the MRT and `GetBackBufferData` branches legal. No production mismatch appeared: Software passes 22/22 isolated lifecycle legs with zero skips, 30/30 backbuffer-order checks and 43/43 pass-boundary checks; both EasyGL profiles pass 43/43 under virtual X | Disproved as implementation gaps; five stale branches now execute, SOFTWARE-313 ✅ |
| Mid-tone color coverage skipped Software cube targets after they were implemented | The shared color-space fixture exercised exact ordinary texture, 2D target, sampling and blend behavior but hard-coded `kCubeSupported=false` for Software, turning its cube face into an unconditional pass. A nearby historical comment also still described the already-fixed Additive equation as open | The cube leg is now active and reads exact `(128,128,127,64)`, including nontrivial alpha. Software, EasyGL GLES and desktop EasyGL all pass 17/17; no production change was required. The historical Additive text now names SOFTWARE-169's repair | Disproved as an implementation gap; stale cube skip removed, SOFTWARE-314 ✅ |
| Software applied `MultiSampleAntiAlias` only to triangle interiors | The shared toggle test used one triangle, while classic `LineList`/`LineStrip` and wireframe edges went through a whole-pixel DDA that always wrote every sample. FNA3D applies a global GL toggle and its D3D11 rasterizer descriptor applies the same state to the complete primitive stream. Desktop EasyGL produced 12 partial pixels for the selected 4x line under true and none under false; pre-fix Software produced no partial pixels and identical images | The CPU line walker now evaluates the one-pixel segment footprint at all four sample positions and forwards the coverage mask through the ordinary fragment stages; false keeps the single pixel-center decision replicated. The expanded shared contract independently tests triangle, `LineList` and wireframe true/false/true transitions. Software and desktop EasyGL pass 26/26; GLES passes its 17 representable checks | Confirmed Software classic-XNA raster gap and repaired, SOFTWARE-315 ✅ |
| Software reused one pixel-center depth for every covered MSAA line/wireframe sample | `SOFTWARE-315` made coverage sample-correct, but all consumers still interpolated one depth from the emitted pixel-center segment parameter. A depth-gradient `LineList` followed by a constant-depth quad retained exactly one of two covered samples on desktop EasyGL; pre-fix Software retained both or neither | The line walker now supplies a segment parameter for each covered sample and all ordinary/wireframe consumers evaluate depth separately, including wireframe bias. The shared proof requires a two-sample coverage pixel to become a one-survivor depth pixel, so coverage shape cannot satisfy it accidentally. Software and desktop EasyGL pass 28/28; GLES passes 19/19 representable checks | Confirmed Software classic-XNA per-sample depth gap and repaired, SOFTWARE-316 ✅ |
| The stock-effect fog fixture relied on the pre-conformance null AlphaTest texture | Its AlphaTest leg omitted both a texture and texture coordinates, so after SOFTWARE-303 restored Microsoft's opaque-black null sample, four cases returned fogged black while the fixture still expected fogged diffuse blue | The fog-specific leg now supplies a deliberate white texture through `VertexPositionTexture`; the separate null contract still requires opaque black. The complete 33-case matrix passes on Software and both EasyGL profiles | Disproved as a production gap; stale shared fixture repaired, SOFTWARE-317 ✅ |
| The compressed-content fixture declared its readable vector stream non-seekable | The source owned a length and cursor but inherited `CanSeek=false`; after SOFTWARE-305 restored the XNA stream precondition, a fully relinked `Software_DxtContent` aborted before decoding | The fixture now uses SharpRuntime's read-only `MemoryStream`, whose capability and position behavior are independently tested. DDS DXT1 storage/draw and XNB DXT5 retention pass 3/3; the complete Software CTest label passes 158/158 | Disproved as a production gap; stale cross-renderer fixture repaired, SOFTWARE-318 ✅ |
| Software used a regular grid instead of the classic standard 4x MSAA pattern | FNA3D's D3D11 backend explicitly requests `D3D11_STANDARD_MULTISAMPLE_PATTERN`; the corresponding 4x coordinates are `(3/8,1/8)`, `(7/8,3/8)`, `(1/8,5/8)`, `(5/8,7/8)`. Software independently hard-coded `(1/4,1/4)`, `(3/4,1/4)`, `(1/4,3/4)`, `(3/4,3/4)` in its line and two triangle routes | An 8x8 horizontal boundary at screen `y=4.2` resolves one green sample on EasyGL (`G=64`) but none on pre-fix Software. A single canonical table now feeds every CPU coverage/depth loop. The shared test passes 29/29 on Software and desktop EasyGL and 20/20 representable checks on GLES; the complete Software label passes 158/158 and full graphics passes 2,561/2,625 with the same 64 classified skips | Confirmed Software classic-XNA raster gap and repaired, SOFTWARE-319 ✅ |
| CNA discarded or rejected bound vertex streams with semantic collisions | REMED-GFX-201 tests encoded the wrong premise that a fully colliding stream is dropped, while no fixture put a unique attribute beside a collision. FNA3D D3D11/OpenGL/SDL instead walk every element in slot order and remap a repeated pair to the first free index of that usage | Before repair the expanded shared fixture passed 16/18 on Software: a repeated `Position0` beside unique `TextureCoordinate0` threw, and a second `TextureCoordinate0` was dropped instead of feeding DualTextureEffect's `TextureCoordinate1`, producing green rather than red. Effective binding indices now cross the common draw tuple and are consumed by Software, EasyGL stock binding and EasyGL compiled-effect reflection. The fixture passes 18/18 on Software, OpenGL33 and OpenGLES3. Full graphics remains 2,561/2,625 Software and 2,573/2,641 desktop EasyGL with unchanged classified skips; the full enabled EasyGL compiled-effect family passes 39/39 | Confirmed shared classic-XNA vertex-input gap and repaired, SOFTWARE-320 ✅ |
| CNA let an inactive short vertex stream bound or endanger a stock-effect draw | CNA's shared validation treated every bound per-vertex stream as fetched, while Software also formed pointers for all of them. Recovered Microsoft draw IL performs no managed capacity check, FNA forwards all bindings, and stock HLSL declares only the semantics selected by effect properties. Two isolated XNA 4.0 HiDef probes accepted six-vertex draws beside one-record streams when the latter became unused `Color1` or supplied disabled `Color0` | The binding tuple now records whether any collision-remapped semantic in each stream is consumed by the current stock vertex shader. Software decoding/pointer formation and EasyGL VAO binding skip wholly inactive streams. The expanded public fixture passes 20/20 on Software and both EasyGL profiles. The task-closure active-short exception expectation was subsequently disproved and superseded by SOFTWARE-322 without changing this inactive-stream result | Confirmed shared classic-XNA vertex-input gap and repaired, SOFTWARE-321 ✅; range premise corrected by SOFTWARE-322 ✅ |
| CNA synthesized managed exceptions for native buffered-draw ranges | REMED-GFX-113/118/201/202 required negative starts/bases, declared windows and actual buffer capacity to throw before submission. Eleven isolated XNA 4.0 HiDef probes instead accepted short active primary/secondary vertex streams, short/crossing index ranges, negative starts/range hints/base and a positive base beyond the vertex buffer. Recovered XNA IL and FNA both forward these inputs after required count/profile/device-state checks | A renderer contract retains the compatibility guard only where unchecked CPU staging requires it. Software and EasyGL opt out. Software checks each index/vertex/instance record before pointer formation and supplies deterministic defaults for missing data; EasyGL bounds-checks only its CPU negative-base/wireframe expansions while direct GL inputs remain native. Folded offsets saturate safely. Required counts/profile ceilings and CNAEXT empty-declaration safety are unchanged. Software passes focused range tests, indexed-addressing 43/43, lifecycle 22/22, full graphics 2,561/2,625 (64 classified skips) and label 158/158; desktop EasyGL passes focused range 13/13, lifecycle 22/22 and full graphics 2,573/2,641 (68 classified skips) under isolated Xvfb | Confirmed shared classic-XNA draw-contract gap and repaired, SOFTWARE-322 ✅ |
| Software's complete instanced-range corpus was hidden by a compile guard | Sixteen renderer-neutral public tests were enclosed in a backend macro list that omitted Software despite its true instancing capability. The binary therefore exposed only one adjacent Software-enabled range test and two implementation-specific skips | Adding `CNA_RENDERER_SOFTWARE` compiles the full zero/nonzero range, state, target, mutation and lifecycle matrix. All 17 applicable tests pass without a production change; only the explicitly EasyGL-only and D3D-only pins skip. Full Software graphics grows from 2,625 to 2,641 cases and passes 2,577 with the same 64 classified skips | Disproved as an implementation gap; stale compile guard removed, SOFTWARE-323 ✅ |
| Software skipped the generic exact non-indexed `PointListEXT` range proof | The CPU renderer already passed thirteen adjacent positive PointListEXT cases, but the renderer selector on `NonIndexedPointListHonorsVertexStartAndExactCount` listed seven other capable renderers and omitted Software | Adding Software to the selector requires no production change and the exact nonzero vertex-start/count pixel proof passes in 59 ms. The full corpus now passes 2,578/2,641 with 63 classified skips | Disproved as an implementation gap; stale CNAEXT runtime skip removed, SOFTWARE-324 ✅ |
| Invalid state-object enum values did not use Microsoft's native fallbacks | Valid-enum matrices were exhaustive, but no test cast an undefined integer into a state enum. Recovered XNA accepts those values in setters and applies defined native conversion defaults. Software threw for blend/depth/stencil and interpreted invalid address/cull as Clamp/CCW; EasyGL interpreted invalid Blend/Cull as One/front-face culling | The renderer-neutral fallback table retains every valid ordinal and pins both negative and upper-invalid inputs. Public behavioral tests retain the raw value in getters while exact pixels prove Blend→Zero/Add, Compare→Always, Stencil→Keep on both windings, Cull→None, Fill→Solid, Address U/V→Wrap and Filter→Linear. SpriteBatch's private sampler recorder closes the bypass path. The focused selection passes 8/8 on Software and isolated EasyGL GLES; full graphics pass 2,586/2,649 and 2,580/2,649 respectively | Confirmed shared classic-XNA conformance bug and repaired, SOFTWARE-325 ✅ |
| EasyGL clamped out-of-byte-range `ReferenceStencil` values instead of consuming their low eight bits | Existing stencil tests stopped at 0, 1 and 255, so both renderers agreed. A negative/high diagnostic produced opposite output: Software treated `-1` as 255 and 256 as 0, while EasyGL inherited OpenGL's reference clamp and treated them as 0 and 255 | Recovered XNA retains every signed `Int32` and forwards it as a D3D9 DWORD; D3D9 accepts every integer reference and masks the comparison against XNA's eight-bit stencil surface. The shared device boundary now normalizes only renderer-facing whole-state and direct-device values. The public state/device getters remain raw. Exact stamp/compare probes cover `-1`/255, `-1`/0, 256/255 and 256/0 plus both direct-property transitions on Software and isolated EasyGL GLES. Full graphics passes 2,587/2,650 and 2,581/2,650 respectively | Confirmed EasyGL/shared-plumbing classic-XNA conformance bug and repaired, SOFTWARE-326 ✅ |
| CNA omitted the classic `GraphicsDevice.Clear(ClearOptions, Vector4, Single, Int32)` overload | The old surface offered only the Color shape plus project conveniences. SOFTWARE-327 restored the signature and correctly marked the extensions, but its FNA-led raw-float implementation claim was later disproved | The overload remains present. Recovered Microsoft XNA evidence establishes a mandatory conversion through `Color`, now covered separately below; the prior unclamped RGBA32F assertion has been removed | Confirmed shared classic-XNA public-surface gap; signature repaired by SOFTWARE-327 ✅, behavior superseded by SOFTWARE-332 ✅ |
| CNA specialized all three generic `GraphicsDevice.GetBackBufferData<T>` overloads to Color only | The June surface audit recorded the limitation as an intentional C++ deviation even though Texture2D/Cube/3D and buffer generics already use constrained pointer templates. Applications could not read an ordinary Color backbuffer as bytes, 16-bit units or an application-defined four-byte value | FNA exposes all three generic shapes and validates that the element width divides the surface texel. CNA now adds trivially-copyable complete, destination-window and rectangle overloads, requires the exact requested byte total before mutation, and retains the safe object-aware Color path. The shared test proves exact raw RGBA order through 1/2/4-byte values, offset sentinels and null/zero/oversized/three-byte failures. Both Software and EasyGL apply Color backbuffers and pass 5/5 focused; full graphics passes 2,589/2,652 and 2,583/2,652 respectively | Confirmed shared classic-XNA public-transfer gap and repaired for the Software/EasyGL applied Color format, SOFTWARE-328 ✅; native non-Color backbuffer raw transfer needs a wider renderer seam |
| Stale `GraphicsDevice` surface audit obscured implemented declaration draws and unmarked extensions | Every explicit-`VertexDeclaration` generic draw shape is already represented by raw-stream plus built-in typed overloads. The default constructor, pointer Reset and five index/vertex convenience aliases were public non-XNA additions without the mandatory visual marker | `SOFTWARE-329` rechecks current CNA against FNA, marks all seven additions `CNAEXT`, and classifies the one truly missing XNA overload precisely: rectangle/foreign-window `Present` needs a physical swapchain/window renderer seam that neither EasyGL nor Software exposes and the campaign explicitly excludes. FNA's no-allocation render-target helper is an extension, not XNA | No hidden Software draw gap; confirmed shared API-marking defect repaired. Foreign-window Present remains an explicit repository-wide, out-of-scope omission; SOFTWARE-329 ✅ |
| `Texture2D.SetData<T>(data, startIndex, elementCount)` was absent | Earlier transfer tests used the five-argument level/rectangle overload whenever they needed a nonzero source index, so full byte/storage parity concealed a missing classic public overload on both renderers | The restored pointer/count template delegates to the already-validated level-zero transfer family. A shared discriminator uses a nontrivial Color object, a nontrivial packed Bgr565 value and an arbitrary four-byte POD with guard elements, then checks exact storage plus negative-index/null failures. The wider texture selection passes 52/52 on displayless Software and isolated EasyGL GLES; full Software passes 2,590/2,653 with 63 classified skips | Confirmed shared classic-XNA public-transfer surface gap and repaired, SOFTWARE-330 ✅ |
| CNA used FNA's truncating `Color` float/vector conversion instead of Microsoft XNA's packed-color rule | Midpoint tests asserted only endpoints or omitted the affected channel, while older IPackedVector tests explicitly encoded FNA's wraparound divergence. The error was exposed only when the actual Microsoft Vector4-clear delegation was recovered | Microsoft `Color.PackHelper` sends all float/vector constructors and `IPackedVector.PackFromVector4` through `PackUNorm`: Single-precision scale, clamp, NaN-to-zero and midpoint-to-even rounding. Exact constructor, `FromNonPremultiplied` and IPackedVector cases now require 0.5→128, the 128.5 tie→128, saturation and defined infinities. All 60 focused Color tests, 847 complete displayless math tests and 2,590/2,653 Software graphics checks pass | Confirmed shared renderer-neutral classic-XNA Core gap and repaired, SOFTWARE-331 ✅ |
| CNA's classic Vector4 clear preserved raw float/HDR components | SOFTWARE-327's test and documentation asserted that Microsoft XNA forwarded the Vector4 directly, but recovered Microsoft source shows the opposite four-line implementation; FNA is the diverging implementation here | Vector4 now constructs `Color` and delegates to the packed-color overload. The shared RGBA32F test uses out-of-range values plus two fractional channels and requires exact `Color(0,255,128,64).ToVector4()` output. The full 15/15 HDR family passes on displayless Software and isolated EasyGL GLES; Software graphics remains 2,590/2,653 | Confirmed shared classic-XNA behavior gap and repaired, SOFTWARE-332 ✅ |
| Explicit `ClearOptions` silently discarded a requested missing depth/stencil plane | Existing tests encoded FNA's mask-to-supported-flags behavior, so a color-plus-missing-depth clear mutated color and returned normally. The common renderer capability predicates did not distinguish support in principle from an attachment on the active surface | Recovered Microsoft XNA reports `InvalidOperationException` when the requested depth/stencil flags are not a subset of its active `DefaultClearOptions`. The common layer now computes real backbuffer/2D/cube attachment flags; explicit failures occur before mutation and renderer dispatch, while `Clear(Color)` requests only available planes. Shared backbuffer and RenderTarget2D tests distinguish no-depth, depth-only and depth/stencil surfaces. Full graphics passes 2,594/2,657 Software and 2,588/2,657 Xvfb-isolated EasyGL GLES with 63/69 classified skips | Confirmed shared classic-XNA clear/error-semantics gap and repaired, SOFTWARE-333 ✅ |
| The MSAA/depth supervisor requested attachments absent from its own targets | The executable had not been relinked after SOFTWARE-333, so its old green result concealed explicit stencil clears on None/Depth16/Depth24 targets, depth clears on None targets, and four Depth24 overlap clears that also requested stencil | A fresh pre-fix run passed only 15/31 isolated Software legs; all 16 failures were the expected missing-plane `InvalidOperationException`. The fixture now derives Target/Depth/Stencil clear bits from each requested `DepthFormat`, while its real stencil and split-clear checks remain explicit. It passes 31/31 with zero skips on Software and isolated Mesa EasyGL GLES | Confirmed stale shared conformance-fixture gap and repaired without a production change, SOFTWARE-359 ✅ |
| `Clear(Color)` used `Viewport.MaxDepth` instead of 1.0 | Task 928 deliberately followed FNA and called the behavior exact without checking the higher-authority Microsoft body or adding a pixel test. Ordinary viewports have MaxDepth=1, so nearly every clear remained indistinguishable | Decompiled Microsoft XNA calls `Clear(DefaultClearOptions, color, 1f, 0)`. With `Viewport.MaxDepth=0.25` and depth comparison `Less`, an explicit 0.25 clear rejects a fragment at the viewport maximum; the single-color clear must admit it after clearing to 1.0. The new test failed 0/1 before repair on both Software and Xvfb-isolated EasyGL GLES, then passed on both | Confirmed shared classic-XNA clear-depth gap and repaired, SOFTWARE-334 ✅ |
| Explicit depth clears rejected values outside `[0,1]` instead of using native saturation | Happy-path tests used only legal normalized values, while an EasyGL-only example explicitly expected CNA's invented managed exception. Recovered Microsoft XNA contains no such guard | An isolated Microsoft XNA 4.0 HiDef probe accepts finite out-of-range values, both infinities and NaN. Rendered depth comparisons prove `-0.25/-∞/NaN → 0` and `1.25/+∞ → 1`. Shared Software/EasyGL pixel tests pin every conversion; the same oracle and a shared test confirm unknown option bits are ignored. Full graphics passes 2,595/2,658 Software and 2,591/2,660 isolated EasyGL GLES | Confirmed shared classic-XNA clear/validation gap and repaired, SOFTWARE-335 ✅ |
| EasyGL stock shaders used OpenGL clip depth directly instead of XNA/D3D clip depth | Existing viewport tests checked ordering inside `[0,1]`, where the incorrect affine map remained monotonic. SAMPLE-028 then narrowed only compiled-effect draws to the same wrong upper-half scale, preserving mixed ordering while concealing the absolute error | The shared stock-effect discriminator uses identity matrices. It passes on Software but failed both pre-fix EasyGL clauses exactly: depth-equal clear 0 rejected clip `z=0` because GL mapped it to 0.5, and depth-disabled clip `z=-0.5` rendered red although XNA rejects `z<0`. Every EasyGL-owned 3D shader now inserts `z'=2z-w`; the compiled-effect range workaround is removed because MojoShader already inserts the same conversion. GLES and desktop focused tests pass, the complete compiled-Effect family is 39/39, and full graphics passes 2,598/2,661 Software plus 2,592/2,661 EasyGL GLES with 63/69 classified skips | Confirmed EasyGL classic-XNA conformance gap and repaired, SOFTWARE-336 ✅ |
| EasyGL discarded `SpriteBatch.layerDepth` before rasterization | Sorting tests and the old EasyGL pixel example used the default `DepthStencilState.None`, so painter ordering stayed correct even though the renderer's eight-float vertex had no Z. FNA explicitly writes the depth to all four `POSITION0.Z` values and applies the caller's depth state | A shared Depth24/LessEqual scene submits a near red sprite before a far green sprite in Deferred mode. Software returned red; pre-fix EasyGL assigned both equal depth and returned green. EasyGL now uses a three-component position, preserves Z through FNA's sprite matrix, converts it to GL clip depth, and declares Vector3 to the compiled SpriteEffect route. The exact shared test passes on Software, GLES and desktop GL; a dedicated compiled pixel-shader test proves the inherited stock vertex path. The wider EasyGL SpriteBatch/depth selection passes 85/85, compiled Effects pass 40/40, and full graphics passes 2,599/2,662 Software plus 2,593/2,662 EasyGL GLES with 63/69 classified skips | Confirmed EasyGL classic-XNA SpriteBatch gap and repaired, SOFTWARE-337 ✅ |
| Software applies a SpriteBatch transform to `(x,y,0)` and discards transformed Z/W | The identity and ordinary 2D transform corpus could not see this. FNA first builds `(x,y,layerDepth,1)` and its full matrix can couple layer depth into XY/Z/W; EasyGL's repaired Vector3 route already preserved that input | Shared exact-pixel discriminators independently expose Z translation/depth acceptance, constant and varying-W perspective division/interpolation, a transformed-Z near-plane crossing and `Viewport.MinDepth/MaxDepth` mapping. The initial two failed pre-fix Software while passing EasyGL. Software now runs the Vector4 through FNA's projection, the shared six-plane clipper and a SpriteBatch-specific no-offset viewport conversion with perspective-correct varyings. The complete corpus plus final discriminator passes 2,604/2,667 Software and 2,598/2,667 EasyGL GLES | Confirmed and repaired classic-XNA combination gap, SOFTWARE-338 ✅ |
| Unknown nonzero `SpriteSortMode` values silently behaved like Deferred at flush | All existing cases used the five declared enum values. CNA queued an unknown value but matched no sorter, so it emitted every sprite in submission order | Recovered Microsoft XNA 4.0 `Flush()` calls `SortSprites()` for every nonzero mode; the recovered switch accepts only values 2, 3 and 4 and otherwise throws parameterless `NotSupportedException`. It skips `Flush()` entirely for an empty queue. The new shared test failed before repair by observing one draw and no exception; it now proves an unknown value throws before any draw while the empty case succeeds. The ten-mode plus End-exception selection passes 11/11 in displayless Software and EasyGL builds | Confirmed shared classic-XNA error-semantics gap and repaired, SOFTWARE-339 ✅ |
| Software clamped nonpositive SpriteBatch source extents to one before applying origin | Existing source-rectangle tests used positive widths/heights or a zero origin, which hides the signed factor because destination/source signs cancel. Recovered Microsoft `InternalDraw` retains raw extents and origin; FNA and EasyGL preserve the same algebra | Two exact-pixel cases use source `(2,0,-2,2)` and origin `(1,0)` through the vector/scalar-scale and rectangle-destination overload families. Pre-fix Software rendered on the opposite side of the pivot and failed 0/2 while EasyGL GLES passed 2/2. Raw signed extents restore both; a zero-width case now proves the CPU path does not fabricate a one-texel quad. The shared nine-test geometry slice and wider SpriteBatch/SpriteEffect selection pass 9/9 and 98/98 on both renderers | Confirmed Software classic-XNA source-geometry gap and repaired, SOFTWARE-340 ✅ |
| SpriteBatch far source endpoints overflowed before float conversion | Existing source tests stayed far from `Int32` limits. Both renderer-private quad builders evaluated `source.X + source.Width` and `source.Y + source.Height` as signed integers, so a legal `INT_MAX` origin plus a one-texel extent invoked C++ undefined behavior. Recovered Microsoft XNA retains origin and extent as separate `Single` values; FNA divides each component separately before float-domain expansion | An isolated Microsoft XNA 4.0 Reach oracle drew a 2x2 red/green/blue/yellow texture through PointClamp with source `(INT_MAX,INT_MAX,1,1)` and returned yellow at all 64 destination pixels. Both pre-fix CNA renderers returned red. Software and EasyGL now add the normalized float extent. EasyGL also canonicalizes only an already-constant, wholly outside Clamp coordinate for its stock SpriteEffect, avoiding Mesa GLES' opposite-edge result for very large coordinates without changing mip derivatives or arbitrary-effect inputs. The exact two-axis check passes on Software, GLES and desktop GL; the wider SpriteBatch/SpriteEffect slice passes 99/99 on Software and GLES | Confirmed shared classic-XNA arithmetic/rendering gap and repaired, SOFTWARE-341 ✅ |
| Software's homogeneous SpriteBatch path displaced exact half-pixel edges | SOFTWARE-338's focused Z/W tests did not include an ordinary W=1 corner exactly on a pixel center. Its clip-projection/viewport round trip moved `x=4.5` to about `4.5000005`, so the top-left rule rejected the whole left edge even though the caller's transform placed it exactly on the included boundary | The shared point-sampling J2 scene combines a nonuniform scale and translation. Its new explicit guard requires all 14 visible centers on the exact `x=4.5` edge; the former implementation returned zero and the complete contract was only 161/162. Software now transports the original viewport-local homogeneous X/Y through clipping and performs one divide plus viewport-origin addition at raster conversion. W=1 placement is exact and varying-W clipping/interpolation remains active. Software and isolated EasyGL GLES pass 163/163; the ten SpriteBatch depth/transform tests and eight subpixel tests remain green | Confirmed Software classic-XNA raster-placement regression and repaired, SOFTWARE-342 ✅ |
| The complete checked-in Microsoft XNA image corpus had never run through Software | Feature-specific shared tests proved isolated contracts, but the previous parity declaration never rendered all 39 renderer-neutral oracle scenes against their real XNA 4.0 PNG references in one reproducible pass | A permanent Software oracle target and displayless runner render all 39 scenes: before the separately tracked line repair, 8 were byte-exact, 26 matched within one byte/channel and 28 within two. All stock lighting, fog, environment-map and skinning scenes except one exact point-boundary case are within one byte. Nine remaining textured scenes differ only at the same interpolated point-texel threshold; independent constant-UV and binary-boundary real-XNA probes match Software exactly, disproving a sampler tie-rule defect. Rotated/flipped sprites differ at only eight two-byte pixels. The two line scenes exposed a deterministic coverage mismatch rather than being waived by tolerance | Corpus audit complete, SOFTWARE-343 ✅; line finding repaired by SOFTWARE-344 ✅ |
| Software used a generic ceil/floor DDA for classic aliased lines | Broad tests accepted any painted pixel in a multi-pixel window, while the corpus had never run on Software. The DDA included a final endpoint whose diamond the line never exited and advanced fractional diagonals at slightly different major-axis intervals | The checked XNA `LineList` reference differed at two right endpoints; `LineStrip` differed at 76 pixels. D3D9 specifies the GDI rule, and Microsoft's later functional specification describes the same directed diamond-exit coverage. A dominant-axis CPU walker now clips against all four diamond planes and rejects an exit at/after the final vertex. Both 256x256 reference scenes are byte-exact on Software and current EasyGL. Five isolated XNA probes additionally prove forward/reverse horizontal and exact diagonal lines plus an empty sub-diamond segment, all byte-exact against both renderers; varying placement is independently repaired by SOFTWARE-346 | Confirmed Software classic-XNA raster gap and repaired, SOFTWARE-344 ✅ |
| Software truncated RGBA8 render-target stores instead of performing UNORM conversion | Constant and binary-colour tests cannot distinguish truncation from rounding, and earlier differentials accepted one-byte noise. The implementation was internally inconsistent: Color used truncation while Rgba1010102, Rg32 and Rgba64 already rounded | Microsoft XNA line gradients produce 128 for an exact 0.5 varying and round both sides of other half-byte boundaries. A direct framebuffer regression pins three such values. Round-to-nearest reduces three XNA gradient differences from 32/32/142 pixels to 2/2/34 and raises the full corpus from 10 to 18 exact scenes plus 28 to 30 within one byte. EasyGL is independently at 2/2/16 on those gradients | Confirmed Software classic-XNA Color-target quantization gap and repaired, SOFTWARE-345 ✅; residual subpixel interpolation remains under separate audit |
| Software evaluated classic line varyings at its translated coverage-diamond centre | Constant-colour line scenes proved only coverage. The CPU's 63/128 geometry displacement correctly emulates D3D9 integer-centred coverage at `pixel+0.5`, but reusing that point for varyings advances the logical interpolation parameter by 1/128 pixel | Real XNA 4.0 renders a 32-pixel red/blue line as exact bytes 255/0, 247/8, 128/128, 120/135 and 8/247 at selected pixels. After the RGBA8 repair, Software remained one low at two midpoint-adjacent pixels. Carrying a separate logical interpolation offset makes both horizontal and 45-degree 64x64 probes byte-exact; a public LineList regression pins the values and directed endpoint exclusion. Current EasyGL remains two one-byte pixels away, while a fractional probe is still within one byte | Confirmed Software classic-XNA line-attribute gap and repaired, SOFTWARE-346 ✅ |
| Could the line-specific logical varying centre repair every triangle's one-byte residuals? | A simple coordinate model suggests evaluating against equally translated geometry at `pixel+63/128`, and the aggregate `colored3d` mismatch count falls modestly under that experiment | Pixel-level accounting rejects the universal model: the alternative fixes 261 `colored3d` pixels but breaks 180 exact ones, including 29 consecutive row-72 pixels whose XNA green byte is 203 and becomes 204. Other stock-effect scenes move in both directions. This is D3D9 fixed-function interpolation precision, not evidence for one global CPU sample displacement | Hypothesis disproved; no production change, SOFTWARE-347 ✅ |
| Graphics-state aliases shared render properties but not their `GraphicsResource` identity | SOFTWARE-232/234 tests stopped at immutable state payload and bind-time disposal. Custom source/device wrappers still had independent null parent devices, `Name`, `Tag`, base `IsDisposed` and event lists; device defaults deep-copied the three named presets, and every sampler slot deep-copied `LinearWrap` despite XNA/FNA storing the same references | Recovered Microsoft `Apply` code reassigns the retained object's parent. The Microsoft `GraphicsDevice` constructor assigns the named presets directly, and FNA's sampler collection confirms the shared `LinearWrap` default. Pre-fix identity checks failed across all four families. The opt-in shared resource identity now follows assignment, source-scope exit and cross-device application; mutations and disposal propagate with source-sender notification. Device defaults and untouched sampler slots share the preset identities. Three focused cases pass in the 177-test state slice on displayless Software and isolated EasyGL GLES | Confirmed shared classic-XNA/Core object-semantics gap and repaired, SOFTWARE-349 ✅; literal C++ address/event-token identity remains SOFTWARE-198 ⬜ |
| CNA rejected reassigning an already-active disposed state before checking retained identity | SOFTWARE-234 correctly made a different disposed state fail `Apply`, but its follow-up test also required the cached object itself to fail. Every CNA whole-state setter called `BindForUse` first, and a sampler source alias likewise reached the disposed check before slot identity | Recovered Microsoft setters compare object references first. An isolated XNA 4.0 probe reports success for same Blend/DepthStencil/Rasterizer/Sampler reassignment and for drawing with all active states disposed, while a different sampler slot fails. `BlendFactor`/`MultiSampleMask` and `ReferenceStencil` dirty their parent caches and make the next same-reference assignment call `Apply`; the probe confirms those disposed paths throw. CNA now compares shared payload identity before bind and tracks the two dirty caches explicitly. Eight no-op assertions failed pre-fix. The 229-test state selection and complete 2,682-test graphics suite pass on both Software and isolated EasyGL GLES, with only their existing classified skips | Confirmed shared classic-XNA lifecycle/error-semantics gap and repaired, SOFTWARE-350 ✅ |
| SpriteBatch had no GraphicsDevice-wide activity accounting and cleared deferred failures too early | Existing tests covered only repeated calls on one object. FNA's implementation has only a per-object `beginCalled` flag and clears it before flushing, so it cannot expose Microsoft's two device counters or retained-failure state | Recovered XNA IL shows `spriteBeginCount` plus `spriteImmediateBeginCount` and decrements them only after a successful End. An isolated XNA 4.0 probe proves two Deferred batches coexist; Immediate conflicts in both directions; failed Immediate setup is retryable; failed deferred End makes Begin fail, makes End retry, permits parallel Deferred, and blocks Immediate even after Dispose. Four CNA regressions failed before repair. Device counters and the pair now follow that order; the shared 78-test slice passes on both renderers, and full graphics passes 2,621/2,684 Software plus 2,615/2,684 isolated EasyGL GLES with the same 63/69 classified skips | Confirmed shared classic-XNA lifecycle/state gap and repaired, SOFTWARE-351 ✅ |
| SpriteBatch rejected finite floating-point destinations outside the `Int32` range | Earlier numeric tests covered non-finite inputs but encoded an unsupported distinction: NaN/infinities flowed to the renderer while finite values beyond an integer rectangle threw. SpriteBatch's float path does not use an XNA integer destination rectangle | Recovered Microsoft `InternalDraw` IL and FNA `PushSprite` store position and scale-derived dimensions directly as `Single`. An isolated XNA 4.0 probe accepts both signs of `float.MaxValue` for position and scale in Immediate and Deferred modes, including successful End. Five corrected tests failed before repair. Draw/DrawString now forward the complete `Single` domain; the integer-only renderer compatibility fallback performs defined saturating/non-finite conversion rather than a C++ undefined cast. A real Software/EasyGL draw proves an extreme off-screen sprite does not prevent a following visible sprite. Focused 9/9 and wider 78/78 selections pass on both; full graphics passes 2,622/2,685 Software and 2,616/2,685 isolated EasyGL GLES with 63/69 classified skips | Confirmed shared classic-XNA validation gap and repaired, SOFTWARE-352 ✅ |
| SpriteFont construction, property validation and failed lookup had been collapsed into one invented invariant | REMED-GFX-002 made both the internal-equivalent constructor and public setter reject a default absent from `Characters`; SOFTWARE-296 then followed FNA's `ArgumentException("text")` without checking Microsoft's private resolver. No test represented invalid custom/content data or asserted the setter's empty parameter name | Recovered XNA IL stores the constructor value directly, validates only the public setter, and resolves every MeasureString/DrawString glyph through `GetIndexForCharacter`, whose terminal failure is `ArgumentException("character")`. An isolated XNA 4.0 reflection/runtime probe confirms invalid construction succeeds, the property retains `?`, MeasureString fails for `character`, an invalid setter throws with no parameter and preserves `?`, and a null setter succeeds. All seven corrected CNA tests failed before repair. One shared resolver now serves both text paths. The 126-case combined family and full 2,687-case suites pass on Software and isolated EasyGL GLES | Confirmed shared classic-XNA content/property/error-semantics gap and repaired, SOFTWARE-353 ✅; supersedes SOFTWARE-296's parameter conclusion |
| SpriteBatch sorting mixed FNA's total float order with an invented stable C++ sorter | Existing tests asserted only distinct finite depths and explicitly required stable order within a texture group. The non-finite regression imposed FNA's total order so `std::stable_sort` would have a valid comparator | Recovered Microsoft IL calls .NET Framework 4 `Array.Sort<int>` for all three sorted modes. Its quicksort is unstable; its bare float comparisons return equality for every NaN pair. Isolated XNA probes prove reversed order for three equal keys, all six three-value NaN placements and exact 64-entry mixed orders. Three corrected CNA tests fail 0/3 pre-fix. An explicit framework-compatible partition loop now gives the measured result without feeding a non-transitive comparator to `std::sort`. The 141-test SpriteBatch/SpriteFont slice and complete 2,688-test graphics suite pass on Software and isolated EasyGL GLES | Confirmed shared classic-XNA ordering gap and repaired, SOFTWARE-354 ✅ |
| Compiled pixel execution was wholly absent after the vertex phase | EasyGL's MojoShader/GL path executes the original D3D9 pixel program. SOFTWARE-163 deliberately stopped after vertex output and every Software compiled draw threw, so even a texture-free `mov oC0,c0` program could not produce a pixel | Opcode inventory across all six stock `.fxb` files and the authentic XNA 4 fixture defined the first bounded executor slice. SOFTWARE-355 implements the observed arithmetic/register/output rules, pixel DCLs and perspective-varying triangle integration. The synthetic compiled Effect writes exact parameter-driven pixels through ordinary/indexed/multi-stream/instanced routes; separate probes prove blend, channel mask, depth, TEXKILL and shader depth output. All texture-free stock programs execute, the dedicated displayless CTest passes and full Software graphics remains 2,625/2,688 | Confirmed material compiled-Effect gap and partially repaired, SOFTWARE-355 ✅; texture/sampler/AddressW and target/MRT/SpriteBatch routing are repaired by SOFTWARE-356/357 ✅, while full SOFTWARE-164/165 remains open and capability remains false |
| Compiled texture instructions, dimensional samplers and AddressW were absent | EasyGL binds the Effect pass's live device slots and executes 2D/cube/volume texture instructions; the Software pixel executor initially threw at every TEX | SOFTWARE-356 retains sampler DCL dimensions, executes SM1/2 TEX plus projected/biased TEX, TEXLDD and TEXLDL, resolves the live texture collection, rejects dimension mismatch and uses existing Software filter/address/mip logic. SOFTWARE-360 corrects TEXLDD to authentic opcode 93 after the first unit repeated the implementation's 94/SETP error. Exact pixels distinguish replacement/null, U wrap/mirror, linear filtering, MaxMipLevel, LOD bias, cube +X, W wrap/clamp/mirror and trilinear volume filtering. Every pixel program in six stock and the authentic XNA fixture executes with a deterministic sampler; SOFTWARE-357 adds rendered 2D/cube source transitions | Confirmed and repaired for the opt-in triangle/target executor, SOFTWARE-356/357/360 ✅; full public conformance remains SOFTWARE-164/165 and capability remains false |
| Software SpriteBatch ignored classic compiled custom Effects | EasyGL/FNA keep an internal SpriteEffect active and let each custom pass replace only the shader stages it actually owns; source texture is rebound to slot zero after every pass. Software always used its fixed stock quad path, so a supplied classic Effect could not run | SOFTWARE-357 buffers the raw sprite POSITION0/COLOR0/TEXCOORD0 stream, groups it by source texture, applies passes in pass-major order and routes it through the compiled interpreter. The original XNA `SpriteEffect.fxb` is embedded in the opt-in build and supplies a retained vertex or pixel stage when the custom pass owns only the other stage. Exact overlapping multi-pass, slot-zero override, pixel-only MatrixTransform inheritance and render-target blit pixels prove the behavior | Confirmed classic-XNA gap and repaired in the opt-in executor, SOFTWARE-357 ✅; capability remains false pending SOFTWARE-164/165 |

### SOFTWARE-161: classic compiled XNA Effect assessment

This is a material classic-XNA gap. It is not `CNAEXT ShaderEffect`, and an opt-in dependency does
not change the API classification.

1. **Runtime representation.** `Effect(GraphicsDevice&, byte[])` accepts the original XNA/FNA
   Direct3D 9 Effect Framework binary emitted as `.fxb` or embedded by `EffectReader` in XNB. The
   public constructor validates that representation and distinctly rejects MonoGame MGFX.
2. **EasyGL representation.** EasyGL does not receive CNA source or a pretranslated CNA IR. Its
   runtime gives the original Effect Framework bytes to the pinned MojoShader effect parser; the
   OpenGL adapter translates the contained D3D9 vertex/pixel programs to GLSL 1.20, GLSL ES or
   GLSL ES 3 according to the active profile.
3. **Usable public APIs.** The shared graphics layer materializes `EffectParameter`, annotations,
   techniques, passes and current-technique selection from `ICompiledEffectRuntime`. Parameter
   writes, texture values, clone independence, pass state assignments and disposal are live under
   EasyGL rather than metadata-only facades.
4. **Executable evidence.** A clean `OPENGL33` build with
   `CNA_EASYGL_COMPILED_EFFECTS=ON` passes all 54 `EasyGLCompiledEffect*` tests on Mesa. The suite
   includes compiler-produced and authentic XNA 4 binaries, exact render-target pixels,
   ordinary/indexed/user/buffer draws, multi-stream input, instancing, pass/effect switches,
   2D/cube/volume samplers, targets and context recreation.
5. **SpriteBatch.** `SpriteBatch::Begin(..., Effect*)` applies every selected compiled pass and
   executes the supplied pixel shader while retaining the stock sprite vertex shader when the
   effect provides only a pixel stage. Shared tests cover custom output, multiple passes, slot 0,
   render-target sources and texture-run transitions.
6. **Models/content.** `EffectReader` reads the byte array from XNB and constructs the public
   Effect. `EffectMaterialReader` resolves an external effect, clones it and applies the parameter
   dictionary, including texture-valued parameters. Model shared resources assign those Effects
   to `ModelMeshPart`, so arbitrary compiled model materials can reach EasyGL's draw path.
7. **Instruction profiles.** MojoShader's translator covers classic D3D shader models through 3.
   Genuine compatibility therefore needs the register files, swizzles/modifiers, SM1 texture
   forms, SM2/3 arithmetic, predication/control flow, discard, derivatives/implicit LOD, VPOS,
   depth and MRT outputs used by valid XNA effect binaries—not merely the current small fixtures.
8. **Reusable parser/IR.** CNA already has reusable Effect Framework reflection/state translation
   around MojoShader. MojoShader exposes translated source/binary and reflection plus an evaluator
   for effect *preshaders*; it does not expose a CPU interpreter for the vertex and pixel shader
   programs. No existing CNA dependency supplies that missing executor.
9. **Reuse decision.** Software can reuse the effect parser, reflection descriptors, preshader
   evaluation and the original shader token buffers. It cannot obtain parity by invoking the GL
   adapter or the preshader evaluator. A Software-owned validated token IR is the smallest direct
   architecture; interpreting translated SPIR-V would add a second large subsystem and obscure
   D3D9-specific semantics.
10. **Minimum architecture.** Add an `ICompiledEffectRuntime` implementation that owns parsed
    programs and state; execute the vertex program against declaration-resolved multi-stream
    inputs; pass semantic outputs through the existing homogeneous clipper and perspective
    interpolator; execute pixels in 2x2 quads where derivatives are required; bind existing CPU
    2D/cube/volume surfaces and samplers; then publish COLOR0-3/depth/discard through existing
    MSAA, depth/stencil and blend stages.
11. **Estimated complexity.** This is a major standalone subsystem, estimated at roughly four to
    eight engineering weeks, 5-10 kLOC of interpreter/integration code, and a sizeable independent
    shader conformance corpus. Implementing only the current fixture opcodes would be a false
    capability claim.
12. **Useful phases.** SOFTWARE-162 builds the runtime and validated IR; SOFTWARE-163 implements
    vertex execution and draw-input integration; SOFTWARE-355 implements the independently testable
    texture-free triangle pixel/output slice; SOFTWARE-356 adds samplers, AddressW and texture
    instructions; SOFTWARE-357 closes MRT/target/SpriteBatch routing; SOFTWARE-358 closes classic
    line/wireframe routing; and SOFTWARE-361/362/363/364/366 close omitted modern/legacy arithmetic,
    structured conditions plus bounded loop/break/call flow under the SOFTWARE-164 umbrella. SOFTWARE-164/165 close the remaining SM1-3,
    content, lifecycle, stress and shared conformance before enabling the capability.
13. **Abstraction impact.** `ICompiledEffectRuntime` and the current draw token already provide the
    correct renderer-neutral boundary. The interpreter can remain reusable CPU-renderer code;
    common GraphicsDevice does not need shader opcodes or a GL-shaped API.
14. **Parity requirement.** Yes. EasyGL demonstrably executes this classic public Effect surface,
    SpriteBatch custom effects and content/model materials. Software advertises false and throws
    for the same valid bytes. Boundary tests are honest capability reporting, but they prove the
    gap rather than excuse it.

The current evidence therefore rules out final classification A. The public vertex
texture/sampler gap found by the audit is now closed by SOFTWARE-168. SOFTWARE-162/163/355/356/357/358/360/361/362/363/364/366/368/370/371/373/374/375/376/377/378/379/380/381/382/383/384/385/386 have completed bounded parser,
vertex, pixel, texture-sampler, MRT/target, SpriteBatch and line/wireframe phases, but the
SOFTWARE-164/165 compiled-Effect umbrellas remain pending. The final classification must account
for that remaining aggregate capability gap after the state/resource/default/skip audit.

### Property-level graphics-state audit

The tables below distinguish a property that merely survives a getter/setter round trip from one
that changes rendered output. Unless a row says otherwise, the named fixture is compiled from the
same public source for Software and EasyGL and passed on both. `PARITY` means the property has an
observable classic stock-effect path; it does not promote a CNAEXT shader facility into scope.

#### `SamplerState`

| Property | XNA/Core relevance | EasyGL / Software consumption | Behavioral evidence | Verdict/task |
|---|---|---|---|---|
| `Filter` | Classic XNA | EasyGL selects the GL min/mag/mip modes; Software selects point, linear, mip and anisotropic CPU paths | `TextureFilterOrdinalContract`, `SamplerComponentIsolation` and `TextureFilterMipContract` distinguish every ordinal, minification and transitions | PARITY, SOFTWARE-116/150 ✅ |
| `AddressU` | Classic XNA | Both address the horizontal coordinate before texel lookup | `TextureAddressMode` plus `SamplerComponentIsolation` cover Clamp/Wrap/Mirror, coordinates beyond both ends and U-only transitions. `PointSamplingContract` additionally checks negative/NPOT LinearClamp, LinearWrap, LinearMirror and mixed Mirror-U/Wrap-V over every pixel | PARITY, SOFTWARE-116/150/308 ✅ |
| `AddressV` | Classic XNA | Both address the vertical coordinate independently of U | The same contracts use asymmetric textures and V-only transitions, including negative point and bilinear Mirror/Wrap coordinates; the SOFTWARE-308 analytic oracle forces both linear neighbours across negative tile boundaries | PARITY, SOFTWARE-116/150/308 ✅ |
| `AddressW` | Classic XNA when a volume sampler is used | EasyGL applies `GL_TEXTURE_WRAP_R`; SOFTWARE-356 stores W independently and consumes it in the opt-in Software compiled volume path | EasyGL's enabled `SharedCubeAndVolumeSamplerContract` is the public reference. The Software focused runtime distinguishes Wrap, Clamp and Mirror with different voxels and proves trilinear W filtering; no fictitious stock-effect test is used because none declares a volume sampler | PARITY in the opt-in executor after SOFTWARE-159/356 ✅; public compiled-effect closure remains SOFTWARE-164/165 ⬜ |
| `MaxAnisotropy` | Classic XNA | EasyGL applies the driver anisotropy parameter; Software sizes and averages its directional footprint up to the requested limit | `AnisotropicFilterContract` distinguishes 1, 2, 4 and 16 taps from trilinear output and restores the state | PARITY, SOFTWARE-117 ✅ |
| `MaxMipLevel` | Classic XNA | Both apply it as FNA3D's lower mip bound after resource clamping; SpriteBatch publishes and consumes the same value | Self-identifying red/green/blue mip images cover lower-bound, oversized clamp, restoration and SpriteBatch | PARITY after repair, SOFTWARE-158 ✅ |
| `MipMapLevelOfDetailBias` | Classic XNA | Desktop EasyGL maps it to the GL LOD bias; Software adds it before mip selection/clamping. GL ES has no FNA3D-equivalent control | The same mip contract distinguishes positive/negative bias, trilinear boundaries and bias combined with `MaxMipLevel`; GLES explicitly omits only the unrepresentable bias legs | PARITY for Software and desktop EasyGL after repair, SOFTWARE-158 ✅ |

#### `RasterizerState`

| Property | XNA/Core relevance | EasyGL / Software consumption | Behavioral evidence | Verdict/task |
|---|---|---|---|---|
| `CullMode` | Classic XNA | Both classify post-transform winding and apply None/CW/CCW on indexed and non-indexed draws | `RasterizerState_CullMode`, camera and indexed BasicEffect contracts plus `Software_Culling` | PARITY, SOFTWARE-81/130 ✅ |
| `FillMode` | Classic XNA | Software fills or emits clipped polygon boundaries; desktop EasyGL uses native polygon mode. ES/WebGL still has a partial line-expansion fallback | `Software_Wireframe`, the shared wireframe/depth-bias oracle and EasyGL fill-mode transitions | Software/desktop parity; ES/WebGL gap SOFTWARE-178 ⬜ |
| `DepthBias` | Classic XNA | Both convert the public normalized value for the active Depth16/Depth24 storage | `RasterizerDepthBiasContract` switches backbuffer/2D targets and proves realistic positive and negative bias | PARITY after repair, SOFTWARE-176 ✅ |
| `SlopeScaleDepthBias` | Classic XNA | Both multiply the maximum screen-space depth slope and combine it with constant bias | `Software_DepthBias` and `EasyGL_DepthBias` independently distinguish flat versus sloped geometry, sign and state restoration | PARITY, SOFTWARE-83/176 ✅ |
| `ScissorTestEnable` | Classic XNA | Both gate the stored `ScissorRectangle`, independently of viewport clipping | `Software_Scissor`, `EasyGL_Scissor`, render-target viewport/scissor reset and state transitions; SOFTWARE-310 proves a legal zero-area rectangle rejects all fragments instead of retaining stale state | PARITY after EasyGL repair, SOFTWARE-80/185/310 ✅ |
| `MultiSampleAntiAlias` | Classic XNA | Software selects the standard D3D 4x sample positions and per-sample depth versus one replicated pixel-center decision for triangles, lines and wireframe; desktop EasyGL toggles native multisampling globally | The 4x boundary oracle distinguishes partial triangle, `LineList` and wireframe-edge pixels under true from none under false, proves three true/false/true restorations, makes two covered line/wireframe samples straddle a depth threshold, and rejects the former regular-grid approximation | PARITY after repairs where representable, SOFTWARE-160/315/316/319 ✅ |

#### `BlendState`

| Property | XNA/Core relevance | EasyGL / Software consumption | Behavioral evidence | Verdict/task |
|---|---|---|---|---|
| `ColorSourceBlend` | Classic XNA | Both apply all 13 factors to RGB | `BlendState_Matrix` analytically checks every factor, including constant factor, source-alpha-saturation and opaque-factor bypass | PARITY, SOFTWARE-152/169/170 ✅ |
| `ColorDestinationBlend` | Classic XNA | Both apply all 13 destination factors to RGB | Same 49-case matrix, with non-neutral source/destination values | PARITY, SOFTWARE-152/169/170 ✅ |
| `ColorBlendFunction` | Classic XNA | Both implement Add/Subtract/ReverseSubtract/Min/Max; Min/Max ignore factors | Same matrix explicitly crosses functions with opaque and zero factors | PARITY after repairs, SOFTWARE-169/170 ✅ |
| `AlphaSourceBlend` | Classic XNA | Both apply every legal alpha factor independently of RGB | Same matrix uses different source RGB/alpha and equations | PARITY, SOFTWARE-152 ✅ |
| `AlphaDestinationBlend` | Classic XNA | Both apply every legal alpha destination factor independently | Same matrix and the four preset blend fixtures | PARITY, SOFTWARE-152 ✅ |
| `AlphaBlendFunction` | Classic XNA | Both select the alpha function independently of the RGB function | `BlendState_SeparateFunctions` and matrix Min/Max/opaque discriminators | PARITY after repairs, SOFTWARE-152/169/170 ✅ |
| `BlendFactor` | Classic XNA state and independent device property | Whole-state assignment and `GraphicsDevice.BlendFactor` both reach each renderer without rewriting the other blend fields | `BlendState_BlendFactor`, matrix constant-factor cases and `GraphicsDeviceOrderedClear`'s direct-property clear-boundary discriminator | PARITY, SOFTWARE-152 ✅ |
| `ColorWriteChannels` | Classic XNA | Both mask target 0 after fragment/depth/stencil evaluation and before storage | `Software_ColorWriteChannels`, `EasyGL_ColorWriteChannels`, occlusion ordering and `MrtStockEffectContract` | PARITY, SOFTWARE-120 ✅ |
| `ColorWriteChannels1` | Classic XNA MRT | EasyGL's multi-output shader path proves slot 1. SOFTWARE-357 makes Software execute a distinct COLOR1 through attachment 1's independent mask | A synthetic ps_2_0 writes four distinguishable colors to four 4x MSAA targets; slot 1 preserves only the requested channels and participates in blending | PARITY after compiled-output proof, SOFTWARE-357 ✅ |
| `ColorWriteChannels2` | Classic XNA MRT | Same independent consumption for attachment 2 | The same synthetic program and target readback distinguish slot 2's output and mask from slots 0/1/3 | PARITY after compiled-output proof, SOFTWARE-357 ✅ |
| `ColorWriteChannels3` | Classic XNA MRT | Same independent consumption for attachment 3 | The same synthetic program proves the combined-channel slot-3 mask without redirecting COLOR0 | PARITY after compiled-output proof, SOFTWARE-357 ✅ |
| `MultiSampleMask` | Classic XNA state and independent device property | Both gate coverage samples before depth/stencil/color. Whole-state and direct device assignments remain independent | `MsaaFragmentContract` and the extended device-property matrix prove masks 0, 1, 2, all, restoration and exact quarter-sample resolves | PARITY after repair, SOFTWARE-110/166 ✅ |

#### `DepthStencilState`

| Property | XNA/Core relevance | EasyGL / Software consumption | Behavioral evidence | Verdict/task |
|---|---|---|---|---|
| `DepthBufferEnable` | Classic XNA | Both bypass comparison and writes when false | `DepthState` and `DepthStencilState_WriteEnable` use overlapping near/far layers | PARITY, SOFTWARE-91 ✅ |
| `DepthBufferWriteEnable` | Classic XNA | Both compare without modifying stored depth in read-only mode | `DepthStencilState_WriteEnable` separates disabled writes from disabled testing | PARITY, SOFTWARE-91/130 ✅ |
| `DepthBufferFunction` | Classic XNA | Both implement all eight `CompareFunction` values | `DepthStencilState_CompareFunction` plus `Software_DepthState` cover equal/near/far inputs | PARITY, SOFTWARE-91/130 ✅ |
| `StencilEnable` | Classic XNA | Both gate the entire stencil tuple while leaving depth/color live | `DepthStencilState_StencilEnable` stamps then enables/disables a rejecting gate | PARITY, SOFTWARE-121 ✅ |
| `StencilFunction` | Classic XNA | Both implement all eight comparisons with masked reference/storage | `DepthStencilState_StencilMatrix` exhausts the comparison matrix | PARITY, SOFTWARE-121 ✅ |
| `StencilPass` | Classic XNA | Both execute the clockwise/front tuple after stencil and depth pass | `StencilOps` and 39-check `StencilMatrix` distinguish Keep/Zero/Replace/inc/dec/invert/wrap | PARITY, SOFTWARE-121 ✅ |
| `StencilFail` | Classic XNA | Both execute it only when stencil comparison fails | Same operation/matrix contracts use a deliberate reference mismatch | PARITY, SOFTWARE-121 ✅ |
| `StencilDepthBufferFail` | Classic XNA | Both execute it only after stencil passes and depth rejects | Same operation/matrix contracts place a nearer depth occluder | PARITY, SOFTWARE-121 ✅ |
| `TwoSidedStencilMode` | Classic XNA | Both choose the separate CCW tuple only when enabled | Correctly named differential renders the same genuinely CCW triangle with true/false | PARITY after repair, SOFTWARE-121/372 ✅ |
| `CounterClockwiseStencilFunction` | Classic XNA | Software positive area and EasyGL `GL_FRONT` select the CCW comparison for triangles; lines/points use ordinary state | 46-check matrix makes ordinary Never and CCW Equal visibly differ and proves the D3D9 line exception | PARITY after repair, SOFTWARE-372 ✅ |
| `CounterClockwiseStencilPass` | Classic XNA | Both select the CCW pass operation on CCW geometry | Distinct Replace-versus-Keep exact stencil result | PARITY after repair, SOFTWARE-372 ✅ |
| `CounterClockwiseStencilFail` | Classic XNA | Both select the CCW stencil-fail operation on CCW geometry | Distinct Never/Replace exact stencil result | PARITY after repair, SOFTWARE-372 ✅ |
| `CounterClockwiseStencilDepthBufferFail` | Classic XNA | Both select the CCW depth-fail operation on CCW geometry | Passing stencil plus failed depth produces distinct Replace result | PARITY after repair, SOFTWARE-372 ✅ |
| `ReferenceStencil` | Classic XNA state and independent device property | Whole-state assignment and `GraphicsDevice.ReferenceStencil` both update comparisons immediately; signed public values remain exact while renderers consume the effective low eight bits | `GraphicsDeviceReferenceStencil`, clear/stencil tests, the shared presentation reset gate and `StateNumericFallbackTest.ReferenceStencilUsesTheLowEightBits` (`-1`/255 and 256/0, whole-state plus direct property) | PARITY after out-of-range repair, SOFTWARE-121/326 ✅ |
| `StencilMask` | Classic XNA | Both mask stored and reference values before comparison | `DepthStencilState_StencilMask` uses high-bit noise to distinguish masked from raw compare | PARITY, SOFTWARE-121 ✅ |
| `StencilWriteMask` | Classic XNA | Both merge operation output with the previous byte under the write mask | `DepthStencilState_StencilMask` and matrix tests preserve deliberately excluded bits | PARITY, SOFTWARE-121 ✅ |

#### Invalid enum application fallbacks

Microsoft's state properties retain any cast enum value; their internal native conversion, not the
setter, supplies a default for an unknown value. CNA therefore preserves the raw value in the
caller-visible state object and normalizes only at the common renderer boundary. This distinction
also prevents an EasyGL implementation detail from becoming the cross-renderer contract.

| Property family | Recovered Microsoft fallback | Software / EasyGL evidence | Verdict/task |
|---|---|---|---|
| Four `Blend` factors | `Blend::Zero` | Transparent result from an all-invalid factor tuple over an opaque target; raw getter remains invalid | PARITY after repair, SOFTWARE-325 ✅ |
| RGB/alpha `BlendFunction` | `BlendFunction::Add` | Same exact blend discriminator and direct lower/upper-invalid table checks | PARITY after repair, SOFTWARE-325 ✅ |
| Depth, front stencil and counter-clockwise stencil `CompareFunction` | `CompareFunction::Always` | Farther depth layer wins; opposite-winding stencil triangles both render under the invalid comparison | PARITY after repair, SOFTWARE-325 ✅ |
| Front/counter-clockwise pass, stencil-fail and depth-fail `StencilOperation` | `StencilOperation::Keep` | Both winding regions remain stencil zero and pass a subsequent Equal-zero gate | PARITY after repair, SOFTWARE-325 ✅ |
| `CullMode` | `CullMode::None` | Two spatially separate opposite-winding solid triangles both survive | PARITY after repair, SOFTWARE-325 ✅ |
| `FillMode` | `FillMode::Solid` | Interior probes in both invalid-cull triangles are filled | PARITY after repair, SOFTWARE-325 ✅ |
| `TextureFilter` | `TextureFilter::Linear` | A stretched red/green SpriteBatch texture produces a non-endpoint bilinear sample; the private sampler channel records Linear | PARITY after repair, SOFTWARE-325 ✅ |
| `AddressU`, `AddressV`, `AddressW` | `TextureAddressMode::Wrap` | A 2×2 texture sampled beyond both U/V axes wraps to the exact red texel, the private channel records Wrap, and SOFTWARE-356 proves the same converted W state in compiled volume sampling | PARITY for stock paths and the opt-in compiled volume path, SOFTWARE-325/356 ✅ |

The property audit therefore finds no unrecorded Software-only state omission. SOFTWARE-356 closes
the formerly unresolved `AddressW` path in the opt-in compiled executor. MRT write masks 1-3 still
require real COLOR1-3 output; SOFTWARE-357 now supplies that acceptance proof.
The renderer-wide vertex-stage collections are closed by SOFTWARE-168. EasyGL ES/WebGL's
wireframe limitation remains SOFTWARE-178.

#### State-object identity and disposal

| Question | XNA/FNA evidence | CNA Software/EasyGL evidence | Verdict/task |
|---|---|---|---|
| Does assigning a state retain the same properties and freeze the source? | Microsoft XNA retains the same reference. On first `Apply`, each state checks disposal/parent, sets its parent and `isBound`; every property setter calls `ThrowIfBound`. FNA retains the reference but diverges by leaving it mutable | SOFTWARE-232 shares an internal property payload on C++ assignment. Device state setters and sampler collection assignment bind that payload, so both the source and cached wrapper reject every property mutation. SpriteBatch retains the payload until its authoritative Immediate-Begin or deferred-End apply boundary | property/reference behavior repaired, SOFTWARE-232 ✅ |
| Is a disposed state bindable? | Microsoft `Apply` throws `ObjectDisposedException`; FNA omits this check | SOFTWARE-232 rejects a disposed state presented directly. SOFTWARE-234 propagates disposal through the shared payload, so device/collection aliases and SpriteBatch retention also reject disposal observed after assignment or deferred Begin | application lifetime repaired, SOFTWARE-232/234 ✅ |
| What happens when the disposed object is already the active state? | Microsoft compares the incoming reference before `Apply`: unchanged Rasterizer/Sampler assignment returns immediately; Blend/DepthStencil do likewise unless `BlendFactor`/`MultiSampleMask` or `ReferenceStencil` has dirtied the cache. Drawing continues with the native state already installed | An isolated Microsoft XNA 4.0 probe pins all four no-op cases, the two dirty exceptions, a different-slot sampler exception and a successful draw. CNA's shared payload is its retained-identity discriminator; two explicit dirty flags preserve the independent-property exception | exact active-reference behavior repaired, SOFTWARE-350 ✅ |
| May one immutable state be applied to multiple devices? | Yes. In all four decompiled Microsoft implementations, `_parent != device` reassigns `_parent`, sets `isBound`, and rebuilds any device-derived native fields. It does not throw. A device already caching that exact reference skips `Apply`, even if another device most recently became its parent | Existing CNA payload sharing permits the transition. `CustomStatesCanBeSharedAcrossDevicesAndCachedIdentityIsNoOp` plus `CustomSamplerCanBeReappliedAcrossDevices` prove second-device application, same-property/slot no-op, and different-slot parent rebinding across all four families on Software and EasyGL | PARITY, SOFTWARE-235/350 ✅ |
| Does the public getter retain the assigned resource semantics? | Microsoft and FNA expose the same managed reference, including `Name`, `Tag`, `IsDisposed`, parent device, event subscribers and address identity. Device defaults and sampler slots likewise store the named preset references | SOFTWARE-349 makes the safe member/source wrappers share `Name`, `Tag`, base disposal, disposal delivery and parent-device rebinding, including source lifetime, named device defaults and every untouched `LinearWrap` slot. Public copy construction deliberately remains deep for mutable preset initialization. The getter is still a distinct C++ wrapper address and its event subscription tokens belong to that wrapper | Observable resource semantics repaired, SOFTWARE-349 ✅; literal `ReferenceEquals`/event-token identity remains SOFTWARE-198 ⬜ |

The remaining identity difference is not a Software-versus-EasyGL output difference; it is the
literal managed-reference versus safe C++ value-wrapper boundary. SOFTWARE-232/234 close property
and application lifetime, while SOFTWARE-349 closes every observable base-resource semantic that
can be retained without returning a potentially dangling caller address. SOFTWARE-198 remains
explicit because literal address equality and one physical event-token collection would require a
public ownership-model change.

### Capability truth table

`SupportsCapability` is treated as a stable renderer ability query; whether the *currently bound*
target actually owns depth or stencil is separately proven by target-format tests. That distinction
is necessary because the enum cannot truthfully change when an application switches targets.

| Capability | Software answer | Concrete proof or exact boundary | Verdict |
|---|---:|---|---|
| `ThreeD` | true | Rasterizer, all six stock-effect contracts and declaration/buffer draws render/read back | truthful |
| `DepthStencilBuffer` | true | `DepthState`, `DepthFormat`, target depth/stencil usage and SOFTWARE-181's no-depth/depth/stencil differential | truthful as renderer ability |
| `MultiSampleAntiAliasing` | true | `MsaaFragmentContract`, target/cube resolve and manager-preference output use four independent samples | truthful |
| `MultipleRenderTargets` | true | `MrtStockEffectContract` binds four ordered CPU attachments, clears/finalizes each and gives slot 0 depth ownership; SOFTWARE-357 executes distinct COLOR0-3 through four independent 4x targets, masks and blending | truthful for stock and compiled-output routing |
| `AnisotropicFiltering` | true | `AnisotropicFilterContract` distinguishes the directional multi-tap result | truthful |
| `WireFrame` | true | `Software_Wireframe`, clipping/bias/cull transitions and capability positive arm | truthful |
| `OcclusionQuery` | true | exact 44-check Software contract counts surviving 1x/4x samples after alpha/depth/stencil/mask; shared recovered-XNA profile/lifecycle tests pass | truthful |
| `CustomEffects` | false | Software accepts CNAEXT `ShaderEffect` resource source but does not execute it; EasyGL does. Modern source shaders are outside this campaign | honest false, deferred CNAEXT |
| `Texture3D` | true | All 15 XNA HiDef formats preserve exact full/window/partial-box/mip Set/Get storage; SOFTWARE-356 additionally samples their canonical CPU planes through the opt-in compiled executor | truthful for storage; public compiled-effect exposure remains gated by SOFTWARE-164/165 |
| `MultiStreamVertexInput` | true | declaration, split semantic, FNA collision remapping, shader-active stream ranges, mixed-rate instancing, offsets and frequency contracts execute | truthful after SOFTWARE-320/321 |
| `Instancing` | true | ordinary/16/32-bit/multi-stream/frequency/offset CPU instances rasterize exact pixels | truthful |
| `StencilBuffer` | true | full comparison/operation/mask/two-sided matrix and target format switching | truthful |
| `AdditiveBlending` | true | preset and analytic blend matrix produce exact additive RGB/alpha | truthful |
| `CompiledEffects` | false | valid compiler-produced/authentic bytes still reject publicly; enabled EasyGL supplies the shared reference, while opt-in Software has completed parser, vertex, scalar and quad pixel execution, 2D/cube/volume sampler, MRT/target, SpriteBatch, structured-condition, destination/`TEXKILL` predication, bounded-loop, subroutine and derivative phases | honest false until remaining profile/lifecycle/content closure, SOFTWARE-164/165 |
| `FloatRenderTargets` | true (derived) | Single/Vector2/Vector4 targets retain values above one through raster, blend, resolve, mip and typed readback | truthful |
| `HalfFloatRenderTargets` | true (derived) | HalfSingle/HalfVector2/HalfVector4/HdrBlendable run the same target-domain contracts | truthful |
| `HalfFloatTextureLinearFiltering` | true (derived) | `HalfVector4LinearFilteringMatchesTheAdvertisedCapability` produces the midpoint purple result | truthful after SOFTWARE-190 |
| `ComputeShaders` | false (derived) | no CPU compute executor or storage-buffer contract is advertised; EasyGL support is CNAEXT-modern | honest false, out of scope |
| `IndirectDraw` | false (derived) | no GPU command-buffer execution is advertised; Software's guarded extension routes do not pretend to be indirect hardware draws | honest false, out of scope |

### Inherited/default renderer-method audit

The classic part of `IGraphicsRenderer` was checked method-by-method against both concrete headers.
Software explicitly overrides backbuffer readback; 2D/cube/volume and render-target factories;
single/cube/plural target binding; 16/32-bit buffers; every clear combination; every ordinary,
indexed, user and instanced stock draw route; blend/depth/stencil/rasterizer/MSAA state; U/V/filter,
anisotropy and mip sampler state; blend factor; reference stencil; scissor; viewport; and the
occlusion query factory. The inherited/default cases that remain are classified here.

| Inherited/default family | Why the default is or is not valid | Verdict/task |
|---|---|---|
| `ApplySamplerAddressW` | No XNA stock effect observes it, but SOFTWARE-356 now overrides and consumes it through the opt-in classic compiled volume sampler | repaired inherited-default omission, SOFTWARE-159/356 ✅ |
| `CreateCompiledEffect` / `SupportsCompiledEffects` | The opt-in factory implements parser, state, vertex, scalar/quad pixel, sampler, MRT/target, SpriteBatch, structured-condition, destination/`TEXKILL` predication, bounded-loop, subroutine and derivative phases, but false still correctly prevents exposing the unfinished profile/lifecycle/content surface | honest boundary, remaining SOFTWARE-164/165 closure |
| `SupportsDepthStencil` / `SupportsDepthBuffer` / `SupportsStencilBuffer` | The inherited true answer describes renderer ability. Active `DepthFormat::None`, Depth16/24 and Depth24Stencil8 behavior is selected by the Software framebuffer and tested separately | correct default |
| Window/logical-coordinate/surface callbacks | Software intentionally owns no platform window or swapchain. GraphicsDevice's renderer-neutral logical state and resize/reset behavior still execute | not applicable to headless CPU presentation |
| Display color space | The default sRGB answer is the only presentation space Software exposes | correct default; HDR swapchains are CNAEXT/platform behavior |
| Shader-source, shadow/IBL, compute/storage/indirect/timer methods | These are CNAEXT-modern facilities and their false/null/no-op defaults match the capability answers | out of campaign scope, truthful |

No other inherited no-op, null factory or optimistic classic capability remains unexplained.

### Exact Software `CnaGraphicsTests` skip classification

The current challenge corpus contains 63 skips. Every skip was inspected by name, message and
source-side guard; exactly 15 are evidence of an in-scope missing capability.

| Exact family/tests | Count | Classification and reason |
|---|---:|---|
| `EffectMaterialTest.InheritedCloneCarriesIndependentCompiledState`, `.CarriesTheSourceEffectsParametersAcross`, `.ParametersAreReachableByName`, `.TechniquesAreClonedTooNotLeftEmpty`, `.TheCloneIsIndependentOfItsSource`, `.RetainedParameterTexturesOutliveTheCallersHandle` | 6 | **REAL CLASSIC GAP:** all are gated only by the Software renderer's truthful lack of compiled-Effect execution; SOFTWARE-162..165 |
| `EffectTest.CompiledTextureAccessorsValidateTheReflectedParameterType`, `.CompiledTextureSetterRejectsDisposedAndActiveRenderTargetsBeforeType`, `.CompiledTypedValueSettersValidateReflectedShape`, `.CompiledScalarSettersBroadcastAndRejectArrayParents`, `.CompiledNumericArraySettersRejectStructureParameters`, `.CompiledTypedGettersValidateShapeBroadcastAndConvert`, `.CompiledArrayGettersReturnRequestedLengthAndPackedValues`, `.CompiledNumericArraySettersConvertToReflectedStorageType`, `.AuthenticXna4ShaderStateIdentifiersSurvivePassApplication` | 9 | **REAL CLASSIC GAP:** all require an executable reflected classic compiled Effect; SOFTWARE-162..165 |
| `GraphicsDeviceSubsystemLifecycleTest.AWindowedDeviceTakesExactlyOneVideoReference`, `.AnInitialisationFailureFollowedByASuccessBalances` | 2 | Build-shape/platform restriction: Software needs no video subsystem and this configuration has only one compiled renderer. The renderer-neutral zero-video-reference and lifecycle tests run |
| `GraphicsDeviceWindowDescriptionTest.XnaOwnedWindowIsNonResizableFromCreation` and all five `GraphicsDevicePlatformWindowTests.*` | 6 | Not applicable: Software creates no native window. Reset, viewport, presentation parameters and device lifecycle are covered without inventing one |
| `BuiltInVertexLayoutTest.SdlGpuObjectArrayWithStaticDeclarationRendersIntoATarget` | 1 | Exact SDL_GPU selector; equivalent public Software declaration/layout positives run |
| all 27 `SdlGpuIndexedDrawRangeTest.*` | 27 | Exact SDL_GPU/deferred-command implementation tests. Shared indexed/range/topology/effect-family tests run on Software; one PBR member is also CNAEXT |
| `EasyGlIndexedDeviceLifecycleTest.ThirtyTwoBitDrawDoesNotPoisonNextDesktopContext` | 1 | Exact desktop-EasyGL GL-dispatch regression, SOFTWARE-175; Software has no GL context |
| `InstancedDrawRangeTest.EasyGLHonorsBindingOffsetsAndInstanceFrequency`, `.D3DHonorsBindingOffsetsAndInstanceFrequency` | 2 | Renderer-selector probes. The renderer-neutral Software instancing/multi-stream/frequency matrix runs |
| `PointListPrimitiveTest.SdlGpuPointListRendersExactRenderTargetPixels` | 1 | Exact SDL_GPU renderer selector for `PointListEXT`, which is CNAEXT rather than an XNA `PrimitiveType`; Software now runs every generic extension case, including the exact non-indexed range proof enabled by SOFTWARE-324 |
| `GraphicsAdapterTest.QueryRenderTargetFormatDoesNotPromiseRgba64OnGles` | 1 | GLES-only negative capability test; Software's positive normalized/float target matrix runs |
| `GraphicsDeviceCapabilityTest.WireFrameIsRefusedDeterministicallyOnThisRenderer` | 1 | Negative control for renderers without wireframe. Software runs the positive output contract |
| `Texture3DUnsupportedRendererTest.ConstructorThrowsNotSupportedExceptionWhenRendererLacksTexture3D` | 1 | Negative control for renderers without volume storage. Software runs positive full/mip/box transfers |
| four declaration-refusal tests in `DeclarationGuardTest` | 4 | Negative controls for renderers that infer/refuse layouts. Software translates them and runs `TheTranslatingRendererStillRendersEveryCollidingDeclaration` |
| `DeclarationGuardTest.CustomShaderEffectKeepsItsElementIndexConvention` | 1 | CNAEXT custom-source shader selector; Software truthfully reports it does not execute source |

Thus 48 of 63 skips are exact negative controls, foreign-renderer selectors, platform-only tests or
CNAEXT facilities. The remaining 15 all identify the same acknowledged compiled-Effect gap; no
skip conceals an additional Software stock-effect/state/resource feature.

## Deferred EasyGL example families

The following EasyGL families were explicitly classified rather than silently treated as parity
requirements: bloom, blur, distortion, cartoon, clouds, particle, normal mapping, shadow mapping,
post-processing, custom shader-effect uniforms/layouts/cube/volume sampling, PBR/SkinnedPBR,
modern glTF material/transmission/tangent extensions, storage/compute/indirect draw and HDR engine
facilities are `DEFERRED-CNAEXT`. Existing Software behavior on shared paths must remain stable, but
extending those facilities is not a campaign completion requirement.

EasyGL's real-window resize, actual swap/present interval, native GL context lease, GL handle/state,
driver extension and context-loss tests are `NOT-APPLICABLE-SOFTWARE` except where they reveal a
renderer-independent public GraphicsDevice/resource contract. Resource lifetime is closed by
`SOFTWARE-124` and meaningful public presentation/reset state by `SOFTWARE-127`. Software will not
invent fake window, GPU, or driver behavior.
