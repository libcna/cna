# Experimental release gate

Generated by `tools/c-api/check_release_gate.py` from `tools/c-api/release_gate.json`.

**Release:** CNA C ABI 0.19.0, experimental

This gate governs publishing an **experimental** C ABI release. ABI 1.0 -- the point at which only additive changes are permitted within a major version -- is a separate decision that this gate does not make and must not be read as making. Content Pipeline continuation on 2026-08-30 records 501 public C++ build-time declarations under open `CBIND-117`; it does not add C routes or exempt that experimental surface from the complete-public-API inventory.

## Verdict

**Not ready.** 1 criteria are unmet.

- ❌ **No public C++ symbol is unaccounted for** — 654 public symbols are still unmapped.

This verdict is measured on every run, not written down once. A criterion recorded as met
that stops being met fails the check; so does a criterion recorded as blocked that has
quietly become met, because a gate nobody re-reads is how a project ships something it had
decided not to ship.

## Criteria

| | Criterion | What it requires | Measured |
|---|---|---|---|
| ✅ | **The compatibility matrix is real, current and honest** | Every public header compiles on its own, in every declared language mode of every installed toolchain, and the published matrix matches its declaration. | 23 declared cells across 7 toolchains |
| ✅ | **No unreviewed ABI break** | Every struct layout, field offset, scalar width, constant value and exported symbol is recorded, and a change to any of them arrives as a reviewable diff. | 222 struct layouts and 4051 exported symbols recorded |
| ❌ | **No public C++ symbol is unaccounted for** | The generated inventory has no `planned` row, and every partially mapped row carries an owner-approved disposition: every public declaration is mapped, partially mapped with its subset named and its limitation approved, or recorded as having no C form with the reason. | 654 public symbols are still unmapped |
| ✅ | **A known-limitations matrix a reader can act on** | What the ABI does not do is published, grouped by reason, with no unclassified reason and no deferral owned by a finished task. | docs/c-api/LIMITATIONS.md is current: 15 partially mapped symbols in 9 groups, 464 unmapped in 75 groups |
| ✅ | **A real C application, built the way a consumer builds one** | A complete C program exists, is built from outside the source tree against an installed CNA through find_package, and is run. | hello_cna is built from an installed prefix and run by CApi_InstalledConsumer |
| ✅ | **The package is installable and findable** | `find_package(CNA CONFIG)` resolves from an installed prefix and yields one target, and the package version is the C ABI version. | CNAConfig.cmake, a version file from abi.h, and a CNACApi component |
| ✅ | **The consumer documentation exists** | Consumption, handles, errors, strings and buffers, callbacks and threading, renderer capabilities, ABI versioning and fuzzing are each documented. | 13 documents present |
| ✅ | **The lifetime and byte-facing contracts are measured, not asserted** | Handle lifetime, teardown ordering and buffer boundaries are stressed under a sanitizer, and the surfaces that read caller-supplied bytes are covered against an independent oracle. | stress, exhaustive oracle and fuzz target all present |
| ✅ | **The package carries the native libraries CNA builds** | An installed CNA links and runs with no environment variable: the SDL3 libraries this project builds are installed beside libcna_c_api.so, whose RPATH is $ORIGIN. | SDL3 ships beside the library; the consumer needs no environment variable |
| ✅ | **A static configuration that keeps the same ABI promise** | A static archive exists, exports the same cna_* names as the shared library and no others, and a consumer links and runs against it from the installed package. | CNA::CApiStatic ships and is linked and run by the consumer gate |

## Why each one is here

### ✅ The compatibility matrix is real, current and honest

*Evidence:* docs/c-api/COMPATIBILITY.md, gated by CApiCompatibilityMatrix and CApiHeaderCompatibility

C99 is a verified floor rather than a claim: the matrix found and killed a duplicate typedef that made C99 impossible.

### ✅ No unreviewed ABI break

*Evidence:* tools/c-api/abi_baseline.json, gated by CApiAbiHeaderBaseline and CApiAbiBaseline

The header half needs no build and runs everywhere; the export half runs in each C API build tree.

### ❌ No public C++ symbol is unaccounted for

*Evidence:* docs/c-api/COVERAGE.md, gated by CApiCoverageMatrix

Closed by CBIND-044 on 2026-08-16, reopened the same day by the merge of `next`, and closed again by CBIND-047 and CBIND-049. The second merge of `next` reopened it on 2026-08-17, and CBIND-050 found the first closure had been partly an illusion: 73 mapping rules matched a whole header with `.*`, so 121 symbols the merge added inherited "implemented and tested" without a single C route existing for them. Rules are now pinned to the stable IDs they were reviewed against, which is why this reopened at 176 rather than 55. CBIND-051A through CBIND-051E bound every one of them -- the KHR material extensions on both PBR effects, the glTF import report and its diagnostics, the mesh-part and clip target-space families, the model's cameras, skins, bounds and material variants, and the animation-player additions -- with a single row recorded not applicable, a `CNA::Internal` entry point that reaches a public header. An unapproved partial row still fails this criterion, and so does a rule that grows past what it was approved for. The third merge of `next` (2026-08-17) reopened it again, at 12 rows: the compiled Effect Framework campaign and the IGL renderer. `CBIND-052A` closed 3 of them -- the `IGL` and `PIXIJS` renderer identities and the `CompiledEffects` capability -- and found that a fourth, `TINYGL`, had been recorded implemented since an earlier merge with no C constant existing for it at all, because `CBIND-050` seeded its approvals from the pre-merge tree and so grandfathered that tree's own false claim. The remaining 9 are the `Effect`, `EffectParameter`, `EffectPass` and `EffectTechnique` members the compiled-effect work added or reshaped, and `CBIND-052B` owns them. This criterion is recorded not met until then, which is the whole point of measuring it rather than remembering it. `CBIND-052B` then bound all 9: `Effect::Clone()` and `OnApply()` stopped being pure virtual, which made the C adapter's Clone() override -- correct when there was nothing to inherit -- silently drop a compiled effect's runtime and parameter values, so the override is gone; `GetCompiledRuntimePtr` became `cna_effect_get_is_compiled_ext`, the one fact a C caller can act on, since the runtime object is renderer-owned implementation; the three `EffectParameter` const overloads are answered by the routes that already answer their non-const twins; and the two reflected constructors became `_ext` siblings rather than growing published routes, with their runtime indices readable back. The owner ruled on 2026-08-17 that `EffectPass`'s index should get the public C++ accessor its `EffectTechnique` counterpart already had rather than be recorded as a partial, so the row is bound in full and the partial count stays at 12. The merge of `origin/modern` into `next` on 2026-08-20 expanded the public CNAEXT engine surface and reopened this criterion at 1402 planned rows. The merge integration binds the six closed renderer and capability identities, leaving 1396 planned rows; this criterion remains recorded not met until those rows receive C routes or approved dispositions. Phase B9 (`CBIND-080`-`CBIND-093`) bound all 1,396: the engine layer's foundations, resources and diagnostics; shadows and lighting; clustered forward lighting; PBR materials and transparency; the render pipeline and its post-process chain; HDR output, tonemapping and colour grading; image-based lighting, probes, sky and area-light shading; instancing, LOD, culling, particles and debug drawing; and the GraphicsDevice EXT surface with the last ShaderEffect members and the assembly title. Recorded met on 2026-08-27 by `CBIND-095`, which verified the closure rather than reading the measurement -- the four previous closures each proved a green `--check` is not the same thing as finished. Both builds publish the same symbol set, measured symbol by symbol rather than by count, which is the engine layer's ABI promise. No absolute export count is written here on purpose: one would go stale, and `CApiDocExportCounts` reads any large number near that word as a live claim, which is exactly the confusion this record must not add to. The sixth merge of `next` (2026-08-28) reopened it again, at 506 rows, and this time the reopening is recorded rather than worked off: 460 of the 506 are `CNA::Content::Cnb`, the CNB content format's 23 public headers, and the project owner ruled on 2026-08-28 that binding them is not this pass's work. Phase B10 (`CBIND-103`-`CBIND-111`) is that backlog, sized from the inventory rather than estimated, and `CBIND-112` is the row that closes it. This criterion is recorded **not met** for exactly as long as those rows are open. Nothing is dispositioned to make it read otherwise: every one of the 506 stays `planned` and names an open task, because deferring work and deciding it has no C form are different claims and only one of them is true here. The CNB format is a byte container of PODs and free functions -- among the more straightforwardly bindable things in this repository -- so `not-applicable` would be a false statement about C, not a scope decision.

The sixth merge of `next` reopened it on 2026-08-28 at **506 rows**, 460 of them the `CNA::Content::Cnb` content format. The owner ruled that binding them was a later pass, and this criterion was recorded `not met` for the whole of that deferral rather than being smoothed over -- which is the direction `CBIND-042B` built the gate to fail in. Phase B10 then bound them: `CBIND-106`-`CBIND-111` the container, the document, every asset schema the format defines and the compile path with the loader registry; `CBIND-103`, `CBIND-104` and `CBIND-105` the math, graphics and reflective-reader tails. **Exactly one of the 506 was dispositioned `not-applicable`** -- a `friend` declaration Doxygen reports as a member, named in `CBIND-111` -- and no row was reclassified `partial`. `CBIND-112` verifies that independently; this record is the measurement, not the verification.

The seventh merge reopened it on 2026-08-28 at **15 rows**, and this one ran the other way: `feature/bindings` merged into `next`. Nothing in the 15 came from the bindings branch, which was textually disjoint from everything `next` had added -- all 15 come from three `next`-only commits the branch never saw: `CNA::Content::ObjectDictionaryEXT` (7), `ReflectiveSharedTypeReader` with the builder's `RegisterShared` (6), and `BasicEffect`'s `VertexColorEnabled` pair (2). Each landed in a header an already-closed slice owns, so `owner_task()` handed all 15 to finished tasks -- the second time the same mechanism reached the state `CBIND-079` gates against, two merges apart and in opposite directions, which is the argument for the gate rather than against it. Until this record was corrected the published `COVERAGE.md` and `RELEASE_GATE.md` read **0 planned** and **Ready** on a tree holding 15 planned rows, because both were generated on the bindings branch before it saw those three commits; neither file was wrong where it was written, which is exactly the locally-consistent-and-unread combination this gate exists to break. `CBIND-114` owns the 15 and this criterion is recorded **not met** for as long as they are open. Nothing is reclassified to read otherwise: no row moved to `partial` or `not-applicable`, and no rule was re-approved to absorb the new symbols.

`CBIND-116` then bound all 15: `ObjectDictionaryEXT` through a loader route and eleven reading routes, `ReflectiveSharedTypeReader` and `RegisterShared` through `cna_reflective_type_reader_builder_register_shared`, and `BasicEffect`'s `VertexColorEnabled` pair through the routes that already answered for the public field beside it. **Not one row was reclassified**: 0 moved to `partial` and 0 to `not-applicable`, the counts for both are unchanged at 15 and 460, and the two new rules name only the 15 stable IDs they were reviewed against. Binding them also corrected two documentation defects the work measured rather than inherited: the canonical `ReflectiveTypeReader` header and this API's own `CBIND-105` text both said a second registration under a canonical name replaces the first, and the reader table keeps the first instead.

### ✅ A known-limitations matrix a reader can act on

*Evidence:* docs/c-api/LIMITATIONS.md, gated by CApiLimitations

This is the document a consumer reads to decide whether to adopt the ABI at all.

### ✅ A real C application, built the way a consumer builds one

*Evidence:* modules/c-api/examples/c/hello_cna.c, gated by CApi_InstalledConsumer

A program that only builds inside this repository would prove nothing about the package.

### ✅ The package is installable and findable

*Evidence:* modules/c-api/cmake/CNAConfig.cmake.in and the CNACApi install component

Before CBIND-041 the module installed a targets file and no package config, so this criterion was silently false.

### ✅ The consumer documentation exists

*Evidence:* docs/c-api/

CONSUMING.md is the entry point for somebody who has never read this repository.

### ✅ The lifetime and byte-facing contracts are measured, not asserted

*Evidence:* CApi_StressSmoke and CApi_Utf8Oracle, plus tests/fuzz/StringViewFuzz.cpp

The stress suite found a heap use-after-free on its first run; that is what this criterion is for.

### ✅ The package carries the native libraries CNA builds

*Evidence:* the CNACApi install component, proved by CApi_InstalledConsumer

Ruled on by the project owner on 2026-08-16: the package ships the SDL libraries CNA builds. When the optional video backend is enabled, FFmpeg stays a system dependency for reasons that are not convenience -- redistribution terms, soname freezing against security updates, and the transitive libraries a distribution build was linked against. `CNA_ENABLE_VIDEO=OFF` now omits FFmpeg while preserving the ABI. CApi_InstalledConsumer passes no `-rpath-link` and no `LD_LIBRARY_PATH`, so a regression here fails a test rather than surprising a consumer.

### ✅ A static configuration that keeps the same ABI promise

*Evidence:* tools/c-api/generate_static_archive.py and CNA::CApiStatic, proved by CApi_InstalledConsumer

Ruled on by the project owner on 2026-08-16: build it. The objection that stopped it before was real -- an archive carries every object it swallowed -- so the archive is not simply ar'd together: the closure is partially linked into one relocatable object and every symbol that is not part of the ABI is localized, with the build failing outright if any non-cna_* symbol other than an unlocalizable STB_GNU_UNIQUE C++ static survives. The same hello_cna.c is built both ways from the installed package and both are run.

Regenerate with:

```bash
python3 tools/c-api/check_release_gate.py --write
```
