# CNA C API 0.1 Feature Matrix

## Scope

This matrix describes the implemented experimental `0.1.0` C ABI. It is a usable initial 2D
vertical slice, not complete CNA or XNA coverage. [`COVERAGE.md`](COVERAGE.md) remains the
source-symbol mapping record and will grow into the complete public-surface inventory.

All declarations are available through the C17 umbrella header `CNA/C/cna.h`. Public structures
use fixed-width C fields and versioned structure prefixes where evolution is expected. All
fallible calls return `CNA_Result`; no C++ or Sharp Runtime type or exception crosses the ABI.

## Implemented surface

| Family | Implemented operations | Current boundary |
|---|---|---|
| ABI | Encoded ABI query through `cna_get_abi_version` | Experimental version `0.1.0`; no stability promise before the release gate |
| Errors | Per-thread result, category and UTF-8 diagnostic count/copy | Diagnostics are replaced by the next failed ordinary call; programs branch on `CNA_Result` |
| Gamepad capabilities | Controller type identities and the complete capabilities value: 35 readable and writable flags plus the type, a canonical-default initializer, and a per-slot read on the game thread | A value, not a handle, because the canonical type is a copyable snapshot; an empty slot reports the canonical disconnected value rather than failing; connection always agrees with the gamepad state snapshot |
| Gamepad values | Button set and directional pad over the existing button mask, thumbstick and trigger values, and the snapshot's constructors, component projections, comparisons, hash and text | One representation per value — the pad is the mask's four directional bits and the analog values are the snapshot's analog block; the thumbstick constructor square-clamps and the trigger constructor clamps; trigger equality is an epsilon comparison; the snapshot carries a single button mask, so a supplied pad is merged into it |
| Gamepad device queries | Vibration and trigger vibration, light bar, gyro and accelerometer, device player index get/set, power info, button labels, GUID/name/path/serial count-copy, firmware version, Steam handle, connection state, touchpad count/finger-count/finger, and the pure axis dead-zone exclusion | Every query needs the game thread of a running game, because CNA is event-driven; an empty slot answers false, zero, empty or `Unknown` rather than failing; a sensor query reports availability separately from its reading, so "no sensor" is not an error |
| Keyboard values and queries | Snapshot construction from an explicit key set, the key-state indexer, comparison, hash and text; per-slot capture, modifier state, scancode translation and both key-name families with reverse lookup | An out-of-range key is refused rather than silently dropped, unlike the canonical constructor; every slot reports the same snapshot because CNA has one keyboard; an unknown key name answers `CNA_KEY_NONE`, not a failure |
| Mouse values and queries | Snapshot construction, comparison, hash and exact text; window handle, position, relative mode, capture, global position and warp; the static clicked event as an owned registration plus a raise route and the test reset | Construction takes a button bit set, not five ordered button arguments; the window handle is an opaque value the API never dereferences; an unsatisfiable request answers false rather than failing; the clicked subscription belongs to the process, and the reset drops it so a later release is a no-op |
| Mouse cursors | Owned cursor handles: the empty cursor, all twelve stock identities, a texture-derived cursor, disposal, release and making one active | A stock handle is a borrowed view of a process-lifetime singleton, so releasing it never frees the shared cursor and disposing it is a no-op; no `SDL_Cursor` is ever exposed, so the raw-pointer constructor and accessor have no C mapping |
| Text input | All three `TextInputEXT` events as owned registrations with one shared release route, a raise route for each, the opaque bound window handle, plain and typed start, stop, the activity and screen-keyboard queries, the IME input rectangle and the test reset | The subscriptions belong to the process, so none takes a game handle; composition text and candidate lists cross as UTF-8 views borrowed only inside the callback; an above-BMP code point arrives as two surrogate calls; `start`/`length` and `selected` are forwarded verbatim exactly as the canonical dispatch does; an undefined input-type identity is refused rather than falling back to plain text; with no window bound every activation is a successful no-op and every query answers false |
| Touch and gestures | The gesture type bit set and the gesture sample value; touch location construction, pressure-blind comparison, hash and text; touch collection construction and the whole mutation surface over the existing fixed snapshot; every `TouchPanel` static including display metrics, orientation, enabled gestures, the gesture queue, slot writes, the frame advance and the test reset | One representation per value — the collection is the existing eight-slot snapshot, so an append beyond it is refused rather than dropping a touch; equality, the hash and the text all ignore the pressure extension and the text carries only the position; reading an empty gesture queue is refused, not answered with a default sample; a raised touch event feeds gesture detection rather than the snapshot and is dropped entirely until a display size is published; the reset clears the display metrics and window handle too |
| Haptics | Device enumeration with identifier and name, opening standalone or from a joystick or the mouse, joystick and mouse capability probes, and an owned device handle carrying open state, name, capabilities, effect support, the simple rumble, the create/update/run/stop/free effect lifecycle, effect status, gain, autocenter, pause, resume, disposal and release; plus the feature bit set and the direction, effect and capability values with their comparisons | Opening never fails for want of hardware — it returns a real handle that reports itself closed, and every route on a closed device answers false, zero or -1 rather than failing; rumble strength, gain and autocenter are passed to the platform unvalidated; a custom waveform travels beside the effect value rather than inside it, so the value owns no heap; the device name is read through count/copy rather than living in a POD; freeing an unknown effect is a successful no-op; no `SDL_Haptic` is ever exposed |
| Raw joysticks | Device enumeration with descriptor and name, capabilities and both device strings by identifier, an owned captured snapshot with a count/copy pair for axes, buttons, hats and trackballs plus snapshot comparison and release, the type and hat-position identities, the descriptor and capability values with their comparisons, both hot-plug events as owned registrations with one shared release route and a raise route each, and the test reset | The snapshot is an owned handle rather than a fixed value, because the canonical arrays have no maximum and one capture must answer from one instant; a hat position is an identity, not a composable bit set; an unconnected identifier answers with the disconnected defaults and empty strings rather than failing, and a power percent of -1 means unknown rather than empty; the device name and GUID are read through count/copy rather than living in a POD, so the capability comparison takes both strings; the enumeration reports the devices the native layer holds open and is valid only until that set changes; trackball motion is relative, so capturing consumes it; no `SDL_Joystick` is ever exposed |
| Host sensors, device enumeration, clipboard and power | The sensor kind identity, the sensor and input-device descriptor values with their comparisons, sensor enumeration and both host readings, mouse/keyboard/touch-device enumeration with names, all four device hot-plug events as owned registrations with one shared release route and a raise route each, the test reset, clipboard read/write/presence, and host power state with remaining runtime and charge | Both sensor reads report availability separately and leave the reading untouched when nothing answered; device enumeration is metadata only, because XNA input state stays merged across devices, and its identifier is 64-bit where the sensor and joystick ones are 32-bit; the clipboard setter reports that the request was made, not that the platform honored it, and the clipboard is process-external state another application can change between two calls; a power value of -1 means unknown rather than none left |
| Core extensions | One route per canonical logger static, the process-wide minimum level, the compile-time platform and desktop operating system, the compile-time renderer identity and name, and both backend classifications for any of the 46 public renderer identities | Nothing here needs a runtime, a game or a device; the log threshold is process-wide static state a consumer should restore; the desktop query refuses on a non-desktop platform instead of returning a fallback; an identity is a name, not a capability claim |
| Game | Create one active game, run one frame, run until exit, request exit, clear, set UTF-8 window title and destroy | Creation-thread only; run and destroy are non-reentrant; a second active C-owned game is refused |
| Lifecycle | Load, update, draw, unload and exit callbacks with copied callback table, context and optional copied UTF-8 failure message | Callbacks are synchronous; a callback failure becomes `CNA_RESULT_CALLBACK` |
| Timing | `CNA_GameTime` snapshots with total/elapsed 100-nanosecond ticks and running-slowly flag | Present only for update and draw callbacks |
| Graphics discovery | Borrow callback-scoped device; query renderer identity/name, maximum 2D texture dimension and 13 capability flags | Device handle expires when the callback returns; renderer selection remains compile-time |
| Graphics resources | Generic device/disposed queries, exact UTF-8 Name/ToString count-copy, C-owned 64-bit tag, explicit disposal and synchronous disposing subscription for supported resource handles | Supports Texture2D, Texture3D, TextureCube, RenderTarget2D, RenderTargetCube, VertexDeclaration, VertexBuffer, IndexBuffer and Effect; tags do not expose native `System::Object*`; creation-thread only |
| Graphics states | Complete BlendState, DepthStencilState, RasterizerState and SamplerState POD descriptors; native presets; device get/set; 16 pixel and vertex sampler slots | Applying an otherwise valid state may fail when the compiled backend cannot represent it; Effect and transform-matrix SpriteBatch variants remain planned |
| Display and presentation | DisplayMode initialization/equality; adapter metadata, UTF-8 strings, modes, preferences, profile/format queries; PresentationParameters init/clone/bounds/device round-trip | Adapter indices are point-in-time; native monitor/window handles are deliberately not disclosed; active-device adapter refresh returns `NOT_SUPPORTED` |
| Backbuffer | Query logical width, height and format; count/query then copy the complete RGBA8 backbuffer | Draw-time use; HEADLESS honestly returns `CNA_RESULT_NOT_SUPPORTED`; no region or non-Color readback |
| Surface formats | Stable identities and Texture block/byte-size/alignment helpers for all 27 currently canonical `SurfaceFormat` values | HEADLESS and SDL_RENDERER create Color Texture2D resources; the larger Skia-native promoted set is exposed but not yet C-runtime-tested |
| Texture / Texture2D | Standalone and game-owned default/device/file/RGBA8/CPU-only/encoded-memory creation; common and 2D properties; all 18 native typed full/mip/rectangle transfer representations; exact type/storage queries; PNG/JPEG count/copy and file output; destroy | Native format/backend gates remain authoritative; SDL_RENDERER rejects compatible mip uploads above level zero as `NOT_SUPPORTED`; streams and renderer/weak pointers do not cross the ABI |
| Texture3D / TextureCube | Owned Color resource creation; dimensions/common state; complete Color full/mip/box or six-face/rectangle transfer descriptors; raw Texture3D byte upload; exact type queries; DDS memory cube factory; RenderTargetCube inheritance; destroy | HEADLESS and SDL_RENDERER truthfully reject Texture3D creation and cube transfer/storage as `NOT_SUPPORTED`; cube construction itself may succeed without storage as in native CNA; renderer pointers and streams do not cross the ABI |
| Render targets | Create/query/destroy owned 2D and cube targets; singular/MRT binding; active binding count/copy; RenderTarget2D accepted by Texture2D routes | Backend storage availability is explicit; HEADLESS binding returns `NOT_SUPPORTED`; bound targets cannot be destroyed; current ContentLost invariant is false |
| SpriteBatch | Create/destroy, begin with all five sort modes or explicit blend/sampler/depth/rasterizer state, submit an array of rectangle-based textured commands, draw text through one command covering all six canonical overloads, submit an indexed 2D mesh, exact type name, end | Text retains its font atlas until a successful end; mesh submission requires Immediate mode and reports `NOT_SUPPORTED` on a backend without it; no transform-matrix or Effect begin variant yet |
| Occlusion queries | Own a game-child query; begin/end; completeness and pixel count; live-renderer state; generic graphics-resource name/string/tag/device/disposal routes | Creation refuses a backend without the occlusion-query capability; some backends report only zero or one visible pixels |
| Sprite commands | Destination/source rectangles, RGBA tint, finite rotation/origin/depth and both flip bits | Every command is validated before native submission; every texture must belong to the same game |
| SpriteFont | Build from a retained Texture2D/RenderTarget2D plus copied UTF-16 glyph/rectangle/kerning table; properties; character copy; UTF-8 measure; destroy | Caller creates the atlas/glyph table; no SpriteBatch text-draw command yet; source texture must outlive the font |
| Keyboard | Fresh 256-key snapshot; all 160 canonical `Keys` names; local down/up, pressed count and ascending count/copy helpers | Non-player `Keyboard::GetState()` only; snapshot capture is creation-thread only, copied POD queries are thread-independent |
| Mouse | Fresh logical position, vertical/horizontal wheel and five-button snapshot | Read-only capture; cursor positioning/capture/events remain planned |
| Gamepad | Four player slots; default and explicit three-mode dead-zone capture; connection/packet, all 31 button bits, sticks/triggers; local combined-button and pure normalization helpers | Disconnected slots succeed with rest snapshots; capabilities, vibration and extensions remain planned |
| Touch | Current capabilities and fixed-capacity eight-location collection with previous location and pressure; local find/previous helpers | Platform absence succeeds as disconnected/empty; display, gestures and events remain planned |
| Content | Own a content manager; UTF-8 root, resolved asset path and normalized cache key count/copy; built-in loader registration; service-provider presence; graphics-device get/set; unload cache; manifest and `.xnb` reader-usage snapshots as count plus indexed accessors; load owned Color Texture2D, TextureCube and SoundEffect handles; destroy | Create from a callback-scoped device; returned resources outlive manager unload/destruction; a service provider is a Sharp Runtime object so only its presence is observable; the canonical sound loader reports a missing or unreadable audio file as `NOT_SUPPORTED` while cube decoding reports `IO`; the reader-usage summary is sorted by reader name so an index is addressable; the XNB reader pipeline and remaining asset types are not mapped yet |
| Compiled-asset readers | Own a reader over a storage stream with optional manager; asset name/version/platform; canonical fixed matrix/quaternion/vector/color/bounding-sphere reads; type-reader-table and shared-resource passes; both bounds checks; capacity-checked exact-byte read; own type readers from the registry or as unsupported placeholders; registry probe/create/clear/re-register | A reader borrows its stream, so the stream cannot be closed while it lives and destroying the reader closes it; the untyped read routes report presence only because a type-erased C++ object has no C representation; typed reader templates and factory registration are C++-only extension points with no C route |
| Networking values and packets | All five session identity enumerations; the quality-of-service value with both canonical factories; own session-property lists with optional elements, the full list contract and owned enumerators; own packet read and write buffers with one route per canonical read and write; read back the join error a failed join carried | No socket, gamer or session route yet; delivery options are discrete identities, not a bit set; the canonical color write/read asymmetry is preserved rather than corrected; two `_ext` routes move packet bytes because the canonical API never exposes them; the join-error record is cleared by any later failure on the thread |
| Session discovery and join | Both searches and both asynchronous searches; join and asynchronous join from a discovered session; all four invited-join routes; three asynchronous creations | Every fake-async pair is one synchronous call that still invokes the completion callback; the callback receives only the caller's context because no operation object crosses the ABI; the asynchronous creations substitute the canonical gamer limit and so differ from the synchronous ones; a local-only search type is refused and only a SystemLink search reaches real discovery |
| Session events | Subscribe to all nine instance events and the static accepted-invite event with typed callbacks receiving the matching event descriptions; one shared unsubscribe | A payload gamer handle lives only for the callback that receives it; an instance registration holds a weak reference, so releasing it after its session is gone is a no-op; gamer-joined replays for every gamer already present at subscription time; nothing canonical raises the leaderboard or invite events yet |
| Network sessions | Own a session through all three canonical creation overloads; both limits as constants; every state property and setter; all four rosters as count plus indexed borrowed views; copied property list; exact type name; pump; local-gamer add; identifier lookup; ready reset; start and end; queued events, remote-gamer add and remove; diagnostic counters; dispose and release. A minimum signed-in-gamer surface comes with it | A session needs at least one signed-in gamer, because the canonical constructor selects its host from them; start and end only queue a state change until the pump runs; a packet event on a non-`SystemLink` session is a canonical no-op; borrowed gamer views block the session's release; a remote gamer is retained because the canonical add does not take ownership |
| Local gamers | Refuse a handle that is not a local gamer; data-available and backing signed-in-gamer queries; declared-no-op voice and party-invite routes; all three `ReceiveData` overloads with a borrowed sender view; all six `SendData` overloads including both packet-writer forms; the two CNAEXT queue routes | A local gamer reuses `CNA_NetworkGamerHandle`, because it is a network gamer; the offset receive consumes its packet before rejecting an out-of-range offset; the packet-reader receive always reports zero bytes; `EnableSendVoice` and `SendPartyInvites` are canonical no-ops |
| Discovered sessions | Own a discovered session from a versioned description; every scalar; count/copy host gamertag and connect address; connect port and session type; copied quality-of-service and property-list values; both equality operators; own a read-only collection with count, indexed copy-out, disposal state and dispose | Only the round-trip sample can be carried into a quality of service, because that is all the canonical type accepts; property lists and collection elements are copied, never aliased; live discovery and the session object itself are a later slice |
| Network gamers and machines | Own gamers with every canonical flag, identifier, round-trip time and owning session; the four CNA extension setters; own machines with a counted roster and borrowed gamer views; seven fixed event descriptions with validating initializers | Session handles arrive with the session slice, so only the no-session form is accepted today; a machine's roster is populated only by a session; the canonical roster-removal placeholder reports `NOT_SUPPORTED`; a copied machine is independent, which is observationally identical because the canonical machine has no mutator |
| Storage | Own storage devices through all four selector routes; free/total space and connection state; process-wide `DeviceChanged` registration; own containers with display/type-name count-copy, disposal state, owning device and `Disposing` registration; directory and file create/exists/delete plus count-copy listings with glob or all-name patterns; own file streams with read/write/seek/position/length/set-length/flush/capability queries; delete whole containers; application name and storage root | The fake-async `Begin`/`End` pairs are one synchronous call each that still invokes the canonical completion callback; `System::IO::Stream` and `System::IAsyncResult` never cross the ABI; streams close before containers and containers are destroyed before devices; the canonical container ignores the share selection; an escaping container title is refused |
| Audio | Probe real playback availability; create owned mono/stereo PCM16LE effects; duration; owned instances; play/pause/resume/immediate or release-tail stop; volume/pitch/pan/loop/state; destroy | Availability is a successful versioned snapshot; creation-thread control; instances before effect before game; no device maps resource creation to `NOT_SUPPORTED`; no file/content, streaming, microphone, XACT or 3D route yet |
| Media identities, visualization and sources | The playback-state, media-source-kind and video-soundtrack identities; the fixed 256-value visualization data with its zeroed initializer and type name; and the media-source enumeration with each source's kind, display name and type name | The media-source-kind identity keeps its canonical 0/4 gap, so it has no dense maximum and is validated by membership; the canonical source enumeration allocates raw pointers its caller must free, so C owns and destroys that list inside every call and an index is a point-in-time value with nothing to release; the canonical string conversion returns the display name unchanged, so one count/copy pair is both; songs, the media library, the player and video remain planned |
| Songs and song collections | Owned song handles from both constructors and the URI factory; display name, file-handle text and type name; duration, play count, rating, track number, protection, rating presence and disposal state; disposal, release, path equality and hash; and owned song collections with count, indexer, disposal state, disposal, release and type name | Several handles may share one song, and a collection retains every song it was given, so releasing a caller's handles cannot dangle an element; an omitted song name stays empty, contradicting the canonical constructor's own comment, which this ABI follows deliberately; equality and the hash come from the file path rather than handle identity; a missing file is `CNA_RESULT_IO` and a non-`file` URI scheme is `CNA_RESULT_INVALID_STATE`; collection disposal empties the collection but leaves its songs alive; iterators have no C counterpart |
| Media library catalog | Owned media libraries from both constructors; disposal state, disposal, release, media-source kind and name, type name, and the song, album, artist, genre and playlist collections; borrowed album, artist, genre and playlist handles with names, disposal, equality, hashes and type names; album artist/genre/duration/art/thumbnail; artist and genre album collections; playlist duration; song album/artist/genre back-pointers | Every entity is a borrowed view that keeps its library alive, so the library handle may be released first and no parent-before-child order applies; the source constructor borrows its source, so C destroys the whole enumerated list before returning; album equality pairs the name with the artist rather than using the name alone; optional entities report availability instead of failing; album art crosses as bytes with no stream in the ABI, and the thumbnail is the same image; collection disposal empties the collection while its elements keep answering; pictures and the player remain planned |
| Values and 3D identities | ABI layouts for Color, Point, Vector2/3/4, Quaternion, Matrix, Plane, Ray, bounding volumes, CurveKey, all 17 PackedVector raw values, VertexElement and all seven built-in `VertexPosition*` values; all MathHelper constants/scalar operations; complete Point/Rectangle, Vector2/3/4, Quaternion, Matrix, Plane, Ray, bounding-volume, CurveKey, CurveKeyCollection, Curve, Color, PackedVector, built-in vertex equality/hash/text and VertexElement operations; all 141 named colors; all three half-conversion routes; canonical built-in GPU stride/element queries; curve loops/evaluation/tangents; transforms/factories/decomposition, caller-capacity corners and explicit optional intersections; stable containment, plane, curve, packed-format, buffer, index, primitive, SetData, vertex and effect-parameter class/type identities | The canonical BoundingFrustum boundary-origin ray branch is callable but returns `NOT_SUPPORTED`; remaining 3D/effect resource and draw behavior remains planned |
| Vertex declarations, buffers and bindings | Own empty, computed-stride or explicit-stride declarations; own static/dynamic buffers; query metadata/type/declaration; complete seven-type count/window SetData/GetData, raw upload, four dynamic option overloads, ContentLost registration and fixed default/initialized binding descriptors | Buffers are game children and creation-thread affine; WriteOnly readback is `NOT_SUPPORTED`; ContentLost is currently never raised; binding tokens are generation/type validated when later consumed by graphics-device draw state; no draw submission yet |
| Index buffers | Own static/dynamic 16- or 32-bit buffers; query metadata/type; complete count/window SetData/GetData; all dynamic streaming options; ContentLost registration | Buffers are game children and creation-thread affine; width conversion is rejected; WriteOnly readback is `NOT_SUPPORTED`; ContentLost is currently never raised; no indexed draw submission yet |
| Effects, metadata, parameters, techniques and passes | Own base-adapter, EffectMaterial, ShaderEffect, SpriteEffect, BasicEffect, AlphaTestEffect, DualTextureEffect, EnvironmentMapEffect, SkinnedEffect, ColorMatrixEffect, PbrEffect and SkinnedPbrEffect game children; same-type clone/dispose/apply; reusable matrix/fog/light interfaces; stable directional lights; complete concrete material, color-transform, five-slot PBR and 72-bone skinning state; current parameter/technique/pass views; exact type/source strings; shader validity/uniforms/textures; fixed parameter identities and complete metadata/value/collection operations | Compiled XNA `.fx` bytecode is explicit `NOT_SUPPORTED`; custom-shader renderer availability is queried; renderer pointers stay private; descendants retain the native effect/game/texture lifetime after parent-handle destruction |
| Model bones | Own default/named bones; exact UTF-8 names, signed indices and copied transforms; add retained child relationships; query optional parent and live children; create empty collections; count/index/find/contains | Standalone and hierarchy views are creation-thread affine; self/ancestor cycles are rejected; skinned animation data/player aggregation remains planned |
| Model mesh parts | Own default/parameterized parts; exact signed geometry state; optional retained Effect, VertexBuffer, IndexBuffer and morph-data assignments; opaque 64-bit C tag; retained snapshot collections with count/index aliases | Creation-thread affine; assigned graphics resources must share one device and cannot be disposed/destroyed while retained; morph data uses the native object tag independently of the C tag |
| Model meshes and collections | Own unnamed/named game-child meshes with bounding spheres, retained parts/optional parent bones, opaque tags and capability-gated Draw; live part/effect views; retained mesh snapshots with count/index/find/contains | Parts belong to one live mesh and safely detach when it expires; effects preserve native duplicate-add/first-remove identity behavior; SDL_RENDERER Draw explicitly returns `NOT_SUPPORTED` |
| Models | Own empty or device aggregate models; retained immutable bone/mesh views, arbitrary root and nullable mesh parents; opaque tag; C context/release-callback resource ownership; local/absolute bulk transforms and Draw | Transform arrays are atomic on capacity failure; non-empty Draw requires native 3D capability; model/collections retain all aggregate objects transitively |
| Morph-target extensions | Deep-copy fixed keyframe/track/target descriptors into owned data; copy nested fields; replace weights/tracks; blend vertex bytes; evaluate LINEAR/STEP/Hermite tracks; retain data on ModelMeshPart and upload weights | Strides are validated to 32/52/56; nested counts and ascending times are checked; outputs are capacity-atomic; upload requires a sufficient supported VertexBuffer |
| Graphics-device values and identities | Fixed `CNA_Viewport` POD with all three constructors, direct property fields, aspect ratio, bounds get/set, title-safe area, project/unproject and exact UTF-8 string count/copy; stable `CNA_ClearOptions`, `CNA_GraphicsDeviceStatus` and `CNA_Unsupported3DGraphicsCallBehavior` identities; native ClearOptions/SpriteEffects operator overloads expressed as C bitwise operators | Pure value operations with no handle, allocation or thread affinity; the canonical default viewport keeps `max_depth` 1 rather than being an all-zero structure |
| Graphics-device state and events | Disposal state, status, adapter index, profile, scissor/viewport/blend-factor/multisample-mask/reference-stencil get and set, exact UTF-8 type name, and owned subscriptions for Disposing, DeviceLost, DeviceReset, DeviceResetting, ResourceCreated and ResourceDestroyed | Borrowed device expires with its callback; the game owns device creation and disposal, so `cna_graphics_device_dispose` is an explicit `NOT_SUPPORTED` result; a created resource and a destroyed resource's tag are reported as presence only; adapter indices are point-in-time; clear/present/reset, binding, draw submission and the `graphics-ext` post-process family remain planned |
| Graphics-device frame control and binding | Three clear overloads, present, both reset routes with a nullable adapter index, windowed back-buffer readback, and the complete vertex/index buffer binding set | Reset preserves the renderer-private window handle; readback capacity and region size are decided in C as `BUFFER_TOO_SMALL`; a binding applied by canonical CNA code reports an invalid handle; draw submission remains planned |
| Graphics-device frame control and binding | Three clear overloads, present, both reset routes with a nullable adapter index, windowed back-buffer readback, and the complete vertex/index buffer binding set | Reset preserves the renderer-private window handle; readback capacity and region size are decided in C as `BUFFER_TOO_SMALL`; a binding applied by canonical CNA code reports an invalid handle; draw submission remains planned |
| Graphics-device draw submission | Buffered non-indexed, indexed and instanced draws; both user-primitive routes covering all twenty-nine canonical overloads through vertex-source, declaration and index-width dimensions; topology vertex-count helper | Built-in vertex sources are converted because the native structures embed a polymorphic Color; a raw stream always needs a declaration; every route refuses a backend without 3D support as `NOT_SUPPORTED` |
| Graphics-device extensions | Tracked-resource count, depth-test/blend/depth-write toggles, graphics profile, context recovery, debug marker, unsupported-3D policy, current effect, multisample renderer rebuild | Pipeline-state toggles are capability-gated; an assigned current effect is kept alive by the C API; resource-notification hooks and the renderer object have no C route by design |
| Graphics extensions | Availability query; seven extension identities; `CNA_PbrMaterial` and `CNA_RenderPipelineSettings` values with canonical defaults; owned CRT, depth-reduction and ASCII post-process effects | Values work in every build; the effect routes report `NOT_SUPPORTED` when the opt-in extension layer is absent, so no exported symbol changes with the build option; PBR texture slots are non-owning |
| Texture sampler collections | Read, bind and unbind any owned texture across 16 pixel and 16 vertex sampler slots | Slots are addressed by stage and index rather than by a collection object; a slot filled by canonical CNA code reports as bound with an invalid handle; binding stores no ownership because texture destruction unbinds itself; a render-target-bound texture cannot be sampled |

The complete exported-function list is mechanically checked so the shared library exposes only
`cna_*` symbols. Pure C and C++ header translation tests freeze the implemented value layouts and
numeric identities.

## Renderer evidence

The C API reports the backend compiled into CNA; it does not choose or emulate a renderer at run
time. The initial slice currently has this automated evidence:

| Behavior | HEADLESS | SDL_RENDERER | Other renderer identities |
|---|---|---|---|
| Game lifecycle, errors, handles and threading | Tested | Tested | Not yet C-tested |
| Renderer identity/name/capability discovery | Tested | Tested | Enumerated, not yet C-tested |
| Color Texture2D upload and exact readback | Tested | Tested | Not yet C-tested |
| Content Texture2D load, cache/unload lifetime and exact decoded pixel | Tested | Tested | Not yet C-tested |
| Keyboard, mouse, gamepad and touch capture/thread rules | Tested | Tested with SDL dummy video | Other platform/device combinations not yet C-tested |
| Pure gamepad dead-zone/button and touch-location helpers | Tested | Tested | Renderer-independent copied POD operations |
| Audio availability, PCM creation, mixer transitions, threading and parent order | Available snapshot plus SDL dummy audio tested | Available snapshot plus SDL dummy audio/video tested | Audio behavior is renderer-independent; physical devices not C-tested |
| Unavailable audio device and shutdown after repeated probes/creation failures | Successful unavailable snapshots in isolated invalid-driver process | Successful unavailable snapshots in isolated invalid-audio/dummy-video process | Exact driver availability is platform-specific |
| SpriteBatch validation, state and lifetime | Tested | Tested | Not yet C-tested |
| Graphics state values, device state/sampler round-trip and explicit-state SpriteBatch begin | Tested | Tested with supported opaque blend state; unsupported backend state paths preserved | Other renderer identities not yet C-tested |
| Display modes, adapter snapshots/queries and PresentationParameters | Tested | Tested with SDL dummy video | Native handle disclosure and unsafe live refresh are explicit callable limitations |
| RenderTarget2D/RenderTargetCube creation and binding contract | Creation/property/lifetime tested; bind returns `NOT_SUPPORTED` | Real RenderTarget2D binding and unavailable cube path tested | Capability and native allocation remain backend-specific |
| SpriteFont glyph/properties/UTF-8 measurement and source-texture lifetime | Tested | Tested | Renderer-independent layout/measurement over a game-owned texture |
| Built-in vertex values, strings and packed GPU declarations | Tested | Tested | Renderer-independent copied POD and declaration operations |
| Owned vertex declarations and binding descriptors | Tested | Tested | Renderer-independent copied arrays and standalone handle lifetime |
| Complete static/dynamic vertex-buffer contract | All typed/raw transfers, metadata, WriteOnly, disposal/events and lifetime tested | Native no-3D capability and atomic creation refusal tested with SDL dummy video | Successful buffer behavior remains native-backend-specific beyond HEADLESS |
| Complete static/dynamic index-buffer contract | Both widths, count/window transfers, all streaming options, metadata, WriteOnly, disposal/events and lifetime tested | Native no-3D capability and atomic creation refusal tested with SDL dummy video | Successful buffer behavior remains native-backend-specific beyond HEADLESS |
| Effect annotation values and collections | All typed/default values, metadata, strings, collection snapshots, ownership and thread/error paths tested | Same renderer-independent strict-C suite tested | Renderer-independent copied metadata operations |
| Effect parameter values and collections | All scalar/array/string/matrix-transpose overloads, defaults, metadata, nesting, stable collection aliases, texture dispatch/retention and thread/error paths tested | Same renderer-independent strict-C suite tested | Parameter behavior is renderer-independent; non-null retention is proven with standalone Texture2D |
| Effect techniques, passes and collections | Default/named construction, canonical P0, identities, Apply dispatch, nesting, stable aliases and thread/error paths tested | Same renderer-independent strict-C suite tested | Ownerless Apply is a native no-op; effect-owned current-technique validation is tested by `EffectSmoke.c` |
| Effect lifecycle, material, source shader and stock sprite | Construction/cloning/disposal, current technique/pass validation, exact type/source strings, all shader uniform/matrix calls, texture retention and descendant lifetime tested | Same strict-C suite with SDL dummy video | HEADLESS/SDL have no valid custom shader program; creation and explicit renderer-state queries remain deterministic |
| BasicEffect, DirectionalLight and effect interfaces | Exact defaults, every matrix/fog/light/material/texture operation, all three shared lights, default lighting, cloning, retention and descendant lifetime tested | Same strict-C suite with SDL dummy video | Property behavior is renderer-independent; GpuDrawParams stays behind Apply/draw paths |
| AlphaTest, DualTexture and EnvironmentMap effects | Exact defaults/types, all generic and concrete properties, unclamped reference alpha, enum/bool/index errors, always-on lighting, Texture2D/TextureCube cloning and retention, Apply and nested-light lifetime tested | Same strict-C suite with SDL dummy video | Property behavior is renderer-independent; renderer-only draw parameters stay behind Apply/draw paths |
| SkinnedEffect | All 72 identity defaults, bounded palette transfer/capacity, weights, complete generic/material/extension state, always-on lighting, texture clone retention, Apply and nested-light lifetime tested | Same strict-C suite with SDL dummy video | Property and palette behavior is renderer-independent; renderer-only bone upload stays behind Apply/draw paths |
| ColorMatrix and PBR extension effects | Identity/custom/grayscale/reset color transforms, non-finite validation, exact PBR defaults, every generic/material property, all five texture slots, clone retention, 72-bone SkinnedPbr palette, Apply and nested-light lifetime tested | Same strict-C suite with SDL dummy video | Extension-only APIs are identified as CNA features; renderer-specific draw parameters remain behind Apply/draw paths |
| ModelBone and ModelBoneCollection | Both constructors, UTF-8/capacity, indices/transforms, live parent/children aliases, count/index/name/contains, hierarchy lifetime, cycle and handle/thread errors tested | Same renderer-independent strict-C suite tested | Standalone hierarchy behavior is renderer-independent |
| MorphTargetEXT | Descriptor/value copies, type/field access, LINEAR/STEP/Hermite evaluation, additive blend, normalized normals, part retention and successful VertexBuffer upload tested | Same pure/value suite with SDL dummy video; unsupported buffer creation is preserved as `NOT_SUPPORTED` | Pure morph math is renderer-independent; upload follows native VertexBuffer availability |
| SkinnedModelEXT | Deep-copied skeleton/clips, transform sampling, move/attach/remove, owned part/resource lifetimes and counts tested | Same pure animation suite with SDL dummy virtual video; unsupported buffer creation is preserved as `NOT_SUPPORTED` | Skeleton math is renderer-independent; render-part storage follows native buffer availability |
| SkinningData and AnimationPlayer | Deep-copied data, clip lookup, retained playback, root-prefix/local/world/skin transforms and all update modes tested | Same renderer-independent strict-C suite tested | No window or GPU operation is required by this animation math path |
| Common graphics-resource name/tag/device/disposal/event contract | Tested | Tested | Generic contract is renderer-independent; device identity is callback-scoped |
| Complete Texture/Texture2D contract | All constructors, Color full/rectangle/mip transfer, image round-trips and error/lifetime paths tested | Same strict-C suite; level-zero transfer succeeds and native mip-upload limitation is explicit | Non-Color successful transfer needs renderer-specific C evidence; Skia has the broadest current native format gate |
| Texture3D/TextureCube complete adapter contract | Capability refusal, all six cube faces, full/rectangle/mip validation, DDS refusal, inheritance and lifetime tested | Same strict-C suite with SDL dummy video; missing storage is atomic `NOT_SUPPORTED` | Successful Texture3D and cube storage/readback need renderer-specific C evidence |
| Graphics-device frame control, reset and buffer binding | All clear/present routes, reset event pairs, windowed readback refusal, and full binding round trips with a real vertex and index buffer tested | Same strict-C suite with SDL dummy video; real windowed readback pixels verified | SDL_RENDERER refuses 3D buffer creation, so binding round trips are exercised on HEADLESS |
| Graphics-device frame control, reset and buffer binding | All clear/present routes, reset event pairs, windowed readback refusal, and full binding round trips with a real vertex and index buffer tested | Same strict-C suite with SDL dummy video; real windowed readback pixels verified | SDL_RENDERER refuses 3D buffer creation, so binding round trips are exercised on HEADLESS |
| Graphics-device draw submission and extensions | Topology vertex counts, malformed-descriptor rejection, both user routes for converted and raw streams with 16- and 32-bit indices under a bound BasicEffect, and every extension operation tested | Capability-accurate refusal of every draw route and pipeline-state toggle tested with SDL dummy video | Observable draw output needs a renderer with real 3D; SDL_RENDERER refuses these routes honestly |
| Texture sampler collections | Initial empty slots, both stages across all 16 slots, bind/read/unbind, self-unbinding on destruction and invalid stage/slot/structure/handle paths tested | Same strict-C suite plus real render-target rejection with SDL dummy video | HEADLESS cannot bind a render target, so that rejection path is exercised on SDL_RENDERER |
| SpriteBatch text, mesh submission and occlusion queries | Exact type name, all transform shapes, malformed-command and foreign-font rejection, mesh submission with and without optional arrays, and capability-accurate query behavior tested | Same strict-C suite with SDL dummy video | HEADLESS and SDL_RENDERER both refuse mesh submission honestly; observable text pixels need a renderer with a real atlas draw |
| Real 3D draw output | Draws succeed but pixel readback is unavailable, so no pixel claim is made | Refusal contract for a backend without the 3D capability | SOFTWARE proves observable center-pixel change through user, indexed, buffered and Model draw routes |
| Graphics extensions | Complete unavailable contract with the extension layer absent | All three effects created, every property exercised and a real ASCII draw with a measured glyph grid, with the extension layer present | Both build states are covered by the same strict-C source |
| Viewport values, transforms and device identities | All constructors, both zero-dimension aspect cases, bounds/title-safe area, depth-range scaling, clip-space corners, identity and perspective project/unproject round trips, exact strings, capacity atomicity and identity/bit operations tested | Same renderer-independent strict-C suite tested | Pure value operations; no device, window or GPU state is involved |
| Graphics-device state, events and exception conversion | All state round trips, defaults, non-finite viewport rejection, exact type name, dispose refusal, all six subscriptions, real resource-created/destroyed delivery, observed Disposing during game destruction and stale-handle failures tested | Same strict-C suite with SDL dummy video | Only the DirectX 9 backend raises device-lost/not-reset/no-suitable-device, so their result/category/message conversion is proven by the adapter test rather than by a live device |
| Compiled-asset readers and type readers | Whole pipeline tested against a storage-built fixture: protocol passes, byte-exact fixed values, limit checks, exact-byte reads, stream borrow/close ordering and the registry round trip | Same renderer-independent strict-C suite tested | SOFTWARE runs the same suite; the reader pipeline touches no renderer |
| Networking values, properties and packets | Whole slice tested: identities, both quality-of-service factories, the full property-list and enumerator contract, byte-exact packet round trips and the color asymmetry | Same renderer-independent strict-C suite tested | SOFTWARE runs the same suite; these values touch no renderer and no socket |
| Storage devices, containers and file streams | Whole module tested: selectors, device/container/stream ownership order, listings, byte-exact file round trip, seek and capability contracts | Same renderer-independent strict-C suite tested | SOFTWARE runs the same suite; storage touches no renderer, and the disconnected-device conversion is proven by the adapter test rather than by an unmountable filesystem |
| Raw joystick enumeration, capabilities, captured snapshots and hot-plug events | Whole family tested with no joystick attached: the zero-device enumeration, the disconnected-identifier capability and capture answers, all four snapshot count/copy pairs, snapshot comparison, release and stale-handle refusal, both hot-plug events raised end to end with per-registration detach, and the wrong-thread refusal | Same source tested with SDL dummy video | A machine with real joystick hardware exercises the connected branch; no renderer identity is involved |
| Host sensors, device enumeration, clipboard and power | Whole family tested: all three device enumerations through one shared validator, the zero-sensor case with the untouched-reading contract, all four hot-plug events raised end to end with per-registration detach and the all-four reset, the clipboard round trip asserted as a relationship with the original content restored, the power ranges, and the wrong-thread refusal | Same source tested with SDL dummy video | A mobile or sensor-equipped host exercises the available-reading branch; no renderer identity is involved |
| Media identities, visualization data and source enumeration | Whole slice tested: the zeroed visualization default over both full buffers, both type names checked literally, every reported source's kind/name/type name round-tripped, and the out-of-range, null-output and buffer-too-small refusals on every route | Same source tested with SDL dummy video | The sanitizer tree with leak detection proves the enumerated source list is released; no renderer identity is involved |
| Songs and song collections | Whole slice tested against fixture files built through the storage API, including a non-ASCII UTF-8 path: constructed defaults, the empty-name deviation, millisecond-to-tick conversion, both setters, path equality and hash agreement, URI resolution and scheme refusal, the missing-file I/O result, idempotent disposal, collection retention after the caller released its handles, and collection disposal emptying the collection | Same source tested with SDL dummy video | The sanitizer tree with leak detection covers the shared-ownership release path; no renderer identity is involved |
| Media library catalog | Whole slice tested against a generated fixture library — SDL's user-folder lookup is pointed at a private directory through XDG_CONFIG_HOME, so the scan finds exactly two tag-only MP3 files sharing an artist, album and genre plus a folder cover, and never reads a real user directory: entity names against the tags, the album's two-song collection, album art returning exactly the fixture's cover bytes, cross-route album equality and hash agreement, empty-collection refusals, disposal emptying a collection without disturbing its elements, library release before its entities, and the wrong-thread refusal | Same source tested with SDL dummy video | The sanitizer tree with leak detection is what proved the source constructor borrows rather than adopts; no renderer identity is involved |
| Observable SpriteBatch pixels | No raster backbuffer | Exact uploaded red/green/blue texels and clear pixel tested | No initial C evidence |
| Full RGBA8 backbuffer readback | `CNA_RESULT_NOT_SUPPORTED`, destination unchanged | Tested before presentation | Depends on the selected native backend; not yet C-tested |

An enumerated renderer identity is not a support claim. Applications must query graphics, touch
and audio capabilities and handle `CNA_RESULT_NOT_SUPPORTED`; future platform work must add
appropriate C evidence before this table claims support. The strict-C suite holds itself to the
same rule: no test branches on a renderer identity, so adding a backend needs no test edits.

## Ownership and call context

| Object/value | Lifetime rule |
|---|---|
| Game | Owned handle; destroy exactly once on its creation thread |
| Callback game | Borrowed for the active callback; callback code must not destroy it |
| Graphics device | Borrowed from the callback game and invalid immediately after that callback |
| Graphics-resource event registration | Owned handle; callback/context remain caller-owned until unsubscription; may be unsubscribed after resource destruction |
| Graphics-device event registration | Owned handle that does not block game destruction; the runtime invalidates live subscriptions once the device has been disposed, so the handle stays releasable afterwards |
| Texture2D | Owned standalone or game child that survives callbacks; game children are destroyed before their game |
| Texture3D / TextureCube | Owned game child that survives callbacks; destroy before its game; RenderTargetCube uses its own typed destroy route |
| VertexBuffer / DynamicVertexBuffer | Owned game child; copied declaration; destroy before game; ContentLost registration may be released after buffer destruction |
| IndexBuffer / DynamicIndexBuffer | Owned game child; fixed 16/32-bit width; destroy before game; ContentLost registration may be released after buffer destruction |
| EffectAnnotation / EffectAnnotationCollection | Owned creation-thread-affine metadata handles; collection insertion and lookup copy values; returned annotations outlive their source collection |
| EffectParameter / EffectParameterCollection | Owned creation-thread-affine mutable handles; element aliases and nested views retain stable native storage; each assigned texture-overload slot blocks that resource's disposal/destruction until cleared or released |
| EffectTechnique / EffectPass and collections | Owned creation-thread-affine handles or stable element aliases; nested pass/annotation views and returned elements retain native storage across collection growth/destruction |
| Effect / EffectMaterial / ShaderEffect / SpriteEffect | Owned game child; destroy before game unless live descendant views retain it; descendants transitively retain the effect/game ownership; bound shader textures remain retained per sampler unit until replacement or final descendant release |
| BasicEffect / DirectionalLight | BasicEffect is an owned game child; assigned Texture2D handles are retained independently by each clone; standalone lights are ordinary owned handles and nested light views transitively retain the parent effect/game |
| AlphaTestEffect / DualTextureEffect / EnvironmentMapEffect | Owned game children; each same-device Texture2D or TextureCube slot retains its C resource independently across clones; EnvironmentMapEffect directional-light views transitively retain their parent effect/game |
| SkinnedEffect | Owned game child with copied 72-matrix palette; its same-device Texture2D is retained independently across clones and nested light views transitively retain the parent effect/game |
| ColorMatrixEffect / PbrEffect / SkinnedPbrEffect | Owned game children; each PBR same-device Texture2D slot is retained independently across clones, SkinnedPbr owns a copied 72-matrix palette, and nested light views transitively retain the parent effect/game |
| ModelBone / ModelBoneCollection | Owned creation-thread-affine stable nodes and live collection views; parents retain children, returned aliases share mutation, and weak parent metadata avoids dangling parents after hierarchy release |
| ModelMeshPart / ModelMeshPartCollection | Owned creation-thread-affine shared parts and retained snapshots; indexed aliases share mutation; assigned Effect/VertexBuffer/IndexBuffer resources remain owned by their original handles and are protected from early disposal/destruction |
| ModelMesh / ModelMeshCollection / ModelEffectCollection | Owned game-child meshes and owned live/snapshot views; parts, parent bones, meshes and same-device effects are retained transitively; releasing the final mesh owner safely detaches independently retained parts |
| Model | Owned creation-thread-affine aggregate or empty standalone handle; immutable collection/root aliases retain bones and meshes; registered C resource context is released on replacement, clearing or final destruction |
| MorphTargetDataEXT | Owned creation-thread-affine handle with deep-copied descriptors; a ModelMeshPart retains attached data independently of the original data handle; all returned arrays remain caller-owned copies |
| SpriteBatch | Owned child that survives callbacks; destroy before its game |
| Submitted texture | Retained by an active batch until successful `End` or batch destruction; destruction while retained is refused |
| RenderTarget2D / RenderTargetCube | Owned game child; unbind and destroy before game; RenderTarget2D also satisfies Texture2D C operations |
| SpriteFont | Owned game child; destroy before game and before its retained source Texture2D/RenderTarget2D |
| Game callbacks/context | Table is copied; function pointers and context remain caller-owned and valid through game destruction |
| POD values and output buffers | Caller-owned; inputs are copied for the call and no output API writes a partial array |
| Keyboard snapshot | Independent copied POD; it has no handle or game lifetime after capture |
| Mouse/gamepad/touch snapshot | Independent copied POD; it has no handle or game lifetime after capture |
| ContentManager | Owned game child; destroy before game; unload/destroy does not destroy issued C resources |
| Content-loaded Texture2D | New owned game child per successful load; remains valid after manager unload/destruction |
| Session event registration | Weakly tied to its session; release unsubscribes; releasing after the session is gone is a no-op |
| NetworkSession | Standalone owner of the caller-owned pointer canonical creation returns; one at a time; destroy after every borrowed gamer view |
| SignedInGamer | Standalone owner; retained while published in the process-wide collection, and released only after it is cleared |
| AvailableNetworkSession | Standalone owner; copies of it are independent, including elements copied out of a collection |
| AvailableNetworkSessionCollection | Standalone owner; copies its input, so source handles stay independently owned |
| NetworkGamer | Standalone owner, or a borrowed view onto a machine's roster; a view is released before its machine |
| NetworkMachine | Standalone owner; destroy after every borrowed gamer view taken from it |
| NetworkSessionProperties | Standalone owner; destroy after every enumerator over it |
| Session-property enumerator | Owned list child; observes the live list; destroy before its list |
| PacketWriter / PacketReader | Standalone owners; independent of any session; destroy explicitly |
| ContentReader | Standalone owner; borrows a storage stream and closes it on destroy; the stream handle is still released separately |
| Content type reader | Standalone owner; independent of any reader or manager; destroy explicitly |
| StorageDevice | Standalone owner, not a game child; destroy after every container it opened |
| StorageContainer | Owned device child; destroy before its device and after closing every stream it opened |
| Storage stream | Owned container child; close before its container; no Sharp Runtime stream is exposed |
| SoundEffect | Owned game child; copied PCM survives input buffer; destroy after all instances and before game |
| SoundEffectInstance | Owned effect/game child; destroy before effect; no C callback/context is retained by mixer |

Game, graphics, texture, batch, content and snapshot-capture calls use the game creation/graphics
thread. Pure keyboard/gamepad/touch snapshot and normalization helpers are documented for any
thread.

## Error and capacity behavior

- Null pointers, invalid structure prefixes, reserved fields, enum values and numeric ranges return
  `CNA_RESULT_INVALID_ARGUMENT`.
- Null, stale, wrong-kind and expired borrowed handles return `CNA_RESULT_INVALID_HANDLE` or the
  documented state failure for an out-of-context borrow.
- Wrong-thread native operations return `CNA_RESULT_THREAD` before touching thread-affine state.
- Unsupported renderer or initial-slice functionality returns `CNA_RESULT_NOT_SUPPORTED` when a
  callable operation reaches that native limitation.
- Count/copy APIs always report the required count. Insufficient capacity returns
  `CNA_RESULT_BUFFER_TOO_SMALL` and does not write a prefix.
- Native exceptions are translated at the boundary. Unexpected native failures become
  `CNA_RESULT_INTERNAL`; no exception crosses into C.

## Intentionally unavailable in 0.1

The following families are planned work, not implicitly supported and not permanent exclusions:

- the remaining typed content asset routes;
- remaining audio (file/content loading, fire-and-forget, globals/3D, streaming, microphone and
  XACT), plus media and video;
- remaining window, platform, service, event and runtime APIs;
- player-indexed keyboard capture, input mutation/events, gamepad control/capabilities/extensions
  and touch display/gesture/event APIs;
- remaining 3D resources and draws, consumption of completed vertex/index-buffer and binding
  values, skinned-model/animation-player data, and remaining stock effects;
- occlusion queries and remaining graphics-device operations;
- Texture3D, TextureCube and renderer-specific successful non-Color transfer evidence;
- SpriteBatch matrices, effects and text drawing;
- advanced and renderer-specific CNA extensions not listed in the implemented table.

Until the generated inventory and completion gates in `plan_binding.md` are finished, any public
CNA symbol not explicitly represented in [`COVERAGE.md`](COVERAGE.md) is unimplemented.
