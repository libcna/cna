# Audit: src/CNA/Internal/Backends/D3DCommon/shaders/hlsl_shaders.hpp

## Metadata

- Source file: `src/CNA/Internal/Backends/D3DCommon/shaders/hlsl_shaders.hpp`
- Audit status: AUDITED
- Subsystem: `backend-d3dcommon` shard
- File type: auto-generated C++ header, 37,996 lines, ~3.3 MB — embedded DXBC shader bytecode
- XNA/FNA relevance: N/A directly (compiled shader bytecode, not source)
- Graphics backend relevance: the actual compiled DXBC bytes `D3DShaderCache.cpp` dispatches by `D3DShaderVariant`
- FNA reference: N/A
- Main related tests: none found exercising this file's content directly (its correctness is implicitly exercised
  by any D3D11/D3D12 draw-path test that reaches a real GPU shader-object creation call)

## Purpose

Machine-generated output of `compile_shaders_hlsl.py`: one `static constexpr uint8_t k*Dxbc[]` array per compiled
shader stage, each immediately followed by a `static constexpr size_t k*Dxbc_size = sizeof(k*Dxbc);` companion.

## Executive Verdict

**Healthy — scoped review appropriate for a generated file.** Structural review (not a byte-by-byte read, which
would exceed any useful review value for machine-generated binary data) confirms the file matches its own
documented generation process exactly.

## Checklist Results

### Behavioral correctness / Logic
The file's own header comment (`// AUTO-GENERATED by compile_shaders_hlsl.py -- do not edit by hand.`) was
verified accurate: counted exactly **34** `static constexpr uint8_t k*Dxbc[]` declarations, matching the 17
vertex + 17 pixel shader stages `compile_shaders_hlsl.py`'s own `SHADERS` list defines — a complete, gap-free
1:1 correspondence with no orphaned or missing array. Every array's DXBC magic bytes begin with `0x44, 0x58, 0x42,
0x43` (`"DXBC"`), spot-checked on the first array (`kColored3dVertDxbc`) — consistent with genuine D3DCompile()
output rather than placeholder/corrupted data.

### Architecture
Header-only, `#pragma once`-guarded, namespaced under `CNA::Internal::Backends::D3DCommon::Shaders` — correctly
scoped to avoid symbol collisions with the rest of the codebase.

### Maintainability / Process risk
As noted in `compile_shaders_hlsl.py`'s own report, nothing in the build automatically detects or regenerates this
file when a `.hlsl` source changes — a possible source of silent drift between source and compiled bytecode if a
shader is edited without the generation script being re-run, though this audit found no *specific* evidence that
drift has actually occurred (a byte-for-byte DXBC disassembly comparison against the current `.hlsl` sources was
not performed, as it would require the Wine+DXVK toolchain this audit's Linux sandbox does not exercise for this
narrow purpose).

## Detailed Findings

None confirmed; the maintainability/process risk above is inherited from `compile_shaders_hlsl.py`, not a defect
introduced by this file itself.

## Cross-File Observations

Full triangulated 1:1 consistency confirmed across `compile_shaders_hlsl.py`'s `SHADERS` list, `D3DShaderCache.hpp`'s
`D3DShaderVariant` enum, `D3DShaderCache.cpp`'s two dispatch switches, and this file's 34 arrays — see those files'
own reports for the individual cross-checks.

## Missing or Weak Tests

No automated check exists (in this repository, at build or test time) that this file's DXBC content is actually
up to date with the current `.hlsl` sources.

## Positive Findings

Complete, gap-free, correctly-structured generated output matching its own documented process exactly.

## Final Assessment

No issues found within the scope appropriate for a machine-generated binary-data header; the "nothing verifies
this stays in sync with its sources" risk is a project-wide process characteristic, not unique to this file.
