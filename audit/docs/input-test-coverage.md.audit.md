# Audit: docs/input-test-coverage.md

## Metadata
- Source file: `docs/input-test-coverage.md` (67 lines)
- Audit status: AUDITED (full read)
- Subsystem: `docs` shard
- File type: Markdown, machine-generated report
- XNA/FNA relevance: maps every Input type to its test-suite coverage
- Main related tests: generated by `tools/input_parity/check_input_test_coverage.py`

## Purpose
Auto-generated mapping of every public XNA Input type and every internal `CNA::Internal::Input`
type to whether it has a dedicated test suite, how many test files reference it, and (for types
without a dedicated same-named suite) which sibling suite actually covers it.

## Executive Verdict
Healthy. Explicitly marked "Generated... Do not hand-edit; re-run the script" — correctly
self-identifying as machine-generated, removing staleness risk from manual drift (assuming the
generating script is re-run per `input-pre-merge-checklist.md`'s own gate, already read in this
batch).

## Checklist Results
- "Gaps" section (line 9) states "None — every Input type has a dedicated suite or a documented
  sibling-suite cover" — a genuinely complete-coverage claim, not a partial one.
- Every "no" dedicated-suite row includes a specific "Note" identifying exactly which sibling suite
  covers it (e.g. `KeyModifiersEXT` -> `KeyboardModStateTests.cpp`) — a real, checkable
  cross-reference, not a bare "covered elsewhere" placeholder.
- Internal (`CNA::Internal::Input`) type rows correctly distinguish backend-seam types (covered via
  a `Fake*Backend` injection test) from types tested through their real consumer — consistent
  categorization throughout.

## Detailed Findings
None.

## Cross-File Observations
Directly satisfies `input-pre-merge-checklist.md`'s "Test coverage" gate (0 orphans) — cross-
verified consistent between the two files (both read in this batch).

## Missing or Weak Tests
Not applicable — this document IS the test-coverage report.

## Positive Findings
Genuinely complete coverage with precise per-type sibling-suite citations — a strong example of
machine-generated documentation staying trustworthy specifically because it's regenerated rather
than hand-maintained.

## Final Assessment
No findings.
