# Graphics Implementation Task Plan — Archive (additional completed tasks, moved 2026-07-10)

> This file holds **additional completed (✅) tasks**, moved out of `plan_graphics.md` on
> 2026-07-10 to keep the active backlog file a manageable size. No task content was changed in
> the move — only the file location. `plan_graphics.md` remains authoritative for all still-open
> (⬜/⛔) work; this file is historical record only.

> See also [`plan_graphics_20260708.md`](plan_graphics_20260708.md) (completed tasks 1-500,
> moved 2026-07-08) and [`plan_graphics_20260709.md`](plan_graphics_20260709.md) (phases whose
> remaining work was completed 2026-07-08 to 2026-07-10, including Phase 72/73's full 2026-07-10
> closure). This file covers 4 more phases that reached 100% ✅ by 2026-07-10 but hadn't yet been
> moved: Phase 28 (PresentationParameters/GraphicsDeviceManager), Phase 31 (user primitives/
> draw-call variants), Phase 54 (documentation and public compatibility status), and Phase 55
> (final Graphics stabilization, closing with Task 500's qualified ~90% XNA/FNA compatibility
> milestone declaration).

---

## Phase 28 — PresentationParameters and GraphicsDeviceManager compatibility

| #   | Task                                                                                                                                            | Status | Notes                                        |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------------------- |
| 226 | Implement `ApplyChanges()` compatibility path for `GraphicsDeviceManager` if added                                                              | ✅      | **Backlog-hygiene closure (2026-07-09) — no new work, already substantially implemented and tested under later task numbers, left unmarked.** `GraphicsDeviceManager::ApplyChanges()` (`GraphicsDeviceManager.cpp:196`) is a real, non-stub implementation: creates the device on first call, detects `prefsChanged_`/resized-backbuffer state, rebuilds a fresh `GraphicsDeviceInformation`, fires `DeviceResetting`/`DeviceReset` (matching FNA's real event contract), drives the game window's `BeginScreenDeviceChange`/`EndScreenDeviceChange`, and delegates to `applyToExistingBackend()` — which Task 902 already confirmed reaches the real backend (`GraphicsDevice::Reset()` → `IGraphicsBackend::ApplyMultiSampleCount()` etc., not a no-op). Directly exercised, by name, in at least 11 existing example tests: `easygl_backbuffer_resize_test`, `easygl_real_window_resize_test`, `easygl_fullscreen_field_test`, `easygl_msaa_change_test`, `easygl_depth_format_test`, `easygl_present_interval_test`, `easygl_presentation_parameters_test`, `easygl_device_reset_events_test`, `viewport_reset_after_resize_test`, `vulkan_msaa_test`, `vulkan_rendertarget2d_msaa_test` (Tasks 711/713/714/902 and others, see their own rows/§3 of `NEXT.md`). No swapchain/window-recreation gap remains open. |

---

## Phase 31 — User primitives and draw-call variants

| #   | Task                                                                                           | Status | Notes              |
| --- | ---------------------------------------------------------------------------------------------- | ------ | ------------------ |
| 253 | Implement missing `DrawUserPrimitives` overloads by staging data into transient vertex buffers | ✅      | **Backlog-hygiene closure (2026-07-09) — no new work, already fully implemented and extensively tested under later task numbers, left unmarked.** `GraphicsDevice.cpp` has 6 real `DrawUserPrimitives` overloads (lines 594, 771, 797, 822, 849, 875) staging caller data into transient vertex buffers, all 3 backends. Dedicated `DrawUserPrimitivesTests.cpp` (22 test cases) covers argument validation across all overloads (Task 259's own coverage, referenced by this section's own orphan Task-252-follow-up row below); used pervasively as the primary draw-call mechanism throughout this project's entire pixel-test suite (hundreds of `examples/*.cpp` files). |
| 254 | Implement missing `DrawUserIndexedPrimitives` overloads by staging data into transient VB/IB   | ✅      | **Backlog-hygiene closure (2026-07-09) — no new work, already fully implemented and extensively tested under later task numbers, left unmarked.** `GraphicsDevice.cpp` has 10 real `DrawUserIndexedPrimitives` overloads (lines 651, 919, 949, 978, 1009, 1041, 1071, 1100, 1131, 1163, 1189) staging caller data into transient VB/IB, all 3 backends. Dedicated `DrawUserIndexedPrimitivesTests.cpp` (37 test cases) covers argument validation across all overloads, including the untyped raw-`void*` overload's `primitiveCount` guard fixed by this section's own orphan Task-252-follow-up row below. |
| — | `GraphicsDevice.cpp`, `DrawUserIndexedPrimitivesTests.cpp`, `plan_graphics.md` | Added the `primitiveCount<=0` argument-guard unit tests for `DrawUserIndexedPrimitives` that Task 252 claimed but never actually landed (mirrors Task 259's `DrawUserPrimitives` coverage): all 8 typed overloads + 2 `VertexDeclaration` overloads, zero and negative counts. Also found and fixed a real gap while writing these: the untyped raw-`void*` overload (predates the Task 252 typed overloads) had no `primitiveCount` guard at all — added `ArgumentOutOfRangeException::ThrowIfNegativeOrZero`, matching the other 10 overloads. 22 new unit tests; 1840/1840 total pass. |

---

## Phase 54 — Documentation and public compatibility status

| #   | Task                                                                                               | Status | Notes                                                |
| --- | -------------------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------- |
| 481 | Rewrite `docs/xna-4-api-coverage.md` with current task status after Phase 25+                      | ✅      | **CLOSED — opens Phase 54; investigated first, found the doc's Graphics-related content was stale by an entire session's worth of work (Phases 47-53), scoped the rewrite to Graphics only (Audio/Input sections are already current from `feature/audio`/`feature/input`'s own recent updates, out of this task's own scope per this session's graphics-focused authorization).** Read the full 637-line doc plus `docs/graphics-backend-feature-matrix.md` (Task 451, this session's own comprehensive, current, per-backend/per-feature matrix) as ground truth. **Central stale claim found and corrected**: §7's "Bgfx `SetDepthTestEnabled`/`SetBlendEnabled` still throw — no 3D pixel tests possible" (dated 2026-06-26, Task 196) was flatly wrong — Phase 72 ("Bgfx: full 2D+3D pixel-verified parity") closed that gap entirely; all 5 stock effects' core rendering is now pixel-verified on EasyGL, Vulkan, AND Bgfx. **Deliberately did NOT re-duplicate the detailed per-effect/per-backend tables** that made §7 go stale in the first place — replaced them with a condensed headline summary plus a pointer to `docs/graphics-backend-feature-matrix.md` as the single currently-maintained source (matching the exact "supersede, don't duplicate" pattern that doc's own header already establishes for `docs/coverage.md`). Updated: the date header (new 2026-07-09 entry summarizing what changed and why); §4's Stock Effects paragraph (corrected the same stale Bgfx claim); §7 (rewritten in full, condensed + pointer); §8 (added a note scoping its EasyGL-only percentages, pointing non-EasyGL-backend questions at the feature matrix instead of inventing new blended percentages across 4 backends with genuinely different maturity); §11's "What remains missing" and "Recommended next steps" (removed 2 items now DONE — Vulkan pixel tests, Bgfx 3D state — replaced with the current real gaps: the 5 BLOCKED tasks 447/686/687/725/732, Vulkan's fake `BlendState` Task 868, `ReferenceStencil`/`ClearOptions::Stencil` Tasks 871/872, and Task 921's `IndexElementSize` divergence found this same session by Task 479's own JSON comparison harness). Pure documentation — no code changes, no build/regression needed. |
| 482 | Add clear definitions: API present, implemented, tested, FNA-compatible, intentionally unsupported | ✅      | **CLOSED — pure documentation.** New "Coverage axes" subsection in `docs/xna-4-api-coverage.md` (right after the existing single-tier "Status Definitions" table), defining the 5 axes named in this task's own title as independent questions, not one blended scale: Present (header exists), Implemented (real `.cpp` body, not a stub), Tested (an automated test/example exercises it), FNA-compatible (directly checked against FNA's own source/real output, not just "looks reasonable"), Intentionally unsupported (a deliberate, documented deviation, not a gap). Each axis has a concrete example of a real "no" pulled from this project's own history (`ContentReader`/`ContentTypeReaderManager`/`LzxDecoder` — no header at all — for Present; `ResourceContentManager::OpenStream` — header/class exist but the body unconditionally throws — and `INTERNAL_applyReverb` for Implemented; `Microphone.BufferReady`'s untested hardware-threshold path for Tested; Task 921's `IndexElementSize` for FNA-compatible — present+implemented+tested for years, still wrong; audio reverb for Intentionally unsupported). **Post-merge correction (2026-07-09, caught during independent verification of this task):** the doc's first draft mis-cited `ResourceContentManager` as a "Present = no" example — it actually has a real header/class in CNA (`include/.../ResourceContentManager.hpp`, a stub that throws), which is a textbook "Implemented = no" case instead; fixed in the doc to use the genuinely headerless XNB-reader classes for the Present example. Explicitly calls out that **FNA-compatible is the axis that predicts hidden bugs**, since a member can sit at Present+Implemented+Tested for years without ever being independently re-checked against FNA's real behavior — tying directly into why Tasks 471-479's FNA reference harness (Task 480's `docs/fna-reference-harness.md`) exists as a dedicated check for exactly that one axis. |
| 483 | Add per-class Graphics coverage table                                                              | ✅      | **CLOSED — opens Phase 54's second task; pure documentation.** New "Per-class Graphics coverage" subsection in `docs/xna-4-api-coverage.md` (right after Task 482's "Coverage axes" section), rating ~26 major Graphics classes (`GraphicsDevice`, `Texture2D`/`Texture3D`/`TextureCube`, `RenderTarget2D`/`RenderTargetCube`, `SpriteBatch`, `SpriteFont`, all 5 stock Effects, `ShaderEffect`, all 4 state classes, `VertexBuffer`/`IndexBuffer`/`VertexDeclaration`, `Model`/`ModelMesh`/`ModelBone`, `OcclusionQuery`, `Viewport`, `PresentationParameters`, `GraphicsAdapter`) against the same 5 Present/Implemented/Tested/FNA-compatible/Intentionally-unsupported axes Task 482 defined. **Deliberately did not re-derive detail already owned by two other docs** — `docs/graphics-backend-feature-matrix.md` (Task 451, per-backend/per-feature ground truth) and `AUDIT.md`'s own per-class FNA-vs-CNA rows (lines ~110–1341) — this table only summarizes and cross-references both by task number, keeping this doc's own "supersede, don't duplicate" pattern from Task 481. **Verified every row against the real header and its own `AUDIT.md` entry before rating it, not from memory** — direct lesson from Task 482's own mis-citation (see below): confirmed all 25 class headers actually exist under `include/Microsoft/Xna/Framework/Graphics/` via `find`, cross-checked each row's Implemented/FNA-compatible rating against the exact `AUDIT.md` line for that class (e.g. `BlendState`'s Vulkan-fake note, Task 868; `IndexElementSize`'s Task 921; `Model`'s Task 916/440), and spot-checked `Tested` via `find tests/ examples/` for each class rather than assuming test coverage exists. Also fixed, in the same session's prior commit (`e0def2be`, independent verification of Tasks 479-482), a real factual error Task 482 shipped with: its "Coverage axes" table cited `ResourceContentManager` as a "Present = no" example when it actually has a real CNA header/stub — corrected to cite the genuinely headerless `ContentReader`/`ContentTypeReaderManager`/`LzxDecoder` classes instead; this task's own table was written with that lesson applied throughout. Pure documentation — no code changes, no build/regression needed. |
| 484 | Add per-backend support table                                                                      | ✅      | **CLOSED — pure documentation, the "flip side" of Task 483's per-class table.** New "Per-backend Graphics support" subsection in `docs/xna-4-api-coverage.md` (right after Task 483's per-class table): one row per backend (EasyGL/Vulkan/Bgfx/SDL_Renderer) summarizing overall maturity, what's fully correct, what's partial/emulated, what's architecturally ⛔ BLOCKED, and the current known-failure-count baseline — deliberately cross-referencing `docs/graphics-backend-feature-matrix.md` (Task 451) and `NEXT.md` §2/§5 rather than re-deriving their per-feature detail. Read all 3 sources plus `docs/sdl-renderer-2d-completeness.md` and Task 483's own "Key gaps" column before writing anything, per this session's own "check the real source, don't recall" discipline established after Task 482's `ResourceContentManager` mis-citation. **Found a real numeric inconsistency while cross-checking sources**: `docs/graphics-backend-feature-matrix.md`'s own Vulkan baseline claims "last full run (Task 911) was 4369/4378 — 12 known pre-existing," but 4378−4369=9, not 12 — it doesn't arithmetically match its own cited totals. `NEXT.md` §2's own itemized breakdown for that identical Task 911 run lists exactly 9 (5× `BlendState`/868, 1 `DepthBias` sub-case, 3 `ContentManagerSkinnedModelTest` segfaults). Used the arithmetically-consistent 9 in the new table and flagged the `graphics-backend-feature-matrix.md` discrepancy inline rather than silently picking one number — not fixed in that file itself (pre-existing content, out of this task's own scope; a candidate follow-up for whoever next touches that doc). Pure documentation — no code changes, no build/regression needed. |
| 485 | Add known deviations from XNA/FNA                                                                  | ✅      | **CLOSED — pure documentation, closes the Tasks 481-485 documentation arc within Phase 54 (Phase 54 itself continues — Tasks 486-490 remain open below).** New "Known deviations from XNA/FNA" subsection in `docs/xna-4-api-coverage.md` (right after Task 484's per-backend table), split into "Confirmed bugs, not yet fixed" (4 rows: `IndexElementSize` numeric mismatch/921, Vulkan `BlendState` fake/868, EasyGL `Anisotropic` trilinear fallback/918, `Model` constructor `Root` auto-default/916 — all re-verified against their real `plan_graphics.md` rows before listing, not transcribed from memory) and "Intentional, permanent deviations" (8 rows sourced from `CHECKLIST.md`'s "Known acceptable C++ deviations" table and `AUDIT.md`'s Texture2D/Texture3D detailed audits — `GetHashCode()` return type, `ref`/`out` → value-ref pairs, `IEnumerable`→`begin()`/`end()`, `Model::Draw()`/`ModelMesh::Draw()` null-safety fallbacks, `Model::CopyBoneTransformsFrom/To`'s loop-bound difference, `Texture2D::FromStream`'s silent DDS auto-detection vs. FNA's throw-on-unsupported contract, `Texture2D::SetData`'s null/zero-count inconsistency between overloads, and `Texture::GetFormatSizeEXT`/`ValidateGetDataFormat`'s `public static` vs. FNA's `internal` visibility). Deliberately scoped to `Microsoft::Xna::Framework::Graphics` only, matching Task 481's own Graphics-only scoping for this file (Audio-specific `CHECKLIST.md` deviations — reverb no-ops, XACT RPC continuity, etc. — are out of this task's scope, already covered by `feature/audio`'s own documentation). Confirmed Task 899 (Vulkan effect fog gaps) is no longer a live deviation — already fixed in a later commit this same session — so it was deliberately left out of the list rather than included stale. Pure documentation — no code changes, no build/regression needed. |
| 486 | Add migration guide for XNA/FNA users trying CNA                                                   | ✅      | **CLOSED — new external-facing practical guide, starting a second Phase 54 mini-arc (Tasks 486-490) distinct from the internal audit doc Tasks 481-485 built.** Confirmed no `docs/migration-*` file existed yet before creating one. New `docs/migration-guide.md`: what CNA is (a C++23 reimplementation, not a runnable .NET runtime — porting means translating C# to C++, not recompiling); the single biggest mechanical difference (C# properties → `getXProperty()`/`setXProperty()`, with a real, source-verified example — confirmed `BlendState& getBlendStateProperty()`/`setBlendStateProperty()` on `GraphicsDevice`, `SurfaceFormat getFormatProperty()` on `Texture`, and `GraphicsDevice* getGraphicsDeviceProperty()` on `GraphicsResource` all actually exist with those exact signatures before using them in an example — an initial draft used an invented `GetGraphicsDevice()` C#-style name, caught and fixed before committing); namespace mapping (`Microsoft::Xna::Framework::*` unchanged, `NOXNA`/`EXT` markers explained); a backend-selection guide (SDL_Renderer for 2D-only, EasyGL for anything 3D, Vulkan/Bgfx have real open gaps) drawn from Task 484's per-backend table rather than repeating it; "what fully works today" pulled from Task 483's per-class ✅ rows (`RenderTarget2D`/`Cube`, `SpriteFont`, `BasicEffect` core, `VertexBuffer`/`VertexDeclaration`, `Viewport`, `PresentationParameters`, `GraphicsAdapter`); "what has caveats" drawn directly from Task 485's deviations list (`IndexElementSize`/921, Vulkan `BlendState`/868, EasyGL `Anisotropic`/918, `Model` root/916); "what doesn't work at all yet" from the BLOCKED task list (447, 686/687, 725, 732); and a content-pipeline section explaining CNA's non-XNB file-extension-based `ContentManager` versus real FNA's `.xnb` format, cross-referencing `docs/xna-4-api-coverage.md` §3/§6 and `Model`'s own Task 440 content-loader gaps rather than re-deriving them. Deliberately a new, separate file rather than another `docs/xna-4-api-coverage.md` section — that doc is this project's own internal audit trail for contributors, this one is written for an external developer with an existing XNA/FNA codebase. Pure documentation — no code changes, no build/regression needed. |
| 487 | Add minimal 2D sample compatibility list                                                           | ✅      | **CLOSED — new "2D compatibility checklist" section appended to `docs/migration-guide.md` (Task 486), not a separate file, since it's the same practical audience and reads better as one continuous guide.** A scannable, tick-through-your-source table for `SpriteBatch`/`SpriteFont`/`Texture2D`/`BlendState`/`SamplerState`, cross-referencing `docs/sdl-renderer-2d-completeness.md` (Task 731) for the full rationale behind any ⚠️/❌/⛔ rather than repeating it. **Verified every listed member against the real current header before including it, not from memory or XNA docs** — a direct continuation of this session's own established discipline. **Found a real, previously-unremarked gap while doing this verification**: real FNA's `SpriteBatch.Draw` has 7 overloads (confirmed by reading `src/Graphics/SpriteBatch.cs` directly — `grep -c "public void Draw("` = 7), but CNA's public (non-`NOXNA`) `Draw` overload set only has 6 of them — the 7th, `Draw(Texture2D, Rectangle destination, Rectangle? source, Color, rotation, origin, SpriteEffects, layerDepth)`, exists in CNA only as a `NOXNA`-marked near-equivalent with a **required** `Rectangle source` parameter instead of FNA's `Rectangle?` (optional) — not a faithful signature match, and C# code calling this overload with a `null` source rectangle won't have a matching CNA overload to port to as-is. Not previously tracked anywhere (`docs/sdl-renderer-2d-completeness.md`'s own "All 9 Draw overloads ✅" line was counting CNA's actual overload set as complete, not cross-checking it against FNA's own count). **Deliberately not fixed here** — this is a genuine but separate, scoped API-completeness task (adding a real overload plus its own tests), not something to fold into a documentation task; opened as new Task 922. Pure documentation otherwise — no other code changes, no build/regression needed. |
| 488 | Add minimal 3D sample compatibility list                                                           | ✅      | **CLOSED — new "3D compatibility checklist" section appended to `docs/migration-guide.md` (Task 486/487), not a separate file, matching Task 487's own format/location pattern.** Covers the 5 stock Effects' construction (verified against real FNA `.cs` source — exactly 1 public `EffectName(GraphicsDevice)` constructor + `Clone()`/protected-copy-ctor per effect in FNA, CNA matches with `GraphicsDevice&` + `Clone()`/private-copy-ctor, no mismatch found, unlike Task 487's own `SpriteBatch::Draw` finding), core rendering per-backend (EasyGL/Vulkan/Bgfx — SDL_Renderer excluded entirely from the per-backend columns since it throws for all 3D by design), the per-effect Vulkan/Bgfx gaps already tracked (887/889/890/891/893-895), `Model`/`ModelMesh`/`ModelBone` (FNA's own `Model` constructor is `internal`, confirmed via `Model.cs` — CNA's `NOXNA` public constructors are an intentional convenience, not a signature mismatch; Task 916's Root-default gap, Task 440's content-loader gaps), and `VertexBuffer`/`IndexBuffer`/`VertexDeclaration` (both FNA overloads present, CNA adds `NOXNA` convenience overloads only, no gap). **Applied Task 487's own overload-diffing discipline** (grep FNA `.cs` constructor counts vs. CNA header) to the 5 stock effects, `Model`, and the 3 buffer types — found no new signature mismatch this time (unlike Task 922). **Found and fixed a real, separate stale claim while cross-checking Vulkan fog support for this checklist**: Task 484's own per-backend table (already merged) claimed Vulkan's `colored3d`/`textured3d`/`colored_textured3d`/`dual_texture3d`/`skinned3d` "still lack fog, Task 899" — but `git merge-base --is-ancestor` confirms Task 899's fog-support commits (`beb83b20`, `6a91d11b`, 2026-07-07) predate Task 484's own commit (`55e59a42`, 2026-07-09) by 2 days, so this was already stale when written, not a later regression. Fixed the claim in `docs/xna-4-api-coverage.md` directly (in scope here since this checklist's own Vulkan fog row depends on it being correct). Pure documentation — no code changes, no build/regression needed. |
| 489 | Add troubleshooting guide for graphics backend selection                                           | ✅      | **CLOSED — pure documentation (Task 490 is the last task in Phase 54, not this one — this only closes Tasks 486-489 of the practical-docs mini-arc).** New "Troubleshooting graphics backend selection" section appended to `docs/migration-guide.md` (same location/style as Tasks 487/488): real configure-time failures (missing `sharp-runtime`/`easy-gl` sibling checkouts, missing vendored SDL from a ZIP download, missing Vulkan SDK, `bgfx.cmake` FetchContent network dependency, missing FFmpeg dev packages per `CLAUDE.md`), real runtime failures (SDL_Renderer's by-design 3D throw, bgfx window/renderer-selection failures, headless/Xvfb GL driver limitations, stale golden images, the "never run backend ctest suites concurrently" rule from `NEXT.md` §2), and a pointer to Tasks 484/485's tables for "bug vs. known limitation" triage. Every command/env var verified against real current source before inclusion, not assumed from general CMake/XNA knowledge: confirmed `CNA_GRAPHICS_BACKEND`'s real default logic and the `sharp-runtime`/`easy-gl` sibling-checkout `FATAL_ERROR` messages directly in `CMakeLists.txt`; found and documented 3 real `getenv`-read environment variables directly via `grep` (`CNA_BGFX_RENDERER` — `src/CNA/Internal/Backends/Bgfx/BgfxRendererSelection.cpp`'s `ParseRendererTypeOverride`, confirmed its exact accepted value list: `AUTO`/`OPENGL`/`OPENGLES`/`VULKAN`/`METAL`/`DIRECT3D11`/`DIRECT3D12`/`NOOP`; `CNA_UPDATE_GOLDEN` — `examples/common/PixelTestGame.hpp`'s golden-image regeneration mode; `CNA_TEST_DISPLAY` — the CMake cache variable, not an env var, confirmed in `CMakeLists.txt`'s own top-level comment). **Found and fixed a real, substantial staleness in `README.md`'s own "Project Status" section while researching real backend maturity for this guide**: it called `VULKAN` an "architecture scaffold... currently incomplete" and its own Roadmap section listed "Advance Vulkan backend from scaffold to practical rendering path" as a future goal — both flatly wrong given this session's own Phase 71-73 work (Vulkan has real, pixel-verified 3D rendering with all 5 stock effects, per Task 484's own per-backend table showing it as the "second-most mature" backend). Also, `BGFX` had no "Project Status" line at all despite being a real, working backend. Fixed both the Project Status bullets (now accurately reflect EasyGL/Vulkan/Bgfx/SDL_Renderer's real relative maturity, cross-referencing `docs/xna-4-api-coverage.md`'s per-backend table rather than re-deriving it) and the stale Roadmap bullet (replaced with the real remaining named gaps: Vulkan `BlendState`/`OcclusionQuery`, EasyGL anisotropic filtering, `IndexElementSize`). Pure documentation — no code changes, no build/regression needed. |
| 490 | Add release checklist for Graphics namespace compatibility milestones                              | ✅      | **CLOSED — closes Phase 54 in full (Tasks 481-490); verified every row 481-489 was already ✅ before making this claim.** New "Release checklist for Graphics compatibility milestones" section in `docs/xna-4-api-coverage.md` (right after Task 485's "Known deviations" list): replaces this project's old informal per-subsystem percentage table (the Phase 9-14 coverage table earlier in this file) with concrete, falsifiable pass/fail criteria for claiming Graphics is ~90%/~95%/100% XNA/FNA-compatible, built directly on Tasks 482-485's own tables rather than inventing new criteria. **90% bar**: every class Present+Implemented on ≥1 backend, Tested on ≥1 backend, ≥1 backend (EasyGL) with zero ❌ classes, the BLOCKED-task list (447/686/687/725/732) has current rationale, `docs/migration-guide.md`'s BLOCKED list matches it exactly. **95% bar**: all 3 3D backends pixel-verify core stock-effect rendering (already true), and the "Confirmed bugs, not yet fixed" count has a hard ceiling — **found this needs to be 5, not the 4 Task 485's own table lists**, since Task 487 (a later task) found and opened Task 922 (`SpriteBatch::Draw`'s 7th-overload signature gap) after Task 485's table was already written; noted this explicitly in the new checklist rather than silently under-counting. **100% bar**: zero confirmed bugs, zero BLOCKED tasks, every class ✅ on every backend that supports its feature domain, FNA-compatible independently verified per-class (not assumed) via the Tasks 471-479 reference harness or an equally-direct source comparison, every "intentional, permanent" deviation re-confirmed still permanent, and zero unexplained pre-existing test failures on any backend. Added an informational "current status against this checklist" note: ~90% looks achievable soon, ~95%/100% are gated on the 5-item bug list and the 5 BLOCKED tasks respectively — deliberately not itself claiming a percentage, since this task's whole point is to stop imprecise percentage claims, not produce a new one. Pure documentation — no code changes, no build/regression needed. |

---

## Phase 55 — Final Graphics stabilization before declaring XNA 4.0 complete

| #   | Task                                                                                                                                      | Status | Notes                                   |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------- |
| 491 | Remove all remaining `CNA_STUB` markers in `Graphics` or convert them to explicit documented unsupported exceptions                       | ✅      | **CLOSED — opens Phase 55; confirmed clean, one stale comment fixed.** Investigated first, broadly: `grep -rn "CNA_STUB"` across `include/`/`src/` finds exactly 2 hits, both in `Content/ResourceContentManager.*` — zero in `Microsoft::Xna::Framework::Graphics`. Went further than the literal task title and searched every stub-shaped pattern across all of `Graphics` (case-insensitive `stub`, `not yet implemented`/`not implemented`, `TODO`/`FIXME`, `no-op`/`noop`, `not supported`/`unsupported`): every hit was checked individually against its real surrounding code, not assumed from the grep match alone. Found: (1) `SpriteBatch.cpp:255`'s `// Draw stubs — XNA 4.0 overloads not yet fully implemented` — a stale, misleading leftover comment; the 6 overloads it labels are fully implemented (each correctly calls the real `pushSprite()` helper) — **fixed**, reworded to `// Draw overloads — position/rectangle + optional source-rectangle variants`; (2) `Texture::ValidateFormat` (`Texture.cpp:109`) already throws a clear, documented `std::runtime_error` for any non-`Color` `SurfaceFormat` — already exactly the "explicit documented unsupported exception" pattern this task asks for, no change needed (Task 176's own already-shipped contract); (3) `VertexElement`/`VertexPositionTexture`/`VertexPositionColorTexture`/`VertexPositionNormalTexture`'s `GetHashCode()` returning `0` with an "(FNA TODO)" doc comment — confirmed via direct read of FNA's own `VertexElement.cs`/`VertexPositionTexture.cs` that these are faithful ports of FNA's own genuine, real `// TODO: Fix hashes` / `return 0` — correct behavior fidelity, not a CNA gap; (4) `Effect.hpp`'s `FillGpuDrawParams` "default implementation is a no-op" — a legitimate C++ template-method base-class default, all 5 concrete stock effects already override it (confirmed by Tasks 483/488's own audits this session); (5) `GraphicsDevice::SetStringMarkerEXT`'s "no-op on all other backends" — a `NOXNA` GPU-profiler debug marker, silently doing nothing on backends without `VK_EXT_debug_utils` is the correct, expected behavior for an optional debug hook, not an XNA API gap. **No genuine silent placeholder found anywhere in Graphics** — this is a real "confirmed clean" closure (matching this session's own precedent for Tasks 452/453/733/734), not a failure to find something. Rebuilt `CNA` target clean after the comment fix. **Note for Task 492** (next row, "search for TODO/FIXME/no-op/stub comments and triage them"): this task's own investigation already did most of that search — Task 492 should start from this row's findings rather than re-searching from scratch, and focus on whatever this row didn't cover (e.g. a broader sweep beyond the specific patterns searched here). |
| 492 | Search for all `TODO`, `FIXME`, `no-op`, and `stub` comments in Graphics source and triage them                                           | ✅      | **CLOSED — exhaustive re-search across all of `include/Microsoft/Xna/Framework/Graphics/` and `src/Microsoft/Xna/Framework/Graphics/`, broader than Task 491's own informal pass** (`TODO`, `FIXME`, `XXX`, `HACK`, `no-op`/`noop`, `stub`, `not yet`, `not implemented`/`unimplemented`, `placeholder`, all case-insensitive). **Full checklist with triage verdict for every real hit found:** (1) `VertexElement.hpp:131`, `VertexPositionTexture.hpp:75`, `VertexPositionColorTexture.hpp:80`, `VertexPositionNormalTexture.hpp:79` — `GetHashCode()` returns `0` with an "(FNA TODO)" doc comment — **already correct**, re-verified directly against FNA's own `VertexElement.cs`/`VertexPositionTexture.cs`/`VertexPositionColorTexture.cs`/`VertexPositionNormalTexture.cs`, all four have the exact real `// TODO: Fix hashes`/`return 0` in FNA itself; `VertexPositionColor.hpp`'s own `GetHashCode()` doc (`"Consistent with FNA (always 0)"`) conveys the identical fact in different wording — not misleading, no change needed. (2) `Effect.hpp:162`/`Effect.cpp:63` (`FillGpuDrawParams` default no-op) — **already correct**, a legitimate C++ template-method base overridden by all 5 stock effects (re-confirmed). (3) `GraphicsDevice.hpp:691` (`SetStringMarkerEXT` no-op on non-Vulkan) — **already correct**, optional NOXNA GPU-profiler debug hook, no-op is the intended behavior on backends without `VK_EXT_debug_utils`. (4) `GraphicsAdapter.cpp:272` (`SDL_free` no-op-on-nullptr comment) — **not a stub at all**, a defensive-code comment describing real SDL null-safety semantics; no action. (5) `GraphicsDevice.cpp:356` (`RemoveResourceReference` re-entrancy no-op comment in `Dispose()`) — **already correct**, a deliberate re-entrancy guard explicitly matching FNA's own real dispose pattern. (6) `GraphicsResource.hpp:63` ("or nullptr if not yet attached") — **not a stub reference**, plain doc prose for a nullable constructor parameter. (7) `SpriteBatch.cpp:118` ("Begin has not yet been called" in a thrown exception message) — **not a stub**, real, correct exception-behavior text. (8) `Effect.cpp:22-25` (`Effect(GraphicsDevice&, const std::vector<bytecs>&)` throws `System::NotImplementedException` naming Phase 74/`docs/fx-bytecode-support-plan.md`) — **already correct and already the target pattern** this whole Task 491/492 pair is asking for (a genuinely unsupported feature converted to a clear, documented, tested throw) — confirmed this was deliberately done by an earlier session's Task 353, both the plan doc and its Phase 74 task block exist for real, tests already cover it (`EffectTests.cpp`). (9) `Texture.cpp:109-115`/`Texture.hpp:58` (`ValidateFormat` throws for any non-`Color` `SurfaceFormat`) — **already correct**, same finding as Task 491's own row, re-confirmed. (10) `stub`/`FIXME`/`XXX`/`HACK`/`placeholder` — **zero hits** anywhere in `Graphics` (Task 491's own `SpriteBatch.cpp:255` fix already removed the one prior `stub` hit). **Net result: zero new fixes needed** — every real hit is either already-correct FNA-faithful behavior, an already-correct architectural no-op, or (in the `Effect` bytecode case) already the exact well-documented-exception pattern this pair of tasks exists to ensure. This is a real, thorough "confirmed clean" closure, not a shortcut — the checklist above is exhaustive, not a sample. No code changes, no rebuild needed (pure re-verification of existing code + comments). |
| 493 | Run full unit test suite with EasyGL enabled                                                                                              | ✅      | **CLOSED — confirmed `cmake-build-debug` is genuinely configured for `CNA_GRAPHICS_BACKEND=EASYGL`, did a full incremental build (`cmake --build cmake-build-debug -j$(nproc) -- -k`, `-k` to see past the pre-existing, unrelated `cna_demo_xact` Content-copy error and confirm every other target — including `CnaTests` itself — actually built), then ran the complete suite: `ctest --test-dir cmake-build-debug --output-on-failure`.** Result: **4522/4525 passed (99%), 3 failed — an exact match to this session's own already-documented EasyGL baseline, nothing new, nothing fewer.** The 3 failures: (1) `EasyGL_MRT_TwoAttachments`; (2) `EasyGL_GraphicsDevice_ReferenceStencil` — reconfirmed the exact same real symptom as Task 484's own table (`setReferenceStencilProperty()` has no effect, the compare still uses the state's own baked-in value, tracked as Task 872, open); (3) `easy-gl-resource-smoke-tests` — a real assertion abort in `easy-gl`'s own vendored `SmokeResourceTests.cpp:336` (`g_state.last_active_texture == 0x84C0`), matching the already-documented Mesa/llvmpipe active-texture-unit tracking quirk in this sandbox, not a CNA regression. 2 unrelated tests reported `(Skipped)` (`AccelerometerTests`/`GyroscopeTests` — hardware-not-present skips, pre-existing, unrelated to Graphics). **No git-stash-revert re-confirmation needed** — Task 491's only production change this session was a comment-only edit in `SpriteBatch.cpp` (zero behavior change, and `SpriteBatch` isn't involved in any of the 3 failures anyway), so a genuine new regression from this session's own work was already implausible before running anything; the full run itself is the real confirmation. Pure verification — no code changes. |
| 494 | Run full graphics integration suite with EasyGL                                                                                           | ✅      | **CLOSED — first established what "integration suite" means as a genuinely distinct thing from Task 493's own "unit suite" run, rather than just re-stating the same numbers.** Task 739's own row ("Final EasyGL perfection gate: re-run Tasks 493-494 (full unit + integration suite)") and Task 822/859's own phrasing ("Run the full Bgfx/Vulkan integration suite... end to end") confirm this project treats "unit" and "integration" as two distinct categories, even though there is no `ctest` LABEL separating them today (`ctest --print-labels` only shows `GraphicsSmoke`/`input`). Investigated the real mechanism instead of guessing: `grep -c "gtest_discover_tests" CMakeLists.txt` = 1 (one bulk call registers the ~4357 pure-GTest logic tests from `tests/`), vs. 388 individually-named `add_test(NAME ...)` calls (the real window/GPU pixel-readback tests from `examples/`), of which exactly 168 are `EasyGL_`-prefixed — this 168-test set **is** the real, concrete EasyGL integration suite. Ran it specifically: `ctest --test-dir cmake-build-debug -R "^EasyGL_" --output-on-failure` → **166/168 passed (99%), 2 failed — `EasyGL_MRT_TwoAttachments` and `EasyGL_GraphicsDevice_ReferenceStencil` (Task 872, open)**, both already-documented, exactly matching 2 of Task 493's own 3 baseline failures (`easy-gl-resource-smoke-tests`, Task 493's 3rd failure, is a vendored `easy-gl` submodule test, not a CNA-authored `EasyGL_*` integration test, so it correctly falls outside this subset's own boundary). Satisfies this row's own "must pass or documented skips" criterion — both failures are documented, tracked, pre-existing, not new. Pure verification, no code changes. |
| 495 | Run full graphics integration suite with Vulkan                                                                                           | ✅      | **CLOSED — Vulkan equivalent of Task 494's EasyGL run.** Confirmed the same unit-vs-integration split applies: Vulkan's integration subset is the `Vulkan_`-prefixed `add_test()` set (93 tests, `ctest --test-dir cmake-build-vulkan -N -R "^Vulkan_"`), distinct from the bulk `gtest_discover_tests` unit pool (e.g. `ContentManagerSkinnedModelTest.*` lives at ctest IDs #1424–1431, well outside the `Vulkan_`-prefixed #4364–4445 range). Full incremental build (`cmake --build cmake-build-vulkan -j$(nproc)`, then `-- -k` to see past the one pre-existing, unrelated `cna_demo_xact` Content-copy error and confirm `CnaTests` and every Vulkan test binary built clean) confirmed no other backend's test run was in progress first (`ps aux` check), per this project's own never-run-concurrently rule. Ran `ctest --test-dir cmake-build-vulkan --output-on-failure -R "^Vulkan_"`: **87/93 passed (94%), 6 failed** — `Vulkan_BlendState_AlphaBlend`/`Additive`/`SeparateFunctions`/`SeparateFactors`/`BlendFactor` (5, Task 868's already-documented fake-blend-equation bug) and `Vulkan_DepthBias` (1, the already-documented isolated `DepthBias=-1e6` sub-case). **Cross-checked against Task 911's own full-suite baseline** (4369/4378, 9 pre-existing failures: 5× `BlendState`, 1 `DepthBias`, 3 `ContentManagerSkinnedModelTest` segfaults) — this integration-only run's 6 failures are exactly the 6 of those 9 that live in the `examples/`-sourced `Vulkan_`-prefixed set; the remaining 3 (`ContentManagerSkinnedModelTest` segfaults) are unit-pool tests outside this task's own integration scope, confirmed via their ctest ID range above rather than assumed. Every failure already-documented, understood, and tracked — satisfies "must pass or documented skips." Pure verification, no code changes, no new task needed. |
| 496 | Run full graphics integration suite with Bgfx                                                                                             | ✅      | **CLOSED — Bgfx equivalent of Tasks 494/495.** Confirmed the same unit-vs-integration split: Bgfx's integration subset is the `Bgfx_`-prefixed `add_test()` set (56 tests, `ctest --test-dir cmake-build-bgfx -N -R "^Bgfx_"`). Full incremental build (`cmake --build cmake-build-bgfx -j$(nproc)`, then `-- -k` to see past the one pre-existing, unrelated `cna_demo_xact` Content-copy error and confirm `CnaTests` and every Bgfx test binary built clean); confirmed no other backend's test run was in progress first (`ps aux` check), per this project's never-run-concurrently rule. Ran `ctest --test-dir cmake-build-bgfx --output-on-failure -R "^Bgfx_"`: **54/56 passed (96%), 2 failed** — `Bgfx_RenderTarget2D_MsaaResolve` (the already-documented Xvfb/no-DRI3 environment limitation, cited in `docs/xna-4-api-coverage.md`'s per-backend table as Bgfx's 1 known full-suite failure) and `Bgfx_RenderTarget2D_MipChain` (NOT in that same table's count, but confirmed via `plan_graphics.md`'s own Tasks 455/877/906/912 history as an already-documented, genuinely pre-existing flaky test — a mip-generation-vs-readback timing race, ~1-in-8 failure rate, reproduces identically on unmodified code per Task 455's own 8-isolated-run investigation). **Verified the flake claim directly rather than trusting the history**: reran `Bgfx_RenderTarget2D_MipChain` alone — passed; reran the full `Bgfx_*` subset again — failed again in the same batch position, consistent with a batch-order/timing-dependent flake rather than a clean-vs-dirty-tree difference. Both failures pre-existing and understood, satisfies "must pass or documented skips." Pure verification, no code changes, no new task needed. |
| 497 | Run SDL_Renderer 2D-only suite and verify all 3D calls throw intentionally                                                                | ✅      | **CLOSED — SDL_Renderer equivalent of Tasks 494/495/496, but with an important shape difference: its own `SDL_Renderer_`-prefixed integration set (67 tests) is written specifically to expect 2D success and 3D-throw behavior, so it passes 100%, not 166/168-style.** Full incremental build confirmed clean (only the pre-existing, unrelated `cna_demo_xact` Content-copy error); confirmed no other backend's test run was concurrently in progress first. `ctest -R "^SDL_Renderer_"`: **67/67 passed, 0 failed** — every 2D feature test (`SpriteBatch`/`SpriteFont`/`BlendState`/`SamplerState`/`RenderTarget2D`/etc.) passes for real, and every dedicated 3D-throw test (`SDL_Renderer_DrawPrimitivesThrows`, `SDL_Renderer_DrawUserPrimitivesThrows`, `SDL_Renderer_DrawUserIndexedPrimitivesThrows`, `SDL_Renderer_BufferConstructionThrows`, `SDL_Renderer_OcclusionQueryThrows`, `SDL_Renderer_ModelDrawThrows`, `SDL_Renderer_Stock3DEffectsApply`) passes because it correctly asserts the throw happens, not despite it. **Also ran the complete `cmake-build-sdl` suite (4421 tests) to re-verify the separate, already-documented "11 failures" baseline** (generic, backend-agnostic unit tests from `tests/` that construct 3D objects — `VertexBuffer`/`IndexBuffer`/etc. — with no per-backend awareness, so they hit SDL_Renderer's real 3D-unsupported throw as a genuine unhandled exception rather than an expected one): **4410/4421 passed, exactly the same 11 failures** (`ContentManagerSkinnedModelTest`×3, `EffectApplyTest`×2, `SkinnedModelEXTPartTest`×6), same test names, same count, nothing new. **Directly confirmed the thrown exception text is genuinely the intentional, documented message, not a different/unexpected error**: re-ran `ContentManagerSkinnedModelTest.PartNameWithUnbalancedEmbeddedBraceParsesCorrectly` and `EffectApplyTest.DrawPrimitivesThrowsWithoutPriorApply` individually with `--output-on-failure` — both show the exact real message `"SDL_Renderer does not support 3D: CreateVertexBuffer"`, matching this backend's own documented, by-design capability line (`"CNA: SDL_Renderer capabilities -- 2D-only backend..."` printed at the start of every SDL_Renderer test run) — a clean, intentional throw, not a crash or an unrelated error. Satisfies "must pass" for the true 2D-only integration scope, and confirms the wider unit-pool's known 11 failures remain exactly the documented, understood, by-design 3D-unsupported case. Pure verification, no code changes. |
| 498 | Build and run at least 5 small FNA/XNA sample ports on CNA                                                                                | ✅      | **CLOSED — targets EasyGL (this project's primary/most-mature backend, per Task 484's per-backend table), deliberately different from Task 730's own SDL_Renderer-specific, 2D-only sample set: 3 of these 5 samples are genuinely 3D, since 2D compatibility was already proven by Task 730 and 3D is what most needs "real compatibility proof" at this final-stabilization phase.** No local FNA-Samples repo exists to literally port files from (same finding as Task 730) — interpreted the same way: reuse an existing genuinely-suitable CNA demo where one exists, author new minimal classic-XNA-idiom samples for the rest. **Sample 1/5 (reuse, 2D)**: `cna_demo_2d` — already smoke-tested on Vulkan/Bgfx (Tasks 88/89) but had ZERO EasyGL registration despite running correctly there, a concrete pre-existing gap mirroring exactly what Task 730 found for SDL_Renderer; closed via new `EasyGL_Demo2D_SmokeTest` (`cna_demo_2d --smoke 3`). **Sample 2/5 (new, 3D)**: `easygl_sample_moving_quad3d_test.cpp` — a small unlit, vertex-colored (`BasicEffect::VertexColorEnabled=true`) Red quad translated along World-space X by `Matrix::CreateTranslation`, +0.3 NDC units/`Update()`, 4 `Update()` calls (start X=-0.6, end X=0.6); View/Projection left at BasicEffect's own real default (Identity), so World-space X coincides with NDC X and the screen-space mapping is the simple `(x+1)/2*width` formula (matching Task 476/479's own `Viewport.Project` convention); final-frame checks confirm the quad is genuinely Red at the end position (screen X≈51 of 64) AND no longer Red at the start position (screen X≈13), proving real per-frame motion rather than a statically-covering quad. **Sample 3/5 (new, 3D)**: `easygl_sample_dualtexture_swap_test.cpp` — a static full-screen `DualTextureEffect` quad (`Texture`=constant white 1x1, isolating "did the live swap reach the shader" from the multiply-blend math itself) whose `Texture2` reference is swapped from a Red 1x1 texture to a Green 1x1 texture once `Update()`'s frame counter reaches 2 (a classic reactive-material-swap gameplay pattern); captured the center pixel at frame 1 (pre-swap, expected Red) and frame 4 (post-swap, expected Green), confirming both the exact expected colors and that they genuinely differ (live state, not baked in at `Initialize()`). **Sample 4/5 (new, 3D)**: `easygl_sample_keyboard_cube3d_test.cpp` — the 3D counterpart to Task 730's own SDL_Renderer keyboard-sprite sample: a Blue `BasicEffect` quad moves along World-space X only while `Keyboard::GetState().IsKeyDown(Keys::Right)` is true, using the same `CNA::Internal::Input::InputManager::SetKeyState` headless-injection seam Task 730/`KeyboardInputTests.cpp` already established; confirmed both the exact final logical position and that the quad is genuinely rendered there. **Sample 5/5 (new, 2D)**: `easygl_sample_layered_blend_test.cpp` — a layered UI/fade scene: an opaque Blue background `SpriteBatch` sprite, then a semi-transparent White overlay sprite drawn with `BlendState::NonPremultiplied` (straight, not premultiplied, alpha compositing — deliberately chosen over the SpriteBatch-default `BlendState.AlphaBlend`, which expects premultiplied source colors and would need a `Color.White * scale`-style premultiply to get a clean expected value), overlay alpha driven by `Update()` state (64 for frames 1-1, 192 for frames 2-4, a simple two-stage fade); hand-derived expected compositing formula `result = fg*a + bg*(1-a)` predicts R=G≈64 then ≈192 (B stays ≈255 throughout since both textures' blue channel is already 255) — all matched on the real first run. **Discriminating power independently verified on 2 of the 4 new samples** (representative, not exhaustive, matching this session's own "at least one" convention): (a) commented out `x_ += kStepX;` in Sample 2 — the "rendered at the final position" check still trivially passed (it reads back at `ndcToScreenX(x_)`, i.e. wherever the live, now-stuck `x_` says the quad should be — no genuine discriminating power on its own), but the fixed-position "start marker is no longer Red" check correctly FAILED (the quad, never having moved, is still sitting exactly on that fixed marker) — 1/2, exactly the predicted single-check failure, confirming it's specifically the *second* check that has real discriminating power for this sabotage — restored, rebuilt, reconfirmed 2/2; (b) hardcoded Sample 3's `Texture2` to always stay Red (never swap) — correctly failed 2 of 3 checks (post-swap-is-Green, and genuinely-changed), 1/3, exactly the predicted pattern (only the pre-swap Red check still trivially passes) — restored, rebuilt, reconfirmed 3/3. Both sabotages produced a clean `git diff` after restoration. **Full regression**: rebuilt cleanly (`cmake --build cmake-build-debug -- -k`, only the already-known, unrelated `cna_demo_xact` Content-copy error) and ran the complete `ctest` suite (4530 tests, +5 over the prior 4525 baseline) — 4527/4530 passed (99%), exactly the same 3 pre-existing failures as every prior regression this phase (`EasyGL_MRT_TwoAttachments`, `EasyGL_GraphicsDevice_ReferenceStencil`, `easy-gl-resource-smoke-tests`), zero new failures, all 5 new samples passing (`EasyGL_Demo2D_SmokeTest`, `EasyGL_Sample_MovingQuad3D`, `EasyGL_Sample_DualTextureSwap`, `EasyGL_Sample_KeyboardCube3D`, `EasyGL_Sample_LayeredBlend`). |
| 499 | Produce final Graphics compatibility report with percentages based on tests, not estimates                                                | ✅      | **CLOSED — new `docs/graphics-compatibility-report.md`, computed entirely from real numbers re-read directly from Tasks 493-498's own `plan_graphics.md` rows and Task 483's per-class table, not from memory.** Per-backend test pass rate (integration-only vs. full-suite, since Task 497 showed these diverge): EasyGL 166/168=98.81% integration / 4527/4530=99.93% full; Vulkan 87/93=93.55% integration / 4369/4378=99.79% full (Task 911 baseline); Bgfx 54/56=96.43% integration / corrected 4412/4414=99.95% full; SDL_Renderer 67/67=100% integration (2D-only by design) / 4410/4421=99.75% full (11 by-design 3D-throw "failures"). Separately, re-tallied Task 483's per-class table (26 classes) by axis: Present 26/26=100%; Implemented 22/26=84.6% fully clean + 4/26=15.4% partial (`Texture2D`/`ShaderEffect`/`BlendState`/`OcclusionQuery`); Tested 26/26=100% (on *at least one* backend — explicitly flagged that a true per-class-per-backend cross-tab doesn't exist as queryable data anywhere in this project today, rather than inventing one); FNA-compatible 12/26=46.2% fully clean, 11/26=42.3% partial, 2/26=7.7% confirmed bug (`BlendState`/`IndexBuffer`), 1/26=3.8% N/A (`ShaderEffect`, NOXNA). **One real inconsistency found and fixed while cross-checking**: `docs/xna-4-api-coverage.md`'s per-backend table (Task 484) cited Bgfx's known-failure baseline as "1" (`Bgfx_RenderTarget2D_MsaaResolve`), but Task 496's own rerun found 2 — `Bgfx_RenderTarget2D_MipChain` is *also* an already-documented pre-existing flake (Tasks 455/877/906/912) never folded into that row's count; fixed inline in `docs/xna-4-api-coverage.md`, correcting the baseline to 4412/4414. **Deliberately did not invent a single blended "Graphics is N% done" score** (matching Task 490's own explicit warning) — instead gave one clearly-labeled combined figure explicitly scoped to "% of classes with zero known FNA-compatibility gap" (46.2%), not conflated with test-pass-rate or LOC. **Assessment for Task 500**: Tasks 491-499 all genuinely pass their own stated criteria (every failure encountered is previously-documented, understood, and named — never an unexplained new regression) — but this is a narrower bar than Task 490's own 95%/100% checklist tiers, which are NOT yet met (5 confirmed bugs remain open — 921/868/918/916/922 — and all 5 BLOCKED tasks remain open — 447/686/687/725/732). Recommended Task 500 declare a qualified milestone (Task 490's own ~90% bar, which the real numbers do support: every class rates ✅/⚠️ never ❌ on Present/Implemented) rather than an unqualified 100%/"1.0" claim. |
| 500 | Declare `Microsoft.Xna.Framework.Graphics` 1.0 compatibility milestone only if Tasks 491–499 pass or deviations are explicitly documented | ✅      | **CLOSED — closes Phase 55 in full (Tasks 491-500; verified every row 491-499 was already ✅ before writing this row).** This is a genuine project-owner decision, not a mechanical continuation: Task 499's own report found that while Tasks 491-499 all genuinely pass their own stated criteria (every test failure across all 4 backends is previously-documented, understood, never an unexplained regression), an unqualified "1.0"/"100%" claim would not be honest given 5 confirmed bugs (921/868/918/916/922) and 5 BLOCKED tasks (447/686/687/725/732) remain open — exactly the distinction Task 490's own release checklist's 90%/95%/100% tiers exist to make precise. Presented directly to the project owner with the real numbers; **decision: declare a qualified ~90% milestone** (test-execution-verified, not estimated) rather than an unqualified 1.0 claim or withholding any declaration. New "Milestone declaration" section (§6) in `docs/graphics-compatibility-report.md` (Task 499's own report — the doc with the real computed numbers backing this) states the ~90% figure concretely (100% of 26 classes ✅ on Present/Tested, 0% ❌ on Implemented per Task 483's table) and names all 10 open items with task numbers and what each actually blocks, rather than vague language. `README.md`'s "Project Status" section (Task 489's own home for per-backend maturity notes) got a matching one-line summary pointing at the full report, rather than duplicating the declaration text in two places. **Next real work**: Phase 55 is the last active phase before the hard-forbidden WebGPU block (Phases 56-69, `plan_webgpu.md`, CLAUDE.md's own standing prohibition — do not open). NEXT.md's own already-established pointer convention (predating this task) names Tasks 738 ("Close the remaining EasyGL-scoped ⬜ tasks in Phases 34–55") and 861 ("Document remaining genuine Vulkan limitations") as the next candidates — both confirmed still genuinely `⬜` via direct grep. **Flagging honestly, not resolved here**: Phases 71/72/73 (Bgfx/Vulkan gap-closure phases this session's own docs otherwise describe as "full 2D+3D pixel-verified parity"/"second-most mature") still show a large number of individually-unchecked `⬜` rows in their own raw task tables (2/84/37 respectively) — this session's narrative treats those backends as mature via many *other*, higher-numbered tasks (868, 899, 911, etc.) that superseded the original granular checklist without formally closing every one of its rows. This is a real backlog-hygiene question outside Task 500's own scope (declaring the Graphics milestone), not something this task resolves — worth a dedicated triage/archival pass (mirroring this project's own `plan_graphics_2026070{8,9}.md` archival convention) before assuming Task 738/861 alone constitute "what's left." |
