// SPDX-License-Identifier: MS-PL

#include "WebAssets.hpp"

namespace CNA::Inspector::Detail
{
    std::string_view InspectorIndexHtml() noexcept
    {
        return R"CNAHTML(<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="cna-ui-token" content="__CNA_UI_TOKEN__">
  <title>CNA Inspector</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <header class="topbar">
    <div><span class="mark">CNA</span><strong>Inspector</strong><span id="app-name" class="app-name">Connecting…</span></div>
    <div id="connection" class="status reconnecting"><span></span>reconnecting</div>
  </header>
  <nav id="nav" aria-label="Inspector views">
    <button class="active" data-view="session">Session</button>
    <button data-view="performance">Performance</button>
    <button data-view="profiler">CPU profiler</button>
    <button data-view="graphics">Graphics</button>
    <button data-view="resources">Resources</button>
    <button data-view="audio">Audio</button>
    <button data-view="input">Input</button>
    <button data-view="events">Events</button>
  </nav>
  <main>
    <aside id="loss-banner" class="banner hidden" role="status"></aside>
    <section id="session" class="view active">
      <h1>Session</h1>
      <div id="session-cards" class="cards"></div>
      <div class="panel"><h2>Application metadata</h2><dl id="metadata" class="facts"></dl></div>
      <div class="panel"><h2>Capabilities</h2><div id="capabilities" class="capabilities"></div></div>
      <p class="note">Inspector is a read-only diagnostics endpoint. Values retain the accuracy classification supplied by CNA.</p>
    </section>
    <section id="performance" class="view">
      <h1>Performance</h1>
      <div id="performance-cards" class="cards"></div>
      <div class="panel graph-panel"><div class="panel-heading"><h2>Frame time</h2><span>bounded 240-frame provider history</span></div><canvas id="frame-graph" height="220"></canvas></div>
      <div class="panel"><h2>Current metrics</h2><div class="table-wrap"><table><thead><tr><th>Name</th><th>Value</th><th>Unit</th><th>Kind</th><th>Accuracy</th></tr></thead><tbody id="metrics-body"></tbody></table></div></div>
    </section>
    <section id="profiler" class="view">
      <h1>CPU profiler</h1>
      <p id="profiler-unavailable" class="empty hidden">FULL diagnostics events are unavailable in this session.</p>
      <div class="panel"><div class="panel-heading"><h2>Hot zones</h2><span>aggregate of the latest 1,000 retained UI events</span></div><div class="table-wrap"><table><thead><tr><th>Zone</th><th>Category</th><th>Calls</th><th>Total</th><th>Average</th><th>Maximum</th></tr></thead><tbody id="hot-zones"></tbody></table></div></div>
      <div class="panel"><h2>Recent timeline</h2><div id="timeline" class="timeline"></div></div>
    </section>
    <section id="graphics" class="view">
      <h1>Graphics</h1>
      <div id="graphics-cards" class="cards"></div>
      <div class="panel"><h2>Graphics metrics</h2><div class="table-wrap"><table><thead><tr><th>Name</th><th>Value</th><th>Accuracy</th></tr></thead><tbody id="graphics-metrics"></tbody></table></div></div>
      <div class="panel"><h2>Effects and shaders</h2><p class="empty">Provider v1 does not invent cross-renderer shader reflection. Only metrics or explicitly registered resource metadata are shown.</p></div>
    </section>
    <section id="resources" class="view">
      <div class="view-title"><div><h1>Resources</h1><p>Metadata is fetched only when requested. Preview readback is manual and separately capability-gated.</p></div><button id="refresh-resources" class="primary">Refresh metadata</button></div>
      <div class="toolbar"><input id="resource-filter" type="search" placeholder="Filter label, kind, format or ID"><select id="resource-kind"><option value="">All kinds</option></select><span id="resource-summary"></span></div>
      <div class="panel"><div class="table-wrap"><table id="resource-table"><thead><tr><th data-sort="id">ID</th><th data-sort="kind">Kind</th><th data-sort="label">Label</th><th data-sort="dimensions">Dimensions</th><th data-sort="format">Format</th><th data-sort="mipCount">Mips</th><th data-sort="estimatedBytes">Bytes</th><th data-sort="byteAccuracy">Accuracy</th><th>Preview</th></tr></thead><tbody id="resources-body"></tbody></table></div></div>
    </section>
    <section id="audio" class="view">
      <h1>Audio</h1>
      <div id="audio-cards" class="cards"></div>
      <div class="panel"><h2>Published audio diagnostics</h2><div class="table-wrap"><table><thead><tr><th>Name / resource</th><th>Value / state</th><th>Accuracy</th></tr></thead><tbody id="audio-body"></tbody></table></div></div>
      <p class="note">Allocated voices are CNA mixer allocations, not a claim that every voice is currently audible.</p>
    </section>
    <section id="input" class="view">
      <h1>Input</h1>
      <div class="panel"><p class="empty">Current diagnostics provider v1 publishes no input device state. Inspector does not probe backend internals or inject input.</p></div>
    </section>
    <section id="events" class="view">
      <h1>Diagnostic events</h1>
      <div class="toolbar"><input id="event-filter" type="search" placeholder="Filter name, category, thread or kind"><button id="clear-events">Clear local view</button><span id="event-summary"></span></div>
      <div class="panel"><div class="table-wrap"><table><thead><tr><th>Sequence</th><th>Frame</th><th>Thread</th><th>Kind</th><th>Category</th><th>Name</th><th>Duration</th><th>Value</th></tr></thead><tbody id="events-body"></tbody></table></div></div>
    </section>
  </main>
  <dialog id="preview-dialog"><form method="dialog"><button class="close" aria-label="Close">×</button></form><h2 id="preview-title">Resource preview</h2><div id="preview-state" class="empty">Requesting bounded preview…</div><img id="preview-image" alt="Requested resource preview"></dialog>
  <script src="/app.js" defer></script>
</body>
</html>)CNAHTML";
    }

    std::string_view InspectorStyleCss() noexcept
    {
        return R"CNACSS(:root{color-scheme:dark light;--bg:#101419;--panel:#171d24;--panel2:#202832;--text:#e9eef5;--muted:#93a2b5;--line:#2e3946;--accent:#68d391;--accent2:#5da9e9;--warn:#f4b860;--bad:#ef6b73;--shadow:0 10px 28px rgba(0,0,0,.2);font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-size:14px}.topbar{height:58px;display:flex;align-items:center;justify-content:space-between;padding:0 24px;border-bottom:1px solid var(--line);position:sticky;top:0;background:color-mix(in srgb,var(--bg) 94%,transparent);backdrop-filter:blur(12px);z-index:4}.mark{display:inline-grid;place-items:center;margin-right:10px;padding:4px 7px;border-radius:6px;background:var(--accent);color:#07140c;font-weight:900;letter-spacing:.08em}.app-name{margin-left:14px;color:var(--muted);font-weight:400}.status{display:flex;align-items:center;gap:8px;text-transform:uppercase;font-size:11px;letter-spacing:.08em}.status span{width:9px;height:9px;border-radius:50%;background:var(--bad);box-shadow:0 0 10px var(--bad)}.status.connected span{background:var(--accent);box-shadow:0 0 10px var(--accent)}.status.reconnecting span{background:var(--warn);box-shadow:0 0 10px var(--warn)}nav{display:flex;gap:4px;padding:10px 24px;border-bottom:1px solid var(--line);overflow:auto;white-space:nowrap}button,input,select{font:inherit;color:inherit}button{border:1px solid var(--line);background:var(--panel);padding:8px 12px;border-radius:7px;cursor:pointer}button:hover{border-color:var(--accent2)}nav button{border-color:transparent;background:transparent;color:var(--muted)}nav button.active{background:var(--panel2);color:var(--text);border-color:var(--line)}button.primary{background:var(--accent);border-color:var(--accent);color:#07140c;font-weight:700}main{max-width:1500px;margin:0 auto;padding:24px}.view{display:none}.view.active{display:block}h1{font-size:25px;margin:0 0 18px}h2{font-size:15px;margin:0 0 14px}.view-title,.panel-heading{display:flex;align-items:flex-start;justify-content:space-between;gap:18px}.view-title p{color:var(--muted);margin:-10px 0 20px}.panel-heading span,.toolbar span{color:var(--muted);font-size:12px}.cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:12px;margin-bottom:14px}.card,.panel{background:var(--panel);border:1px solid var(--line);border-radius:10px;box-shadow:var(--shadow)}.card{padding:16px}.card .label{color:var(--muted);font-size:12px;margin-bottom:7px}.card .value{font-size:21px;font-variant-numeric:tabular-nums;overflow-wrap:anywhere}.card .detail{color:var(--muted);font-size:11px;margin-top:5px}.panel{padding:17px;margin-bottom:14px}.facts{display:grid;grid-template-columns:minmax(130px,220px) 1fr;gap:9px 20px;margin:0}.facts dt{color:var(--muted)}.facts dd{margin:0;overflow-wrap:anywhere}.capabilities{display:flex;gap:8px;flex-wrap:wrap}.badge{display:inline-flex;align-items:center;border:1px solid var(--line);border-radius:999px;padding:3px 8px;font-size:11px}.badge.on,.accuracy.exact{color:var(--accent)}.badge.off,.accuracy.unavailable{color:var(--muted)}.accuracy.estimated{color:var(--warn)}.note,.empty{color:var(--muted)}.banner{padding:11px 14px;border:1px solid color-mix(in srgb,var(--warn) 65%,var(--line));background:color-mix(in srgb,var(--warn) 12%,var(--panel));border-radius:8px;margin-bottom:16px;color:var(--warn)}.hidden{display:none!important}.toolbar{display:flex;gap:9px;align-items:center;margin:0 0 12px}.toolbar input{flex:1;min-width:180px}.toolbar input,.toolbar select{border:1px solid var(--line);border-radius:7px;background:var(--panel);padding:9px 11px}.table-wrap{overflow:auto;max-height:62vh}table{width:100%;border-collapse:collapse;white-space:nowrap}th,td{text-align:left;padding:9px 11px;border-bottom:1px solid var(--line);font-variant-numeric:tabular-nums}th{position:sticky;top:0;background:var(--panel);color:var(--muted);font-size:11px;text-transform:uppercase;letter-spacing:.05em;z-index:1}th[data-sort]{cursor:pointer}tr:last-child td{border-bottom:0}.graph-panel canvas{display:block;width:100%;height:220px}.timeline{display:flex;flex-direction:column;gap:5px;max-height:420px;overflow:auto}.timeline-row{display:grid;grid-template-columns:105px 1fr 95px;gap:9px;align-items:center}.timeline-track{height:19px;background:var(--panel2);border-radius:4px;overflow:hidden}.timeline-bar{width:100%;height:19px;border:0;background:var(--panel2);accent-color:var(--accent2)}.timeline-bar::-webkit-progress-bar{background:var(--panel2)}.timeline-bar::-webkit-progress-value{background:var(--accent2)}.timeline-bar::-moz-progress-bar{background:var(--accent2)}.timeline-row small{color:var(--muted);text-align:right}dialog{width:min(900px,90vw);max-height:90vh;color:var(--text);background:var(--panel);border:1px solid var(--line);border-radius:12px;box-shadow:var(--shadow)}dialog::backdrop{background:rgba(0,0,0,.7)}dialog form{float:right}.close{border:0;background:transparent;font-size:24px;padding:0 6px}#preview-image{display:none;max-width:100%;max-height:70vh;margin:12px auto 0;image-rendering:auto}#preview-image.visible{display:block}.resource-preview[disabled]{opacity:.45;cursor:not-allowed}@media(max-width:700px){.topbar{padding:0 14px}.app-name{display:none}nav{padding:9px 12px}main{padding:16px}.view-title{display:block}.view-title button{margin-bottom:14px}.facts{grid-template-columns:1fr}.facts dd{margin:0 0 8px}.toolbar{flex-wrap:wrap}.timeline-row{grid-template-columns:80px 1fr}.timeline-row small{display:none}}@media(prefers-color-scheme:light){:root{--bg:#f4f7fa;--panel:#fff;--panel2:#edf2f7;--text:#18212c;--muted:#607085;--line:#d7e0e9;--accent:#24965a;--accent2:#287fbd;--warn:#9c6413;--bad:#c83643;--shadow:0 8px 24px rgba(28,44,64,.08)}})CNACSS";
    }

    std::string_view InspectorAppJavaScript() noexcept
    {
        return R"CNAJS('use strict';
const uiToken=document.querySelector('meta[name="cna-ui-token"]').content;
const state={session:null,snapshot:null,resources:[],events:[],cursor:'0',resourceSort:{key:'id',direction:1},activeView:'session'};
const $=id=>document.getElementById(id);
const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const num=value=>Number(value||0);
const formatBytes=value=>{let n=num(value);if(!Number.isFinite(n))return 'unavailable';const units=['B','KiB','MiB','GiB'];let i=0;while(n>=1024&&i<units.length-1){n/=1024;i++}return `${n.toFixed(i?2:0)} ${units[i]}`};
const formatDuration=ns=>{const n=num(ns);if(n>=1e6)return `${(n/1e6).toFixed(2)} ms`;if(n>=1e3)return `${(n/1e3).toFixed(1)} µs`;return `${n.toFixed(0)} ns`};
const accuracy=value=>`<span class="accuracy ${esc(value)}">${esc(value)}</span>`;
async function api(path,options={}){const headers=new Headers(options.headers||{});headers.set('X-CNA-Inspector-UI-Token',uiToken);const response=await fetch(path,{...options,headers,cache:'no-store'});const body=await response.json();if(!response.ok)throw new Error(body.error||`HTTP ${response.status}`);return body}
function setConnection(kind,message){const node=$('connection');node.className=`status ${kind}`;node.innerHTML=`<span></span>${esc(message)}`}
function card(label,value,detail=''){return `<div class="card"><div class="label">${esc(label)}</div><div class="value">${esc(value)}</div>${detail?`<div class="detail">${esc(detail)}</div>`:''}</div>`}
function renderSession(){const s=state.session;if(!s)return;$('app-name').textContent=s.applicationName;$('session-cards').innerHTML=card('CNA version',s.cnaVersion)+card('Target',s.targetPlatform,s.platformBackend)+card('Renderer',s.graphicsRenderer)+card('Build',s.buildConfiguration)+card('Protocol',`${s.protocolMajor}.${s.protocolMinor}`,`provider interface ${s.providerInterfaceVersion}`)+card('Exposure',s.localOnly?'localhost only':'remote enabled');$('metadata').innerHTML=s.metadata.length?s.metadata.map(x=>`<dt>${esc(x.key)}</dt><dd>${esc(x.value)}</dd>`).join(''):'<dt>Status</dt><dd>No application metadata supplied</dd>';$('capabilities').innerHTML=Object.entries(s.capabilities).map(([key,value])=>`<span class="badge ${value?'on':'off'}">${esc(key)}: ${value?'available':'unavailable'}</span>`).join('');$('profiler-unavailable').classList.toggle('hidden',s.capabilities.events)}
function renderSnapshot(){const s=state.snapshot;if(!s)return;const frames=s.frames||[];const latest=frames.at(-1);const fps=latest?latest.framesPerSecond:0;const frameMs=latest?num(latest.durationNs)/1e6:0;$('performance-cards').innerHTML=card('FPS',fps?fps.toFixed(1):'—')+card('Frame time',frameMs?`${frameMs.toFixed(2)} ms`:'—')+card('Frame',s.currentFrameNumber)+card('Metrics',s.metricCount)+card('Resources',s.resourceCount)+card('Profiler memory',formatBytes(s.profilerOwnedBytes),'conservative lower bound');$('metrics-body').innerHTML=(s.metrics||[]).map(m=>`<tr><td>${esc(m.name)}</td><td>${esc(m.value)}</td><td>${esc(m.unit)}</td><td>${esc(m.kind)}</td><td>${accuracy(m.accuracy)}</td></tr>`).join('');drawFrames(frames);renderGraphics();renderAudio();renderLoss()}
function drawFrames(frames){const canvas=$('frame-graph');const ratio=devicePixelRatio||1;const width=Math.max(300,canvas.clientWidth);canvas.width=width*ratio;canvas.height=220*ratio;const ctx=canvas.getContext('2d');ctx.scale(ratio,ratio);ctx.clearRect(0,0,width,220);const values=frames.slice(-240).map(x=>num(x.durationNs)/1e6);if(!values.length){ctx.fillStyle=getComputedStyle(document.documentElement).getPropertyValue('--muted');ctx.fillText('No completed frame samples',12,24);return}const max=Math.max(20,...values)*1.1;ctx.strokeStyle=getComputedStyle(document.documentElement).getPropertyValue('--line');ctx.fillStyle=getComputedStyle(document.documentElement).getPropertyValue('--muted');ctx.font='11px system-ui';[8.33,16.67,33.33].forEach(ms=>{const y=205-ms/max*185;ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(width,y);ctx.stroke();ctx.fillText(`${ms.toFixed(ms<10?2:1)} ms`,6,y-4)});ctx.strokeStyle=getComputedStyle(document.documentElement).getPropertyValue('--accent2');ctx.lineWidth=2;ctx.beginPath();values.forEach((value,i)=>{const x=i/Math.max(1,values.length-1)*width;const y=205-Math.min(value,max)/max*185;i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.stroke()}
function namedMetrics(prefix){return (state.snapshot?.metrics||[]).filter(x=>x.name.startsWith(prefix))}
function metricValue(name){return state.snapshot?.metrics?.find(x=>x.name===name)?.value??'0'}
function renderGraphics(){const s=state.snapshot;if(!s)return;$('graphics-cards').innerHTML=card('Draw calls',metricValue('Graphics/DrawCalls'))+card('Primitives',metricValue('Graphics/SubmittedPrimitives'))+card('Texture changes',metricValue('Graphics/TextureBindingChanges'))+card('Resource metadata',s.resourceCount)+card('Declared resource bytes',formatBytes(s.registeredResourceBytes),'mixed exact / estimated aggregate');$('graphics-metrics').innerHTML=namedMetrics('Graphics/').map(m=>`<tr><td>${esc(m.name)}</td><td>${esc(m.value)}</td><td>${accuracy(m.accuracy)}</td></tr>`).join('')||'<tr><td colspan="3" class="empty">No graphics metrics published yet</td></tr>'}
function renderAudio(){const metrics=namedMetrics('Audio/');const resources=state.resources.filter(x=>x.kind==='audio-voice');$('audio-cards').innerHTML=card('Allocated voices',metricValue('Audio/AllocatedVoices'),'not necessarily audible')+card('Voice creations',metricValue('Audio/VoiceCreations'))+card('Audio resources',resources.length);$('audio-body').innerHTML=[...metrics.map(m=>`<tr><td>${esc(m.name)}</td><td>${esc(m.value)} ${esc(m.unit)}</td><td>${accuracy(m.accuracy)}</td></tr>`),...resources.map(r=>`<tr><td>#${esc(r.id)} ${esc(r.label)}</td><td>${esc(r.kind)}</td><td>${accuracy(r.byteAccuracy)}</td></tr>`)].join('')||'<tr><td colspan="3" class="empty">No audio diagnostics published yet</td></tr>'}
function renderLoss(extra={}){const s=state.snapshot||{};const before=num(extra.eventsDroppedBeforeStart);const producer=Math.max(num(s.producerEventsDropped),num(extra.producerEventsDropped));const overwritten=num(s.eventHistoryOverwrites);const malformed=num(s.malformedZoneCount)+num(s.malformedFrameCount);const parts=[];if(before)parts.push(`${before} events were unavailable before the requested cursor`);if(producer)parts.push(`${producer} producer events dropped`);if(overwritten)parts.push(`${overwritten} history overwrites`);if(malformed)parts.push(`${malformed} malformed profiling operations`);const banner=$('loss-banner');banner.classList.toggle('hidden',!parts.length);banner.textContent=parts.length?`Discontinuous diagnostics: ${parts.join('; ')}.`:''}
function renderEvents(){const filter=$('event-filter').value.toLowerCase();const filtered=state.events.filter(e=>`${e.name} ${e.kind} ${e.category} ${e.threadId}`.toLowerCase().includes(filter));$('event-summary').textContent=`${filtered.length} shown / ${state.events.length} retained locally (max 1,000)`;$('events-body').innerHTML=filtered.slice().reverse().map(e=>`<tr><td>${esc(e.sequence)}</td><td>${esc(e.frameNumber)}</td><td>${esc(e.threadId)}</td><td>${esc(e.kind)}</td><td>${esc(e.category)}</td><td>${esc(e.name||`name #${e.nameId}`)}</td><td>${e.durationNs!=='0'?formatDuration(e.durationNs):'—'}</td><td>${esc(e.value)}</td></tr>`).join('')||'<tr><td colspan="8" class="empty">No matching events</td></tr>';renderProfiler(filtered)}
function renderProfiler(events){const zones=events.filter(x=>x.kind==='zone');const aggregates=new Map;for(const event of zones){const key=`${event.category}\0${event.name}`;const current=aggregates.get(key)||{name:event.name||`name #${event.nameId}`,category:event.category,calls:0,total:0,max:0};const duration=num(event.durationNs);current.calls++;current.total+=duration;current.max=Math.max(current.max,duration);aggregates.set(key,current)}const hot=[...aggregates.values()].sort((a,b)=>b.total-a.total).slice(0,30);$('hot-zones').innerHTML=hot.map(x=>`<tr><td>${esc(x.name)}</td><td>${esc(x.category)}</td><td>${x.calls}</td><td>${formatDuration(x.total)}</td><td>${formatDuration(x.total/x.calls)}</td><td>${formatDuration(x.max)}</td></tr>`).join('')||'<tr><td colspan="6" class="empty">No retained CPU zones</td></tr>';const recent=zones.slice(-80);const max=Math.max(1,...recent.map(x=>num(x.durationNs)));$('timeline').innerHTML=recent.map(x=>`<div class="timeline-row"><span title="${esc(x.name)}">${esc((x.name||'unnamed').slice(0,16))}</span><div class="timeline-track"><progress class="timeline-bar" max="100" value="${Math.max(1,num(x.durationNs)/max*100)}"></progress></div><small>${formatDuration(x.durationNs)}</small></div>`).join('')||'<p class="empty">No retained CPU zones</p>'}
function renderResources(){const filter=$('resource-filter').value.toLowerCase();const kind=$('resource-kind').value;let rows=state.resources.filter(r=>(!kind||r.kind===kind)&&`${r.id} ${r.kind} ${r.label} ${r.format}`.toLowerCase().includes(filter));const {key,direction}=state.resourceSort;rows.sort((a,b)=>{let left=key==='dimensions'?a.width*a.height*a.depth:a[key];let right=key==='dimensions'?b.width*b.height*b.depth:b[key];if(typeof left==='string'&&/^\d+$/.test(left)&&typeof right==='string'&&/^\d+$/.test(right)){left=BigInt(left);right=BigInt(right)}return left<right?-direction:left>right?direction:0});$('resource-summary').textContent=`${rows.length} shown / ${state.resources.length} fetched`;$('resources-body').innerHTML=rows.map(r=>{const previewable=['texture-2d','texture-cube','render-target-2d','render-target-cube'].includes(r.kind);const enabled=previewable&&state.session?.capabilities.resourcePreviews;return `<tr><td>${esc(r.id)}</td><td>${esc(r.kind)}</td><td>${esc(r.label||'—')}</td><td>${r.width}×${r.height}${r.depth>1?`×${r.depth}`:''}</td><td>${esc(r.format||'—')}</td><td>${r.mipCount}</td><td>${r.byteAccuracy==='unavailable'?'unavailable':formatBytes(r.estimatedBytes)}</td><td>${accuracy(r.byteAccuracy)}</td><td><button class="resource-preview" data-id="${esc(r.id)}" ${enabled?'':'disabled'} title="${enabled?'Manual bounded readback':'No preview capability for this resource'}">Preview</button></td></tr>`}).join('')||'<tr><td colspan="9" class="empty">No matching resource metadata</td></tr>';document.querySelectorAll('.resource-preview:not([disabled])').forEach(button=>button.onclick=()=>requestPreview(button.dataset.id));renderAudio()}
async function requestPreview(id){const dialog=$('preview-dialog');$('preview-title').textContent=`Resource #${id}`;$('preview-state').textContent='Requesting bounded preview…';$('preview-state').classList.remove('hidden');$('preview-image').classList.remove('visible');dialog.showModal();try{let result=await api(`/api/preview?id=${encodeURIComponent(id)}`,{method:'POST'});const deadline=Date.now()+10000;while(result.status==='pending'&&Date.now()<deadline){$('preview-state').textContent='Asynchronous readback pending…';await new Promise(resolve=>setTimeout(resolve,250));result=await api(`/api/preview?ticket=${encodeURIComponent(result.ticket)}`)}if(result.status==='ready'){const image=$('preview-image');image.src=`data:${result.mimeType};base64,${result.data}`;image.width=result.width;image.height=result.height;image.classList.add('visible');$('preview-state').classList.add('hidden')}else $('preview-state').textContent=result.message||`Preview ${result.status}`}catch(error){$('preview-state').textContent=error.message}}
async function loadResources(){try{const response=await api('/api/snapshot?resources=1');state.resources=response.resources||[];const kinds=[...new Set(state.resources.map(x=>x.kind))].sort();$('resource-kind').innerHTML='<option value="">All kinds</option>'+kinds.map(x=>`<option>${esc(x)}</option>`).join('');renderResources()}catch(error){setConnection('reconnecting','reconnecting')}}
async function poll(){try{if(!state.session){state.session=await api('/api/session');state.snapshot=null;state.resources=[];state.events=[];state.cursor='0';renderSession();renderResources();renderEvents()}state.snapshot=await api('/api/snapshot');renderSnapshot();if(state.session.capabilities.events){const batch=await api(`/api/events?after=${encodeURIComponent(state.cursor)}&max=512`);state.cursor=batch.events.length?batch.events.at(-1).sequence:batch.newestAvailableSequence;if(batch.events.length){state.events.push(...batch.events);if(state.events.length>1000)state.events.splice(0,state.events.length-1000);renderEvents()}renderLoss(batch)}setConnection('connected','connected')}catch(error){state.session=null;setConnection('reconnecting','reconnecting')}finally{setTimeout(poll,500)}}
document.querySelectorAll('#nav button').forEach(button=>button.onclick=()=>{document.querySelectorAll('#nav button').forEach(x=>x.classList.toggle('active',x===button));document.querySelectorAll('.view').forEach(x=>x.classList.toggle('active',x.id===button.dataset.view));state.activeView=button.dataset.view;if(state.activeView==='resources'&&!state.resources.length)loadResources()});$('refresh-resources').onclick=loadResources;$('resource-filter').oninput=renderResources;$('resource-kind').onchange=renderResources;$('event-filter').oninput=renderEvents;$('clear-events').onclick=()=>{state.events=[];renderEvents()};document.querySelectorAll('#resource-table th[data-sort]').forEach(header=>header.onclick=()=>{const key=header.dataset.sort;if(state.resourceSort.key===key)state.resourceSort.direction*=-1;else state.resourceSort={key,direction:1};renderResources()});addEventListener('resize',()=>state.snapshot&&drawFrames(state.snapshot.frames||[]));poll();
)CNAJS";
    }
}
