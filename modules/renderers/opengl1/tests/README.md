# OPENGL1 renderer tests

Configure CNA with `-DCNA_GRAPHICS_RENDERER=OPENGL1` on Linux or Windows. The renderer requires a desktop OpenGL compatibility implementation. Linux CI can use Xvfb plus Mesa/llvmpipe; Windows CI can at minimum compile/link the renderer and run on a software/VM OpenGL implementation where available.

Run the suite with `ctest -R "OpenGL1_"` from the build directory (needs `-DCNA_BUILD_TESTS=ON -DCNA_BUILD_EXAMPLES=ON`, real `DISPLAY`/Xvfb; see `cmake/Tests/OpenGL1Tests.cmake`).

The shared, renderer-agnostic `CnaTests` gtest binary also builds and mostly passes under `CNA_GRAPHICS_RENDERER=OPENGL1` (plans/plan_opengl1.md phase 12's Reach-profile audit ran it in full for the first time). Two shared test files had EasyGL-only/D3D9-only-shaped assumptions that didn't anticipate OPENGL1 as a second, legitimately-different 3D-capable renderer -- both fixed with `#ifdef CNA_RENDERER_OPENGL1` branches, see plans/plan_opengl1.md phase 12 for the detail. The other ~126 `CnaTests` failures on this sandbox are pre-existing and environment-specific (Media/Video/Sound/Xnb content fixtures needing files/codecs this sandbox doesn't have), unrelated to OPENGL1.

Priority runtime smoke coverage, each wired to a real CTest registration:
1. clear + present — `OpenGL1_PixelTestGame_Smoke`;
2. SpriteBatch textured quad — `OpenGL1_TexturedQuad_Readback`;
3. colored 3D triangle with depth — `OpenGL1_RasterizerState_CullMode`;
4. textured 3D triangle — `OpenGL1_BasicEffect_TextureEnabled`;
5. indexed cube with depth/culling — `OpenGL1_RasterizerState_CullMode_IndexedBasicEffect`;
6. fixed-function directional lighting on `VertexPositionNormalTexture` — also `OpenGL1_RasterizerState_CullMode_IndexedBasicEffect` (uses `BasicEffect.EnableDefaultLighting()`);
7. fog and alpha-test approximations — `OpenGL1_Fog_AlphaTest` (`modules/renderers/opengl1/examples/opengl1_fog_alphatest_test.cpp`, OPENGL1-specific: asserts only the qualitative/coarse behavior this renderer's real fixed-function `GL_FOG`/`glAlphaFunc` approximation can honestly claim — see that file's header for why the shader-exact EasyGL fog/alpha-test files aren't reused as-is);
8. stencil and scissor state — `OpenGL1_Scissor` + `OpenGL1_DepthStencilState_StencilOps`.

Scenarios 1-6 and 8 reuse already-verified, renderer-agnostic example sources (real public `Game`/`GraphicsDevice`/`BasicEffect`/`SpriteBatch` API only) from `examples/`, confirmed by inspection to contain no EasyGL-specific code before reuse.

Additional coverage beyond the 8 priority scenarios:
- `OpenGL1_Anisotropic_GlState` (`modules/renderers/opengl1/examples/opengl1_anisotropic_gl_state_test.cpp`) — plans/plan_opengl1.md phase 1's real consumer: verifies `GL_EXT_texture_filter_anisotropic` detection (`OpenGL1Capabilities`) actually reaches the driver through `SamplerState`/`ApplySamplerState()` — sub-cap request round-trips, an over-cap request gets clamped to the driver's real max, and switching back to a non-anisotropic filter resets `GL_TEXTURE_MAX_ANISOTROPY_EXT` to 1.0 rather than leaking stale state. Skips cleanly (exit 0) if the extension genuinely isn't present.
- `OpenGL1_RenderTarget2D_Sample` — plans/plan_opengl1.md phase 2 (RenderTarget2D via FBO): reused verbatim from the SDL_Renderer renderer's own Task 705 test (`modules/renderers/sdl-renderer/examples/sdlrenderer_rendertarget2d_sample_test.cpp`, renderer-agnostic). Draws a distinctive corner marker into a render target, unbinds it, then samples it back as a plain `Texture2D` via `SpriteBatch` — the marker's position surviving the round trip is exactly the proof that render-target orientation (see "Bugs found" below) is handled correctly.
- `OpenGL1_DualTextureEffect_Doubling` — plans/plan_opengl1.md phase 3 (`ARB_multitexture`/core-1.3 dual-texture path): reused verbatim from EasyGL's own Task 383 test (`modules/renderers/easygl/examples/easygl_dualtextureeffect_doubling_test.cpp`, renderer-agnostic). Isolates FNA's real `texture0.rgb *= 2` doubling factor (`tex0=gray(100), tex1=white → gray(200)`, distinct from the `gray(100)` a naive multiply-without-doubling would produce) from a plain sanity check (`two white textures, diffuse=red → red`).
- `OpenGL1_GraphicsCapability` (`modules/renderers/opengl1/examples/opengl1_graphics_capability_test.cpp`) — plans/plan_opengl1.md phase 11: locks in `GraphicsDevice::SupportsCapability()`'s truth table against concrete evidence rather than just the flag value — `AnisotropicFiltering` is cross-checked against `glGetString(GL_EXTENSIONS)` directly, and `WireFrame=true` is cross-checked against an actual `glGetIntegerv(GL_POLYGON_MODE)` readback after setting `FillMode::WireFrame`. Extended for plans/plan_opengl1.md item 23 (EasyGL parity: real occlusion queries) — `OcclusionQuery` is cross-checked against an independent raw `GL_VERSION`/`GL_EXTENSIONS` scan (same style as `AnisotropicFiltering`), then backed by a real, functionally meaningful query: an unoccluded full-viewport quad reports `PixelCount()` matching the real viewport pixel area (exact `512` for a `32x16` viewport), and the same quad fully covered by a nearer opaque occluder under real depth testing reports `PixelCount()==0` — genuine `GL_LESS_EQUAL` depth-test rejection via `GL_SAMPLES_PASSED`, not a placeholder. Both the capability-detection fix and the `GL_SAMPLES_PASSED` mechanism itself are mutation-tested independently. Falls back to the original "never completes" assertion on a driver that genuinely lacks the extension.
- `OpenGL1_BasicEffect_Combined` — plans/plan_opengl1.md phase 4 (fixed-function texture environment/combine mappings): reused verbatim from EasyGL's own Task 370 capstone test (`modules/renderers/easygl/examples/easygl_basiceffect_combined_test.cpp`, renderer-agnostic). Asserts the exact multiplicative formula `TextureColor x VertexColor x (DiffuseColor+EmissiveColor)` across 4 independently-colored texels — this is the test that caught `DrawInternal`'s stride==16/24 paths ignoring `diffuseColor` entirely (see "Bugs found" below).
- `OpenGL1_EnvironmentMapEffect` (`modules/renderers/opengl1/examples/opengl1_environmentmapeffect_test.cpp`, OPENGL1-specific) — plans/plan_opengl1.md phase 5 (`ARB_texture_cube_map` cube maps + `EnvironmentMapEffect`'s fixed-function reflection-mapping subset). Uses a solid-color cube map to isolate the `GL_REFLECTION_MAP`/`GL_INTERPOLATE` blend formula from reflection-vector face selection: `Amount=0`/`1` prove each term is fully suppressed in turn, `Amount=0.5` proves the blend is a genuine `mix()` and not a saturating additive combine (same 0/1-blind-spot precedent `docs/environmentmapeffect-support.md`'s own Task 383/394 already established). Pixel-exact on the first attempt. Deliberately does not test Fresnel edge-weighting or `EnvironmentMapSpecular` — both are documented, intentional OPENGL1 fixed-function limitations (see the test file's own header and plans/plan_opengl1.md).
- `OpenGL1_ContextLoss` (`modules/renderers/opengl1/examples/opengl1_context_loss_test.cpp`, OPENGL1-specific) — plans/plan_opengl1.md phase 8 (context-loss resource recreation registry). Draws a `Texture2D`, calls `dev.GetRenderer().DebugSimulateContextLoss()` (destroys and recreates the real SDL GL context), then re-draws and reads back the SAME pixel to prove `OpenGL1ResourceRegistry` actually rebuilt the texture from its retained CPU-side pixel data rather than leaving a stale/blank handle. Does the same directly (`TextureCube::GetData()`, no draw needed) for a `TextureCube` face, plus confirms a never-written face survives recovery without crashing — this closed plans/plan_opengl1.md phase 8's own previously-documented gap (`ITextureCubeRenderer` gained a `ShareCpuPixels(face,...)` hook, added with a default no-op so every other renderer stays source-compatible). Separately keeps a `RenderTarget2D` bound as the ACTIVE render target *through* the simulated loss and reads its own FBO back directly via `RenderTarget2D::GetData()` (not indirectly through `SpriteBatch`/the backbuffer — see plans/plan_opengl1.md phase 8's own writeup for why that indirect approach can't actually discriminate a real bug) to prove `OpenGL1Renderer` correctly re-binds it after recovery, not silently leaving every post-recovery draw landing on the backbuffer instead. This test has caught 2 real, mutation-tested bugs across two nights of work — see plans/plan_opengl1.md phase 8 for the full writeup.
- `OpenGL1_GoldenImage_Smoke`, `OpenGL1_BasicEffect_Golden`, `OpenGL1_SpriteBatch_Rotation_Golden`, `OpenGL1_TextureFilter_Linear_Golden`, `OpenGL1_BlendState_Additive_Golden`, `OpenGL1_DualTextureEffect_Golden`, `OpenGL1_RasterizerState_CullMode_Golden`, `OpenGL1_DepthStencilState_WriteEnable_Golden`, `OpenGL1_AlphaTestEffect_Golden` — plans/plan_opengl1.md phase 10 (visual golden-image tests): 9 of EasyGL's own golden-image `.cpp` files reused verbatim (renderer-agnostic, real public API only), each comparing an 8x8 live-rendered region against the EXACT same checked-in `examples/golden/*.png` EasyGL uses, via the shared `PixelTestGame::CompareGoldenImage()` helper — no new reference images. All 9 pixel-match on the first attempt despite OPENGL1's completely different (legacy fixed-function/immediate-mode) rasterizer, because every reused scene was originally chosen for a flat, edge-free sample region (constant UV, no antialiased boundary inside the checked 8x8 box). `PbrEffect`/`SkinnedEffect`/`SkinnedPbrEffect`/`EnvironmentMapEffect`'s own golden scenes are deliberately NOT reused — see plans/plan_opengl1.md phase 10 for why.
- `OpenGL1_MipmapGeneration` (`modules/renderers/opengl1/examples/opengl1_mipmap_generation_test.cpp`, OPENGL1-specific) — plans/plan_opengl1.md phase 6 (automatic mipmap generation). A single-pixel checkerboard texture's 2x2 box average is EXACTLY uniform gray at every mip level beyond level 0 regardless of driver-specific averaging/LOD-rounding details, making it a robust discriminator: sampled at native size it reads sharply aliased (near-white), minified 4x with `TextureFilter.LinearMipPoint` it reads exact `(128,128,128)` mid-gray — proof the mip chain was both generated (via `glGenerateMipmap`/`GL_GENERATE_MIPMAP`/CPU box-filter, whichever the driver supports) and actually sampled (via `ApplySamplerState`'s now-full 9-value `TextureFilter`→GL min/mag mapping, not just the old flat Point-vs-Linear split).
- `OpenGL1_SamplerState_BindOrder` (`modules/renderers/opengl1/examples/opengl1_samplerstate_bind_order_test.cpp`, OPENGL1-specific) — 3 real SamplerState bugs found and fixed the same day as phase 6 (see plans/plan_opengl1.md phase 6's own "Adjacent finding" entry for the full writeup): `ApplySamplerState` firing before the corresponding texture is bound, its `slot` parameter being ignored entirely, and `SpriteBatch`'s own address-mode never being applied. Mutation-tested: temporarily reverted the fix, confirmed both checks fail with the exact predicted wrong values, restored the fix, reconfirmed the exact predicted correct values. Deliberately uses the non-default `TextureAddressMode.Clamp` and grayscale-magnitude texels rather than the more obvious red/green + default-Wrap design — an earlier version of this test using the XNA default (`Wrap`) passed even against the deliberately-reintroduced bug, because the bug's fallback behavior coincidentally also produces `Wrap`-like results; this is exactly the kind of false-negative this project's own "verify against the real driver, not by inspection" culture exists to catch.
- `OpenGL1_SwapInterval` (`modules/renderers/opengl1/examples/opengl1_swapinterval_test.cpp`, OPENGL1-specific) — plans/plan_opengl1.md item 20 (EasyGL parity: runtime `SetSwapInterval()`). Before this, `OpenGL1Renderer` never overrode `IGraphicsRenderer::SetSwapInterval()` at all (inherited the interface's no-op default) — only the construction-time value ever reached SDL, so a runtime vsync change silently did nothing. Verified directly against the real driver via `SDL_GL_GetSwapInterval()`: `SetSwapInterval(0)` then `SetSwapInterval(1)`, asserting the driver reports `0` then `1` — the second call proves this is a genuine runtime change and not a value that merely happened to already match construction time. Mutation-tested: reverting the override to a no-op reproduces the predicted failure (`SDL_GL_GetSwapInterval=1` after `SetSwapInterval(0)`); restoring the fix reproduces the predicted correct values.
- `OpenGL1_RenderTarget2D_Mip` (`modules/renderers/opengl1/examples/opengl1_rendertarget2d_mip_test.cpp`, OPENGL1-specific) — plans/plan_opengl1.md item 21 (EasyGL parity: RenderTarget2D mip-chain generation on unbind). Before this, `CreateRenderTarget2D` silently dropped its own `mipMap` parameter, and every 3D/SpriteBatch texture-sampling call site decided mip-awareness via `dynamic_cast<const OpenGL1TextureRenderer*>` — always null for a render target — forcing `mip=false` regardless. Fixed with `OpenGL1RenderTargetRenderer::UnbindAsRenderTarget()` calling `glGenerateMipmap` on every unbind, plus a shared `HasMipsFor()` helper all sampling call sites now use. Draws an 8x8-block checkerboard into a `mipMap=true` render target via `SpriteBatch`, unbinds it, then samples it back at native size (must alias sharply, far from gray — proves the pattern itself is a valid high-frequency probe) and heavily minified (must read exact mid-gray — proves a real, box-filtered mip level was generated and sampled), reusing `OpenGL1_MipmapGeneration`'s own proven discriminator. Two independent mutation tests: reverting mip generation alone, and separately forcing the sampling-path helper to always report no-mips with generation intact — both reproduce the identical predicted failure (minified sample reads pure white `(255,255,255)` instead of `(128,128,128)` mid-gray), confirming both fixes are independently load-bearing. A solid-color-fill version of this test, and a first checkerboard version using a naive minification amount, both passed even with the bug reintroduced (see plans/plan_opengl1.md item 21's own "Test-design finding" for the exact false-pass mechanism) before landing on this discriminator.
- `OpenGL1_MSAA` (`modules/renderers/opengl1/examples/opengl1_msaa_test.cpp`, OPENGL1-specific) — plans/plan_opengl1.md item 22 (EasyGL parity: backbuffer MSAA). Before this, `SDL_GL_MULTISAMPLEBUFFERS`/`SAMPLES` were never requested, `GetMultiSampleCount()` always returned 0, and `SupportsCapability(MultiSampleAntiAliasing)` was hardcoded false. Constructs a `GraphicsDevice` directly (not via `Game`/`GraphicsDeviceManager` — see the test file's own header for why that common idiom cannot reach this fix, a pre-existing `Game::GraphicsDevice_` construction-order constraint) with `PresentationParameters.MultiSampleCount=4`, then asserts `IGraphicsRenderer::GetMultiSampleCount()==4` (the honestly-applied count, not the `PresentationParameters` echo — a second finding documented in the test/plan) and `SupportsCapability(MultiSampleAntiAliasing)==true`, both mutation-tested independently. Also draws a triangle whose edge passes exactly through a known pixel center and prints (diagnostic only, not asserted — confirmed via a standalone CNA-independent SDL3+OpenGL probe to be a real Mesa llvmpipe software-rasterizer resolve-on-read limitation on this sandbox, not a bug in the fix) whether real per-sample coverage blending is observable.
- `OpenGL1_DirectionalLight12` (`modules/renderers/opengl1/examples/opengl1_directionallight12_test.cpp`, OPENGL1-specific) — plans/plan_opengl1.md item 14 (EasyGL parity: `BasicEffect.DirectionalLight1`/`DirectionalLight2`). Before this, `DrawInternal`'s lit path only ever configured `GL_LIGHT0`, silently dropping `DirectionalLight1`/`DirectionalLight2` entirely even though `GpuDrawParams` already carried both lights' data. Explicitly disables `DirectionalLight0` (so the test cannot pass via the already-working light0 path alone), sets `DirectionalLight1`=red and `DirectionalLight2`=blue both shining straight at a camera-facing quad, and asserts the result is exactly magenta `(255,0,255)` — matched exactly on the first attempt. Mutation-tested: removing the `GL_LIGHT1`/`GL_LIGHT2` configuration reproduces the predicted failure (pure black, both lights silently dropped).
- `OpenGL1_Specular` (`modules/renderers/opengl1/examples/opengl1_specular_test.cpp`, OPENGL1-specific) — plans/plan_opengl1.md item 15 (EasyGL parity: `BasicEffect` specular highlights). Before this, `GpuDrawParams::specularColor`/`specularPower`/`light0-2Specular` were never read by `DrawInternal` at all. Isolates the specular term completely (all diffuse/ambient/emissive contributions black) and places a small quad away from the origin along -Z with `View`/`World=Identity` so the local-viewer eye position and light direction are both close to the surface normal — the classic near-maximal Blinn-Phong configuration, avoiding a razor-thin highlight fragile to assert against. `SpecularColor=black` reads pure black (sanity); `SpecularColor=white` reads a strong real highlight (`R=236`). Mutation-tested: removing the `GL_SPECULAR`/`GL_SHININESS`/`GL_LIGHT_MODEL_*` configuration reproduces the predicted failure (`R=0`, specular silently dropped).
- `OpenGL1_TextureAddressMode_Mirror` (`modules/renderers/opengl1/examples/opengl1_textureaddressmode_mirror_test.cpp`, OPENGL1-specific) — plans/plan_opengl1.md item 16 (EasyGL parity: `TextureAddressMode.Mirror`). Before this, `Mirror` was silently treated as `Clamp` at both the 3D sampling call site and `SpriteBatch::Draw`'s own. Samples a 2x1 red/blue texture across `U=0..2` with `AddressU=Mirror` and `TextureFilter.Point` — the three wrap modes (`Wrap`/`Clamp`/`Mirror`) are only distinguishable in the second repeat, and specifically only at `U=1.75` (`U=1.25` reads blue under both real `Mirror` and the pre-existing `Clamp` bug, a coincidence of this texel layout — `U=1.75` reads red only under genuine `Mirror`, blue under both `Clamp` and plain `Wrap`). Matched the predicted values exactly on the first attempt. Mutation-tested: reverting to the old binary wrap mapping reproduces the predicted failure at `U=1.75` specifically, while confirming `U=1.25` alone would NOT have caught the bug.
- `OpenGL1_BlendFactor`/`OpenGL1_BlendEquation`/`OpenGL1_BlendAlphaFactor` (`modules/renderers/opengl1/examples/opengl1_blendfactor_test.cpp`/`opengl1_blendequation_test.cpp`/`opengl1_blendalphafactor_test.cpp`, OPENGL1-specific) — plans/plan_opengl1.md items 17/18/19 (EasyGL parity: constant blend color, blend equation beyond additive, separate alpha blend factors — all core GL 1.4, implemented together). All three use the 3D draw path (`BasicEffect`+`DrawUserPrimitives`), not `SpriteBatch` — found while writing `OpenGL1_BlendFactor` that `OpenGL1SpriteBatchRenderer::Begin()` hardcodes its own `glBlendFunc(GL_SRC_ALPHA,GL_ONE_MINUS_SRC_ALPHA)` independent of any `BlendState` a game passes to `SpriteBatch.Begin()`, a separate pre-existing limitation that would otherwise mask all three fixes. `OpenGL1_BlendFactor`: white over black with `BlendFactor=(128,0,0,255)` → correct `(128,0,0)` (pre-existing bug: white `(255,255,255)`), matched on the first attempt; mutation-tested (reverting `SetBlendFactor` to a no-op) reproduces a distinct third wrong answer `(0,0,0)`. `OpenGL1_BlendEquation`: opaque dest `(200,50,50)` then `ReverseSubtract` source `(50,20,20)` → correct `(150,30,30)` (pre-existing bug: always-Add `(250,70,70)`), matched on the first attempt; mutation-tested exactly. `OpenGL1_BlendAlphaFactor`: uses a `RenderTarget2D` for a genuine alpha-channel readback (`RenderTarget2D::GetData()`'s real `glReadPixels`) — dest alpha=100, source alpha=200 with `AlphaSourceBlend=Zero`/`AlphaDestinationBlend=One` → correct alpha=100 (pre-existing bug: alpha=200, reusing color's factors), matched on the first attempt; mutation-tested exactly. Full `ctest -R "OpenGL1_"` regression sweep: 35/35 passed.
- `OpenGL1_PresentationMode` (`modules/renderers/opengl1/examples/opengl1_presentationmode_test.cpp`, OPENGL1-specific) — plans/plan_opengl1.md item 13 (EasyGL parity: virtual-resolution/presentation-mode scaling). Before this, `EffectiveWidth()`/`EffectiveHeight()` (`OpenGL1SpriteBatchRenderer::Begin()`'s own `glOrtho` range) always returned the raw, never-recomputed construction-time size, and `TransformWindowToLogical`/`TransformLogicalToWindow` were never overridden. Confirms a baseline stripe at logical X=160 lands at physical X=160 (320x240, unresized), genuinely resizes the real SDL window to 480x480 (a real 4:3→1:1 aspect change) via `SDL_SetWindowSize`+`SDL_SyncWindow` (found empirically that `SDL_SetWindowSize` alone is asynchronous even under this sandbox's Xvfb), then confirms a stripe at logical X=120 lands at physical X=~240 (the real centre, matching the correctly-recomputed logical width 240) rather than ~180 (the pre-existing stuck-at-320 bug's answer) — matched exactly on the first attempt. Also checks `TransformWindowToLogical`/`TransformLogicalToWindow` as an exact inverse pair against the same scale. Mutation-tested three ways independently (reverting the recomputation; reverting `TransformWindowToLogical` alone) — each reproduces its own predicted failure with no cross-check masking. See plans/plan_opengl1.md item 13 for why `GetViewportSize()` itself was deliberately NOT changed (it feeds the real `glViewport()` call directly; only the `SpriteBatch`-facing logical size needed fixing).
- `OpenGL1_RenderTargetCube` (`modules/renderers/opengl1/examples/opengl1_rendertargetcube_test.cpp`, OPENGL1-specific) — plans/plan_opengl1.md item 24 (further improvement beyond EasyGL parity: `RenderTargetCube`). Before this, `CreateRenderTargetCube()` inherited the `IGraphicsRenderer` `nullptr` default. Renders two different solid colors into two different faces of the same target (+X red, +Y blue), unbinds, and reads both back independently via `RenderTargetCube::GetData()` (a genuine GPU readback via `glGetTexImage`, not a CPU shadow) — confirming each face kept its own distinct content, not just "something got painted somewhere". A third, never-rendered face is checked to have picked up neither color. Also exercises `DebugSimulateContextLoss()` while a face is genuinely bound and active, matching the established item-8 rigor for `RenderTarget2D`/`TextureCube`. All 4 checks matched on the first attempt. Mutation-tested two ways independently: `BindAsRenderTargetFace()` forced to always attach face 0 (reproduces the predicted last-write-wins/never-written failure pattern exactly), and `CreateRenderTargetCube()` forced to always return `nullptr` (reproduces the predicted graceful all-black degradation, no crash).
- `OpenGL1_RenderTarget2D_MSAA` (`modules/renderers/opengl1/examples/opengl1_rendertarget2d_msaa_test.cpp`, OPENGL1-specific) — plans/plan_opengl1.md item 25 (further improvement beyond EasyGL parity: `RenderTarget2D` MSAA). Before this, `GetMultiSampleCount()` was hardcoded 0 and the `multiSampleCount` constructor argument was silently ignored. Creates a `RenderTarget2D` with `preferredMultiSampleCount=4` and checks: `MultiSampleCount` reflects a real, genuinely-applied value (not silently 0); a solid-color fill survives the separate-FBO render+`glBlitFramebuffer` resolve pipeline intact (a broken MSAA FBO or resolve blit could easily produce black/garbage even for trivial full-coverage content); an edge-crossing triangle produces a genuinely blended pixel at the diagonal edge, proving a real multisample resolve occurred, not a degenerate copy. Unlike `OpenGL1_MSAA`'s own backbuffer test (downgraded to diagnostic-only after a standalone probe proved even a driver-confirmed multisample GLX window visual reads back perfectly binary on this sandbox's Mesa llvmpipe), this render-target path uses its own explicit `glBlitFramebuffer` resolve and empirically DOES produce a real, exactly-mid-value blended pixel (`R=128` exactly) — reliable enough to assert as a real hard check here. All 3 checks matched on the first attempt. Mutation-tested two ways independently: `BindAsRenderTarget()` forced to always bind the single-sample FBO regardless of `multiSampleCount_` (reproduces the predicted corruption — resolved centre reads `(0,0,0)` instead of `(0,200,0)`, since the resolve blit overwrites correct content with garbage from the never-written MSAA buffer), and `BuildMsaa()` forced to leave `multiSampleCount_=0` even after a fully successful build (reproduces a DIFFERENT, complementary failure signature: `MultiSampleCount` check fails while the fill-survives check still PASSES, since `multiSampleCount_` also drives the bind branch and the single-sample fallback still renders correctly). Full `ctest -R "OpenGL1_"` regression sweep: 38/38 passed.

## Bugs found and fixed while wiring this suite up

- `OpenGL1Renderer::glContext_` was `void*` instead of `SDL_GLContext` — didn't compile against SDL3's typed `SDL_GL_MakeCurrent`/`SDL_GL_DestroyContext`.
- `ApplyRasterizerState` mapped `CullMode::CullClockwiseFace`/`CullCounterClockwiseFace` to `glCullFace(GL_FRONT/GL_BACK)` backwards relative to GL's default CCW-front convention — XNA's default `RasterizerState` (`CullCounterClockwiseFace`) was back-face-culling every `SpriteBatch` quad and any 3D triangle with the same winding, so nothing but the `Clear()` color ever reached the screen.
- `OpenGL1SpriteBatchRenderer::Draw()` translated by `(dest + origin)` before rotating and then subtracted `origin` again, double-applying the origin offset instead of leaving it invariant under rotation (same bug class as the SDL_Renderer renderer's own Task 671 fix).
- `DrawInternal`'s stride==20 (`VertexPositionTexture`) vertex path hardcoded `glColor4f(1,1,1,1)`, silently ignoring `GpuDrawParams::diffuseColor` — any `BasicEffect.TextureEnabled` draw with a non-white `DiffuseColor` and no vertex-color channel rendered with the tint completely missing.
- `OpenGL1Renderer` requested `SDL_GL_STENCIL_SIZE=8` only in its own constructor, which runs after `GraphicsDevice` has already created the SDL window. On X11/GLX (unlike EasyGL's ES/EGL context path, which can defer config selection to `SDL_GL_CreateContext()` time), the window's visual — and therefore its depth/stencil buffer bits — is fixed at `SDL_CreateWindow()` time; the late request was silently dropped, producing a 0-bit stencil buffer and making every `DepthStencilState.StencilEnable` a permanent no-op. Fixed by requesting `SDL_GL_STENCIL_SIZE`/`SDL_GL_DEPTH_SIZE`/`SDL_GL_DOUBLEBUFFER` in `GraphicsDevice.cpp` before `SDL_CreateWindow()` for `CNA_RENDERER_OPENGL1`, in addition to the (still-needed, now-redundant-but-harmless) request in the renderer's own constructor. Confirmed empirically via `glGetIntegerv(GL_STENCIL_BITS)`: 0 before the fix, 8 after.
- `ReadBackbuffer` was unimplemented (inherited the default that throws), so `GraphicsDevice::GetBackBufferData()` — and therefore every pixel-readback test in this suite — could not run at all against this renderer. Implemented via `glReadPixels` off `GL_BACK`, matching the EasyGL renderer's own y-flip convention.
- `OpenGL1SpriteBatchRenderer::Begin()` never disabled `GL_CULL_FACE` — real XNA `SpriteBatch` is never subject to 3D face culling, but a game's `RasterizerState.CullMode` could silently make sprites disappear depending on winding. Latent on the backbuffer (the fixed quad winding happened to survive the default state); became directly observable once render-target rendering exercised a genuinely different code path. See plans/plan_opengl1.md's "Bugs found while adding test coverage" section for this and two more (`GL_CLAMP` vs `GL_CLAMP_TO_EDGE`, and render-target texture-sampling orientation) found while getting `OpenGL1_RenderTarget2D_Sample` green.
- `DrawInternal`'s stride==16 (`VertexPositionColor`) and stride==24 (`VertexPositionColorTexture`) vertex paths used the raw per-vertex color alone (`glColor4ub`), silently ignoring `GpuDrawParams::diffuseColor` — the same bug class as the stride==20 fix above, but for the vertex-color paths. XNA's real `BasicEffect.VertexColorEnabled` formula multiplies vertex color BY the material's `(DiffuseColor+EmissiveColor)*Alpha`, not vertex color alone. Fixed with an `emitVertexColor` lambda that does the component-wise multiply before `glColor4f`. Caught by `OpenGL1_BasicEffect_Combined`.
- `DrawInternal`'s lit (stride 32, `VertexPositionNormalTexture`) path enabled `GL_COLOR_MATERIAL`/`GL_AMBIENT_AND_DIFFUSE` but never set `GL_EMISSION` — any lit draw's `EmissiveColor` (`BasicEffect`) or combined `EmissiveColor`+`AmbientLightColor*DiffuseColor` (`EnvironmentMapEffect`) was silently dropped from the rendered color entirely. Fixed with `glMaterialfv(GL_FRONT_AND_BACK,GL_EMISSION,...)` from `GpuDrawParams::emissiveColor`. Caught while building `OpenGL1_EnvironmentMapEffect` — its `Amount=0` case would have read back black instead of the expected ambient-only base color without this fix.
