{
  "schema_version": 1,
  "themes": [
    {
      "id": "inward-direction",
      "title": "The inward direction of a two-way contract",
      "match": [
        "the **inward** direction"
      ],
      "note": "Some CNA surfaces come in pairs: one class a game calls, and a separate one the implementing module calls back with facts about the build. `GraphicsRendererSelection` and `GraphicsRendererSelectionAccessEXT` are the example, and the canonical header keeps them apart so the two directions are not confusable. The C ABI binds the outward half only: telling the selection which renderers are compiled in is a fact about the build, not a choice a caller may make."
    },
    {
      "id": "platform-hooks",
      "title": "Hooks the platform layer calls into",
      "match": [
        "the `INTERNAL_` prefix marks the hooks"
      ],
      "note": "The platform layer hands input its window and its touch state through methods whose names say `INTERNAL_` out loud. A C caller owns no window handle and must not be able to re-point input at one; it reads input through the snapshot routes instead."
    },
    {
      "id": "platform-substrate",
      "title": "The platform substrate the ABI is built on",
      "match": [
        "platform substrate",
        "is the audio module's platform substrate"
      ],
      "note": "Interfaces a backend implements and the value types they exchange -- audio devices, recording devices, their formats. The C ABI is built ON this layer rather than exposing it: a C caller reaches the behaviour through the routes that use it, never through a C++ interface it would have to implement. The platform module itself is excluded from the inventory for the same reason (CBIND-047)."
    },
    {
      "id": "deleted",
      "title": "Operations the C++ class deletes",
      "match": [
        "explicitly deleted"
      ],
      "note": "A deleted copy constructor or assignment operator has no behavior to expose. Its absence from the C ABI is the same statement the C++ class already makes."
    },
    {
      "id": "iterators",
      "title": "C++ iterators and range-for support",
      "match": [
        "iterator",
        "range-for"
      ],
      "note": "An iterator has no fixed size, no stable representation and no C spelling. Every collection this ABI exposes is iterated as a count plus indexed access, or through an explicit enumerator handle where the canonical type has one."
    },
    {
      "id": "protected",
      "title": "Protected members with no derived class to hang them on",
      "match": [
        "protected"
      ],
      "note": "A protected member is mappable only when this ABI supplies a derived class that would override it. Where it does -- a game component's hooks -- the member is mapped; where the derived class is the caller's C++ code, there is nothing for C to override."
    },
    {
      "id": "templates",
      "title": "Templates over an arbitrary C++ type",
      "match": [
        "template over",
        "class template",
        "arbitrary C++ type",
        "naming an arbitrary",
        "compile-time trait",
        "constructs a C++ reader object",
        "implements a C++ interface",
        "platform substrate"
      ],
      "note": "C cannot name a C++ type, so a generic operation cannot be called from C at all. Where the canonical template has a small closed set of useful instantiations, the ABI names them one by one instead; the generic form itself stays unmapped."
    },
    {
      "id": "sharp-runtime",
      "title": "Values that are Sharp Runtime objects",
      "match": [
        "Sharp Runtime",
        "boxed value",
        "IDisposable",
        "serialization objects",
        "IServiceProvider"
      ],
      "note": "Sharp Runtime is an implementation dependency and never a C type. An operation whose value *is* such an object has nothing to hand across the boundary; see SHARP_RUNTIME_BOUNDARY.md."
    },
    {
      "id": "native-pointers",
      "title": "Raw native backend pointers",
      "match": [
        "SDL_",
        "native backend pointer",
        "native window handle"
      ],
      "note": "A route that accepts or returns an SDL pointer would make the caller's program depend on CNA's backend choice, which is the one thing this ABI exists to prevent."
    },
    {
      "id": "friendship",
      "title": "Friendship declarations",
      "match": [
        "friendship"
      ],
      "note": "A friend declaration grants a canonical CNA type access to another's internals. It declares no callable operation."
    },
    {
      "id": "unimplemented-facade",
      "title": "Declared and never defined in CNA itself",
      "match": [
        "declared and defi",
        "declared and never"
      ],
      "note": "Not a design choice: the canonical declaration has no definition anywhere in CNA, so there is nothing to bind. CApi_UnimplementedRuntimeFacade keeps that record from going stale in silence."
    },
    {
      "id": "lifetime-owned",
      "title": "Objects the caller never constructs or destroys",
      "match": [
        "created and destroyed with it",
        "the game creates and destroys",
        "function-local static",
        "every method that produces one is private",
        "exactly one",
        "delegate to",
        "the only collection that means anything",
        "owns no platform device"
      ],
      "note": "Where CNA owns the only instance and its lifetime, the constructor and destructor are not operations a caller can perform, so they are not routes."
    },
    {
      "id": "cpp-mechanics",
      "title": "C++ language mechanics with no runtime behavior",
      "match": [
        "virtual destructor",
        "base-from-member",
        "spelling of",
        "documentation-only marker",
        "movable values",
        "a value needs",
        "two flags",
        "cross-module signalling",
        "no second name to move from",
        "`std::function` alias",
        "an alias naming the platform's",
        "a second name for the same resource"
      ],
      "note": "Aliases, markers, move semantics and destructors that exist for the C++ type system. C holds every object by handle and never deletes through an interface."
    },
    {
      "id": "async-result",
      "title": "Asynchronous-result objects nothing produces",
      "match": [
        "asynchronous-result object",
        "GamerAction"
      ],
      "note": "The canonical asynchronous pair completes before its Begin returns, so the C route is a single call that invokes the callback. The result object itself is never handed out."
    },
    {
      "id": "unsafe-canonical",
      "title": "Canonical routes this ABI declines to bind",
      "match": [
        "constraint the canonical source states outright",
        "real limitation rather than an oversight",
        "finding rather than a decision"
      ],
      "note": "A small number of canonical operations are unsafe or meaningless when reached through a C handle. Binding them would hand a caller a way to corrupt memory or to observe an event that can never fire; each is recorded individually in COVERAGE.md."
    },
    {
      "id": "internal-entry-points-in-public-headers",
      "title": "`CNA::Internal` and `CNA::Graphics::detail` entry points declared in a public header",
      "match": [
        "a `CNA::Internal` entry point",
        "a `CNA::Graphics::detail` entry point"
      ],
      "note": "The inventory excludes `CNA::Internal` and `Detail` by path, because those namespaces are how CNA's own modules talk to each other rather than surface an application uses. A few such entry points are declared in a public header for the compiler's convenience and reach the inventory that way. `ConfigureModelMaterialVariantsEXT` is the example: it is how the glTF importer installs a model's variant bindings, not an operation an application performs. What an application does with variants -- read the declared names, select one -- is bound on `CNA_ModelHandle` (CBIND-051D). The engine layer adds the same shape under its own `detail` namespace: `requireCapability`, `nameOfCapability` and `reportShaderCompileFailure` are the choke points its subsystems call, and a C caller sees their effect as a refusal or a log line rather than calling them. The questions they answer are already reachable -- `cna_graphics_device_supports_capability` for the capability, and each shader route's own validity reporting for the compile."
    },
    {
      "id": "renderer-private-interfaces",
      "title": "Renderer-private C++ interfaces returned by a public accessor",
      "match": [
        "a renderer-private C++ interface"
      ],
      "note": "A few engine-layer resources expose the renderer object behind them through an `*EXT` accessor -- `StorageBuffer::getRendererEXT` is the example, returning `CNA::Internal::Renderers::IStorageBufferRenderer*`. That type is an abstract C++ interface with virtual dispatch whose shape is a contract between CNA and its renderers, not between CNA and an application. Handing a C caller that pointer would put a renderer-private vtable into the ABI, and every operation reachable through it is already reachable through the resource's own routes. This is the same argument the inventory's `Internal`/`Detail` path exclusion makes, applied to a return type rather than a declaration site."
    },
    {
      "id": "private-helpers-reported-public",
      "title": "Private implementation helpers the inventory reports as public",
      "match": [
        "a private implementation helper"
      ],
      "note": "`ContentManager` declares `private:` and then uses a `DEF_PROP` property macro, after which Doxygen reports the following members as public. `ResolveExistingAssetPath` is one: it is a private helper the loader calls to find which extension of an asset actually exists on disk, and CNA's visibility rules say a private member is not C API surface. Its neighbour `BuildAssetPath` is in the same private block and is currently recorded as implemented against `cna_content_manager_copy_asset_path`; that route really does call `BuildAssetPath`, so the mapping is not false, but the two rows disagree about whether a private member is bindable. Whoever revisits that row should settle it for both."
    }
  ],
  "environment": [
    {
      "subject": "FFmpeg stays a system dependency",
      "impact": "The package installs the SDL3, SDL3_image and SDL3_mixer libraries this project builds, beside `libcna_c_api.so`, whose RPATH is `$ORIGIN` -- so an installed CNA links and runs with no environment variable. FFmpeg is different: libavcodec, libavformat, libavutil and libswresample come from the distribution. Copying a distribution's binaries into this package would take on their redistribution terms, freeze their soname against future security updates and drag in the transitive libraries they were linked against, so they remain a system dependency a deployment installs the ordinary way.",
      "status": "by design",
      "owner": "CONSUMING.md"
    },
    {
      "subject": "The static archive keeps 83 C++ statics visible",
      "impact": "A static CNA exists and exports exactly the same 3,653 `cna_*` names the shared library does: the whole closure is partially linked into one object and every other global symbol is localized. What cannot be localized is the handful GCC emits as `STB_GNU_UNIQUE` -- function-local statics in inline and template code, whose uniqueness is what makes them correct. They are mangled C++ names, no C program can collide with them, and none is callable API. The build **fails** if a symbol of any other binding survives, so the exception cannot widen quietly.",
      "status": "by design",
      "owner": "CONSUMING.md"
    },
    {
      "subject": "One active CNA runtime per process",
      "impact": "A second `cna_game_create` while a game is alive returns `CNA_RESULT_INVALID_STATE`. This makes the interaction with CNA's process-level graphics and input state explicit; simultaneous runtimes are an ABI-semantic change requiring a reviewed design.",
      "status": "by design",
      "owner": "HANDLES.md"
    },
    {
      "subject": "Handles are thread-affine and never leave their process",
      "impact": "A handle used from a thread other than its creator's answers `CNA_RESULT_THREAD`. Handles are not pointers, not serializable and not stable across processes.",
      "status": "by design",
      "owner": "HANDLES.md"
    },
    {
      "subject": "The ABI is 0.x and experimental",
      "impact": "An incompatible change requires only a minor-version increment, release notes and a regenerated baseline. The additive-only guarantee begins at 1.0, which is a separate, later decision.",
      "status": "by design",
      "owner": "ABI_VERSIONING.md"
    },
    {
      "subject": "Four of the 49 renderers are built and run here",
      "impact": "The other 45 share the same C surface and the same capability queries. What a given backend supports is answered by `cna_graphics_device_supports_capability`, never by its renderer identity.",
      "status": "by design",
      "owner": "RENDERERS_AND_CAPABILITIES.md"
    },
    {
      "subject": "No verification tree can create a compiled Effect Framework effect",
      "impact": "`cna_effect_create_compiled`'s three refusals are renderer-independent and are tested, but none of the four trees this campaign builds -- HEADLESS, SDL_RENDERER and SOFTWARE twice -- advertises `CNA_GRAPHICS_CAPABILITY_COMPILED_EFFECTS`, so the accepting path is never taken. That leaves `cna_effect_get_is_compiled_ext` proved only on its `CNA_FALSE` branch, and clone-preserves-compiled-state asserted as a relationship between source and clone rather than on a genuinely compiled effect. A tree on a renderer that implements compiled effects would close both.",
      "status": "not tested here",
      "owner": "EFFECTS.md"
    }
  ],
  "partial_kinds": {
    "c-cannot-name-a-type": "C cannot name a C++ type",
    "sharp-runtime-object": "the value is a Sharp Runtime object",
    "type-erased-value": "the value is type-erased",
    "no-c-counterpart": "the canonical value is a proxy"
  }
}
