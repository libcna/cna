# Root-level build output directories only. This was once an unanchored 'build*', which matched
# ANY path whose basename began with "build" -- it silently swallowed
# tools/gltf_fixtures/builder.py, the fixture generator's core module, so the generator could not
# run on a clean checkout at all (GLTF-003/GLTF-011). Keep this anchored and directory-only.
/build*/
cmake-build-*/
# The same names without a trailing slash. A trailing-slash pattern matches only real
# directories, so this second form also covers symlinked build trees. Build trees once
# lived on an external disk reached by an in-repo symlink; that disk is gone and they
# now live in the repo (see /build*/ above).
cmake-build-*
.idea/*
cmake-build-debug/*
cmake-build-web/*
.junie/guide.md
cmake-build-vulkan/*
cmake-build-easygl/*
.sdl-prebuilt/
.sdl-prebuilt-*/
cmake-build-bgfx/*
cmake-build-sdl/*
cmake-build-webgpu/*
cmake-build-headless/*
cmake-build-software/*
cmake-build-stub/*
cmake-build-d3d11/*
cmake-build-d3d12/*
cmake-build-canvas/*
cmake-build-ascii/*
cmake-build-freedirect/*
cmake-build-d3d9/*
cmake-build-sdlgpu/*
cmake-build-dx1/*
cmake-build-opengles1/*
cmake-build-opengl4/*
cmake-build-opengl1/*
cmake-build-opengl2/*
vkd3d-proton.cache.write
vkd3d-proton.cache
vendor/wgpu-native/
cmake-build-input-easygl/*
cmake-build-input-vulkan/*
cmake-build-input-bgfx/*
cmake-build-input-sdlrenderer/*
cmake-build-input-asan/*
cmake-build-android/*
cmake-build-devices-asan/*
cmake-build-devices-tsan/*
cmake-build-devices-ubsan/*
cmake-build-net-asan/*
cmake-build-media-asan/*
cmake-build-windows/*
cmake-build-tests/*
cmake-build-ubsan/*
cmake-build-dx2/*
cmake-build-dx3/*
cmake-build-dx5/*
cmake-build-dx6/*
cmake-build-dx7/*
cmake-build-dx8/*
cmake-build-d3d10/*
spikes/dx10-spike/*.exe
spikes/dx10-spike/*.o
spikes/dx2-spike/*.exe
spikes/dx2-spike/*.o
spikes/dx3-spike/*.exe
spikes/dx3-spike/*.o
spikes/dx5-spike/*.exe
spikes/dx5-spike/*.o
spikes/dx6-spike/*.exe
spikes/dx6-spike/*.o
spikes/dx7-spike/*.exe
spikes/dx7-spike/*.o
spikes/dx8-spike/*.exe
spikes/dx8-spike/*.o
spikes/mingw-cnaext-spike/*.exe
spikes/mingw-cnaext-spike/*.o
__pycache__/
*.pyc

# Task 471: tools/fna-reference/ C# build output (bin/, obj/) and its generated JSON output --
# not source, all regenerated by `xbuild`/running the tool.
tools/fna-reference/bin/
tools/fna-reference/obj/

# DXVK shader/pipeline state cache, written next to wherever a D3D9/D3D11/D3D12 test .exe was
# invoked from (e.g. running a CTest binary directly from the repo root) -- runtime artifact, not
# source.
*.dxvk-cache



# CTest scratch (Testing/Temporary/LastTest.log, CTestCostData.txt), written into whatever
# directory ctest was invoked from -- including the repo root when a build dir is passed with -C
# or the invocation misses. Runtime artifact, not source.
Testing/

# plan_modern.md MOD-7: generated Doxygen output for the CNA::Graphics engine layer
# (`doxygen docs/Doxyfile.cnaext`, or the CNAEXT_DoxygenGroup ctest). Regenerated, never source.
build/doxygen-cnaext/

# plan_modern.md MOD-134: scratch configure trees and logs from
# scripts/check_renderer_configure_sweep.sh. Each tree is deleted as soon as its identity is
# checked; the logs are kept only until the next run.
build/renderer-sweep/
spikes/occlusion-count-spike/occlusion_count_probe
